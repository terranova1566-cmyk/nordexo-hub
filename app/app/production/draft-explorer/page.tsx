"use client";

import {
  Button,
  Card,
  Checkbox,
  Dialog,
  DialogActions,
  DialogBody,
  DialogSurface,
  DialogTitle,
  Field,
  Input,
  Popover,
  PopoverSurface,
  PopoverTrigger,
  Menu,
  MenuItem,
  MenuList,
  MenuPopover,
  MenuTrigger,
  Textarea,
  Spinner,
  Switch,
  Tab,
  TabList,
  Table,
  TableBody,
  TableCell,
  TableHeader,
  TableHeaderCell,
  TableRow,
  Text,
  mergeClasses,
  makeStyles,
  tokens,
} from "@fluentui/react-components";
import {
  Fragment,
  PointerEvent as ReactPointerEvent,
  useCallback,
  useEffect,
  useLayoutEffect,
  useMemo,
  useRef,
  useState,
} from "react";
import { useI18n } from "@/components/i18n-provider";
import { formatDate, formatDateTime } from "@/lib/format";

type DraftEntry = {
  name: string;
  path: string;
  type: "file" | "dir";
  size: number;
  modifiedAt: string;
  pixelQualityScore?: number | null;
  zimageUpscaled?: boolean;
  whiteSides?: number | null;
  borderWhiteDensity?: number | null;
  hasNonChineseText?: boolean | null;
  densityProxyKb?: number | null;
};

type DraftFolder = {
  name: string;
  path: string;
  modifiedAt: string;
  run_type?: "draft" | "re_edit" | "archive";
};

type DraftRunPreviewItem = {
  draft_spu: string;
  title: string;
  draft_main_image_url: string | null;
  preview_image_path?: string | null;
  preview_image_modified_at?: string | null;
};

type DraftFolderTreeNode = {
  name: string;
  path: string;
  modifiedAt: string;
  fileCount: number;
  children: DraftFolderTreeNode[];
};

type RunSpuOption = {
  name: string;
  path: string;
};

type RunSpuStatus = {
  hasPendingAi: boolean;
  hasDigi: boolean;
  hasTaggedImage: boolean;
  hasImageIssue: boolean;
  hasUnseenAiUpdate: boolean;
};

type DraftExplorerUrlState = {
  run: string;
  spu: string;
  path: string;
  tab: "spu" | "sku" | "";
  showAll: boolean;
  imageTab: ImageFolderTabValue | "";
  imagePath: string;
};

type DraftProductFlagNote = {
  note: string;
  createdAt: string;
  updatedAt: string;
  updatedBy?: {
    id?: string | null;
    email?: string | null;
  } | null;
};

type DraftDeletedImageUndoItem = {
  sourcePath: string;
  destinationPath: string;
};

type ExplorerContextMenuState = {
  entry: DraftEntry;
  image: boolean;
  x: number;
  y: number;
};

type AiEditProvider = "chatgpt" | "gemini" | "zimage";
type AiPromptMode =
  | "template"
  | "direct"
  | "white_background"
  | "auto_center_white"
  | "eraser"
  | "upscale";
type AiTemplatePreset =
  | "standard"
  | "digideal_main"
  | "digideal_main_dual"
  | "product_scene"
  | "product_collection";
type AiResolveDecision = "keep_original" | "replace_with_ai" | "keep_both";
type AiEditJobStatus = "queued" | "running";

type AiDualDialogState = {
  provider: Exclude<AiEditProvider, "zimage">;
  outputCount: number;
  productEntry: DraftEntry;
  guidanceEntry: DraftEntry;
};

type AiCollectionDialogState = {
  provider: Exclude<AiEditProvider, "zimage">;
  entries: DraftEntry[];
};

type PendingAiEditRecord = {
  id: string;
  originalPath: string;
  pendingPath: string;
  pendingPixelQualityScore?: number | null;
  provider: AiEditProvider;
  mode: AiPromptMode;
  prompt: string;
  status: "pending";
  createdAt: string;
  updatedAt: string;
};

type AiEditRuntimeJob = {
  provider: AiEditProvider;
  mode: AiPromptMode;
  status: AiEditJobStatus;
  startedAt: number;
};

type DraftSpuRow = {
  id: string;
  draft_spu: string;
  draft_title: string | null;
  draft_subtitle: string | null;
  draft_status: string | null;
  draft_source: string | null;
  draft_supplier_1688_url: string | null;
  draft_updated_at: string | null;
  draft_created_at: string | null;
  draft_description_html: string | null;
  draft_product_description_main_html: string | null;
  draft_mf_product_description_short_html: string | null;
  draft_mf_product_description_extended_html: string | null;
  draft_mf_product_short_title: string | null;
  draft_mf_product_long_title: string | null;
  draft_mf_product_subtitle: string | null;
  draft_mf_product_bullets_short: string | null;
  draft_mf_product_bullets: string | null;
  draft_mf_product_bullets_long: string | null;
  draft_mf_product_specs: string | null;
  draft_image_folder: string | null;
  draft_main_image_url: string | null;
  draft_image_urls: string[] | null;
  draft_variant_image_urls: string[] | null;
  draft_raw_row: Record<string, unknown> | null;
  image_count: number;
  variant_image_count: number;
  video_count: number;
  variant_count: number;
};

type DraftSkuRow = {
  id: string;
  draft_sku: string | null;
  draft_spu: string | null;
  draft_option1: string | null;
  draft_option2: string | null;
  draft_option3: string | null;
  draft_option4: string | null;
  draft_option_combined_zh: string | null;
  draft_price: number | string | null;
  draft_weight: number | string | null;
  draft_weight_unit: string | null;
  draft_option1_zh: string | null;
  draft_option2_zh: string | null;
  draft_option3_zh: string | null;
  draft_option4_zh: string | null;
  draft_compare_at_price: number | string | null;
  draft_cost: number | string | null;
  draft_barcode: string | null;
  draft_variant_image_url: string | null;
  draft_shipping_name_en: string | null;
  draft_short_title_zh: string | null;
  draft_shipping_name_zh: string | null;
  draft_shipping_class: string | null;
  draft_taxable: string | null;
  draft_tax_code: string | null;
  draft_hs_code: string | null;
  draft_country_of_origin: string | null;
  draft_category_code_fq: string | null;
  draft_category_code_ld: string | null;
  draft_supplier_name: string | null;
  draft_supplier_location: string | null;
  draft_b2b_dropship_price_se: number | string | null;
  draft_b2b_dropship_price_no: number | string | null;
  draft_b2b_dropship_price_dk: number | string | null;
  draft_b2b_dropship_price_fi: number | string | null;
  draft_purchase_price_cny: number | string | null;
  draft_status: string | null;
  draft_updated_at: string | null;
  draft_raw_row: Record<string, unknown> | null;
};

type EditingCell = {
  table: "spu" | "sku";
  id: string;
  field: string;
};

type DraftVariantEditorRow = {
  key: string;
  id: string | null;
  draft_spu: string;
  draft_sku: string;
  draft_option1: string;
  draft_option2: string;
  draft_option3: string;
  draft_option4: string;
  draft_option_combined_zh: string;
  draft_price: string;
  draft_weight: string;
  draft_weight_unit: string;
  draft_option1_zh: string;
  draft_option2_zh: string;
  draft_option3_zh: string;
  draft_option4_zh: string;
  draft_compare_at_price: string;
  draft_cost: string;
  draft_barcode: string;
  draft_variant_image_url: string;
  draft_shipping_name_en: string;
  draft_short_title_zh: string;
  draft_shipping_name_zh: string;
  draft_shipping_class: string;
  draft_taxable: string;
  draft_tax_code: string;
  draft_hs_code: string;
  draft_country_of_origin: string;
  draft_category_code_fq: string;
  draft_category_code_ld: string;
  draft_supplier_name: string;
  draft_supplier_location: string;
  draft_b2b_dropship_price_se: string;
  draft_b2b_dropship_price_no: string;
  draft_b2b_dropship_price_dk: string;
  draft_b2b_dropship_price_fi: string;
  draft_purchase_price_cny: string;
  variation_color_se: string;
  variation_size_se: string;
  variation_other_se: string;
  variation_amount_se: string;
  draft_raw_row: Record<string, unknown>;
};

type SpuJsonViewerState = {
  spu: string;
  content: string;
  sourceLabel: string;
  loading: boolean;
};

type DraftVariantEditorEditableField =
  | "draft_sku"
  | "variation_color_se"
  | "variation_size_se"
  | "variation_other_se"
  | "variation_amount_se"
  | "draft_option_combined_zh"
  | "draft_option1"
  | "draft_option2"
  | "draft_option3"
  | "draft_option4"
  | "draft_shipping_class"
  | "draft_price"
  | "draft_weight";

type DraftVariantEditorClearableField =
  | "variation_color_se"
  | "variation_size_se"
  | "variation_other_se"
  | "variation_amount_se";

type VariantEditorSortKey = "sku" | "color" | "size" | "order" | "amount";
type VariantEditorSortDirection = "asc" | "desc";
type ImageFolderTabValue =
  | "main"
  | "variants"
  | "sizecharts"
  | "ocr"
  | "others"
  | "downloaded";
type MainImageViewFilter = "all" | "var_only" | "size_only";
type ReverseSearchLocale = "sweden" | "us_global";

const SHIPPING_CLASS_OPTIONS = ["NOR", "BAT", "PBA", "LIQ"] as const;
const SHIPPING_CLASS_OPTION_SET = new Set<string>(SHIPPING_CLASS_OPTIONS);
const normalizeShippingClassCode = (value: unknown) => {
  const text = String(value ?? "").trim().toUpperCase();
  if (!text) return "";
  return SHIPPING_CLASS_OPTION_SET.has(text) ? text : "";
};

const VARIANT_EDITOR_HIDDEN_FIELD_KEYS = [
  "draft_option1_zh",
  "draft_option2_zh",
  "draft_option3_zh",
  "draft_option4_zh",
  "draft_compare_at_price",
  "draft_cost",
  "draft_barcode",
  "draft_shipping_name_en",
  "draft_short_title_zh",
  "draft_shipping_name_zh",
  "draft_taxable",
  "draft_tax_code",
  "draft_hs_code",
  "draft_country_of_origin",
  "draft_category_code_fq",
  "draft_category_code_ld",
  "draft_supplier_name",
  "draft_supplier_location",
  "draft_b2b_dropship_price_se",
  "draft_b2b_dropship_price_no",
  "draft_b2b_dropship_price_dk",
  "draft_b2b_dropship_price_fi",
  "draft_purchase_price_cny",
] as const satisfies readonly (keyof DraftVariantEditorRow)[];

const copyVariantEditorHiddenFields = (
  row: DraftVariantEditorRow,
  source: DraftVariantEditorRow
): DraftVariantEditorRow => {
  const next = { ...row };
  next.draft_raw_row = {
    ...(source.draft_raw_row ?? {}),
    ...(row.draft_raw_row ?? {}),
  };
  for (const key of VARIANT_EDITOR_HIDDEN_FIELD_KEYS) {
    next[key] = source[key];
  }
  return next;
};

type ReverseSearchResultItem = {
  rank: number;
  source: string | null;
  websiteName: string | null;
  title: string | null;
  link: string | null;
  thumbnail: string | null;
  image: string | null;
  sourceIcon: string | null;
  sourceDomain: string | null;
  exactMatches: unknown[] | null;
  serpapiExactMatchesLink: string | null;
  metadata: Record<string, unknown> | null;
  originalImage: string | null;
  originalWidth: number | null;
  originalHeight: number | null;
  domain: string | null;
  isAmazon: boolean;
  bucket: "visualMatches" | "imageResults" | "inlineImages";
};

type ReverseSearchJobState = {
  targetImagePath: string;
  targetImageName: string;
  searchImagePath: string;
  searchImageName: string;
  status: "running" | "ready" | "error";
  startedAt: string;
  finishedAt: string | null;
  searchId: string | null;
  items: ReverseSearchResultItem[];
  amazonLinks: string[];
  inputPayload: Record<string, unknown> | null;
  debugPayload: Record<string, unknown> | null;
  error: string | null;
};

const useStyles = makeStyles({
  page: {
    display: "flex",
    flexDirection: "column",
    gap: "20px",
  },
  header: {
    display: "flex",
    flexDirection: "column",
    gap: "6px",
  },
  uploadCard: {
    padding: "18px",
    borderRadius: "16px",
    backgroundColor: "#fafafa",
    display: "flex",
    flexDirection: "column",
    gap: "16px",
  },
  uploadRow: {
    display: "flex",
    flexWrap: "wrap",
    gap: "12px",
    alignItems: "flex-end",
  },
  fileInput: {
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    borderRadius: "8px",
    padding: "6px 8px",
    backgroundColor: tokens.colorNeutralBackground1,
    minWidth: "240px",
  },
  logCard: {
    padding: "16px",
    borderRadius: "16px",
    backgroundColor: "#fafafa",
  },
  explorerHeader: {
    display: "flex",
    flexWrap: "wrap",
    gap: "10px",
    alignItems: "center",
    justifyContent: "space-between",
  },
  explorerHeaderActions: {
    display: "inline-flex",
    alignItems: "center",
    gap: "8px",
  },
  draftHeader: {
    display: "flex",
    flexDirection: "column",
    gap: "6px",
  },
  draftSearch: {
    width: "280px",
    maxWidth: "100%",
  },
  draftToolbarRow: {
    display: "flex",
    flexWrap: "wrap",
    gap: "12px",
    alignItems: "center",
  },
  draftToolbarTabs: {
    flexShrink: 0,
  },
  draftTabLabel: {
    display: "inline-flex",
    alignItems: "center",
    gap: "6px",
  },
  draftTabDescription: {
    color: tokens.colorNeutralForeground3,
    fontSize: tokens.fontSizeBase100,
    lineHeight: 1,
  },
  draftSearchOffset: {
    marginLeft: "20px",
    flex: "1 1 160px",
    minWidth: "140px",
    maxWidth: "280px",
  },
  draftToolbarActions: {
    display: "flex",
    flexWrap: "wrap",
    gap: "8px",
    alignItems: "center",
    marginLeft: "auto",
  },
  draftToolbarActionButton: {
    backgroundColor: "#ffffff",
    ":hover": {
      backgroundColor: tokens.colorNeutralBackground2,
    },
    ":active": {
      backgroundColor: tokens.colorNeutralBackground3,
    },
    ":disabled": {
      backgroundColor: tokens.colorNeutralBackground3,
    },
  },
  draftToolbarActionButtonActive: {
    border: `1px solid ${tokens.colorBrandStroke1}`,
    color: tokens.colorBrandForeground1,
    backgroundColor: "#ffffff",
    ":hover": {
      backgroundColor: tokens.colorNeutralBackground2,
    },
  },
  draftToolbarActionButtonPrimaryActive: {
    border: `1px solid ${tokens.colorBrandBackground}`,
    color: "#ffffff",
    backgroundColor: tokens.colorBrandBackground,
    ":hover": {
      backgroundColor: tokens.colorBrandBackgroundHover,
      border: `1px solid ${tokens.colorBrandBackgroundHover}`,
      color: "#ffffff",
    },
    ":active": {
      backgroundColor: tokens.colorBrandBackgroundPressed,
      border: `1px solid ${tokens.colorBrandBackgroundPressed}`,
      color: "#ffffff",
    },
    ":disabled": {
      backgroundColor: tokens.colorNeutralBackground3,
      border: `1px solid ${tokens.colorNeutralStroke2}`,
      color: tokens.colorNeutralForegroundDisabled,
    },
  },
  draftToolbarActionButtonNeedsSku: {
    backgroundColor: "#fff4ce",
    border: "1px solid #f2c94c",
    ":hover": {
      backgroundColor: "#ffefb3",
    },
    ":active": {
      backgroundColor: "#ffe699",
    },
    ":disabled": {
      backgroundColor: "#fff4ce",
      opacity: 0.8,
    },
  },
  draftToolbarFlagButton: {
    minWidth: "34px",
    width: "34px",
    height: "34px",
    padding: 0,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
  },
  draftToolbarFlagIcon: {
    width: "17px",
    height: "17px",
    display: "block",
  },
  draftToolbarFlagIconHasNote: {
    color: "#d13438",
  },
  explorerControls: {
    display: "flex",
    flexWrap: "wrap",
    gap: "8px",
    alignItems: "center",
  },
  explorerControlsLeft: {
    display: "flex",
    flexWrap: "wrap",
    gap: "8px",
    alignItems: "center",
    flex: 1,
    minWidth: "280px",
  },
  explorerControlsCenter: {
    display: "flex",
    flexWrap: "wrap",
    gap: "8px",
    alignItems: "center",
  },
  explorerControlsRow: {
    display: "flex",
    flexWrap: "wrap",
    gap: "12px",
    alignItems: "center",
    justifyContent: "flex-start",
    marginTop: "10px",
  },
  explorerControlsRight: {
    display: "flex",
    flexWrap: "wrap",
    gap: "8px",
    alignItems: "center",
    justifyContent: "flex-end",
    marginLeft: "auto",
  },
  iconButton: {
    minWidth: "34px",
    width: "34px",
    height: "34px",
    padding: 0,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
  },
  explorerWhiteButton: {
    backgroundColor: "#ffffff",
    ":hover": {
      backgroundColor: tokens.colorNeutralBackground2,
    },
    ":active": {
      backgroundColor: tokens.colorNeutralBackground3,
    },
    ":disabled": {
      backgroundColor: tokens.colorNeutralBackground3,
    },
  },
  iconWithZipLabel: {
    display: "inline-flex",
    alignItems: "center",
    gap: "6px",
  },
  actionMenuTriggerButton: {
    minWidth: "auto",
    width: "auto",
    paddingLeft: "10px",
    paddingRight: "10px",
  },
  actionMenuTriggerContent: {
    display: "inline-flex",
    alignItems: "center",
    gap: "6px",
    whiteSpace: "nowrap",
  },
  actionMenuCaretIcon: {
    width: "16px",
    height: "16px",
    flexShrink: 0,
    opacity: 0.8,
    transition: "transform 140ms ease",
  },
  iconSvg: {
    width: "18px",
    height: "18px",
    flexShrink: 0,
  },
  imageResizeIconSvg: {
    width: "19px",
    height: "19px",
    flexShrink: 0,
  },
  batchPickerTrigger: {
    minWidth: "346px",
    maxWidth: "484px",
    justifyContent: "space-between",
    paddingLeft: "12px",
    paddingRight: "10px",
  },
  batchPickerTriggerLabel: {
    overflow: "hidden",
    whiteSpace: "nowrap",
    textOverflow: "ellipsis",
    maxWidth: "100%",
    textAlign: "left",
  },
  batchPickerChevron: {
    width: "16px",
    height: "16px",
    flexShrink: 0,
    marginLeft: "8px",
    opacity: 0.8,
  },
  batchPickerSurface: {
    padding: "6px",
    borderRadius: "12px",
    minWidth: "360px",
    maxWidth: "460px",
    maxHeight: "520px",
    overflow: "auto",
  },
  batchPickerRow: {
    display: "grid",
    gridTemplateColumns: "minmax(0, 1fr) auto auto",
    gap: "6px",
    alignItems: "center",
    padding: "4px 6px",
    borderRadius: "8px",
    cursor: "pointer",
    ":hover": {
      backgroundColor: tokens.colorNeutralBackground2,
    },
  },
  batchPickerRowActive: {
    backgroundColor: tokens.colorBrandBackground2,
  },
  batchPickerRowReEdit: {
    backgroundColor: "#fff8d8",
    border: "1px solid #f2dc98",
    ":hover": {
      backgroundColor: "#fff2bf",
    },
  },
  batchPickerRowReEditActive: {
    backgroundColor: "#ffefb3",
    border: "1px solid #e7c769",
  },
  batchPickerRowName: {
    overflow: "hidden",
    whiteSpace: "nowrap",
    textOverflow: "ellipsis",
    fontSize: tokens.fontSizeBase200,
    lineHeight: tokens.lineHeightBase200,
  },
  batchPickerViewButton: {
    minWidth: "48px",
    height: "26px",
    padding: "0 9px",
  },
  batchPickerActionButton: {
    minWidth: "48px",
    height: "26px",
    padding: "0 9px",
  },
  batchPickerArchiveDivider: {
    marginTop: "6px",
    paddingTop: "6px",
    borderTop: `1px solid ${tokens.colorNeutralStroke2}`,
  },
  batchPickerRowArchive: {
    gridTemplateColumns: "minmax(0, 1fr)",
  },
  batchPickerMergeCheckbox: {
    "& .fui-Checkbox__indicator": {
      backgroundColor: "#ffffff",
    },
    "& .fui-Checkbox__input:checked + .fui-Checkbox__indicator, & .fui-Checkbox__input:indeterminate + .fui-Checkbox__indicator, &[aria-checked='true'] .fui-Checkbox__indicator, &[aria-checked='mixed'] .fui-Checkbox__indicator":
      {
        backgroundColor: tokens.colorBrandBackground,
        color: "#ffffff",
      },
    "& .fui-Checkbox__input:checked + .fui-Checkbox__indicator svg, & .fui-Checkbox__input:indeterminate + .fui-Checkbox__indicator svg, &[aria-checked='true'] .fui-Checkbox__indicator svg, &[aria-checked='mixed'] .fui-Checkbox__indicator svg":
      {
        color: "#ffffff",
        fill: "#ffffff",
      },
    "& .fui-Checkbox__input:checked + .fui-Checkbox__indicator path, & .fui-Checkbox__input:indeterminate + .fui-Checkbox__indicator path, &[aria-checked='true'] .fui-Checkbox__indicator path, &[aria-checked='mixed'] .fui-Checkbox__indicator path":
      {
        stroke: "#ffffff",
        fill: "#ffffff",
      },
  },
  whiteActionButton: {
    backgroundColor: "#ffffff",
    ":hover": {
      backgroundColor: tokens.colorNeutralBackground2,
    },
    ":active": {
      backgroundColor: tokens.colorNeutralBackground3,
    },
    ":disabled": {
      backgroundColor: tokens.colorNeutralBackground3,
    },
  },
  batchPickerActions: {
    display: "flex",
    gap: "6px",
    paddingTop: "6px",
    marginTop: "6px",
    borderTop: `1px solid ${tokens.colorNeutralStroke2}`,
    flexWrap: "wrap",
  },
  spuPickerTrigger: {
    justifyContent: "space-between",
    paddingLeft: "12px",
    paddingRight: "10px",
  },
  spuPickerSurface: {
    padding: "6px",
    borderRadius: "12px",
  },
  spuPickerSurfaceScrollable: {
    maxHeight: "520px",
    overflowY: "auto",
  },
  spuPickerRow: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    gap: "4px",
    padding: "5px 8px",
    borderRadius: "8px",
    cursor: "pointer",
    ":hover": {
      backgroundColor: tokens.colorNeutralBackground2,
    },
  },
  spuPickerRowActive: {
    backgroundColor: tokens.colorBrandBackground2,
  },
  spuPickerRowCompleted: {
    backgroundColor: "#e7f5e8",
    ":hover": {
      backgroundColor: "#d9efdb",
    },
  },
  spuPickerRowText: {
    flex: "1 1 auto",
    display: "inline-flex",
    alignItems: "center",
    gap: "4px",
    minWidth: 0,
    overflow: "hidden",
  },
  spuPickerRowName: {
    flexShrink: 0,
    whiteSpace: "nowrap",
    fontSize: tokens.fontSizeBase200,
    lineHeight: tokens.lineHeightBase200,
  },
  spuPickerRowSeparator: {
    flexShrink: 0,
    color: tokens.colorNeutralForeground2,
    fontSize: tokens.fontSizeBase200,
    lineHeight: tokens.lineHeightBase200,
  },
  spuPickerRowTitleWord: {
    flexShrink: 1,
    whiteSpace: "nowrap",
    color: tokens.colorNeutralForeground1,
    fontSize: tokens.fontSizeBase200,
    lineHeight: tokens.lineHeightBase200,
  },
  spuPickerRowStatusIcon: {
    width: "16px",
    height: "16px",
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    flexShrink: 0,
  },
  spuPickerRowStatusIconComplete: {
    color: "#107c10",
  },
  spuPickerRowStatusIconIncomplete: {
    color: tokens.colorNeutralForeground3,
  },
  spuPickerRowIndicators: {
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "flex-end",
    gap: "4px",
    marginLeft: "auto",
    flexShrink: 0,
  },
  spuPickerRowIndicatorIcon: {
    width: "14px",
    height: "14px",
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    flexShrink: 0,
  },
  spuPickerIndicatorSvg: {
    width: "14px",
    height: "14px",
    display: "block",
  },
  spuPickerTagSvg: {
    width: "12px",
    height: "12px",
    display: "block",
  },
  spuPickerRowAiIcon: {
    color: "#a77ce6",
  },
  spuPickerRowAiNeedsReviewIcon: {
    color: "#dc6d78",
  },
  spuPickerRowImageIssueIcon: {
    color: "#dc6d78",
  },
  spuPickerRowDigiIconPresent: {
    color: "#107c10",
  },
  spuPickerRowDigiIconMissing: {
    color: "#dc6d78",
  },
  spuPickerRowTagIconPresent: {
    color: "#6610F2",
  },
  spuPickerRowTagIconMissing: {
    color: "#dc6d78",
  },
  runPreviewSurface: {
    width: "820px",
    maxWidth: "92vw",
  },
  runPreviewBody: {
    display: "flex",
    flexDirection: "column",
    gap: "12px",
    padding: "16px 18px 14px",
  },
  runPreviewTableWrap: {
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    borderRadius: "12px",
    overflow: "auto",
    maxHeight: "62vh",
  },
  runPreviewTable: {
    width: "100%",
    tableLayout: "fixed",
  },
  runPreviewHeaderRow: {
    backgroundColor: "#fafafa",
  },
  runPreviewImageHeaderCell: {
    width: "69px",
    paddingLeft: 0,
    paddingRight: 0,
    textAlign: "center",
  },
  runPreviewSpuHeaderCell: {
    width: "180px",
  },
  runPreviewImageCell: {
    paddingLeft: 0,
    paddingRight: 0,
  },
  runPreviewCellCenter: {
    width: "100%",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
  runPreviewSelectHeaderCell: {
    width: "54px",
    textAlign: "center",
    paddingLeft: 0,
    paddingRight: 0,
  },
  // Backwards-compatible aliases (older code referenced these names).
  runPreviewActionHeaderCell: {
    width: "54px",
    textAlign: "center",
    paddingLeft: 0,
    paddingRight: 0,
  },
  runPreviewActionHeaderInner: {
    width: "100%",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
  runPreviewSelectCell: {
    paddingLeft: 0,
    paddingRight: 0,
  },
  runPreviewDeleteHeaderCell: {
    width: "130px",
    textAlign: "center",
    paddingLeft: 0,
    paddingRight: 0,
  },
  runPreviewDeleteCell: {
    paddingLeft: 0,
    paddingRight: 0,
  },
  runPreviewTitleCell: {
    whiteSpace: "normal",
    wordBreak: "break-word",
  },
  runPreviewTitleText: {
    fontSize: tokens.fontSizeBase100,
    lineHeight: tokens.lineHeightBase200,
  },
  runPreviewThumb: {
    width: "46px",
    height: "46px",
    borderRadius: "8px",
    objectFit: "cover",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: tokens.colorNeutralBackground1,
  },
  runPreviewDeleteButton: {
    minWidth: "70px",
    height: "30px",
  },
  runPreviewActions: {
    display: "flex",
    justifyContent: "space-between",
    gap: "8px",
    alignItems: "center",
  },
  runPreviewActionsRight: {
    display: "flex",
    justifyContent: "flex-end",
    gap: "8px",
  },
  viewToggle: {
    display: "flex",
    gap: "6px",
    alignItems: "center",
  },
  explorerLayout: {
    marginTop: "12px",
    display: "grid",
    gridTemplateColumns: "290px minmax(0, 1fr)",
    gap: "12px",
    alignItems: "start",
    "@media (max-width: 1100px)": {
      gridTemplateColumns: "1fr",
    },
  },
  explorerLayoutSingleColumn: {
    gridTemplateColumns: "minmax(0, 1fr)",
  },
  folderPane: {
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    borderRadius: "12px",
    backgroundColor: "#ffffff",
    padding: "10px",
    display: "flex",
    flexDirection: "column",
    gap: "6px",
    maxHeight: "780px",
    overflow: "auto",
  },
  folderPaneHeader: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
  },
  folderPaneActions: {
    display: "flex",
    gap: "8px",
    marginTop: "6px",
    flexWrap: "wrap",
  },
  folderTreeRoot: {
    display: "flex",
    flexDirection: "column",
    gap: 0,
  },
  folderTreeChildren: {
    display: "flex",
    flexDirection: "column",
    gap: 0,
  },
  folderTreeRow: {
    display: "flex",
    alignItems: "center",
    gap: "2px",
    borderRadius: "8px",
    padding: "1px 2px",
    minHeight: "22px",
    cursor: "pointer",
    minWidth: 0,
    ":hover": {
      backgroundColor: tokens.colorNeutralBackground2,
    },
  },
  folderTreeRowActive: {
    backgroundColor: tokens.colorBrandBackground2,
  },
  folderTreeRowCompleted: {
    backgroundColor: "#e7f5e8",
    ":hover": {
      backgroundColor: "#d9efdb",
    },
  },
  folderTreeRowDrop: {
    outline: `1px solid ${tokens.colorBrandStroke1}`,
    boxShadow: `0 0 0 1px ${tokens.colorBrandStroke1} inset`,
    backgroundColor: tokens.colorBrandBackground2,
  },
  folderTreeConnector: {
    display: "inline-flex",
    alignItems: "center",
    flexShrink: 0,
    marginRight: "1px",
  },
  folderTreeConnectorSegment: {
    position: "relative",
    width: "10px",
    height: "16px",
    flexShrink: 0,
  },
  folderTreeConnectorLine: {
    position: "absolute",
    left: "5px",
    top: 0,
    bottom: 0,
    borderLeft: `1px solid ${tokens.colorNeutralStroke3}`,
  },
  folderTreeConnectorJoin: {
    position: "relative",
    width: "12px",
    height: "16px",
    flexShrink: 0,
  },
  folderTreeConnectorJoinVertical: {
    position: "absolute",
    left: "5px",
    borderLeft: `1px solid ${tokens.colorNeutralStroke3}`,
  },
  folderTreeConnectorJoinHorizontal: {
    position: "absolute",
    left: "5px",
    top: "8px",
    width: "8px",
    borderTop: `1px solid ${tokens.colorNeutralStroke3}`,
  },
  folderTreeCaretButton: {
    border: "none",
    background: "transparent",
    cursor: "pointer",
    color: tokens.colorNeutralForeground2,
    width: "16px",
    height: "16px",
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    padding: 0,
    flexShrink: 0,
    fontSize: "14px",
    fontWeight: tokens.fontWeightSemibold,
    lineHeight: "1",
  },
  folderTreeCaretSpacer: {
    width: "16px",
    height: "16px",
    flexShrink: 0,
  },
  folderTreeFolderIcon: {
    width: "14px",
    height: "14px",
    color: "#E7B325",
    flexShrink: 0,
    marginRight: "2px",
  },
  folderTreeName: {
    border: "none",
    background: "transparent",
    color: tokens.colorNeutralForeground1,
    textAlign: "left",
    padding: 0,
    cursor: "pointer",
    fontWeight: tokens.fontWeightSemibold,
    minWidth: 0,
    flex: 1,
    display: "inline-flex",
    alignItems: "center",
    gap: "2px",
  },
  folderTreeNameText: {
    minWidth: 0,
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
  },
  folderTreeCount: {
    color: tokens.colorNeutralForeground3,
    fontWeight: tokens.fontWeightRegular,
    marginLeft: "4px",
    flexShrink: 0,
  },
  contentColumn: {
    display: "flex",
    flexDirection: "column",
    gap: "12px",
    minWidth: 0,
  },
  filePane: {
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    borderRadius: "12px",
    backgroundColor: "#ffffff",
    padding: "10px",
    display: "flex",
    flexDirection: "column",
    gap: "10px",
  },
  filePaneReEdit: {
    backgroundColor: "#fffbe8",
    border: "1px solid #f2dc98",
  },
  entriesContentArea: {
    position: "relative",
    minHeight: "44px",
  },
  entriesContentAreaCompleted: {
    backgroundColor: "#e7f5e8",
    borderRadius: "10px",
    padding: "8px",
  },
  entriesContentOverlay: {
    position: "absolute",
    inset: 0,
    zIndex: 6,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "rgba(255,255,255,0.55)",
    borderRadius: "10px",
    pointerEvents: "all",
    cursor: "wait",
  },
  explorerPathLabel: {
    color: tokens.colorNeutralForeground3,
    fontSize: tokens.fontSizeBase100,
    marginBottom: "2px",
  },
  dualGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fill, minmax(228px, 1fr))",
    gap: "12px",
  },
  dualGridTagging: {
    filter: "blur(1.9px)",
    opacity: 0.86,
    pointerEvents: "none",
    userSelect: "none",
  },
  mediaCard: {
    borderRadius: "12px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: "#ffffff",
    padding: "8px",
    display: "flex",
    flexDirection: "column",
    gap: "8px",
    minWidth: 0,
  },
  mediaCardSelected: {
    border: `1px solid ${tokens.colorBrandStroke1}`,
    boxShadow: `0 0 0 1px ${tokens.colorBrandStroke1} inset`,
    backgroundColor: tokens.colorBrandBackground2,
  },
  mediaCardDropTarget: {
    border: `1px dashed ${tokens.colorBrandStroke1}`,
    boxShadow: `0 0 0 2px ${tokens.colorBrandStroke1} inset`,
  },
  mediaSquare: {
    width: "100%",
    aspectRatio: "1 / 1",
    borderRadius: "10px",
    backgroundColor: tokens.colorNeutralBackground2,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    overflow: "hidden",
    position: "relative",
    cursor: "pointer",
    "&:hover .thumbDownloadButton": {
      opacity: 0.8,
      transform: "translateY(0)",
      pointerEvents: "auto",
    },
    "&:focus-within .thumbDownloadButton": {
      opacity: 0.8,
      transform: "translateY(0)",
      pointerEvents: "auto",
    },
  },
  mediaTagStack: {
    position: "absolute",
    top: "8px",
    left: "8px",
    zIndex: 2,
    display: "inline-flex",
    flexDirection: "row",
    gap: "4px",
    pointerEvents: "none",
    alignItems: "center",
  },
  mediaTagStackShifted: {
    left: "42px",
  },
  mediaTagBadge: {
    borderRadius: "6px",
    padding: "2px 6px",
    fontSize: "10px",
    fontWeight: tokens.fontWeightSemibold,
    color: "#ffffff",
    letterSpacing: "0.02em",
    lineHeight: 1.2,
    opacity: 1,
    textTransform: "uppercase",
  },
  mediaTagMain: {
    backgroundColor: "#007BFF",
  },
  mediaTagEnv: {
    backgroundColor: "#6F42C1",
  },
  mediaTagVar: {
    backgroundColor: "#20C997",
  },
  mediaTagInf: {
    backgroundColor: "#FD7E14",
  },
  mediaTagDigi: {
    backgroundColor: "#E83E8C",
  },
  mediaTagSize: {
    backgroundColor: "#6610F2",
  },
  mediaImageBusy: {
    filter: "blur(2.4px)",
    transform: "scale(1.015)",
  },
  mediaImageReverseSearchRunning: {
    filter: "blur(2px)",
    transform: "scale(1.01)",
    opacity: 0.78,
  },
  reverseSearchRunningOverlay: {
    position: "absolute",
    inset: 0,
    zIndex: 3,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    pointerEvents: "none",
    backgroundColor: "rgba(255,255,255,0.06)",
  },
  reverseSearchRunningContent: {
    display: "inline-flex",
    alignItems: "center",
    gap: "6px",
    color: tokens.colorNeutralForeground2,
    fontSize: tokens.fontSizeBase100,
    fontWeight: tokens.fontWeightSemibold,
    textShadow: "0 1px 2px rgba(255,255,255,0.35)",
  },
  googleSearchReadyBadge: {
    position: "absolute",
    right: "8px",
    bottom: "8px",
    zIndex: 4,
    width: "28px",
    height: "28px",
    borderRadius: "999px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: "#ffffff",
    boxShadow: "0 2px 6px rgba(0, 0, 0, 0.16)",
    padding: 0,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    cursor: "pointer",
    transition: "transform 120ms ease, box-shadow 120ms ease",
    ":hover": {
      transform: "translateY(-1px)",
      boxShadow: "0 3px 8px rgba(0, 0, 0, 0.22)",
    },
    ":focus-visible": {
      outline: `2px solid ${tokens.colorStrokeFocus2}`,
      outlineOffset: "1px",
    },
  },
  googleSearchBadgeImage: {
    width: "14px",
    height: "14px",
    display: "block",
  },
  mediaBusyOverlay: {
    position: "absolute",
    inset: 0,
    zIndex: 3,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "transparent",
    pointerEvents: "none",
  },
  mediaBusyContent: {
    display: "inline-flex",
    flexDirection: "column",
    alignItems: "center",
    gap: "6px",
    color: tokens.colorBrandForeground1,
    fontSize: tokens.fontSizeBase100,
    fontWeight: tokens.fontWeightSemibold,
  },
  aiPendingBadge: {
    position: "absolute",
    top: "8px",
    left: "8px",
    borderRadius: "8px",
    backgroundColor: "#854aff",
    color: "#ffffff",
    width: "28px",
    height: "28px",
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    zIndex: 2,
    border: "1px solid #6f35e6",
    boxShadow: tokens.shadow4,
    padding: 0,
    cursor: "pointer",
    transition: "background-color 120ms ease, border-color 120ms ease",
    ":hover": {
      backgroundColor: "#9660ff",
      border: "1px solid #7a3ff0",
    },
    ":focus-visible": {
      outline: `2px solid ${tokens.colorStrokeFocus2}`,
      outlineOffset: "1px",
    },
  },
  mediaFooter: {
    display: "flex",
    alignItems: "center",
    gap: "8px",
    minWidth: 0,
  },
  mediaFooterColumn: {
    display: "flex",
    flexDirection: "column",
    minWidth: 0,
    flex: 1,
    gap: "2px",
  },
  mediaLabel: {
    border: "none",
    background: "transparent",
    textAlign: "left",
    color: tokens.colorNeutralForeground1,
    padding: 0,
    margin: 0,
    fontWeight: tokens.fontWeightSemibold,
    cursor: "text",
    minWidth: 0,
    flex: 1,
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
  },
  mediaMeta: {
    color: tokens.colorNeutralForeground3,
    fontSize: tokens.fontSizeBase100,
    lineHeight: tokens.lineHeightBase100,
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
  },
  mediaMetaDimLow: {
    color: "#8B1D1D",
    fontWeight: tokens.fontWeightSemibold,
  },
  mediaMetaDivider: {
    color: tokens.colorNeutralForeground4,
    marginLeft: "4px",
    marginRight: "4px",
  },
  mediaMetaQuality: {
    fontWeight: tokens.fontWeightSemibold,
  },
  mediaMetaQualityPendingInline: {
    display: "inline-flex",
    alignItems: "center",
    gap: "4px",
    verticalAlign: "middle",
  },
  mediaMetaQualityPendingSpinner: {
    transform: "scale(0.72)",
    transformOrigin: "center",
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
  },
  mediaMetaQualityUnknown: {
    color: tokens.colorNeutralForeground3,
  },
  mediaMetaQualityGood: {
    color: "#107C10",
  },
  mediaMetaQualityMedium: {
    color: "#B65A00",
  },
  mediaMetaQualityBad: {
    color: "#A4262C",
  },
  mediaMetaUpscaleIcon: {
    display: "inline-flex",
    alignItems: "center",
    color: "#0f6cbd",
    verticalAlign: "middle",
  },
  mediaMetaUpscaleIconSvg: {
    width: "14px",
    height: "14px",
    display: "block",
  },
  imageToolbar: {
    display: "flex",
    alignItems: "center",
    gap: "8px",
    flexWrap: "wrap",
  },
  imageToolbarTabs: {
    display: "inline-flex",
    alignItems: "center",
    gap: "6px",
    marginRight: "auto",
    flexWrap: "wrap",
  },
  imageTabLabel: {
    display: "inline-flex",
    alignItems: "center",
    gap: "6px",
  },
  imageTabMainBadgeSpacer: {
    display: "inline-block",
    width: "6px",
    flexShrink: 0,
  },
  imageTabBadge: {
    minWidth: "22px",
    height: "22px",
    borderRadius: "999px",
    padding: "0 7px",
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    fontSize: tokens.fontSizeBase100,
    fontWeight: tokens.fontWeightSemibold,
    lineHeight: 1,
    backgroundColor: "#0f6cbd",
    color: "#ffffff",
    boxSizing: "border-box",
  },
  imageTabBadgeDisabled: {
    backgroundColor: tokens.colorNeutralBackground3,
    color: tokens.colorNeutralForeground3,
  },
  imageToolbarRight: {
    display: "inline-flex",
    alignItems: "center",
    gap: "8px",
    flexWrap: "wrap",
    justifyContent: "flex-end",
  },
  imageToolbarActions: {
    marginRight: "2px",
  },
  dangerOutlineButton: {
    color: "#a4262c",
    border: "1px solid #d13438",
    ":hover": {
      color: "#a4262c",
      border: "1px solid #d13438",
      backgroundColor: "#fde7e9",
    },
    ":active": {
      color: "#a4262c",
      border: "1px solid #d13438",
      backgroundColor: "#f9d7da",
    },
    ":disabled": {
      color: tokens.colorNeutralForeground4,
      border: `1px solid ${tokens.colorNeutralStroke2}`,
      backgroundColor: tokens.colorNeutralBackground1,
    },
  },
  imageToolbarIconGroup: {
    display: "inline-flex",
    alignItems: "center",
    gap: "6px",
  },
  imageToggleIcon: {
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    color: tokens.colorNeutralForeground3,
  },
  fileActions: {
    display: "flex",
    alignItems: "center",
    gap: "6px",
    flexWrap: "nowrap",
    justifyContent: "flex-end",
    whiteSpace: "nowrap",
  },
  fileActionButton: {
    borderRadius: "8px",
    boxShadow: "none",
    minWidth: "auto",
    paddingLeft: "10px",
    paddingRight: "10px",
  },
  fileActionButtonNarrow: {
    paddingLeft: "8px",
    paddingRight: "8px",
  },
  textViewerSurface: {
    width: "min(1350px, 96vw)",
    maxHeight: "86vh",
    padding: "14px",
  },
  textViewerBody: {
    display: "flex",
    flexDirection: "column",
    gap: "10px",
    minHeight: "72vh",
  },
  textViewerArea: {
    flex: 1,
    minHeight: "60vh",
    fontFamily:
      '"Cascadia Mono", "Consolas", "SFMono-Regular", Menlo, Monaco, monospace',
    fontSize: tokens.fontSizeBase200,
    lineHeight: tokens.lineHeightBase300,
    "& textarea": {
      minHeight: "60vh",
      height: "60vh",
      fontFamily:
        '"Cascadia Mono", "Consolas", "SFMono-Regular", Menlo, Monaco, monospace',
      fontSize: tokens.fontSizeBase200,
      lineHeight: tokens.lineHeightBase300,
    },
  },
  textViewerActions: {
    justifyContent: "flex-end",
  },
  jsonViewerSurface: {
    width: "min(2200px, 99.5vw)",
    maxWidth: "99.5vw",
    height: "96vh",
    maxHeight: "96vh",
    padding: "14px",
  },
  jsonViewerBody: {
    display: "flex",
    flexDirection: "column",
    gap: "10px",
    height: "100%",
    minHeight: 0,
  },
  jsonViewerSource: {
    color: tokens.colorNeutralForeground3,
  },
  jsonViewerArea: {
    flex: 1,
    minHeight: 0,
    fontFamily:
      '"Cascadia Mono", "Consolas", "SFMono-Regular", Menlo, Monaco, monospace',
    fontSize: tokens.fontSizeBase200,
    lineHeight: tokens.lineHeightBase300,
    "& textarea": {
      minHeight: "78vh",
      height: "100%",
      fontFamily:
        '"Cascadia Mono", "Consolas", "SFMono-Regular", Menlo, Monaco, monospace',
      fontSize: tokens.fontSizeBase200,
      lineHeight: tokens.lineHeightBase300,
    },
  },
  supplierEditorSurface: {
    width: "min(560px, 92vw)",
    padding: "14px",
  },
  supplierEditorBody: {
    display: "flex",
    flexDirection: "column",
    gap: "10px",
  },
  productNoteDialogSurface: {
    width: "min(560px, 92vw)",
    padding: "14px",
  },
  productNoteDialogBody: {
    display: "flex",
    flexDirection: "column",
    gap: "10px",
  },
  productNoteCurrentWrap: {
    display: "flex",
    flexDirection: "column",
    gap: "6px",
  },
  productNoteCurrentText: {
    borderRadius: "8px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: tokens.colorNeutralBackground2,
    padding: "8px 10px",
    whiteSpace: "pre-wrap",
    wordBreak: "break-word",
    maxHeight: "150px",
    overflow: "auto",
  },
  productNoteTextarea: {
    "& textarea": {
      minHeight: "92px",
      height: "92px",
    },
  },
  reverseSearchSurface: {
    width: "min(990px, 92vw)",
    maxWidth: "92vw",
    padding: "14px",
  },
  reverseSearchBody: {
    display: "flex",
    flexDirection: "column",
    gap: "10px",
    minHeight: "420px",
    maxHeight: "82vh",
    overflow: "hidden",
  },
  reverseSearchHeaderRow: {
    display: "flex",
    alignItems: "flex-start",
    justifyContent: "space-between",
    gap: "10px",
    flexWrap: "wrap",
  },
  reverseSearchSourceBlock: {
    display: "inline-flex",
    alignItems: "center",
    gap: "10px",
    minWidth: 0,
    flex: "1 1 360px",
  },
  reverseSearchSourceFrame: {
    width: "250px",
    height: "250px",
    borderRadius: "8px",
    overflow: "hidden",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: tokens.colorNeutralBackground2,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    flexShrink: 0,
  },
  reverseSearchSourceImage: {
    width: "100%",
    height: "100%",
    objectFit: "cover",
    display: "block",
  },
  reverseSearchSourceMeta: {
    display: "flex",
    flexDirection: "column",
    gap: "2px",
    minWidth: 0,
  },
  reverseSearchSourceTitle: {
    color: tokens.colorNeutralForeground2,
    fontSize: tokens.fontSizeBase400,
    lineHeight: tokens.lineHeightBase400,
  },
  reverseSearchContent: {
    position: "relative",
    display: "flex",
    flexDirection: "column",
    flex: "1 1 auto",
    minHeight: "280px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    borderRadius: "10px",
    overflow: "hidden",
    backgroundColor: "#ffffff",
  },
  reverseSearchDebugBlock: {
    display: "grid",
    gridTemplateColumns: "repeat(2, minmax(0, 1fr))",
    gap: "8px",
    "@media (max-width: 900px)": {
      gridTemplateColumns: "1fr",
    },
  },
  reverseSearchDebugCard: {
    borderRadius: "8px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: tokens.colorNeutralBackground1,
    padding: "6px 8px",
    display: "flex",
    flexDirection: "column",
    gap: "4px",
    minHeight: "84px",
  },
  reverseSearchDebugText: {
    margin: 0,
    fontFamily:
      '"Cascadia Mono", "Consolas", "SFMono-Regular", Menlo, Monaco, monospace',
    fontSize: "11px",
    lineHeight: 1.35,
    color: tokens.colorNeutralForeground3,
    whiteSpace: "pre-wrap",
    wordBreak: "break-word",
    maxHeight: "128px",
    overflow: "auto",
  },
  reverseSearchTableWrap: {
    flex: "1 1 auto",
    minHeight: 0,
    overflow: "auto",
    maxHeight: "100%",
    paddingBottom: "6px",
  },
  reverseSearchTable: {
    width: "100%",
    tableLayout: "fixed",
  },
  reverseSearchImageColumn: {
    width: "106px",
  },
  reverseSearchImageCell: {
    verticalAlign: "top",
    paddingTop: "4px",
    paddingBottom: "4px",
    paddingLeft: "6px",
    paddingRight: "6px",
  },
  reverseSearchImageCellContent: {
    width: "100%",
    minHeight: "78px",
    paddingTop: "2px",
    paddingBottom: "2px",
    paddingLeft: "4px",
    paddingRight: "4px",
    display: "flex",
    alignItems: "flex-start",
    justifyContent: "center",
  },
  reverseSearchTitleColumn: {
    width: "50%",
  },
  reverseSearchLinkColumn: {
    width: "24%",
  },
  reverseSearchRowCellMiddle: {
    verticalAlign: "middle",
  },
  reverseSearchActionColumn: {
    width: "132px",
    textAlign: "right",
    whiteSpace: "nowrap",
  },
  reverseSearchLoadingOverlay: {
    position: "absolute",
    inset: 0,
    zIndex: 3,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "rgba(255,255,255,0.6)",
    backdropFilter: "blur(2px)",
  },
  reverseSearchBusyContent: {
    display: "inline-flex",
    flexDirection: "column",
    alignItems: "center",
    gap: "6px",
    color: tokens.colorBrandForeground1,
  },
  reverseSearchBlurred: {
    filter: "blur(2.2px)",
    pointerEvents: "none",
    userSelect: "none",
  },
  reverseSearchThumbWrap: {
    width: "78px",
    height: "78px",
    borderRadius: "8px",
    overflow: "hidden",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: tokens.colorNeutralBackground2,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    flexShrink: 0,
  },
  reverseSearchThumb: {
    width: "auto",
    height: "auto",
    maxWidth: "100%",
    maxHeight: "100%",
    objectFit: "contain",
  },
  reverseSearchTitleClamp: {
    display: "-webkit-box",
    WebkitLineClamp: 2,
    WebkitBoxOrient: "vertical",
    overflow: "hidden",
    textOverflow: "ellipsis",
    wordBreak: "break-word",
    lineHeight: "20px",
    maxHeight: "40px",
  },
  reverseSearchRowActionButton: {
    minWidth: "118px",
    backgroundColor: "#ffffff",
    ":hover": {
      backgroundColor: tokens.colorNeutralBackground2,
    },
    ":active": {
      backgroundColor: tokens.colorNeutralBackground3,
    },
  },
  reverseSearchLink: {
    color: tokens.colorBrandForegroundLink,
    textDecorationLine: "none",
    ":hover": {
      textDecorationLine: "underline",
    },
  },
  reverseSearchOpenLinkButton: {
    minWidth: "92px",
    backgroundColor: "#ffffff",
    ":hover": {
      backgroundColor: tokens.colorNeutralBackground2,
    },
    ":active": {
      backgroundColor: tokens.colorNeutralBackground3,
    },
  },
  reverseSearchLinkMeta: {
    marginTop: "4px",
    color: tokens.colorNeutralForeground3,
    fontSize: tokens.fontSizeBase200,
    lineHeight: tokens.lineHeightBase200,
  },
  reverseSearchSmallText: {
    color: tokens.colorNeutralForeground3,
    fontSize: tokens.fontSizeBase100,
  },
  reverseSearchActions: {
    justifyContent: "space-between",
  },
  photopeaSurface: {
    width: "min(1500px, 94vw)",
    height: "92vh",
    maxWidth: "94vw",
    maxHeight: "92vh",
    padding: "12px",
    overflow: "hidden",
  },
  photopeaBody: {
    display: "flex",
    flexDirection: "column",
    gap: "8px",
    height: "100%",
    minHeight: 0,
  },
  photopeaFrameWrap: {
    position: "relative",
    flex: 1,
    minHeight: 0,
    borderRadius: "10px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    overflow: "hidden",
    backgroundColor: tokens.colorNeutralBackground1,
  },
  photopeaFrame: {
    width: "100%",
    height: "100%",
    border: "none",
    display: "block",
  },
  autoLevelsSurface: {
    width: "min(980px, 92vw)",
    maxWidth: "92vw",
    maxHeight: "88vh",
    padding: "12px",
    overflow: "hidden",
  },
  autoLevelsBody: {
    display: "flex",
    flexDirection: "column",
    gap: "10px",
    minHeight: "460px",
    maxHeight: "84vh",
    overflow: "hidden",
  },
  autoLevelsControls: {
    display: "grid",
    gridTemplateColumns: "1fr 1fr",
    gap: "10px",
    alignItems: "start",
    "@media (max-width: 880px)": {
      gridTemplateColumns: "1fr",
    },
  },
  autoLevelsControlCard: {
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    borderRadius: "10px",
    backgroundColor: "#ffffff",
    padding: "10px",
    display: "flex",
    flexDirection: "column",
    gap: "8px",
  },
  autoLevelsSlider: {
    width: "100%",
  },
  autoLevelsInlineRow: {
    display: "inline-flex",
    alignItems: "center",
    gap: "8px",
    flexWrap: "wrap",
  },
  autoLevelsNumberInput: {
    width: "110px",
  },
  autoLevelsThumbWrap: {
    position: "relative",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    borderRadius: "10px",
    overflow: "hidden",
    backgroundColor: "#ffffff",
    minHeight: 0,
    flex: "1 1 auto",
    display: "flex",
    flexDirection: "column",
  },
  autoLevelsThumbGrid: {
    position: "relative",
    flex: "1 1 auto",
    minHeight: "220px",
    overflow: "auto",
    display: "grid",
    gridTemplateColumns: "repeat(auto-fill, minmax(140px, 1fr))",
    gap: "8px",
    padding: "10px",
    alignContent: "start",
  },
  autoLevelsThumbGridBusy: {
    filter: "blur(2.2px)",
    opacity: 0.72,
    pointerEvents: "none",
    userSelect: "none",
  },
  autoLevelsThumbCard: {
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    borderRadius: "8px",
    backgroundColor: tokens.colorNeutralBackground1,
    overflow: "hidden",
    display: "flex",
    flexDirection: "column",
    gap: "6px",
    padding: "6px",
  },
  autoLevelsThumbImageFrame: {
    width: "100%",
    aspectRatio: "1 / 1",
    borderRadius: "6px",
    overflow: "hidden",
    backgroundColor: tokens.colorNeutralBackground2,
  },
  autoLevelsThumbImage: {
    width: "100%",
    height: "100%",
    objectFit: "cover",
    display: "block",
  },
  autoLevelsThumbName: {
    fontSize: tokens.fontSizeBase100,
    lineHeight: tokens.lineHeightBase100,
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
  },
  autoLevelsBusyOverlay: {
    position: "absolute",
    inset: 0,
    zIndex: 2,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "rgba(255,255,255,0.34)",
    pointerEvents: "none",
  },
  stackOverlaySurface: {
    width: "min(900px, 92vw)",
    maxWidth: "92vw",
    maxHeight: "86vh",
    padding: "12px",
    overflow: "hidden",
  },
  stackOverlayBody: {
    display: "flex",
    flexDirection: "column",
    gap: "10px",
    minHeight: "380px",
    maxHeight: "82vh",
    overflow: "hidden",
  },
  stackOverlayThumbGrid: {
    position: "relative",
    flex: "1 1 auto",
    minHeight: "220px",
    overflow: "auto",
    display: "grid",
    gridTemplateColumns: "repeat(auto-fill, minmax(140px, 1fr))",
    gap: "8px",
    padding: "10px",
    alignContent: "start",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    borderRadius: "10px",
    backgroundColor: "#ffffff",
  },
  stackOverlayThumbCardButton: {
    border: "none",
    background: "transparent",
    padding: 0,
    textAlign: "left",
    cursor: "pointer",
    width: "100%",
  },
  stackOverlayThumbCardSelected: {
    boxShadow: `inset 0 0 0 2px ${tokens.colorBrandStroke1}`,
    backgroundColor: tokens.colorBrandBackground2,
  },
  stackOverlayTagLine: {
    display: "inline-flex",
    alignItems: "center",
    gap: "6px",
    fontSize: tokens.fontSizeBase100,
    color: tokens.colorNeutralForeground3,
  },
  stackOverlayTopMarker: {
    fontSize: tokens.fontSizeBase100,
    color: tokens.colorBrandForeground1,
    fontWeight: tokens.fontWeightSemibold,
  },
  filesSection: {
    borderRadius: "12px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: "#ffffff",
    padding: "10px",
    display: "flex",
    flexDirection: "column",
    gap: "8px",
    minWidth: 0,
  },
  filesHeader: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
  },
  filesTable: {
    tableLayout: "fixed",
    width: "100%",
    "& [role='cell'], & [role='columnheader']": {
      paddingTop: "4px",
      paddingBottom: "4px",
    },
  },
  filesColSelect: {
    width: "42px",
  },
  filesColName: {
    width: "44%",
  },
  filesColSize: {
    width: "14%",
  },
  filesColDate: {
    width: "22%",
  },
  filesColAction: {
    width: "20%",
  },
  filesInfo: {
    color: tokens.colorNeutralForeground3,
    fontSize: tokens.fontSizeBase100,
    whiteSpace: "nowrap",
    overflow: "hidden",
    textOverflow: "ellipsis",
  },
  filesNameButton: {
    fontWeight: tokens.fontWeightSemibold,
    color: tokens.colorNeutralForeground1,
    background: "transparent",
    border: "none",
    padding: 0,
    textAlign: "left",
    cursor: "text",
    width: "100%",
    minWidth: 0,
    whiteSpace: "normal",
    overflowWrap: "anywhere",
    lineHeight: 1.2,
  },
  contextMenu: {
    position: "fixed",
    // Needs to sit above Fluent Dialog layers.
    zIndex: 2000000,
    borderRadius: "10px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: tokens.colorNeutralBackground1,
    boxShadow: tokens.shadow16,
    padding: "4px",
    minWidth: "212px",
    display: "flex",
    flexDirection: "column",
    gap: "1px",
  },
  contextMenuButton: {
    border: "none",
    background: "transparent",
    color: tokens.colorNeutralForeground1,
    textAlign: "left",
    padding: "6px 9px",
    borderRadius: "6px",
    cursor: "pointer",
    fontSize: tokens.fontSizeBase200,
    lineHeight: 1.2,
    display: "flex",
    width: "100%",
    alignItems: "center",
    gap: "8px",
    ":hover": {
      backgroundColor: tokens.colorNeutralBackground2,
    },
    ":disabled": {
      color: tokens.colorNeutralForeground4,
      cursor: "not-allowed",
      backgroundColor: "transparent",
      opacity: 0.65,
    },
  },
  contextMenuButtonCaret: {
    marginLeft: "auto",
    color: tokens.colorNeutralForeground3,
    fontSize: tokens.fontSizeBase200,
    lineHeight: 1,
  },
  contextMenuSubmenuWrap: {
    position: "relative",
    width: "100%",
  },
  contextMenuSubmenu: {
    position: "absolute",
    left: "100%",
    top: 0,
    zIndex: 2000001,
    borderRadius: "10px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: tokens.colorNeutralBackground1,
    boxShadow: tokens.shadow16,
    padding: "4px",
    minWidth: "158px",
    display: "flex",
    flexDirection: "column",
    gap: "1px",
  },
  contextMenuSubmenuScrollable: {
    maxHeight: "320px",
    overflowY: "auto",
    minWidth: "280px",
  },
  contextMenuSubmenuLeft: {
    left: "auto",
    right: "100%",
  },
  contextMenuIcon: {
    width: "16px",
    height: "16px",
    flexShrink: 0,
    color: tokens.colorNeutralForeground3,
  },
  contextMenuTagCheckbox: {
    marginLeft: "auto",
    width: "16px",
    height: "16px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    borderRadius: "4px",
    backgroundColor: tokens.colorNeutralBackground1,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    flexShrink: 0,
    boxSizing: "border-box",
  },
  contextMenuTagCheckboxChecked: {
    border: "1px solid #0f6cbd",
    backgroundColor: "#0f6cbd",
    color: "#ffffff",
  },
  contextMenuTagCheckboxMixed: {
    border: "1px solid #0f6cbd",
    backgroundColor: "#deecff",
  },
  contextMenuTagCheckboxIcon: {
    width: "11px",
    height: "11px",
    display: "block",
  },
  contextMenuTagCheckboxBar: {
    width: "8px",
    height: "2px",
    borderRadius: "999px",
    backgroundColor: "#0f6cbd",
    display: "block",
  },
  contextMenuTagLabel: {
    flex: 1,
    lineHeight: 1,
  },
  contextMenuVariantLabelWrap: {
    display: "inline-flex",
    flexDirection: "column",
    alignItems: "flex-start",
    gap: "1px",
    minWidth: 0,
    flex: "1 1 auto",
  },
  contextMenuVariantPrimaryLabel: {
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
    maxWidth: "220px",
  },
  contextMenuVariantSecondaryLabel: {
    color: tokens.colorNeutralForeground3,
    fontSize: tokens.fontSizeBase100,
    lineHeight: 1.2,
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
    maxWidth: "220px",
  },
  contextMenuVariantSelectionWrap: {
    marginLeft: "auto",
    display: "inline-flex",
    alignItems: "center",
    gap: "6px",
    flexShrink: 0,
  },
  compactMenuList: {
    paddingTop: "2px",
    paddingBottom: "2px",
    "& [role='menuitem']": {
      minHeight: "26px",
      paddingTop: "5px",
      paddingBottom: "5px",
      lineHeight: "1.2",
    },
  },
  explorerTable: {
    marginTop: "8px",
    tableLayout: "fixed",
    width: "100%",
  },
  explorerColName: {
    width: "50%",
  },
  explorerColSize: {
    width: "10%",
  },
  explorerColModified: {
    width: "20%",
  },
  explorerColActions: {
    width: "20%",
  },
  explorerRow: {
    display: "flex",
    alignItems: "center",
    gap: "8px",
    minWidth: 0,
  },
  explorerIcon: {
    width: "16px",
    height: "16px",
    color: tokens.colorNeutralForeground3,
  },
  explorerName: {
    fontWeight: tokens.fontWeightSemibold,
    color: tokens.colorNeutralForeground1,
    background: "transparent",
    border: "none",
    padding: 0,
    textAlign: "left",
    cursor: "text",
    flex: 1,
    minWidth: 0,
    maxWidth: "100%",
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
  },
  renameInput: {
    flex: 1,
    minWidth: 0,
    maxWidth: "100%",
    width: "100%",
  },
  explorerMeta: {
    color: tokens.colorNeutralForeground3,
    fontSize: tokens.fontSizeBase100,
  },
  explorerPreview: {
    marginTop: "12px",
    borderRadius: "12px",
    backgroundColor: tokens.colorNeutralBackground2,
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    padding: "10px",
  },
  previewImage: {
    width: "100%",
    maxHeight: "360px",
    objectFit: "contain",
    borderRadius: "10px",
    backgroundColor: tokens.colorNeutralBackground1,
  },
  dropZone: {
    padding: "12px",
    borderRadius: "10px",
    border: `1px dashed ${tokens.colorNeutralStroke2}`,
    backgroundColor: "#ffffff",
    textAlign: "left",
    color: tokens.colorNeutralForeground3,
    display: "flex",
    flexDirection: "column",
    gap: "8px",
    minHeight: "126px",
    justifyContent: "space-between",
  },
  dropZoneActive: {
    border: `1px dashed ${tokens.colorBrandStroke1}`,
    backgroundColor: tokens.colorBrandBackground,
    color: tokens.colorBrandForeground1,
  },
  filesUploadRow: {
    marginTop: "10px",
    display: "grid",
    gridTemplateColumns: "minmax(0, 1fr)",
    gap: "10px",
    alignItems: "stretch",
  },
  uploadDropHint: {
    color: tokens.colorNeutralForeground3,
    fontSize: tokens.fontSizeBase200,
    lineHeight: tokens.lineHeightBase300,
  },
  uploadInputWrap: {
    display: "flex",
    justifyContent: "flex-start",
    "& input[type='file']": {
      fontSize: tokens.fontSizeBase200,
      lineHeight: tokens.lineHeightBase300,
      maxWidth: "100%",
    },
    "& input[type='file']::file-selector-button": {
      fontSize: tokens.fontSizeBase200,
      padding: "6px 10px",
      borderRadius: "8px",
      border: `1px solid ${tokens.colorNeutralStroke2}`,
      backgroundColor: tokens.colorNeutralBackground1,
      cursor: "pointer",
    },
  },
  uploadDropCenter: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    width: "100%",
  },
  uploadDropIcon: {
    width: "48px",
    height: "48px",
    color: "#e9e9e9",
  },
  urlUploadPanel: {
    borderRadius: "10px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: "#ffffff",
    padding: "12px",
    display: "flex",
    flexDirection: "column",
    gap: "8px",
    minHeight: "126px",
  },
  urlUploadInput: {
    width: "100%",
    "& textarea": {
      minHeight: "74px",
      resize: "vertical",
      fontSize: tokens.fontSizeBase200,
      lineHeight: tokens.lineHeightBase300,
    },
  },
  urlUploadActions: {
    display: "flex",
    justifyContent: "flex-end",
    marginTop: "auto",
  },
  addImagesDialogSurface: {
    width: "min(760px, 94vw)",
    maxWidth: "94vw",
    padding: "14px",
  },
  addImagesDialogBody: {
    display: "flex",
    flexDirection: "column",
    gap: "12px",
  },
  addImagesDialogSection: {
    borderRadius: "10px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: "#ffffff",
    padding: "12px",
    display: "flex",
    flexDirection: "column",
    gap: "8px",
  },
  addImagesDialogDropZone: {
    borderRadius: "10px",
    border: `1px dashed ${tokens.colorNeutralStroke2}`,
    backgroundColor: "#ffffff",
    color: tokens.colorNeutralForeground3,
    display: "flex",
    flexDirection: "column",
    gap: "8px",
    minHeight: "140px",
    padding: "12px",
    justifyContent: "space-between",
  },
  addImagesDialogDropZoneActive: {
    border: `1px dashed ${tokens.colorBrandStroke1}`,
    backgroundColor: tokens.colorBrandBackground2,
    color: tokens.colorBrandForeground1,
  },
  addImagesDialogFileList: {
    margin: 0,
    paddingLeft: "18px",
    maxHeight: "124px",
    overflowY: "auto",
    color: tokens.colorNeutralForeground2,
  },
  addImagesDialogFileItem: {
    lineHeight: tokens.lineHeightBase300,
    fontSize: tokens.fontSizeBase200,
    whiteSpace: "nowrap",
    textOverflow: "ellipsis",
    overflow: "hidden",
  },
  addImagesDialogActions: {
    justifyContent: "flex-end",
  },
  previewDialog: {
    width: "min(900px, 95vw)",
    maxWidth: "95vw",
    padding: "16px",
  },
  previewDialogBody: {
    display: "flex",
    flexDirection: "column",
    gap: "12px",
  },
  previewImageFrame: {
    position: "relative",
    width: "100%",
    borderRadius: "12px",
    backgroundColor: tokens.colorNeutralBackground1,
    overflow: "hidden",
  },
  previewImageLarge: {
    width: "100%",
    maxHeight: "78vh",
    objectFit: "contain",
    borderRadius: "12px",
    backgroundColor: tokens.colorNeutralBackground1,
  },
  previewNavButton: {
    position: "absolute",
    top: "50%",
    transform: "translateY(-50%)",
    width: "44px",
    height: "44px",
    borderRadius: "999px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: "#ffffff",
    color: tokens.colorBrandForeground1,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    cursor: "pointer",
    zIndex: 4,
    opacity: 0.7,
    boxShadow: tokens.shadow8,
    transition: "opacity 120ms ease, transform 120ms ease",
    ":hover": {
      opacity: 1,
    },
    ":disabled": {
      opacity: 0.35,
      cursor: "default",
    },
  },
  previewNavButtonLeft: {
    left: "12px",
  },
  previewNavButtonRight: {
    right: "12px",
  },
  previewNavIcon: {
    width: "22px",
    height: "22px",
    flexShrink: 0,
  },
  previewMetaRow: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    gap: "10px",
  },
  previewMetaText: {
    color: tokens.colorNeutralForeground3,
    fontSize: tokens.fontSizeBase100,
    lineHeight: tokens.lineHeightBase100,
    whiteSpace: "nowrap",
    overflow: "hidden",
    textOverflow: "ellipsis",
  },
  previewActions: {
    justifyContent: "flex-end",
    marginLeft: "auto",
  },
  thumbGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fill, minmax(208px, 1fr))",
    gap: "12px",
    marginTop: "12px",
  },
  thumbCard: {
    borderRadius: "12px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: tokens.colorNeutralBackground1,
    padding: "8px",
    display: "flex",
    flexDirection: "column",
    gap: "6px",
  },
  thumbImageWrap: {
    borderRadius: "10px",
    backgroundColor: tokens.colorNeutralBackground2,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    height: "182px",
    overflow: "hidden",
    position: "relative",
    cursor: "pointer",
    "&:hover .thumbDownloadButton": {
      opacity: 0.5,
      transform: "translateY(0)",
      pointerEvents: "auto",
    },
    "&:focus-within .thumbDownloadButton": {
      opacity: 0.5,
      transform: "translateY(0)",
      pointerEvents: "auto",
    },
  },
  thumbImage: {
    width: "100%",
    height: "100%",
    objectFit: "contain",
  },
  thumbIcon: {
    width: "46px",
    height: "46px",
    color: tokens.colorNeutralForeground3,
  },
  thumbDownloadButton: {
    position: "absolute",
    top: "8px",
    right: "8px",
    width: "30px",
    height: "30px",
    borderRadius: "999px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: tokens.colorNeutralBackground1,
    color: "#b7b7b7",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    padding: 0,
    cursor: "pointer",
    boxShadow: tokens.shadow4,
    opacity: 0,
    transform: "translateY(-2px)",
    transition:
      "opacity 120ms ease, transform 120ms ease, color 120ms ease, border-color 120ms ease, background-color 120ms ease",
    pointerEvents: "none",
    ":hover": {
      opacity: 1,
      color: tokens.colorBrandForeground1,
      border: `1px solid ${tokens.colorBrandStroke1}`,
      backgroundColor: "#ffffff",
    },
    ":focus-visible": {
      opacity: 1,
      color: tokens.colorBrandForeground1,
      border: `1px solid ${tokens.colorBrandStroke1}`,
      outline: `2px solid ${tokens.colorStrokeFocus2}`,
      outlineOffset: "1px",
    },
  },
  thumbAiButton: {
    right: "42px",
  },
  aiPromptSurface: {
    width: "min(620px, 92vw)",
    padding: "14px",
  },
  aiPromptBody: {
    display: "flex",
    flexDirection: "column",
    gap: "10px",
  },
  aiPromptImageWrap: {
    width: "100%",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    padding: "4px 0 8px",
  },
  aiPromptImagePreview: {
    width: "100%",
    maxWidth: "500px",
    maxHeight: "500px",
    objectFit: "contain",
    borderRadius: "10px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: tokens.colorNeutralBackground2,
  },
  aiPromptHistoryPopover: {
    width: "min(760px, 90vw)",
    maxWidth: "90vw",
  },
  aiPromptHistoryMenuList: {
    width: "100%",
    maxHeight: "320px",
    overflowY: "auto",
    "& [role='menuitem']": {
      alignItems: "flex-start",
      whiteSpace: "normal",
      lineHeight: "1.35",
      paddingTop: "8px",
      paddingBottom: "8px",
    },
  },
  aiPromptHistoryMenuItemText: {
    whiteSpace: "normal",
    wordBreak: "break-word",
    display: "block",
    maxWidth: "100%",
  },
  aiDualSurface: {
    width: "min(980px, 96vw)",
    padding: "14px",
  },
  aiDualBody: {
    display: "flex",
    flexDirection: "column",
    gap: "12px",
  },
  aiDualGrid: {
    display: "grid",
    gap: "12px",
    gridTemplateColumns: "repeat(2, minmax(0, 1fr))",
    "@media (max-width: 880px)": {
      gridTemplateColumns: "1fr",
    },
  },
  aiDualCard: {
    borderRadius: "12px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: tokens.colorNeutralBackground2,
    padding: "10px",
    display: "flex",
    flexDirection: "column",
    gap: "8px",
    cursor: "grab",
    userSelect: "none",
  },
  aiDualCardDropTarget: {
    boxShadow: `0 0 0 1px ${tokens.colorBrandStroke1} inset`,
  },
  aiDualCardDragging: {
    opacity: 0.6,
  },
  aiDualCardTitle: {
    fontWeight: tokens.fontWeightSemibold,
  },
  aiDualCardHint: {
    color: tokens.colorNeutralForeground3,
  },
  aiDualPreviewFrame: {
    width: "100%",
    aspectRatio: "1 / 1",
    minHeight: "220px",
    borderRadius: "10px",
    overflow: "hidden",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: "#ffffff",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  aiDualPreviewImage: {
    width: "100%",
    height: "100%",
    objectFit: "contain",
    backgroundColor: "#ffffff",
  },
  aiDualSwapRow: {
    display: "flex",
    justifyContent: "space-between",
    gap: "8px",
    alignItems: "center",
    flexWrap: "wrap",
  },
  aiCollectionSurface: {
    width: "min(980px, 96vw)",
    padding: "14px",
  },
  aiCollectionBody: {
    display: "flex",
    flexDirection: "column",
    gap: "12px",
  },
  aiCollectionPreviewBox: {
    borderRadius: "12px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: tokens.colorNeutralBackground2,
    padding: "12px",
  },
  aiCollectionGrid: {
    display: "grid",
    gap: "12px",
    gridTemplateColumns: "repeat(2, minmax(0, 1fr))",
  },
  aiCollectionCard: {
    borderRadius: "10px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: "#ffffff",
    padding: "8px",
    display: "flex",
    flexDirection: "column",
    gap: "6px",
  },
  aiCollectionCardTitle: {
    color: tokens.colorNeutralForeground2,
    fontWeight: tokens.fontWeightSemibold,
  },
  aiCollectionThumbFrame: {
    width: "100%",
    maxWidth: "300px",
    aspectRatio: "1 / 1",
    margin: "0 auto",
    borderRadius: "8px",
    overflow: "hidden",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: "#ffffff",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  aiCollectionThumbImage: {
    width: "100%",
    height: "100%",
    objectFit: "contain",
    backgroundColor: "#ffffff",
  },
  aiCollectionHint: {
    color: tokens.colorNeutralForeground3,
  },
  aiMaskEditor: {
    display: "flex",
    flexDirection: "column",
    gap: "10px",
  },
  aiMaskStage: {
    position: "relative",
    width: "100%",
    maxWidth: "500px",
    borderRadius: "10px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: tokens.colorNeutralBackground2,
    overflow: "hidden",
    marginInline: "auto",
  },
  aiMaskBaseImage: {
    display: "block",
    width: "100%",
    height: "auto",
    maxHeight: "500px",
    objectFit: "contain",
    userSelect: "none",
    pointerEvents: "none",
  },
  aiMaskCanvas: {
    position: "absolute",
    inset: 0,
    width: "100%",
    height: "100%",
    touchAction: "none",
    cursor: "none",
  },
  aiMaskCursor: {
    position: "absolute",
    pointerEvents: "none",
    borderRadius: "999px",
    border: "1px solid rgba(36, 116, 255, 0.95)",
    backgroundColor: "rgba(36, 116, 255, 0.5)",
    boxShadow: "0 0 0 1px rgba(255, 255, 255, 0.35)",
    zIndex: 4,
    transform: "translate(-50%, -50%)",
  },
  aiMaskToolbar: {
    display: "flex",
    flexDirection: "column",
    gap: "8px",
  },
  aiMaskBrushSlider: {
    width: "100%",
  },
  aiMaskHint: {
    color: tokens.colorNeutralForeground3,
  },
  aiCompareSurface: {
    width: "70vw",
    maxWidth: "70vw",
    height: "85vh",
    maxHeight: "85vh",
    padding: "14px",
  },
  aiCompareBody: {
    display: "flex",
    flexDirection: "column",
    gap: "10px",
    height: "100%",
    minHeight: 0,
    overflow: "hidden",
  },
  aiCompareGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(2, minmax(0, 1fr))",
    gap: "16px",
    flex: 1,
    minHeight: 0,
    overflow: "auto",
    alignItems: "start",
    "@media (max-width: 960px)": {
      gridTemplateColumns: "1fr",
    },
  },
  aiComparePanel: {
    borderRadius: "10px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: tokens.colorNeutralBackground2,
    padding: "8px",
    display: "flex",
    flexDirection: "column",
    gap: "8px",
    alignItems: "center",
  },
  aiCompareImageFrame: {
    width: "min(100%, calc(85vh - 260px))",
    aspectRatio: "1 / 1",
    borderRadius: "8px",
    backgroundColor: tokens.colorNeutralBackground1,
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    overflow: "hidden",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    "@media (max-width: 1200px)": {
      minHeight: "320px",
    },
  },
  aiCompareLabel: {
    fontWeight: tokens.fontWeightSemibold,
  },
  aiCompareImage: {
    width: "100%",
    height: "100%",
    objectFit: "contain",
  },
  thumbName: {
    fontSize: tokens.fontSizeBase200,
    fontWeight: tokens.fontWeightSemibold,
    color: tokens.colorNeutralForeground1,
    wordBreak: "break-word",
    display: "block",
    background: "transparent",
    border: "none",
    padding: 0,
    textAlign: "left",
    cursor: "text",
    width: "100%",
    maxWidth: "100%",
  },
  thumbMeta: {
    fontSize: tokens.fontSizeBase100,
    color: tokens.colorNeutralForeground3,
    display: "block",
    marginTop: "4px",
  },
  thumbActions: {
    display: "flex",
    gap: "6px",
    flexWrap: "wrap",
  },
  tableCard: {
    padding: "16px",
    borderRadius: "16px",
    backgroundColor: "#fafafa",
    display: "flex",
    flexDirection: "column",
    gap: "12px",
  },
  tableWrapper: {
    maxHeight: "420px",
    overflow: "auto",
    borderRadius: "12px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: "#ffffff",
  },
  contentSizedTable: {
    width: "max-content",
    minWidth: "100%",
    tableLayout: "fixed",
  },
  stickyHeader: {
    position: "sticky",
    top: 0,
    zIndex: 1,
    backgroundColor: tokens.colorNeutralBackground1,
  },
  tableRow: {
    backgroundColor: tokens.colorNeutralBackground1,
    ":hover": {
      backgroundColor: tokens.colorNeutralBackground2,
    },
  },
  tableRowAlt: {
    backgroundColor: tokens.colorNeutralBackground2,
  },
  tableRowCompleted: {
    backgroundColor: "#e7f5e8",
    ":hover": {
      backgroundColor: "#d9efdb",
    },
  },
  folderRow: {
    cursor: "pointer",
  },
  tableCell: {
    verticalAlign: "middle",
    overflow: "visible",
  },
  tableEditInputWrap: {
    position: "relative",
    width: "100%",
    overflow: "visible",
    minHeight: "28px",
  },
  tableEditInput: {
    minWidth: "100%",
    maxWidth: "none",
  },
  expandedInlineInput: {
    position: "relative",
    left: "50%",
    transform: "translateX(-50%)",
    zIndex: 6,
    boxSizing: "border-box",
  },
  tableCellLinkButton: {
    border: "none",
    background: "transparent",
    color: tokens.colorBrandForeground1,
    padding: 0,
    textAlign: "left",
    cursor: "pointer",
    font: "inherit",
    width: "100%",
    minWidth: 0,
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
  },
  tableCellLinkRow: {
    display: "flex",
    alignItems: "center",
    gap: "8px",
    minWidth: 0,
  },
  tableCellInlineEditable: {
    flex: "1 1 auto",
    minWidth: 0,
    overflow: "hidden",
  },
  tableCellLinkActionButton: {
    flexShrink: 0,
    width: "auto",
    minWidth: "76px",
    textAlign: "left",
  },
  tableActionButton: {
    backgroundColor: "#ffffff",
    ":hover": {
      backgroundColor: tokens.colorNeutralBackground2,
    },
    ":active": {
      backgroundColor: tokens.colorNeutralBackground3,
    },
    ":disabled": {
      backgroundColor: tokens.colorNeutralBackground3,
    },
  },
  variantEditButton: {
    width: "auto",
    minWidth: 0,
    maxWidth: "100%",
  },
  variantEditButtonCritical: {
    backgroundColor: "#fde9e9",
    ":hover": {
      backgroundColor: "#f9dbdb",
    },
    ":active": {
      backgroundColor: "#f4cece",
    },
    ":disabled": {
      backgroundColor: "#f4dede",
    },
  },
  variantEditButtonContent: {
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "flex-start",
    gap: "8px",
    minWidth: 0,
  },
  variantEditIssueIcons: {
    display: "inline-flex",
    alignItems: "center",
    gap: "4px",
    color: "#c65a5a",
    flexShrink: 0,
  },
  variantEditIssueIcon: {
    width: "12px",
    height: "12px",
    display: "inline-block",
    flexShrink: 0,
  },
  supplierSplitGroup: {
    display: "inline-flex",
    alignItems: "stretch",
    maxWidth: "100%",
  },
  supplierLinkButton: {
    borderTopRightRadius: 0,
    borderBottomRightRadius: 0,
    minHeight: "24px",
    height: "24px",
    maxHeight: "24px",
  },
  supplierMenuButton: {
    minWidth: "24px",
    width: "24px",
    minHeight: "24px",
    height: "24px",
    maxHeight: "24px",
    paddingLeft: 0,
    paddingRight: 0,
    borderTopLeftRadius: 0,
    borderBottomLeftRadius: 0,
    marginLeft: "-1px",
  },
  supplierMenuPopover: {
    padding: "2px",
  },
  supplierMenuList: {
    paddingTop: 0,
    paddingBottom: 0,
    minWidth: "104px",
    "& [role='menuitem']": {
      minHeight: "24px",
      paddingTop: "3px",
      paddingBottom: "3px",
      paddingLeft: "8px",
      paddingRight: "8px",
      fontSize: tokens.fontSizeBase200,
      lineHeight: tokens.lineHeightBase200,
    },
  },
  supplierMenuIcon: {
    width: "12px",
    height: "12px",
    flexShrink: 0,
    opacity: 0.85,
  },
  tableActionButtonCompleted: {
    backgroundColor: "#e7f5e8",
    ":hover": {
      backgroundColor: "#d9efdb",
    },
    ":active": {
      backgroundColor: "#cbe6cf",
    },
    ":disabled": {
      backgroundColor: "#d9efdb",
    },
  },
  tableActionButtonNeedsReview: {
    backgroundColor: "#fff4ce",
    border: "1px solid #f2c94c",
    ":hover": {
      backgroundColor: "#ffefb3",
    },
    ":active": {
      backgroundColor: "#ffe699",
    },
    ":disabled": {
      backgroundColor: "#fff4ce",
      opacity: 0.85,
    },
  },
  selectionCol: {
    width: "44px",
    maxWidth: "44px",
    paddingLeft: "6px",
    paddingRight: "6px",
  },
  selectionCheckbox: {
    backgroundColor: "transparent",
    borderRadius: 0,
    padding: 0,
    boxSizing: "border-box",
    "& .fui-Checkbox__indicator": {
      backgroundColor: "#ffffff",
      color: "transparent",
    },
    "& .fui-Checkbox__input:checked + .fui-Checkbox__indicator, & .fui-Checkbox__input:indeterminate + .fui-Checkbox__indicator, &[aria-checked='true'] .fui-Checkbox__indicator, &[aria-checked='mixed'] .fui-Checkbox__indicator":
      {
        backgroundColor: tokens.colorBrandBackground,
        color: "#ffffff",
      },
    "& .fui-Checkbox__input:checked:hover + .fui-Checkbox__indicator, & .fui-Checkbox__input:indeterminate:hover + .fui-Checkbox__indicator, &[aria-checked='true']:hover .fui-Checkbox__indicator, &[aria-checked='mixed']:hover .fui-Checkbox__indicator":
      {
        backgroundColor: tokens.colorBrandBackgroundHover,
      },
    "& .fui-Checkbox__input:checked:active + .fui-Checkbox__indicator, & .fui-Checkbox__input:indeterminate:active + .fui-Checkbox__indicator, &[aria-checked='true']:active .fui-Checkbox__indicator, &[aria-checked='mixed']:active .fui-Checkbox__indicator":
      {
        backgroundColor: tokens.colorBrandBackgroundPressed,
      },
    "& .fui-Checkbox__input:disabled + .fui-Checkbox__indicator, &[aria-disabled='true'] .fui-Checkbox__indicator":
      {
        backgroundColor: tokens.colorNeutralBackgroundDisabled,
        color: tokens.colorNeutralForegroundDisabled,
      },
    "& .fui-Checkbox__input:checked + .fui-Checkbox__indicator svg, & .fui-Checkbox__input:indeterminate + .fui-Checkbox__indicator svg, &[aria-checked='true'] .fui-Checkbox__indicator svg, &[aria-checked='mixed'] .fui-Checkbox__indicator svg":
      {
        color: "#ffffff",
        fill: "#ffffff",
      },
    "& .fui-Checkbox__input:checked + .fui-Checkbox__indicator path, & .fui-Checkbox__input:indeterminate + .fui-Checkbox__indicator path, &[aria-checked='true'] .fui-Checkbox__indicator path, &[aria-checked='mixed'] .fui-Checkbox__indicator path":
      {
        stroke: "#ffffff",
        fill: "#ffffff",
      },
    ":hover": {
      backgroundColor: "transparent",
    },
    ":active": {
      backgroundColor: "transparent",
    },
  },
  spuCol: {
    width: "140px",
    maxWidth: "140px",
  },
  statusCol: {
    width: "90px",
    maxWidth: "90px",
  },
  sourceCol: {
    width: "90px",
    maxWidth: "90px",
  },
  supplierCol: {
    width: "150px",
    maxWidth: "150px",
  },
  imagesCol: {
    width: "92px",
    maxWidth: "92px",
  },
  videosCol: {
    width: "70px",
    maxWidth: "70px",
  },
  variantsCol: {
    width: "120px",
    maxWidth: "120px",
  },
  updatedCol: {
    width: "110px",
    maxWidth: "110px",
  },
  createdCol: {
    width: "110px",
    maxWidth: "110px",
  },
  detailsCol: {
    width: "90px",
    maxWidth: "90px",
  },
  jsonCol: {
    width: "98px",
    maxWidth: "98px",
  },
  numericCell: {
    textAlign: "right",
  },
  clampTwo: {
    display: "-webkit-box",
    WebkitLineClamp: 2,
    WebkitBoxOrient: "vertical",
    overflow: "hidden",
  },
  clampOne: {
    display: "-webkit-box",
    WebkitLineClamp: 1,
    WebkitBoxOrient: "vertical",
    overflow: "hidden",
  },
  resizableHeader: {
    resize: "horizontal",
    overflow: "hidden",
  },
  detailsRow: {
    backgroundColor: tokens.colorNeutralBackground1,
  },
  detailsBlock: {
    display: "flex",
    flexDirection: "column",
    gap: "4px",
  },
  detailsGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))",
    gap: "12px",
    padding: "8px 0",
  },
  detailsDialogSurface: {
    width: "60vw",
    maxWidth: "60vw",
    maxHeight: "calc(100vh - 140px)",
    overflow: "hidden",
    display: "flex",
    flexDirection: "column",
  },
  detailsDialogBody: {
    display: "flex",
    flexDirection: "column",
    gap: "20px",
    height: "100%",
    minHeight: 0,
    overflow: "auto",
    paddingTop: "4px",
    paddingBottom: "28px",
    paddingRight: "16px",
  },
  detailsDialogContent: {
    display: "flex",
    flexDirection: "column",
    gap: "20px",
    alignItems: "stretch",
  },
  detailsDialogColumns: {
    display: "grid",
    gridTemplateColumns: "repeat(2, minmax(0, 1fr))",
    gap: "24px",
    "@media (max-width: 1100px)": {
      gridTemplateColumns: "1fr",
    },
  },
  detailsDialogColumn: {
    display: "flex",
    flexDirection: "column",
    gap: "16px",
  },
  detailsGallery: {
    display: "flex",
    flexDirection: "column",
    gap: "10px",
    padding: "12px",
    borderRadius: "12px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: tokens.colorNeutralBackground2,
  },
  detailsGalleryGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fill, minmax(120px, 1fr))",
    gap: "10px",
  },
  detailsGalleryImage: {
    width: "100%",
    height: "110px",
    objectFit: "cover",
    borderRadius: "10px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: tokens.colorNeutralBackground1,
  },
  detailsInstruction: {
    display: "flex",
    flexDirection: "column",
    gap: "6px",
  },
  detailsActionsRow: {
    display: "flex",
    gap: "8px",
    justifyContent: "flex-end",
    flexWrap: "wrap",
  },
  detailsCloseButton: {
    backgroundColor: "#ffffff",
    borderTop: `1px solid ${tokens.colorNeutralStroke1}`,
    borderRight: `1px solid ${tokens.colorNeutralStroke1}`,
    borderBottom: `1px solid ${tokens.colorNeutralStroke1}`,
    borderLeft: `1px solid ${tokens.colorNeutralStroke1}`,
    ":hover": {
      backgroundColor: tokens.colorNeutralBackground2,
    },
    ":active": {
      backgroundColor: tokens.colorNeutralBackground3,
    },
  },
  detailsInlineSpinner: {
    color: tokens.colorNeutralForeground3,
    transform: "scale(0.78)",
    transformOrigin: "center",
  },
  variantsEditorSurface: {
    width: "70vw",
    maxWidth: "70vw",
    minWidth: "980px",
    maxHeight: "92vh",
    padding: "14px",
    overflow: "hidden",
    "@media (max-width: 1200px)": {
      width: "96vw",
      maxWidth: "96vw",
      minWidth: 0,
    },
  },
  variantsEditorBody: {
    display: "flex",
    flexDirection: "column",
    gap: "10px",
    minHeight: "68vh",
    position: "relative",
  },
  variantsEditorContent: {
    display: "flex",
    flexDirection: "column",
    gap: "10px",
    minHeight: 0,
    flex: 1,
  },
  variantsEditorBusy: {
    filter: "blur(2.2px)",
    opacity: 0.75,
    pointerEvents: "none",
    userSelect: "none",
  },
  variantsEditorOverlay: {
    position: "absolute",
    inset: 0,
    zIndex: 4,
    backgroundColor: "rgba(255,255,255,0.18)",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  variantsEditorToolbar: {
    display: "flex",
    alignItems: "center",
    justifyContent: "flex-start",
    gap: "10px",
    flexWrap: "wrap",
  },
  variantsEditorToolbarLeft: {
    display: "inline-flex",
    alignItems: "center",
    gap: "8px",
    flexWrap: "wrap",
  },
  variantsEditorPacksInput: {
    width: "160px",
  },
  variantsEditorTableWrap: {
    borderRadius: "10px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    overflow: "auto",
    maxHeight: "48vh",
    minHeight: "260px",
    backgroundColor: tokens.colorNeutralBackground1,
  },
  variantsEditorTable: {
    width: "100%",
    borderCollapse: "collapse",
    tableLayout: "fixed",
  },
  variantsEditorHeadCell: {
    position: "sticky",
    top: 0,
    zIndex: 1,
    backgroundColor: tokens.colorNeutralBackground2,
    borderBottom: `1px solid ${tokens.colorNeutralStroke2}`,
    textAlign: "left",
    fontSize: tokens.fontSizeBase100,
    padding: "7px 6px",
    whiteSpace: "nowrap",
  },
  variantsEditorSortButton: {
    appearance: "none",
    border: "none",
    background: "transparent",
    padding: 0,
    margin: 0,
    width: "100%",
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "space-between",
    gap: "6px",
    color: "inherit",
    font: "inherit",
    cursor: "pointer",
    textAlign: "left",
  },
  variantsEditorSortButtonInline: {
    width: "auto",
    flex: "1 1 auto",
    minWidth: 0,
  },
  variantsEditorHeadCellActions: {
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "space-between",
    gap: "8px",
    width: "100%",
  },
  variantsEditorClearColumnLink: {
    appearance: "none",
    border: "none",
    backgroundColor: "transparent",
    padding: 0,
    margin: 0,
    color: tokens.colorNeutralForeground3,
    cursor: "pointer",
    fontSize: tokens.fontSizeBase100,
    lineHeight: tokens.lineHeightBase100,
    textDecoration: "underline",
    whiteSpace: "nowrap",
    ":hover": {
      color: tokens.colorStatusDangerForeground1,
    },
    ":disabled": {
      color: tokens.colorNeutralForegroundDisabled,
      cursor: "not-allowed",
      textDecoration: "none",
    },
  },
  variantsEditorSortIndicator: {
    color: tokens.colorNeutralForeground3,
    fontSize: tokens.fontSizeBase100,
    lineHeight: 1,
    minWidth: "10px",
    textAlign: "center",
  },
  variantsEditorCell: {
    borderBottom: `1px solid ${tokens.colorNeutralStroke2}`,
    padding: "6px",
    verticalAlign: "middle",
    overflow: "visible",
  },
  variantsEditorInputWrap: {
    position: "relative",
    width: "100%",
    overflow: "hidden",
    minHeight: "28px",
  },
  variantsEditorInput: {
    width: "100%",
    minWidth: 0,
    maxWidth: "100%",
    boxSizing: "border-box",
    "& input": {
      minWidth: 0,
      width: "100%",
      maxWidth: "100%",
      boxSizing: "border-box",
    },
  },
  variantsEditorSelect: {
    width: "100%",
    minWidth: 0,
    maxWidth: "100%",
    boxSizing: "border-box",
    height: "28px",
    borderRadius: tokens.borderRadiusMedium,
    borderTop: `1px solid ${tokens.colorNeutralStroke1}`,
    borderRight: `1px solid ${tokens.colorNeutralStroke1}`,
    borderBottom: `1px solid ${tokens.colorNeutralStroke1}`,
    borderLeft: `1px solid ${tokens.colorNeutralStroke1}`,
    backgroundColor: tokens.colorNeutralBackground1,
    color: tokens.colorNeutralForeground1,
    paddingTop: "0px",
    paddingRight: "24px",
    paddingBottom: "0px",
    paddingLeft: "8px",
    fontSize: tokens.fontSizeBase200,
    lineHeight: tokens.lineHeightBase200,
    outlineStyle: "none",
    cursor: "pointer",
    "&:focus": {
      borderTopColor: tokens.colorBrandStroke1,
      borderRightColor: tokens.colorBrandStroke1,
      borderBottomColor: tokens.colorBrandStroke1,
      borderLeftColor: tokens.colorBrandStroke1,
    },
  },
  variantsEditorCheckCol: {
    width: "44px",
    minWidth: "44px",
    maxWidth: "44px",
    paddingLeft: "4px",
    paddingRight: "4px",
  },
  variantsEditorInstruction: {
    display: "flex",
    flexDirection: "column",
    gap: "4px",
    justifyContent: "space-between",
  },
  variantsEditorInstructionActions: {
    display: "flex",
    justifyContent: "flex-end",
  },
  variantsEditorBottomSplit: {
    display: "grid",
    gridTemplateColumns: "1fr 1fr",
    gap: "10px",
    minHeight: "170px",
    alignItems: "stretch",
    "@media (max-width: 1100px)": {
      gridTemplateColumns: "1fr",
    },
  },
  variantsEditorThumbPanel: {
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    borderRadius: "10px",
    backgroundColor: tokens.colorNeutralBackground2,
    padding: "8px",
    display: "flex",
    flexDirection: "column",
    gap: "8px",
    minHeight: "170px",
    height: "100%",
  },
  variantsEditorThumbGrid: {
    display: "flex",
    flexDirection: "row",
    alignItems: "flex-start",
    gap: "8px",
    overflowX: "auto",
    overflowY: "hidden",
    paddingBottom: "6px",
    minHeight: "126px",
    flex: 1,
  },
  variantsEditorThumbCard: {
    display: "flex",
    flexDirection: "column",
    gap: "4px",
    width: "108px",
    minWidth: "108px",
    flex: "0 0 108px",
  },
  variantsEditorThumbImage: {
    width: "100%",
    height: "108px",
    aspectRatio: "1 / 1",
    objectFit: "cover",
    borderRadius: "8px",
    border: `1px solid ${tokens.colorNeutralStroke2}`,
    backgroundColor: tokens.colorNeutralBackground1,
  },
  variantsEditorThumbLabel: {
    color: tokens.colorNeutralForeground3,
    fontSize: tokens.fontSizeBase100,
    lineHeight: tokens.lineHeightBase100,
    overflow: "hidden",
    textOverflow: "ellipsis",
    display: "-webkit-box",
    WebkitLineClamp: "2",
    WebkitBoxOrient: "vertical",
  },
  variantsEditorThumbButton: {
    width: "100%",
    border: "none",
    background: "transparent",
    padding: 0,
    textAlign: "left",
    cursor: "pointer",
  },
  variantsEditorThumbImageClickable: {
    cursor: "pointer",
  },
  variantsImagePreviewSurface: {
    width: "min(820px, 92vw)",
    height: "min(820px, 92vw)",
    maxWidth: "92vw",
    maxHeight: "92vw",
    padding: "10px",
    display: "flex",
    flexDirection: "column",
    gap: "8px",
  },
  variantsImagePreviewBody: {
    display: "flex",
    flexDirection: "column",
    gap: "8px",
    height: "100%",
  },
  variantsImagePreviewTop: {
    display: "flex",
    alignItems: "center",
    justifyContent: "flex-end",
  },
  variantsImagePreviewImgWrap: {
    flex: 1,
    borderRadius: "12px",
    backgroundColor: tokens.colorNeutralBackground1,
    overflow: "hidden",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  variantsImagePreviewImg: {
    width: "100%",
    height: "100%",
    objectFit: "contain",
    display: "block",
  },
  variantsEditorActions: {
    justifyContent: "flex-end",
    marginTop: "auto",
  },
  link: {
    color: tokens.colorBrandForeground1,
    textDecoration: "none",
  },
});

const stripHtml = (value: string | null | undefined) => {
  if (!value) return "";
  return value.replace(/<[^>]*>/g, " ").replace(/\s+/g, " ").trim();
};

const getShortTitleLabel = (value: string | null | undefined) =>
  stripHtml(value).replace(/\s+/g, " ").trim();

const getRawValue = (raw: Record<string, unknown> | null | undefined, key: string) => {
  if (!raw || typeof raw !== "object") return "";
  const value = (raw as Record<string, unknown>)[key];
  return value == null ? "" : String(value);
};

const normalizeDraftRelativePath = (value: string) =>
  value.replace(/\\/g, "/").replace(/^\/+/, "").replace(/\.\.+/g, "");

const normalizeExplorerLocationPath = (value: string) =>
  normalizeDraftRelativePath(String(value || "").trim()).replace(/^\/+|\/+$/g, "");

const parseRunFromExplorerPath = (pathValue: string) => {
  const normalizedPath = normalizeExplorerLocationPath(pathValue);
  if (!normalizedPath) return "";
  const parts = normalizedPath.split("/").filter(Boolean);
  return String(parts[0] || "").trim();
};

const parseDraftRunTimestampMs = (value: string) => {
  const raw = String(value || "").trim();
  if (!raw) return null;

  const dateTimeMatch = raw.match(
    /(20\d{2})(\d{2})(\d{2})[-_]?([01]\d)([0-5]\d)([0-5]\d)(?!\d)/
  );
  if (dateTimeMatch) {
    const [, year, month, day, hour, minute, second] = dateTimeMatch;
    const timestamp = Date.UTC(
      Number(year),
      Number(month) - 1,
      Number(day),
      Number(hour),
      Number(minute),
      Number(second)
    );
    if (Number.isFinite(timestamp)) {
      return timestamp;
    }
  }

  const dateMatch = raw.match(/(20\d{2})(\d{2})(\d{2})(?!\d)/);
  if (dateMatch) {
    const [, year, month, day] = dateMatch;
    const timestamp = Date.UTC(Number(year), Number(month) - 1, Number(day), 0, 0, 0);
    if (Number.isFinite(timestamp)) {
      return timestamp;
    }
  }

  return null;
};

const getDraftFolderRecencyMs = (folder: Pick<DraftFolder, "path" | "name" | "modifiedAt">) => {
  const candidates = [folder.path, folder.name]
    .map((value) => parseDraftRunTimestampMs(value))
    .filter((value): value is number => typeof value === "number" && Number.isFinite(value));
  if (candidates.length > 0) {
    return Math.max(...candidates);
  }
  const modifiedAtMs = Date.parse(String(folder.modifiedAt || ""));
  if (Number.isFinite(modifiedAtMs)) {
    return modifiedAtMs;
  }
  return 0;
};

const normalizeDraftSpuCode = (value: string) =>
  String(value || "")
    .trim()
    .replace(/[^a-zA-Z0-9_-]/g, "")
    .toUpperCase();

const parseImageFolderTabValue = (value: string): ImageFolderTabValue | "" => {
  const normalized = String(value || "").trim().toLowerCase();
  if (
    normalized === "main" ||
    normalized === "variants" ||
    normalized === "sizecharts" ||
    normalized === "others" ||
    normalized === "ocr" ||
    normalized === "downloaded"
  ) {
    return normalized;
  }
  return "";
};

const toMainImageViewFilter = (imageTab: ImageFolderTabValue | ""): MainImageViewFilter => {
  if (imageTab === "variants") return "var_only";
  if (imageTab === "sizecharts") return "size_only";
  return "all";
};

const toImageTabFromMainViewFilter = (
  filter: MainImageViewFilter
): ImageFolderTabValue => {
  if (filter === "var_only") return "variants";
  if (filter === "size_only") return "sizecharts";
  return "main";
};

const parseExplorerStateParam = (
  searchParams: URLSearchParams,
  hashParams: URLSearchParams,
  keys: string[]
) => {
  for (const key of keys) {
    const value = String(searchParams.get(key) || "").trim();
    if (value) return value;
  }
  for (const key of keys) {
    const value = String(hashParams.get(key) || "").trim();
    if (value) return value;
  }
  return "";
};

const tryExtractDraftRelativePath = (value: string | null | undefined) => {
  const raw = String(value || "").trim();
  if (!raw) return null;
  let candidate = raw;
  if (/^https?:\/\//i.test(candidate)) {
    try {
      candidate = new URL(candidate).pathname || candidate;
    } catch {
      candidate = raw;
    }
  }
  candidate = candidate.replace(/\\/g, "/");
  const knownRoots = [
    "/srv/resources/media/images/draft_products/",
    "srv/resources/media/images/draft_products/",
    "/resources/media/images/draft_products/",
  ];
  for (const root of knownRoots) {
    const idx = candidate.indexOf(root);
    if (idx !== -1) {
      candidate = candidate.slice(idx + root.length);
      return normalizeDraftRelativePath(candidate);
    }
  }
  const marker = "images/draft_products/";
  const markerIndex = candidate.indexOf(marker);
  if (markerIndex !== -1) {
    candidate = candidate.slice(markerIndex + marker.length);
    return normalizeDraftRelativePath(candidate);
  }
  const genericMarker = "draft_products/";
  const genericIndex = candidate.indexOf(genericMarker);
  if (genericIndex !== -1) {
    candidate = candidate.slice(genericIndex + genericMarker.length);
    return normalizeDraftRelativePath(candidate);
  }
  const normalized = normalizeDraftRelativePath(candidate);
  if (!normalized || normalized.split("/").filter(Boolean).length < 1) return null;
  return normalized;
};

const parseDraftRawRow = (value: unknown): Record<string, unknown> => {
  if (value && typeof value === "object" && !Array.isArray(value)) {
    return value as Record<string, unknown>;
  }
  if (typeof value === "string") {
    try {
      const parsed = JSON.parse(value);
      if (parsed && typeof parsed === "object" && !Array.isArray(parsed)) {
        return parsed as Record<string, unknown>;
      }
    } catch {
      return {};
    }
  }
  return {};
};

const URL_EXTRACT_REGEX = /https?:\/\/[^\s<>"'`]+/gi;
const IMAGE_URL_EXTENSION_REGEX =
  /\.(?:avif|bmp|gif|ico|jpe?g|png|svg|tiff?|webp)(?:$|[?#])/i;
const IMAGE_FORMAT_VALUE_REGEX = /^(?:avif|bmp|gif|ico|jpe?g|png|svg|tiff?|webp)$/i;
const IMAGE_QUERY_PARAM_KEY_REGEX =
  /^(?:img|image|images|photo|photos|picture|thumbnail|thumb|icon|logo|avatar|banner|src|srcset|format|fmt|ext)$/i;
const EXTERNAL_SOURCE_IMAGE_FIELD_KEY_REGEX =
  /(?:^|[_\-.])(img|image|images|photo|photos|picture|thumbnail|thumb|icon|logo|avatar|banner)(?:$|[_\-.])/i;
const EXTERNAL_SOURCE_BLOCKED_HOST_REGEX_LIST = [
  /(?:^|\.)1688\.com$/i,
  /(?:^|\.)alicdn\.com$/i,
];

const normalizeSpuKey = (value: unknown) =>
  String(value ?? "")
    .trim()
    .toUpperCase();

const trimTrailingUrlPunctuation = (value: string) =>
  String(value || "")
    .trim()
    .replace(/[)\],.;!?'"`>]+$/g, "")
    .trim();

const maybeParseJsonString = (value: string): unknown => {
  const trimmed = String(value || "").trim();
  if (!trimmed) return null;
  const looksLikeJsonObject = trimmed.startsWith("{") && trimmed.endsWith("}");
  const looksLikeJsonArray = trimmed.startsWith("[") && trimmed.endsWith("]");
  if (!looksLikeJsonObject && !looksLikeJsonArray) return null;
  try {
    return JSON.parse(trimmed);
  } catch {
    return null;
  }
};

const extractUrlMatchesFromText = (value: string) => {
  const out: string[] = [];
  for (const match of String(value || "").matchAll(URL_EXTRACT_REGEX)) {
    const candidate = trimTrailingUrlPunctuation(String(match[0] || ""));
    if (candidate) out.push(candidate);
  }
  return out;
};

const normalizeUrlCandidate = (value: string) => {
  const trimmed = trimTrailingUrlPunctuation(value);
  if (!trimmed) return "";
  const withProtocol = /^[a-z][a-z0-9+\-.]*:\/\//i.test(trimmed)
    ? trimmed
    : `https://${trimmed}`;
  try {
    const parsed = new URL(withProtocol);
    if (!/^https?:$/i.test(parsed.protocol)) return "";
    return parsed.toString();
  } catch {
    return "";
  }
};

const isBlockedExternalSourceHost = (hostName: string) =>
  EXTERNAL_SOURCE_BLOCKED_HOST_REGEX_LIST.some((pattern) => pattern.test(hostName));

const isImageLikeUrl = (value: string) => {
  try {
    const parsed = new URL(value);
    if (IMAGE_URL_EXTENSION_REGEX.test(parsed.pathname)) return true;
    const params = parsed.searchParams;
    for (const [rawKey, rawValue] of params.entries()) {
      const key = String(rawKey || "").trim().toLowerCase();
      const valueTrimmed = String(rawValue || "").trim();
      if (!valueTrimmed) continue;
      if (IMAGE_URL_EXTENSION_REGEX.test(valueTrimmed)) return true;
      if (IMAGE_QUERY_PARAM_KEY_REGEX.test(key) && IMAGE_FORMAT_VALUE_REGEX.test(valueTrimmed)) {
        return true;
      }
      if (/^https?:\/\//i.test(valueTrimmed)) {
        try {
          const nested = new URL(valueTrimmed);
          if (IMAGE_URL_EXTENSION_REGEX.test(nested.pathname)) return true;
        } catch {
          // Ignore nested parse failures.
        }
      }
    }
    return false;
  } catch {
    return IMAGE_URL_EXTENSION_REGEX.test(value);
  }
};

const dedupeExternalSourceUrls = (value: string[]) => {
  const seen = new Set<string>();
  const out: string[] = [];
  value.forEach((entry) => {
    const normalized = normalizeUrlCandidate(entry);
    if (!normalized) return;
    const key = normalized.toLowerCase();
    if (seen.has(key)) return;
    seen.add(key);
    out.push(normalized);
  });
  return out;
};

const mergeExternalSourceUrls = (left: string[], right: string[]) =>
  dedupeExternalSourceUrls([...(left || []), ...(right || [])]);

const collectExternalSourceUrlCandidates = (
  value: unknown,
  output: string[],
  seenObjects = new WeakSet<object>()
) => {
  if (value == null) return;
  if (typeof value === "string") {
    extractUrlMatchesFromText(value).forEach((match) => output.push(match));
    const parsed = maybeParseJsonString(value);
    if (parsed && typeof parsed === "object") {
      collectExternalSourceUrlCandidates(parsed, output, seenObjects);
    }
    return;
  }
  if (Array.isArray(value)) {
    value.forEach((entry) => collectExternalSourceUrlCandidates(entry, output, seenObjects));
    return;
  }
  if (typeof value === "object") {
    if (seenObjects.has(value as object)) return;
    seenObjects.add(value as object);
    Object.entries(value as Record<string, unknown>).forEach(([key, entry]) => {
      if (EXTERNAL_SOURCE_IMAGE_FIELD_KEY_REGEX.test(String(key || ""))) return;
      collectExternalSourceUrlCandidates(entry, output, seenObjects);
    });
  }
};

const extractExternalSourceUrlsFromRaw = (
  rawValue: unknown,
  supplierValue: string | null | undefined
) => {
  const supplierNormalized = normalizeUrlCandidate(String(supplierValue || ""));
  const supplierComparable = supplierNormalized.toLowerCase();
  const candidates: string[] = [];
  collectExternalSourceUrlCandidates(rawValue, candidates);
  return dedupeExternalSourceUrls(candidates).filter((entry) => {
    if (!entry) return false;
    const normalized = normalizeUrlCandidate(entry);
    if (!normalized) return false;
    if (supplierComparable && normalized.toLowerCase() === supplierComparable) {
      return false;
    }
    if (isImageLikeUrl(normalized)) return false;
    try {
      const parsed = new URL(normalized);
      if (isBlockedExternalSourceHost(parsed.hostname.toLowerCase())) {
        return false;
      }
    } catch {
      return false;
    }
    return true;
  });
};

const toObjectRecordOrNull = (value: unknown): Record<string, unknown> | null => {
  if (!value || typeof value !== "object" || Array.isArray(value)) return null;
  return value as Record<string, unknown>;
};

const toText = (value: unknown) => (value == null ? "" : String(value));
const DRAFT_EXPLORER_REVIEW_KEY = "draft_explorer_review";
const VARIANTS_REVIEWED_AT_KEY = "variants_confirmed_at";
const DESCRIPTIONS_REVIEWED_AT_KEY = "descriptions_confirmed_at";
const DRAFT_EXPLORER_LAST_LOCATION_KEY = "draftExplorer.lastLocation.v1";
const AI_DIRECT_PROMPT_HISTORY_KEY = "draftExplorer.aiDirectPromptHistory.v1";
const AI_DIRECT_PROMPT_HISTORY_LIMIT = 5;
const AI_DIRECT_PROMPT_HISTORY_PREVIEW_WORDS = 60;
const AI_DIRECT_TEMPLATE_SIMPLE_SWEDISH_PROMPT = "Translate this text to Swedish.";
const AI_DIRECT_TEMPLATE_ADVANCED_SWEDEN_PROMPT = [
  "Localize and normalize this product text for Swedish e-commerce.",
  "Requirements:",
  "- Convert all measurements to Swedish metric units (cm, mm, g, kg, ml, l).",
  "- Convert inches/oz/lb/ft and remove imperial units from final text.",
  "- Translate all non-brand product text to natural Swedish.",
  "- Normalize wording for Swedish e-commerce readability and consistency.",
  "- Tone down aggressive sales language and remove over-elaborated claims.",
  "- Rewrite awkward direct-translation phrases so the text reads native Swedish.",
  "Output only the final Swedish text.",
].join("\n");

const parseDraftExplorerLastLocation = (
  value: unknown
): { run: string; path: string } | null => {
  if (!value || typeof value !== "object" || Array.isArray(value)) return null;
  const record = value as Record<string, unknown>;
  const run = normalizeExplorerLocationPath(String(record.run || ""));
  const path = normalizeExplorerLocationPath(String(record.path || ""));
  if (!run) return null;
  return { run, path };
};

const parseAiDirectPromptHistory = (value: unknown): string[] => {
  if (!Array.isArray(value)) return [];
  const deduped = new Set<string>();
  const result: string[] = [];
  for (const item of value) {
    const prompt = String(item || "").trim();
    if (!prompt || deduped.has(prompt)) continue;
    deduped.add(prompt);
    result.push(prompt);
    if (result.length >= AI_DIRECT_PROMPT_HISTORY_LIMIT) break;
  }
  return result;
};

const buildPromptHistoryPreview = (prompt: string) => {
  const words = String(prompt || "")
    .trim()
    .split(/\s+/)
    .filter(Boolean);
  if (words.length <= AI_DIRECT_PROMPT_HISTORY_PREVIEW_WORDS) {
    return words.join(" ");
  }
  return `${words.slice(0, AI_DIRECT_PROMPT_HISTORY_PREVIEW_WORDS).join(" ")}...`;
};

const sanitizeFileNameSegment = (value: string) =>
  String(value || "")
    .trim()
    .replace(/[^\w.-]+/g, "-")
    .replace(/-+/g, "-")
    .replace(/^[-._]+|[-._]+$/g, "") || "item";

const formatClipboardDateTime = (date = new Date()) => {
  const pad = (value: number) => String(value).padStart(2, "0");
  return `${date.getFullYear()}${pad(date.getMonth() + 1)}${pad(date.getDate())}-${pad(
    date.getHours()
  )}${pad(date.getMinutes())}${pad(date.getSeconds())}`;
};

const imageExtensionFromMime = (mimeType: string) => {
  const normalized = String(mimeType || "").toLowerCase();
  if (normalized.includes("png")) return "png";
  if (normalized.includes("webp")) return "webp";
  if (normalized.includes("gif")) return "gif";
  if (normalized.includes("bmp")) return "bmp";
  if (normalized.includes("avif")) return "avif";
  if (normalized.includes("tiff")) return "tiff";
  return "jpg";
};

const parseImageUrlsInput = (value: string) => {
  const tokens = value
    .split(/[\n,]+/)
    .map((token) => token.trim())
    .filter(Boolean);
  const seen = new Set<string>();
  const out: string[] = [];
  tokens.forEach((token) => {
    try {
      const parsed = new URL(token);
      if (!["http:", "https:"].includes(parsed.protocol)) return;
      const normalized = parsed.toString();
      if (seen.has(normalized)) return;
      seen.add(normalized);
      out.push(normalized);
    } catch {
      return;
    }
  });
  return out;
};

const splitFileNameAndExtension = (fileName: string) => {
  const name = String(fileName || "");
  const dotIndex = name.lastIndexOf(".");
  if (dotIndex <= 0) {
    return { baseName: name, extension: "" };
  }
  return {
    baseName: name.slice(0, dotIndex),
    extension: name.slice(dotIndex),
  };
};

const TAG_IMAGE_OPTIONS = ["MAIN", "ENV", "VAR", "INF", "DIGI"] as const;
const DISPLAY_IMAGE_TAG_OPTIONS = [...TAG_IMAGE_OPTIONS, "SIZE"] as const;
type ImageTagOption = (typeof DISPLAY_IMAGE_TAG_OPTIONS)[number];
const GOOGLE_SEARCH_BADGE_ICON_SRC = "/icons/google-logo.png";
const TAG_IMAGE_SUFFIXES_TO_STRIP = [
  ...DISPLAY_IMAGE_TAG_OPTIONS,
  "ENF",
  "TAG IMAGE",
] as const;
const IMAGE_TAG_SUFFIX_PAREN_REGEX =
  /\(\s*(MAIN|ENV|VAR|INF|ENF|DIGI|SIZE)\s*\)\s*$/i;
const IMAGE_TAG_SUFFIX_TOKEN_REGEX =
  /(?:[-_ ]+)(MAIN|ENV|VAR|INF|ENF|DIGI|SIZE)\s*$/i;

const escapeRegExp = (value: string) =>
  value.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");

const stripTrailingImageTagSuffixes = (baseName: string) => {
  let nextBase = baseName.trim();
  let changed = true;
  while (changed) {
    changed = false;
    TAG_IMAGE_SUFFIXES_TO_STRIP.forEach((tagValue) => {
      const parenPattern = new RegExp(
        `\\s*\\(${escapeRegExp(tagValue)}\\)\\s*$`,
        "i"
      );
      const tokenPattern = new RegExp(
        `(?:[-_ ]+${escapeRegExp(tagValue).replace(/\\ /g, "[-_ ]+")})\\s*$`,
        "i"
      );
      const strippedParen = nextBase.replace(parenPattern, "").trim();
      if (strippedParen !== nextBase) {
        nextBase = strippedParen;
        changed = true;
        return;
      }
      const strippedToken = nextBase.replace(tokenPattern, "").trim();
      if (strippedToken !== nextBase) {
        nextBase = strippedToken;
        changed = true;
      }
    });
  }
  return nextBase;
};

const normalizeImageTags = (tags: ImageTagOption[]) =>
  DISPLAY_IMAGE_TAG_OPTIONS.filter((option) => tags.includes(option));

const buildTaggedImageFileNameFromTags = (
  fileName: string,
  tags: ImageTagOption[]
) => {
  const { baseName, extension } = splitFileNameAndExtension(fileName);
  const cleanedBase = stripTrailingImageTagSuffixes(baseName);
  const orderedTags = normalizeImageTags(tags);
  const suffix = orderedTags.map((tag) => ` (${tag})`).join("");
  return `${cleanedBase}${suffix}${extension}`;
};

const buildTaggedImageFileName = (fileName: string, tag: ImageTagOption) => {
  return buildTaggedImageFileNameFromTags(fileName, [tag]);
};

const extractImageTagsFromFileName = (fileName: string): ImageTagOption[] => {
  const ordered: ImageTagOption[] = [];
  const { baseName } = splitFileNameAndExtension(String(fileName || ""));
  let working = String(baseName || "").trim();

  const appendNormalizedTag = (rawTag: string) => {
    const normalizedRaw = String(rawTag || "").trim().toUpperCase();
    const normalized = normalizedRaw === "ENF" ? "INF" : normalizedRaw;
    if (
      (normalized === "MAIN" ||
        normalized === "ENV" ||
        normalized === "VAR" ||
        normalized === "INF" ||
        normalized === "DIGI" ||
        normalized === "SIZE") &&
      !ordered.includes(normalized as ImageTagOption)
    ) {
      ordered.push(normalized as ImageTagOption);
    }
  };

  // Only treat explicit trailing suffixes as tags.
  // This prevents words like "...-main-..." inside normal filenames from becoming MAIN tags.
  while (working) {
    const parenMatch = working.match(IMAGE_TAG_SUFFIX_PAREN_REGEX);
    if (parenMatch) {
      appendNormalizedTag(String(parenMatch[1] || ""));
      working = working.slice(0, parenMatch.index ?? working.length).trim();
      continue;
    }

    const tokenMatch = working.match(IMAGE_TAG_SUFFIX_TOKEN_REGEX);
    if (tokenMatch) {
      appendNormalizedTag(String(tokenMatch[1] || ""));
      working = working.slice(0, working.length - tokenMatch[0].length).trim();
      continue;
    }

    break;
  }

  return normalizeImageTags(ordered);
};

const isSizeChartImageName = (fileName: string) => {
  const source = String(fileName || "");
  if (!source) return false;
  if (extractImageTagsFromFileName(source).includes("SIZE")) return true;
  const normalized = source.toLowerCase();
  if (normalized.includes("sizechart")) return true;
  if (/size[\s_-]*chart/.test(normalized)) return true;
  return /size-(?:se|no|en)\b/.test(normalized);
};

const getWhiteSignalsFromEntry = (entry: DraftEntry) => {
  const whiteSidesRaw = Number(entry.whiteSides);
  const whiteSides = Number.isFinite(whiteSidesRaw) ? whiteSidesRaw : 0;
  const borderWhiteDensityRaw = Number(entry.borderWhiteDensity);
  const borderWhiteDensity = Number.isFinite(borderWhiteDensityRaw)
    ? borderWhiteDensityRaw
    : 0;
  return { whiteSides, borderWhiteDensity };
};

const isWhiteBackgroundPriorityImage = (entry: DraftEntry) => {
  const { whiteSides, borderWhiteDensity } = getWhiteSignalsFromEntry(entry);
  return whiteSides >= 4 || borderWhiteDensity >= 0.97;
};

const isWhiteBorderPriorityImage = (entry: DraftEntry) => {
  const { whiteSides, borderWhiteDensity } = getWhiteSignalsFromEntry(entry);
  const whiteBorderBySides = whiteSides >= 2 && whiteSides < 4;
  const whiteBorderByDensity = borderWhiteDensity >= 0.88 && borderWhiteDensity < 0.97;
  return whiteBorderBySides || whiteBorderByDensity;
};

const getTagGroupSortRank = (tags: ImageTagOption[]) => {
  // Tagged section order:
  // INF/ENF -> ENV -> DIGI -> VAR -> SIZE
  if (tags.includes("INF")) return 1;
  if (tags.includes("ENV")) return 2;
  if (tags.includes("DIGI")) return 3;
  if (tags.includes("VAR")) return 4;
  if (tags.includes("SIZE")) return 5;
  return 0;
};

const getImagePrimarySortRank = (
  entry: DraftEntry,
  resolveTags: (entry: DraftEntry) => ImageTagOption[]
) => {
  const tags = resolveTags(entry);
  if (tags.includes("MAIN")) return 0;
  // White-priority is only for untagged images after MAIN.
  if (tags.length === 0) {
    if (isWhiteBackgroundPriorityImage(entry)) return 1;
    if (isWhiteBorderPriorityImage(entry)) return 2;
    return 3;
  }
  // Tagged images come after all untagged buckets.
  return 4;
};

const buildTaggedImageOrderPaths = (
  imageEntries: DraftEntry[],
  options?: { resolveTags?: (entry: DraftEntry) => ImageTagOption[] }
) => {
  const resolveTags = options?.resolveTags ?? ((entry: DraftEntry) => extractImageTagsFromFileName(entry.name));
  const originalOrder = new Map<string, number>();
  imageEntries.forEach((entry, index) => {
    originalOrder.set(entry.path, index);
  });

  return [...imageEntries]
    .sort((left, right) => {
      const leftPrimaryRank = getImagePrimarySortRank(left, resolveTags);
      const rightPrimaryRank = getImagePrimarySortRank(right, resolveTags);
      const rankDiff = leftPrimaryRank - rightPrimaryRank;
      if (rankDiff !== 0) return rankDiff;

      if (leftPrimaryRank === 4 && rightPrimaryRank === 4) {
        const leftTagRank = getTagGroupSortRank(resolveTags(left));
        const rightTagRank = getTagGroupSortRank(resolveTags(right));
        if (leftTagRank !== rightTagRank) {
          return leftTagRank - rightTagRank;
        }
      }

      const leftWhiteSides = Number.isFinite(Number(left.whiteSides))
        ? Number(left.whiteSides)
        : 0;
      const rightWhiteSides = Number.isFinite(Number(right.whiteSides))
        ? Number(right.whiteSides)
        : 0;
      if (leftWhiteSides !== rightWhiteSides) {
        return rightWhiteSides - leftWhiteSides;
      }

      const leftBorderDensity = Number.isFinite(Number(left.borderWhiteDensity))
        ? Number(left.borderWhiteDensity)
        : 0;
      const rightBorderDensity = Number.isFinite(Number(right.borderWhiteDensity))
        ? Number(right.borderWhiteDensity)
        : 0;
      if (leftBorderDensity !== rightBorderDensity) {
        return rightBorderDensity - leftBorderDensity;
      }

      const leftQualityScore = Number.isFinite(Number(left.pixelQualityScore))
        ? Number(left.pixelQualityScore)
        : -1;
      const rightQualityScore = Number.isFinite(Number(right.pixelQualityScore))
        ? Number(right.pixelQualityScore)
        : -1;
      if (leftQualityScore !== rightQualityScore) {
        return rightQualityScore - leftQualityScore;
      }

      const leftIndex = originalOrder.get(left.path) ?? 0;
      const rightIndex = originalOrder.get(right.path) ?? 0;
      if (leftIndex !== rightIndex) return leftIndex - rightIndex;

      return left.name.localeCompare(right.name);
    })
    .map((entry) => entry.path);
};

const isDigiTaggedImageName = (fileName: string) =>
  extractImageTagsFromFileName(fileName).includes("DIGI");

const toggleImageTagInFileName = (fileName: string, tag: ImageTagOption) => {
  const currentTags = extractImageTagsFromFileName(fileName);
  const nextTags = currentTags.includes(tag)
    ? currentTags.filter((value) => value !== tag)
    : [...currentTags, tag];
  return buildTaggedImageFileNameFromTags(fileName, nextTags);
};

const extractFileNameFromPath = (pathValue: string) => {
  const tail = String(pathValue || "").split("/").filter(Boolean).pop() || "";
  try {
    return decodeURIComponent(tail);
  } catch {
    return tail;
  }
};

const AMAZON_ASIN_PATTERN_LIST = [
  /\/dp\/([A-Z0-9]{10})(?:[/?#]|$)/i,
  /\/gp\/product\/([A-Z0-9]{10})(?:[/?#]|$)/i,
  /\/product\/([A-Z0-9]{10})(?:[/?#]|$)/i,
  /[?&]asin=([A-Z0-9]{10})(?:[&#]|$)/i,
];

const normalizeAsinValue = (value: unknown) => {
  const normalized = String(value || "")
    .trim()
    .toUpperCase();
  return /^[A-Z0-9]{10}$/.test(normalized) ? normalized : "";
};

const extractAmazonAsinFromUrl = (value: string | null | undefined) => {
  const link = String(value || "").trim();
  if (!link) return "";
  for (const pattern of AMAZON_ASIN_PATTERN_LIST) {
    const match = link.match(pattern);
    const asin = normalizeAsinValue(match?.[1] || "");
    if (asin) return asin;
  }
  return "";
};

const extractAmazonAsin = (item: ReverseSearchResultItem) => {
  const metadata = item.metadata || null;
  if (metadata) {
    const metadataAsinKeys = [
      "asin",
      "ASIN",
      "parent_asin",
      "parentAsin",
      "sourceAsin",
      "productAsin",
    ];
    for (const key of metadataAsinKeys) {
      const asin = normalizeAsinValue(metadata[key]);
      if (asin) return asin;
    }
  }
  return extractAmazonAsinFromUrl(item.link);
};

const getParentFolderPathFromEntryPath = (pathValue: string) => {
  const parts = String(pathValue || "").split("/").filter(Boolean);
  if (parts.length <= 1) return "";
  parts.pop();
  return parts.join("/");
};

const buildStableImageCardKey = (entry: DraftEntry) => {
  const fullPath = String(entry.path || "").trim();
  const parts = fullPath.split("/").filter(Boolean);
  const fileNameFromPath = parts.pop() || String(entry.name || "").trim();
  const directory = parts.join("/");
  const { baseName, extension } = splitFileNameAndExtension(fileNameFromPath);
  const stripped = stripTrailingImageTagSuffixes(baseName);
  return `${directory}/${stripped}${extension}`;
};

const stripSkuPackSuffix = (value: string) =>
  String(value || "")
    .trim()
    .replace(/(?:[-_ ]?\d+\s*(?:pack|p))$/i, "")
    .replace(/[-_ ]+$/, "");

const extractSkuPackMultiplier = (value: string): number | null => {
  const match = String(value || "")
    .trim()
    .match(/(?:[-_ ]?)(\d+)\s*(?:pack|p)$/i);
  if (!match) return null;
  const parsed = Number(match[1]);
  if (!Number.isFinite(parsed) || parsed <= 0) return null;
  return parsed;
};

const parsePackLikeMultiplier = (value: string): number | null => {
  const text = String(value || "").trim();
  if (!text) return null;
  const exactNumeric = text.match(/^(\d+(?:[.,]\d+)?)$/);
  const packNumeric = text.match(/^(\d+(?:[.,]\d+)?)\s*(?:pack|p)$/i);
  const source = exactNumeric?.[1] ?? packNumeric?.[1] ?? "";
  if (!source) return null;
  const parsed = Number(source.replace(",", "."));
  if (!Number.isFinite(parsed) || parsed <= 0) return null;
  return parsed;
};

const formatScaledVariantNumeric = (value: number) => {
  if (!Number.isFinite(value)) return "";
  const rounded = Number(value.toFixed(6));
  const asText = rounded.toFixed(6).replace(/\.?0+$/, "");
  return asText === "-0" ? "0" : asText;
};

const buildVariantCombinedZhValue = (row: {
  draft_option1?: string;
  draft_option2?: string;
  draft_option3?: string;
  draft_option4?: string;
  fallback?: string;
}) => {
  const combined = [
    String(row.draft_option1 || "").trim(),
    String(row.draft_option2 || "").trim(),
    String(row.draft_option3 || "").trim(),
    String(row.draft_option4 || "").trim(),
  ]
    .filter(Boolean)
    .join(" / ");
  return combined || String(row.fallback || "").trim();
};

const extractVariantImageFileName = (value: string | null | undefined) => {
  const raw = String(value || "").trim();
  if (!raw) return "";
  if (/^https?:\/\//i.test(raw)) {
    try {
      const parsed = new URL(raw);
      const pathQuery = parsed.searchParams.get("path");
      if (pathQuery) {
        return extractFileNameFromPath(pathQuery);
      }
      return extractFileNameFromPath(parsed.pathname);
    } catch {
      // Fall back to generic parsing below.
    }
  }
  return extractFileNameFromPath(raw);
};

const normalizeVariantImageFileNameKey = (value: string | null | undefined) =>
  String(value || "")
    .trim()
    .toLowerCase();

const buildVariantSwedishReadableLabel = (row: DraftSkuRow) => {
  const raw = parseDraftRawRow(row.draft_raw_row);
  const swedishParts = [
    toText(raw.variation_color_se).trim(),
    toText(raw.variation_size_se).trim(),
    toText(raw.variation_other_se).trim(),
    toText(raw.variation_amount_se).trim(),
  ].filter(Boolean);
  if (swedishParts.length > 0) {
    return swedishParts.join(" / ");
  }
  const zhCombined = buildVariantCombinedZhValue({
    draft_option1: String(row.draft_option1 || "").trim(),
    draft_option2: String(row.draft_option2 || "").trim(),
    draft_option3: String(row.draft_option3 || "").trim(),
    draft_option4: String(row.draft_option4 || "").trim(),
    fallback: String(row.draft_option_combined_zh || "").trim(),
  });
  return zhCombined || String(row.draft_sku || "").trim() || String(row.id || "").trim();
};

const normalizeVariantGroupToken = (value: unknown) =>
  toText(value)
    .trim()
    .toLowerCase();

const getVariantGroupingFieldsFromRow = (row: DraftSkuRow) => {
  const raw = parseDraftRawRow(row.draft_raw_row);
  const color = toText(raw.variation_color_se || row.draft_option1).trim();
  const other = toText(raw.variation_other_se || row.draft_option3).trim();
  const amount = toText(raw.variation_amount_se || row.draft_option4).trim();
  return { color, other, amount };
};

const buildVariantAssignmentGroupIdFromRow = (row: DraftSkuRow) => {
  const rowId = String(row.id || "").trim();
  if (!rowId) return "";
  const { color, other, amount } = getVariantGroupingFieldsFromRow(row);
  const colorKey = normalizeVariantGroupToken(color);
  const otherKey = normalizeVariantGroupToken(other);
  const amountKey = normalizeVariantGroupToken(amount);
  if (!colorKey && !otherKey && !amountKey) {
    return `row:${rowId}`;
  }
  return `group:${colorKey}|${otherKey}|${amountKey}`;
};

const buildVariantAssignmentGroupLabelFromRow = (row: DraftSkuRow) => {
  const { color, other, amount } = getVariantGroupingFieldsFromRow(row);
  const parts = [color, other, amount].filter(Boolean);
  if (parts.length > 0) {
    return parts.join(" / ");
  }
  return buildVariantSwedishReadableLabel(row);
};

const createVariantEditorKey = () =>
  `variant-${Date.now()}-${Math.random().toString(36).slice(2, 9)}`;

const extractVariantLabelFromFilename = (fileName: string, spu: string) => {
  const rawBase = String(fileName || "").replace(/\.[^.]+$/, "");
  const base = stripTrailingImageTagSuffixes(rawBase);
  if (!base) return "";
  let next = base;
  const normalizedSpu = String(spu || "").trim();
  if (
    normalizedSpu &&
    next.toLowerCase().startsWith(normalizedSpu.toLowerCase())
  ) {
    next = next.slice(normalizedSpu.length);
  }
  next = next.replace(/^-+/, "").replace(/-+$/, "");
  if (!next) return base;
  return next.replace(/[_-]+/g, " ").trim();
};

const normalizeVariantComparableValue = (value: string) =>
  String(value || "")
    .normalize("NFKC")
    .toLowerCase()
    .replace(/[^0-9a-z\u00c0-\u024f\u0400-\u04ff\u4e00-\u9fff]+/g, "");

const hasHanCharacters = (value: string) => /[\u4e00-\u9fff]/.test(String(value || ""));

const buildVariantComparableCandidatesFromRow = (row: DraftSkuRow) => {
  const rawRow = parseDraftRawRow(row.draft_raw_row);
  const zhCombined = buildVariantCombinedZhValue({
    draft_option1: String(row.draft_option1 || "").trim(),
    draft_option2: String(row.draft_option2 || "").trim(),
    draft_option3: String(row.draft_option3 || "").trim(),
    draft_option4: String(row.draft_option4 || "").trim(),
    fallback: String(row.draft_option_combined_zh || "").trim(),
  });
  const candidates = [
    zhCombined,
    String(row.draft_option_combined_zh || "").trim(),
    String(row.draft_option1 || "").trim(),
    String(row.draft_option2 || "").trim(),
    String(row.draft_option3 || "").trim(),
    String(row.draft_option4 || "").trim(),
    String(rawRow.variation_color_zh || "").trim(),
    String(rawRow.variation_size_zh || "").trim(),
    String(rawRow.variation_other_zh || "").trim(),
    String(rawRow.variation_amount_zh || "").trim(),
  ]
    .filter((value) => Boolean(value))
    .filter((value) => hasHanCharacters(value))
    .map((value) => normalizeVariantComparableValue(value))
    .filter((value) => value.length >= 2);
  return Array.from(new Set(candidates));
};

const doesVariantRowLikelyMatchImageFileName = (
  row: DraftSkuRow,
  imageFileName: string,
  spu: string
) => {
  const fileLabel = extractVariantLabelFromFilename(imageFileName, spu);
  const fileKey = normalizeVariantComparableValue(fileLabel);
  if (!fileKey) return false;
  const rawRow = parseDraftRawRow(row.draft_raw_row);
  const zhCombined = buildVariantCombinedZhValue({
    draft_option1: String(row.draft_option1 || "").trim(),
    draft_option2: String(row.draft_option2 || "").trim(),
    draft_option3: String(row.draft_option3 || "").trim(),
    draft_option4: String(row.draft_option4 || "").trim(),
    fallback: String(row.draft_option_combined_zh || "").trim(),
  });
  const candidates = [
    zhCombined,
    String(row.draft_option_combined_zh || "").trim(),
    String(row.draft_option1 || "").trim(),
    String(row.draft_option2 || "").trim(),
    String(row.draft_option3 || "").trim(),
    String(row.draft_option4 || "").trim(),
    String(rawRow.variation_color_zh || "").trim(),
    String(rawRow.variation_size_zh || "").trim(),
    String(rawRow.variation_other_zh || "").trim(),
    String(rawRow.variation_amount_zh || "").trim(),
  ]
    .map((value) => normalizeVariantComparableValue(value))
    .filter((value) => value.length >= 2);
  if (candidates.length === 0) return false;
  return candidates.some((candidate) => {
    if (candidate.length < 2) return false;
    return fileKey.includes(candidate) || candidate.includes(fileKey);
  });
};

const RAW_ROW_FIELD_MAP: Record<string, string[]> = {
  draft_mf_product_short_title: ["SE_shorttitle"],
  draft_mf_product_long_title: ["SE_longtitle"],
  draft_mf_product_subtitle: ["SE_subtitle"],
  draft_mf_product_bullets_short: ["SE_bullets_short"],
  draft_mf_product_bullets: ["SE_bullets"],
  draft_mf_product_bullets_long: ["SE_bullets_long"],
  draft_product_description_main_html: ["SE_description_main"],
  draft_description_html: ["SE_description_short"],
  draft_mf_product_description_short_html: ["SE_description_short"],
  draft_mf_product_description_extended_html: ["SE_description_extended"],
  draft_mf_product_specs: ["SE_specifications"],
};

const USE_NEW_FILE_EXPLORER = true;
const COMPLETED_SPU_STORAGE_KEY = "draftExplorer.completedSpuFolders.v1";
const UNSEEN_AI_UPDATES_STORAGE_KEY = "draftExplorer.unseenAiUpdatesBySpu.v1";
const CONTEXT_MENU_STICKY_CLOSE_MS = 520;
const AUTO_LEVELS_MAX_IMAGES = 8;
const FLIP_HORIZONTAL_MAX_IMAGES = 12;
const COLLAGE_MAX_IMAGES = 9;
const STACK_IMAGES_MAX_IMAGES = 9;
const STACK_OVERLAY_MIN_IMAGES = 2;
const DIGIDEAL_MAIN_STACK_OVERLAY_MIN_IMAGES = 3;
const ENABLE_VARIANT_IMAGE_AUTO_INFER = false;

const getSpuRootFromDraftPath = (pathValue: string) => {
  const normalized = String(pathValue || "").trim().replace(/^\/+|\/+$/g, "");
  if (!normalized) return "";
  const parts = normalized.split("/").filter(Boolean);
  if (parts.length < 2) return "";
  const run = String(parts[0] || "").trim();
  const spu = String(parts[1] || "").trim();
  if (!run || !spu) return "";
  const normalizedSpu = spu.toLowerCase().replace(/[\s_-]+/g, "");
  if (normalizedSpu === "chunks") return "";
  return `${run}/${spu}`;
};

const estimateExpandedInputWidthPx = (
  value: string,
  options?: { minPx?: number; maxPx?: number }
) => {
  const text = String(value ?? "");
  const wideCharCount = (text.match(/[^\u0000-\u00ff]/g) ?? []).length;
  const approximateCharCount = text.length + wideCharCount;
  const estimated = approximateCharCount * 8 + 56;
  const minPx = options?.minPx ?? 0;
  const maxPx = options?.maxPx ?? 1400;
  if (estimated < minPx) return minPx;
  if (estimated > maxPx) return maxPx;
  return estimated;
};

const normalizeColumnSampleText = (value: unknown, maxChars = 120) => {
  const text = String(value ?? "").trim().replace(/\s+/g, " ");
  if (!text) return "";
  if (text.length <= maxChars) return text;
  return `${text.slice(0, maxChars)}...`;
};

const estimateTableTextWidthPx = (value: unknown) => {
  const text = normalizeColumnSampleText(value, 160);
  if (!text) return 0;
  const wideCharCount = (text.match(/[^\u0000-\u00ff]/g) ?? []).length;
  const asciiCount = text.length - wideCharCount;
  return Math.ceil(asciiCount * 7.1 + wideCharCount * 12.4);
};

const clampNumber = (value: number, min: number, max: number) =>
  Math.min(max, Math.max(min, value));

const computeAdaptiveColumnWidthPx = (
  headerText: string,
  values: unknown[],
  options: {
    minPx: number;
    maxPx: number;
    headerAsMax?: boolean;
    headerPaddingPx?: number;
    contentPaddingPx?: number;
  }
) => {
  const headerPaddingPx = options.headerPaddingPx ?? 24;
  const contentPaddingPx = options.contentPaddingPx ?? 24;
  const headerWidth = estimateTableTextWidthPx(headerText) + headerPaddingPx;
  const contentWidth =
    values.length > 0
      ? Math.max(...values.map((value) => estimateTableTextWidthPx(value))) + contentPaddingPx
      : 0;

  let target = Math.max(headerWidth, contentWidth, options.minPx);
  if (options.headerAsMax) {
    target = Math.min(target, Math.max(headerWidth, options.minPx));
  }
  return Math.round(clampNumber(target, options.minPx, options.maxPx));
};

const parseNumericFieldValue = (value: number | string | null | undefined): number | null => {
  if (typeof value === "number") {
    return Number.isFinite(value) ? value : null;
  }
  const text = String(value ?? "").trim();
  if (!text) return null;
  const normalized = text
    .replace(",", ".")
    .replace(/[^0-9.+-]/g, "")
    .trim();
  if (!normalized || normalized === "." || normalized === "+" || normalized === "-") {
    return null;
  }
  const parsed = Number(normalized);
  return Number.isFinite(parsed) ? parsed : null;
};

const clampAutoLevelsPercent = (value: number) =>
  Math.min(100, Math.max(0, Number.isFinite(value) ? value : 50));

const clampAutoLevelsQuality = (value: number) =>
  Math.min(100, Math.max(1, Math.round(Number.isFinite(value) ? value : 92)));

const mapAutoLevelsBounds = (levelPercent: number) => {
  const t = clampAutoLevelsPercent(levelPercent) / 100;
  return {
    lower: Number((0.4 + 1.2 * t).toFixed(3)),
    upper: Number((99.6 - 1.2 * t).toFixed(3)),
  };
};

export default function DraftExplorerPage() {
  const styles = useStyles();
  const { t } = useI18n();
  const [folders, setFolders] = useState<DraftFolder[]>([]);
  const [selectedFolder, setSelectedFolder] = useState<string>("");
  const [currentPath, setCurrentPath] = useState<string>("");
  const [batchPickerOpen, setBatchPickerOpen] = useState(false);
  const [spuPickerOpen, setSpuPickerOpen] = useState(false);
  const [explorerActionsMenuOpen, setExplorerActionsMenuOpen] = useState(false);
  const [imageActionsMenuOpen, setImageActionsMenuOpen] = useState(false);
  const [runSpuOptions, setRunSpuOptions] = useState<RunSpuOption[]>([]);
  const [runSpuStatusByPath, setRunSpuStatusByPath] = useState<
    Record<string, RunSpuStatus>
  >({});
  const [unseenAiUpdateSpuPaths, setUnseenAiUpdateSpuPaths] = useState<Set<string>>(
    new Set()
  );
  const [selectedRunsForMerge, setSelectedRunsForMerge] = useState<Set<string>>(
    new Set()
  );
  const [mergeRunsPending, setMergeRunsPending] = useState(false);

  const [runPreviewOpen, setRunPreviewOpen] = useState(false);
  const [runPreviewRun, setRunPreviewRun] = useState<string>("");
  const [runPreviewLoading, setRunPreviewLoading] = useState(false);
	  const [runPreviewItems, setRunPreviewItems] = useState<DraftRunPreviewItem[]>(
	    []
	  );
	  const [runPreviewSelectedSpus, setRunPreviewSelectedSpus] = useState<
	    Set<string>
	  >(new Set());
	  const [runPreviewDeletedSpus, setRunPreviewDeletedSpus] = useState<Set<string>>(
	    new Set()
	  );
  const [runPreviewSaving, setRunPreviewSaving] = useState(false);
  const [entries, setEntries] = useState<DraftEntry[]>([]);
  const [mainViewVariantImageEntries, setMainViewVariantImageEntries] = useState<
    DraftEntry[]
  >([]);
  const [imageTabImageCounts, setImageTabImageCounts] = useState<
    Record<ImageFolderTabValue, number>
  >({
    main: 0,
    variants: 0,
    sizecharts: 0,
    ocr: 0,
    others: 0,
    downloaded: 0,
  });
  const [entriesLoading, setEntriesLoading] = useState(false);
  const [entriesRefreshing, setEntriesRefreshing] = useState(false);
  const [pendingImageScorePaths, setPendingImageScorePaths] = useState<Set<string>>(
    new Set()
  );
  const [mainImageViewFilter, setMainImageViewFilter] =
    useState<MainImageViewFilter>("all");
  const [selectedFiles, setSelectedFiles] = useState<Set<string>>(new Set());
  const [optimisticHiddenImagePaths, setOptimisticHiddenImagePaths] = useState<
    Set<string>
  >(new Set());
  const [completedSpuFolders, setCompletedSpuFolders] = useState<Set<string>>(
    new Set()
  );
  const [imageViewMode, setImageViewMode] = useState<"small" | "big">("small");
  const [imageDimensions, setImageDimensions] = useState<
    Record<string, { width: number; height: number }>
  >({});
  const [selectedTreeFolders, setSelectedTreeFolders] = useState<Set<string>>(
    new Set()
  );
  const [collapsedTreeFolders, setCollapsedTreeFolders] = useState<Set<string>>(
    new Set()
  );
  const [folderTree, setFolderTree] = useState<DraftFolderTreeNode | null>(null);
  const [folderTreeLoading, setFolderTreeLoading] = useState(false);
  const [folderTreeHidden, setFolderTreeHidden] = useState(true);
  const [filesPaneHidden, setFilesPaneHidden] = useState(true);
  const [movingEntry, setMovingEntry] = useState(false);
  const [draggingEntryPaths, setDraggingEntryPaths] = useState<string[]>([]);
  const [folderDropTargetPath, setFolderDropTargetPath] = useState<string | null>(
    null
  );
  const [imageReorderDropPath, setImageReorderDropPath] = useState<string | null>(
    null
  );
  const [imageOrderPersisting, setImageOrderPersisting] = useState(false);
  const [contextMenu, setContextMenu] = useState<ExplorerContextMenuState | null>(
    null
  );
  const [contextMenuSubmenu, setContextMenuSubmenu] = useState<
    | "tag-image"
    | "assign-variant"
    | "edit-image"
    | "google-lens-search"
    | "edit-chatgpt"
    | "edit-gemini"
    | null
  >(null);
  const [contextMenuNestedSubmenu, setContextMenuNestedSubmenu] = useState<
    | "chatgpt-template-count"
    | "chatgpt-direct-count"
    | "chatgpt-digideal-single"
    | "chatgpt-digideal-dual"
    | "chatgpt-scene"
    | "gemini-template-count"
    | "gemini-direct-count"
    | "gemini-digideal-single"
    | "gemini-digideal-dual"
    | "gemini-scene"
    | null
  >(null);
  const contextMenuRef = useRef<HTMLDivElement | null>(null);
  const contextMenuCloseTimerRef = useRef<number | null>(null);
  const contextMenuSubmenuCloseTimerRef = useRef<number | null>(null);
  const contextMenuNestedCloseTimerRef = useRef<number | null>(null);
  const [contextMenuSubmenuSide, setContextMenuSubmenuSide] = useState<
    "right" | "left"
  >("right");
  const [variantPickerRowsBySpu, setVariantPickerRowsBySpu] = useState<
    Record<string, DraftSkuRow[]>
  >({});
  const [contextMenuVariantRowsLoading, setContextMenuVariantRowsLoading] = useState(false);
  const [contextMenuVariantRowsError, setContextMenuVariantRowsError] = useState<string | null>(
    null
  );
  const [contextMenuVariantSavingIds, setContextMenuVariantSavingIds] = useState<Set<string>>(
    new Set()
  );
  const [contextMenuVariantRowsRefreshing, setContextMenuVariantRowsRefreshing] =
    useState(false);
  const nonImageFileSelectAllRef = useRef<HTMLInputElement | null>(null);
  const [pendingAiEditsByOriginal, setPendingAiEditsByOriginal] = useState<
    Record<string, PendingAiEditRecord>
  >({});
  const [aiEditTargets, setAiEditTargets] = useState<DraftEntry[]>([]);
  const [aiEditProvider, setAiEditProvider] = useState<AiEditProvider>("chatgpt");
  const [aiEditMode, setAiEditMode] = useState<AiPromptMode>("template");
  const [aiEditTemplatePreset, setAiEditTemplatePreset] =
    useState<AiTemplatePreset>("standard");
  const [aiEditOutputCount, setAiEditOutputCount] = useState(1);
  const [aiEditPrompt, setAiEditPrompt] = useState("");
  const [aiDirectPromptHistory, setAiDirectPromptHistory] = useState<string[]>([]);
  const [aiEraserRunMode, setAiEraserRunMode] = useState<"prompt" | "mask">("prompt");
  const [aiEditSubmitting, setAiEditSubmitting] = useState(false);
  const [aiEditError, setAiEditError] = useState<string | null>(null);
  const [aiDualDialog, setAiDualDialog] = useState<AiDualDialogState | null>(null);
  const [aiDualPrompt, setAiDualPrompt] = useState("");
  const [aiDualSubmitting, setAiDualSubmitting] = useState(false);
  const [aiDualError, setAiDualError] = useState<string | null>(null);
  const [aiDualDragRole, setAiDualDragRole] = useState<"product" | "guidance" | null>(
    null
  );
  const [aiCollectionDialog, setAiCollectionDialog] =
    useState<AiCollectionDialogState | null>(null);
  const [aiCollectionPrompt, setAiCollectionPrompt] = useState("");
  const [aiCollectionSubmitting, setAiCollectionSubmitting] = useState(false);
  const [aiCollectionError, setAiCollectionError] = useState<string | null>(null);
  const [aiMaskBrushSize, setAiMaskBrushSize] = useState(36);
  const [aiMaskHasPaint, setAiMaskHasPaint] = useState(false);
  const [aiMaskCursor, setAiMaskCursor] = useState<{
    x: number;
    y: number;
    size: number;
    visible: boolean;
  }>({
    x: 0,
    y: 0,
    size: 18,
    visible: false,
  });
  const aiMaskImageRef = useRef<HTMLImageElement | null>(null);
  const aiMaskCanvasRef = useRef<HTMLCanvasElement | null>(null);
  const aiMaskDrawingRef = useRef(false);
  const aiMaskPointerIdRef = useRef<number | null>(null);
  const aiMaskLastPointRef = useRef<{ x: number; y: number } | null>(null);
  const [aiEditJobsByPath, setAiEditJobsByPath] = useState<Record<string, AiEditRuntimeJob>>(
    {}
  );
  const [aiReviewOriginalPath, setAiReviewOriginalPath] = useState<string | null>(null);
  const [aiReviewSubmitting, setAiReviewSubmitting] = useState(false);
  const [confirmAiEditsPending, setConfirmAiEditsPending] = useState(false);
  const [confirmingAiEditPaths, setConfirmingAiEditPaths] = useState<Set<string>>(
    new Set()
  );
  const [fileViewerPath, setFileViewerPath] = useState<string | null>(null);
  const [fileViewerContent, setFileViewerContent] = useState("");
  const [fileViewerLoading, setFileViewerLoading] = useState(false);
  const [fileViewerSaving, setFileViewerSaving] = useState(false);
  const [fileViewerError, setFileViewerError] = useState<string | null>(null);
  const [previewPath, setPreviewPath] = useState<string | null>(null);
  const [reloadingImagePaths, setReloadingImagePaths] = useState<Set<string>>(
    new Set()
  );
  const [previewDeletePending, setPreviewDeletePending] = useState(false);
  const [photopeaOpen, setPhotopeaOpen] = useState(false);
  const [photopeaEntry, setPhotopeaEntry] = useState<DraftEntry | null>(null);
  const [photopeaReady, setPhotopeaReady] = useState(false);
  const [photopeaLoading, setPhotopeaLoading] = useState(false);
  const [photopeaExporting, setPhotopeaExporting] = useState(false);
  const [photopeaPersisting, setPhotopeaPersisting] = useState(false);
  const [photopeaError, setPhotopeaError] = useState<string | null>(null);
  const [photopeaSessionKey, setPhotopeaSessionKey] = useState(0);
  const photopeaIframeRef = useRef<HTMLIFrameElement | null>(null);
  const photopeaFileBufferRef = useRef<ArrayBuffer | null>(null);
  const photopeaExportBufferRef = useRef<ArrayBuffer | null>(null);
  const photopeaPersistingRef = useRef(false);
  const photopeaReadyRef = useRef(false);
  const photopeaFileSentRef = useRef(false);
  const [variantsImagePreview, setVariantsImagePreview] = useState<{
    src: string;
    label: string;
  } | null>(null);
  const [addImagesDialogOpen, setAddImagesDialogOpen] = useState(false);
  const [addImagesDialogDragActive, setAddImagesDialogDragActive] = useState(false);
  const [addImagesDialogFiles, setAddImagesDialogFiles] = useState<File[]>([]);
  const [addImagesSubmitting, setAddImagesSubmitting] = useState(false);
  const addImagesFileInputRef = useRef<HTMLInputElement | null>(null);
  const [importEditsDialogOpen, setImportEditsDialogOpen] = useState(false);
  const [importEditsSubmitting, setImportEditsSubmitting] = useState(false);
  const importEditsExcelInputRef = useRef<HTMLInputElement | null>(null);
  const importEditsZipInputRef = useRef<HTMLInputElement | null>(null);
  const [reverseSearchOpen, setReverseSearchOpen] = useState(false);
  const [reverseSearchLoading, setReverseSearchLoading] = useState(false);
  const [reverseSearchError, setReverseSearchError] = useState<string | null>(null);
  const [reverseSearchItems, setReverseSearchItems] = useState<ReverseSearchResultItem[]>([]);
  const [reverseSearchAmazonLinks, setReverseSearchAmazonLinks] = useState<string[]>([]);
  const [reverseSearchImagePath, setReverseSearchImagePath] = useState("");
  const [reverseSearchImageName, setReverseSearchImageName] = useState("");
  const [reverseSearchSearchId, setReverseSearchSearchId] = useState<string | null>(null);
  const [reverseSearchFetchingAmazon, setReverseSearchFetchingAmazon] = useState(false);
  const [reverseSearchAddingDownloaded, setReverseSearchAddingDownloaded] = useState(false);
  const [reverseSearchDownloadedCount, setReverseSearchDownloadedCount] = useState(0);
  const [reverseSearchJobsByPath, setReverseSearchJobsByPath] = useState<
    Record<string, ReverseSearchJobState>
  >({});
  const [reverseSearchInputPayload, setReverseSearchInputPayload] = useState<
    Record<string, unknown> | null
  >(null);
  const [reverseSearchDebugPayload, setReverseSearchDebugPayload] = useState<
    Record<string, unknown> | null
  >(null);
  const [reverseSearchBrokenPreviewUrls, setReverseSearchBrokenPreviewUrls] = useState<
    Record<string, true>
  >({});
  const [imageUrlInput, setImageUrlInput] = useState("");
  const [explorerView, setExplorerView] = useState<"list" | "grid">("list");
  const [renamingPath, setRenamingPath] = useState<string | null>(null);
  const [renameValue, setRenameValue] = useState("");
  const [renameExtension, setRenameExtension] = useState("");
  const [renamePending, setRenamePending] = useState(false);
  const [bulkImageActionPending, setBulkImageActionPending] = useState(false);
  const [lastDeletedImageUndoItems, setLastDeletedImageUndoItems] = useState<
    DraftDeletedImageUndoItem[]
  >([]);
  const [tagImagesRunningPaths, setTagImagesRunningPaths] = useState<Set<string>>(
    new Set()
  );
  const [tagAllImagesRunning, setTagAllImagesRunning] = useState(false);
  const [deleteFolderPending, setDeleteFolderPending] = useState(false);
  const [deleteProductPending, setDeleteProductPending] = useState(false);
  const [deleteRunsPending, setDeleteRunsPending] = useState(false);
  const [excelFile, setExcelFile] = useState<File | null>(null);
  const [zipFile, setZipFile] = useState<File | null>(null);
  const [excelStatus, setExcelStatus] = useState<"idle" | "uploading" | "done" | "error">("idle");
  const [zipStatus, setZipStatus] = useState<"idle" | "uploading" | "done" | "error">("idle");
  const [error, setError] = useState<string | null>(null);
  const [draftTab, setDraftTab] = useState<"spu" | "sku">("spu");
  const [searchInput, setSearchInput] = useState("");
  const [searchQuery, setSearchQuery] = useState("");
  const [draftLoading, setDraftLoading] = useState(false);
  const [draftError, setDraftError] = useState<string | null>(null);
  const [spuRows, setSpuRows] = useState<DraftSpuRow[]>([]);
  const [skuRows, setSkuRows] = useState<DraftSkuRow[]>([]);
  const [skuBadgeCountPrefetch, setSkuBadgeCountPrefetch] = useState<number>(0);
  const [draftTableShowAll, setDraftTableShowAll] = useState(false);
  const [expandedSkus, setExpandedSkus] = useState<Set<string>>(new Set());
  const [editingCell, setEditingCell] = useState<EditingCell | null>(null);
  const [editingValue, setEditingValue] = useState("");
  const [, setIsSaving] = useState(false);
  const [selectedSpus, setSelectedSpus] = useState<Set<string>>(new Set());
  const [selectedSkus, setSelectedSkus] = useState<Set<string>>(new Set());
  const [productNotesByKey, setProductNotesByKey] = useState<
    Record<string, DraftProductFlagNote | null>
  >({});
  const [productNoteDialogOpen, setProductNoteDialogOpen] = useState(false);
  const [productNoteValue, setProductNoteValue] = useState("");
  const [productNoteLoading, setProductNoteLoading] = useState(false);
  const [productNoteSaving, setProductNoteSaving] = useState(false);
  const [productNoteError, setProductNoteError] = useState<string | null>(null);
  const [deleteRowsPending, setDeleteRowsPending] = useState(false);
  const [duplicateProductPending, setDuplicateProductPending] = useState(false);
  const [duplicateRolesPending, setDuplicateRolesPending] = useState(false);
  const [detailOpen, setDetailOpen] = useState(false);
  const [detailTarget, setDetailTarget] = useState<DraftSpuRow | null>(null);
  const [detailDraft, setDetailDraft] = useState<Record<string, string | null>>({});
  const [detailRawRow, setDetailRawRow] = useState<Record<string, unknown> | null>(
    null
  );
  const [detailImages, setDetailImages] = useState<DraftEntry[]>([]);
  const [detailImagesLoading, setDetailImagesLoading] = useState(false);
  const [detailInstruction, setDetailInstruction] = useState("");
  const [detailSaving, setDetailSaving] = useState(false);
  const [detailRegenerating, setDetailRegenerating] = useState(false);
  const [detailBackgroundRegeneratingRowIds, setDetailBackgroundRegeneratingRowIds] =
    useState<Set<string>>(new Set());
  const [detailError, setDetailError] = useState<string | null>(null);
  const [spuJsonViewer, setSpuJsonViewer] = useState<SpuJsonViewerState | null>(
    null
  );
  const [supplierEditorOpen, setSupplierEditorOpen] = useState(false);
  const [supplierEditorRowId, setSupplierEditorRowId] = useState<string | null>(null);
  const [supplierEditorSpu, setSupplierEditorSpu] = useState("");
  const [supplierEditorValue, setSupplierEditorValue] = useState("");
  const [supplierEditorSaving, setSupplierEditorSaving] = useState(false);
  const [supplierEditorError, setSupplierEditorError] = useState<string | null>(
    null
  );
  const [sourceJsonExternalSourcesBySpu, setSourceJsonExternalSourcesBySpu] = useState<
    Record<string, string[]>
  >({});
  const [sourceJsonExternalSourcesLoadingBySpu, setSourceJsonExternalSourcesLoadingBySpu] =
    useState<Record<string, boolean>>({});
  const sourceJsonExternalSourcesFetchedRef = useRef<Set<string>>(new Set());
  const [publishStatus, setPublishStatus] = useState<
    "idle" | "running" | "done" | "error"
  >("idle");
  const [publishMessage, setPublishMessage] = useState<string | null>(null);
  const [skuStatus, setSkuStatus] = useState<
    "idle" | "running" | "done" | "error"
  >("idle");
  const [skuMessage, setSkuMessage] = useState<string | null>(null);
  const [skuMissingCount, setSkuMissingCount] = useState<number | null>(null);
  const [skuTotalCount, setSkuTotalCount] = useState<number | null>(null);
  const [variantsEditorOpen, setVariantsEditorOpen] = useState(false);
  const [variantsEditorSpu, setVariantsEditorSpu] = useState("");
  const [variantsEditorRows, setVariantsEditorRows] = useState<DraftVariantEditorRow[]>(
    []
  );
  const [variantsEditorThumbs, setVariantsEditorThumbs] = useState<DraftEntry[]>([]);
  const [variantsEditorThumbsLoading, setVariantsEditorThumbsLoading] = useState(false);
  const [variantsEditorLoading, setVariantsEditorLoading] = useState(false);
  const [variantsEditorSaving, setVariantsEditorSaving] = useState(false);
  const [variantsEditorError, setVariantsEditorError] = useState<string | null>(
    null
  );
  const [variantsEditorSelectedRows, setVariantsEditorSelectedRows] = useState<
    Set<string>
  >(new Set());
  const [variantsEditorPacksText, setVariantsEditorPacksText] = useState("");
  const [variantsEditorAiPrompt, setVariantsEditorAiPrompt] = useState("");
  const [variantsEditorAiRunning, setVariantsEditorAiRunning] = useState(false);
  const [variantsEditorSkuRegenerating, setVariantsEditorSkuRegenerating] =
    useState(false);
  const [variantsEditorSort, setVariantsEditorSort] = useState<{
    key: VariantEditorSortKey | null;
    direction: VariantEditorSortDirection;
  }>({
    key: null,
    direction: "asc",
  });
  const [autoLevelsOpen, setAutoLevelsOpen] = useState(false);
  const [autoLevelsTargets, setAutoLevelsTargets] = useState<DraftEntry[]>([]);
  const [autoLevelsLevelPercent, setAutoLevelsLevelPercent] = useState(50);
  const [autoLevelsQuality, setAutoLevelsQuality] = useState(92);
  const [autoLevelsSubmitting, setAutoLevelsSubmitting] = useState(false);
  const [autoLevelsError, setAutoLevelsError] = useState<string | null>(null);
  const [stackOverlayPickerOpen, setStackOverlayPickerOpen] = useState(false);
  const [stackOverlayTargets, setStackOverlayTargets] = useState<DraftEntry[]>([]);
  const [stackOverlayCandidateTargets, setStackOverlayCandidateTargets] = useState<DraftEntry[]>(
    []
  );
  const [stackOverlayMode, setStackOverlayMode] = useState<
    "digideal_main_stack_overlay" | "stack_overlay"
  >("digideal_main_stack_overlay");
  const [stackOverlayTopImagePath, setStackOverlayTopImagePath] = useState("");
  const [stackOverlayInfo, setStackOverlayInfo] = useState<string | null>(null);
  const [stackOverlaySubmitting] = useState(false);
  const [stackOverlayError, setStackOverlayError] = useState<string | null>(null);
  const [initialOpenSpu, setInitialOpenSpu] = useState("");
  const pendingFolderOpenPathRef = useRef<string | null>(null);
  const initialUrlStateRef = useRef<DraftExplorerUrlState | null>(null);
  const initialUrlStateParsedRef = useRef(false);
  const initialUrlShowAllPendingRef = useRef(false);
  const initialUrlImageTabPendingRef = useRef<ImageFolderTabValue | "">("");
  const initialUrlImagePathPendingRef = useRef("");
  const hasSyncedUrlStateRef = useRef(false);
  const lastUrlLocationStateRef = useRef<{ run: string; spu: string; path: string }>({
    run: "",
    spu: "",
    path: "",
  });
  const initialOpenSpuHandledRef = useRef(false);
  const selectedFolderRef = useRef("");
  const foldersRequestSeqRef = useRef(0);
  const folderSelectRequestSeqRef = useRef(0);
  const selectionAnchorImagePathRef = useRef<string | null>(null);
  const currentPathRef = useRef("");
  const currentMainPathRef = useRef("");
  const currentVariantsPathRef = useRef("");
  const previousSelectedSpuPathForDraftTableRef = useRef("");
  const previousRunSpuPendingAiByPathRef = useRef<Record<string, boolean>>({});
  const autoMainSortPersistSignatureRef = useRef("");
  const sizeChartAutoGenerateSignatureRef = useRef("");
  const sizeChartAutoGenerateInFlightRef = useRef(false);
  const imageOrderSaveQueueRef = useRef<Map<string, Promise<void>>>(new Map());
  const entriesRequestSeqRef = useRef(0);
  const variantEntriesCacheRef = useRef<Map<string, DraftEntry[]>>(new Map());
  const autoVariantLinkInFlightSpusRef = useRef<Set<string>>(new Set());
  const autoVariantLinkAttemptedSignaturesRef = useRef<Set<string>>(new Set());

  const tagImagesRunning = tagImagesRunningPaths.size > 0;
  const hasPendingDetailAiRewrite =
    detailRegenerating || detailBackgroundRegeneratingRowIds.size > 0;
  const tagImagesRunningInCurrentPath = useMemo(() => {
    const pathValue = String(currentPath || "").trim();
    return Boolean(pathValue && tagImagesRunningPaths.has(pathValue));
  }, [currentPath, tagImagesRunningPaths]);

  useEffect(() => {
    try {
      const raw = window.localStorage.getItem(COMPLETED_SPU_STORAGE_KEY);
      if (!raw) return;
      const parsed = JSON.parse(raw) as unknown;
      if (!Array.isArray(parsed)) return;
      const paths = parsed
        .map((value) => String(value || "").trim())
        .filter((value) => Boolean(value));
      setCompletedSpuFolders(new Set(paths));
    } catch {
      // ignore bad local storage payloads
    }
  }, []);

  useEffect(() => {
    try {
      window.localStorage.setItem(
        COMPLETED_SPU_STORAGE_KEY,
        JSON.stringify(Array.from(completedSpuFolders))
      );
    } catch {
      // ignore storage failures
    }
  }, [completedSpuFolders]);

  useEffect(() => {
    try {
      const raw = window.localStorage.getItem(UNSEEN_AI_UPDATES_STORAGE_KEY);
      if (!raw) return;
      const parsed = JSON.parse(raw) as unknown;
      if (!Array.isArray(parsed)) return;
      const paths = parsed
        .map((value) => getSpuRootFromDraftPath(String(value || "")))
        .filter((value) => Boolean(value));
      setUnseenAiUpdateSpuPaths(new Set(paths));
    } catch {
      // ignore bad local storage payloads
    }
  }, []);

  useEffect(() => {
    try {
      window.localStorage.setItem(
        UNSEEN_AI_UPDATES_STORAGE_KEY,
        JSON.stringify(Array.from(unseenAiUpdateSpuPaths))
      );
    } catch {
      // ignore storage failures
    }
  }, [unseenAiUpdateSpuPaths]);

  useEffect(() => {
    try {
      const raw = window.localStorage.getItem(AI_DIRECT_PROMPT_HISTORY_KEY);
      if (!raw) return;
      const parsed = JSON.parse(raw) as unknown;
      const history = parseAiDirectPromptHistory(parsed);
      if (history.length > 0) {
        setAiDirectPromptHistory(history);
      }
    } catch {
      // ignore bad local storage payloads
    }
  }, []);

  useEffect(() => {
    try {
      window.localStorage.setItem(
        AI_DIRECT_PROMPT_HISTORY_KEY,
        JSON.stringify(aiDirectPromptHistory.slice(0, AI_DIRECT_PROMPT_HISTORY_LIMIT))
      );
    } catch {
      // ignore storage failures
    }
  }, [aiDirectPromptHistory]);

  useEffect(() => {
    const run = normalizeExplorerLocationPath(selectedFolder);
    const path = normalizeExplorerLocationPath(currentPath);
    if (!run) return;
    const pathInRun =
      path && (path === run || path.startsWith(`${run}/`)) ? path : run;
    try {
      window.localStorage.setItem(
        DRAFT_EXPLORER_LAST_LOCATION_KEY,
        JSON.stringify({
          run,
          path: pathInRun,
          touchedAt: new Date().toISOString(),
        })
      );
    } catch {
      // ignore storage failures
    }
  }, [currentPath, selectedFolder]);

  const markSpuNeedsAiReview = useCallback((pathValues: string[] | string) => {
    const values = Array.isArray(pathValues) ? pathValues : [pathValues];
    const currentViewedSpuPath = getSpuRootFromDraftPath(
      String(currentPathRef.current || "")
    );
    const normalized = Array.from(
      new Set(
        values
          .map((value) => getSpuRootFromDraftPath(String(value || "")))
          .filter((value) => Boolean(value) && value !== currentViewedSpuPath)
      )
    );
    if (normalized.length === 0) return;
    setUnseenAiUpdateSpuPaths((prev) => {
      let changed = false;
      const next = new Set(prev);
      normalized.forEach((pathValue) => {
        if (!next.has(pathValue)) {
          next.add(pathValue);
          changed = true;
        }
      });
      return changed ? next : prev;
    });
    setRunSpuStatusByPath((prev) => {
      let changed = false;
      const next = { ...prev };
      normalized.forEach((pathValue) => {
        const currentStatus = next[pathValue];
        if (!currentStatus) return;
        if (!currentStatus.hasUnseenAiUpdate) {
          next[pathValue] = { ...currentStatus, hasUnseenAiUpdate: true };
          changed = true;
        }
      });
      return changed ? next : prev;
    });
  }, []);

  const clearSpuNeedsAiReview = useCallback((pathValue: string) => {
    const spuPath = getSpuRootFromDraftPath(pathValue);
    if (!spuPath) return;
    setUnseenAiUpdateSpuPaths((prev) => {
      if (!prev.has(spuPath)) return prev;
      const next = new Set(prev);
      next.delete(spuPath);
      return next;
    });
    setRunSpuStatusByPath((prev) => {
      const currentStatus = prev[spuPath];
      if (!currentStatus?.hasUnseenAiUpdate) return prev;
      return {
        ...prev,
        [spuPath]: {
          ...currentStatus,
          hasUnseenAiUpdate: false,
        },
      };
    });
  }, []);

  const imageExtensions = useMemo(
    () => [".png", ".jpg", ".jpeg", ".webp", ".gif"],
    []
  );
  const isImage = useCallback(
    (name: string) =>
      imageExtensions.some((ext) => name.toLowerCase().endsWith(ext)),
    [imageExtensions]
  );

  const entryByPath = useMemo(() => {
    const map = new Map<string, DraftEntry>();
    entries.forEach((entry) => {
      map.set(entry.path, entry);
    });
    return map;
  }, [entries]);
  const displayImageEntriesRef = useRef<DraftEntry[]>([]);
  const isImageEntryUpscaled = useCallback(
    (entry: DraftEntry | null | undefined) => {
      if (!entry || entry.type !== "file") return false;
      const latest = entryByPath.get(entry.path) ?? entry;
      return Boolean(latest.zimageUpscaled);
    },
    [entryByPath]
  );

  const buildDraftDownloadUrl = useCallback(
    (pathValue: string, cacheVersion?: string | number | null) => {
      const query = new URLSearchParams();
      query.set("path", pathValue);
      if (cacheVersion !== undefined && cacheVersion !== null && String(cacheVersion)) {
        query.set("v", String(cacheVersion));
      }
      return `/api/drafts/download?${query.toString()}`;
    },
    []
  );

  const reverseSearchDisplayImagePath = useMemo(() => {
    const selectedPath = String(reverseSearchImagePath || "").trim();
    if (!selectedPath) return "";
    const parentPath = selectedPath.includes("/")
      ? selectedPath.slice(0, selectedPath.lastIndexOf("/"))
      : "";
    if (!parentPath) return selectedPath;
    const mainEntry = entries.find(
      (entry) =>
        entry.type === "file" &&
        isImage(entry.name) &&
        entry.path.startsWith(`${parentPath}/`) &&
        extractImageTagsFromFileName(entry.name).includes("MAIN")
    );
    return mainEntry?.path || selectedPath;
  }, [entries, isImage, reverseSearchImagePath]);

  const reverseSearchDisplayImageModifiedAt =
    entryByPath.get(reverseSearchDisplayImagePath)?.modifiedAt ?? null;
  const reverseSearchDisplayImageUrl = reverseSearchDisplayImagePath
    ? buildDraftDownloadUrl(
        reverseSearchDisplayImagePath,
        reverseSearchDisplayImageModifiedAt
      )
    : "";
  const reverseSearchDisplayProductTitle = useMemo(() => {
    const targetPath = String(
      reverseSearchDisplayImagePath || reverseSearchImagePath || ""
    ).trim();
    if (!targetPath) return "";
    const parts = targetPath.split("/").filter(Boolean);
    const spuCode = normalizeDraftSpuCode(parts[1] || "");
    if (!spuCode) return "";
    const row = spuRows.find(
      (candidate) =>
        normalizeDraftSpuCode(candidate.draft_spu || "") === spuCode
    );
    if (!row) return "";
    return (
      getShortTitleLabel(
        row.draft_mf_product_short_title ||
          row.draft_title ||
          row.draft_mf_product_long_title
      ) || ""
    );
  }, [reverseSearchDisplayImagePath, reverseSearchImagePath, spuRows]);

  const handleDownloadReverseSearchJson = useCallback(() => {
    const payload = {
      exportedAt: new Date().toISOString(),
      request: reverseSearchInputPayload,
      response: {
        searchId: reverseSearchSearchId,
        imagePath: reverseSearchImagePath || null,
        imageName: reverseSearchImageName || null,
        total: reverseSearchItems.length,
        amazonCount: reverseSearchAmazonLinks.length,
        items: reverseSearchItems,
        amazonLinks: reverseSearchAmazonLinks,
      },
      debug: reverseSearchDebugPayload,
    };
    const blob = new Blob([JSON.stringify(payload, null, 2)], {
      type: "application/json",
    });
    const url = URL.createObjectURL(blob);
    const anchor = document.createElement("a");
    const stamp = new Date().toISOString().replace(/[:.]/g, "-");
    anchor.href = url;
    anchor.download = `google-lens-search-${stamp}.json`;
    document.body.appendChild(anchor);
    anchor.click();
    anchor.remove();
    URL.revokeObjectURL(url);
  }, [
    reverseSearchAmazonLinks,
    reverseSearchDebugPayload,
    reverseSearchImageName,
    reverseSearchImagePath,
    reverseSearchInputPayload,
    reverseSearchItems,
    reverseSearchSearchId,
  ]);

  const buildPhotopeaUrl = useCallback(() => {
    const config = {
      environment: {
        // Make Photopea "File -> Save" return a JPG ArrayBuffer to the parent window.
        customIO: {
          save: "app.activeDocument.saveToOE('jpg:0.92');",
        },
      },
    };
    return `https://www.photopea.com/#${encodeURIComponent(JSON.stringify(config))}`;
  }, []);

  const buildDraggedPathsForEntry = useCallback(
    (entry: DraftEntry) => {
      if (entry.type !== "file") return [entry.path];
      const selectedSource = isImage(entry.name)
        ? displayImageEntriesRef.current
        : entries;
      const selected = selectedSource
        .filter((candidate) => candidate.type === "file" && selectedFiles.has(candidate.path))
        .map((candidate) => candidate.path);
      if (selected.length > 1 && selected.includes(entry.path)) {
        return Array.from(new Set(selected));
      }
      return [entry.path];
    },
    [entries, isImage, selectedFiles]
  );

  const readDraggedPaths = useCallback(
    (transfer: DataTransfer | null) => {
      if (!transfer) {
        return draggingEntryPaths;
      }
      const raw = transfer.getData("application/x-nordexo-paths");
      if (raw) {
        try {
          const parsed = JSON.parse(raw) as unknown;
          if (Array.isArray(parsed)) {
            return parsed
              .map((value) => String(value || "").trim())
              .filter((value) => Boolean(value));
          }
        } catch {
          // Fallback to text/plain
        }
      }
      const single = String(transfer.getData("text/plain") || "").trim();
      if (single) return [single];
      return draggingEntryPaths;
    },
    [draggingEntryPaths]
  );

  useEffect(() => {
    const handle = setTimeout(() => {
      setSearchQuery(searchInput.trim());
    }, 300);
    return () => clearTimeout(handle);
  }, [searchInput]);

  const fetchSpuRows = useCallback(async () => {
    const runFilter = String(selectedFolder || "").trim();
    if (!runFilter) {
      setSpuRows([]);
      return;
    }
    setDraftLoading(true);
    setDraftError(null);
    try {
      const url = new URL("/api/drafts/products", window.location.origin);
      url.searchParams.set("run", runFilter);
      if (searchQuery) url.searchParams.set("q", searchQuery);
      const response = await fetch(url.toString());
      if (!response.ok) {
        const text = await response.text();
        let message = text;
        if (text) {
          try {
            const parsed = JSON.parse(text) as { error?: string; message?: string };
            message = parsed?.error || parsed?.message || text;
          } catch {
            // Keep raw text.
          }
        }
        if (!message) {
          message = `Unable to load drafts (HTTP ${response.status}).`;
        }
        throw new Error(message);
      }
      const payload = await response.json();
      setSpuRows(payload.items ?? []);
    } catch (err) {
      setDraftError((err as Error).message);
      // Keep existing rows on error so we don't look like we deleted data.
    } finally {
      setDraftLoading(false);
    }
  }, [searchQuery, selectedFolder]);

  const fetchSkuRows = useCallback(async (options?: { silent?: boolean }) => {
    const runFilter = String(selectedFolder || "").trim();
    const silent = Boolean(options?.silent);
    if (!runFilter) {
      setSkuRows([]);
      return;
    }
    if (!silent) {
      setDraftLoading(true);
      setDraftError(null);
    }
    try {
      const url = new URL("/api/drafts/variants", window.location.origin);
      url.searchParams.set("run", runFilter);
      if (searchQuery) url.searchParams.set("q", searchQuery);
      const response = await fetch(url.toString());
      if (!response.ok) {
        const text = await response.text();
        let message = text;
        if (text) {
          try {
            const parsed = JSON.parse(text) as { error?: string; message?: string };
            message = parsed?.error || parsed?.message || text;
          } catch {
            // Keep raw text.
          }
        }
        if (!message) {
          message = `Unable to load drafts (HTTP ${response.status}).`;
        }
        throw new Error(message);
      }
      const payload = await response.json();
      setSkuRows(payload.items ?? []);
    } catch (err) {
      if (!silent) {
        setDraftError((err as Error).message);
      }
      // Keep existing rows on error so we don't look like we deleted data.
    } finally {
      if (!silent) {
        setDraftLoading(false);
      }
    }
  }, [searchQuery, selectedFolder]);

  const fetchSkuStatus = useCallback(async () => {
    try {
      const response = await fetch("/api/drafts/sku/status");
      if (!response.ok) {
        const text = await response.text();
        let message = text;
        if (text) {
          try {
            const parsed = JSON.parse(text) as { error?: string };
            if (parsed?.error) message = parsed.error;
          } catch {
            // Keep raw text as message.
          }
        }
        if (!message) {
          message = `Unable to read SKU status (HTTP ${response.status}).`;
        }
        throw new Error(message);
      }
      const payload = await response.json();
      const nextStatus = payload.status ?? "idle";
      const nextMissing =
        typeof payload.missingCount === "number" ? payload.missingCount : null;
      const nextTotal =
        typeof payload.totalCount === "number" ? payload.totalCount : null;
      let nextMessage = payload.message ?? null;
      if (nextStatus === "done" && (!nextTotal || nextTotal === 0)) {
        nextMessage = null;
      }
      setSkuStatus(nextStatus);
      setSkuMessage(nextMessage);
      setSkuMissingCount(nextMissing);
      setSkuTotalCount(nextTotal);
      return payload;
    } catch (err) {
      setSkuStatus("error");
      setSkuMessage((err as Error).message);
      throw err;
    }
  }, []);

  const runSkuPipelineForSpus = useCallback(
    async (spus: string[]) => {
      const response = await fetch("/api/drafts/sku/generate", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          spus: spus.length ? spus : undefined,
        }),
      });
      if (!response.ok) {
        const message = await response.text();
        throw new Error(message || "SKU generation failed.");
      }
      let payload = await fetchSkuStatus();
      let attempts = 0;
      while (payload?.status === "running") {
        await new Promise((resolve) => setTimeout(resolve, 5000));
        payload = await fetchSkuStatus();
        attempts += 1;
        if (attempts > 360) {
          throw new Error("SKU generation timed out.");
        }
      }
      if (payload?.status === "error") {
        throw new Error(payload?.message || "SKU generation failed.");
      }
    },
    [fetchSkuStatus]
  );

  useEffect(() => {
    const params = new URLSearchParams(window.location.search);
    const hashParams = new URLSearchParams(
      String(window.location.hash || "").replace(/^#/, "")
    );
    const requestedTabRaw = parseExplorerStateParam(params, hashParams, ["tab"])
      .trim()
      .toLowerCase();
    const requestedTab =
      requestedTabRaw === "spu" || requestedTabRaw === "sku" ? requestedTabRaw : "";
    const requestedRun = normalizeExplorerLocationPath(
      parseExplorerStateParam(params, hashParams, ["run"])
    );
    const requestedPath = normalizeExplorerLocationPath(
      parseExplorerStateParam(params, hashParams, ["path"])
    );
    const requestedSpu = normalizeDraftSpuCode(
      parseExplorerStateParam(params, hashParams, ["spu"])
    );
    const requestedOpenSpu = normalizeDraftSpuCode(
      parseExplorerStateParam(params, hashParams, ["open_spu"])
    );
    const requestedSpuCode = requestedSpu || requestedOpenSpu;
    const requestedImageTab = parseImageFolderTabValue(
      parseExplorerStateParam(params, hashParams, ["imgTab", "imageTab"])
    );
    const requestedImagePath = normalizeExplorerLocationPath(
      parseExplorerStateParam(params, hashParams, ["img", "image"])
    );
    const runFromPath = parseRunFromExplorerPath(requestedPath);
    const resolvedRun = requestedRun || runFromPath;
    const showAllRaw = parseExplorerStateParam(params, hashParams, [
      "showAll",
      "show_all",
    ])
      .trim()
      .toLowerCase();
    const requestedShowAll =
      showAllRaw === "1" || showAllRaw === "true" || showAllRaw === "yes";

    initialUrlStateRef.current = {
      run: resolvedRun,
      spu: requestedSpuCode,
      path: requestedPath,
      tab: requestedTab,
      showAll: requestedShowAll,
      imageTab: requestedImageTab,
      imagePath: requestedImagePath,
    };
    initialUrlImageTabPendingRef.current = requestedImageTab;
    initialUrlImagePathPendingRef.current = requestedImagePath;
    lastUrlLocationStateRef.current = {
      run: resolvedRun,
      spu: requestedSpuCode,
      path: requestedPath,
    };
    hasSyncedUrlStateRef.current = false;
    initialUrlStateParsedRef.current = true;
    initialUrlShowAllPendingRef.current = requestedShowAll;

    if (requestedTab) {
      setDraftTab(requestedTab);
    }
    setMainImageViewFilter(toMainImageViewFilter(requestedImageTab));
    if (!requestedImagePath) {
      setPreviewPath(null);
    }

    const shouldResolveSpuByRowLookup = Boolean(requestedSpuCode && !resolvedRun && !requestedPath);
    if (shouldResolveSpuByRowLookup) {
      if (!requestedTab) {
        setDraftTab("spu");
      }
      setInitialOpenSpu(requestedSpuCode);
    }
  }, []);

  useEffect(() => {
    const applyUrlState = () => {
      const params = new URLSearchParams(window.location.search);
      const hashParams = new URLSearchParams(
        String(window.location.hash || "").replace(/^#/, "")
      );
      const requestedTabRaw = parseExplorerStateParam(params, hashParams, ["tab"])
        .trim()
        .toLowerCase();
      const requestedTab =
        requestedTabRaw === "spu" || requestedTabRaw === "sku" ? requestedTabRaw : "spu";
      const requestedRun = normalizeExplorerLocationPath(
        parseExplorerStateParam(params, hashParams, ["run"])
      );
      const requestedPath = normalizeExplorerLocationPath(
        parseExplorerStateParam(params, hashParams, ["path"])
      );
      const requestedSpu = normalizeDraftSpuCode(
        parseExplorerStateParam(params, hashParams, ["spu"])
      );
      const requestedOpenSpu = normalizeDraftSpuCode(
        parseExplorerStateParam(params, hashParams, ["open_spu"])
      );
      const requestedSpuCode = requestedSpu || requestedOpenSpu;
      const requestedImageTab = parseImageFolderTabValue(
        parseExplorerStateParam(params, hashParams, ["imgTab", "imageTab"])
      );
      const requestedImagePath = normalizeExplorerLocationPath(
        parseExplorerStateParam(params, hashParams, ["img", "image"])
      );
      const runFromPath = parseRunFromExplorerPath(requestedPath);
      const resolvedRun = requestedRun || runFromPath;
      const showAllRaw = parseExplorerStateParam(params, hashParams, [
        "showAll",
        "show_all",
      ])
        .trim()
        .toLowerCase();
      const requestedShowAll =
        showAllRaw === "1" || showAllRaw === "true" || showAllRaw === "yes";

      initialUrlStateRef.current = {
        run: resolvedRun,
        spu: requestedSpuCode,
        path: requestedPath,
        tab: requestedTab,
        showAll: requestedShowAll,
        imageTab: requestedImageTab,
        imagePath: requestedImagePath,
      };
      initialUrlStateParsedRef.current = true;
      initialUrlShowAllPendingRef.current = requestedShowAll;
      initialUrlImageTabPendingRef.current = requestedImageTab;
      initialUrlImagePathPendingRef.current = requestedImagePath;
      hasSyncedUrlStateRef.current = false;
      lastUrlLocationStateRef.current = {
        run: resolvedRun,
        spu: requestedSpuCode,
        path: requestedPath,
      };
      initialOpenSpuHandledRef.current = false;

      setDraftTab(requestedTab);
      setMainImageViewFilter(toMainImageViewFilter(requestedImageTab));
      if (!requestedImagePath) {
        setPreviewPath(null);
      }
      if (resolvedRun) {
        const normalizedRun = normalizeExplorerLocationPath(resolvedRun);
        const normalizedPath = normalizeExplorerLocationPath(requestedPath);
        const nextPath =
          normalizedPath &&
          (normalizedPath === normalizedRun ||
            normalizedPath.startsWith(`${normalizedRun}/`))
            ? normalizedPath
            : normalizedRun;
        pendingFolderOpenPathRef.current = nextPath;
        setSelectedFolder(normalizedRun);
        setCurrentPath(nextPath);
        setInitialOpenSpu("");
      } else if (requestedSpuCode) {
        setInitialOpenSpu(requestedSpuCode);
      }
      setDraftTableShowAll(requestedShowAll);
    };

    // Apply URL state on first load as well (not only on browser back/forward).
    applyUrlState();

    const handlePopState = () => {
      applyUrlState();
    };

    window.addEventListener("popstate", handlePopState);
    return () => {
      window.removeEventListener("popstate", handlePopState);
    };
  }, []);

  useEffect(() => {
    if (draftTab === "spu") {
      void fetchSpuRows();
      // Keep SKU rows hydrated while on SPU tab so variant issue flags stay accurate.
      void fetchSkuRows({ silent: true });
    } else {
      void fetchSkuRows();
    }
  }, [draftTab, fetchSpuRows, fetchSkuRows]);

  useEffect(() => {
    setSelectedSpus(new Set());
    setSelectedSkus(new Set());
    setExpandedSkus(new Set());
    setDraftTableShowAll(false);
  }, [selectedFolder]);

  useEffect(() => {
    if (!initialUrlShowAllPendingRef.current) return;
    if (!selectedFolder) return;
    setDraftTableShowAll(true);
    initialUrlShowAllPendingRef.current = false;
  }, [selectedFolder]);

  useEffect(() => {
    fetchSkuStatus().catch(() => {
      // fetchSkuStatus already updates error state; avoid unhandled rejections.
    });
  }, [fetchSkuStatus]);

  useEffect(() => {
    if (skuStatus !== "running") return;
    const handle = setInterval(() => {
      fetchSkuStatus().catch(() => {
        // Avoid unhandled rejections in the polling timer.
      });
    }, 5000);
    return () => clearInterval(handle);
  }, [skuStatus, fetchSkuStatus]);

  useEffect(() => {
    if (skuStatus === "done") {
      if (draftTab === "sku") {
        void fetchSkuRows();
      } else {
        void fetchSpuRows();
        void fetchSkuRows({ silent: true });
      }
    }
  }, [skuStatus, draftTab, fetchSkuRows, fetchSpuRows]);

  useEffect(() => {
    if (!skuMessage) return;
    if (skuStatus !== "done" && skuStatus !== "error") return;
    const timeoutMs = skuStatus === "error" ? 6500 : 6000;
    const handle = setTimeout(() => {
      setSkuMessage(null);
      setSkuStatus((prev) => (prev === "done" || prev === "error" ? "idle" : prev));
    }, timeoutMs);
    return () => clearTimeout(handle);
  }, [skuMessage, skuStatus]);

  useEffect(() => {
    if (!publishMessage) return;
    if (publishStatus !== "done" && publishStatus !== "error") return;
    const timeoutMs = publishStatus === "error" ? 6500 : 2500;
    const handle = setTimeout(() => {
      setPublishMessage(null);
      setPublishStatus((prev) =>
        prev === "done" || prev === "error" ? "idle" : prev
      );
    }, timeoutMs);
    return () => clearTimeout(handle);
  }, [publishMessage, publishStatus]);

  useEffect(() => {
    if (!error) return;
    const handle = setTimeout(() => {
      setError((prev) => (prev === error ? null : prev));
    }, 4500);
    return () => clearTimeout(handle);
  }, [error]);

  const currentImageSpuForDraftFilter = useMemo(() => {
    const run = String(selectedFolder || "").trim();
    const pathValue = String(currentPath || "").trim();
    if (!run || !pathValue) return "";
    const parts = pathValue.split("/").filter(Boolean);
    if (parts.length < 2) return "";
    if (parts[0] !== run) return "";
    const candidate = String(parts[1] || "").trim();
    if (!candidate) return "";
    const normalized = candidate.toLowerCase().replace(/[\s_-]+/g, "");
    if (normalized === "chunks") return "";
    return candidate;
  }, [currentPath, selectedFolder]);

  useEffect(() => {
    if (publishStatus === "running" || skuStatus === "running") return;
    setPublishMessage(null);
    setPublishStatus((prev) => (prev === "running" ? prev : "idle"));
    setSkuMessage(null);
    setSkuStatus((prev) => (prev === "running" ? prev : "idle"));
    setError(null);
  }, [currentImageSpuForDraftFilter, selectedFolder, publishStatus, skuStatus]);

  const draftTableSpuFilterActive =
    Boolean(currentImageSpuForDraftFilter) && !draftTableShowAll;

  const activeNoteRun = String(selectedFolder || "").trim();
  const activeNoteSpu = String(currentImageSpuForDraftFilter || "")
    .trim()
    .toUpperCase();
  const activeProductNoteKey =
    activeNoteRun && activeNoteSpu ? `${activeNoteRun}::${activeNoteSpu}` : "";
  const activeProductNote = activeProductNoteKey
    ? productNotesByKey[activeProductNoteKey] ?? null
    : null;
  const hasActiveProductNote = Boolean(
    activeProductNote && String(activeProductNote.note || "").trim()
  );

  const loadActiveProductNote = useCallback(async () => {
    if (!activeNoteRun || !activeNoteSpu) {
      return null;
    }
    const key = `${activeNoteRun}::${activeNoteSpu}`;
    try {
      const url = new URL("/api/drafts/notes", window.location.origin);
      url.searchParams.set("run", activeNoteRun);
      url.searchParams.set("spu", activeNoteSpu);
      const response = await fetch(url.toString());
      if (!response.ok) {
        const text = await response.text();
        let message = text;
        try {
          const parsed = JSON.parse(text) as { error?: string; message?: string };
          message = parsed?.error || parsed?.message || message;
        } catch {
          // Keep raw response text.
        }
        throw new Error(message || "Unable to load product note.");
      }
      const payload = (await response.json()) as { note?: DraftProductFlagNote | null };
      const nextNote =
        payload && payload.note && typeof payload.note === "object"
          ? payload.note
          : null;
      setProductNotesByKey((prev) => ({ ...prev, [key]: nextNote }));
      return nextNote;
    } catch (err) {
      setProductNoteError((err as Error).message);
      return null;
    }
  }, [activeNoteRun, activeNoteSpu]);

  const openProductNoteDialog = useCallback(async () => {
    if (!activeNoteRun || !activeNoteSpu) return;
    setProductNoteError(null);
    setProductNoteLoading(true);
    setProductNoteDialogOpen(true);
    const loaded = await loadActiveProductNote();
    const fallback = activeProductNoteKey ? productNotesByKey[activeProductNoteKey] : null;
    setProductNoteValue(String((loaded ?? fallback)?.note || ""));
    setProductNoteLoading(false);
  }, [
    activeNoteRun,
    activeNoteSpu,
    activeProductNoteKey,
    loadActiveProductNote,
    productNotesByKey,
  ]);

  const closeProductNoteDialog = useCallback(() => {
    if (productNoteSaving) return;
    setProductNoteDialogOpen(false);
    setProductNoteError(null);
  }, [productNoteSaving]);

  const saveProductNote = useCallback(async () => {
    if (!activeNoteRun || !activeNoteSpu) return;
    setProductNoteSaving(true);
    setProductNoteError(null);
    try {
      const response = await fetch("/api/drafts/notes", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          run: activeNoteRun,
          spu: activeNoteSpu,
          note: productNoteValue,
        }),
      });
      if (!response.ok) {
        const text = await response.text();
        let message = text;
        try {
          const parsed = JSON.parse(text) as { error?: string; message?: string };
          message = parsed?.error || parsed?.message || message;
        } catch {
          // Keep raw text.
        }
        throw new Error(message || "Unable to save note.");
      }
      const payload = (await response.json()) as { note?: DraftProductFlagNote | null };
      const key = `${activeNoteRun}::${activeNoteSpu}`;
      const nextNote =
        payload && payload.note && typeof payload.note === "object"
          ? payload.note
          : null;
      setProductNotesByKey((prev) => ({ ...prev, [key]: nextNote }));
      setProductNoteDialogOpen(false);
    } catch (err) {
      setProductNoteError((err as Error).message);
    } finally {
      setProductNoteSaving(false);
    }
  }, [activeNoteRun, activeNoteSpu, productNoteValue]);

  useEffect(() => {
    setProductNoteError(null);
    if (!activeNoteRun || !activeNoteSpu) return;
    const key = `${activeNoteRun}::${activeNoteSpu}`;
    if (Object.prototype.hasOwnProperty.call(productNotesByKey, key)) return;
    loadActiveProductNote().catch(() => {
      // Best effort in background.
    });
  }, [activeNoteRun, activeNoteSpu, loadActiveProductNote, productNotesByKey]);

  const visibleSpuRows = useMemo(() => {
    if (!draftTableSpuFilterActive) return spuRows;
    const filterSpu = String(currentImageSpuForDraftFilter || "").trim().toUpperCase();
    if (!filterSpu) return spuRows;
    return spuRows.filter(
      (row) => String(row.draft_spu || "").trim().toUpperCase() === filterSpu
    );
  }, [currentImageSpuForDraftFilter, draftTableSpuFilterActive, spuRows]);

  const visibleSkuRows = useMemo(() => {
    if (!draftTableSpuFilterActive) return skuRows;
    const filterSpu = String(currentImageSpuForDraftFilter || "").trim().toUpperCase();
    if (!filterSpu) return skuRows;
    return skuRows.filter(
      (row) => String(row.draft_spu || "").trim().toUpperCase() === filterSpu
    );
  }, [currentImageSpuForDraftFilter, draftTableSpuFilterActive, skuRows]);

  const rawExternalSourcesBySpu = useMemo(() => {
    const bySpu: Record<string, string[]> = {};
    spuRows.forEach((row) => {
      const spuKey = normalizeSpuKey(row.draft_spu);
      if (!spuKey) return;
      bySpu[spuKey] = extractExternalSourceUrlsFromRaw(
        row.draft_raw_row,
        row.draft_supplier_1688_url
      );
    });
    return bySpu;
  }, [spuRows]);

  useEffect(() => {
    setSourceJsonExternalSourcesBySpu({});
    setSourceJsonExternalSourcesLoadingBySpu({});
    sourceJsonExternalSourcesFetchedRef.current.clear();
  }, [selectedFolder]);

  const getExternalSourceUrlsForRow = useCallback(
    (row: DraftSpuRow) => {
      const spuKey = normalizeSpuKey(row.draft_spu);
      if (!spuKey) return [];
      const rawUrls = rawExternalSourcesBySpu[spuKey] ?? [];
      const sourceJsonUrls = sourceJsonExternalSourcesBySpu[spuKey] ?? [];
      return mergeExternalSourceUrls(rawUrls, sourceJsonUrls);
    },
    [rawExternalSourcesBySpu, sourceJsonExternalSourcesBySpu]
  );

  const handleSupplierSourcesMenuOpen = useCallback(
    async (row: DraftSpuRow) => {
      const spuRaw = String(row.draft_spu || "").trim();
      const spuKey = normalizeSpuKey(spuRaw);
      if (!spuKey) return;
      if (sourceJsonExternalSourcesFetchedRef.current.has(spuKey)) return;
      if (sourceJsonExternalSourcesLoadingBySpu[spuKey]) return;

      sourceJsonExternalSourcesFetchedRef.current.add(spuKey);
      setSourceJsonExternalSourcesLoadingBySpu((prev) => ({
        ...prev,
        [spuKey]: true,
      }));

      try {
        const run = String(selectedFolder || "").trim();
        const folder = String(row.draft_image_folder || "").trim();
        if (!run && !folder) {
          setSourceJsonExternalSourcesBySpu((prev) => ({
            ...prev,
            [spuKey]: [],
          }));
          return;
        }

        const url = new URL("/api/drafts/source-json", window.location.origin);
        url.searchParams.set("spu", spuRaw);
        if (folder) {
          url.searchParams.set("folder", folder);
        } else if (run) {
          url.searchParams.set("run", run);
        }

        const response = await fetch(url.toString());
        if (!response.ok) {
          setSourceJsonExternalSourcesBySpu((prev) => ({
            ...prev,
            [spuKey]: [],
          }));
          return;
        }
        const payload = (await response.json()) as { content?: unknown };
        const extracted = extractExternalSourceUrlsFromRaw(
          payload.content,
          row.draft_supplier_1688_url
        );
        setSourceJsonExternalSourcesBySpu((prev) => ({
          ...prev,
          [spuKey]: extracted,
        }));
      } catch {
        setSourceJsonExternalSourcesBySpu((prev) => ({
          ...prev,
          [spuKey]: [],
        }));
      } finally {
        setSourceJsonExternalSourcesLoadingBySpu((prev) => ({
          ...prev,
          [spuKey]: false,
        }));
      }
    },
    [selectedFolder, sourceJsonExternalSourcesLoadingBySpu]
  );

  const variantCriticalIssuesBySpu = useMemo(() => {
    const bySpu = new Map<string, { missingPrice: boolean; missingWeight: boolean }>();
    for (const row of skuRows) {
      const spuKey = String(row.draft_spu || "").trim().toUpperCase();
      if (!spuKey) continue;
      const current = bySpu.get(spuKey) ?? {
        missingPrice: false,
        missingWeight: false,
      };
      const priceValue = parseNumericFieldValue(row.draft_price);
      if (priceValue === null || priceValue <= 0) {
        current.missingPrice = true;
      }
      const weightValue = parseNumericFieldValue(row.draft_weight);
      if (weightValue === null || weightValue <= 0) {
        current.missingWeight = true;
      }
      bySpu.set(spuKey, current);
    }
    return bySpu;
  }, [skuRows]);

  useEffect(() => {
    if (draftTab === "sku") {
      setSkuBadgeCountPrefetch(visibleSkuRows.length);
      return;
    }

    const runFilter = String(selectedFolder || "").trim();
    if (!runFilter) {
      setSkuBadgeCountPrefetch(0);
      return;
    }

    let cancelled = false;
    const fetchSkuBadgeCount = async () => {
      try {
        const url = new URL("/api/drafts/variants", window.location.origin);
        url.searchParams.set("run", runFilter);
        url.searchParams.set("countOnly", "1");
        if (searchQuery) {
          url.searchParams.set("q", searchQuery);
        }
        if (draftTableSpuFilterActive && currentImageSpuForDraftFilter) {
          url.searchParams.set("spu", currentImageSpuForDraftFilter);
        }
        const response = await fetch(url.toString());
        if (!response.ok) {
          throw new Error("Unable to load SKU count.");
        }
        const payload = await response.json().catch(() => ({}));
        const count =
          typeof payload?.count === "number" && Number.isFinite(payload.count)
            ? Math.max(0, Math.trunc(payload.count))
            : 0;
        if (!cancelled) {
          setSkuBadgeCountPrefetch(count);
        }
      } catch {
        if (!cancelled) {
          setSkuBadgeCountPrefetch(0);
        }
      }
    };

    void fetchSkuBadgeCount();
    return () => {
      cancelled = true;
    };
  }, [
    currentImageSpuForDraftFilter,
    draftTab,
    draftTableSpuFilterActive,
    searchQuery,
    selectedFolder,
    visibleSkuRows.length,
  ]);

  const skuTabBadgeCount =
    draftTab === "sku" ? visibleSkuRows.length : skuBadgeCountPrefetch;

  useEffect(() => {
    const visibleIds = new Set(visibleSpuRows.map((row) => row.id));
    setSelectedSpus((prev) => {
      if (prev.size === 0) return prev;
      let changed = false;
      const next = new Set<string>();
      prev.forEach((id) => {
        if (visibleIds.has(id)) {
          next.add(id);
        } else {
          changed = true;
        }
      });
      return changed ? next : prev;
    });
  }, [visibleSpuRows]);

  useEffect(() => {
    const visibleIds = new Set(visibleSkuRows.map((row) => row.id));
    setSelectedSkus((prev) => {
      if (prev.size === 0) return prev;
      let changed = false;
      const next = new Set<string>();
      prev.forEach((id) => {
        if (visibleIds.has(id)) {
          next.add(id);
        } else {
          changed = true;
        }
      });
      return changed ? next : prev;
    });
    setExpandedSkus((prev) => {
      if (prev.size === 0) return prev;
      let changed = false;
      const next = new Set<string>();
      prev.forEach((id) => {
        if (visibleIds.has(id)) {
          next.add(id);
        } else {
          changed = true;
        }
      });
      return changed ? next : prev;
    });
  }, [visibleSkuRows]);

  const allSpuSelected = useMemo(
    () =>
      visibleSpuRows.length > 0 &&
      visibleSpuRows.every((row) => selectedSpus.has(row.id)),
    [selectedSpus, visibleSpuRows]
  );

  const someSpuSelected = useMemo(
    () => visibleSpuRows.some((row) => selectedSpus.has(row.id)),
    [selectedSpus, visibleSpuRows]
  );

  const allSkuSelected = useMemo(
    () =>
      visibleSkuRows.length > 0 &&
      visibleSkuRows.every((row) => selectedSkus.has(row.id)),
    [selectedSkus, visibleSkuRows]
  );

  const someSkuSelected = useMemo(
    () => visibleSkuRows.some((row) => selectedSkus.has(row.id)),
    [selectedSkus, visibleSkuRows]
  );

  const spuColumnStyles = useMemo(() => {
    const sample = visibleSpuRows;
    const makeStyle = (
      headerText: string,
      values: unknown[],
      options: {
        minPx: number;
        maxPx: number;
        headerAsMax?: boolean;
        headerPaddingPx?: number;
        contentPaddingPx?: number;
      }
    ) => {
      const width = computeAdaptiveColumnWidthPx(headerText, values, options);
      return {
        width: `${width}px`,
        minWidth: `${options.minPx}px`,
        maxWidth: `${options.maxPx}px`,
      };
    };

    const detailsLabel = "Edit";
    const viewLabel = "View";

    return {
      selection: {
        width: "44px",
        minWidth: "44px",
        maxWidth: "44px",
      },
      spu: makeStyle(
        t("draftExplorer.columns.spu"),
        sample.map((row) => row.draft_spu),
        { minPx: 110, maxPx: 220 }
      ),
      title: makeStyle(
        t("draftExplorer.columns.title"),
        sample.map((row) => row.draft_title ?? ""),
        { minPx: 180, maxPx: 560, contentPaddingPx: 12 }
      ),
      source: makeStyle(
        t("draftExplorer.columns.source"),
        sample.map((row) => row.draft_source ?? ""),
        { minPx: 90, maxPx: 150 }
      ),
      supplier: makeStyle(
        "Sources",
        sample.map(() => "Supplier Link"),
        { minPx: 126, maxPx: 170, headerAsMax: true }
      ),
      images: makeStyle(
        t("draftExplorer.columns.images"),
        sample.map(() => viewLabel),
        { minPx: 86, maxPx: 124, headerAsMax: true }
      ),
      videos: makeStyle(
        t("draftExplorer.columns.videos"),
        sample.map((row) =>
          Number(row.video_count ?? 0) > 0 ? String(row.video_count) : "-"
        ),
        { minPx: 68, maxPx: 110, headerAsMax: true }
      ),
      variants: makeStyle(
        t("draftExplorer.columns.variants"),
        sample.map((row) => `Edit (${row.variant_count ?? 0})`),
        { minPx: 132, maxPx: 220 }
      ),
      updated: makeStyle(
        t("draftExplorer.columns.updated"),
        sample.map((row) => formatDate(row.draft_updated_at)),
        { minPx: 92, maxPx: 124, headerAsMax: true, contentPaddingPx: 12 }
      ),
      created: makeStyle(
        t("draftExplorer.columns.created"),
        sample.map((row) => formatDate(row.draft_created_at)),
        { minPx: 92, maxPx: 124, headerAsMax: true, contentPaddingPx: 12 }
      ),
      details: makeStyle(
        "Descriptions",
        sample.map(() => detailsLabel),
        { minPx: 92, maxPx: 130, headerAsMax: true }
      ),
      json: makeStyle(
        "JSON",
        sample.map(() => "JSON File"),
        { minPx: 94, maxPx: 130, headerAsMax: true }
      ),
    };
  }, [t, visibleSpuRows]);

  const skuColumnStyles = useMemo(() => {
    const sample = visibleSkuRows;
    const makeStyle = (
      headerText: string,
      values: unknown[],
      options: {
        minPx: number;
        maxPx: number;
        headerAsMax?: boolean;
        headerPaddingPx?: number;
        contentPaddingPx?: number;
      }
    ) => {
      const width = computeAdaptiveColumnWidthPx(headerText, values, options);
      return {
        width: `${width}px`,
        minWidth: `${options.minPx}px`,
        maxWidth: `${options.maxPx}px`,
      };
    };

    const detailsExpandLabel = t("draftExplorer.expand");
    const detailsCollapseLabel = t("draftExplorer.collapse");

    return {
      selection: {
        width: "44px",
        minWidth: "44px",
        maxWidth: "44px",
      },
      sku: makeStyle(
        t("draftExplorer.columns.sku"),
        sample.map((row) => row.draft_sku ?? ""),
        { minPx: 170, maxPx: 340 }
      ),
      colorSe: makeStyle(
        t("draftExplorer.columns.colorSe"),
        sample.map((row) => getRawValue(row.draft_raw_row, "variation_color_se")),
        { minPx: 110, maxPx: 260 }
      ),
      sizeSe: makeStyle(
        t("draftExplorer.columns.sizeSe"),
        sample.map((row) => getRawValue(row.draft_raw_row, "variation_size_se")),
        { minPx: 96, maxPx: 210 }
      ),
      otherSe: makeStyle(
        t("draftExplorer.columns.otherSe"),
        sample.map((row) => getRawValue(row.draft_raw_row, "variation_other_se")),
        { minPx: 126, maxPx: 340 }
      ),
      amountSe: makeStyle(
        t("draftExplorer.columns.amountSe"),
        sample.map((row) => getRawValue(row.draft_raw_row, "variation_amount_se")),
        { minPx: 112, maxPx: 260 }
      ),
      optionCombined: makeStyle(
        t("draftExplorer.columns.optionCombined"),
        sample.map((row) => row.draft_option_combined_zh ?? ""),
        { minPx: 190, maxPx: 520 }
      ),
      price: makeStyle(
        t("draftExplorer.columns.price"),
        sample.map((row) => row.draft_price ?? ""),
        { minPx: 90, maxPx: 150 }
      ),
      weight: makeStyle(
        t("draftExplorer.columns.weight"),
        sample.map((row) =>
          `${row.draft_weight ?? ""}${row.draft_weight_unit ? ` ${row.draft_weight_unit}` : ""}`
        ),
        { minPx: 100, maxPx: 170 }
      ),
      variantImage: makeStyle(
        t("draftExplorer.columns.variantImage"),
        sample.map((row) => row.draft_variant_image_url ?? ""),
        { minPx: 170, maxPx: 430 }
      ),
      status: makeStyle(
        t("draftExplorer.columns.status"),
        sample.map((row) => row.draft_status ?? ""),
        { minPx: 90, maxPx: 150 }
      ),
      updated: makeStyle(
        t("draftExplorer.columns.updated"),
        sample.map((row) => formatDate(row.draft_updated_at)),
        { minPx: 92, maxPx: 124, headerAsMax: true, contentPaddingPx: 12 }
      ),
      details: makeStyle(
        t("draftExplorer.columns.details"),
        sample.map(() => `${detailsExpandLabel} ${detailsCollapseLabel}`),
        { minPx: 106, maxPx: 170 }
      ),
    };
  }, [t, visibleSkuRows]);

  const skuHeaderColumns = useMemo(
    () => [
      {
        key: "sku",
        label: t("draftExplorer.columns.sku"),
        style: skuColumnStyles.sku,
      },
      {
        key: "colorSe",
        label: t("draftExplorer.columns.colorSe"),
        style: skuColumnStyles.colorSe,
      },
      {
        key: "sizeSe",
        label: t("draftExplorer.columns.sizeSe"),
        style: skuColumnStyles.sizeSe,
      },
      {
        key: "otherSe",
        label: t("draftExplorer.columns.otherSe"),
        style: skuColumnStyles.otherSe,
      },
      {
        key: "amountSe",
        label: t("draftExplorer.columns.amountSe"),
        style: skuColumnStyles.amountSe,
      },
      {
        key: "optionCombined",
        label: t("draftExplorer.columns.optionCombined"),
        style: skuColumnStyles.optionCombined,
      },
      {
        key: "price",
        label: t("draftExplorer.columns.price"),
        style: skuColumnStyles.price,
      },
      {
        key: "weight",
        label: t("draftExplorer.columns.weight"),
        style: skuColumnStyles.weight,
      },
      {
        key: "variantImage",
        label: t("draftExplorer.columns.variantImage"),
        style: skuColumnStyles.variantImage,
      },
      {
        key: "status",
        label: t("draftExplorer.columns.status"),
        style: skuColumnStyles.status,
      },
      {
        key: "updated",
        label: t("draftExplorer.columns.updated"),
        style: skuColumnStyles.updated,
      },
      {
        key: "details",
        label: t("draftExplorer.columns.details"),
        style: skuColumnStyles.details,
      },
    ],
    [skuColumnStyles, t]
  );

  const toggleSelectAllSpus = () => {
    if (allSpuSelected) {
      setSelectedSpus(new Set());
      return;
    }
    setSelectedSpus(new Set(visibleSpuRows.map((row) => row.id)));
  };

  const toggleSelectSpu = (id: string) => {
    setSelectedSpus((prev) => {
      const next = new Set(prev);
      if (next.has(id)) {
        next.delete(id);
      } else {
        next.add(id);
      }
      return next;
    });
  };

  const toggleSelectAllSkus = () => {
    if (allSkuSelected) {
      setSelectedSkus(new Set());
      return;
    }
    setSelectedSkus(new Set(visibleSkuRows.map((row) => row.id)));
  };

  const toggleSelectSku = (id: string) => {
    setSelectedSkus((prev) => {
      const next = new Set(prev);
      if (next.has(id)) {
        next.delete(id);
      } else {
        next.add(id);
      }
      return next;
    });
  };

  const buildRawRowWithReviewConfirmation = useCallback(
    (
      rawRow: unknown,
      kind: "variants" | "descriptions",
      atIso?: string
    ): Record<string, unknown> => {
      const baseRaw = parseDraftRawRow(rawRow);
      const reviewRaw =
        toObjectRecordOrNull(baseRaw[DRAFT_EXPLORER_REVIEW_KEY]) ?? {};
      const stamp = atIso ?? new Date().toISOString();
      const reviewKey =
        kind === "variants"
          ? VARIANTS_REVIEWED_AT_KEY
          : DESCRIPTIONS_REVIEWED_AT_KEY;
      return {
        ...baseRaw,
        [DRAFT_EXPLORER_REVIEW_KEY]: {
          ...reviewRaw,
          [reviewKey]: stamp,
        },
      };
    },
    []
  );

  const markSpuReviewConfirmed = useCallback(
    async (spu: string, kind: "variants" | "descriptions") => {
      const targetSpu = String(spu || "").trim();
      if (!targetSpu) return;
      const row = spuRows.find(
        (entry) => String(entry.draft_spu || "").trim() === targetSpu
      );
      if (!row?.id) return;
      const nextRawRow = buildRawRowWithReviewConfirmation(row.draft_raw_row, kind);

      setSpuRows((prev) =>
        prev.map((entry) =>
          entry.id === row.id ? { ...entry, draft_raw_row: nextRawRow } : entry
        )
      );

      try {
        const response = await fetch("/api/drafts/products/bulk-update", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            id: row.id,
            updates: {
              draft_raw_row: nextRawRow,
            },
          }),
        });
        const payload = await response.json().catch(() => ({}));
        if (!response.ok) {
          throw new Error(payload?.error || "Unable to update review marker.");
        }
      } catch (err) {
        setDraftError((err as Error).message);
      }
    },
    [buildRawRowWithReviewConfirmation, spuRows]
  );

  const buildDetailDraft = (row: DraftSpuRow) => ({
    draft_mf_product_short_title: row.draft_mf_product_short_title ?? "",
    draft_mf_product_long_title: row.draft_mf_product_long_title ?? "",
    draft_mf_product_subtitle: row.draft_mf_product_subtitle ?? "",
    draft_mf_product_bullets_short: row.draft_mf_product_bullets_short ?? "",
    draft_mf_product_bullets: row.draft_mf_product_bullets ?? "",
    draft_mf_product_bullets_long: row.draft_mf_product_bullets_long ?? "",
    draft_product_description_main_html:
      row.draft_product_description_main_html ?? "",
    draft_mf_product_description_short_html:
      row.draft_mf_product_description_short_html ?? row.draft_description_html ?? "",
    draft_mf_product_description_extended_html:
      row.draft_mf_product_description_extended_html ?? "",
    draft_description_html:
      row.draft_description_html ??
      row.draft_mf_product_description_short_html ??
      "",
    draft_mf_product_specs: row.draft_mf_product_specs ?? "",
    draft_title: row.draft_title ?? "",
    draft_subtitle: row.draft_subtitle ?? "",
  });

  const updateDetailField = (field: string, value: string) => {
    setDetailDraft((prev) => ({ ...prev, [field]: value }));
    const rawKeys = RAW_ROW_FIELD_MAP[field];
    if (rawKeys && detailRawRow) {
      setDetailRawRow((prev) => {
        if (!prev) return prev;
        const next = { ...prev };
        rawKeys.forEach((key) => {
          next[key] = value;
        });
        return next;
      });
    }
  };

  const openDetails = (row: DraftSpuRow) => {
    setDetailTarget(row);
    setDetailDraft(buildDetailDraft(row));
    setDetailRawRow(row.draft_raw_row ?? null);
    setDetailInstruction("");
    setDetailError(null);
    setDetailOpen(true);
  };

  const resolveDetailFolder = (row: DraftSpuRow) => {
    const rawFolder = row.draft_image_folder ?? "";
    if (!rawFolder) return null;
    const normalized = rawFolder.replace(/^\/+/, "");
    const marker = "images/draft_products/";
    const idx = normalized.indexOf(marker);
    const relative = idx >= 0 ? normalized.slice(idx + marker.length) : normalized;
    return relative || null;
  };

  const fetchDetailImages = useCallback(async (row: DraftSpuRow) => {
    const relative = resolveDetailFolder(row);
    if (!relative) {
      setDetailImages([]);
      return;
    }
    const parts = relative.split("/").filter(Boolean);
    if (parts.length === 0) {
      setDetailImages([]);
      return;
    }
    const [run, ...rest] = parts;
    const subPath = rest.join("/");
    setDetailImagesLoading(true);
    try {
      const url = new URL(
        `/api/drafts/folders/${encodeURIComponent(run)}/list`,
        window.location.origin
      );
      if (subPath) url.searchParams.set("path", subPath);
      const response = await fetch(url.toString());
      if (!response.ok) throw new Error();
      const payload = await response.json();
      const items = (payload.items ?? []) as DraftEntry[];
      const images = items
        .filter(
          (entry) =>
            entry.type === "file" &&
            /\.jpe?g$/i.test(entry.name)
        )
        .sort((a, b) => b.modifiedAt.localeCompare(a.modifiedAt))
        .slice(0, 6);
      setDetailImages(images);
    } catch {
      setDetailImages([]);
    } finally {
      setDetailImagesLoading(false);
    }
  }, []);

  useEffect(() => {
    if (!detailOpen || !detailTarget) return;
    fetchDetailImages(detailTarget);
  }, [detailOpen, detailTarget, fetchDetailImages]);

  const handleOpenSpuJsonViewer = useCallback(async (row: DraftSpuRow) => {
    const spu = String(row.draft_spu || "").trim();
    const folder = tryExtractDraftRelativePath(row.draft_image_folder) ?? "";

    setSpuJsonViewer({
      spu,
      content: "Loading original input JSON...",
      sourceLabel: "Source: resolving original input payload...",
      loading: true,
    });

    try {
      const url = new URL("/api/drafts/source-json", window.location.origin);
      url.searchParams.set("spu", spu);
      if (folder) {
        url.searchParams.set("folder", folder);
      }

      const response = await fetch(url.toString(), { cache: "no-store" });
      if (!response.ok) {
        const payload = await response.json().catch(() => null);
        const errorMessage =
          payload && typeof payload.error === "string"
            ? payload.error
            : `HTTP ${response.status}`;
        throw new Error(errorMessage);
      }

      const payload = (await response.json()) as {
        sourcePath?: string;
        content?: unknown;
      };
      const raw = payload.content && typeof payload.content === "object" ? payload.content : {};
      const sourcePath =
        typeof payload.sourcePath === "string" && payload.sourcePath.trim()
          ? payload.sourcePath.trim()
          : "";

      setSpuJsonViewer({
        spu,
        content: JSON.stringify(raw, null, 2) || "{}",
        sourceLabel: sourcePath
          ? `Source: ${sourcePath}`
          : "Source: original input JSON",
        loading: false,
      });
    } catch {
      const fallback = row.draft_raw_row ?? {};
      setSpuJsonViewer({
        spu,
        content: JSON.stringify(fallback, null, 2) || "{}",
        sourceLabel: "Source: current draft row (fallback)",
        loading: false,
      });
    }
  }, []);

  const normalizeSupplierExternalUrl = useCallback((value: string | null | undefined) => {
    const trimmed = String(value || "").trim();
    if (!trimmed) return "";
    if (/^[a-z][a-z0-9+\-.]*:\/\//i.test(trimmed)) {
      return trimmed;
    }
    return `https://${trimmed}`;
  }, []);

  const handleOpenSupplierLink = useCallback(
    (value: string | null | undefined) => {
      const normalized = normalizeSupplierExternalUrl(value);
      if (!normalized) {
        setDraftError("Supplier URL is empty.");
        return;
      }
      window.open(normalized, "_blank", "noopener,noreferrer");
    },
    [normalizeSupplierExternalUrl]
  );

  const openSupplierEditor = useCallback((row: DraftSpuRow) => {
    setSupplierEditorRowId(row.id);
    setSupplierEditorSpu(String(row.draft_spu || "").trim());
    setSupplierEditorValue(String(row.draft_supplier_1688_url || ""));
    setSupplierEditorError(null);
    setSupplierEditorOpen(true);
  }, []);

  const closeSupplierEditor = useCallback(() => {
    if (supplierEditorSaving) return;
    setSupplierEditorOpen(false);
    setSupplierEditorRowId(null);
    setSupplierEditorSpu("");
    setSupplierEditorValue("");
    setSupplierEditorError(null);
  }, [supplierEditorSaving]);

  const handleSaveSupplierEditor = useCallback(async () => {
    if (!supplierEditorRowId || supplierEditorSaving) return;
    setSupplierEditorSaving(true);
    setSupplierEditorError(null);
    try {
      const nextValueRaw = supplierEditorValue.trim();
      const nextValue = nextValueRaw === "" ? null : nextValueRaw;
      const response = await fetch("/api/drafts/products/update", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          id: supplierEditorRowId,
          field: "draft_supplier_1688_url",
          value: nextValue,
        }),
      });
      if (!response.ok) {
        const message = await response.text();
        throw new Error(message || "Unable to update supplier URL.");
      }
      setSpuRows((prev) =>
        prev.map((row) =>
          row.id === supplierEditorRowId
            ? { ...row, draft_supplier_1688_url: nextValue }
            : row
        )
      );
      setSupplierEditorOpen(false);
      setSupplierEditorRowId(null);
      setSupplierEditorSpu("");
      setSupplierEditorValue("");
      setSupplierEditorError(null);
    } catch (err) {
      setSupplierEditorError((err as Error).message);
    } finally {
      setSupplierEditorSaving(false);
    }
  }, [supplierEditorRowId, supplierEditorSaving, supplierEditorValue]);

  const validateSkuReadinessForSpus = useCallback(
    async (spus: string[]) => {
      const targetSpus = Array.from(
        new Set(spus.map((value) => String(value || "").trim()).filter(Boolean))
      );
      if (targetSpus.length === 0) {
        return { ready: true, missingSkuRows: 0, missingVariantSpus: 0, inspectedRows: 0 };
      }

      const rowsBySpu = new Map<string, DraftSkuRow[]>();
      const collectRows = (rows: DraftSkuRow[]) => {
        for (const row of rows) {
          const spu = String(row?.draft_spu || "").trim();
          if (!spu || !targetSpus.includes(spu)) continue;
          const list = rowsBySpu.get(spu) ?? [];
          list.push(row);
          rowsBySpu.set(spu, list);
        }
      };

      const runFilter = String(selectedFolder || "").trim();
      if (runFilter) {
        const url = new URL("/api/drafts/variants", window.location.origin);
        url.searchParams.set("run", runFilter);
        const response = await fetch(url.toString());
        if (!response.ok) {
          const message = await response.text();
          throw new Error(message || "Unable to verify SKU readiness.");
        }
        const payload = await response.json().catch(() => ({}));
        const items = Array.isArray(payload?.items)
          ? (payload.items as DraftSkuRow[])
          : [];
        collectRows(items);
      } else {
        collectRows(skuRows);
      }

      let missingSkuRows = 0;
      let missingVariantSpus = 0;
      let inspectedRows = 0;
      for (const spu of targetSpus) {
        const rows = rowsBySpu.get(spu) ?? [];
        if (rows.length === 0) {
          missingVariantSpus += 1;
          continue;
        }
        for (const row of rows) {
          inspectedRows += 1;
          if (!String(row?.draft_sku || "").trim()) {
            missingSkuRows += 1;
          }
        }
      }

      return {
        ready: missingSkuRows === 0 && missingVariantSpus === 0,
        missingSkuRows,
        missingVariantSpus,
        inspectedRows,
      };
    },
    [selectedFolder, skuRows]
  );

  const skuReadinessBySpu = useMemo(() => {
    const map = new Map<
      string,
      { totalRows: number; missingSkuRows: number; hasVariants: boolean; ready: boolean }
    >();
    for (const row of skuRows) {
      const spu = String(row?.draft_spu || "").trim();
      if (!spu) continue;
      const current = map.get(spu) ?? {
        totalRows: 0,
        missingSkuRows: 0,
        hasVariants: false,
        ready: false,
      };
      current.totalRows += 1;
      current.hasVariants = true;
      if (!String(row?.draft_sku || "").trim()) {
        current.missingSkuRows += 1;
      }
      current.ready = current.hasVariants && current.missingSkuRows === 0;
      map.set(spu, current);
    }
    return map;
  }, [skuRows]);

  const selectedSpuCodesForPublish = useMemo(
    () =>
      visibleSpuRows
        .filter((row) => selectedSpus.has(row.id))
        .map((row) => String(row.draft_spu || "").trim())
        .filter(Boolean),
    [selectedSpus, visibleSpuRows]
  );

  const selectedSkuGenerationState = useMemo(() => {
    const selectedSpus = selectedSpuCodesForPublish;
    if (selectedSpus.length === 0) {
      return {
        hasSelection: false,
        totalVariants: 0,
        missingSkuRows: 0,
        missingVariantSpus: 0,
        needsGeneration: false,
      };
    }
    let totalVariants = 0;
    let missingSkuRows = 0;
    let missingVariantSpus = 0;
    for (const spu of selectedSpus) {
      const state = skuReadinessBySpu.get(spu);
      if (!state || !state.hasVariants) {
        missingVariantSpus += 1;
        continue;
      }
      totalVariants += state.totalRows;
      missingSkuRows += state.missingSkuRows;
    }
    return {
      hasSelection: true,
      totalVariants,
      missingSkuRows,
      missingVariantSpus,
      needsGeneration: missingVariantSpus > 0 || missingSkuRows > 0,
    };
  }, [selectedSpuCodesForPublish, skuReadinessBySpu]);

  const canGenerateSkusForSelection = useMemo(() => {
    const hasRunSelected = Boolean(String(selectedFolder || "").trim());
    if (!hasRunSelected) return false;
    if (draftTab !== "spu") return false;
    if (!selectedSkuGenerationState.hasSelection) return false;
    return selectedSkuGenerationState.needsGeneration;
  }, [draftTab, selectedFolder, selectedSkuGenerationState]);

  const selectedPublishHasSkuGaps = useMemo(() => {
    if (String(searchQuery || "").trim()) return false;
    if (selectedSpuCodesForPublish.length === 0) return false;
    return selectedSpuCodesForPublish.some((spu) => {
      const state = skuReadinessBySpu.get(spu);
      return !state || !state.ready;
    });
  }, [searchQuery, selectedSpuCodesForPublish, skuReadinessBySpu]);

  const handlePublishDrafts = async () => {
    if (draftTab !== "spu") return;
    setPublishMessage(null);
    if (hasPendingDetailAiRewrite) {
      setPublishStatus("error");
      setPublishMessage(
        "Wait for AI product description updates to finish before saving to database."
      );
      return;
    }
    if (skuStatus === "running") {
      setPublishStatus("error");
      setPublishMessage(t("draftExplorer.publishBlockedSkuRunning"));
      return;
    }
    const runAtStart = String(selectedFolder || "").trim();
    const currentPathAtStart = String(currentPath || "").trim();
    const foldersAtStart = [...folders];
    const runSpuOptionsAtStart = [...runSpuOptions];
    const currentPathParts = currentPathAtStart.split("/").filter(Boolean);
    const currentSpuPathAtStart =
      runAtStart && currentPathParts.length >= 2 && currentPathParts[0] === runAtStart
        ? `${currentPathParts[0]}/${currentPathParts[1]}`
        : "";
    const currentSpuAtStart = currentSpuPathAtStart.split("/")[1] ?? "";
    const selectedRows = visibleSpuRows.filter((row) => selectedSpus.has(row.id));
    const selectedSpuValues = selectedRows
      .map((row) => row.draft_spu)
      .filter(Boolean);
    const targetSpuValues = selectedSpuValues;
    if (targetSpuValues.length === 0) {
      setPublishStatus("error");
      setPublishMessage(
        "Select at least one SPU to save. Use Actions > Save to Database to publish a full draft folder."
      );
      return;
    }

    try {
      const readiness = await validateSkuReadinessForSpus(targetSpuValues);
      if (!readiness.ready) {
        setPublishStatus("error");
        const issueParts: string[] = [];
        if (readiness.missingVariantSpus > 0) {
          issueParts.push(
            `${readiness.missingVariantSpus} product${
              readiness.missingVariantSpus === 1 ? "" : "s"
            } without variants`
          );
        }
        if (readiness.missingSkuRows > 0) {
          issueParts.push(
            `${readiness.missingSkuRows} row${
              readiness.missingSkuRows === 1 ? "" : "s"
            } without SKU`
          );
        }
        setPublishMessage(
          `Selected SPUs still have missing SKU coverage (${issueParts.join(
            ", "
          )}). Generate SKUs for those products and retry.`
        );
        return;
      }
    } catch (err) {
      setPublishStatus("error");
      setPublishMessage((err as Error).message);
      return;
    }

    setPublishStatus("running");

    const confirmSelected = window.confirm(
      t("draftExplorer.publishConfirmSelected", {
        count: targetSpuValues.length,
      })
    );
    if (!confirmSelected) {
      setPublishStatus("idle");
      return;
    }
    try {
      const response = await fetch("/api/drafts/publish", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          spus: targetSpuValues,
        }),
      });
      if (!response.ok) {
        const message = await response.text();
        throw new Error(message || "Publish failed.");
      }
      const payload = await response.json();
      const publishedSpus: string[] = Array.isArray(payload?.spus)
        ? payload.spus
            .map((entry: unknown) => String(entry || "").trim())
            .filter(Boolean)
        : [];
      const publishedSpuSet = new Set(
        publishedSpus.map((value) => value.toUpperCase())
      );
      const count = publishedSpus.length;
      setPublishMessage(t("draftExplorer.publishSuccess", { count }));
      setPublishStatus("done");
      setSelectedSpus(new Set());
      await Promise.all([fetchSpuRows(), fetchSkuRows()]);
      const refreshedFolders = (await fetchFolders()) ?? foldersAtStart;

      const shouldAdvanceFromCurrentSpu =
        Boolean(currentSpuAtStart) &&
        publishedSpuSet.has(currentSpuAtStart.toUpperCase());

      if (shouldAdvanceFromCurrentSpu) {
        const isPublishedRunOption = (option: RunSpuOption) => {
          const parts = String(option.path || "").split("/").filter(Boolean);
          const spu = String(parts[1] || option.name || "").trim();
          return Boolean(spu) && publishedSpuSet.has(spu.toUpperCase());
        };

        let nextSpuPath = "";
        const currentRunSpuIndex = runSpuOptionsAtStart.findIndex(
          (option) => option.path === currentSpuPathAtStart
        );
        if (currentRunSpuIndex >= 0) {
          for (
            let idx = currentRunSpuIndex + 1;
            idx < runSpuOptionsAtStart.length;
            idx += 1
          ) {
            const option = runSpuOptionsAtStart[idx];
            if (!option?.path || isPublishedRunOption(option)) continue;
            nextSpuPath = option.path;
            break;
          }
        }
        if (!nextSpuPath) {
          nextSpuPath =
            runSpuOptionsAtStart.find((option) => !isPublishedRunOption(option))
              ?.path ?? "";
        }

        if (nextSpuPath) {
          pendingFolderOpenPathRef.current = nextSpuPath;
          setCurrentPath(nextSpuPath);
          if (runAtStart) {
            void fetchFolderTree(runAtStart);
          }
          return;
        }

        const refreshedSet = new Set(
          refreshedFolders.map((folder) => folder.path)
        );
        const previousOrder = foldersAtStart.map((folder) => folder.path);
        let nextRunPath = "";
        const startIndex = previousOrder.indexOf(runAtStart);
        if (startIndex >= 0) {
          for (let idx = startIndex + 1; idx < previousOrder.length; idx += 1) {
            const candidate = previousOrder[idx];
            if (refreshedSet.has(candidate)) {
              nextRunPath = candidate;
              break;
            }
          }
        }
        if (!nextRunPath) {
          nextRunPath =
            refreshedFolders.find((folder) => folder.path !== runAtStart)?.path ??
            refreshedFolders[0]?.path ??
            "";
        }

        if (nextRunPath) {
          await handleSelectFolder(nextRunPath);
        } else {
          setSelectedFolder("");
          setCurrentPath("");
          setEntries([]);
          setFolderTree(null);
        }
        return;
      }

      if (currentPathAtStart) {
        await refreshEntries(currentPathAtStart);
        await fetchPendingAiEdits(currentPathAtStart);
      }
      if (runAtStart) {
        await fetchFolderTree(runAtStart);
      }
    } catch (err) {
      setPublishStatus("error");
      setPublishMessage((err as Error).message);
    }
  };

  const handleGenerateSkus = async () => {
    setSkuMessage(null);
    if (!canGenerateSkusForSelection) {
      setSkuStatus("idle");
      return;
    }
    setSkuStatus("running");
    const selectedSpuValues = selectedSpuCodesForPublish;
    if (selectedSpuValues.length === 0) {
      const confirmAll = window.confirm(t("draftExplorer.generateSkuConfirmAll"));
      if (!confirmAll) {
        setSkuStatus("idle");
        return;
      }
    } else {
      const confirmSelected = window.confirm(
        t("draftExplorer.generateSkuConfirmSelected", {
          count: selectedSpuValues.length,
        })
      );
      if (!confirmSelected) {
        setSkuStatus("idle");
        return;
      }
    }
    try {
      const response = await fetch("/api/drafts/sku/generate", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          spus: selectedSpuValues.length ? selectedSpuValues : undefined,
        }),
      });
      if (!response.ok) {
        const message = await response.text();
        throw new Error(message || "SKU generation failed.");
      }
      await fetchSkuStatus();
    } catch (err) {
      setSkuStatus("error");
      setSkuMessage((err as Error).message);
    }
  };

  const handleDeleteRows = async () => {
    if (deleteRowsPending) return;
    const selectedRows =
      draftTab === "spu"
        ? visibleSpuRows.filter((row) => selectedSpus.has(row.id))
        : visibleSkuRows.filter((row) => selectedSkus.has(row.id));
    if (selectedRows.length === 0) return;
    const confirmDelete = window.confirm(
      t("draftExplorer.deleteRowsConfirm", { count: selectedRows.length })
    );
    if (!confirmDelete) return;
    setDeleteRowsPending(true);
    setDraftError(null);
    try {
      if (draftTab === "spu") {
        const selectedSpuValues = selectedRows
          .map((row) => (row as DraftSpuRow).draft_spu)
          .filter(Boolean);
        if (selectedSpuValues.length === 0) {
          setDeleteRowsPending(false);
          return;
        }
        const response = await fetch("/api/drafts/products/delete", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ spus: selectedSpuValues }),
        });
        const payload = await response.json().catch(() => ({}));
        if (!response.ok) {
          throw new Error(
            payload?.error || t("draftExplorer.deleteRowsError")
          );
        }
        setSelectedSpus(new Set());
      } else {
        const selectedSkuIds = selectedRows.map((row) => row.id);
        const selectedSkuIdSet = new Set(
          selectedSkuIds.map((id) => String(id || "").trim()).filter(Boolean)
        );
        const response = await fetch("/api/drafts/variants/delete", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ ids: selectedSkuIds }),
        });
        const payload = await response.json().catch(() => ({}));
        if (!response.ok) {
          throw new Error(
            payload?.error || t("draftExplorer.deleteRowsError")
          );
        }
        if (selectedSkuIdSet.size > 0) {
          setSkuRows((prev) =>
            prev.filter((row) => !selectedSkuIdSet.has(String(row.id || "").trim()))
          );
          setVariantPickerRowsBySpu((prev) => {
            let changed = false;
            const nextEntries = Object.entries(prev).map(([spu, rows]) => {
              const filtered = rows.filter(
                (row) => !selectedSkuIdSet.has(String(row.id || "").trim())
              );
              if (filtered.length !== rows.length) {
                changed = true;
              }
              return [spu, filtered] as const;
            });
            if (!changed) return prev;
            return Object.fromEntries(nextEntries);
          });
        }
        setSelectedSkus(new Set());
      }
      fetchSpuRows();
      fetchSkuRows();
    } catch (err) {
      setDraftError(
        err instanceof Error
          ? err.message
          : t("draftExplorer.deleteRowsError")
      );
    } finally {
      setDeleteRowsPending(false);
    }
  };

  const handleDuplicateSkuRoles = useCallback(async () => {
    if (duplicateRolesPending) return;
    const ids = visibleSkuRows
      .filter((row) => selectedSkus.has(row.id))
      .map((row) => row.id);
    if (ids.length === 0) return;
    setDuplicateRolesPending(true);
    setDraftError(null);
    try {
      const response = await fetch("/api/drafts/variants/duplicate", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ ids }),
      });
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        throw new Error(payload?.error || "Unable to duplicate selected SKU rows.");
      }
      setSelectedSkus(new Set());
      await fetchSkuRows();
      await fetchSpuRows();
    } catch (err) {
      setDraftError(
        err instanceof Error
          ? err.message
          : "Unable to duplicate selected SKU rows."
      );
    } finally {
      setDuplicateRolesPending(false);
    }
  }, [
    duplicateRolesPending,
    fetchSkuRows,
    fetchSpuRows,
    selectedSkus,
    visibleSkuRows,
  ]);

  const handleDuplicateProducts = useCallback(async () => {
    if (duplicateProductPending) return;
    const ids = visibleSpuRows
      .filter((row) => selectedSpus.has(row.id))
      .map((row) => row.id);
    if (ids.length === 0) return;
    setDuplicateProductPending(true);
    setDraftError(null);
    try {
      const response = await fetch("/api/drafts/products/duplicate", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ ids }),
      });
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        throw new Error(payload?.error || "Unable to duplicate selected products.");
      }
      setSelectedSpus(new Set());
      await fetchSpuRows();
      await fetchSkuRows({ silent: true });
    } catch (err) {
      setDraftError(
        err instanceof Error
          ? err.message
          : "Unable to duplicate selected products."
      );
    } finally {
      setDuplicateProductPending(false);
    }
  }, [
    duplicateProductPending,
    fetchSkuRows,
    fetchSpuRows,
    selectedSpus,
    visibleSpuRows,
  ]);

  const handleDetailSave = async () => {
    if (!detailTarget || detailSaving) return;
    setDetailSaving(true);
    setDetailError(null);
    try {
      const reviewedRawRow = buildRawRowWithReviewConfirmation(
        detailRawRow ?? detailTarget.draft_raw_row,
        "descriptions"
      );
      const updates: Record<string, unknown> = {
        draft_title:
          detailDraft.draft_mf_product_long_title ||
          detailDraft.draft_mf_product_short_title ||
          detailDraft.draft_title,
        draft_subtitle:
          detailDraft.draft_mf_product_subtitle || detailDraft.draft_subtitle,
        draft_description_html:
          detailDraft.draft_mf_product_description_short_html ||
          detailDraft.draft_description_html,
        draft_product_description_main_html:
          detailDraft.draft_product_description_main_html,
        draft_mf_product_description_short_html:
          detailDraft.draft_mf_product_description_short_html,
        draft_mf_product_description_extended_html:
          detailDraft.draft_mf_product_description_extended_html,
        draft_mf_product_short_title: detailDraft.draft_mf_product_short_title,
        draft_mf_product_long_title: detailDraft.draft_mf_product_long_title,
        draft_mf_product_subtitle: detailDraft.draft_mf_product_subtitle,
        draft_mf_product_bullets_short: detailDraft.draft_mf_product_bullets_short,
        draft_mf_product_bullets: detailDraft.draft_mf_product_bullets,
        draft_mf_product_bullets_long: detailDraft.draft_mf_product_bullets_long,
        draft_mf_product_specs: detailDraft.draft_mf_product_specs,
        draft_raw_row: reviewedRawRow,
      };
      const response = await fetch("/api/drafts/products/bulk-update", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ id: detailTarget.id, updates }),
      });
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        throw new Error(payload?.error || "Save failed.");
      }
      setSpuRows((prev) =>
        prev.map((row) =>
          row.id === detailTarget.id
            ? {
                ...row,
                draft_raw_row: reviewedRawRow,
              }
            : row
        )
      );
      setDetailOpen(false);
      setDetailTarget(null);
      fetchSpuRows();
    } catch (err) {
      setDetailError(err instanceof Error ? err.message : "Save failed.");
    } finally {
      setDetailSaving(false);
    }
  };

  const handleDetailRegenerate = async (mode: "stay" | "close") => {
    if (!detailTarget || detailRegenerating) return;
    const instruction = detailInstruction.trim();
    if (!instruction) {
      setDetailError(t("draftExplorer.detailsDialog.instructionRequired"));
      return;
    }
    const targetId = detailTarget.id;
    const backgroundMode = mode === "close";
    setDetailRegenerating(true);
    if (backgroundMode) {
      setDetailBackgroundRegeneratingRowIds((prev) => {
        const next = new Set(prev);
        next.add(targetId);
        return next;
      });
    }
    setDetailError(null);
    if (mode === "close") {
      setDetailOpen(false);
    }
    try {
      const response = await fetch("/api/drafts/products/rewrite", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          id: targetId,
          instruction,
        }),
      });
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        throw new Error(payload?.error || "Rewrite failed.");
      }
      const updates = payload?.updates ?? {};
      const rawRow = payload?.raw_row ?? null;

      if (mode === "close") {
        const saveUpdates: Record<string, unknown> = { ...updates };
        saveUpdates.draft_raw_row = buildRawRowWithReviewConfirmation(
          rawRow ?? detailRawRow ?? detailTarget.draft_raw_row,
          "descriptions"
        );
        const saveResponse = await fetch("/api/drafts/products/bulk-update", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ id: targetId, updates: saveUpdates }),
        });
        const savePayload = await saveResponse.json().catch(() => ({}));
        if (!saveResponse.ok) {
          throw new Error(savePayload?.error || "Save failed.");
        }
        setSpuRows((prev) =>
          prev.map((row) =>
            row.id === targetId
              ? {
                  ...row,
                  draft_raw_row: saveUpdates.draft_raw_row as Record<string, unknown>,
                }
              : row
          )
        );
        fetchSpuRows();
      } else {
        setDetailDraft((prev) => ({ ...prev, ...updates }));
        if (rawRow) {
          setDetailRawRow(rawRow);
        }
      }
    } catch (err) {
      const message = err instanceof Error ? err.message : "Rewrite failed.";
      if (mode === "close") {
        setDraftError(message);
      } else {
        setDetailError(message);
      }
    } finally {
      setDetailRegenerating(false);
      if (backgroundMode) {
        setDetailBackgroundRegeneratingRowIds((prev) => {
          if (!prev.has(targetId)) return prev;
          const next = new Set(prev);
          next.delete(targetId);
          return next;
        });
      }
    }
  };

  const closeDetails = () => {
    setDetailOpen(false);
    setDetailTarget(null);
    setDetailDraft({});
    setDetailRawRow(null);
    setDetailInstruction("");
    setDetailImages([]);
    setDetailError(null);
  };

  const handleRerunSkuImages = async () => {
    setSkuMessage(null);
    setSkuStatus("running");
    const selectedRows = visibleSpuRows.filter((row) => selectedSpus.has(row.id));
    const selectedSpuValues = selectedRows
      .map((row) => row.draft_spu)
      .filter(Boolean);
    if (selectedSpuValues.length === 0) {
      const confirmAll = window.confirm(
        t("draftExplorer.rerunSkuConfirmAll")
      );
      if (!confirmAll) {
        setSkuStatus("idle");
        return;
      }
    } else {
      const confirmSelected = window.confirm(
        t("draftExplorer.rerunSkuConfirmSelected", {
          count: selectedSpuValues.length,
        })
      );
      if (!confirmSelected) {
        setSkuStatus("idle");
        return;
      }
    }
    try {
      await runSkuPipelineForSpus(selectedSpuValues);
    } catch (err) {
      setSkuStatus("error");
      setSkuMessage((err as Error).message);
    }
  };

  const handleImport = async (type: "excel" | "zip"): Promise<boolean> => {
    const file = type === "excel" ? excelFile : zipFile;
    if (!file) return true;
    type === "excel" ? setExcelStatus("uploading") : setZipStatus("uploading");
    try {
      type ImportCollisionStrategy =
        | "ask"
        | "skip"
        | "replace"
        | "create_revision";
      let strategy: ImportCollisionStrategy = "ask";
      let replaceConfirmed = false;
      let payload: Record<string, unknown> | null = null;

      // Collision-aware import loop. We retry with user-selected strategy when
      // the API reports SPU collisions.
      while (true) {
        const formData = new FormData();
        if (type === "excel") {
          formData.append("workbook", file);
        } else {
          formData.append("images_zip", file);
        }
        formData.append("purgeMissing", "false");
        formData.append("collisionStrategy", strategy);
        if (replaceConfirmed) {
          formData.append("replaceConfirmed", "true");
        }

        const response = await fetch("/api/drafts/import", {
          method: "POST",
          body: formData,
        });
        const parsed = (await response.json().catch(() => null)) as
          | Record<string, unknown>
          | null;

        if (response.status === 409 && parsed?.code === "draft_spu_collision") {
          const collisions = Array.isArray(parsed?.collisionSpus)
            ? (parsed.collisionSpus as string[])
            : [];
          const collisionPreview =
            collisions.length > 6
              ? `${collisions.slice(0, 6).join(", ")} +${collisions.length - 6} more`
              : collisions.join(", ");
          const choice = window.prompt(
            [
              `SPU collision detected (${collisions.length}).`,
              collisionPreview ? `SPUs: ${collisionPreview}` : "",
              "Type one option:",
              "- skip (recommended)",
              "- create_revision (backup + replace)",
              "- replace (overwrite existing)",
              "- cancel",
            ]
              .filter(Boolean)
              .join("\n"),
            "skip"
          );
          const normalizedChoice = String(choice || "")
            .trim()
            .toLowerCase();
          if (!normalizedChoice || normalizedChoice === "cancel") {
            throw new Error("Import cancelled.");
          }
          if (
            normalizedChoice !== "skip" &&
            normalizedChoice !== "replace" &&
            normalizedChoice !== "create_revision"
          ) {
            throw new Error(
              "Invalid collision choice. Use skip, create_revision, or replace."
            );
          }
          strategy = normalizedChoice as ImportCollisionStrategy;
          if (strategy === "replace") {
            replaceConfirmed = window.confirm(
              `Replace ${collisions.length} existing draft SPU(s)? This will overwrite current SPU/SKU draft data for those SPUs.`
            );
            if (!replaceConfirmed) {
              throw new Error("Import cancelled.");
            }
          } else {
            replaceConfirmed = false;
          }
          continue;
        }

        if (!response.ok) {
          const apiError =
            (parsed && typeof parsed.error === "string" && parsed.error) || "";
          const message = apiError || `Import failed (HTTP ${response.status}).`;
          throw new Error(message);
        }

        payload = parsed;
        break;
      }

      if (!payload) {
        throw new Error("Import failed: empty response payload.");
      }
      const errors = Array.isArray(payload?.errors)
        ? payload.errors.filter((entry: unknown) => Boolean(entry))
        : [];
      const skippedCollisionSpus = Array.isArray(payload?.skippedCollisionSpus)
        ? (payload.skippedCollisionSpus as string[])
        : [];
      if (errors.length > 0) {
        setError(`Import completed with errors: ${errors.join(" | ")}`);
        type === "excel" ? setExcelStatus("error") : setZipStatus("error");
      } else {
        type === "excel" ? setExcelStatus("done") : setZipStatus("done");
        if (skippedCollisionSpus.length > 0) {
          setError(
            `Import completed. Skipped ${skippedCollisionSpus.length} existing draft SPU(s): ${skippedCollisionSpus.join(
              ", "
            )}`
          );
        }
      }
      if (draftTab === "spu") {
        fetchSpuRows();
      } else {
        fetchSkuRows();
      }
      return errors.length === 0;
    } catch (err) {
      type === "excel" ? setExcelStatus("error") : setZipStatus("error");
      setError((err as Error).message);
      return false;
    }
  };

  const handleSubmitImportEditsDialog = useCallback(async () => {
    if (importEditsSubmitting) return;
    if (!excelFile && !zipFile) {
      setError("Upload an Excel file and optionally a ZIP file.");
      return;
    }
    setImportEditsSubmitting(true);
    setError(null);
    try {
      let allSucceeded = true;
      if (excelFile) {
        allSucceeded = (await handleImport("excel")) && allSucceeded;
      }
      if (zipFile) {
        allSucceeded = (await handleImport("zip")) && allSucceeded;
      }
      if (allSucceeded) {
        setImportEditsDialogOpen(false);
        setExcelFile(null);
        setZipFile(null);
        setExcelStatus("idle");
        setZipStatus("idle");
        if (importEditsExcelInputRef.current) {
          importEditsExcelInputRef.current.value = "";
        }
        if (importEditsZipInputRef.current) {
          importEditsZipInputRef.current.value = "";
        }
      }
    } finally {
      setImportEditsSubmitting(false);
    }
  }, [excelFile, handleImport, importEditsSubmitting, zipFile]);

  const listPathEntries = useCallback(async (pathValue: string) => {
    if (!pathValue) return [] as DraftEntry[];
    const [run, ...rest] = pathValue.split("/");
    const subPath = rest.join("/");
    const url = new URL(
      `/api/drafts/folders/${encodeURIComponent(run)}/list`,
      window.location.origin
    );
    if (subPath) {
      url.searchParams.set("path", subPath);
    }
    const response = await fetch(url.toString());
    if (!response.ok) {
      const text = await response.text();
      throw new Error(text || `Unable to load folder (HTTP ${response.status})`);
    }
    const payload = await response.json();
    return (payload.items ?? []) as DraftEntry[];
  }, []);

  const listImageFileEntriesForPath = useCallback(
    async (pathValue: string) => {
      const items = await listPathEntries(pathValue);
      return items.filter((entry) => entry.type === "file" && isImage(entry.name));
    },
    [isImage, listPathEntries]
  );

  const fetchFolderTree = useCallback(async (pathValue: string) => {
    if (!pathValue) {
      setFolderTree(null);
      return;
    }
    setFolderTreeLoading(true);
    try {
      const [run, ...rest] = pathValue.split("/");
      const subPath = rest.join("/");
      const url = new URL(
        `/api/drafts/folders/${encodeURIComponent(run)}/tree`,
        window.location.origin
      );
      if (subPath) {
        url.searchParams.set("path", subPath);
      }
      const response = await fetch(url.toString());
      if (!response.ok) {
        throw new Error();
      }
      const payload = await response.json();
      setFolderTree((payload.root ?? null) as DraftFolderTreeNode | null);
    } catch {
      setFolderTree(null);
    } finally {
      setFolderTreeLoading(false);
    }
  }, []);

  const detectRunType = useCallback((value: string) => {
    const normalized = String(value || "")
      .trim()
      .toLowerCase()
      .replace(/[_-]+/g, " ");
    if (!normalized) return "draft" as const;
    if (normalized.includes("archive")) return "archive" as const;
    if (
      /^re\s*edit(?:ing)?\b/.test(normalized) ||
      /\bre\s*edit(?:ing)?\b/.test(normalized)
    ) {
      return "re_edit" as const;
    }
    return "draft" as const;
  }, []);

  const runTypeByPath = useMemo(() => {
    const map = new Map<string, "draft" | "re_edit" | "archive">();
    folders.forEach((folder) => {
      const runType = folder.run_type ?? detectRunType(`${folder.name} ${folder.path}`);
      map.set(folder.path, runType);
    });
    return map;
  }, [detectRunType, folders]);

  const getRunTypeForPath = useCallback(
    (value: string) => {
      if (runTypeByPath.has(value)) {
        return runTypeByPath.get(value) ?? "draft";
      }
      const matchedFolder = folders.find((folder) => folder.path === value);
      if (matchedFolder) {
        return (
          matchedFolder.run_type ??
          detectRunType(`${matchedFolder.name} ${matchedFolder.path}`)
        );
      }
      return detectRunType(value);
    },
    [detectRunType, folders, runTypeByPath]
  );

  const isArchiveFolder = useCallback(
    (value: string) => getRunTypeForPath(value) === "archive",
    [getRunTypeForPath]
  );
  const isReEditFolder = useCallback(
    (value: string) => getRunTypeForPath(value) === "re_edit",
    [getRunTypeForPath]
  );

  const isChunksDirectory = useCallback((value: string) => {
    const normalized = value.toLowerCase().replace(/[\s_-]+/g, "");
    return normalized === "chunks";
  }, []);

  const pickPreferredRunFolder = useCallback(
    (items: DraftFolder[]) => {
      if (items.length === 0) return null;
      const byRecencyDesc = (left: DraftFolder, right: DraftFolder) => {
        const leftRecency = getDraftFolderRecencyMs(left);
        const rightRecency = getDraftFolderRecencyMs(right);
        if (leftRecency !== rightRecency) {
          return rightRecency - leftRecency;
        }
        return String(right.path || "").localeCompare(String(left.path || ""), undefined, {
          numeric: true,
          sensitivity: "base",
        });
      };

      const nonArchive = items.filter((item) => !isArchiveFolder(item.path));
      const pool = nonArchive.length > 0 ? nonArchive : items;
      const draftOnly = pool.filter((item) => !isReEditFolder(item.path));
      const draftPool = draftOnly.length > 0 ? draftOnly : pool;
      const draftedProducts = draftPool.filter((item) =>
        /\bdraft(?:ed)?[-_\s]*products?\b/i.test(`${item.name} ${item.path}`)
      );
      const preferredPool = draftedProducts.length > 0 ? draftedProducts : draftPool;
      return [...preferredPool].sort(byRecencyDesc)[0] ?? null;
    },
    [isArchiveFolder, isReEditFolder]
  );

  const resolveInitialExplorerPath = useCallback(
    async (runPath: string) => {
      if (!runPath) return "";
      try {
        const rootEntries = await listPathEntries(runPath);
        const preferredDir = rootEntries
          .filter((entry) => entry.type === "dir" && !isChunksDirectory(entry.name))
          .sort((left, right) =>
            left.name.localeCompare(right.name, undefined, {
              numeric: true,
              sensitivity: "base",
            })
          )[0];
        return preferredDir?.path ?? runPath;
      } catch {
        return runPath;
      }
    },
    [isChunksDirectory, listPathEntries]
  );

  const resolveStoredExplorerPathForRun = useCallback(
    async (runPath: string, storedPath: string) => {
      const normalizedRunPath = normalizeExplorerLocationPath(runPath);
      const normalizedStoredPath = normalizeExplorerLocationPath(storedPath);
      if (!normalizedRunPath) return "";
      if (
        !normalizedStoredPath ||
        (normalizedStoredPath !== normalizedRunPath &&
          !normalizedStoredPath.startsWith(`${normalizedRunPath}/`))
      ) {
        return resolveInitialExplorerPath(normalizedRunPath);
      }

      const pathParts = normalizedStoredPath.split("/").filter(Boolean);
      while (pathParts.length > 0) {
        const candidate = pathParts.join("/");
        try {
          await listPathEntries(candidate);
          return candidate;
        } catch {
          pathParts.pop();
        }
      }
      return resolveInitialExplorerPath(normalizedRunPath);
    },
    [listPathEntries, resolveInitialExplorerPath]
  );

  const resolveRequestedExplorerPathForRun = useCallback(
    async (runPath: string) => {
      const normalizedRunPath = normalizeExplorerLocationPath(runPath);
      if (!normalizedRunPath) return "";
      const requested = initialUrlStateRef.current;
      if (!requested) {
        return resolveInitialExplorerPath(normalizedRunPath);
      }

      const validatePathWithinRun = async (candidatePath: string) => {
        const normalizedCandidate = normalizeExplorerLocationPath(candidatePath);
        if (!normalizedCandidate) return null;
        if (
          normalizedCandidate !== normalizedRunPath &&
          !normalizedCandidate.startsWith(`${normalizedRunPath}/`)
        ) {
          return null;
        }
        try {
          await listPathEntries(normalizedCandidate);
          return normalizedCandidate;
        } catch {
          return null;
        }
      };

      const requestedPath = normalizeExplorerLocationPath(requested.path);
      if (
        requestedPath &&
        (requestedPath === normalizedRunPath ||
          requestedPath.startsWith(`${normalizedRunPath}/`))
      ) {
        const pathParts = requestedPath.split("/").filter(Boolean);
        while (pathParts.length > 0) {
          const candidate = pathParts.join("/");
          const validated = await validatePathWithinRun(candidate);
          if (validated) {
            return validated;
          }
          pathParts.pop();
        }
      }

      const requestedSpu = normalizeDraftSpuCode(requested.spu);
      if (requestedSpu) {
        const requestedSpuPath = `${normalizedRunPath}/${requestedSpu}`;
        const validated = await validatePathWithinRun(requestedSpuPath);
        if (validated) {
          return validated;
        }
      }

      return resolveInitialExplorerPath(normalizedRunPath);
    },
    [listPathEntries, resolveInitialExplorerPath]
  );

  const fetchFolders = useCallback(async () => {
    const requestSeq = foldersRequestSeqRef.current + 1;
    foldersRequestSeqRef.current = requestSeq;
    try {
      const response = await fetch("/api/drafts/folders");
      if (!response.ok) return null;
      const payload = await response.json();
      if (requestSeq !== foldersRequestSeqRef.current) {
        return null;
      }
      const items = (payload.items ?? []) as DraftFolder[];
      setFolders(items);
      if (items.length === 0) {
        setSelectedFolder("");
        setCurrentPath("");
        setFolderTree(null);
        return items;
      }
      const currentSelectedFolder = String(selectedFolderRef.current || "").trim();
      const selectedStillExists = items.some((item) => item.path === currentSelectedFolder);
      if (currentSelectedFolder && selectedStillExists) {
        return items;
      }

      let nextRunPath = "";
      const requested = initialUrlStateRef.current;
      if (requested) {
        const requestedRunCandidates = Array.from(
          new Set(
            [requested.run, parseRunFromExplorerPath(requested.path)]
              .map((value) => normalizeExplorerLocationPath(value))
              .filter(Boolean)
          )
        );
        for (const candidate of requestedRunCandidates) {
          const match = items.find((item) => item.path === candidate);
          if (match) {
            nextRunPath = match.path;
            break;
          }
        }
      }
      if (!nextRunPath) {
        const hasExplicitUrlLocation =
          Boolean(normalizeExplorerLocationPath(requested?.run || "")) ||
          Boolean(normalizeExplorerLocationPath(requested?.path || "")) ||
          Boolean(normalizeDraftSpuCode(requested?.spu || ""));
        if (!hasExplicitUrlLocation) {
          try {
            const rawLastLocation = window.localStorage.getItem(
              DRAFT_EXPLORER_LAST_LOCATION_KEY
            );
            if (rawLastLocation) {
              const parsedLastLocation = parseDraftExplorerLastLocation(
                JSON.parse(rawLastLocation)
              );
              const storedRun = String(parsedLastLocation?.run || "").trim();
              if (storedRun) {
                const match = items.find((item) => item.path === storedRun);
                if (match) {
                  nextRunPath = match.path;
                }
              }
            }
          } catch {
            // Ignore storage/parse issues and fall back to normal selection.
          }
        }
      }
      if (!nextRunPath) {
        const preferredRun = pickPreferredRunFolder(items);
        nextRunPath = preferredRun?.path ?? items[0]?.path ?? "";
      }
      if (!nextRunPath) return items;

      let nextPath = "";
      const hasExplicitUrlLocation =
        Boolean(normalizeExplorerLocationPath(requested?.run || "")) ||
        Boolean(normalizeExplorerLocationPath(requested?.path || "")) ||
        Boolean(normalizeDraftSpuCode(requested?.spu || ""));
      if (!hasExplicitUrlLocation) {
        try {
          const rawLastLocation = window.localStorage.getItem(
            DRAFT_EXPLORER_LAST_LOCATION_KEY
          );
          if (rawLastLocation) {
            const parsedLastLocation = parseDraftExplorerLastLocation(
              JSON.parse(rawLastLocation)
            );
            if (parsedLastLocation?.run === nextRunPath) {
              nextPath = await resolveStoredExplorerPathForRun(
                nextRunPath,
                parsedLastLocation.path
              );
            }
          }
        } catch {
          // Ignore storage/parse issues and use standard path resolution below.
        }
      }
      if (!nextPath) {
        nextPath = await resolveRequestedExplorerPathForRun(nextRunPath);
      }
      if (requestSeq !== foldersRequestSeqRef.current) {
        return null;
      }
      pendingFolderOpenPathRef.current = nextPath;
      setSelectedFolder(nextRunPath);
      setCurrentPath(nextPath);
      initialUrlStateRef.current = null;
      return items;
    } catch {
      return null;
    }
  }, [
    pickPreferredRunFolder,
    resolveRequestedExplorerPathForRun,
    resolveStoredExplorerPathForRun,
  ]);

  const fetchEntries = useCallback(
    async (pathValue: string, options?: { variantsPath?: string | null }) => {
      if (!pathValue) {
        setEntries([]);
        setMainViewVariantImageEntries([]);
        return;
      }
      const normalizedVariantsPath = String(options?.variantsPath || "").trim();
      const shouldLoadVariants = Boolean(normalizedVariantsPath);
      if (shouldLoadVariants) {
        const cachedVariantEntries = variantEntriesCacheRef.current.get(normalizedVariantsPath);
        setMainViewVariantImageEntries(cachedVariantEntries ? [...cachedVariantEntries] : []);
      } else {
        setMainViewVariantImageEntries([]);
      }
      const requestSeq = entriesRequestSeqRef.current + 1;
      entriesRequestSeqRef.current = requestSeq;
      setEntriesLoading(true);
      try {
        const [items, variantImages] = await Promise.all([
          listPathEntries(pathValue),
          shouldLoadVariants
            ? listImageFileEntriesForPath(normalizedVariantsPath).catch(
                () => [] as DraftEntry[]
              )
            : Promise.resolve([] as DraftEntry[]),
        ]);
        if (entriesRequestSeqRef.current !== requestSeq) return;
        setEntries(items);
        if (shouldLoadVariants) {
          variantEntriesCacheRef.current.set(normalizedVariantsPath, variantImages);
          setMainViewVariantImageEntries(variantImages);
        } else {
          setMainViewVariantImageEntries([]);
        }
        clearSpuNeedsAiReview(pathValue);
        setSelectedFiles(new Set());
        setPreviewPath(null);
      } catch {
        if (entriesRequestSeqRef.current !== requestSeq) return;
        setEntries([]);
        if (shouldLoadVariants) {
          variantEntriesCacheRef.current.set(normalizedVariantsPath, []);
        }
        setMainViewVariantImageEntries([]);
      } finally {
        if (entriesRequestSeqRef.current === requestSeq) {
          setEntriesLoading(false);
        }
      }
    },
    [clearSpuNeedsAiReview, listImageFileEntriesForPath, listPathEntries]
  );

  const refreshEntries = useCallback(
    async (pathValue: string) => {
      if (!pathValue || entriesRefreshing) return;
      const normalizedPath = String(pathValue || "").trim();
      const activePath = String(currentPathRef.current || "").trim();
      const normalizedMainPath = String(currentMainPathRef.current || "").trim();
      const normalizedVariantsPath = String(currentVariantsPathRef.current || "").trim();
      const shouldLoadVariants =
        Boolean(normalizedPath) &&
        Boolean(normalizedMainPath) &&
        Boolean(normalizedVariantsPath) &&
        normalizedPath === normalizedMainPath &&
        activePath === normalizedMainPath;
      if (shouldLoadVariants) {
        const cachedVariantEntries = variantEntriesCacheRef.current.get(
          normalizedVariantsPath
        );
        setMainViewVariantImageEntries(cachedVariantEntries ? [...cachedVariantEntries] : []);
      }
      setEntriesRefreshing(true);
      try {
        const [items, variantImages] = await Promise.all([
          listPathEntries(pathValue),
          shouldLoadVariants
            ? listImageFileEntriesForPath(normalizedVariantsPath).catch(
                () => [] as DraftEntry[]
              )
            : Promise.resolve([] as DraftEntry[]),
        ]);
        const available = new Set(items.map((entry) => entry.path));
        const includeMergedVisiblePaths =
          activePath === normalizedPath;
        const mergedVisiblePaths = includeMergedVisiblePaths
          ? new Set(displayImageEntriesRef.current.map((entry) => entry.path))
          : new Set<string>();
        if (shouldLoadVariants) {
          variantEntriesCacheRef.current.set(normalizedVariantsPath, variantImages);
          variantImages.forEach((entry) => mergedVisiblePaths.add(entry.path));
          setMainViewVariantImageEntries(variantImages);
        }
        setEntries(items);
        setSelectedFiles((prev) => {
          if (prev.size === 0) return prev;
          const next = new Set<string>();
          prev.forEach((value) => {
            if (available.has(value)) next.add(value);
          });
          return next;
        });
        setPreviewPath((prev) => (prev && available.has(prev) ? prev : null));
        setImageDimensions((prev) => {
          const next: Record<string, { width: number; height: number }> = {};
          Object.entries(prev).forEach(([pathValue, dims]) => {
            if (available.has(pathValue) || mergedVisiblePaths.has(pathValue)) {
              next[pathValue] = dims;
            }
          });
          return next;
        });
      } catch {
        // Keep previous entries on refresh failure to avoid flicker.
      } finally {
        setEntriesRefreshing(false);
      }
    },
    [entriesRefreshing, listImageFileEntriesForPath, listPathEntries]
  );

  const refreshImageScoresForPaths = useCallback(
    async (paths: string[]) => {
      const normalizedPaths = Array.from(
        new Set(
          (Array.isArray(paths) ? paths : [])
            .map((value) => String(value || "").trim())
            .filter(Boolean)
        )
      );
      if (normalizedPaths.length === 0) return;

      setPendingImageScorePaths((prev) => {
        const next = new Set(prev);
        normalizedPaths.forEach((pathValue) => next.add(pathValue));
        return next;
      });

      try {
        const response = await fetch("/api/drafts/images/refresh-scores", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ paths: normalizedPaths }),
        });
        const payload = await response.json().catch(() => ({}));
        if (!response.ok) {
          throw new Error(payload?.error || "Unable to refresh image scores.");
        }

        const refreshedScoresRaw = Array.isArray(payload?.refreshedScores)
          ? (payload.refreshedScores as Array<{ path?: unknown; pixelQualityScore?: unknown }>)
          : [];
        const refreshedScoreByPath = new Map<string, number | null>();
        refreshedScoresRaw.forEach((row) => {
          const pathValue = String(row?.path || "").trim();
          if (!pathValue) return;
          const scoreValue =
            typeof row?.pixelQualityScore === "number" &&
            Number.isFinite(row.pixelQualityScore)
              ? Math.round(row.pixelQualityScore)
              : null;
          refreshedScoreByPath.set(pathValue, scoreValue);
        });

        if (refreshedScoreByPath.size > 0) {
          const now = new Date().toISOString();
          setEntries((prev) =>
            prev.map((item) => {
              if (!refreshedScoreByPath.has(item.path)) return item;
              return {
                ...item,
                modifiedAt: now,
                pixelQualityScore: refreshedScoreByPath.get(item.path) ?? null,
              };
            })
          );
          setMainViewVariantImageEntries((prev) =>
            prev.map((item) => {
              if (!refreshedScoreByPath.has(item.path)) return item;
              return {
                ...item,
                modifiedAt: now,
                pixelQualityScore: refreshedScoreByPath.get(item.path) ?? null,
              };
            })
          );
        }
      } catch (err) {
        setError((err as Error).message || "Unable to refresh image scores.");
      } finally {
        setPendingImageScorePaths((prev) => {
          const next = new Set(prev);
          normalizedPaths.forEach((pathValue) => next.delete(pathValue));
          return next;
        });
      }

      const activePath = String(currentPathRef.current || "").trim();
      if (activePath) {
        void refreshEntries(activePath);
      }
    },
    [refreshEntries]
  );

  const postImageOrderForFolder = useCallback(
    async (folderPath: string, orderedPaths: string[]) => {
      const response = await fetch("/api/drafts/images/order", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          folderPath,
          orderedPaths,
        }),
      });

      const raw = await response.text();
      let payload: Record<string, unknown> = {};
      if (raw) {
        try {
          payload = JSON.parse(raw) as Record<string, unknown>;
        } catch {
          payload = {};
        }
      }

      if (!response.ok) {
        const payloadError =
          typeof payload?.error === "string" ? payload.error.trim() : "";
        const fallbackError = raw.trim();
        throw new Error(
          payloadError ||
            fallbackError ||
            `Unable to save image order (HTTP ${response.status}).`
        );
      }
    },
    []
  );

  const saveImageOrderForFolder = useCallback(
    async (folderPath: string, orderedPaths: string[]) => {
      const normalizedFolderPath = String(folderPath || "").trim();
      if (!normalizedFolderPath) return;

      const normalizeOrderedPaths = (values: string[]) =>
        Array.from(
          new Set(
            (Array.isArray(values) ? values : [])
              .map((value) => String(value || "").trim())
              .filter(Boolean)
          )
        );

      let candidateOrder = normalizeOrderedPaths(orderedPaths);
      let lastError: Error | null = null;

      for (let attempt = 0; attempt < 2; attempt += 1) {
        try {
          await postImageOrderForFolder(normalizedFolderPath, candidateOrder);
          return;
        } catch (error) {
          lastError =
            error instanceof Error
              ? error
              : new Error("Unable to save image order.");
          if (attempt >= 1) break;

          await new Promise((resolve) => setTimeout(resolve, 160 * (attempt + 1)));

          try {
            const refreshedEntries = await listPathEntries(normalizedFolderPath);
            const availableImagePaths = refreshedEntries
              .filter((entry) => entry.type === "file" && isImage(entry.name))
              .map((entry) => entry.path);
            if (availableImagePaths.length > 0) {
              const availableSet = new Set(availableImagePaths);
              const filtered = candidateOrder.filter((pathValue) =>
                availableSet.has(pathValue)
              );
              candidateOrder =
                filtered.length > 0 ? filtered : [...availableImagePaths];
            } else {
              candidateOrder = [];
            }
          } catch {
            // Keep existing candidate order if refresh fails.
          }
        }
      }

      throw lastError ?? new Error("Unable to save image order.");
    },
    [isImage, listPathEntries, postImageOrderForFolder]
  );

  const enqueueImageOrderSaveForFolder = useCallback(
    async (folderPath: string, orderedPaths: string[]) => {
      const normalizedFolderPath = String(folderPath || "").trim();
      if (!normalizedFolderPath) return;

      const queue = imageOrderSaveQueueRef.current;
      const previous = queue.get(normalizedFolderPath) ?? Promise.resolve();
      const next = previous
        .catch(() => undefined)
        .then(() => saveImageOrderForFolder(normalizedFolderPath, orderedPaths));
      queue.set(normalizedFolderPath, next);
      try {
        await next;
      } finally {
        if (queue.get(normalizedFolderPath) === next) {
          queue.delete(normalizedFolderPath);
        }
      }
    },
    [saveImageOrderForFolder]
  );

  const resortImageFoldersByHierarchy = useCallback(
    async (folderPaths: Iterable<string>) => {
      const normalizedFolders = Array.from(
        new Set(
          Array.from(folderPaths)
            .map((pathValue) => String(pathValue || "").trim())
            .filter(Boolean)
        )
      );
      if (normalizedFolders.length === 0) {
        return [] as string[];
      }

      const failures: string[] = [];
      for (const folderPath of normalizedFolders) {
        try {
          const folderEntries = await listPathEntries(folderPath);
          const folderImageEntries = folderEntries.filter(
            (entry) => entry.type === "file" && isImage(entry.name)
          );
          if (folderImageEntries.length <= 1) continue;

          const currentOrder = folderImageEntries.map((entry) => entry.path);
          const nextOrder = buildTaggedImageOrderPaths(folderImageEntries);
          if (nextOrder.length !== currentOrder.length) continue;
          const changed = nextOrder.some(
            (pathValue, index) => pathValue !== currentOrder[index]
          );
          if (!changed) continue;

          await enqueueImageOrderSaveForFolder(folderPath, nextOrder);

          if (String(currentPathRef.current || "").trim() === folderPath) {
            const byPath = new Map(folderEntries.map((entry) => [entry.path, entry]));
            const dirs = folderEntries.filter((entry) => entry.type === "dir");
            const nonImageFiles = folderEntries.filter(
              (entry) => entry.type === "file" && !isImage(entry.name)
            );
            const sortedImages = nextOrder
              .map((pathValue) => byPath.get(pathValue))
              .filter(
                (entry): entry is DraftEntry =>
                  Boolean(entry && entry.type === "file" && isImage(entry.name))
              );
            setEntries([...dirs, ...sortedImages, ...nonImageFiles]);
          }
        } catch (err) {
          failures.push(
            `${folderPath}: ${err instanceof Error ? err.message : "Unable to reorder images."}`
          );
        }
      }

      return failures;
    },
    [enqueueImageOrderSaveForFolder, isImage, listPathEntries]
  );

  const fetchPendingAiEdits = useCallback(async (pathValue: string) => {
    if (!pathValue) {
      setPendingAiEditsByOriginal({});
      return;
    }
    const normalizedPath = String(pathValue || "").trim();
    const currentMainPath = String(currentMainPathRef.current || "").trim();
    const currentVariantsPath = String(currentVariantsPathRef.current || "").trim();
    const folders = new Set<string>();
    if (normalizedPath) folders.add(normalizedPath);
    if (
      normalizedPath &&
      currentMainPath &&
      currentVariantsPath &&
      (normalizedPath === currentMainPath || normalizedPath === currentVariantsPath)
    ) {
      folders.add(currentMainPath);
      folders.add(currentVariantsPath);
    }
    const folderList = Array.from(folders).filter(Boolean);
    if (folderList.length === 0) {
      setPendingAiEditsByOriginal({});
      return;
    }

    try {
      const settled = await Promise.allSettled(
        folderList.map(async (folderPath) => {
          const response = await fetch(
            `/api/drafts/ai-edits?folder=${encodeURIComponent(folderPath)}`
          );
          const payload = await response.json().catch(() => ({}));
          if (!response.ok) {
            throw new Error(payload?.error || "Unable to load AI edit state.");
          }
          return Array.isArray(payload?.items)
            ? (payload.items as PendingAiEditRecord[])
            : [];
        })
      );

      const next: Record<string, PendingAiEditRecord> = {};
      let successCount = 0;
      settled.forEach((result) => {
        if (result.status !== "fulfilled") return;
        successCount += 1;
        result.value.forEach((item) => {
          if (!item?.originalPath) return;
          next[item.originalPath] = item;
        });
      });

      if (successCount === 0) {
        throw new Error("Unable to load AI edit state.");
      }
      setPendingAiEditsByOriginal(next);
    } catch {
      setPendingAiEditsByOriginal({});
    }
  }, []);

  const handleSelectFolder = useCallback(
    async (nextFolderValue: string) => {
      const nextFolder = String(nextFolderValue || "");
      const requestSeq = folderSelectRequestSeqRef.current + 1;
      folderSelectRequestSeqRef.current = requestSeq;
      if (!nextFolder) {
        if (requestSeq !== folderSelectRequestSeqRef.current) return;
        setSelectedFolder("");
        setCurrentPath("");
        setEntries([]);
        setFolderTree(null);
        return;
      }
      setSelectedFolder(nextFolder);
      setSpuPickerOpen(false);
      const nextPath = await resolveInitialExplorerPath(nextFolder);
      if (requestSeq !== folderSelectRequestSeqRef.current) return;
      pendingFolderOpenPathRef.current = nextPath;
      setCurrentPath(nextPath);
    },
    [resolveInitialExplorerPath]
  );

  useEffect(() => {
    const run = String(selectedFolder || "").trim();
    if (!run) {
      setRunSpuOptions([]);
      return;
    }

    let cancelled = false;
    const loadRunSpuOptions = async () => {
      try {
        const items = await listPathEntries(run);
        if (cancelled) return;
        const options = items
          .filter(
            (entry) => entry.type === "dir" && !isChunksDirectory(entry.name)
          )
          .map((entry) => ({
            name: entry.name,
            path: entry.path,
          }))
          .sort((left, right) =>
            left.name.localeCompare(right.name, undefined, {
              numeric: true,
              sensitivity: "base",
            })
          );
        setRunSpuOptions(options);
      } catch {
        if (!cancelled) {
          setRunSpuOptions([]);
        }
      }
    };

    void loadRunSpuOptions();
    return () => {
      cancelled = true;
    };
  }, [folderTree, isChunksDirectory, listPathEntries, selectedFolder]);

  useEffect(() => {
    const run = String(selectedFolder || "").trim();
    if (!run) {
      setRunSpuStatusByPath({});
      return;
    }
    if (runSpuOptions.length === 0) {
      setRunSpuStatusByPath({});
      return;
    }

    let cancelled = false;
    const loadRunSpuStatuses = async () => {
      const optionPathSet = new Set(
        runSpuOptions
          .map((option) => String(option.path || "").trim())
          .filter(Boolean)
      );
      try {
        const response = await fetch(
          `/api/drafts/folders/${encodeURIComponent(run)}/spu-status`
        );
        const payload = await response.json().catch(() => ({}));
        if (!response.ok) {
          throw new Error(payload?.error || "Unable to load SPU status.");
        }

        const items = Array.isArray(payload?.items)
          ? (payload.items as Array<Record<string, unknown>>)
          : [];
        const next: Record<string, RunSpuStatus> = {};

        for (const row of items) {
          const rowPath = String(row.path || "").trim();
          if (!rowPath || !optionPathSet.has(rowPath)) continue;
          next[rowPath] = {
            hasPendingAi: Boolean(row.hasPendingAi),
            hasDigi: Boolean(row.hasDigi),
            hasTaggedImage: Boolean(row.hasTaggedImage),
            hasImageIssue: Boolean(row.hasImageIssue),
            hasUnseenAiUpdate: false,
          };
        }

        optionPathSet.forEach((pathValue) => {
          if (!next[pathValue]) {
            next[pathValue] = {
              hasPendingAi: false,
              hasDigi: false,
              hasTaggedImage: false,
              hasImageIssue: false,
              hasUnseenAiUpdate: false,
            };
          }
        });

        if (!cancelled) {
          setRunSpuStatusByPath(next);
        }
      } catch {
        if (!cancelled) {
          const fallback: Record<string, RunSpuStatus> = {};
          optionPathSet.forEach((pathValue) => {
            fallback[pathValue] = {
              hasPendingAi: false,
              hasDigi: false,
              hasTaggedImage: false,
              hasImageIssue: false,
              hasUnseenAiUpdate: false,
            };
          });
          setRunSpuStatusByPath(fallback);
        }
      }
    };

    void loadRunSpuStatuses();
    return () => {
      cancelled = true;
    };
  }, [runSpuOptions, selectedFolder]);

  useEffect(() => {
    setRunSpuStatusByPath((prev) => {
      const keys = Object.keys(prev);
      if (keys.length === 0) return prev;
      let changed = false;
      const next: Record<string, RunSpuStatus> = {};
      keys.forEach((pathValue) => {
        const row = prev[pathValue];
        const hasUnseenAiUpdate = unseenAiUpdateSpuPaths.has(pathValue);
        if ((row?.hasUnseenAiUpdate ?? false) !== hasUnseenAiUpdate) {
          changed = true;
        }
        next[pathValue] = {
          hasPendingAi: Boolean(row?.hasPendingAi),
          hasDigi: Boolean(row?.hasDigi),
          hasTaggedImage: Boolean(row?.hasTaggedImage),
          hasImageIssue: Boolean(row?.hasImageIssue),
          hasUnseenAiUpdate,
        };
      });
      return changed ? next : prev;
    });
  }, [unseenAiUpdateSpuPaths]);

  useEffect(() => {
    const currentSpuPath = getSpuRootFromDraftPath(currentPath);
    const previous = previousRunSpuPendingAiByPathRef.current;
    const snapshot: Record<string, boolean> = {};
    const newlyPendingElsewhere: string[] = [];

    Object.entries(runSpuStatusByPath).forEach(([pathValue, row]) => {
      const pending = Boolean(row?.hasPendingAi);
      snapshot[pathValue] = pending;
      if (pending && !previous[pathValue] && pathValue !== currentSpuPath) {
        newlyPendingElsewhere.push(pathValue);
      }
    });

    previousRunSpuPendingAiByPathRef.current = snapshot;
    if (newlyPendingElsewhere.length > 0) {
      markSpuNeedsAiReview(newlyPendingElsewhere);
    }
  }, [currentPath, markSpuNeedsAiReview, runSpuStatusByPath]);

  const selectedSpuPathInRun = useMemo(() => {
    const run = String(selectedFolder || "").trim();
    const pathValue = String(currentPath || "").trim();
    if (!run || !pathValue) return "";
    const parts = pathValue.split("/").filter(Boolean);
    if (parts.length < 2) return "";
    if (parts[0] !== run) return "";
    return `${parts[0]}/${parts[1]}`;
  }, [currentPath, selectedFolder]);

  useEffect(() => {
    if (!initialUrlStateParsedRef.current) return;
    const runValue = normalizeExplorerLocationPath(selectedFolder);
    const pathValue = normalizeExplorerLocationPath(currentPath);
    if (!runValue && !pathValue) return;

    const spuRoot = getSpuRootFromDraftPath(pathValue);
    const spuValue = normalizeDraftSpuCode(spuRoot.split("/").filter(Boolean)[1] || "");

    const url = new URL(window.location.href);
    const params = url.searchParams;
    if (runValue) {
      params.set("run", runValue);
    } else {
      params.delete("run");
    }
    if (spuValue) {
      params.set("spu", spuValue);
    } else {
      params.delete("spu");
    }
    if (pathValue) {
      params.set("path", pathValue);
    } else {
      params.delete("path");
    }
    if (draftTab !== "spu") {
      params.set("tab", draftTab);
    } else {
      params.delete("tab");
    }
    if (draftTableShowAll) {
      params.set("showAll", "1");
    } else {
      params.delete("showAll");
    }
    if (mainImageViewFilter !== "all") {
      params.set("imgTab", toImageTabFromMainViewFilter(mainImageViewFilter));
    } else {
      params.delete("imgTab");
    }
    const imagePathValue = normalizeExplorerLocationPath(previewPath || "");
    if (imagePathValue) {
      params.set("img", imagePathValue);
    } else {
      params.delete("img");
    }
    params.delete("open_spu");
    params.delete("show_all");
    params.delete("imageTab");
    params.delete("image");

    const hashParams = new URLSearchParams();
    if (runValue) {
      hashParams.set("run", runValue);
    }
    if (spuValue) {
      hashParams.set("spu", spuValue);
    }
    if (pathValue) {
      hashParams.set("path", pathValue);
    }
    if (draftTab !== "spu") {
      hashParams.set("tab", draftTab);
    }
    if (draftTableShowAll) {
      hashParams.set("showAll", "1");
    }
    if (mainImageViewFilter !== "all") {
      hashParams.set("imgTab", toImageTabFromMainViewFilter(mainImageViewFilter));
    }
    if (imagePathValue) {
      hashParams.set("img", imagePathValue);
    }

    const nextSearch = params.toString();
    const nextHash = hashParams.toString();
    const nextUrl = `${url.pathname}${nextSearch ? `?${nextSearch}` : ""}${
      nextHash ? `#${nextHash}` : ""
    }`;
    const currentUrl = `${url.pathname}${url.search}${url.hash}`;
    const previousLocationState = lastUrlLocationStateRef.current;
    const locationChanged =
      previousLocationState.run !== runValue ||
      previousLocationState.spu !== spuValue ||
      previousLocationState.path !== pathValue;
    const shouldPushNavigationEntry =
      hasSyncedUrlStateRef.current && locationChanged;

    if (nextUrl !== currentUrl) {
      const method = shouldPushNavigationEntry ? "pushState" : "replaceState";
      try {
        // Keep state payload clone-safe across browsers; Next router can store
        // non-trivial state values that sometimes fail to clone.
        window.history[method](null, "", nextUrl);
      } catch {
        try {
          window.history[method]({}, "", nextUrl);
        } catch {
          // Last-resort hard navigation if history API update is blocked.
          window.location.assign(nextUrl);
        }
      }
    }
    lastUrlLocationStateRef.current = {
      run: runValue,
      spu: spuValue,
      path: pathValue,
    };
    hasSyncedUrlStateRef.current = true;
  }, [
    currentPath,
    draftTab,
    draftTableShowAll,
    mainImageViewFilter,
    previewPath,
    selectedFolder,
  ]);

  useEffect(() => {
    const previousSpuPath = previousSelectedSpuPathForDraftTableRef.current;
    const nextSpuPath = String(selectedSpuPathInRun || "").trim();
    if (
      draftTableShowAll &&
      nextSpuPath &&
      nextSpuPath !== previousSpuPath
    ) {
      setDraftTableShowAll(false);
    }
    previousSelectedSpuPathForDraftTableRef.current = nextSpuPath;
  }, [draftTableShowAll, selectedSpuPathInRun]);

  const selectedRunSpuIndex = useMemo(
    () => runSpuOptions.findIndex((option) => option.path === selectedSpuPathInRun),
    [runSpuOptions, selectedSpuPathInRun]
  );

  const selectedRunSpuLabel = useMemo(() => {
    if (selectedRunSpuIndex >= 0) {
      return runSpuOptions[selectedRunSpuIndex]?.name ?? "Select SPU";
    }
    return "Select SPU";
  }, [runSpuOptions, selectedRunSpuIndex]);

  const handleSelectRunSpu = useCallback((spuPath: string) => {
    const normalizedPath = String(spuPath || "").trim();
    if (!normalizedPath) return;
    setSpuPickerOpen(false);
    setCurrentPath(normalizedPath);
  }, []);

  const canNavigatePrevSpu =
    selectedRunSpuIndex >= 0 && runSpuOptions.length > 1;
  const canNavigateNextSpu =
    selectedRunSpuIndex >= 0 && runSpuOptions.length > 1;
  const currentBatchSpuValues = useMemo(
    () =>
      Array.from(
        new Set(
          runSpuOptions
            .map((option) => String(option.name || "").trim())
            .filter(Boolean)
        )
      ),
    [runSpuOptions]
  );
  const currentBatchHasSkuGaps = useMemo(() => {
    if (String(searchQuery || "").trim()) return false;
    if (currentBatchSpuValues.length === 0) return false;
    return currentBatchSpuValues.some((spu) => {
      const state = skuReadinessBySpu.get(spu);
      return !state || !state.ready;
    });
  }, [searchQuery, currentBatchSpuValues, skuReadinessBySpu]);
  const currentBatchSpuCount = currentBatchSpuValues.length;
  const runSpuShortTitleByCode = useMemo(() => {
    const next: Record<string, string> = {};
    for (const row of spuRows) {
      const code = String(row.draft_spu || "").trim().toUpperCase();
      if (!code || next[code]) continue;
      const shortTitle = getShortTitleLabel(
        row.draft_mf_product_short_title || row.draft_title || row.draft_mf_product_long_title
      );
      if (shortTitle) {
        next[code] = shortTitle;
      }
    }
    return next;
  }, [spuRows]);

  const spuPickerMenuWidthPx = useMemo(() => {
    const labels = runSpuOptions.map((option) => {
      const code = String(option.name || "").trim();
      const title = runSpuShortTitleByCode[code.toUpperCase()] || "";
      return title ? `${code} - ${title}` : code;
    });
    const baseLabel = selectedRunSpuLabel || "Select SPU";
    const longestTextWidth = Math.max(
      estimateTableTextWidthPx(baseLabel),
      ...labels.map((value) => estimateTableTextWidthPx(value))
    );
    const indicatorAllowance = 112;
    const padded = longestTextWidth + indicatorAllowance;
    return clampNumber(Math.round(padded), 220, 1020);
  }, [runSpuOptions, runSpuShortTitleByCode, selectedRunSpuLabel]);

  const spuPickerTriggerWidthPx = useMemo(() => {
    const label = selectedRunSpuLabel || "Select SPU";
    const padded = estimateTableTextWidthPx(label) + 56;
    const widened = padded * 1.6;
    return clampNumber(Math.round(widened), 173, 352);
  }, [selectedRunSpuLabel]);
  const spuPickerMenuShouldScroll = runSpuOptions.length > 20;

  const handlePublishCurrentBatchDrafts = useCallback(async () => {
    if (!selectedFolder) return;
    setPublishMessage(null);
    if (hasPendingDetailAiRewrite) {
      setPublishStatus("error");
      setPublishMessage(
        "Wait for AI product description updates to finish before saving to database."
      );
      return;
    }
    if (skuStatus === "running") {
      setPublishStatus("error");
      setPublishMessage(t("draftExplorer.publishBlockedSkuRunning"));
      return;
    }

    const spus = [...currentBatchSpuValues];
    if (spus.length === 0) {
      setPublishStatus("error");
      setPublishMessage("No SPUs found in the selected draft folder.");
      return;
    }

    try {
      const readiness = await validateSkuReadinessForSpus(spus);
      if (!readiness.ready) {
        setPublishStatus("error");
        const issueParts: string[] = [];
        if (readiness.missingVariantSpus > 0) {
          issueParts.push(
            `${readiness.missingVariantSpus} product${
              readiness.missingVariantSpus === 1 ? "" : "s"
            } without variants`
          );
        }
        if (readiness.missingSkuRows > 0) {
          issueParts.push(
            `${readiness.missingSkuRows} row${
              readiness.missingSkuRows === 1 ? "" : "s"
            } without SKU`
          );
        }
        setPublishMessage(
          `Draft folder still has missing SKU coverage (${issueParts.join(
            ", "
          )}). Generate SKUs and retry.`
        );
        return;
      }
    } catch (err) {
      setPublishStatus("error");
      setPublishMessage((err as Error).message);
      return;
    }

    const confirmSave = window.confirm(
      t("draftExplorer.publishConfirmSelected", { count: spus.length })
    );
    if (!confirmSave) {
      setPublishStatus("idle");
      return;
    }

    setPublishStatus("running");
    try {
      const response = await fetch("/api/drafts/publish", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ spus }),
      });
      if (!response.ok) {
        const message = await response.text();
        throw new Error(message || "Publish failed.");
      }
      const payload = await response.json();
      const publishedSpus: string[] = Array.isArray(payload?.spus)
        ? payload.spus
            .map((entry: unknown) => String(entry || "").trim())
            .filter(Boolean)
        : [];
      const count = publishedSpus.length;
      setPublishMessage(t("draftExplorer.publishSuccess", { count }));
      setPublishStatus("done");
      setSelectedSpus(new Set());
      await Promise.all([fetchSpuRows(), fetchSkuRows()]);
      await fetchFolders();
      if (selectedFolder) {
        await fetchFolderTree(selectedFolder);
      }
      if (currentPath) {
        await refreshEntries(currentPath);
        await fetchPendingAiEdits(currentPath);
      }
    } catch (err) {
      setPublishStatus("error");
      setPublishMessage((err as Error).message);
    }
  }, [
    currentBatchSpuValues,
    currentPath,
    fetchFolderTree,
    fetchFolders,
    fetchPendingAiEdits,
    fetchSkuRows,
    fetchSpuRows,
    hasPendingDetailAiRewrite,
    refreshEntries,
    selectedFolder,
    skuStatus,
    validateSkuReadinessForSpus,
    t,
  ]);

  const handleNavigateRunSpu = useCallback(
    (direction: -1 | 1) => {
      const count = runSpuOptions.length;
      if (count <= 1 || selectedRunSpuIndex < 0) return;
      let nextIndex = selectedRunSpuIndex + direction;
      if (nextIndex < 0) {
        nextIndex = count - 1;
      } else if (nextIndex >= count) {
        nextIndex = 0;
      }
      const target = runSpuOptions[nextIndex];
      if (!target?.path) return;
      setCurrentPath(target.path);
    },
    [runSpuOptions, selectedRunSpuIndex]
  );

  const handleExplorerRefresh = useCallback(() => {
    fetchFolders();
    if (currentPath) {
      refreshEntries(currentPath);
      fetchPendingAiEdits(currentPath);
    }
    if (selectedFolder) {
      fetchFolderTree(selectedFolder);
    }
  }, [
    fetchFolders,
    currentPath,
    refreshEntries,
    fetchPendingAiEdits,
    fetchFolderTree,
    selectedFolder,
  ]);

  const handleDeleteFolder = useCallback(async () => {
    if (!selectedFolder) return;
    if (isArchiveFolder(selectedFolder)) {
      setError("Draft Archive is permanent and cannot be deleted.");
      return;
    }
    if (selectedFolder.includes("/") || selectedFolder.includes("\\")) {
      setError(t("bulkProcessing.explorer.deleteFolderError"));
      return;
    }
    const productCount = currentBatchSpuCount;
    const confirmed = window.confirm(
      `Delete draft folder "${selectedFolder}" and all ${productCount} product${
        productCount === 1 ? "" : "s"
      } inside it? This will also delete related SPU and SKU draft data.`
    );
    if (!confirmed) return;
    setDeleteFolderPending(true);
    setError(null);
    try {
      const response = await fetch(
        `/api/drafts/folders/${encodeURIComponent(selectedFolder)}`,
        { method: "DELETE" }
      );
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        throw new Error(
          payload?.error || t("bulkProcessing.explorer.deleteFolderError")
        );
      }
      const deletedRunPrefix = `${selectedFolder}/`;
      setUnseenAiUpdateSpuPaths((prev) => {
        if (prev.size === 0) return prev;
        let changed = false;
        const next = new Set<string>();
        prev.forEach((pathValue) => {
          if (
            pathValue === selectedFolder ||
            pathValue.startsWith(deletedRunPrefix)
          ) {
            changed = true;
            return;
          }
          next.add(pathValue);
        });
        return changed ? next : prev;
      });
      setSelectedFolder("");
      setCurrentPath("");
      setEntries([]);
      setSelectedFiles(new Set());
      setSelectedTreeFolders(new Set());
      setFolderTree(null);
      setPreviewPath(null);
      fetchFolders();
    } catch (err) {
      setError(
        err instanceof Error
          ? err.message
          : t("bulkProcessing.explorer.deleteFolderError")
      );
    } finally {
      setDeleteFolderPending(false);
    }
  }, [currentBatchSpuCount, selectedFolder, fetchFolders, isArchiveFolder, t]);

  const handleSelectAllRunsForMerge = useCallback(() => {
    setSelectedRunsForMerge(() => {
      const nonArchive = folders.filter(
        (folder) => !isArchiveFolder(folder.path)
      );
      if (nonArchive.length === 0) return new Set<string>();

      const baseRunPath =
        selectedFolder && !isArchiveFolder(selectedFolder)
          ? selectedFolder
          : nonArchive[0]?.path ?? "";
      const baseType = getRunTypeForPath(baseRunPath);

      return new Set(
        nonArchive
          .filter((folder) => getRunTypeForPath(folder.path) === baseType)
          .map((folder) => folder.path)
      );
    });
  }, [folders, getRunTypeForPath, isArchiveFolder, selectedFolder]);

  const handleUnselectRunsForMerge = useCallback(() => {
    setSelectedRunsForMerge(new Set());
  }, []);

  useEffect(() => {
    setSelectedRunsForMerge((prev) => {
      if (prev.size === 0) return prev;
      const filteredRuns = Array.from(prev).filter((run) => !isArchiveFolder(run));
      const baseType =
        filteredRuns.length > 0 ? getRunTypeForPath(filteredRuns[0]) : null;
      const next = new Set(
        filteredRuns.filter((run) => !baseType || getRunTypeForPath(run) === baseType)
      );
      return next.size === prev.size ? prev : next;
    });
  }, [getRunTypeForPath, isArchiveFolder, folders]);

  const setRunMergeSelection = useCallback(
    (run: string, checked: boolean) => {
      if (isArchiveFolder(run)) return;
      setSelectedRunsForMerge((prev) => {
        const hasRun = prev.has(run);
        if (checked === hasRun) return prev;
        const next = new Set(prev);
        if (checked) {
          if (next.size > 0) {
            const selectedTypes = new Set(
              Array.from(next)
                .map((entry) => getRunTypeForPath(entry))
                .filter((entry) => entry !== "archive")
            );
            const runType = getRunTypeForPath(run);
            if (
              selectedTypes.size > 0 &&
              runType !== "archive" &&
              !selectedTypes.has(runType)
            ) {
              setError(
                "Cannot merge mixed folder types. Re-edit folders can only merge with re-edit folders."
              );
              return prev;
            }
          }
          next.add(run);
        }
        else next.delete(run);
        return next;
      });
    },
    [getRunTypeForPath, isArchiveFolder]
  );

  const handleDeleteRuns = useCallback(async () => {
    const runs = Array.from(selectedRunsForMerge).filter(
      (run) => !isArchiveFolder(run)
    );
    if (runs.length === 0 || deleteRunsPending) return;
    const confirmed = window.confirm(
      `Delete ${runs.length} selected batch folder${
        runs.length === 1 ? "" : "s"
      }? This will also delete linked SPU and SKU draft data.`
    );
    if (!confirmed) return;

    setDeleteRunsPending(true);
    setError(null);

    const failedRuns: string[] = [];
    const failureMessages: string[] = [];
    let deletedRuns = 0;

    for (const run of runs) {
      try {
        const response = await fetch(
          `/api/drafts/folders/${encodeURIComponent(run)}`,
          { method: "DELETE" }
        );
        const payload = await response.json().catch(() => ({}));
        if (!response.ok) {
          throw new Error(
            payload?.error || `Delete failed (HTTP ${response.status}).`
          );
        }
        deletedRuns += 1;
      } catch (err) {
        failedRuns.push(run);
        failureMessages.push(
          `${run}: ${err instanceof Error ? err.message : "Delete failed."}`
        );
      }
    }

    setSelectedRunsForMerge(new Set(failedRuns));
    if (failedRuns.length === 0) {
      setBatchPickerOpen(false);
    }
    const deletedRunSet = new Set(
      runs.filter((run) => !failedRuns.includes(run))
    );
    if (deletedRunSet.size > 0) {
      setUnseenAiUpdateSpuPaths((prev) => {
        if (prev.size === 0) return prev;
        let changed = false;
        const next = new Set<string>();
        prev.forEach((pathValue) => {
          const runName = String(pathValue || "").split("/").filter(Boolean)[0] || "";
          if (runName && deletedRunSet.has(runName)) {
            changed = true;
            return;
          }
          next.add(pathValue);
        });
        return changed ? next : prev;
      });
    }

    await fetchFolders();
    if (draftTab === "spu") {
      await fetchSpuRows();
    } else {
      await fetchSkuRows();
    }

    if (failureMessages.length > 0) {
      setError(
        `Deleted ${deletedRuns}/${runs.length} batch folder${
          runs.length === 1 ? "" : "s"
        }. Errors: ${failureMessages.join(" | ")}`
      );
    }

    setDeleteRunsPending(false);
  }, [
    selectedRunsForMerge,
    deleteRunsPending,
    fetchFolders,
    draftTab,
    fetchSpuRows,
    fetchSkuRows,
    isArchiveFolder,
  ]);

	  const openRunPreview = useCallback(async (run: string) => {
	    setRunPreviewRun(run);
	    setRunPreviewSelectedSpus(new Set());
	    setRunPreviewDeletedSpus(new Set());
	    setRunPreviewItems([]);
	    setRunPreviewOpen(true);
	    setRunPreviewLoading(true);
    try {
      const response = await fetch(
        `/api/drafts/folders/${encodeURIComponent(run)}/preview`
      );
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        throw new Error(payload?.error || "Unable to load batch preview.");
      }
      const items = Array.isArray(payload?.items)
        ? (payload.items as DraftRunPreviewItem[])
        : [];
      setRunPreviewItems(items);
    } catch (err) {
      setError(err instanceof Error ? err.message : "Unable to load batch preview.");
      setRunPreviewOpen(false);
    } finally {
      setRunPreviewLoading(false);
    }
  }, []);

	  const handleRunPreviewDelete = useCallback((spu: string) => {
	    setRunPreviewDeletedSpus((prev) => {
	      const next = new Set(prev);
	      next.add(spu);
	      return next;
	    });
	    setRunPreviewSelectedSpus((prev) => {
	      if (!prev.has(spu)) return prev;
	      const next = new Set(prev);
	      next.delete(spu);
	      return next;
	    });
	  }, []);

	  const visibleRunPreviewItems = useMemo(
	    () => runPreviewItems.filter((item) => !runPreviewDeletedSpus.has(item.draft_spu)),
	    [runPreviewItems, runPreviewDeletedSpus]
	  );

	  const allRunPreviewSelected = useMemo(
	    () =>
	      visibleRunPreviewItems.length > 0 &&
	      visibleRunPreviewItems.every((item) => runPreviewSelectedSpus.has(item.draft_spu)),
	    [visibleRunPreviewItems, runPreviewSelectedSpus]
	  );

	  const someRunPreviewSelected = useMemo(
	    () => visibleRunPreviewItems.some((item) => runPreviewSelectedSpus.has(item.draft_spu)),
	    [visibleRunPreviewItems, runPreviewSelectedSpus]
	  );

	  const toggleSelectAllRunPreview = useCallback(() => {
	    if (allRunPreviewSelected) {
	      setRunPreviewSelectedSpus(new Set());
	      return;
	    }
	    setRunPreviewSelectedSpus(new Set(visibleRunPreviewItems.map((item) => item.draft_spu)));
	  }, [allRunPreviewSelected, visibleRunPreviewItems]);

	  const toggleSelectRunPreviewSpu = useCallback((spu: string) => {
	    setRunPreviewSelectedSpus((prev) => {
	      const next = new Set(prev);
	      if (next.has(spu)) next.delete(spu);
	      else next.add(spu);
	      return next;
	    });
	  }, []);

	  const handleRunPreviewDeleteSelected = useCallback(() => {
	    if (runPreviewSelectedSpus.size === 0) return;
	    setRunPreviewDeletedSpus((prev) => {
	      const next = new Set(prev);
	      runPreviewSelectedSpus.forEach((spu) => next.add(spu));
	      return next;
	    });
	    setRunPreviewSelectedSpus(new Set());
	  }, [runPreviewSelectedSpus]);

  const handleRunPreviewSave = useCallback(async () => {
    if (!runPreviewRun || runPreviewDeletedSpus.size === 0) {
      setRunPreviewOpen(false);
      return;
    }
    const confirmed = window.confirm(
      `Delete ${runPreviewDeletedSpus.size} item(s) from ${runPreviewRun}?`
    );
    if (!confirmed) return;
    setRunPreviewSaving(true);
    setError(null);
    try {
      const response = await fetch(
        `/api/drafts/folders/${encodeURIComponent(runPreviewRun)}/cleanup`,
        {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ spus: Array.from(runPreviewDeletedSpus) }),
        }
      );
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        throw new Error(payload?.error || "Cleanup failed.");
      }
      setRunPreviewOpen(false);
      if (draftTab === "spu") fetchSpuRows();
      if (draftTab === "sku") fetchSkuRows();
      if (selectedFolder === runPreviewRun) {
        handleExplorerRefresh();
      }
    } catch (err) {
      setError(err instanceof Error ? err.message : "Cleanup failed.");
    } finally {
      setRunPreviewSaving(false);
    }
  }, [
    runPreviewRun,
    runPreviewDeletedSpus,
    draftTab,
    fetchSpuRows,
    fetchSkuRows,
    selectedFolder,
    handleExplorerRefresh,
  ]);

  const handleMergeRuns = useCallback(async () => {
    const runs = Array.from(selectedRunsForMerge).filter(
      (run) => !isArchiveFolder(run)
    );
    if (runs.length < 2 || mergeRunsPending) return;
    const runTypes = Array.from(
      new Set(runs.map((run) => getRunTypeForPath(run)))
    ).filter((runType) => runType !== "archive");
    if (runTypes.length > 1) {
      setError(
        "Cannot merge mixed folder types. Re-edit folders can only merge with re-edit folders."
      );
      return;
    }
    const confirmed = window.confirm(
      `Merge ${runs.length} batches into one? This will copy folders and update draft pointers.`
    );
    if (!confirmed) return;
    setMergeRunsPending(true);
    setError(null);
    try {
      const response = await fetch("/api/drafts/folders/merge", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ runs }),
      });
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        const collisions = Array.isArray(payload?.collisions)
          ? ` Collisions: ${payload.collisions.join(", ")}`
          : "";
        throw new Error((payload?.error || "Merge failed.") + collisions);
      }
      const mergedRun = String(payload?.merged_run || "");
      if (!mergedRun) throw new Error("Merge completed but missing merged folder.");
      setSelectedRunsForMerge(new Set());
      setBatchPickerOpen(false);
      await fetchFolders();
      await handleSelectFolder(mergedRun);
    } catch (err) {
      setError(err instanceof Error ? err.message : "Merge failed.");
    } finally {
      setMergeRunsPending(false);
    }
  }, [
    selectedRunsForMerge,
    mergeRunsPending,
    fetchFolders,
    getRunTypeForPath,
    handleSelectFolder,
    isArchiveFolder,
  ]);

  useEffect(() => {
    fetchFolders();
  }, [fetchFolders]);

  useEffect(() => {
    if (!selectedFolder) return;
    const pendingPath = pendingFolderOpenPathRef.current;
    const normalizedPendingPath = String(pendingPath || "").trim();
    const hasSpecificPendingPath =
      Boolean(normalizedPendingPath) &&
      normalizedPendingPath.startsWith(`${selectedFolder}/`);
    pendingFolderOpenPathRef.current = null;
    setSelectedTreeFolders(new Set());
    setCollapsedTreeFolders(new Set());
    setContextMenu(null);
    fetchFolderTree(selectedFolder);
    if (hasSpecificPendingPath) {
      setCurrentPath(normalizedPendingPath);
      return;
    }
    let cancelled = false;
    void (async () => {
      const resolvedInitialPath = await resolveInitialExplorerPath(selectedFolder);
      if (cancelled) return;
      setCurrentPath(String(resolvedInitialPath || "").trim() || selectedFolder);
    })();
    return () => {
      cancelled = true;
    };
  }, [selectedFolder, fetchFolderTree, resolveInitialExplorerPath]);

  useEffect(() => {
    selectedFolderRef.current = String(selectedFolder || "").trim();
  }, [selectedFolder]);

  useEffect(() => {
    currentPathRef.current = String(currentPath || "");
  }, [currentPath]);

  useEffect(() => {
    const currentSpuPath = getSpuRootFromDraftPath(currentPath);
    const runningElsewhere = Object.keys(aiEditJobsByPath)
      .map((pathValue) => getSpuRootFromDraftPath(pathValue))
      .filter((pathValue) => Boolean(pathValue) && pathValue !== currentSpuPath);
    if (runningElsewhere.length > 0) {
      markSpuNeedsAiReview(runningElsewhere);
    }
  }, [aiEditJobsByPath, currentPath, markSpuNeedsAiReview]);

  useEffect(() => {
    if (!currentPath) {
      setPendingAiEditsByOriginal({});
      setMainViewVariantImageEntries([]);
      return;
    }
    const normalizedCurrentPath = String(currentPath || "").trim();
    const normalizedMainPath = String(currentMainPathRef.current || "").trim();
    const shouldLoadMergedVariants =
      Boolean(normalizedCurrentPath) &&
      Boolean(normalizedMainPath) &&
      normalizedCurrentPath === normalizedMainPath;
    const variantsPathForCurrentMain = shouldLoadMergedVariants
      ? String(currentVariantsPathRef.current || "").trim()
      : "";
    if (shouldLoadMergedVariants) {
      // Main path with merged variants is hydrated by the imageTabTargets effect below.
      // Fall back to main-only load if variants path is not available yet.
      if (!variantsPathForCurrentMain) {
        void fetchEntries(currentPath, { variantsPath: null });
      }
    } else {
      void fetchEntries(currentPath, { variantsPath: null });
    }
    fetchPendingAiEdits(currentPath);
    setImageDimensions({});
    setAiReviewOriginalPath(null);
  }, [currentPath, fetchEntries, fetchPendingAiEdits]);

  useEffect(() => {
    if (!currentPath) return;
    const segments = currentPath.split("/").filter(Boolean);
    const ancestors: string[] = [];
    let accumulated = "";
    segments.forEach((segment) => {
      accumulated = accumulated ? `${accumulated}/${segment}` : segment;
      ancestors.push(accumulated);
    });
    setCollapsedTreeFolders((prev) => {
      const next = new Set(prev);
      ancestors.forEach((folderPath) => {
        next.delete(folderPath);
      });
      return next;
    });
  }, [currentPath]);

  const clearContextMenuCloseTimer = useCallback(() => {
    if (contextMenuCloseTimerRef.current == null) return;
    window.clearTimeout(contextMenuCloseTimerRef.current);
    contextMenuCloseTimerRef.current = null;
  }, []);

  const clearContextMenuSubmenuCloseTimer = useCallback(() => {
    if (contextMenuSubmenuCloseTimerRef.current == null) return;
    window.clearTimeout(contextMenuSubmenuCloseTimerRef.current);
    contextMenuSubmenuCloseTimerRef.current = null;
  }, []);

  const clearContextMenuNestedCloseTimer = useCallback(() => {
    if (contextMenuNestedCloseTimerRef.current == null) return;
    window.clearTimeout(contextMenuNestedCloseTimerRef.current);
    contextMenuNestedCloseTimerRef.current = null;
  }, []);

  const clearAllContextMenuCloseTimers = useCallback(() => {
    clearContextMenuCloseTimer();
    clearContextMenuSubmenuCloseTimer();
    clearContextMenuNestedCloseTimer();
  }, [
    clearContextMenuCloseTimer,
    clearContextMenuNestedCloseTimer,
    clearContextMenuSubmenuCloseTimer,
  ]);

  const closeContextMenuImmediately = useCallback(() => {
    clearAllContextMenuCloseTimers();
    setContextMenu(null);
    setContextMenuSubmenu(null);
    setContextMenuNestedSubmenu(null);
  }, [clearAllContextMenuCloseTimers]);

  const keepContextMenuOpen = useCallback(() => {
    clearAllContextMenuCloseTimers();
  }, [clearAllContextMenuCloseTimers]);

  const scheduleContextMenuClose = useCallback(
    (delayMs = CONTEXT_MENU_STICKY_CLOSE_MS) => {
      clearContextMenuCloseTimer();
      contextMenuCloseTimerRef.current = window.setTimeout(() => {
        contextMenuCloseTimerRef.current = null;
        setContextMenu(null);
        setContextMenuSubmenu(null);
        setContextMenuNestedSubmenu(null);
      }, delayMs);
    },
    [clearContextMenuCloseTimer]
  );

  const scheduleContextMenuSubmenuClose = useCallback(
    (submenu: NonNullable<typeof contextMenuSubmenu>, delayMs = CONTEXT_MENU_STICKY_CLOSE_MS) => {
      clearContextMenuSubmenuCloseTimer();
      contextMenuSubmenuCloseTimerRef.current = window.setTimeout(() => {
        contextMenuSubmenuCloseTimerRef.current = null;
        setContextMenuSubmenu((prev) => (prev === submenu ? null : prev));
        setContextMenuNestedSubmenu(null);
      }, delayMs);
    },
    [clearContextMenuSubmenuCloseTimer]
  );

  const scheduleContextMenuNestedClose = useCallback(
    (
      nested: NonNullable<typeof contextMenuNestedSubmenu>,
      delayMs = CONTEXT_MENU_STICKY_CLOSE_MS
    ) => {
      clearContextMenuNestedCloseTimer();
      contextMenuNestedCloseTimerRef.current = window.setTimeout(() => {
        contextMenuNestedCloseTimerRef.current = null;
        setContextMenuNestedSubmenu((prev) => (prev === nested ? null : prev));
      }, delayMs);
    },
    [clearContextMenuNestedCloseTimer]
  );

  const openContextMenuSubmenu = useCallback(
    (submenu: NonNullable<typeof contextMenuSubmenu>, options?: { resetNested?: boolean }) => {
      keepContextMenuOpen();
      setContextMenuSubmenu(submenu);
      if (options?.resetNested) {
        setContextMenuNestedSubmenu(null);
      }
    },
    [keepContextMenuOpen]
  );

  const openContextMenuNestedSubmenu = useCallback(
    (submenu: NonNullable<typeof contextMenuNestedSubmenu>) => {
      keepContextMenuOpen();
      setContextMenuNestedSubmenu(submenu);
    },
    [keepContextMenuOpen]
  );

  const closeAnyOpenContextMenuSubmenuWithDelay = useCallback(() => {
    keepContextMenuOpen();
    if (contextMenuSubmenu) {
      scheduleContextMenuSubmenuClose(contextMenuSubmenu);
    }
    if (contextMenuNestedSubmenu) {
      scheduleContextMenuNestedClose(contextMenuNestedSubmenu);
    }
  }, [
    contextMenuNestedSubmenu,
    contextMenuSubmenu,
    keepContextMenuOpen,
    scheduleContextMenuNestedClose,
    scheduleContextMenuSubmenuClose,
  ]);

  const closeAnyOpenContextMenuNestedSubmenuWithDelay = useCallback(() => {
    keepContextMenuOpen();
    if (contextMenuNestedSubmenu) {
      scheduleContextMenuNestedClose(contextMenuNestedSubmenu);
    }
  }, [contextMenuNestedSubmenu, keepContextMenuOpen, scheduleContextMenuNestedClose]);

  useLayoutEffect(() => {
    if (!contextMenu) return;
    const menu = contextMenuRef.current;
    if (!menu) return;

    const clampToViewport = () => {
      const padding = 8;
      const rect = menu.getBoundingClientRect();
      const maxX = window.innerWidth - rect.width - padding;
      const maxY = window.innerHeight - rect.height - padding;
      const nextX = Math.min(Math.max(padding, contextMenu.x), Math.max(padding, maxX));
      const nextY = Math.min(Math.max(padding, contextMenu.y), Math.max(padding, maxY));

      if (nextX !== contextMenu.x || nextY !== contextMenu.y) {
        setContextMenu((prev) => (prev ? { ...prev, x: nextX, y: nextY } : prev));
        return;
      }

      const submenuWidthEstimate = 320;
      const gap = 8;
      const finalRect = menu.getBoundingClientRect();
      const needed = submenuWidthEstimate + gap;
      const availableRight = window.innerWidth - padding - finalRect.right;
      const availableLeft = finalRect.left - padding;
      const nextSide: "right" | "left" =
        availableRight >= needed
          ? "right"
          : availableLeft >= needed
            ? "left"
            : availableRight >= availableLeft
              ? "right"
              : "left";
      setContextMenuSubmenuSide(nextSide);
    };

    clampToViewport();
    const handleResize = () => {
      window.requestAnimationFrame(() => clampToViewport());
    };
    window.addEventListener("resize", handleResize);
    return () => {
      window.removeEventListener("resize", handleResize);
    };
  }, [contextMenu]);

  useEffect(() => {
    if (!contextMenu) return;
    const close = () => {
      closeContextMenuImmediately();
    };
    const handleEscape = (event: KeyboardEvent) => {
      if (event.key === "Escape") {
        closeContextMenuImmediately();
      }
    };
    window.addEventListener("click", close);
    window.addEventListener("scroll", close);
    window.addEventListener("keydown", handleEscape);
    return () => {
      window.removeEventListener("click", close);
      window.removeEventListener("scroll", close);
      window.removeEventListener("keydown", handleEscape);
    };
  }, [closeContextMenuImmediately, contextMenu]);

  useEffect(() => {
    if (contextMenu) return;
    clearAllContextMenuCloseTimers();
  }, [clearAllContextMenuCloseTimers, contextMenu]);

  useEffect(() => {
    setContextMenuNestedSubmenu(null);
  }, [contextMenuSubmenu]);

  const handleToggleFile = useCallback(
    (
      pathValue: string,
      options?: { shiftKey?: boolean; imageRange?: boolean }
    ) => {
      const shiftRangeRequested = Boolean(options?.shiftKey && options?.imageRange);
      setSelectedFiles((prev) => {
        const next = new Set(prev);
        if (shiftRangeRequested) {
          const anchorPath = selectionAnchorImagePathRef.current;
          const imagePaths = displayImageEntriesRef.current
            .filter(
              (entry) => entry.type === "file" && isImage(entry.name)
            )
            .map((entry) => entry.path);
          const anchorIndex = anchorPath ? imagePaths.indexOf(anchorPath) : -1;
          const targetIndex = imagePaths.indexOf(pathValue);
          if (anchorIndex >= 0 && targetIndex >= 0) {
            const [start, end] =
              anchorIndex <= targetIndex
                ? [anchorIndex, targetIndex]
                : [targetIndex, anchorIndex];
            for (let index = start; index <= end; index += 1) {
              next.add(imagePaths[index]);
            }
            selectionAnchorImagePathRef.current = pathValue;
            return next;
          }
        }

        if (next.has(pathValue)) {
          next.delete(pathValue);
        } else {
          next.add(pathValue);
        }
        selectionAnchorImagePathRef.current = pathValue;
        return next;
      });
    },
    [isImage]
  );

  const handleSelectImageCard = useCallback(
    (
      pathValue: string,
      options?: { shiftKey?: boolean; ctrlKey?: boolean; metaKey?: boolean }
    ) => {
      const shiftRangeRequested = Boolean(options?.shiftKey);
      const additiveSelectionRequested = Boolean(options?.ctrlKey || options?.metaKey);
      setSelectedFiles((prev) => {
        const next = new Set(prev);
        const imagePaths = displayImageEntriesRef.current
          .filter((entry) => entry.type === "file" && isImage(entry.name))
          .map((entry) => entry.path);

        if (shiftRangeRequested) {
          const anchorPath = selectionAnchorImagePathRef.current;
          const anchorIndex = anchorPath ? imagePaths.indexOf(anchorPath) : -1;
          const targetIndex = imagePaths.indexOf(pathValue);
          if (anchorIndex >= 0 && targetIndex >= 0) {
            if (!additiveSelectionRequested) {
              next.clear();
            }
            const [start, end] =
              anchorIndex <= targetIndex
                ? [anchorIndex, targetIndex]
                : [targetIndex, anchorIndex];
            for (let index = start; index <= end; index += 1) {
              next.add(imagePaths[index]);
            }
            selectionAnchorImagePathRef.current = pathValue;
            return next;
          }
        }

        if (additiveSelectionRequested) {
          if (next.has(pathValue)) {
            next.delete(pathValue);
          } else {
            next.add(pathValue);
          }
          selectionAnchorImagePathRef.current = pathValue;
          return next;
        }

        if (next.size === 1 && next.has(pathValue)) {
          selectionAnchorImagePathRef.current = pathValue;
          return next;
        }

        next.clear();
        next.add(pathValue);
        selectionAnchorImagePathRef.current = pathValue;
        return next;
      });
    },
    [isImage]
  );

  const syncSelectionForContextMenuEntry = useCallback((entry: DraftEntry) => {
    if (entry.type !== "file") return;
    setSelectedTreeFolders(new Set());
    setSelectedFiles((prev) => {
      if (prev.has(entry.path)) return prev;
      selectionAnchorImagePathRef.current = entry.path;
      return new Set([entry.path]);
    });
  }, []);

  const handleToggleAllNonImageFiles = useCallback(() => {
    setSelectedFiles((prev) => {
      const next = new Set(prev);
      const nonImageFilePaths = entries
        .filter((entry) => entry.type === "file" && !isImage(entry.name))
        .map((entry) => entry.path);
      const allSelected =
        nonImageFilePaths.length > 0 && nonImageFilePaths.every((p) => next.has(p));
      nonImageFilePaths.forEach((p) => {
        if (allSelected) next.delete(p);
        else next.add(p);
      });
      return next;
    });
  }, [entries, isImage]);

  const clearSelectedFilesForImageActions = useCallback(() => {
    selectionAnchorImagePathRef.current = null;
    setSelectedFiles(new Set());
  }, []);

  const isTextFileEditable = useCallback((entry: DraftEntry) => {
    if (entry.type !== "file") return false;
    const name = entry.name.toLowerCase();
    return name.endsWith(".txt") || name.endsWith(".json");
  }, []);

  const formatSizeKb = useCallback((bytes: number) => {
    const kb = bytes / 1024;
    if (kb >= 100) {
      return `${Math.round(kb)} KB`;
    }
    return `${Math.round(kb * 10) / 10} KB`;
  }, []);

  const resolveSpuImageExplorerPath = useCallback((row: DraftSpuRow) => {
    const candidates: string[] = [];
    if (row.draft_image_folder) candidates.push(row.draft_image_folder);
    if (row.draft_main_image_url) candidates.push(row.draft_main_image_url);
    if (Array.isArray(row.draft_image_urls)) {
      row.draft_image_urls.forEach((value) => {
        if (value) candidates.push(String(value));
      });
    }
    for (const candidate of candidates) {
      const relative = tryExtractDraftRelativePath(candidate);
      if (!relative) continue;
      const parts = relative.split("/").filter(Boolean);
      if (parts.length >= 2) {
        return parts.join("/");
      }
    }
    if (row.draft_spu) {
      const currentRun =
        selectedFolder ||
        folders.find((folder) => folder.path)?.path ||
        "";
      if (currentRun) {
        return `${currentRun}/${row.draft_spu}`;
      }
    }
    return null;
  }, [folders, selectedFolder]);

  const resolveSpuMainFolderPath = useCallback(
    (row: DraftSpuRow) => {
      const targetPath = resolveSpuImageExplorerPath(row);
      const targetParts = targetPath?.split("/").filter(Boolean) ?? [];
      if (targetParts.length >= 2) {
        return `${targetParts[0]}/${targetParts[1]}`;
      }

      const run = String(selectedFolder || "").trim();
      const spu = String(row.draft_spu || "").trim();
      if (run && spu) {
        return `${run}/${spu}`;
      }
      return null;
    },
    [resolveSpuImageExplorerPath, selectedFolder]
  );
  const completedSpuByCode = useMemo(() => {
    const next: Record<string, boolean> = {};
    spuRows.forEach((row) => {
      const spu = String(row.draft_spu || "").trim();
      if (!spu) return;
      const spuMainFolderPath = resolveSpuMainFolderPath(row);
      next[spu.toUpperCase()] = Boolean(
        spuMainFolderPath && completedSpuFolders.has(spuMainFolderPath)
      );
    });
    return next;
  }, [completedSpuFolders, resolveSpuMainFolderPath, spuRows]);
  const isSpuImagesMarkedCompleted = useCallback(
    (spuValue: string | null | undefined) => {
      const spu = String(spuValue || "").trim();
      if (!spu) return false;
      const normalizedSpu = spu.toUpperCase();
      if (Object.prototype.hasOwnProperty.call(completedSpuByCode, normalizedSpu)) {
        return Boolean(completedSpuByCode[normalizedSpu]);
      }
      const run = String(selectedFolder || "").trim();
      return Boolean(run && completedSpuFolders.has(`${run}/${spu}`));
    },
    [completedSpuByCode, completedSpuFolders, selectedFolder]
  );

  const openSpuImagesInExplorer = useCallback(
    (row: DraftSpuRow) => {
      const targetPath = resolveSpuImageExplorerPath(row);
      if (!targetPath) {
        setError(`Unable to resolve image folder for ${row.draft_spu}.`);
        return;
      }
      const parts = targetPath.split("/").filter(Boolean);
      if (parts.length === 0) {
        setError(`Unable to resolve image folder for ${row.draft_spu}.`);
        return;
      }
      const run = parts[0];
      pendingFolderOpenPathRef.current = targetPath;
      setSelectedFolder(run);
      setCurrentPath(targetPath);
    },
    [resolveSpuImageExplorerPath]
  );

  useEffect(() => {
    if (initialOpenSpuHandledRef.current) return;
    const requestedSpu = String(initialOpenSpu || "").trim().toUpperCase();
    if (!requestedSpu) return;
    if (draftLoading) return;

    const targetRow = spuRows.find(
      (row) => String(row.draft_spu || "").trim().toUpperCase() === requestedSpu
    );

    if (!targetRow) {
      initialOpenSpuHandledRef.current = true;
      setError(`SPU ${requestedSpu} was not found in Draft Explorer.`);
      return;
    }

    openSpuImagesInExplorer(targetRow);
    initialOpenSpuHandledRef.current = true;
  }, [draftLoading, initialOpenSpu, openSpuImagesInExplorer, spuRows]);

  const fetchVariantEditorThumbs = useCallback(
    async (spu: string) => {
      const targetSpu = String(spu || "").trim();
      if (!targetSpu) {
        setVariantsEditorThumbs([]);
        return;
      }
      const spuRow = spuRows.find((row) => row.draft_spu === targetSpu) ?? null;
      let rootPath = spuRow ? resolveSpuImageExplorerPath(spuRow) : null;
      if (!rootPath && selectedFolder) {
        rootPath = `${selectedFolder}/${targetSpu}`;
      }
      if (!rootPath) {
        setVariantsEditorThumbs([]);
        return;
      }
      setVariantsEditorThumbsLoading(true);
      try {
        const rootEntries = await listPathEntries(rootPath);
        const normalizeToken = (value: string) =>
          value.toLowerCase().replace(/[\s_-]+/g, "");
        const variantDirs = rootEntries
          .filter((entry) => entry.type === "dir")
          .map((entry) => {
            const normalized = normalizeToken(entry.name);
            if (!normalized.includes("variant")) {
              return { entry, score: -1 };
            }
            if (normalized.includes("reject") || normalized.includes("mismatch")) {
              return { entry, score: -1 };
            }
            if (normalized === "variantimages" || normalized === "variantimage") {
              return { entry, score: 4 };
            }
            if (normalized.includes("variant") && normalized.includes("image")) {
              return { entry, score: 3 };
            }
            return { entry, score: 1 };
          })
          .filter((row) => row.score > 0)
          .sort((left, right) => {
            if (right.score !== left.score) return right.score - left.score;
            return left.entry.name.localeCompare(right.entry.name);
          });
        if (variantDirs.length === 0) {
          setVariantsEditorThumbs([]);
          return;
        }
        const primaryVariantDir = variantDirs[0].entry;
        const variantEntries = await listPathEntries(primaryVariantDir.path);
        const directVariantImages = variantEntries
          .filter((entry) => entry.type === "file" && isImage(entry.name))
          .sort((left, right) => left.name.localeCompare(right.name));
        setVariantsEditorThumbs(directVariantImages);
      } catch {
        setVariantsEditorThumbs([]);
      } finally {
        setVariantsEditorThumbsLoading(false);
      }
    },
    [
      isImage,
      listPathEntries,
      resolveSpuImageExplorerPath,
      selectedFolder,
      spuRows,
    ]
  );

  const mapDraftSkuToVariantEditorRow = useCallback(
    (row: DraftSkuRow): DraftVariantEditorRow => {
      const raw = parseDraftRawRow(row.draft_raw_row);
      const variationColor = toText(raw.variation_color_se).trim();
      const variationSize = toText(raw.variation_size_se).trim();
      const variationOther = toText(raw.variation_other_se).trim();
      const variationAmount = toText(raw.variation_amount_se).trim();
      const readRawText = (...keys: string[]) => {
        for (const key of keys) {
          const value = toText(raw[key]).trim();
          if (value) return value;
        }
        return "";
      };
      const readField = (
        value: unknown,
        ...rawKeys: string[]
      ) => {
        const normalized = toText(value).trim();
        return normalized || readRawText(...rawKeys);
      };
      const shippingClass = normalizeShippingClassCode(
        row.draft_shipping_class ?? raw.draft_shipping_class
      );
      const optionColorZh = toText(row.draft_option1).trim();
      const optionSizeZh = toText(row.draft_option2).trim();
      const optionOtherZh = toText(row.draft_option3).trim();
      const optionAmountZh = toText(row.draft_option4).trim();
      const combined =
        buildVariantCombinedZhValue({
          draft_option1: optionColorZh,
          draft_option2: optionSizeZh,
          draft_option3: optionOtherZh,
          draft_option4: optionAmountZh,
          fallback: toText(row.draft_option_combined_zh).trim(),
        });
      return {
        key: createVariantEditorKey(),
        id: String(row.id || "").trim() || null,
        draft_spu: row.draft_spu ?? "",
        draft_sku: row.draft_sku ?? "",
        draft_option1: optionColorZh,
        draft_option2: optionSizeZh,
        draft_option3: optionOtherZh,
        draft_option4: optionAmountZh,
        draft_option_combined_zh: combined,
        draft_price: row.draft_price == null ? "" : String(row.draft_price),
        draft_weight: row.draft_weight == null ? "" : String(row.draft_weight),
        draft_weight_unit: row.draft_weight_unit ?? "",
        draft_option1_zh: readField(row.draft_option1_zh, "draft_option1_zh", "option1_zh"),
        draft_option2_zh: readField(row.draft_option2_zh, "draft_option2_zh", "option2_zh"),
        draft_option3_zh: readField(row.draft_option3_zh, "draft_option3_zh", "option3_zh"),
        draft_option4_zh: readField(row.draft_option4_zh, "draft_option4_zh", "option4_zh"),
        draft_compare_at_price: readField(
          row.draft_compare_at_price,
          "draft_compare_at_price",
          "compare_at_price"
        ),
        draft_cost: readField(row.draft_cost, "draft_cost", "cost"),
        draft_barcode: readField(row.draft_barcode, "draft_barcode", "barcode"),
        draft_variant_image_url: row.draft_variant_image_url ?? "",
        draft_shipping_name_en: readField(
          row.draft_shipping_name_en,
          "draft_shipping_name_en",
          "shipping_name_en",
          "EN_shipname"
        ),
        draft_short_title_zh: readField(
          row.draft_short_title_zh,
          "draft_short_title_zh",
          "short_title_zh",
          "CN_title"
        ),
        draft_shipping_name_zh: readField(
          row.draft_shipping_name_zh,
          "draft_shipping_name_zh",
          "shipping_name_zh",
          "CN_shipname"
        ),
        draft_shipping_class: shippingClass,
        draft_taxable: readField(row.draft_taxable, "draft_taxable", "taxable"),
        draft_tax_code: readField(row.draft_tax_code, "draft_tax_code", "tax_code"),
        draft_hs_code: readField(row.draft_hs_code, "draft_hs_code", "hs_code", "HS_code"),
        draft_country_of_origin: readField(
          row.draft_country_of_origin,
          "draft_country_of_origin",
          "country_of_origin"
        ),
        draft_category_code_fq: readField(
          row.draft_category_code_fq,
          "draft_category_code_fq",
          "category_code_fq"
        ),
        draft_category_code_ld: readField(
          row.draft_category_code_ld,
          "draft_category_code_ld",
          "category_code_ld"
        ),
        draft_supplier_name: readField(
          row.draft_supplier_name,
          "draft_supplier_name",
          "supplier_name"
        ),
        draft_supplier_location: readField(
          row.draft_supplier_location,
          "draft_supplier_location",
          "supplier_location"
        ),
        draft_b2b_dropship_price_se: readField(
          row.draft_b2b_dropship_price_se,
          "draft_b2b_dropship_price_se",
          "b2b_dropship_price_se"
        ),
        draft_b2b_dropship_price_no: readField(
          row.draft_b2b_dropship_price_no,
          "draft_b2b_dropship_price_no",
          "b2b_dropship_price_no"
        ),
        draft_b2b_dropship_price_dk: readField(
          row.draft_b2b_dropship_price_dk,
          "draft_b2b_dropship_price_dk",
          "b2b_dropship_price_dk"
        ),
        draft_b2b_dropship_price_fi: readField(
          row.draft_b2b_dropship_price_fi,
          "draft_b2b_dropship_price_fi",
          "b2b_dropship_price_fi"
        ),
        draft_purchase_price_cny: readField(
          row.draft_purchase_price_cny,
          "draft_purchase_price_cny",
          "purchase_price_cny",
          "purchase_price"
        ),
        variation_color_se: variationColor,
        variation_size_se: variationSize,
        variation_other_se: variationOther,
        variation_amount_se: variationAmount,
        draft_raw_row: raw,
      };
    },
    []
  );

  const buildVariantRowsPayload = useCallback((rows: DraftVariantEditorRow[]) => {
    const parseOptionalNumber = (value: string) => {
      const text = String(value || "").trim();
      if (!text) return null;
      const normalized = text.replace(/\s+/g, "").replace(",", ".");
      const numeric = Number(normalized);
      return Number.isFinite(numeric) ? numeric : null;
    };
    return rows.map((row) => {
      const raw = {
        ...(row.draft_raw_row ?? {}),
        draft_option1: row.draft_option1.trim(),
        draft_option2: row.draft_option2.trim(),
        draft_option3: row.draft_option3.trim(),
        draft_option4: row.draft_option4.trim(),
        variation_color_se: row.variation_color_se.trim(),
        variation_size_se: row.variation_size_se.trim(),
        variation_other_se: row.variation_other_se.trim(),
        variation_amount_se: row.variation_amount_se.trim(),
        draft_shipping_class: normalizeShippingClassCode(row.draft_shipping_class) || null,
      };
      const combined =
        buildVariantCombinedZhValue({
          draft_option1: row.draft_option1,
          draft_option2: row.draft_option2,
          draft_option3: row.draft_option3,
          draft_option4: row.draft_option4,
          fallback: row.draft_option_combined_zh,
        });
      return {
        id: row.id,
        draft_sku: row.draft_sku.trim() || null,
        draft_option1: row.draft_option1.trim() || null,
        draft_option2: row.draft_option2.trim() || null,
        draft_option3: row.draft_option3.trim() || null,
        draft_option4: row.draft_option4.trim() || null,
        draft_option_combined_zh: combined || null,
        draft_price: parseOptionalNumber(row.draft_price),
        draft_weight: parseOptionalNumber(row.draft_weight),
        draft_weight_unit: row.draft_weight_unit.trim() || null,
        draft_option1_zh: row.draft_option1_zh.trim() || null,
        draft_option2_zh: row.draft_option2_zh.trim() || null,
        draft_option3_zh: row.draft_option3_zh.trim() || null,
        draft_option4_zh: row.draft_option4_zh.trim() || null,
        draft_compare_at_price: parseOptionalNumber(row.draft_compare_at_price),
        draft_cost: parseOptionalNumber(row.draft_cost),
        draft_barcode: row.draft_barcode.trim() || null,
        draft_variant_image_url: row.draft_variant_image_url.trim() || null,
        draft_shipping_name_en: row.draft_shipping_name_en.trim() || null,
        draft_short_title_zh: row.draft_short_title_zh.trim() || null,
        draft_shipping_name_zh: row.draft_shipping_name_zh.trim() || null,
        draft_shipping_class: normalizeShippingClassCode(row.draft_shipping_class) || null,
        draft_taxable: row.draft_taxable.trim() || null,
        draft_tax_code: row.draft_tax_code.trim() || null,
        draft_hs_code: row.draft_hs_code.trim() || null,
        draft_country_of_origin: row.draft_country_of_origin.trim() || null,
        draft_category_code_fq: row.draft_category_code_fq.trim() || null,
        draft_category_code_ld: row.draft_category_code_ld.trim() || null,
        draft_supplier_name: row.draft_supplier_name.trim() || null,
        draft_supplier_location: row.draft_supplier_location.trim() || null,
        draft_b2b_dropship_price_se: parseOptionalNumber(row.draft_b2b_dropship_price_se),
        draft_b2b_dropship_price_no: parseOptionalNumber(row.draft_b2b_dropship_price_no),
        draft_b2b_dropship_price_dk: parseOptionalNumber(row.draft_b2b_dropship_price_dk),
        draft_b2b_dropship_price_fi: parseOptionalNumber(row.draft_b2b_dropship_price_fi),
        draft_purchase_price_cny: parseOptionalNumber(row.draft_purchase_price_cny),
        variation_color_se: row.variation_color_se.trim(),
        variation_size_se: row.variation_size_se.trim(),
        variation_other_se: row.variation_other_se.trim(),
        variation_amount_se: row.variation_amount_se.trim(),
        draft_raw_row: raw,
      };
    });
  }, []);

  const closeVariantsEditor = useCallback((force = false) => {
    if (
      !force &&
      (variantsEditorSaving || variantsEditorAiRunning || variantsEditorSkuRegenerating)
    )
      return;
    setVariantsEditorOpen(false);
    setVariantsEditorSpu("");
    setVariantsEditorRows([]);
    setVariantsEditorThumbs([]);
    setVariantsEditorError(null);
    setVariantsEditorSelectedRows(new Set());
    setVariantsEditorPacksText("");
    setVariantsEditorAiPrompt("");
    setVariantsEditorSkuRegenerating(false);
    setVariantsEditorSort({ key: null, direction: "asc" });
  }, [variantsEditorAiRunning, variantsEditorSaving, variantsEditorSkuRegenerating]);

  const openVariantsEditor = useCallback(
    async (spu: string | null | undefined) => {
      const targetSpu = String(spu || "").trim();
      if (!targetSpu) return;
      setVariantsEditorOpen(true);
      setVariantsEditorSpu(targetSpu);
      setVariantsEditorRows([]);
      setVariantsEditorThumbs([]);
      setVariantsEditorSelectedRows(new Set());
      setVariantsEditorSort({ key: null, direction: "asc" });
      setVariantsEditorError(null);
      setVariantsEditorLoading(true);
      void fetchVariantEditorThumbs(targetSpu);
      try {
        const url = new URL("/api/drafts/variants", window.location.origin);
        url.searchParams.set("spu", targetSpu);
        const response = await fetch(url.toString());
        const payload = await response.json().catch(() => ({}));
        if (!response.ok) {
          throw new Error(payload?.error || "Unable to load variants.");
        }
        const rows = Array.isArray(payload?.items)
          ? (payload.items as DraftSkuRow[]).map(mapDraftSkuToVariantEditorRow)
          : [];
        setVariantsEditorRows(rows);
      } catch (err) {
        setVariantsEditorError((err as Error).message);
      } finally {
        setVariantsEditorLoading(false);
      }
    },
    [fetchVariantEditorThumbs, mapDraftSkuToVariantEditorRow]
  );

  const handleVariantEditorCellChange = useCallback(
    (rowKey: string, field: keyof DraftVariantEditorRow, value: string) => {
      setVariantsEditorRows((prev) =>
        prev.map((row) => {
          if (row.key !== rowKey) return row;
          const nextRow = { ...row, [field]: value } as DraftVariantEditorRow;
          return nextRow;
        })
      );
    },
    []
  );

  const handleVariantEditorToggleRow = useCallback((rowKey: string) => {
    setVariantsEditorSelectedRows((prev) => {
      const next = new Set(prev);
      if (next.has(rowKey)) {
        next.delete(rowKey);
      } else {
        next.add(rowKey);
      }
      return next;
    });
  }, []);

  const handleVariantEditorToggleAll = useCallback(() => {
    setVariantsEditorSelectedRows((prev) => {
      if (variantsEditorRows.length > 0 && prev.size === variantsEditorRows.length) {
        return new Set();
      }
      return new Set(variantsEditorRows.map((row) => row.key));
    });
  }, [variantsEditorRows]);

  const handleVariantEditorSort = useCallback((key: VariantEditorSortKey) => {
    setVariantsEditorSort((prev) => {
      if (prev.key === key) {
        return {
          key,
          direction: prev.direction === "asc" ? "desc" : "asc",
        };
      }
      return {
        key,
        direction: "asc",
      };
    });
  }, []);

  const variantsEditorSortedRows = useMemo(() => {
    const sortKey = variantsEditorSort.key;
    if (!sortKey) {
      return variantsEditorRows;
    }
    const getValue = (row: DraftVariantEditorRow, key: VariantEditorSortKey) => {
      if (key === "sku") return row.draft_sku;
      if (key === "color") return row.variation_color_se || row.draft_option1;
      if (key === "size") return row.variation_size_se || row.draft_option2;
      if (key === "order") return row.variation_other_se || row.draft_option3;
      return row.variation_amount_se || row.draft_option4;
    };
    const parseNumericAmount = (value: string) => {
      const normalized = String(value || "")
        .trim()
        .replace(",", ".")
        .match(/-?\d+(?:\.\d+)?/);
      if (!normalized) return null;
      const numeric = Number(normalized[0]);
      return Number.isFinite(numeric) ? numeric : null;
    };
    const directionFactor = variantsEditorSort.direction === "asc" ? 1 : -1;
    return [...variantsEditorRows].sort((left, right) => {
      const leftValue = String(getValue(left, sortKey) || "").trim();
      const rightValue = String(getValue(right, sortKey) || "").trim();
      if (sortKey === "amount") {
        const leftNumeric = parseNumericAmount(leftValue);
        const rightNumeric = parseNumericAmount(rightValue);
        if (leftNumeric != null && rightNumeric != null && leftNumeric !== rightNumeric) {
          return (leftNumeric - rightNumeric) * directionFactor;
        }
      }
      return (
        leftValue.localeCompare(rightValue, undefined, {
          numeric: true,
          sensitivity: "base",
        }) * directionFactor
      );
    });
  }, [variantsEditorRows, variantsEditorSort]);

  const variantsEditorColumnStyles = useMemo(() => {
    const sample = variantsEditorRows.slice(0, 420);
    const makeStyle = (
      headerText: string,
      values: unknown[],
      options: {
        minPx: number;
        maxPx: number;
        headerAsMax?: boolean;
        headerPaddingPx?: number;
        contentPaddingPx?: number;
      }
    ) => {
      const width = computeAdaptiveColumnWidthPx(headerText, values, options);
      return {
        width: `${width}px`,
        minWidth: `${options.minPx}px`,
        maxWidth: `${options.maxPx}px`,
      };
    };

    return {
      selection: {
        width: "44px",
        minWidth: "44px",
        maxWidth: "44px",
      },
      sku: makeStyle(
        "SKU",
        sample.map((row) => row.draft_sku),
        { minPx: 85, maxPx: 320, headerPaddingPx: 16, contentPaddingPx: 16 }
      ),
      colorSe: makeStyle(
        "Color (SE)",
        sample.map((row) => row.variation_color_se || row.draft_option1),
        { minPx: 70, maxPx: 250, headerPaddingPx: 16, contentPaddingPx: 16 }
      ),
      sizeSe: makeStyle(
        "Size (SE)",
        sample.map((row) => row.variation_size_se || row.draft_option2),
        { minPx: 64, maxPx: 190, headerPaddingPx: 16, contentPaddingPx: 16 }
      ),
      otherSe: makeStyle(
        "Other (SE)",
        sample.map((row) => row.variation_other_se || row.draft_option3),
        { minPx: 72, maxPx: 320, headerPaddingPx: 16, contentPaddingPx: 16 }
      ),
      amountSe: makeStyle(
        "Amount (SE)",
        sample.map((row) => row.variation_amount_se || row.draft_option4),
        { minPx: 70, maxPx: 220, headerPaddingPx: 16, contentPaddingPx: 16 }
      ),
      optionCombinedZh: makeStyle(
        "Option Combined (ZH)",
        sample.map((row) => row.draft_option_combined_zh),
        { minPx: 90, maxPx: 500, headerPaddingPx: 16, contentPaddingPx: 16 }
      ),
      colorZh: makeStyle(
        "Color (ZH)",
        sample.map((row) => row.draft_option1),
        { minPx: 70, maxPx: 250, headerPaddingPx: 16, contentPaddingPx: 16 }
      ),
      sizeZh: makeStyle(
        "Size (ZH)",
        sample.map((row) => row.draft_option2),
        { minPx: 64, maxPx: 190, headerPaddingPx: 16, contentPaddingPx: 16 }
      ),
      otherZh: makeStyle(
        "Other (ZH)",
        sample.map((row) => row.draft_option3),
        { minPx: 72, maxPx: 320, headerPaddingPx: 16, contentPaddingPx: 16 }
      ),
      amountZh: makeStyle(
        "Amount (ZH)",
        sample.map((row) => row.draft_option4),
        { minPx: 70, maxPx: 220, headerPaddingPx: 16, contentPaddingPx: 16 }
      ),
      shippingClass: makeStyle(
        "Shipping class",
        sample.map((row) => row.draft_shipping_class).concat("PBA"),
        { minPx: 112, maxPx: 170, headerPaddingPx: 16, contentPaddingPx: 16 }
      ),
      price: makeStyle(
        "Price",
        sample.map((row) => row.draft_price).concat("99999"),
        { minPx: 60, maxPx: 96, headerPaddingPx: 14, contentPaddingPx: 14 }
      ),
      weight: makeStyle(
        "Weight",
        sample.map((row) => row.draft_weight).concat("99999"),
        { minPx: 64, maxPx: 100, headerPaddingPx: 14, contentPaddingPx: 14 }
      ),
    };
  }, [variantsEditorRows]);

  const handleVariantEditorAddRow = useCallback(() => {
    const fallbackSkuBase = variantsEditorSpu || "SKU";
    setVariantsEditorRows((prev) => {
      const template = prev[prev.length - 1];
      return [
        ...prev,
        {
          key: createVariantEditorKey(),
          id: null,
          draft_spu: variantsEditorSpu,
          draft_sku: template?.draft_sku
            ? `${template.draft_sku}-copy`
            : `${fallbackSkuBase}-copy`,
          draft_option1: template?.draft_option1 ?? "",
          draft_option2: template?.draft_option2 ?? "",
          draft_option3: template?.draft_option3 ?? "",
          draft_option4: template?.draft_option4 ?? "",
          draft_option_combined_zh: "",
          draft_price: template?.draft_price ?? "",
          draft_weight: template?.draft_weight ?? "",
          draft_weight_unit: template?.draft_weight_unit ?? "",
          draft_option1_zh: template?.draft_option1_zh ?? "",
          draft_option2_zh: template?.draft_option2_zh ?? "",
          draft_option3_zh: template?.draft_option3_zh ?? "",
          draft_option4_zh: template?.draft_option4_zh ?? "",
          draft_compare_at_price: template?.draft_compare_at_price ?? "",
          draft_cost: template?.draft_cost ?? "",
          draft_barcode: template?.draft_barcode ?? "",
          draft_variant_image_url: "",
          draft_shipping_name_en: template?.draft_shipping_name_en ?? "",
          draft_short_title_zh: template?.draft_short_title_zh ?? "",
          draft_shipping_name_zh: template?.draft_shipping_name_zh ?? "",
          draft_shipping_class: template?.draft_shipping_class ?? "",
          draft_taxable: template?.draft_taxable ?? "",
          draft_tax_code: template?.draft_tax_code ?? "",
          draft_hs_code: template?.draft_hs_code ?? "",
          draft_country_of_origin: template?.draft_country_of_origin ?? "",
          draft_category_code_fq: template?.draft_category_code_fq ?? "",
          draft_category_code_ld: template?.draft_category_code_ld ?? "",
          draft_supplier_name: template?.draft_supplier_name ?? "",
          draft_supplier_location: template?.draft_supplier_location ?? "",
          draft_b2b_dropship_price_se: template?.draft_b2b_dropship_price_se ?? "",
          draft_b2b_dropship_price_no: template?.draft_b2b_dropship_price_no ?? "",
          draft_b2b_dropship_price_dk: template?.draft_b2b_dropship_price_dk ?? "",
          draft_b2b_dropship_price_fi: template?.draft_b2b_dropship_price_fi ?? "",
          draft_purchase_price_cny: template?.draft_purchase_price_cny ?? "",
          variation_color_se: template?.variation_color_se ?? "",
          variation_size_se: template?.variation_size_se ?? "",
          variation_other_se: template?.variation_other_se ?? "",
          variation_amount_se: template?.variation_amount_se ?? "",
          draft_raw_row: { ...(template?.draft_raw_row ?? {}) },
        },
      ];
    });
  }, [variantsEditorSpu]);

  const handleVariantEditorDeleteSelected = useCallback(() => {
    if (variantsEditorSelectedRows.size === 0) return;
    setVariantsEditorRows((prev) =>
      prev.filter((row) => !variantsEditorSelectedRows.has(row.key))
    );
    setVariantsEditorSelectedRows(new Set());
  }, [variantsEditorSelectedRows]);

  const handleVariantEditorClearColumn = useCallback(
    (field: DraftVariantEditorClearableField) => {
      setVariantsEditorRows((prev) =>
        prev.map((row) => ({
          ...row,
          [field]: "",
          draft_raw_row: {
            ...(row.draft_raw_row ?? {}),
            [field]: "",
          },
        }))
      );
    },
    []
  );

  const handleVariantEditorRegenerateSkus = useCallback(async () => {
    if (
      variantsEditorLoading ||
      variantsEditorSaving ||
      variantsEditorAiRunning ||
      variantsEditorSkuRegenerating
    ) {
      return;
    }
    if (!variantsEditorSpu || variantsEditorRows.length === 0) return;
    setVariantsEditorSkuRegenerating(true);
    setVariantsEditorError(null);
    try {
      const response = await fetch("/api/drafts/variants/regenerate-skus", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          spu: variantsEditorSpu,
          variants: variantsEditorRows.map((row) => ({
            key: row.key,
            id: row.id,
            draft_option1: row.draft_option1,
            draft_option2: row.draft_option2,
            draft_option3: row.draft_option3,
            draft_option4: row.draft_option4,
            draft_option_combined_zh: row.draft_option_combined_zh,
            variation_color_se: row.variation_color_se,
            variation_size_se: row.variation_size_se,
            variation_other_se: row.variation_other_se,
            variation_amount_se: row.variation_amount_se,
            draft_raw_row: row.draft_raw_row,
          })),
        }),
      });
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        throw new Error(payload?.error || "Unable to regenerate SKUs.");
      }
      const updatedRows = Array.isArray(payload?.variants)
        ? (payload.variants as Array<Record<string, unknown>>)
        : [];
      const patchByKey = new Map(
        updatedRows
          .map((row) => [String(row.key || "").trim(), row] as const)
          .filter(([key]) => Boolean(key))
      );
      setVariantsEditorRows((prev) =>
        prev.map((row) => {
          const patch = patchByKey.get(row.key);
          if (!patch) return row;
          const nextSku = String(patch.draft_sku ?? row.draft_sku).trim();
          const nextCombined = String(
            patch.draft_option_combined_zh ?? row.draft_option_combined_zh
          ).trim();
          const patchRaw =
            patch.draft_raw_row &&
            typeof patch.draft_raw_row === "object" &&
            !Array.isArray(patch.draft_raw_row)
              ? (patch.draft_raw_row as Record<string, unknown>)
              : null;
          return {
            ...row,
            draft_sku: nextSku || row.draft_sku,
            draft_option_combined_zh: nextCombined || row.draft_option_combined_zh,
            draft_raw_row: patchRaw
              ? {
                  ...(row.draft_raw_row ?? {}),
                  ...patchRaw,
                }
              : row.draft_raw_row,
          };
        })
      );
      const warnings = Array.isArray(payload?.warnings)
        ? payload.warnings.map((value: unknown) => String(value || "").trim()).filter(Boolean)
        : [];
      if (warnings.length > 0) {
        setVariantsEditorError(
          `SKU regeneration completed with ${warnings.length} warning(s): ${warnings
            .slice(0, 2)
            .join("; ")}${warnings.length > 2 ? "..." : ""}`
        );
      }
    } catch (err) {
      setVariantsEditorError((err as Error).message);
    } finally {
      setVariantsEditorSkuRegenerating(false);
    }
  }, [
    variantsEditorAiRunning,
    variantsEditorLoading,
    variantsEditorRows,
    variantsEditorSaving,
    variantsEditorSkuRegenerating,
    variantsEditorSpu,
  ]);

  const handleVariantEditorAddPacks = useCallback(() => {
    const tokens = variantsEditorPacksText.match(/\d+/g) ?? [];
    const seenPacks = new Set<number>();
    const packValues: number[] = [];
    tokens.forEach((token) => {
      const numeric = Number(token);
      if (!Number.isFinite(numeric) || numeric <= 0 || seenPacks.has(numeric)) return;
      seenPacks.add(numeric);
      packValues.push(numeric);
    });
    if (packValues.length === 0) {
      setVariantsEditorError("Enter pack numbers, for example: 1, 2, 4, 10.");
      return;
    }
    setVariantsEditorError(null);
    setVariantsEditorRows((prev) => {
      const resolveSourcePackMultiplier = (row: DraftVariantEditorRow) => {
        const fromSku = extractSkuPackMultiplier(row.draft_sku);
        if (fromSku) return fromSku;
        const fromOption = parsePackLikeMultiplier(row.draft_option4);
        if (fromOption) return fromOption;
        const fromVariation = parsePackLikeMultiplier(row.variation_amount_se);
        if (fromVariation) return fromVariation;
        return 1;
      };
      const scalePackField = (
        rawValue: string,
        sourcePackMultiplier: number,
        targetPackMultiplier: number
      ) => {
        const numericValue = parseNumericFieldValue(rawValue);
        if (numericValue == null) return rawValue;
        const sourceMultiplier =
          Number.isFinite(sourcePackMultiplier) && sourcePackMultiplier > 0
            ? sourcePackMultiplier
            : 1;
        const targetMultiplier =
          Number.isFinite(targetPackMultiplier) && targetPackMultiplier > 0
            ? targetPackMultiplier
            : 1;
        const unitValue = numericValue / sourceMultiplier;
        const scaledValue = unitValue * targetMultiplier;
        return formatScaledVariantNumeric(scaledValue);
      };
      const originalRows: DraftVariantEditorRow[] = prev.map((row): DraftVariantEditorRow => {
        const skuBaseRaw = stripSkuPackSuffix(row.draft_sku);
        const skuBase = skuBaseRaw || String(variantsEditorSpu || "SKU").trim();
        const onePackLabel = "1";
        const sourcePackMultiplier = resolveSourcePackMultiplier(row);
        const onePackPrice = scalePackField(row.draft_price, sourcePackMultiplier, 1);
        const onePackWeight = scalePackField(row.draft_weight, sourcePackMultiplier, 1);
        return {
          ...row,
          draft_sku: `${skuBase}-1P`,
          draft_option4: onePackLabel,
          variation_amount_se: onePackLabel,
          draft_price: onePackPrice,
          draft_weight: onePackWeight,
          draft_option_combined_zh: buildVariantCombinedZhValue({
            draft_option1: row.draft_option1,
            draft_option2: row.draft_option2,
            draft_option3: row.draft_option3,
            draft_option4: onePackLabel,
            fallback: row.draft_option_combined_zh,
          }),
          draft_raw_row: {
            ...(row.draft_raw_row ?? {}),
            draft_option4: onePackLabel,
            variation_amount_se: onePackLabel,
            draft_price: onePackPrice || null,
            draft_weight: onePackWeight || null,
          } as Record<string, unknown>,
        };
      });
      const packsToClone = packValues.slice(1).filter((packValue) => packValue > 1);
      if (packsToClone.length === 0) {
        return originalRows;
      }
      const next: DraftVariantEditorRow[] = [...originalRows];
      const usedSkus = new Set(
        originalRows.map((row) => row.draft_sku.trim().toLowerCase()).filter(Boolean)
      );
      const createUniqueSku = (baseSku: string) => {
        const base = baseSku.trim() || `${variantsEditorSpu || "sku"}-copy`;
        let candidate = base;
        let index = 2;
        while (usedSkus.has(candidate.toLowerCase())) {
          candidate = `${base}-${index}`;
          index += 1;
        }
        usedSkus.add(candidate.toLowerCase());
        return candidate;
      };
      originalRows.forEach((row) => {
        const skuBaseRaw = stripSkuPackSuffix(row.draft_sku);
        const skuBase = skuBaseRaw || String(variantsEditorSpu || "SKU").trim();
        packsToClone.forEach((packValue) => {
          const label = String(packValue);
          const nextSku = createUniqueSku(`${skuBase}-${packValue}P`);
          const scaledPrice = scalePackField(row.draft_price, 1, packValue);
          const scaledWeight = scalePackField(row.draft_weight, 1, packValue);
          const cloned: DraftVariantEditorRow = {
            ...row,
            key: createVariantEditorKey(),
            id: null,
            draft_sku: nextSku,
            draft_option4: label,
            variation_amount_se: label,
            draft_price: scaledPrice,
            draft_weight: scaledWeight,
            draft_option_combined_zh: buildVariantCombinedZhValue({
              draft_option1: row.draft_option1,
              draft_option2: row.draft_option2,
              draft_option3: row.draft_option3,
              draft_option4: label,
              fallback: row.draft_option_combined_zh,
            }),
            draft_raw_row: {
              ...(row.draft_raw_row ?? {}),
              draft_option4: label,
              variation_amount_se: label,
              draft_price: scaledPrice || null,
              draft_weight: scaledWeight || null,
            } as Record<string, unknown>,
          };
          next.push(cloned);
        });
      });
      return next;
    });
  }, [variantsEditorPacksText, variantsEditorSpu]);

  const handleVariantEditorRunAi = useCallback(async () => {
    if (variantsEditorAiRunning || variantsEditorSaving || variantsEditorSkuRegenerating) return;
    const prompt = variantsEditorAiPrompt.trim();
    if (!variantsEditorSpu) return;
    if (!prompt) {
      setVariantsEditorError("Add instructions before running AI.");
      return;
    }
    setVariantsEditorAiRunning(true);
    setVariantsEditorError(null);
    try {
      const response = await fetch("/api/drafts/variants/ai-update", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          spu: variantsEditorSpu,
          instruction: prompt,
          variants: buildVariantRowsPayload(variantsEditorRows),
        }),
      });
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        throw new Error(payload?.error || "AI update failed.");
      }
      const nextRows = Array.isArray(payload?.variants)
        ? (payload.variants as Array<Record<string, unknown>>).map((row) =>
            mapDraftSkuToVariantEditorRow({
              id: String(row.id ?? ""),
              draft_sku: String(row.draft_sku ?? ""),
              draft_spu: String(row.draft_spu ?? variantsEditorSpu),
              draft_option1:
                row.draft_option1 == null ? null : String(row.draft_option1),
              draft_option2:
                row.draft_option2 == null ? null : String(row.draft_option2),
              draft_option3:
                row.draft_option3 == null ? null : String(row.draft_option3),
              draft_option4:
                row.draft_option4 == null ? null : String(row.draft_option4),
              draft_option_combined_zh: String(row.draft_option_combined_zh ?? ""),
              draft_price:
                row.draft_price == null ? null : (row.draft_price as number | string),
              draft_weight:
                row.draft_weight == null ? null : (row.draft_weight as number | string),
              draft_weight_unit: row.draft_weight_unit == null ? null : String(row.draft_weight_unit),
              draft_option1_zh:
                row.draft_option1_zh == null ? null : String(row.draft_option1_zh),
              draft_option2_zh:
                row.draft_option2_zh == null ? null : String(row.draft_option2_zh),
              draft_option3_zh:
                row.draft_option3_zh == null ? null : String(row.draft_option3_zh),
              draft_option4_zh:
                row.draft_option4_zh == null ? null : String(row.draft_option4_zh),
              draft_compare_at_price:
                row.draft_compare_at_price == null
                  ? null
                  : (row.draft_compare_at_price as number | string),
              draft_cost:
                row.draft_cost == null ? null : (row.draft_cost as number | string),
              draft_barcode: row.draft_barcode == null ? null : String(row.draft_barcode),
              draft_variant_image_url:
                row.draft_variant_image_url == null
                  ? null
                  : String(row.draft_variant_image_url),
              draft_shipping_name_en:
                row.draft_shipping_name_en == null
                  ? null
                  : String(row.draft_shipping_name_en),
              draft_short_title_zh:
                row.draft_short_title_zh == null ? null : String(row.draft_short_title_zh),
              draft_shipping_name_zh:
                row.draft_shipping_name_zh == null
                  ? null
                  : String(row.draft_shipping_name_zh),
              draft_shipping_class:
                row.draft_shipping_class == null
                  ? null
                  : String(row.draft_shipping_class),
              draft_taxable: row.draft_taxable == null ? null : String(row.draft_taxable),
              draft_tax_code: row.draft_tax_code == null ? null : String(row.draft_tax_code),
              draft_hs_code: row.draft_hs_code == null ? null : String(row.draft_hs_code),
              draft_country_of_origin:
                row.draft_country_of_origin == null
                  ? null
                  : String(row.draft_country_of_origin),
              draft_category_code_fq:
                row.draft_category_code_fq == null
                  ? null
                  : String(row.draft_category_code_fq),
              draft_category_code_ld:
                row.draft_category_code_ld == null
                  ? null
                  : String(row.draft_category_code_ld),
              draft_supplier_name:
                row.draft_supplier_name == null ? null : String(row.draft_supplier_name),
              draft_supplier_location:
                row.draft_supplier_location == null
                  ? null
                  : String(row.draft_supplier_location),
              draft_b2b_dropship_price_se:
                row.draft_b2b_dropship_price_se == null
                  ? null
                  : (row.draft_b2b_dropship_price_se as number | string),
              draft_b2b_dropship_price_no:
                row.draft_b2b_dropship_price_no == null
                  ? null
                  : (row.draft_b2b_dropship_price_no as number | string),
              draft_b2b_dropship_price_dk:
                row.draft_b2b_dropship_price_dk == null
                  ? null
                  : (row.draft_b2b_dropship_price_dk as number | string),
              draft_b2b_dropship_price_fi:
                row.draft_b2b_dropship_price_fi == null
                  ? null
                  : (row.draft_b2b_dropship_price_fi as number | string),
              draft_purchase_price_cny:
                row.draft_purchase_price_cny == null
                  ? null
                  : (row.draft_purchase_price_cny as number | string),
              draft_status: "draft",
              draft_updated_at: null,
              draft_raw_row:
                row.draft_raw_row && typeof row.draft_raw_row === "object"
                  ? (row.draft_raw_row as Record<string, unknown>)
                  : ({
                      variation_color_se: row.variation_color_se,
                      variation_size_se: row.variation_size_se,
                      variation_other_se: row.variation_other_se,
                      variation_amount_se: row.variation_amount_se,
                    } as Record<string, unknown>),
            })
          )
        : [];
      const previousById = new Map(
        variantsEditorRows
          .filter((row) => row.id)
          .map((row) => [String(row.id), row] as const)
      );
      const previousBySku = new Map(
        variantsEditorRows
          .map((row) => [row.draft_sku.trim().toLowerCase(), row] as const)
          .filter(([sku]) => Boolean(sku))
      );
      const fallbackTemplate = variantsEditorRows[0] ?? null;
      const nextRowsWithHidden = nextRows.map((row) => {
        const byId = row.id ? previousById.get(String(row.id)) : undefined;
        const bySku = row.draft_sku
          ? previousBySku.get(row.draft_sku.trim().toLowerCase())
          : undefined;
        const source = byId ?? bySku ?? fallbackTemplate;
        return source ? copyVariantEditorHiddenFields(row, source) : row;
      });
      setVariantsEditorRows(nextRowsWithHidden);
      setVariantsEditorSelectedRows(new Set());
    } catch (err) {
      setVariantsEditorError((err as Error).message);
    } finally {
      setVariantsEditorAiRunning(false);
    }
  }, [
    buildVariantRowsPayload,
    mapDraftSkuToVariantEditorRow,
    variantsEditorAiPrompt,
    variantsEditorAiRunning,
    variantsEditorRows,
    variantsEditorSaving,
    variantsEditorSkuRegenerating,
    variantsEditorSpu,
  ]);

  const handleVariantEditorSave = useCallback(async () => {
    if (
      !variantsEditorSpu ||
      variantsEditorSaving ||
      variantsEditorAiRunning ||
      variantsEditorSkuRegenerating
    )
      return;
    setVariantsEditorSaving(true);
    setVariantsEditorError(null);
    try {
      const response = await fetch("/api/drafts/variants/replace", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          spu: variantsEditorSpu,
          variants: buildVariantRowsPayload(variantsEditorRows),
        }),
      });
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        throw new Error(payload?.error || "Unable to save variants.");
      }
      await markSpuReviewConfirmed(variantsEditorSpu, "variants");
      await fetchSpuRows();
      await fetchSkuRows();
      closeVariantsEditor(true);
    } catch (err) {
      setVariantsEditorError((err as Error).message);
    } finally {
      setVariantsEditorSaving(false);
    }
  }, [
    buildVariantRowsPayload,
    closeVariantsEditor,
    fetchSkuRows,
    fetchSpuRows,
    markSpuReviewConfirmed,
    variantsEditorAiRunning,
    variantsEditorRows,
    variantsEditorSaving,
    variantsEditorSkuRegenerating,
    variantsEditorSpu,
  ]);

  const handleOpenFileViewer = useCallback(async (entry: DraftEntry) => {
    if (!isTextFileEditable(entry)) return;
    setFileViewerPath(entry.path);
    setFileViewerContent("");
    setFileViewerError(null);
    setFileViewerLoading(true);
    try {
      const response = await fetch(
        `/api/drafts/files/content?path=${encodeURIComponent(entry.path)}`
      );
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        throw new Error(payload?.error || "Unable to open file.");
      }
      setFileViewerContent(String(payload?.content ?? ""));
    } catch (err) {
      setFileViewerError((err as Error).message);
    } finally {
      setFileViewerLoading(false);
    }
  }, [isTextFileEditable]);

  const handleSaveFileViewer = useCallback(async () => {
    if (!fileViewerPath || fileViewerSaving) return;
    setFileViewerSaving(true);
    setFileViewerError(null);
    try {
      const response = await fetch("/api/drafts/files/content", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          path: fileViewerPath,
          content: fileViewerContent,
        }),
      });
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        throw new Error(payload?.error || "Unable to save file.");
      }
      if (currentPath) {
        refreshEntries(currentPath);
      }
      if (selectedFolder) {
        fetchFolderTree(selectedFolder);
      }
    } catch (err) {
      setFileViewerError((err as Error).message);
    } finally {
      setFileViewerSaving(false);
    }
  }, [
    currentPath,
    fetchFolderTree,
    fileViewerContent,
    fileViewerPath,
    fileViewerSaving,
    refreshEntries,
    selectedFolder,
  ]);

  const trySendPhotopeaFile = useCallback(() => {
    const targetWindow = photopeaIframeRef.current?.contentWindow;
    if (!targetWindow) return;
    if (!photopeaReadyRef.current) return;
    if (photopeaFileSentRef.current) return;
    const buffer = photopeaFileBufferRef.current;
    if (!buffer) return;
    photopeaFileSentRef.current = true;
    photopeaFileBufferRef.current = null;
    targetWindow.postMessage(buffer, "https://www.photopea.com", [buffer]);
  }, []);

  const closePhotopea = useCallback(() => {
    setPhotopeaOpen(false);
    setPhotopeaEntry(null);
    setPhotopeaReady(false);
    setPhotopeaLoading(false);
    setPhotopeaExporting(false);
    setPhotopeaPersisting(false);
    photopeaPersistingRef.current = false;
    setPhotopeaError(null);
    photopeaReadyRef.current = false;
    photopeaFileSentRef.current = false;
    photopeaFileBufferRef.current = null;
    photopeaExportBufferRef.current = null;
  }, []);

  const openPhotopeaEditor = useCallback(
    async (entry: DraftEntry) => {
      if (entry.type !== "file" || !isImage(entry.name)) return;
      setPhotopeaOpen(true);
      setPhotopeaEntry(entry);
      setPhotopeaReady(false);
      setPhotopeaError(null);
      setPhotopeaLoading(true);
      setPhotopeaExporting(false);
      setPhotopeaPersisting(false);
      photopeaPersistingRef.current = false;
      setPhotopeaSessionKey((prev) => prev + 1);
      photopeaReadyRef.current = false;
      photopeaFileSentRef.current = false;
      photopeaExportBufferRef.current = null;
      photopeaFileBufferRef.current = null;
      try {
        const response = await fetch(buildDraftDownloadUrl(entry.path, entry.modifiedAt));
        if (!response.ok) {
          const message = await response.text().catch(() => "");
          throw new Error(message || "Unable to load image.");
        }
        const buffer = await response.arrayBuffer();
        photopeaFileBufferRef.current = buffer;
        trySendPhotopeaFile();
      } catch (err) {
        setPhotopeaError((err as Error).message);
      } finally {
        setPhotopeaLoading(false);
      }
    },
    [buildDraftDownloadUrl, isImage, trySendPhotopeaFile]
  );

  const closeAutoLevelsDialog = useCallback(
    (force = false) => {
      if (!force && autoLevelsSubmitting) return;
      setAutoLevelsOpen(false);
      setAutoLevelsTargets([]);
      setAutoLevelsLevelPercent(50);
      setAutoLevelsQuality(92);
      setAutoLevelsError(null);
    },
    [autoLevelsSubmitting]
  );

  const openAutoLevelsForEntries = useCallback(
    (sourceEntries: DraftEntry[]) => {
      const targets = sourceEntries.filter(
        (item) => item.type === "file" && isImage(item.name)
      );
      if (targets.length === 0) {
        setError("Select at least one image.");
        return;
      }
      const limitedTargets = targets.slice(0, AUTO_LEVELS_MAX_IMAGES);
      setAutoLevelsTargets(limitedTargets);
      setAutoLevelsLevelPercent(50);
      setAutoLevelsQuality(92);
      setAutoLevelsError(
        targets.length > AUTO_LEVELS_MAX_IMAGES
          ? `Only the first ${AUTO_LEVELS_MAX_IMAGES} selected images are included.`
          : null
      );
      setAutoLevelsOpen(true);
    },
    [isImage]
  );

  const applyAutoLevelsForDialogTargets = useCallback(async () => {
    const targets = autoLevelsTargets.filter(
      (item) => item.type === "file" && isImage(item.name)
    );
    if (targets.length === 0 || autoLevelsSubmitting) return;

    const targetPaths = targets.map((item) => item.path);
    const levelPercent = clampAutoLevelsPercent(autoLevelsLevelPercent);
    const quality = clampAutoLevelsQuality(autoLevelsQuality);

    setAutoLevelsSubmitting(true);
    setError(null);
    setAutoLevelsError(null);
    setReloadingImagePaths((prev) => {
      const next = new Set(prev);
      targetPaths.forEach((pathValue) => next.add(pathValue));
      return next;
    });

    try {
      const response = await fetch("/api/drafts/images/auto-levels", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          paths: targetPaths,
          levelPercent,
          quality,
        }),
      });
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        throw new Error(payload?.error || "Auto levels failed.");
      }

      const updatedRows = Array.isArray(payload?.updated)
        ? (payload.updated as Array<Record<string, unknown>>)
        : [];
      const errors = Array.isArray(payload?.errors)
        ? (payload.errors as Array<Record<string, unknown>>)
        : [];

      const updatedMap = new Map<
        string,
        {
          modifiedAt: string;
          size: number;
          pixelQualityScore: number | null;
        }
      >();
      updatedRows.forEach((row) => {
        const rowPath = String(row.path || "").trim();
        if (!rowPath) return;
        const modifiedAt =
          String(row.modifiedAt || "").trim() || new Date().toISOString();
        const size =
          typeof row.size === "number" && Number.isFinite(row.size) ? row.size : 0;
        const pixelQualityScore =
          typeof row.pixelQualityScore === "number" &&
          Number.isFinite(row.pixelQualityScore)
            ? Math.round(row.pixelQualityScore)
            : null;
        updatedMap.set(rowPath, {
          modifiedAt,
          size,
          pixelQualityScore,
        });
      });

      if (updatedMap.size > 0) {
        setEntries((prev) =>
          prev.map((item) => {
            const update = updatedMap.get(item.path);
            if (!update) return item;
            return {
              ...item,
              modifiedAt: update.modifiedAt,
              size: update.size || item.size,
              pixelQualityScore:
                update.pixelQualityScore !== null
                  ? update.pixelQualityScore
                  : item.pixelQualityScore ?? null,
            };
          })
        );
        setImageDimensions((prev) => {
          const next: Record<string, { width: number; height: number }> = {
            ...prev,
          };
          targetPaths.forEach((pathValue) => {
            delete next[pathValue];
          });
          return next;
        });
      }

      if (selectedFolder) {
        await fetchFolderTree(selectedFolder);
      }

      if (errors.length > 0) {
        const preview = errors
          .slice(0, 2)
          .map((row) => String(row.error || "Unknown error"))
          .join("; ");
        setAutoLevelsError(
          `Failed to process ${errors.length} image(s). ${preview}${
            errors.length > 2 ? "..." : ""
          }`
        );
      } else {
        closeAutoLevelsDialog(true);
      }
    } catch (err) {
      setAutoLevelsError((err as Error).message);
    } finally {
      setAutoLevelsSubmitting(false);
      setReloadingImagePaths((prev) => {
        const next = new Set(prev);
        targetPaths.forEach((pathValue) => next.delete(pathValue));
        return next;
      });
    }
  }, [
    autoLevelsLevelPercent,
    autoLevelsQuality,
    autoLevelsSubmitting,
    autoLevelsTargets,
    closeAutoLevelsDialog,
    fetchFolderTree,
    isImage,
    selectedFolder,
  ]);

  const closeStackOverlayPickerDialog = useCallback(
    (force = false) => {
      if (!force && stackOverlaySubmitting) return;
      setStackOverlayPickerOpen(false);
      setStackOverlayTargets([]);
      setStackOverlayCandidateTargets([]);
      setStackOverlayMode("digideal_main_stack_overlay");
      setStackOverlayTopImagePath("");
      setStackOverlayInfo(null);
      setStackOverlayError(null);
    },
    [stackOverlaySubmitting]
  );

  const openStackOverlayPickerForEntries = useCallback(
    (
      sourceEntries: DraftEntry[],
      mode: "digideal_main_stack_overlay" | "stack_overlay" = "digideal_main_stack_overlay"
    ) => {
      const targets = sourceEntries.filter(
        (entry) => entry.type === "file" && isImage(entry.name)
      );
      const minImages =
        mode === "stack_overlay"
          ? STACK_OVERLAY_MIN_IMAGES
          : DIGIDEAL_MAIN_STACK_OVERLAY_MIN_IMAGES;
      if (targets.length < minImages) {
        setError(
          mode === "stack_overlay"
            ? "Select at least two images."
            : "Select at least three images (white-background cards + one scenic image)."
        );
        return;
      }

      const limitedTargets = targets.slice(0, STACK_IMAGES_MAX_IMAGES);
      const whiteCandidates = limitedTargets.filter((entry) =>
        isWhiteBackgroundPriorityImage(entry)
      );
      const candidateTargets =
        whiteCandidates.length > 0 ? whiteCandidates : limitedTargets;
      const firstCandidatePath = candidateTargets[0]?.path || "";

      let warningMessage: string | null = null;
      if (targets.length > STACK_IMAGES_MAX_IMAGES) {
        warningMessage = `Only the first ${STACK_IMAGES_MAX_IMAGES} selected images are included.`;
      } else if (whiteCandidates.length === 0) {
        warningMessage = "No clear white-background candidates detected; showing all selected images.";
      }

      setStackOverlayTargets(limitedTargets);
      setStackOverlayCandidateTargets(candidateTargets);
      setStackOverlayMode(mode);
      setStackOverlayTopImagePath(firstCandidatePath);
      setStackOverlayInfo(warningMessage);
      setStackOverlayError(null);
      setStackOverlayPickerOpen(true);
      setError(null);
    },
    [isImage]
  );

  const applyStackOverlayForDialogTargets = useCallback(async () => {
    if (stackOverlaySubmitting) return;
    const targets = stackOverlayTargets.filter(
      (entry) => entry.type === "file" && isImage(entry.name)
    );
    const isStandaloneMode = stackOverlayMode === "stack_overlay";
    const minImages = isStandaloneMode
      ? STACK_OVERLAY_MIN_IMAGES
      : DIGIDEAL_MAIN_STACK_OVERLAY_MIN_IMAGES;
    if (targets.length < minImages) {
      setStackOverlayError(
        isStandaloneMode ? "Select at least two images." : "Select at least three images."
      );
      return;
    }
    const selectedTopPath = String(stackOverlayTopImagePath || "").trim();
    if (!selectedTopPath) {
      setStackOverlayError("Select one top image.");
      return;
    }
    if (!stackOverlayCandidateTargets.some((entry) => entry.path === selectedTopPath)) {
      setStackOverlayError("Selected top image is no longer valid.");
      return;
    }

    const fallbackTargetPath = getParentFolderPathFromEntryPath(targets[0]?.path || "");
    const resolvedTargetPath = String(currentPath || fallbackTargetPath || "").trim();
    if (!resolvedTargetPath) {
      setStackOverlayError(
        isStandaloneMode
          ? "Unable to resolve target folder for Stack Overlay."
          : "Unable to resolve target folder for DigiDeal Main Stack Overlay."
      );
      return;
    }

    setError(null);
    const targetPaths = targets.map((entry) => entry.path);
    closeStackOverlayPickerDialog(true);
    clearSelectedFilesForImageActions();

    // Run long stack-overlay generation in the background so the gallery stays usable.
    void (async () => {
      try {
        const response = await fetch("/api/drafts/images/merge-digideal-main-stack-overlay", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            paths: targetPaths,
            targetPath: resolvedTargetPath,
            topImagePath: selectedTopPath,
            standalone: isStandaloneMode,
          }),
        });
        const payload = await response.json().catch(() => ({}));
        if (!response.ok) {
          throw new Error(
            payload?.error ||
              (isStandaloneMode
                ? "Unable to create Stack Overlay."
                : "Unable to create DigiDeal Main Stack Overlay.")
          );
        }

        const latestPath = String(currentPathRef.current || "").trim();
        if (latestPath === resolvedTargetPath) {
          await refreshEntries(resolvedTargetPath);
        }

        const latestSelectedFolder = String(selectedFolderRef.current || "").trim();
        if (latestSelectedFolder) {
          await fetchFolderTree(latestSelectedFolder);
        }
      } catch (err) {
        setError(
          (err as Error).message ||
            (isStandaloneMode
              ? "Unable to create Stack Overlay."
              : "Unable to create DigiDeal Main Stack Overlay.")
        );
      }
    })();
  }, [
    clearSelectedFilesForImageActions,
    closeStackOverlayPickerDialog,
    currentPath,
    fetchFolderTree,
    isImage,
    refreshEntries,
    stackOverlayCandidateTargets,
    stackOverlayMode,
    stackOverlaySubmitting,
    stackOverlayTargets,
    stackOverlayTopImagePath,
  ]);

  const requestPhotopeaExport = useCallback(() => {
    const win = photopeaIframeRef.current?.contentWindow;
    if (!win) return;
    setPhotopeaError(null);
    setPhotopeaExporting(true);
    photopeaExportBufferRef.current = null;
    win.postMessage("app.activeDocument.saveToOE('jpg:0.92');", "https://www.photopea.com");
  }, []);

  const savePhotopeaResult = useCallback(async () => {
    const buffer = photopeaExportBufferRef.current;
    const activeEntry = photopeaEntry;
    if (!buffer || !activeEntry || photopeaPersistingRef.current) return;
    photopeaExportBufferRef.current = null;
    setPhotopeaExporting(false);
    setPhotopeaPersisting(true);
    photopeaPersistingRef.current = true;
    setPhotopeaError(null);

    const oldPath = activeEntry.path;
    setReloadingImagePaths((prev) => new Set(prev).add(oldPath));
    try {
      const formData = new FormData();
      formData.append("path", oldPath);
      formData.append(
        "file",
        new Blob([buffer], { type: "image/jpeg" }),
        "photopea.jpg"
      );
      const response = await fetch("/api/drafts/images/replace", {
        method: "POST",
        body: formData,
      });
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        throw new Error(payload?.error || "Save failed.");
      }

      const newPath = String(payload?.path || oldPath);
      const newName = String(payload?.name || activeEntry.name);
      const newModifiedAt = String(payload?.modifiedAt || new Date().toISOString());
      const newSize = Number(payload?.size ?? activeEntry.size ?? 0);
      const newPixelQualityScore =
        typeof payload?.pixelQualityScore === "number" &&
        Number.isFinite(payload.pixelQualityScore)
          ? Math.round(payload.pixelQualityScore)
          : null;
      const newZimageUpscaled =
        typeof payload?.zimageUpscaled === "boolean"
          ? payload.zimageUpscaled
          : false;

      setEntries((prev) => {
        let hadNew = false;
        let hadOld = false;
        const next = prev
          .map((item) => {
            if (item.path === newPath) {
              hadNew = true;
              return {
                ...item,
                name: newName,
                path: newPath,
                modifiedAt: newModifiedAt,
                size: newSize,
                pixelQualityScore: newPixelQualityScore,
                zimageUpscaled: newZimageUpscaled,
              };
            }
            if (item.path === oldPath) {
              hadOld = true;
              // If we changed extension, drop the old entry and keep/patch the newPath entry above.
              return null;
            }
            return item;
          })
          .filter(Boolean) as DraftEntry[];

        if (!hadNew) {
          if (hadOld) {
            next.push({
              ...activeEntry,
              name: newName,
              path: newPath,
              modifiedAt: newModifiedAt,
              size: newSize,
              pixelQualityScore: newPixelQualityScore,
              zimageUpscaled: newZimageUpscaled,
            });
          }
        }
        return next;
      });
      setSelectedFiles((prev) => {
        if (!prev.has(oldPath)) return prev;
        const next = new Set(prev);
        next.delete(oldPath);
        next.add(newPath);
        return next;
      });
      setPreviewPath((prev) => (prev === oldPath ? newPath : prev));
      setImageDimensions((prev) => {
        const next: Record<string, { width: number; height: number }> = { ...prev };
        if (oldPath !== newPath) {
          delete next[oldPath];
        }
        return next;
      });
      setReloadingImagePaths((prev) => {
        const next = new Set(prev);
        if (oldPath !== newPath) {
          next.delete(oldPath);
        }
        next.add(newPath);
        return next;
      });
      setPhotopeaEntry((prev) =>
        prev
          ? {
              ...prev,
              path: newPath,
              name: newName,
              modifiedAt: newModifiedAt,
              size: newSize,
              pixelQualityScore: newPixelQualityScore,
              zimageUpscaled: newZimageUpscaled,
            }
          : prev
      );
      if (selectedFolder) {
        fetchFolderTree(selectedFolder);
      }
    } catch (err) {
      setPhotopeaError((err as Error).message);
    } finally {
      setPhotopeaPersisting(false);
      photopeaPersistingRef.current = false;
    }
  }, [fetchFolderTree, photopeaEntry, selectedFolder]);

  useEffect(() => {
    const handleMessage = (event: MessageEvent) => {
      if (event.origin !== "https://www.photopea.com") return;
      const data = event.data as unknown;
      if (typeof data === "string") {
        if (data === "__hub_photopea_ready__") {
          photopeaReadyRef.current = true;
          setPhotopeaReady(true);
          trySendPhotopeaFile();
          return;
        }
        if (data === "done") {
          // Photopea sends "done" after each script finishes. If we just received an
          // exported buffer (via saveToOE), finalize by writing it to the draft path.
          return;
        }
        return;
      }
      if (data instanceof ArrayBuffer) {
        photopeaExportBufferRef.current = data;
        void savePhotopeaResult();
      }
    };
    window.addEventListener("message", handleMessage);
    return () => window.removeEventListener("message", handleMessage);
  }, [savePhotopeaResult, trySendPhotopeaFile]);

  const handleToggleTreeFolder = (pathValue: string) => {
    setSelectedTreeFolders((prev) => {
      const next = new Set(prev);
      if (next.has(pathValue)) {
        next.delete(pathValue);
      } else {
        next.add(pathValue);
      }
      return next;
    });
  };

  const toggleTreeFolder = (pathValue: string) => {
    setCollapsedTreeFolders((prev) => {
      const next = new Set(prev);
      if (next.has(pathValue)) {
        next.delete(pathValue);
      } else {
        next.add(pathValue);
      }
      return next;
    });
  };

  const handleMoveEntriesToFolder = async (sourcePaths: string[], targetPath: string) => {
    if (!targetPath || movingEntry) {
      return;
    }
    const normalizedTargetPath = String(targetPath || "")
      .trim()
      .replace(/\\/g, "/")
      .toLowerCase();
    const targetIsDeletedImages =
      normalizedTargetPath.endsWith("/deleted images") ||
      normalizedTargetPath.includes("/deleted images/");
    const uniqueSources = Array.from(
      new Set(
        sourcePaths
          .map((value) => String(value || "").trim())
          .filter((value) => Boolean(value))
      )
    );
    if (uniqueSources.length === 0) {
      return;
    }

    const validSources = uniqueSources.filter(
      (sourcePath) =>
        sourcePath !== targetPath && !targetPath.startsWith(`${sourcePath}/`)
    );
    const visibleImageSourcePaths = new Set(
      displayImageEntriesRef.current.map((entry) => entry.path)
    );
    const hiddenPendingSourcePaths = new Set(
      Object.values(pendingAiEditsByOriginal)
        .map((row) => String(row.pendingPath || "").trim())
        .filter(Boolean)
    );
    let existingSources = validSources.filter(
      (sourcePath) =>
        entryByPath.has(sourcePath) ||
        visibleImageSourcePaths.has(sourcePath) ||
        (targetIsDeletedImages && hiddenPendingSourcePaths.has(sourcePath))
    );
    if (targetIsDeletedImages) {
      const expanded = new Set(existingSources);
      existingSources.forEach((sourcePath) => {
        const pendingRecord = pendingAiEditsByOriginal[sourcePath];
        const pendingPath = String(pendingRecord?.pendingPath || "").trim();
        if (pendingPath) {
          expanded.add(pendingPath);
        }
      });
      existingSources = Array.from(expanded);
    }
    if (existingSources.length === 0) {
      setFolderDropTargetPath(null);
      setDraggingEntryPaths([]);
      return;
    }
    const optimisticDeleteSourcePaths = targetIsDeletedImages
      ? existingSources.filter(
          (sourcePath) =>
            visibleImageSourcePaths.has(sourcePath) ||
            hiddenPendingSourcePaths.has(sourcePath)
        )
      : [];

    const locked = existingSources.filter(
      (sourcePath) =>
        (!targetIsDeletedImages && Boolean(pendingAiEditsByOriginal[sourcePath])) ||
        Boolean(aiEditJobsByPath[sourcePath])
    );
    if (locked.length > 0) {
      setError(
        targetIsDeletedImages
          ? "Resolve running AI edits before deleting those files."
          : "Resolve pending/running AI edits before moving those files."
      );
      return;
    }

    if (optimisticDeleteSourcePaths.length > 0) {
      setOptimisticHiddenImagePaths((prev) => {
        const next = new Set(prev);
        optimisticDeleteSourcePaths.forEach((sourcePath) => next.add(sourcePath));
        return next;
      });
      setSelectedFiles((prev) => {
        const next = new Set(prev);
        optimisticDeleteSourcePaths.forEach((sourcePath) => next.delete(sourcePath));
        return next;
      });
      setPreviewPath((prev) =>
        prev && optimisticDeleteSourcePaths.includes(prev) ? null : prev
      );
    }

    setMovingEntry(true);
    setError(null);
    const failures: string[] = [];
    const moved = new Set<string>();
    const movedRecords: DraftDeletedImageUndoItem[] = [];
    try {
      for (const sourcePath of existingSources) {
        try {
          const response = await fetch("/api/drafts/move", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({
              sourcePath,
              targetPath,
              allowRenameOnConflict: targetIsDeletedImages,
            }),
          });
          const payload = await response.json().catch(() => ({}));
          if (!response.ok) {
            throw new Error(payload?.error || "Move failed.");
          }
          moved.add(sourcePath);
          const sourceBaseName = sourcePath.split("/").filter(Boolean).pop() || "";
          const fallbackDestinationPath = sourceBaseName
            ? `${targetPath}/${sourceBaseName}`.replace(/\/{2,}/g, "/")
            : sourcePath;
          const destinationPath =
            String(payload?.path || "").trim() || fallbackDestinationPath;
          movedRecords.push({
            sourcePath,
            destinationPath,
          });
        } catch (err) {
          failures.push(`${sourcePath}: ${(err as Error).message}`);
        }
      }

      if (moved.size > 0) {
        if (targetIsDeletedImages) {
          const undoItems = movedRecords.filter(
            (record) =>
              moved.has(record.sourcePath) &&
              Boolean(record.destinationPath) &&
              record.sourcePath !== record.destinationPath
          );
          setLastDeletedImageUndoItems(undoItems);
        } else {
          setLastDeletedImageUndoItems([]);
        }
        setEntries((prev) => prev.filter((entry) => !moved.has(entry.path)));
        setMainViewVariantImageEntries((prev) =>
          prev.filter((entry) => !moved.has(entry.path))
        );
        setSelectedFiles((prev) => {
          const next = new Set(prev);
          moved.forEach((sourcePath) => next.delete(sourcePath));
          return next;
        });
        setPendingAiEditsByOriginal((prev) => {
          const next: Record<string, PendingAiEditRecord> = {};
          Object.entries(prev).forEach(([pathValue, row]) => {
            if (moved.has(pathValue) || moved.has(row.pendingPath)) return;
            next[pathValue] = row;
          });
          return next;
        });
        setAiEditJobsByPath((prev) => {
          const next: Record<string, AiEditRuntimeJob> = {};
          Object.entries(prev).forEach(([pathValue, job]) => {
            if (moved.has(pathValue)) return;
            next[pathValue] = job;
          });
          return next;
        });
        setImageDimensions((prev) => {
          const next: Record<string, { width: number; height: number }> = {};
          Object.entries(prev).forEach(([pathValue, dims]) => {
            if (moved.has(pathValue)) return;
            next[pathValue] = dims;
          });
          return next;
        });
        setPreviewPath((prev) => (prev && moved.has(prev) ? null : prev));
      }

      if (moved.size > 0) {
        const foldersNeedingResort = new Set<string>();
        foldersNeedingResort.add(String(targetPath || "").trim());
        movedRecords.forEach((record) => {
          const sourceFolderPath = getParentFolderPathFromEntryPath(record.sourcePath);
          const destinationFolderPath = getParentFolderPathFromEntryPath(
            record.destinationPath
          );
          if (sourceFolderPath) foldersNeedingResort.add(sourceFolderPath);
          if (destinationFolderPath) foldersNeedingResort.add(destinationFolderPath);
        });
        const reorderFailures = await resortImageFoldersByHierarchy(foldersNeedingResort);
        reorderFailures.forEach((message) => failures.push(`re-sort: ${message}`));
      }

      if (selectedFolder) {
        await fetchFolderTree(selectedFolder);
      }
      if (failures.length > 0) {
        setError(
          `Moved ${moved.size}/${existingSources.length}. Errors: ${failures
            .slice(0, 2)
            .join("; ")}${failures.length > 2 ? "..." : ""}`
        );
      }
    } finally {
      if (optimisticDeleteSourcePaths.length > 0) {
        setOptimisticHiddenImagePaths((prev) => {
          const next = new Set(prev);
          optimisticDeleteSourcePaths.forEach((sourcePath) =>
            next.delete(sourcePath)
          );
          return next;
        });
      }
      setMovingEntry(false);
      setDraggingEntryPaths([]);
      setFolderDropTargetPath(null);
      setImageReorderDropPath(null);
    }
  };

  const handleCreateCopy = useCallback(
    async (entry: DraftEntry) => {
      if (entry.type !== "file") return;
      setError(null);
      try {
        const response = await fetch("/api/drafts/files/copy", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ path: entry.path }),
        });
        const payload = await response.json().catch(() => ({}));
        if (!response.ok) {
          throw new Error(payload?.error || "Unable to create copy.");
        }
        const copy = payload?.item as {
          name?: string;
          path?: string;
          zimageUpscaled?: boolean;
        } | undefined;
        const copyName = String(copy?.name || "").trim();
        const copyPath = String(copy?.path || "").trim();
        const copyZimageUpscaled =
          typeof copy?.zimageUpscaled === "boolean"
            ? copy.zimageUpscaled
            : false;
        if (copyName && copyPath) {
          const now = new Date().toISOString();
          setEntries((prev) => {
            if (prev.some((row) => row.path === copyPath)) return prev;
            const copyEntry: DraftEntry = {
              type: "file",
              name: copyName,
              path: copyPath,
              size: entry.size,
              modifiedAt: now,
              zimageUpscaled: copyZimageUpscaled,
            };
            const sourceIndex = prev.findIndex((row) => row.path === entry.path);
            if (sourceIndex < 0) return [...prev, copyEntry];
            const next = [...prev];
            next.splice(sourceIndex + 1, 0, copyEntry);
            return next;
          });
        } else if (currentPath) {
          await refreshEntries(currentPath);
        }
        if (selectedFolder) {
          fetchFolderTree(selectedFolder);
        }
      } catch (err) {
        setError((err as Error).message);
      }
    },
    [currentPath, fetchFolderTree, refreshEntries, selectedFolder]
  );

  const handleCopyImageToClipboard = useCallback(
    async (entry: DraftEntry) => {
      if (entry.type !== "file" || !isImage(entry.name)) return;
      if (!navigator?.clipboard || typeof ClipboardItem === "undefined") {
        setError("Clipboard image copy is not supported in this browser.");
        return;
      }
      try {
        setError(null);
        const response = await fetch(buildDraftDownloadUrl(entry.path, entry.modifiedAt), {
          cache: "no-store",
        });
        if (!response.ok) {
          throw new Error("Unable to download image for clipboard copy.");
        }
        const blob = await response.blob();
        let pngBlob: Blob = blob;
        if (blob.type !== "image/png") {
          const objectUrl = URL.createObjectURL(blob);
          try {
            const image = new Image();
            image.decoding = "async";
            image.src = objectUrl;
            await new Promise<void>((resolve, reject) => {
              image.onload = () => resolve();
              image.onerror = () =>
                reject(new Error("Unable to prepare image for clipboard copy."));
            });
            const canvas = document.createElement("canvas");
            canvas.width = image.naturalWidth || image.width || 1;
            canvas.height = image.naturalHeight || image.height || 1;
            const context = canvas.getContext("2d");
            if (!context) {
              throw new Error("Unable to prepare image for clipboard copy.");
            }
            context.drawImage(image, 0, 0);
            const converted = await new Promise<Blob | null>((resolve) =>
              canvas.toBlob(resolve, "image/png")
            );
            if (!converted) {
              throw new Error("Unable to prepare image for clipboard copy.");
            }
            pngBlob = converted;
          } finally {
            URL.revokeObjectURL(objectUrl);
          }
        }
        const clipboardItem = new ClipboardItem({
          "image/png": pngBlob,
        });
        await navigator.clipboard.write([clipboardItem]);
      } catch (err) {
        setError((err as Error).message || "Unable to copy image to clipboard.");
      }
    },
    [buildDraftDownloadUrl, isImage]
  );

  const handleCreateCopiesForEntries = useCallback(
    async (sourceEntries: DraftEntry[]) => {
      clearSelectedFilesForImageActions();
      if (bulkImageActionPending) return;
      const targets = sourceEntries.filter((entry) => entry.type === "file");
      if (targets.length === 0) return;
      setBulkImageActionPending(true);
      setError(null);
      const failures: string[] = [];
      const created: Array<{
        sourcePath: string;
        sourceIsVariant: boolean;
        name: string;
        path: string;
        size: number;
        pixelQualityScore: number | null;
        zimageUpscaled: boolean;
      }> = [];
      try {
        for (const entry of targets) {
          try {
            const response = await fetch("/api/drafts/files/copy", {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({ path: entry.path }),
            });
            const payload = await response.json().catch(() => ({}));
            if (!response.ok) {
              throw new Error(payload?.error || "Unable to create copy.");
            }
            const copy = payload?.item as {
              name?: string;
              path?: string;
              pixelQualityScore?: number | null;
              zimageUpscaled?: boolean;
            } | undefined;
            const copyName = String(copy?.name || "").trim();
            const copyPath = String(copy?.path || "").trim();
            const copyPixelQualityScore =
              typeof copy?.pixelQualityScore === "number" &&
              Number.isFinite(copy.pixelQualityScore)
                ? Math.round(copy.pixelQualityScore)
                : null;
            const copyZimageUpscaled =
              typeof copy?.zimageUpscaled === "boolean"
                ? copy.zimageUpscaled
                : false;
            const sourceIsVariant = extractImageTagsFromFileName(entry.name).includes("VAR");
            if (copyName && copyPath) {
              created.push({
                sourcePath: entry.path,
                sourceIsVariant,
                name: copyName,
                path: copyPath,
                size: entry.size,
                pixelQualityScore: copyPixelQualityScore,
                zimageUpscaled: copyZimageUpscaled,
              });
            }
          } catch (err) {
            failures.push(`${entry.name}: ${(err as Error).message}`);
          }
        }
        if (created.length > 0) {
          const finalizedCreated = [...created];
          for (let index = 0; index < finalizedCreated.length; index += 1) {
            const item = finalizedCreated[index];
            if (!item.sourceIsVariant) continue;
            const mainPath = getSpuRootFromDraftPath(item.sourcePath);
            if (!mainPath) continue;
            try {
              const moveResponse = await fetch("/api/drafts/move", {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  sourcePath: item.path,
                  targetPath: mainPath,
                  allowRenameOnConflict: true,
                }),
              });
              const movePayload = await moveResponse.json().catch(() => ({}));
              if (!moveResponse.ok) {
                throw new Error(movePayload?.error || "Unable to move copy to Main.");
              }

              let movedPath = String(movePayload?.path || item.path).trim();
              let movedName = String(movePayload?.name || item.name).trim();
              if (extractImageTagsFromFileName(movedName).includes("VAR")) {
                const requestedName = toggleImageTagInFileName(movedName, "VAR");
                if (requestedName && requestedName !== movedName) {
                  const renameResponse = await fetch("/api/drafts/images/rename", {
                    method: "POST",
                    headers: { "Content-Type": "application/json" },
                    body: JSON.stringify({ path: movedPath, name: requestedName }),
                  });
                  const renamePayload = await renameResponse.json().catch(() => ({}));
                  if (!renameResponse.ok) {
                    throw new Error(
                      renamePayload?.error || "Unable to remove VAR tag from copied image."
                    );
                  }
                  movedPath = String(renamePayload?.path || movedPath).trim();
                  movedName = String(renamePayload?.name || requestedName).trim();
                }
              }

              finalizedCreated[index] = {
                ...item,
                name: movedName || item.name,
                path: movedPath || item.path,
                sourceIsVariant: false,
              };
            } catch (err) {
              failures.push(`${item.name}: ${(err as Error).message}`);
            }
          }

          const now = new Date().toISOString();
          const normalizedCurrentPath = String(currentPath || "").trim();
          const createdForCurrentPath = normalizedCurrentPath
            ? finalizedCreated.filter(
                (item) =>
                  String(getParentFolderPathFromEntryPath(item.path) || "").trim() ===
                  normalizedCurrentPath
              )
            : [];
          if (createdForCurrentPath.length > 0) {
            setEntries((prev) => {
              const existing = new Set(prev.map((row) => row.path));
              const next = [...prev];
              for (const item of createdForCurrentPath) {
                if (existing.has(item.path)) continue;
                existing.add(item.path);
                const copyEntry: DraftEntry = {
                  type: "file",
                  name: item.name,
                  path: item.path,
                  size: item.size,
                  modifiedAt: now,
                  pixelQualityScore: item.pixelQualityScore,
                  zimageUpscaled: item.zimageUpscaled,
                };
                const sourceIndex = next.findIndex((row) => row.path === item.sourcePath);
                if (sourceIndex < 0) {
                  next.push(copyEntry);
                } else {
                  next.splice(sourceIndex + 1, 0, copyEntry);
                }
              }
              return next;
            });
          } else if (currentPath) {
            await refreshEntries(currentPath);
          }
        } else if (currentPath) {
          await refreshEntries(currentPath);
        }
        if (selectedFolder) {
          await fetchFolderTree(selectedFolder);
        }
      } finally {
        setBulkImageActionPending(false);
        clearSelectedFilesForImageActions();
      }
      if (failures.length > 0) {
        setError(
          `Failed to copy ${failures.length} image(s): ${failures
            .slice(0, 3)
            .join("; ")}${failures.length > 3 ? "..." : ""}`
        );
      }
    },
    [
      bulkImageActionPending,
      clearSelectedFilesForImageActions,
      currentPath,
      fetchFolderTree,
      refreshEntries,
      selectedFolder,
    ]
  );

  const handleCreateCollageForEntries = useCallback(
    async (sourceEntries: DraftEntry[]) => {
      clearSelectedFilesForImageActions();
      if (bulkImageActionPending) return;
      const targets = sourceEntries.filter(
        (entry) => entry.type === "file" && isImage(entry.name)
      );
      if (targets.length < 2) {
        setError("Select at least two images.");
        return;
      }

      const limitedTargets = targets.slice(0, COLLAGE_MAX_IMAGES);
      const fallbackTargetPath = getParentFolderPathFromEntryPath(limitedTargets[0]?.path || "");
      const resolvedTargetPath = String(currentPath || fallbackTargetPath || "").trim();
      if (!resolvedTargetPath) {
        setError("Unable to resolve target folder for collage.");
        return;
      }

      setBulkImageActionPending(true);
      setError(null);
      try {
        const response = await fetch("/api/drafts/images/collage", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            paths: limitedTargets.map((entry) => entry.path),
            targetPath: resolvedTargetPath,
          }),
        });
        const payload = await response.json().catch(() => ({}));
        if (!response.ok) {
          throw new Error(payload?.error || "Unable to create collage.");
        }

        await refreshEntries(resolvedTargetPath);
        if (selectedFolder) {
          await fetchFolderTree(selectedFolder);
        }
      } catch (err) {
        setError((err as Error).message || "Unable to create collage.");
      } finally {
        setBulkImageActionPending(false);
        clearSelectedFilesForImageActions();
      }
    },
    [
      bulkImageActionPending,
      clearSelectedFilesForImageActions,
      currentPath,
      fetchFolderTree,
      isImage,
      refreshEntries,
      selectedFolder,
    ]
  );

  const handleStackImagesForEntries = useCallback(
    async (sourceEntries: DraftEntry[]) => {
      clearSelectedFilesForImageActions();
      if (bulkImageActionPending) return;
      const targets = sourceEntries.filter(
        (entry) => entry.type === "file" && isImage(entry.name)
      );
      if (targets.length < 2) {
        setError("Select at least two images.");
        return;
      }

      const limitedTargets = targets.slice(0, STACK_IMAGES_MAX_IMAGES);
      const fallbackTargetPath = getParentFolderPathFromEntryPath(limitedTargets[0]?.path || "");
      const resolvedTargetPath = String(currentPath || fallbackTargetPath || "").trim();
      if (!resolvedTargetPath) {
        setError("Unable to resolve target folder for Stack Images.");
        return;
      }

      setBulkImageActionPending(true);
      setError(null);
      try {
        const response = await fetch("/api/drafts/images/stack", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            paths: limitedTargets.map((entry) => entry.path),
            targetPath: resolvedTargetPath,
          }),
        });
        const payload = await response.json().catch(() => ({}));
        if (!response.ok) {
          throw new Error(payload?.error || "Unable to create image stack.");
        }

        await refreshEntries(resolvedTargetPath);
        if (selectedFolder) {
          await fetchFolderTree(selectedFolder);
        }
      } catch (err) {
        setError((err as Error).message || "Unable to create image stack.");
      } finally {
        setBulkImageActionPending(false);
        clearSelectedFilesForImageActions();
      }
    },
    [
      bulkImageActionPending,
      clearSelectedFilesForImageActions,
      currentPath,
      fetchFolderTree,
      isImage,
      refreshEntries,
      selectedFolder,
    ]
  );

  const handleFlipHorizontalForEntries = useCallback(
    async (sourceEntries: DraftEntry[]) => {
      clearSelectedFilesForImageActions();
      if (bulkImageActionPending) return;
      const targets = sourceEntries.filter(
        (entry) => entry.type === "file" && isImage(entry.name)
      );
      if (targets.length === 0) {
        setError("Select at least one image.");
        return;
      }

      const limitedTargets = targets.slice(0, FLIP_HORIZONTAL_MAX_IMAGES);
      const targetPaths = limitedTargets.map((entry) => entry.path);

      setBulkImageActionPending(true);
      setError(null);
      setReloadingImagePaths((prev) => {
        const next = new Set(prev);
        targetPaths.forEach((pathValue) => next.add(pathValue));
        return next;
      });
      try {
        const response = await fetch("/api/drafts/images/flip-horizontal", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            paths: targetPaths,
          }),
        });
        const payload = await response.json().catch(() => ({}));
        if (!response.ok) {
          throw new Error(payload?.error || "Unable to flip image(s).");
        }

        const updatedRows = Array.isArray(payload?.updated)
          ? (payload.updated as Array<Record<string, unknown>>)
          : [];
        const errors = Array.isArray(payload?.errors)
          ? (payload.errors as Array<Record<string, unknown>>)
          : [];

        const updates = new Map<
          string,
          {
            modifiedAt: string;
            size: number;
            pixelQualityScore: number | null;
          }
        >();
        updatedRows.forEach((row) => {
          const rowPath = String(row.path || "").trim();
          if (!rowPath) return;
          const modifiedAt =
            String(row.modifiedAt || "").trim() || new Date().toISOString();
          const size =
            typeof row.size === "number" && Number.isFinite(row.size) ? row.size : 0;
          const pixelQualityScore =
            typeof row.pixelQualityScore === "number" &&
            Number.isFinite(row.pixelQualityScore)
              ? Math.round(row.pixelQualityScore)
              : null;
          updates.set(rowPath, { modifiedAt, size, pixelQualityScore });
        });

        if (updates.size > 0) {
          setEntries((prev) =>
            prev.map((item) => {
              const update = updates.get(item.path);
              if (!update) return item;
              return {
                ...item,
                modifiedAt: update.modifiedAt,
                size: update.size || item.size,
                pixelQualityScore:
                  update.pixelQualityScore !== null
                    ? update.pixelQualityScore
                    : item.pixelQualityScore ?? null,
              };
            })
          );
          setMainViewVariantImageEntries((prev) =>
            prev.map((item) => {
              const update = updates.get(item.path);
              if (!update) return item;
              return {
                ...item,
                modifiedAt: update.modifiedAt,
                size: update.size || item.size,
                pixelQualityScore:
                  update.pixelQualityScore !== null
                    ? update.pixelQualityScore
                    : item.pixelQualityScore ?? null,
              };
            })
          );
          setImageDimensions((prev) => {
            const next: Record<string, { width: number; height: number }> = {
              ...prev,
            };
            targetPaths.forEach((pathValue) => {
              delete next[pathValue];
            });
            return next;
          });
        }

        if (selectedFolder) {
          await fetchFolderTree(selectedFolder);
        }

        if (errors.length > 0) {
          const preview = errors
            .slice(0, 2)
            .map((row) => String(row.error || "Unknown error"))
            .join("; ");
          setError(
            `Flip Horizontal failed for ${errors.length} image(s). ${preview}${
              errors.length > 2 ? "..." : ""
            }`
          );
        }
      } catch (err) {
        setError((err as Error).message || "Unable to flip image(s).");
      } finally {
        setBulkImageActionPending(false);
        clearSelectedFilesForImageActions();
        setReloadingImagePaths((prev) => {
          const next = new Set(prev);
          targetPaths.forEach((pathValue) => next.delete(pathValue));
          return next;
        });
      }
    },
    [
      bulkImageActionPending,
      clearSelectedFilesForImageActions,
      fetchFolderTree,
      isImage,
      selectedFolder,
    ]
  );

  const handleMergeDigidealMainForEntries = useCallback(
    async (sourceEntries: DraftEntry[]) => {
      clearSelectedFilesForImageActions();
      if (bulkImageActionPending) return;
      const targets = sourceEntries.filter(
        (entry) => entry.type === "file" && isImage(entry.name)
      );
      if (targets.length !== 2) {
        setError("Select exactly two images.");
        return;
      }

      const fallbackTargetPath = getParentFolderPathFromEntryPath(targets[0]?.path || "");
      const resolvedTargetPath = String(currentPath || fallbackTargetPath || "").trim();
      if (!resolvedTargetPath) {
        setError("Unable to resolve target folder for Merge DigiDeal Main.");
        return;
      }

      setBulkImageActionPending(true);
      setError(null);
      try {
        const response = await fetch("/api/drafts/images/merge-digideal-main", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            paths: targets.map((entry) => entry.path),
            targetPath: resolvedTargetPath,
            fadePercent: 35,
          }),
        });
        const payload = await response.json().catch(() => ({}));
        if (!response.ok) {
          throw new Error(payload?.error || "Unable to merge images.");
        }

        await refreshEntries(resolvedTargetPath);
        if (selectedFolder) {
          await fetchFolderTree(selectedFolder);
        }
      } catch (err) {
        setError((err as Error).message || "Unable to merge images.");
      } finally {
        setBulkImageActionPending(false);
        clearSelectedFilesForImageActions();
      }
    },
    [
      bulkImageActionPending,
      clearSelectedFilesForImageActions,
      currentPath,
      fetchFolderTree,
      isImage,
      refreshEntries,
      selectedFolder,
    ]
  );

  const handleMergeDigidealMainOverlayForEntries = useCallback(
    async (sourceEntries: DraftEntry[]) => {
      clearSelectedFilesForImageActions();
      if (bulkImageActionPending) return;
      const targets = sourceEntries.filter(
        (entry) => entry.type === "file" && isImage(entry.name)
      );
      if (targets.length !== 2) {
        setError("Select exactly two images.");
        return;
      }

      const fallbackTargetPath = getParentFolderPathFromEntryPath(targets[0]?.path || "");
      const resolvedTargetPath = String(currentPath || fallbackTargetPath || "").trim();
      if (!resolvedTargetPath) {
        setError("Unable to resolve target folder for DigiDeal Main Overlay.");
        return;
      }

      setBulkImageActionPending(true);
      setError(null);
      try {
        const response = await fetch("/api/drafts/images/merge-digideal-main-overlay-v2", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            paths: targets.map((entry) => entry.path),
            targetPath: resolvedTargetPath,
          }),
        });
        const payload = await response.json().catch(() => ({}));
        if (!response.ok) {
          throw new Error(payload?.error || "Unable to create DigiDeal Main Overlay.");
        }

        await refreshEntries(resolvedTargetPath);
        if (selectedFolder) {
          await fetchFolderTree(selectedFolder);
        }
      } catch (err) {
        setError((err as Error).message || "Unable to create DigiDeal Main Overlay.");
      } finally {
        setBulkImageActionPending(false);
        clearSelectedFilesForImageActions();
      }
    },
    [
      bulkImageActionPending,
      clearSelectedFilesForImageActions,
      currentPath,
      fetchFolderTree,
      isImage,
      refreshEntries,
      selectedFolder,
    ]
  );

  const handleMergeDigidealMainStackOverlayForEntries = useCallback(
    (sourceEntries: DraftEntry[]) => {
      clearSelectedFilesForImageActions();
      if (bulkImageActionPending) return;
      openStackOverlayPickerForEntries(sourceEntries, "digideal_main_stack_overlay");
    },
    [bulkImageActionPending, clearSelectedFilesForImageActions, openStackOverlayPickerForEntries]
  );

  const handleStackOverlayForEntries = useCallback(
    (sourceEntries: DraftEntry[]) => {
      clearSelectedFilesForImageActions();
      if (bulkImageActionPending) return;
      openStackOverlayPickerForEntries(sourceEntries, "stack_overlay");
    },
    [bulkImageActionPending, clearSelectedFilesForImageActions, openStackOverlayPickerForEntries]
  );

  const handleUndoLastChangeForEntries = useCallback(
    async (sourceEntries: DraftEntry[]) => {
      clearSelectedFilesForImageActions();
      if (bulkImageActionPending) return;
      const targets = sourceEntries.filter(
        (entry) => entry.type === "file" && isImage(entry.name)
      );
      if (targets.length === 0) return;
      const locked = targets.filter(
        (entry) =>
          Boolean(pendingAiEditsByOriginal[entry.path]) ||
          Boolean(aiEditJobsByPath[entry.path])
      );
      if (locked.length > 0) {
        setError("Resolve pending/running AI edits before undo.");
        return;
      }

      setBulkImageActionPending(true);
      setError(null);
      const failures: string[] = [];
      const updates: Record<
        string,
        {
          path: string;
          name: string;
          size: number;
          modifiedAt: string;
          pixelQualityScore: number | null;
          zimageUpscaled: boolean;
        }
      > = {};

      try {
        for (const entry of targets) {
          try {
            const response = await fetch("/api/drafts/images/undo", {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({ path: entry.path }),
            });
            const payload = await response.json().catch(() => ({}));
            if (!response.ok) {
              throw new Error(payload?.error || "Undo failed.");
            }
            const nextPath = String(payload?.path || entry.path).trim() || entry.path;
            const nextName = String(payload?.name || entry.name).trim() || entry.name;
            const nextSize =
              typeof payload?.size === "number" && Number.isFinite(payload.size)
                ? payload.size
                : entry.size;
            const nextModifiedAt =
              String(payload?.modifiedAt || "").trim() || new Date().toISOString();
            const nextPixelQualityScore =
              typeof payload?.pixelQualityScore === "number" &&
              Number.isFinite(payload.pixelQualityScore)
                ? Math.round(payload.pixelQualityScore)
                : null;
            const nextZimageUpscaled =
              typeof payload?.zimageUpscaled === "boolean"
                ? payload.zimageUpscaled
                : false;
            updates[entry.path] = {
              path: nextPath,
              name: nextName,
              size: nextSize,
              modifiedAt: nextModifiedAt,
              pixelQualityScore: nextPixelQualityScore,
              zimageUpscaled: nextZimageUpscaled,
            };
          } catch (err) {
            failures.push(`${entry.name}: ${(err as Error).message}`);
          }
        }

        if (Object.keys(updates).length > 0) {
          setEntries((prev) =>
            prev.map((item) => {
              const update = updates[item.path];
              if (!update) return item;
              return {
                ...item,
                path: update.path,
                name: update.name,
                size: update.size,
                modifiedAt: update.modifiedAt,
                pixelQualityScore: update.pixelQualityScore,
                zimageUpscaled: update.zimageUpscaled,
              };
            })
          );
          setMainViewVariantImageEntries((prev) =>
            prev.map((item) => {
              const update = updates[item.path];
              if (!update) return item;
              return {
                ...item,
                path: update.path,
                name: update.name,
                size: update.size,
                modifiedAt: update.modifiedAt,
                pixelQualityScore: update.pixelQualityScore,
                zimageUpscaled: update.zimageUpscaled,
              };
            })
          );
          setSelectedFiles((prev) => {
            if (prev.size === 0) return prev;
            const next = new Set(prev);
            Object.entries(updates).forEach(([oldPath, update]) => {
              if (!next.has(oldPath)) return;
              next.delete(oldPath);
              next.add(update.path);
            });
            return next;
          });
          setPreviewPath((prev) => {
            if (!prev) return prev;
            const update = updates[prev];
            return update ? update.path : prev;
          });
          setImageDimensions((prev) => {
            const next: Record<string, { width: number; height: number }> = {
              ...prev,
            };
            Object.entries(updates).forEach(([oldPath, update]) => {
              delete next[oldPath];
              delete next[update.path];
            });
            return next;
          });
          const activePath = String(currentPathRef.current || "").trim();
          if (activePath) {
            await refreshEntries(activePath);
            await fetchPendingAiEdits(activePath);
          }
        }

        if (selectedFolder) {
          await fetchFolderTree(selectedFolder);
        }
      } finally {
        setBulkImageActionPending(false);
        clearSelectedFilesForImageActions();
      }

      if (failures.length > 0) {
        setError(
          `Failed to undo ${failures.length} image(s): ${failures
            .slice(0, 3)
            .join("; ")}${failures.length > 3 ? "..." : ""}`
        );
      }
    },
    [
      aiEditJobsByPath,
      bulkImageActionPending,
      clearSelectedFilesForImageActions,
      fetchFolderTree,
      fetchPendingAiEdits,
      isImage,
      pendingAiEditsByOriginal,
      refreshEntries,
      selectedFolder,
    ]
  );

  const handleUndoLastDeletedImageMove = useCallback(
    async (explicitUndoItems?: DraftDeletedImageUndoItem[]) => {
    clearSelectedFilesForImageActions();
    if (bulkImageActionPending || movingEntry) return;
    const sourceUndoItems =
      Array.isArray(explicitUndoItems) && explicitUndoItems.length > 0
        ? explicitUndoItems
        : lastDeletedImageUndoItems;
    const undoItems = sourceUndoItems
      .map((item) => ({
        sourcePath: String(item.sourcePath || "").trim(),
        destinationPath: String(item.destinationPath || "").trim(),
      }))
      .filter(
        (item) =>
          Boolean(item.sourcePath) &&
          Boolean(item.destinationPath) &&
          item.sourcePath !== item.destinationPath
      );
    if (undoItems.length === 0) return;

    setBulkImageActionPending(true);
    setError(null);
    const failures: string[] = [];
    const restoredKeys = new Set<string>();

    try {
      for (const item of undoItems) {
        const sourcePath = item.destinationPath;
        const sourceParts = item.sourcePath.split("/");
        sourceParts.pop();
        const targetPath = sourceParts.join("/");
        if (!sourcePath || !targetPath) {
          failures.push(`${item.destinationPath}: invalid undo path.`);
          continue;
        }

        try {
          const response = await fetch("/api/drafts/move", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ sourcePath, targetPath }),
          });
          const payload = await response.json().catch(() => ({}));
          if (!response.ok) {
            throw new Error(payload?.error || "Undo delete failed.");
          }
          restoredKeys.add(`${item.sourcePath}=>${item.destinationPath}`);
        } catch (err) {
          failures.push(`${sourcePath}: ${(err as Error).message}`);
        }
      }

      if (restoredKeys.size > 0) {
        setLastDeletedImageUndoItems((prev) =>
          prev.filter(
            (item) =>
              !restoredKeys.has(
                `${String(item.sourcePath || "").trim()}=>${String(
                  item.destinationPath || ""
                ).trim()}`
              )
          )
        );
        if (currentPath) {
          await refreshEntries(currentPath);
          await fetchPendingAiEdits(currentPath);
        }
        if (selectedFolder) {
          await fetchFolderTree(selectedFolder);
        }
      }
    } finally {
      setBulkImageActionPending(false);
      clearSelectedFilesForImageActions();
    }

    if (failures.length > 0) {
      setError(
        `Failed to undo ${failures.length} deleted image(s): ${failures
          .slice(0, 3)
          .join("; ")}${failures.length > 3 ? "..." : ""}`
      );
    }
    },
    [
      bulkImageActionPending,
      clearSelectedFilesForImageActions,
      currentPath,
      fetchFolderTree,
      fetchPendingAiEdits,
      lastDeletedImageUndoItems,
      movingEntry,
      refreshEntries,
      selectedFolder,
    ]
  );

  const aiMaskTargetPath =
    aiEditMode === "eraser" && aiEditTargets.length === 1 ? aiEditTargets[0].path : "";

  const syncAiMaskCanvasToImage = useCallback((resetMask: boolean) => {
    const imageEl = aiMaskImageRef.current;
    const canvasEl = aiMaskCanvasRef.current;
    if (!imageEl || !canvasEl) return;
    const naturalWidth = Number(imageEl.naturalWidth || 0);
    const naturalHeight = Number(imageEl.naturalHeight || 0);
    if (!naturalWidth || !naturalHeight) return;
    const displayWidth = Number(imageEl.clientWidth || naturalWidth);
    const displayHeight = Number(imageEl.clientHeight || naturalHeight);
    canvasEl.style.width = `${displayWidth}px`;
    canvasEl.style.height = `${displayHeight}px`;
    if (resetMask || canvasEl.width !== naturalWidth || canvasEl.height !== naturalHeight) {
      canvasEl.width = naturalWidth;
      canvasEl.height = naturalHeight;
      const ctx = canvasEl.getContext("2d");
      if (ctx) {
        ctx.clearRect(0, 0, naturalWidth, naturalHeight);
      }
      setAiMaskHasPaint(false);
    }
  }, []);

  const getAiMaskPointer = useCallback(
    (event: ReactPointerEvent<HTMLCanvasElement>) => {
      const canvasEl = aiMaskCanvasRef.current;
      if (!canvasEl) return null;
      const rect = canvasEl.getBoundingClientRect();
      if (!rect.width || !rect.height) return null;
      const canvasWidth = canvasEl.width || rect.width;
      const canvasHeight = canvasEl.height || rect.height;
      const displayX = Math.max(0, Math.min(rect.width, event.clientX - rect.left));
      const displayY = Math.max(0, Math.min(rect.height, event.clientY - rect.top));
      const imageX = (displayX / rect.width) * canvasWidth;
      const imageY = (displayY / rect.height) * canvasHeight;
      const brushWidth = Math.max(2, Math.round(aiMaskBrushSize));
      const displayScale = rect.width / canvasWidth;
      return {
        imagePoint: {
          x: Math.max(0, Math.min(canvasWidth, imageX)),
          y: Math.max(0, Math.min(canvasHeight, imageY)),
        },
        displayPoint: { x: displayX, y: displayY },
        displayBrushSize: Math.max(6, Math.round(brushWidth * displayScale)),
      };
    },
    [aiMaskBrushSize]
  );

  const setAiMaskCursorFromPointer = useCallback(
    (event: ReactPointerEvent<HTMLCanvasElement>, visible: boolean) => {
      const pointer = getAiMaskPointer(event);
      if (!pointer) {
        setAiMaskCursor((prev) => (prev.visible ? { ...prev, visible: false } : prev));
        return null;
      }
      setAiMaskCursor({
        x: pointer.displayPoint.x,
        y: pointer.displayPoint.y,
        size: pointer.displayBrushSize,
        visible,
      });
      return pointer.imagePoint;
    },
    [getAiMaskPointer]
  );

  const drawAiMaskStroke = useCallback(
    (from: { x: number; y: number }, to: { x: number; y: number }) => {
      const canvasEl = aiMaskCanvasRef.current;
      if (!canvasEl) return;
      const ctx = canvasEl.getContext("2d");
      if (!ctx) return;
      const brushWidth = Math.max(2, Math.round(aiMaskBrushSize));
      const brushRadius = Math.max(1, brushWidth / 2);
      ctx.lineCap = "round";
      ctx.lineJoin = "round";
      // 50% transparency so masked area remains visible while painting.
      ctx.strokeStyle = "rgba(36, 116, 255, 0.5)";
      ctx.fillStyle = "rgba(36, 116, 255, 0.5)";
      ctx.lineWidth = brushWidth;
      ctx.beginPath();
      ctx.moveTo(from.x, from.y);
      ctx.lineTo(to.x, to.y);
      ctx.stroke();
      ctx.beginPath();
      ctx.arc(to.x, to.y, brushRadius, 0, Math.PI * 2);
      ctx.fill();
    },
    [aiMaskBrushSize]
  );

  const stopAiMaskDrawing = useCallback((pointerId?: number) => {
    aiMaskDrawingRef.current = false;
    aiMaskLastPointRef.current = null;
    const canvasEl = aiMaskCanvasRef.current;
    const activePointerId =
      typeof pointerId === "number" ? pointerId : aiMaskPointerIdRef.current;
    if (canvasEl && typeof activePointerId === "number") {
      try {
        if (canvasEl.hasPointerCapture(activePointerId)) {
          canvasEl.releasePointerCapture(activePointerId);
        }
      } catch {}
    }
    aiMaskPointerIdRef.current = null;
  }, []);

  const handleAiMaskPointerDown = useCallback(
    (event: ReactPointerEvent<HTMLCanvasElement>) => {
      if (event.button !== 0 && event.pointerType !== "touch") return;
      const point = setAiMaskCursorFromPointer(event, true);
      if (!point) return;
      aiMaskDrawingRef.current = true;
      aiMaskPointerIdRef.current = event.pointerId;
      aiMaskLastPointRef.current = point;
      try {
        event.currentTarget.setPointerCapture(event.pointerId);
      } catch {}
      drawAiMaskStroke(point, point);
      setAiMaskHasPaint(true);
      event.preventDefault();
    },
    [drawAiMaskStroke, setAiMaskCursorFromPointer]
  );

  const handleAiMaskPointerMove = useCallback(
    (event: ReactPointerEvent<HTMLCanvasElement>) => {
      const point = setAiMaskCursorFromPointer(event, true);
      if (!point) return;
      if (!aiMaskDrawingRef.current) return;
      if (
        typeof aiMaskPointerIdRef.current === "number" &&
        aiMaskPointerIdRef.current !== event.pointerId
      ) {
        return;
      }
      const lastPoint = aiMaskLastPointRef.current ?? point;
      drawAiMaskStroke(lastPoint, point);
      aiMaskLastPointRef.current = point;
      setAiMaskHasPaint(true);
      event.preventDefault();
    },
    [drawAiMaskStroke, setAiMaskCursorFromPointer]
  );

  const clearAiMaskCanvas = useCallback(() => {
    const canvasEl = aiMaskCanvasRef.current;
    if (!canvasEl) {
      setAiMaskHasPaint(false);
      return;
    }
    const ctx = canvasEl.getContext("2d");
    if (!ctx) return;
    ctx.clearRect(0, 0, canvasEl.width, canvasEl.height);
    setAiMaskHasPaint(false);
  }, []);

  const buildAiMaskDataUrl = useCallback(() => {
    const canvasEl = aiMaskCanvasRef.current;
    if (!canvasEl || !aiMaskHasPaint || !canvasEl.width || !canvasEl.height) {
      return "";
    }
    const srcCtx = canvasEl.getContext("2d");
    if (!srcCtx) return "";
    const srcImage = srcCtx.getImageData(0, 0, canvasEl.width, canvasEl.height);
    const maskCanvas = document.createElement("canvas");
    maskCanvas.width = canvasEl.width;
    maskCanvas.height = canvasEl.height;
    const maskCtx = maskCanvas.getContext("2d");
    if (!maskCtx) return "";
    const maskImage = maskCtx.createImageData(maskCanvas.width, maskCanvas.height);
    for (let index = 0; index < srcImage.data.length; index += 4) {
      const painted = srcImage.data[index + 3] > 8;
      const color = painted ? 255 : 0;
      maskImage.data[index] = color;
      maskImage.data[index + 1] = color;
      maskImage.data[index + 2] = color;
      maskImage.data[index + 3] = 255;
    }
    maskCtx.putImageData(maskImage, 0, 0);
    return maskCanvas.toDataURL("image/png");
  }, [aiMaskHasPaint]);

  useEffect(() => {
    aiMaskDrawingRef.current = false;
    aiMaskPointerIdRef.current = null;
    aiMaskLastPointRef.current = null;
    setAiMaskCursor((prev) => (prev.visible ? { ...prev, visible: false } : prev));
    clearAiMaskCanvas();
    if (!aiMaskTargetPath) return;
    const handleResize = () => syncAiMaskCanvasToImage(false);
    window.addEventListener("resize", handleResize);
    return () => {
      window.removeEventListener("resize", handleResize);
    };
  }, [aiMaskTargetPath, clearAiMaskCanvas, syncAiMaskCanvasToImage]);

  const runAiEditsForEntries = useCallback(
    async (
      sourceEntries: DraftEntry[],
      provider: AiEditProvider,
      mode: AiPromptMode,
      promptText: string,
      options?: { templatePreset?: AiTemplatePreset; outputCount?: number; maskDataUrl?: string }
    ) => {
      clearSelectedFilesForImageActions();
      const pathAtStart = String(currentPathRef.current || "");
      const deduped = sourceEntries.filter(
        (entry, index, arr) =>
          entry.type === "file" &&
          isImage(entry.name) &&
          arr.findIndex((candidate) => candidate.path === entry.path) === index
      );
      if (deduped.length === 0) return;
      if (deduped.length > 1) {
        const providerLabel =
          provider === "chatgpt" ? "ChatGPT" : provider === "gemini" ? "Gemini" : "Z-Image";
        const modeLabel =
          options?.templatePreset === "digideal_main"
            ? "DigiDeal Main Single"
            : options?.templatePreset === "digideal_main_dual"
              ? "DigiDeal Main Dual"
            : options?.templatePreset === "product_scene"
              ? "Product Scene"
            : options?.templatePreset === "product_collection"
              ? "Product Collection"
            : mode === "template"
                ? "Standard Template"
                : mode === "direct"
                  ? "Direct"
                  : mode === "white_background"
                    ? "White BG"
                    : mode === "auto_center_white"
                      ? "Auto Center Wide"
                      : mode === "eraser"
                        ? "Eraser"
                        : "Upscale";
        const outputCount = Number(options?.outputCount ?? 1);
        const outputSuffix =
          Number.isFinite(outputCount) && outputCount > 1
            ? ` (${outputCount} outputs per image)`
            : "";
        const confirmed = window.confirm(
          `Run ${providerLabel} ${modeLabel} on ${deduped.length} selected images${outputSuffix}?`
        );
        if (!confirmed) return;
      }

      const skippedPendingOrRunning: string[] = [];
      const skippedAlreadyUpscaled: string[] = [];
      const runnable = deduped.filter((entry) => {
        if (pendingAiEditsByOriginal[entry.path] || aiEditJobsByPath[entry.path]) {
          skippedPendingOrRunning.push(entry.name);
          return false;
        }
        if (provider === "zimage" && mode === "upscale" && isImageEntryUpscaled(entry)) {
          skippedAlreadyUpscaled.push(entry.name);
          return false;
        }
        return true;
      });

      if (runnable.length === 0) {
        const reasons: string[] = [];
        if (skippedPendingOrRunning.length > 0) {
          reasons.push("all selected images already have pending/running AI edits");
        }
        if (skippedAlreadyUpscaled.length > 0) {
          reasons.push("all selected images already have Z-image upscale applied");
        }
        setError(
          reasons.length > 0
            ? `Cannot run action: ${reasons.join(" and ")}.`
            : "No images can be processed."
        );
        return;
      }

      setAiEditJobsByPath((prev) => {
        const next = { ...prev };
        for (const entry of runnable) {
          next[entry.path] = {
            provider,
            mode,
            status: "queued",
            startedAt: Date.now(),
          };
        }
        return next;
      });

      const failures: string[] = [];
      const spuPathsNeedingReview = new Set<string>();
      const foldersNeedingResort = new Set<string>();
      let nextIndex = 0;
      const maxWorkers = Math.min(3, runnable.length);
      const worker = async () => {
        while (true) {
          const index = nextIndex;
          if (index >= runnable.length) return;
          nextIndex += 1;
          const entry = runnable[index];
          setAiEditJobsByPath((prev) => ({
            ...prev,
            [entry.path]: {
              provider,
              mode,
              status: "running",
              startedAt: Date.now(),
            },
          }));
	          try {
		            const response = await fetch("/api/drafts/ai-edits", {
		              method: "POST",
		              headers: { "Content-Type": "application/json" },
		              body: JSON.stringify({
		                path: entry.path,
		                provider,
		                mode,
		                prompt: promptText,
                    maskDataUrl:
                      mode === "eraser" && runnable.length === 1
                        ? options?.maskDataUrl
                        : undefined,
		                templatePreset: options?.templatePreset,
                  outputCount: options?.outputCount,
		              }),
		            });
            const payload = await response.json().catch(() => ({}));
            if (!response.ok) {
              throw new Error(payload?.error || "AI image edit failed.");
            }
            const payloadCreatedPaths = Array.isArray(payload?.createdPaths)
              ? payload.createdPaths
                  .map((value: unknown) => String(value || "").trim())
                  .filter((value: string) => Boolean(value))
              : [];
            payloadCreatedPaths.forEach((createdPath: string) => {
              const createdFolderPath = getParentFolderPathFromEntryPath(createdPath);
              if (createdFolderPath) {
                foldersNeedingResort.add(createdFolderPath);
              }
              const createdSpuPath = getSpuRootFromDraftPath(createdPath);
              if (createdSpuPath) {
                spuPathsNeedingReview.add(createdSpuPath);
              }
            });
            const payloadOriginalPath =
              typeof payload?.originalPath === "string"
                ? String(payload.originalPath).trim()
                : "";
            if (payloadOriginalPath) {
              const targetSpuPath = getSpuRootFromDraftPath(payloadOriginalPath);
              if (targetSpuPath) {
                spuPathsNeedingReview.add(targetSpuPath);
              }
            } else {
              const fallbackSpuPath = getSpuRootFromDraftPath(entry.path);
              if (fallbackSpuPath) {
                spuPathsNeedingReview.add(fallbackSpuPath);
              }
            }
            if (payload?.applied === true) {
              const originalPath = payloadOriginalPath || entry.path;
              const originalFolderPath =
                getParentFolderPathFromEntryPath(originalPath);
              if (originalFolderPath) {
                foldersNeedingResort.add(originalFolderPath);
              }
              const now = new Date().toISOString();
              const nextPixelQualityScore =
                typeof payload?.pixelQualityScore === "number" &&
                Number.isFinite(payload.pixelQualityScore)
                  ? Math.round(payload.pixelQualityScore)
                  : null;
              setReloadingImagePaths((prev) => {
                const next = new Set(prev);
                next.add(originalPath);
                return next;
              });
              setEntries((prev) =>
                prev.map((item) =>
                      item.path === originalPath
                    ? {
                        ...item,
                        modifiedAt: now,
                        pixelQualityScore:
                          nextPixelQualityScore !== null
                            ? nextPixelQualityScore
                            : item.pixelQualityScore ?? null,
                        zimageUpscaled:
                          provider === "zimage" && mode === "upscale"
                            ? true
                            : item.zimageUpscaled,
                      }
                    : item
                )
              );
              setMainViewVariantImageEntries((prev) =>
                prev.map((item) =>
                  item.path === originalPath
                    ? {
                        ...item,
                        modifiedAt: now,
                        pixelQualityScore:
                          nextPixelQualityScore !== null
                            ? nextPixelQualityScore
                            : item.pixelQualityScore ?? null,
                        zimageUpscaled:
                          provider === "zimage" && mode === "upscale"
                            ? true
                            : item.zimageUpscaled,
                      }
                    : item
                )
              );
              // Safety timeout to avoid stuck spinners if the browser never fires onLoad.
              setTimeout(() => {
                setReloadingImagePaths((prev) => {
                  if (!prev.has(originalPath)) return prev;
                  const next = new Set(prev);
                  next.delete(originalPath);
                  return next;
                });
              }, 45000);
            }
            const item = payload?.item as PendingAiEditRecord | undefined;
            if (item?.originalPath) {
              const itemSpuPath = getSpuRootFromDraftPath(item.originalPath);
              if (itemSpuPath) {
                spuPathsNeedingReview.add(itemSpuPath);
              }
              setPendingAiEditsByOriginal((prev) => ({
                ...prev,
                [item.originalPath]: item,
              }));
            }
          } catch (err) {
            failures.push(`${entry.name}: ${(err as Error).message}`);
          } finally {
            setAiEditJobsByPath((prev) => {
              const next = { ...prev };
              delete next[entry.path];
              return next;
            });
          }
        }
      };
      try {
        await Promise.all(Array.from({ length: maxWorkers }, () => worker()));
        if (spuPathsNeedingReview.size > 0) {
          markSpuNeedsAiReview(Array.from(spuPathsNeedingReview));
        }

        let reorderFailures: string[] = [];
        if (foldersNeedingResort.size > 0) {
          reorderFailures = await resortImageFoldersByHierarchy(foldersNeedingResort);
        }

        const latestPath = String(currentPathRef.current || "");
        if (pathAtStart && latestPath === pathAtStart) {
          await refreshEntries(pathAtStart);
          await fetchPendingAiEdits(pathAtStart);
        }

        const messages: string[] = [];
        if (skippedPendingOrRunning.length > 0) {
          messages.push(
            `Skipped ${skippedPendingOrRunning.length} image(s) with existing pending/running edits.`
          );
        }
        if (skippedAlreadyUpscaled.length > 0) {
          messages.push(
            `Skipped ${skippedAlreadyUpscaled.length} image(s) already upscaled with Z-image.`
          );
        }
        if (failures.length > 0) {
          messages.push(
            `Failed ${failures.length} image(s): ${failures
              .slice(0, 3)
              .join("; ")}${failures.length > 3 ? "..." : ""}`
          );
        }
        if (reorderFailures.length > 0) {
          messages.push(
            `Re-sort failed in ${reorderFailures.length} folder(s): ${reorderFailures
              .slice(0, 2)
              .join("; ")}${reorderFailures.length > 2 ? "..." : ""}`
          );
        }
        if (messages.length > 0) {
          setError(messages.join(" "));
        }
      } finally {
        clearSelectedFilesForImageActions();
      }
    },
    [
      aiEditJobsByPath,
      clearSelectedFilesForImageActions,
      fetchPendingAiEdits,
      markSpuNeedsAiReview,
      refreshEntries,
      resortImageFoldersByHierarchy,
      isImage,
      isImageEntryUpscaled,
      pendingAiEditsByOriginal,
    ]
  );

  const startAiEditForEntries = useCallback(
    (
      sourceEntries: DraftEntry[],
      provider: AiEditProvider,
      mode: AiPromptMode,
      options?: { outputCount?: number }
    ) => {
      clearSelectedFilesForImageActions();
      const targets = sourceEntries.filter(
        (entry, index, arr) =>
          entry.type === "file" &&
          isImage(entry.name) &&
          arr.findIndex((candidate) => candidate.path === entry.path) === index
      );
      if (targets.length === 0) return;
      const outputCount = Number(options?.outputCount ?? 1);
      const normalizedOutputCount = Number.isFinite(outputCount)
        ? Math.max(1, Math.min(3, Math.floor(outputCount)))
        : 1;
      if (
        provider === "zimage" &&
        (mode === "upscale" || mode === "white_background" || mode === "auto_center_white")
      ) {
        setAiEditTargets([]);
        setAiEditProvider(provider);
        setAiEditMode(mode);
        setAiEditTemplatePreset("standard");
        setAiEditOutputCount(1);
        setAiEditPrompt("");
        setAiEraserRunMode("prompt");
        setAiEditError(null);
        setAiMaskHasPaint(false);
        setError(null);
        void runAiEditsForEntries(targets, provider, mode, "");
        return;
      }
      setAiEditTargets(targets);
      setAiEditProvider(provider);
      setAiEditMode(mode);
      setAiEditTemplatePreset("standard");
      setAiEditOutputCount(normalizedOutputCount);
      setAiEditPrompt("");
      setAiEraserRunMode("prompt");
      setAiEditError(null);
      setAiMaskHasPaint(false);
    },
    [clearSelectedFilesForImageActions, isImage, runAiEditsForEntries]
  );

  const startAiTemplatePresetForEntries = useCallback(
    (
      sourceEntries: DraftEntry[],
      provider: Exclude<AiEditProvider, "zimage">,
      templatePreset: Exclude<AiTemplatePreset, "standard">,
      outputCount: number
    ) => {
      clearSelectedFilesForImageActions();
      const targets = sourceEntries.filter(
        (entry, index, arr) =>
          entry.type === "file" &&
          isImage(entry.name) &&
          arr.findIndex((candidate) => candidate.path === entry.path) === index
      );
      if (targets.length === 0) return;
      setAiEditTargets(targets);
      setAiEditProvider(provider);
      setAiEditMode("template");
      setAiEditTemplatePreset(templatePreset);
      setAiEditOutputCount(
        Number.isFinite(outputCount)
          ? Math.max(1, Math.min(3, Math.floor(outputCount)))
          : 1
      );
      setAiEditPrompt("");
      setAiEraserRunMode("prompt");
      setAiEditError(null);
      setAiMaskHasPaint(false);
      setError(null);
    },
    [clearSelectedFilesForImageActions, isImage]
  );

  const closeAiDualDialog = useCallback(() => {
    setAiDualDialog(null);
    setAiDualPrompt("");
    setAiDualError(null);
    setAiDualDragRole(null);
  }, []);

  const startAiDualTemplatePresetForEntries = useCallback(
    (
      sourceEntries: DraftEntry[],
      provider: Exclude<AiEditProvider, "zimage">,
      outputCount: number
    ) => {
      clearSelectedFilesForImageActions();
      const targets = sourceEntries.filter(
        (entry, index, arr) =>
          entry.type === "file" &&
          isImage(entry.name) &&
          arr.findIndex((candidate) => candidate.path === entry.path) === index
      );
      if (targets.length !== 2) {
        setError("DigiDeal Main Dual requires exactly two selected images.");
        return;
      }
      const blocked = targets.filter(
        (entry) => pendingAiEditsByOriginal[entry.path] || aiEditJobsByPath[entry.path]
      );
      if (blocked.length > 0) {
        setError("Resolve pending/running AI edits before using DigiDeal Main Dual.");
        return;
      }
      const scoreForProductRole = (entry: DraftEntry) => {
        const whiteSides = Number(entry.whiteSides ?? 0);
        const borderWhiteDensity = Number(entry.borderWhiteDensity ?? 0);
        const sideScore = Number.isFinite(whiteSides) ? whiteSides : 0;
        const borderScore = Number.isFinite(borderWhiteDensity) ? borderWhiteDensity : 0;
        return sideScore * 100 + borderScore;
      };
      const ordered = [...targets].sort((a, b) => {
        const scoreDiff = scoreForProductRole(b) - scoreForProductRole(a);
        if (scoreDiff !== 0) return scoreDiff;
        return a.name.localeCompare(b.name);
      });
      setAiDualDialog({
        provider,
        outputCount: Number.isFinite(outputCount)
          ? Math.max(1, Math.min(3, Math.floor(outputCount)))
          : 1,
        productEntry: ordered[0],
        guidanceEntry: ordered[1],
      });
      setAiDualPrompt("");
      setAiDualError(null);
      setAiDualDragRole(null);
      setError(null);
    },
    [
      aiEditJobsByPath,
      clearSelectedFilesForImageActions,
      isImage,
      pendingAiEditsByOriginal,
    ]
  );

  const swapAiDualRoles = useCallback(() => {
    setAiDualDialog((prev) => {
      if (!prev) return prev;
      return {
        ...prev,
        productEntry: prev.guidanceEntry,
        guidanceEntry: prev.productEntry,
      };
    });
  }, []);

  const handleAiDualRoleDrop = useCallback(
    (targetRole: "product" | "guidance") => {
      if (!aiDualDragRole || aiDualDragRole === targetRole) {
        setAiDualDragRole(null);
        return;
      }
      swapAiDualRoles();
      setAiDualDragRole(null);
    },
    [aiDualDragRole, swapAiDualRoles]
  );

  const submitAiDualTemplatePreset = useCallback(async () => {
    if (!aiDualDialog || aiDualSubmitting) return;
    const pathAtStart = String(currentPathRef.current || "");
    const nextPrompt = aiDualPrompt.trim();
    const productPath = aiDualDialog.productEntry.path;
    const guidancePath = aiDualDialog.guidanceEntry.path;
    const provider = aiDualDialog.provider;
    const outputCount = aiDualDialog.outputCount;
    let runningProductPath = productPath;
    let runningGuidancePath = guidancePath;

    setAiDualSubmitting(true);
    setAiDualError(null);
    setError(null);
    // Submit in background: close the dialog immediately after trigger.
    setAiDualDialog(null);
    setAiDualPrompt("");
    setAiDualError(null);
    setAiDualDragRole(null);
    clearSelectedFilesForImageActions();
    try {
      const resolveLiveImagePath = async (candidatePathRaw: string) => {
        const candidatePath = String(candidatePathRaw || "").trim();
        if (!candidatePath) {
          throw new Error("File not found: (empty path)");
        }
        const folderPath = getParentFolderPathFromEntryPath(candidatePath);
        const fileName = extractFileNameFromPath(candidatePath);
        if (!folderPath || !fileName) {
          throw new Error(`File not found: ${candidatePath}`);
        }

        const folderEntries = await listPathEntries(folderPath);
        const imageFiles = folderEntries.filter(
          (entry) => entry.type === "file" && isImage(entry.name)
        );

        const exactPathMatch = imageFiles.find((entry) => entry.path === candidatePath);
        if (exactPathMatch) {
          return exactPathMatch.path;
        }

        const exactFileNameMatch = imageFiles.find((entry) => entry.name === fileName);
        if (exactFileNameMatch) {
          return exactFileNameMatch.path;
        }

        const lowerFileName = fileName.toLowerCase();
        const caseInsensitiveMatches = imageFiles.filter(
          (entry) => String(entry.name || "").toLowerCase() === lowerFileName
        );
        if (caseInsensitiveMatches.length === 1) {
          return caseInsensitiveMatches[0].path;
        }

        throw new Error(`File not found: ${candidatePath}`);
      };

      const resolvedProductPath = await resolveLiveImagePath(productPath);
      const resolvedGuidancePath = await resolveLiveImagePath(guidancePath);
      if (resolvedProductPath === resolvedGuidancePath) {
        throw new Error("Dual preset requires two different images.");
      }
      runningProductPath = resolvedProductPath;
      runningGuidancePath = resolvedGuidancePath;

      setAiEditJobsByPath((prev) => ({
        ...prev,
        [runningProductPath]: {
          provider,
          mode: "template",
          status: "running",
          startedAt: Date.now(),
        },
        [runningGuidancePath]: {
          provider,
          mode: "template",
          status: "running",
          startedAt: Date.now(),
        },
      }));

      const response = await fetch("/api/drafts/ai-edits", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          path: runningProductPath,
          secondaryPath: runningGuidancePath,
          provider,
          mode: "template",
          templatePreset: "digideal_main_dual",
          outputCount,
          prompt: nextPrompt,
        }),
      });
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        throw new Error(payload?.error || "Dual image generation failed.");
      }

      const targetSpuPath = getSpuRootFromDraftPath(productPath);
      if (targetSpuPath) {
        markSpuNeedsAiReview([targetSpuPath]);
      }

      const latestPath = String(currentPathRef.current || "");
      if (pathAtStart && latestPath === pathAtStart) {
        await refreshEntries(pathAtStart);
        await fetchPendingAiEdits(pathAtStart);
      }
    } catch (err) {
      const message = (err as Error).message;
      const isMissingFileError = /\bfile not found\b/i.test(message);
      if (isMissingFileError) {
        const latestPath = String(currentPathRef.current || "");
        if (latestPath) {
          await refreshEntries(latestPath);
          await fetchPendingAiEdits(latestPath);
        }
      }
      setAiDualError(message);
      setError(
        isMissingFileError
          ? `DigiDeal Main Dual failed because one selected image path no longer exists (${message}). The folder view was refreshed; reselect two current images and run again.`
          : `DigiDeal Main Dual failed: ${message}`
      );
    } finally {
      setAiDualSubmitting(false);
      setAiEditJobsByPath((prev) => {
        const next = { ...prev };
        if (
          next[productPath]?.provider === provider &&
          next[productPath]?.mode === "template"
        ) {
          delete next[productPath];
        }
        if (
          next[guidancePath]?.provider === provider &&
          next[guidancePath]?.mode === "template"
        ) {
          delete next[guidancePath];
        }
        if (
          next[runningProductPath]?.provider === provider &&
          next[runningProductPath]?.mode === "template"
        ) {
          delete next[runningProductPath];
        }
        if (
          next[runningGuidancePath]?.provider === provider &&
          next[runningGuidancePath]?.mode === "template"
        ) {
          delete next[runningGuidancePath];
        }
        return next;
      });
    }
  }, [
    aiDualDialog,
    aiDualPrompt,
    aiDualSubmitting,
    clearSelectedFilesForImageActions,
    getParentFolderPathFromEntryPath,
    extractFileNameFromPath,
    fetchPendingAiEdits,
    isImage,
    listPathEntries,
    markSpuNeedsAiReview,
    refreshEntries,
  ]);

  const closeAiCollectionDialog = useCallback(() => {
    setAiCollectionDialog(null);
    setAiCollectionPrompt("");
    setAiCollectionError(null);
  }, []);

  const startAiProductCollectionForEntries = useCallback(
    (
      sourceEntries: DraftEntry[],
      provider: Exclude<AiEditProvider, "zimage">
    ) => {
      clearSelectedFilesForImageActions();
      const targets = sourceEntries.filter(
        (entry, index, arr) =>
          entry.type === "file" &&
          isImage(entry.name) &&
          arr.findIndex((candidate) => candidate.path === entry.path) === index
      );
      if (targets.length < 2 || targets.length > 4) {
        setError("Product Collection requires selecting 2 to 4 images.");
        return;
      }
      const blocked = targets.filter(
        (entry) => pendingAiEditsByOriginal[entry.path] || aiEditJobsByPath[entry.path]
      );
      if (blocked.length > 0) {
        setError("Resolve pending/running AI edits before using Product Collection.");
        return;
      }
      setAiCollectionDialog({
        provider,
        entries: [...targets],
      });
      setAiCollectionPrompt("");
      setAiCollectionError(null);
      setError(null);
    },
    [
      aiEditJobsByPath,
      clearSelectedFilesForImageActions,
      isImage,
      pendingAiEditsByOriginal,
    ]
  );

  const submitAiCollection = useCallback(async () => {
    if (!aiCollectionDialog || aiCollectionSubmitting) return;
    const pathAtStart = String(currentPathRef.current || "");
    const provider = aiCollectionDialog.provider;
    const selectedEntries = [...aiCollectionDialog.entries];
    const collectionPaths = selectedEntries.map((entry) => entry.path);
    if (collectionPaths.length < 2 || collectionPaths.length > 4) {
      setAiCollectionError("Product Collection requires selecting 2 to 4 images.");
      return;
    }
    const promptText = aiCollectionPrompt.trim();

    setAiCollectionSubmitting(true);
    setAiCollectionError(null);
    setError(null);
    setAiEditJobsByPath((prev) => {
      const next = { ...prev };
      for (const path of collectionPaths) {
        next[path] = {
          provider,
          mode: "template",
          status: "running",
          startedAt: Date.now(),
        };
      }
      return next;
    });
    // Run in background: close the window immediately after trigger.
    setAiCollectionDialog(null);
    setAiCollectionPrompt("");
    setAiCollectionError(null);
    clearSelectedFilesForImageActions();
    try {
      const response = await fetch("/api/drafts/ai-edits", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          path: collectionPaths[0],
          collectionPaths,
          provider,
          mode: "template",
          templatePreset: "product_collection",
          outputCount: 1,
          prompt: promptText,
        }),
      });
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        throw new Error(payload?.error || "Product Collection generation failed.");
      }

      const targetSpuPath = getSpuRootFromDraftPath(collectionPaths[0]);
      if (targetSpuPath) {
        markSpuNeedsAiReview([targetSpuPath]);
      }

      const latestPath = String(currentPathRef.current || "");
      if (pathAtStart && latestPath === pathAtStart) {
        await refreshEntries(pathAtStart);
        await fetchPendingAiEdits(pathAtStart);
      }
    } catch (err) {
      const message = (err as Error).message;
      setAiCollectionError(message);
      setError(`Product Collection failed: ${message}`);
    } finally {
      setAiCollectionSubmitting(false);
      setAiEditJobsByPath((prev) => {
        const next = { ...prev };
        for (const path of collectionPaths) {
          if (next[path]?.provider === provider && next[path]?.mode === "template") {
            delete next[path];
          }
        }
        return next;
      });
    }
  }, [
    aiCollectionDialog,
    aiCollectionPrompt,
    aiCollectionSubmitting,
    clearSelectedFilesForImageActions,
    fetchPendingAiEdits,
    markSpuNeedsAiReview,
    refreshEntries,
  ]);

  const startAiEdit = useCallback(
    (entry: DraftEntry, provider: AiEditProvider, mode: AiPromptMode) => {
      startAiEditForEntries([entry], provider, mode);
    },
    [startAiEditForEntries]
  );

  const cancelAiEdit = useCallback(() => {
    if (aiEditSubmitting) return;
    stopAiMaskDrawing();
    setAiMaskCursor((prev) => (prev.visible ? { ...prev, visible: false } : prev));
    clearAiMaskCanvas();
    setAiEditTargets([]);
    setAiEditTemplatePreset("standard");
    setAiEditOutputCount(1);
    setAiEditPrompt("");
    setAiEraserRunMode("prompt");
    setAiEditError(null);
  }, [aiEditSubmitting, clearAiMaskCanvas, stopAiMaskDrawing]);

  const submitAiEdit = useCallback(async () => {
    if (aiEditTargets.length === 0 || aiEditSubmitting) return;
    const promptText = aiEditPrompt.trim();
    if ((aiEditMode === "direct" || aiEditMode === "eraser") && !promptText) {
      setAiEditError(
        aiEditMode === "eraser"
          ? "Prompt is required for Z-image eraser."
          : "Prompt is required in direct mode."
      );
      return;
    }
    const targets = [...aiEditTargets];
    const provider = aiEditProvider;
    const mode = aiEditMode;
    const templatePreset = mode === "template" ? aiEditTemplatePreset : undefined;
    const normalizedOutputCount = Number.isFinite(aiEditOutputCount)
      ? Math.max(1, Math.min(3, Math.floor(aiEditOutputCount)))
      : 1;
    const outputCount =
      mode === "template" || mode === "direct" ? normalizedOutputCount : undefined;
    if (mode === "direct" && promptText) {
      setAiDirectPromptHistory((prev) => {
        const deduped = [promptText, ...prev.filter((item) => item !== promptText)];
        return deduped.slice(0, AI_DIRECT_PROMPT_HISTORY_LIMIT);
      });
    }
    let maskDataUrl: string | undefined;
    if (mode === "eraser" && aiEraserRunMode === "mask") {
      if (targets.length !== 1) {
        setAiEditError("Mask mode supports one image at a time.");
        return;
      }
      if (!aiMaskHasPaint) {
        setAiEditError("Paint the mask area first, then run eraser.");
        return;
      }
      const nextMask = buildAiMaskDataUrl();
      if (!nextMask) {
        setAiEditError("Unable to build mask image. Please paint again.");
        return;
      }
      maskDataUrl = nextMask;
    }
    setAiEditSubmitting(true);
    setAiEditTargets([]);
    setAiEditTemplatePreset("standard");
    setAiEditOutputCount(1);
    setAiEditPrompt("");
    setAiEraserRunMode("prompt");
    setAiEditError(null);
    setAiMaskHasPaint(false);
    setError(null);
    setAiEditSubmitting(false);
    void runAiEditsForEntries(targets, provider, mode, promptText, {
      templatePreset,
      outputCount,
      maskDataUrl,
    });
  }, [
    aiMaskHasPaint,
    aiEraserRunMode,
    aiEditOutputCount,
    aiEditTemplatePreset,
    aiEditTargets,
    aiEditMode,
    aiEditPrompt,
    aiEditProvider,
    aiEditSubmitting,
    buildAiMaskDataUrl,
    runAiEditsForEntries,
  ]);

  const resolveAiEdit = useCallback(
    async (originalPath: string, decision: AiResolveDecision) => {
      if (aiReviewSubmitting) return;
      const pathAtStart = String(currentPathRef.current || "");
      setAiReviewSubmitting(true);
      setError(null);
      try {
        const response = await fetch("/api/drafts/ai-edits/resolve", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ originalPath, decision }),
        });
        const payload = await response.json().catch(() => ({}));
        if (!response.ok) {
          throw new Error(payload?.error || "Unable to resolve AI edit.");
        }
        setPendingAiEditsByOriginal((prev) => {
          const next = { ...prev };
          delete next[originalPath];
          return next;
        });
        const refreshedScoresRaw = Array.isArray(payload?.refreshedScores)
          ? (payload.refreshedScores as Array<{ path?: unknown; pixelQualityScore?: unknown }>)
          : [];
        const discardedMovesRaw = Array.isArray(payload?.discardedMoves)
          ? (payload.discardedMoves as Array<{ sourcePath?: unknown; destinationPath?: unknown }>)
          : [];
        const refreshedScoreByPath = new Map<string, number | null>();
        const discardedUndoItems = discardedMovesRaw
          .map((row) => ({
            sourcePath: String(row?.sourcePath || "").trim(),
            destinationPath: String(row?.destinationPath || "").trim(),
          }))
          .filter(
            (row) =>
              Boolean(row.sourcePath) &&
              Boolean(row.destinationPath) &&
              row.sourcePath !== row.destinationPath
          );
        refreshedScoresRaw.forEach((row) => {
          const pathValue = String(row?.path || "").trim();
          if (!pathValue) return;
          const scoreValue =
            typeof row?.pixelQualityScore === "number" &&
            Number.isFinite(row.pixelQualityScore)
              ? Math.round(row.pixelQualityScore)
              : null;
            refreshedScoreByPath.set(pathValue, scoreValue);
        });
        setLastDeletedImageUndoItems(discardedUndoItems);
        if (refreshedScoreByPath.size > 0) {
          const now = new Date().toISOString();
          setEntries((prev) =>
            prev.map((item) => {
              if (!refreshedScoreByPath.has(item.path)) return item;
              return {
                ...item,
                modifiedAt: now,
                pixelQualityScore: refreshedScoreByPath.get(item.path) ?? null,
              };
            })
          );
          setMainViewVariantImageEntries((prev) =>
            prev.map((item) => {
              if (!refreshedScoreByPath.has(item.path)) return item;
              return {
                ...item,
                modifiedAt: now,
                pixelQualityScore: refreshedScoreByPath.get(item.path) ?? null,
              };
            })
          );
        }
        const latestPath = String(currentPathRef.current || "");
        const canRefreshActiveFolder = Boolean(pathAtStart && latestPath === pathAtStart);
        const shouldRefreshActiveFolder =
          canRefreshActiveFolder &&
          (decision === "replace_with_ai" || decision === "keep_both");
        if (decision === "replace_with_ai") {
          if (!canRefreshActiveFolder) {
            const now = new Date().toISOString();
            setEntries((prev) =>
              prev.map((item) =>
                item.path === originalPath ? { ...item, modifiedAt: now } : item
              )
            );
            setMainViewVariantImageEntries((prev) =>
              prev.map((item) =>
                item.path === originalPath ? { ...item, modifiedAt: now } : item
              )
            );
          }
        }
        if (decision !== "keep_original") {
          markSpuNeedsAiReview(originalPath);
        }
        // Close dialog immediately after server ack + optimistic state updates.
        // Heavy housekeeping runs in background to keep save/replace responsive.
        setAiReviewOriginalPath(null);
        setAiReviewSubmitting(false);

        const folderPath = getParentFolderPathFromEntryPath(originalPath);
        void (async () => {
          if (
            folderPath &&
            (decision === "replace_with_ai" || decision === "keep_both")
          ) {
            const reorderFailures = await resortImageFoldersByHierarchy([folderPath]);
            if (reorderFailures.length > 0) {
              setError(
                `AI edit saved, but re-sort failed: ${reorderFailures
                  .slice(0, 2)
                  .join("; ")}${reorderFailures.length > 2 ? "..." : ""}`
              );
            }
          }
          if (shouldRefreshActiveFolder && pathAtStart) {
            await refreshEntries(pathAtStart);
            await fetchPendingAiEdits(pathAtStart);
          }
        })();

      } catch (err) {
        setError((err as Error).message);
        setAiReviewSubmitting(false);
      }
    },
    [
      aiReviewSubmitting,
      fetchPendingAiEdits,
      markSpuNeedsAiReview,
      refreshEntries,
      resortImageFoldersByHierarchy,
    ]
  );

  const startRename = (entry: DraftEntry) => {
    if (entry.type !== "file") return;
    if (pendingAiEditsByOriginal[entry.path]) {
      setError("Resolve the pending AI edit before renaming this file.");
      return;
    }
    if (aiEditJobsByPath[entry.path]) {
      setError("Wait for the running AI edit before renaming this file.");
      return;
    }
    setRenamingPath(entry.path);
    const { baseName, extension } = splitFileNameAndExtension(entry.name);
    setRenameValue(baseName);
    setRenameExtension(extension);
  };

  const cancelRename = () => {
    setRenamingPath(null);
    setRenameValue("");
    setRenameExtension("");
  };

  const commitRename = async (entry: DraftEntry) => {
    if (renamePending) return;
    let nextBaseName = renameValue.trim();
    const fallbackExtension = splitFileNameAndExtension(entry.name).extension;
    const extension = renameExtension || fallbackExtension;
    if (
      extension &&
      nextBaseName.toLowerCase().endsWith(extension.toLowerCase())
    ) {
      nextBaseName = nextBaseName.slice(0, -extension.length).trim();
    }
    const nextName = `${nextBaseName}${extension}`;
    if (!nextBaseName || nextName === entry.name) {
      cancelRename();
      return;
    }
    setRenamePending(true);
    setError(null);
    try {
      const response = await fetch("/api/drafts/images/rename", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ path: entry.path, name: nextName }),
      });
      if (!response.ok) {
        const message = await response.text();
        throw new Error(message || "Rename failed.");
      }
      const payload = await response.json();
      const newName = String(payload.name || nextName);
      const newPath = String(payload.path || entry.path);
      const now = new Date().toISOString();

      setEntries((prev) => {
        return prev.map((item) =>
          item.path === entry.path
            ? { ...item, name: newName, path: newPath, modifiedAt: now }
            : item
        );
      });
      setSelectedFiles((prev) => {
        if (!prev.has(entry.path)) return prev;
        const next = new Set(prev);
        next.delete(entry.path);
        next.add(newPath);
        return next;
      });
      setPreviewPath((prev) => (prev === entry.path ? newPath : prev));
      setImageDimensions((prev) => {
        const dims = prev[entry.path];
        if (!dims) return prev;
        const next: Record<string, { width: number; height: number }> = { ...prev };
        delete next[entry.path];
        next[newPath] = dims;
        return next;
      });
      if (currentPath) {
        fetchPendingAiEdits(currentPath);
      }
    } catch (err) {
      setError((err as Error).message);
    } finally {
      setRenamePending(false);
      cancelRename();
    }
  };

  const formatFileSize = (bytes: number | null | undefined) => {
    if (bytes == null) return "-";
    if (bytes < 1024) return `${bytes} B`;
    const units = ["KB", "MB", "GB", "TB"];
    let value = bytes / 1024;
    let unitIndex = 0;
    while (value >= 1024 && unitIndex < units.length - 1) {
      value /= 1024;
      unitIndex += 1;
    }
    const rounded =
      value >= 100 ? Math.round(value) : Math.round(value * 10) / 10;
    return `${rounded} ${units[unitIndex]}`;
  };

  const triggerBrowserDownload = (href: string) => {
    const link = document.createElement("a");
    link.href = href;
    link.rel = "noreferrer";
    document.body.appendChild(link);
    link.click();
    link.remove();
  };

  const handleDownloadEntries = async (sourceEntries: DraftEntry[]) => {
    clearSelectedFilesForImageActions();
    for (const entry of sourceEntries) {
      if (entry.type === "dir") {
        triggerBrowserDownload(`/api/drafts/zip?path=${encodeURIComponent(entry.path)}`);
      } else {
        triggerBrowserDownload(`/api/drafts/download?path=${encodeURIComponent(entry.path)}`);
      }
      await new Promise((resolve) => setTimeout(resolve, 250));
    }
  };

  const handleDownloadAllFolders = async () => {
    if (!selectedFolder) return;
    triggerBrowserDownload(
      `/api/drafts/zip?path=${encodeURIComponent(selectedFolder)}`
    );
  };

  const handleDownloadSelectedIndividually = async () => {
    if (selectedFiles.size === 0) return;
    const selected = Array.from(selectedFiles);
    const selectedEntries = selected
      .map((relativePath) => entries.find((candidate) => candidate.path === relativePath))
      .filter((entry): entry is DraftEntry => Boolean(entry));
    await handleDownloadEntries(selectedEntries);
  };

  const handleDownloadSelectedZip = async () => {
    if (selectedTreeFolders.size === 0) return;
    const selected = Array.from(selectedTreeFolders);
    for (const relativePath of selected) {
      triggerBrowserDownload(`/api/drafts/zip?path=${encodeURIComponent(relativePath)}`);
      await new Promise((resolve) => setTimeout(resolve, 250));
    }
  };

  const handleDeleteSelectedFolders = async () => {
    const paths = Array.from(selectedTreeFolders);
    if (paths.length === 0) return;
    const locked = Object.values(pendingAiEditsByOriginal).some((row) =>
      paths.some(
        (folderPath) =>
          row.originalPath.startsWith(`${folderPath}/`) ||
          row.pendingPath.startsWith(`${folderPath}/`)
      )
    );
    if (locked) {
      setError("Resolve pending/running AI edits before deleting those folders.");
      return;
    }
    const confirmed = window.confirm(t("bulkProcessing.explorer.deleteConfirm"));
    if (!confirmed) return;
    try {
      const response = await fetch("/api/drafts/delete", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ paths }),
      });
      if (!response.ok) {
        const message = await response.text();
        throw new Error(message || "Delete failed.");
      }
      const isDeletedPath = (candidatePath: string) =>
        paths.some(
          (deletedPath) =>
            candidatePath === deletedPath || candidatePath.startsWith(`${deletedPath}/`)
        );
      setEntries((prev) => prev.filter((entry) => !isDeletedPath(entry.path)));
      setMainViewVariantImageEntries((prev) =>
        prev.filter((entry) => !isDeletedPath(entry.path))
      );
      setSelectedTreeFolders(new Set());
      setLastDeletedImageUndoItems([]);
      setPendingAiEditsByOriginal((prev) => {
        const next: Record<string, PendingAiEditRecord> = {};
        Object.entries(prev).forEach(([pathValue, row]) => {
          if (isDeletedPath(pathValue) || isDeletedPath(row.pendingPath)) return;
          next[pathValue] = row;
        });
        return next;
      });
      setAiEditJobsByPath((prev) => {
        const next: Record<string, AiEditRuntimeJob> = {};
        Object.entries(prev).forEach(([pathValue, row]) => {
          if (isDeletedPath(pathValue)) return;
          next[pathValue] = row;
        });
        return next;
      });
      setImageDimensions((prev) => {
        const next: Record<string, { width: number; height: number }> = {};
        Object.entries(prev).forEach(([pathValue, dims]) => {
          if (isDeletedPath(pathValue)) return;
          next[pathValue] = dims;
        });
        return next;
      });
      if (
        currentPath &&
        paths.some(
          (deletedPath) =>
            currentPath === deletedPath || currentPath.startsWith(`${deletedPath}/`)
        )
      ) {
        setCurrentPath(selectedFolder || "");
      }
      if (selectedFolder) {
        fetchFolderTree(selectedFolder);
      }
      const activePath = String(currentPathRef.current || "").trim();
      if (activePath) {
        await refreshEntries(activePath);
        await fetchPendingAiEdits(activePath);
      }
    } catch (err) {
      setError((err as Error).message);
    }
  };

  const collectDeletePathsWithPendingCompanions = useCallback(
    (basePaths: string[]) => {
      const next = new Set(
        (Array.isArray(basePaths) ? basePaths : [])
          .map((pathValue) => String(pathValue || "").trim())
          .filter(Boolean)
      );
      Array.from(next).forEach((pathValue) => {
        const pending = pendingAiEditsByOriginal[pathValue];
        const pendingPath = String(pending?.pendingPath || "").trim();
        if (pendingPath) {
          next.add(pendingPath);
        }
      });
      return Array.from(next);
    },
    [pendingAiEditsByOriginal]
  );

  const handleDeleteSelected = async (explicitPaths?: string[]) => {
    const selectedPaths = Array.isArray(explicitPaths)
      ? Array.from(
          new Set(
            explicitPaths
              .map((pathValue) => String(pathValue || "").trim())
              .filter(Boolean)
          )
        )
      : Array.from(new Set([...selectedFiles, ...selectedTreeFolders]));
    const paths = collectDeletePathsWithPendingCompanions(selectedPaths);
    if (paths.length === 0) return;
    const locked = selectedPaths.filter((item) => Boolean(aiEditJobsByPath[item]));
    if (locked.length > 0) {
      setError("Resolve running AI edits before deleting those files.");
      return;
    }
    const lockedPendingCompanions = paths.filter(
      (item) =>
        !selectedPaths.includes(item) &&
        Boolean(aiEditJobsByPath[item])
    );
    if (lockedPendingCompanions.length > 0) {
      setError("Resolve running AI edits before deleting those files.");
      return;
    }
    const confirmed = window.confirm(t("bulkProcessing.explorer.deleteConfirm"));
    if (!confirmed) return;
    const isDeletedPath = (candidatePath: string) =>
      paths.some(
        (deletedPath) =>
          candidatePath === deletedPath || candidatePath.startsWith(`${deletedPath}/`)
      );

    const previousEntries = entries;
    const previousMainViewVariantImageEntries = mainViewVariantImageEntries;
    const previousSelectedFiles = selectedFiles;
    const previousSelectedTreeFolders = selectedTreeFolders;
    const previousLastDeletedImageUndoItems = lastDeletedImageUndoItems;
    const previousPendingAiEditsByOriginal = pendingAiEditsByOriginal;
    const previousAiEditJobsByPath = aiEditJobsByPath;
    const previousImageDimensions = imageDimensions;
    const previousPreviewPath = previewPath;

    setEntries((prev) => prev.filter((entry) => !isDeletedPath(entry.path)));
    setMainViewVariantImageEntries((prev) =>
      prev.filter((entry) => !isDeletedPath(entry.path))
    );
    setSelectedFiles(new Set());
    setSelectedTreeFolders(new Set());
    setLastDeletedImageUndoItems([]);
    setPendingAiEditsByOriginal((prev) => {
      const next: Record<string, PendingAiEditRecord> = {};
      Object.entries(prev).forEach(([pathValue, row]) => {
        if (isDeletedPath(pathValue) || isDeletedPath(row.pendingPath)) return;
        next[pathValue] = row;
      });
      return next;
    });
    setAiEditJobsByPath((prev) => {
      const next: Record<string, AiEditRuntimeJob> = {};
      Object.entries(prev).forEach(([pathValue, row]) => {
        if (isDeletedPath(pathValue)) return;
        next[pathValue] = row;
      });
      return next;
    });
    setImageDimensions((prev) => {
      const next: Record<string, { width: number; height: number }> = {};
      Object.entries(prev).forEach(([pathValue, dims]) => {
        if (isDeletedPath(pathValue)) return;
        next[pathValue] = dims;
      });
      return next;
    });
    setPreviewPath((prev) => (prev && isDeletedPath(prev) ? null : prev));
    try {
      const response = await fetch("/api/drafts/delete", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ paths }),
      });
      if (!response.ok) {
        const message = await response.text();
        throw new Error(message || "Delete failed.");
      }
      if (selectedFolder) {
        fetchFolderTree(selectedFolder);
      }
      const activePath = String(currentPathRef.current || "").trim();
      if (activePath) {
        await refreshEntries(activePath);
        await fetchPendingAiEdits(activePath);
      }
    } catch (err) {
      setEntries(previousEntries);
      setMainViewVariantImageEntries(previousMainViewVariantImageEntries);
      setSelectedFiles(previousSelectedFiles);
      setSelectedTreeFolders(previousSelectedTreeFolders);
      setLastDeletedImageUndoItems(previousLastDeletedImageUndoItems);
      setPendingAiEditsByOriginal(previousPendingAiEditsByOriginal);
      setAiEditJobsByPath(previousAiEditJobsByPath);
      setImageDimensions(previousImageDimensions);
      setPreviewPath(previousPreviewPath);
      setError((err as Error).message);
    }
  };

  const resolveDeletedImagesTargetPath = useCallback(
    (sourcePathRaw: string) => {
      const sourcePath = String(sourcePathRaw || "").trim();
      if (!sourcePath) return "";
      const parentPath = getParentFolderPathFromEntryPath(sourcePath);
      const basePath = parentPath || String(currentPath || "").trim();
      if (!basePath) return "";
      return `${basePath}/deleted images`.replace(/\/{2,}/g, "/");
    },
    [currentPath]
  );

  const handleMoveEntriesToDeletedImages = useCallback(
    async (sourcePaths: string[]) => {
      const targetsByFolder = new Map<string, string[]>();
      (Array.isArray(sourcePaths) ? sourcePaths : []).forEach((sourcePathRaw) => {
        const sourcePath = String(sourcePathRaw || "").trim();
        if (!sourcePath) return;
        const targetPath = resolveDeletedImagesTargetPath(sourcePath);
        if (!targetPath) return;
        const existing = targetsByFolder.get(targetPath);
        if (existing) {
          if (!existing.includes(sourcePath)) {
            existing.push(sourcePath);
          }
          return;
        }
        targetsByFolder.set(targetPath, [sourcePath]);
      });

      for (const [targetPath, paths] of targetsByFolder.entries()) {
        if (paths.length === 0) continue;
        await handleMoveEntriesToFolder(paths, targetPath);
      }
    },
    [handleMoveEntriesToFolder, resolveDeletedImagesTargetPath]
  );

  useEffect(() => {
    const handleDeleteKey = (event: KeyboardEvent) => {
      if (event.key !== "Delete") return;
      const target = event.target as HTMLElement | null;
      const tagName = String(target?.tagName || "").toLowerCase();
      if (
        target?.isContentEditable ||
        tagName === "input" ||
        tagName === "textarea" ||
        tagName === "select"
      ) {
        return;
      }
      if (
        !currentPath ||
        movingEntry ||
        renamePending ||
        Boolean(renamingPath) ||
        Boolean(fileViewerPath) ||
        Boolean(previewPath) ||
        Boolean(aiReviewOriginalPath) ||
        aiEditTargets.length > 0 ||
        variantsEditorOpen ||
        detailOpen
      ) {
        return;
      }

      const visibleImagePaths = new Set(
        displayImageEntriesRef.current
          .filter((entry) => entry.type === "file" && isImage(entry.name))
          .map((entry) => entry.path)
      );
      const selectedFilePaths = Array.from(selectedFiles).filter((pathValue) => {
        const fileEntry = entryByPath.get(pathValue);
        if (fileEntry?.type === "file") return true;
        return visibleImagePaths.has(pathValue);
      });
      if (selectedFilePaths.length === 0) return;
      event.preventDefault();
      void handleMoveEntriesToDeletedImages(selectedFilePaths);
    };

    window.addEventListener("keydown", handleDeleteKey);
    return () => {
      window.removeEventListener("keydown", handleDeleteKey);
    };
  }, [
    aiEditTargets.length,
    aiReviewOriginalPath,
    currentPath,
    detailOpen,
    entryByPath,
    fileViewerPath,
    handleMoveEntriesToDeletedImages,
    isImage,
    movingEntry,
    previewPath,
    renamePending,
    renamingPath,
    selectedFiles,
    variantsEditorOpen,
  ]);

  useEffect(() => {
    const handleSelectAllImagesKey = (event: KeyboardEvent) => {
      const key = String(event.key || "").toLowerCase();
      const selectAllPressed =
        key === "a" && (event.ctrlKey || event.metaKey) && !event.altKey;
      if (!selectAllPressed) return;
      const target = event.target as HTMLElement | null;
      const tagName = String(target?.tagName || "").toLowerCase();
      if (
        target?.isContentEditable ||
        tagName === "input" ||
        tagName === "textarea" ||
        tagName === "select"
      ) {
        return;
      }
      if (
        !currentPath ||
        movingEntry ||
        renamePending ||
        Boolean(renamingPath) ||
        Boolean(fileViewerPath) ||
        Boolean(previewPath) ||
        Boolean(aiReviewOriginalPath) ||
        aiEditTargets.length > 0 ||
        variantsEditorOpen ||
        detailOpen
      ) {
        return;
      }
      const visibleImagePaths = displayImageEntriesRef.current
        .filter((entry) => entry.type === "file" && isImage(entry.name))
        .map((entry) => entry.path);
      if (visibleImagePaths.length === 0) return;
      event.preventDefault();
      selectionAnchorImagePathRef.current = visibleImagePaths[0] ?? null;
      setSelectedTreeFolders(new Set());
      setSelectedFiles(new Set(visibleImagePaths));
    };

    window.addEventListener("keydown", handleSelectAllImagesKey);
    return () => {
      window.removeEventListener("keydown", handleSelectAllImagesKey);
    };
  }, [
    aiEditTargets.length,
    aiReviewOriginalPath,
    currentPath,
    detailOpen,
    fileViewerPath,
    isImage,
    movingEntry,
    previewPath,
    renamePending,
    renamingPath,
    variantsEditorOpen,
  ]);

  const uploadFilesToCurrentPath = useCallback(
    async (files: File[]) => {
      if (!currentPath || files.length === 0) return;
      try {
        const formData = new FormData();
        formData.append("targetPath", currentPath);
        files.forEach((file) => formData.append("files", file));
        const response = await fetch("/api/drafts/upload", {
          method: "POST",
          body: formData,
        });
        const raw = await response.text();
        let payload: Record<string, unknown> = {};
        if (raw) {
          try {
            payload = JSON.parse(raw) as Record<string, unknown>;
          } catch {
            payload = { error: raw };
          }
        }
        if (!response.ok) {
          throw new Error(String(payload?.error || "").trim() || "Upload failed.");
        }
        const uploadedPaths: string[] = (Array.isArray(payload?.items) ? payload.items : [])
          .map((item: unknown) =>
            item && typeof item === "object"
              ? String((item as { path?: unknown }).path || "").trim()
              : ""
          )
          .filter(Boolean);
        if (uploadedPaths.length > 0) {
          setPendingImageScorePaths((prev) => {
            const next = new Set(prev);
            uploadedPaths.forEach((pathValue) => next.add(pathValue));
            return next;
          });
        }
        await refreshEntries(currentPath);
        void fetchPendingAiEdits(currentPath);
        if (uploadedPaths.length > 0) {
          void refreshImageScoresForPaths(uploadedPaths);
        }
        if (selectedFolder) {
          void fetchFolderTree(selectedFolder);
        }
      } catch (err) {
        setError((err as Error).message);
      }
    },
    [
      currentPath,
      fetchPendingAiEdits,
      fetchFolderTree,
      refreshImageScoresForPaths,
      refreshEntries,
      selectedFolder,
    ]
  );

  const uploadImageUrlsToPath = useCallback(
    async (
      targetPath: string,
      urls: string[],
      options?: { minWidth?: number; minHeight?: number }
    ) => {
      const normalizedTargetPath = String(targetPath || "").trim();
      if (!normalizedTargetPath || urls.length === 0) {
        return { uploaded: 0, failed: 0, items: [] } as Record<string, unknown>;
      }
      const minWidth = Number(options?.minWidth);
      const minHeight = Number(options?.minHeight);
      const requestBody: Record<string, unknown> = {
        targetPath: normalizedTargetPath,
        urls,
      };
      if (Number.isFinite(minWidth) && minWidth > 0) {
        requestBody.minWidth = Math.round(minWidth);
      }
      if (Number.isFinite(minHeight) && minHeight > 0) {
        requestBody.minHeight = Math.round(minHeight);
      }
      const response = await fetch("/api/drafts/upload-urls", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify(requestBody),
      });
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        throw new Error(payload?.error || "Unable to add images from URL.");
      }
      const uploadedPaths: string[] = (Array.isArray(payload?.items) ? payload.items : [])
        .map((item: unknown) =>
          item && typeof item === "object"
            ? String((item as { path?: unknown }).path || "").trim()
            : ""
        )
        .filter(Boolean);
      if (uploadedPaths.length > 0) {
        setPendingImageScorePaths((prev) => {
          const next = new Set(prev);
          uploadedPaths.forEach((pathValue) => next.add(pathValue));
          return next;
        });
      }
      if (currentPath && currentPath === normalizedTargetPath) {
        await refreshEntries(normalizedTargetPath);
        void fetchPendingAiEdits(normalizedTargetPath);
      }
      if (uploadedPaths.length > 0) {
        void refreshImageScoresForPaths(uploadedPaths);
      }
      if (selectedFolder) {
        void fetchFolderTree(selectedFolder);
      }
      return payload as Record<string, unknown>;
    },
    [
      currentPath,
      fetchPendingAiEdits,
      fetchFolderTree,
      refreshImageScoresForPaths,
      refreshEntries,
      selectedFolder,
    ]
  );

  const uploadImageUrlsToCurrentPath = useCallback(
    async (urls: string[]) => {
      if (!currentPath) {
        return { uploaded: 0, failed: 0, items: [] } as Record<string, unknown>;
      }
      return uploadImageUrlsToPath(currentPath, urls);
    },
    [currentPath, uploadImageUrlsToPath]
  );

  const queueAddImagesDialogFiles = useCallback((files: File[]) => {
    const nextFiles = Array.from(files || []).filter((file): file is File => file instanceof File);
    if (nextFiles.length === 0) return;
    setAddImagesDialogFiles((prev) => [...prev, ...nextFiles]);
  }, []);

  const closeAddImagesDialog = useCallback(() => {
    if (addImagesSubmitting) return;
    setAddImagesDialogOpen(false);
    setAddImagesDialogDragActive(false);
    setAddImagesDialogFiles([]);
    setImageUrlInput("");
    if (addImagesFileInputRef.current) {
      addImagesFileInputRef.current.value = "";
    }
  }, [addImagesSubmitting]);

  const closeImportEditsDialog = useCallback(() => {
    if (importEditsSubmitting) return;
    setImportEditsDialogOpen(false);
    setExcelFile(null);
    setZipFile(null);
    setExcelStatus("idle");
    setZipStatus("idle");
    if (importEditsExcelInputRef.current) {
      importEditsExcelInputRef.current.value = "";
    }
    if (importEditsZipInputRef.current) {
      importEditsZipInputRef.current.value = "";
    }
  }, [importEditsSubmitting]);

  const resolveDownloadedFolderPath = useCallback(() => {
    const parts = String(currentPath || "").split("/").filter(Boolean);
    if (parts.length < 2) return "";
    return `${parts[0]}/${parts[1]}/Downloaded Images (D)`;
  }, [currentPath]);

  const normalizeExternalImageUrl = useCallback((value: unknown) => {
    if (typeof value !== "string") return "";
    const trimmed = value.trim();
    if (!trimmed) return "";
    try {
      const parsed = new URL(trimmed);
      if (!["http:", "https:"].includes(parsed.protocol)) return "";
      return parsed.toString();
    } catch {
      return "";
    }
  }, []);

  const isLikelyAmazonGalleryImageUrl = useCallback((value: string) => {
    try {
      const parsed = new URL(String(value || "").trim());
      if (!["http:", "https:"].includes(parsed.protocol)) return false;
      const host = parsed.hostname.toLowerCase();
      if (!/(^|\.)media-amazon\.com$/.test(host)) return false;
      const pathname = parsed.pathname.toLowerCase();
      if (!pathname.startsWith("/images/i/")) return false;
      if (!/\.(jpg|jpeg|png|webp)$/.test(pathname)) return false;
      if (pathname.endsWith(".css") || pathname.endsWith(".js")) return false;
      return true;
    } catch {
      return false;
    }
  }, []);

  const handleSubmitAddImagesDialog = useCallback(async () => {
    if (!currentPath || addImagesSubmitting) return;
    const urls = parseImageUrlsInput(imageUrlInput);
    const files = [...addImagesDialogFiles];
    if (urls.length === 0 && files.length === 0) {
      setError("Add at least one valid image URL or upload file.");
      return;
    }

    setAddImagesSubmitting(true);
    setError(null);
    try {
      if (files.length > 0) {
        await uploadFilesToCurrentPath(files);
      }
      if (urls.length > 0) {
        const payload = await uploadImageUrlsToCurrentPath(urls);
        const failed = Number(payload?.failed || 0);
        if (failed > 0) {
          setError(`Added ${payload?.uploaded || 0} image(s), ${failed} failed.`);
        }
      }
      setImageUrlInput("");
      setAddImagesDialogFiles([]);
      setAddImagesDialogDragActive(false);
      setAddImagesDialogOpen(false);
      if (addImagesFileInputRef.current) {
        addImagesFileInputRef.current.value = "";
      }
    } catch (err) {
      setError((err as Error).message);
    } finally {
      setAddImagesSubmitting(false);
    }
  }, [
    addImagesDialogFiles,
    addImagesSubmitting,
    currentPath,
    imageUrlInput,
    uploadFilesToCurrentPath,
    uploadImageUrlsToCurrentPath,
  ]);

  const closeReverseSearchDialog = useCallback(() => {
    if (reverseSearchLoading || reverseSearchFetchingAmazon || reverseSearchAddingDownloaded) {
      return;
    }
    setReverseSearchOpen(false);
    setReverseSearchError(null);
    setReverseSearchItems([]);
    setReverseSearchAmazonLinks([]);
    setReverseSearchImagePath("");
    setReverseSearchImageName("");
    setReverseSearchSearchId(null);
    setReverseSearchDownloadedCount(0);
    setReverseSearchInputPayload(null);
    setReverseSearchDebugPayload(null);
    setReverseSearchBrokenPreviewUrls({});
  }, [reverseSearchAddingDownloaded, reverseSearchFetchingAmazon, reverseSearchLoading]);

  const parseReverseSearchJobFromStoredRow = useCallback((value: unknown) => {
    if (!value || typeof value !== "object") return null;
    const row = value as Record<string, unknown>;
    if (!row.hasRecord) return null;
    const targetImagePath = String(row.targetImagePath || "").trim();
    if (!targetImagePath) return null;
    const targetImageName =
      String(row.targetImageName || "").trim() || extractFileNameFromPath(targetImagePath);
    const searchImagePath =
      String(row.searchImagePath || "").trim() || targetImagePath;
    const searchImageName =
      String(row.searchImageName || "").trim() || extractFileNameFromPath(searchImagePath);
    const statusRaw = String(row.status || "").trim().toLowerCase();
    const status: ReverseSearchJobState["status"] =
      statusRaw === "error"
        ? "error"
        : statusRaw === "running"
          ? "running"
          : "ready";
    const items = Array.isArray(row.items)
      ? (row.items as ReverseSearchResultItem[])
      : [];
    const amazonLinks = Array.isArray(row.amazonLinks)
      ? (row.amazonLinks as unknown[])
          .map((entry) => String(entry || "").trim())
          .filter(Boolean)
      : [];
    return {
      targetImagePath,
      targetImageName,
      searchImagePath,
      searchImageName,
      status,
      startedAt: String(row.startedAt || "").trim() || new Date(0).toISOString(),
      finishedAt: String(row.finishedAt || "").trim() || null,
      searchId:
        typeof row.searchId === "string" && row.searchId.trim()
          ? row.searchId.trim()
          : null,
      items,
      amazonLinks,
      inputPayload: toObjectRecordOrNull(row.inputPayload),
      debugPayload: toObjectRecordOrNull(row.debugPayload),
      error:
        typeof row.error === "string" && row.error.trim() ? row.error.trim() : null,
    } satisfies ReverseSearchJobState;
  }, []);

  const fetchStoredReverseSearchJobs = useCallback(
    async (imagePaths: string[], includeItems: boolean) => {
      const targets = Array.from(
        new Set(
          imagePaths
            .map((entry) => String(entry || "").trim())
            .filter(Boolean)
        )
      );
      if (targets.length === 0) return {} as Record<string, ReverseSearchJobState>;
      const response = await fetch("/api/drafts/google-lens/records", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          imagePaths: targets,
          includeItems,
        }),
      });
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        throw new Error(payload?.error || "Unable to load saved Google Lens search.");
      }
      const rows = Array.isArray(payload?.records) ? payload.records : [];
      const jobs: Record<string, ReverseSearchJobState> = {};
      rows.forEach((row: unknown) => {
        const parsed = parseReverseSearchJobFromStoredRow(row);
        if (!parsed) return;
        jobs[parsed.targetImagePath] = parsed;
      });
      return jobs;
    },
    [parseReverseSearchJobFromStoredRow]
  );

  const openReverseSearchDialogFromJob = useCallback((job: ReverseSearchJobState) => {
    setReverseSearchOpen(true);
    setReverseSearchLoading(false);
    setReverseSearchFetchingAmazon(false);
    setReverseSearchAddingDownloaded(false);
    setReverseSearchError(job.error || null);
    setReverseSearchItems(job.items || []);
    setReverseSearchAmazonLinks(job.amazonLinks || []);
    setReverseSearchImagePath(job.searchImagePath || job.targetImagePath);
    setReverseSearchImageName(job.searchImageName || job.targetImageName || "");
    setReverseSearchSearchId(job.searchId || null);
    setReverseSearchDownloadedCount(0);
    setReverseSearchInputPayload(job.inputPayload || null);
    setReverseSearchDebugPayload(job.debugPayload || null);
    setReverseSearchBrokenPreviewUrls({});
  }, []);

  const openReverseSearchDialogForImage = useCallback(
    async (targetImagePath: string) => {
      const normalizedTargetPath = String(targetImagePath || "").trim();
      if (!normalizedTargetPath) return;
      const inMemoryJob = reverseSearchJobsByPath[normalizedTargetPath];
      if (inMemoryJob?.status === "running") return;
      const hasLoadedPayload = Boolean(
        inMemoryJob &&
          (inMemoryJob.items.length > 0 ||
            inMemoryJob.amazonLinks.length > 0 ||
            inMemoryJob.inputPayload ||
            inMemoryJob.debugPayload ||
            inMemoryJob.error)
      );
      if (inMemoryJob && hasLoadedPayload) {
        openReverseSearchDialogFromJob(inMemoryJob);
        return;
      }

      setReverseSearchOpen(true);
      setReverseSearchLoading(true);
      setReverseSearchFetchingAmazon(false);
      setReverseSearchAddingDownloaded(false);
      setReverseSearchError(null);
      setReverseSearchItems([]);
      setReverseSearchAmazonLinks([]);
      setReverseSearchImagePath(normalizedTargetPath);
      setReverseSearchImageName(extractFileNameFromPath(normalizedTargetPath));
      setReverseSearchSearchId(null);
      setReverseSearchDownloadedCount(0);
      setReverseSearchInputPayload(null);
      setReverseSearchDebugPayload(null);
      setReverseSearchBrokenPreviewUrls({});
      try {
        const jobs = await fetchStoredReverseSearchJobs([normalizedTargetPath], true);
        const storedJob = jobs[normalizedTargetPath] ?? null;
        if (!storedJob) {
          setReverseSearchLoading(false);
          setReverseSearchError("No saved Google Lens search found for this image.");
          return;
        }
        setReverseSearchJobsByPath((prev) => ({
          ...prev,
          [normalizedTargetPath]: storedJob,
        }));
        openReverseSearchDialogFromJob(storedJob);
      } catch (err) {
        setReverseSearchLoading(false);
        setReverseSearchError(
          err instanceof Error
            ? err.message
            : "Unable to load saved Google Lens search."
        );
      }
    },
    [fetchStoredReverseSearchJobs, openReverseSearchDialogFromJob, reverseSearchJobsByPath]
  );

  const handleRunGoogleReverseSearch = useCallback(
    async (entry: DraftEntry, locale: ReverseSearchLocale = "sweden") => {
      if (entry.type !== "file" || !isImage(entry.name)) return;
      const clickedPath = String(entry.path || "").trim();
      if (!clickedPath) return;
      // Always search the exact image that was clicked to keep badge/result mapping
      // deterministic per image path.
      const searchEntry = entry;
      const searchImagePath = String(searchEntry.path || "").trim();
      if (!searchImagePath) return;
      const requestPayload: Record<string, unknown> = {
        imagePath: searchImagePath,
        targetImagePath: clickedPath,
        locale,
        limit: 120,
      };
      const startedAt = new Date().toISOString();
      setReverseSearchJobsByPath((prev) => ({
        ...prev,
        [clickedPath]: {
          targetImagePath: clickedPath,
          targetImageName: entry.name,
          searchImagePath,
          searchImageName: searchEntry.name || entry.name,
          status: "running",
          startedAt,
          finishedAt: null,
          searchId: null,
          items: [],
          amazonLinks: [],
          inputPayload: requestPayload,
          debugPayload: null,
          error: null,
        },
      }));

      try {
        const response = await fetch("/api/drafts/google-lens/search", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify(requestPayload),
        });
        const payload = await response.json().catch(() => ({}));
        const debugPayload = toObjectRecordOrNull(payload?.debug);
        if (!response.ok) {
          const errorMessage = payload?.error || "Google Lens search failed.";
          throw new Error(String(errorMessage));
        }

        const items = Array.isArray(payload?.items)
          ? (payload.items as ReverseSearchResultItem[])
          : [];
        const amazonLinks = Array.isArray(payload?.amazonLinks)
          ? (payload.amazonLinks as unknown[])
              .map((value) => String(value || "").trim())
              .filter(Boolean)
          : [];
        const searchId =
          typeof payload?.id === "string" && payload.id.trim()
            ? payload.id.trim()
            : null;
        const finishedAt = new Date().toISOString();
        setReverseSearchJobsByPath((prev) => ({
          ...prev,
          [clickedPath]: {
            targetImagePath: clickedPath,
            targetImageName: entry.name,
            searchImagePath,
            searchImageName: searchEntry.name || entry.name,
            status: "ready",
            startedAt,
            finishedAt,
            searchId,
            items,
            amazonLinks: Array.from(new Set(amazonLinks)),
            inputPayload: requestPayload,
            debugPayload,
            error: null,
          },
        }));
      } catch (err) {
        const message =
          err instanceof Error ? err.message : "Google Lens search failed.";
        const finishedAt = new Date().toISOString();
        setReverseSearchJobsByPath((prev) => ({
          ...prev,
          [clickedPath]: {
            targetImagePath: clickedPath,
            targetImageName: entry.name,
            searchImagePath,
            searchImageName: searchEntry.name || entry.name,
            status: "error",
            startedAt,
            finishedAt,
            searchId: null,
            items: [],
            amazonLinks: [],
            inputPayload: requestPayload,
            debugPayload: null,
            error: message,
          },
        }));
      }
    },
    [entries, isImage]
  );

  const handleFetchAmazonDataFromReverseSearch = useCallback(
    async (overrideLinks?: string[]) => {
      if (reverseSearchFetchingAmazon || reverseSearchAddingDownloaded) return;
      const sourceLinks =
        Array.isArray(overrideLinks) && overrideLinks.length > 0
          ? overrideLinks
          : reverseSearchAmazonLinks;
      const amazonUrls = Array.from(
        new Set(
          sourceLinks
            .map((value) => String(value || "").trim())
            .filter(Boolean)
        )
      );
      if (amazonUrls.length === 0) {
        setReverseSearchError("No Amazon links found in the current search results.");
        return;
      }

      setReverseSearchFetchingAmazon(true);
      setReverseSearchError(null);
      try {
        const response = await fetch("/api/amazon/scrape-product", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            urls: amazonUrls.slice(0, 8),
            provider: "direct",
            include_variant_images: true,
            include_related_products: false,
            download_images: false,
            persist_to_db: false,
            return_scraped: true,
          }),
        });
        const responseText = await response.text();
        const payload = responseText
          ? (() => {
              try {
                return JSON.parse(responseText) as Record<string, unknown>;
              } catch {
                return {};
              }
            })()
          : {};
        if (!response.ok) {
          const responsePayload =
            payload && typeof payload === "object"
              ? (payload as Record<string, unknown>)
              : {};
          const errorMessage =
            (typeof responsePayload.error === "string" && responsePayload.error.trim()) ||
            (responseText ? responseText.slice(0, 300).trim() : "") ||
            `Unable to fetch Amazon data (HTTP ${response.status}).`;
          throw new Error(errorMessage);
        }

        const imageUrls = new Set<string>();
        const responsePayload =
          payload && typeof payload === "object"
            ? (payload as Record<string, unknown>)
            : {};
        const results = Array.isArray(responsePayload.results)
          ? (responsePayload.results as unknown[])
          : [];
        const apiErrors = Array.isArray(responsePayload.errors)
          ? (responsePayload.errors as unknown[])
          : [];
        if (results.length === 0 && apiErrors.length > 0) {
          const firstError =
            apiErrors[0] && typeof apiErrors[0] === "object"
              ? (apiErrors[0] as Record<string, unknown>)
              : null;
          const firstMessage =
            (firstError && typeof firstError.error === "string" && firstError.error.trim()) ||
            "Amazon scrape did not return any usable products.";
          throw new Error(firstMessage);
        }
        results.forEach((row: unknown) => {
          const scraped =
            row && typeof row === "object"
              ? (row as { scraped?: unknown }).scraped
              : null;
          if (!scraped || typeof scraped !== "object") return;
          const scrapedRow = scraped as Record<string, unknown>;
          const mainImages = Array.isArray(scrapedRow.images) ? scrapedRow.images : [];
          mainImages.forEach((value) => {
            const normalized = normalizeExternalImageUrl(value);
            if (normalized) imageUrls.add(normalized);
          });
          const variants = Array.isArray(scrapedRow.variants) ? scrapedRow.variants : [];
          variants.forEach((variant) => {
            const variantImages =
              variant && typeof variant === "object"
                ? (variant as { images?: unknown }).images
                : null;
            if (!Array.isArray(variantImages)) return;
            variantImages.forEach((value) => {
              const normalized = normalizeExternalImageUrl(value);
              if (normalized) imageUrls.add(normalized);
            });
          });
        });

        const urls = Array.from(imageUrls).slice(0, 120);
        if (urls.length === 0) {
          throw new Error("Amazon scrape returned no image URLs to import.");
        }

        const downloadedPath = resolveDownloadedFolderPath();
        if (!downloadedPath) {
          throw new Error("Unable to resolve Downloaded folder path for this product.");
        }

        const galleryUrls = urls.filter((url) => isLikelyAmazonGalleryImageUrl(url));
        const preferredUrls = (galleryUrls.length > 0 ? galleryUrls : urls).slice(0, 120);

        setReverseSearchAddingDownloaded(true);
        try {
          const uploadPayload = await uploadImageUrlsToPath(downloadedPath, preferredUrls, {
            minWidth: 800,
            minHeight: 800,
          });
          const uploadedHighRes = Number(uploadPayload?.uploaded || 0);
          const skippedLowResHighRes = Number(uploadPayload?.skippedLowResCount || 0);

          let uploadedFallback = 0;
          let skippedLowResFallback = 0;
          if (galleryUrls.length > 0 && skippedLowResHighRes > 0) {
            const skippedRows = Array.isArray(uploadPayload?.skippedLowRes)
              ? (uploadPayload.skippedLowRes as unknown[])
              : [];
            const retryUrls = Array.from(
              new Set(
                skippedRows
                  .map((row) =>
                    row && typeof row === "object"
                      ? String((row as { url?: unknown }).url || "").trim()
                      : ""
                  )
                  .filter((value) => value && galleryUrls.includes(value))
              )
            );
            if (retryUrls.length > 0) {
              const fallbackPayload = await uploadImageUrlsToPath(downloadedPath, retryUrls, {
                minWidth: 500,
                minHeight: 500,
              });
              uploadedFallback = Number(fallbackPayload?.uploaded || 0);
              skippedLowResFallback = Number(fallbackPayload?.skippedLowResCount || 0);
            }
          }

          const uploadedTotal = uploadedHighRes + uploadedFallback;
          if (uploadedTotal > 0) {
            setReverseSearchDownloadedCount((prev) => prev + uploadedTotal);
          }

          if (uploadedFallback > 0) {
            setReverseSearchError(
              `Imported ${uploadedTotal} image(s) (${uploadedHighRes} at >=800, ${uploadedFallback} at >=500 gallery fallback).`
            );
          } else if (skippedLowResHighRes > 0 || skippedLowResFallback > 0) {
            const finalSkipped = skippedLowResFallback || skippedLowResHighRes;
            setReverseSearchError(
              `Imported ${uploadedTotal} image(s). Skipped ${finalSkipped} image(s) below ${
                skippedLowResFallback > 0 ? "500x500" : "800x800"
              }.`
            );
          }
        } finally {
          setReverseSearchAddingDownloaded(false);
        }

        if (apiErrors.length > 0) {
          const firstIssue =
            apiErrors[0] && typeof apiErrors[0] === "object"
              ? (apiErrors[0] as Record<string, unknown>)
              : null;
          const firstIssueMessage =
            (firstIssue && typeof firstIssue.error === "string" && firstIssue.error.trim()) ||
            null;
          setReverseSearchError(
            firstIssueMessage
              ? `Amazon data fetched with ${apiErrors.length} issue(s). First: ${firstIssueMessage}`
              : `Amazon data fetched with ${apiErrors.length} issue(s). Imported available images to Downloaded.`
          );
        }
      } catch (err) {
        setReverseSearchError(
          err instanceof Error ? err.message : "Unable to fetch Amazon data."
        );
      } finally {
        setReverseSearchFetchingAmazon(false);
      }
    },
    [
      normalizeExternalImageUrl,
      isLikelyAmazonGalleryImageUrl,
      resolveDownloadedFolderPath,
      reverseSearchAddingDownloaded,
      reverseSearchAmazonLinks,
      reverseSearchFetchingAmazon,
      uploadImageUrlsToPath,
    ]
  );

  const handleOpenDownloadedFolderFromReverseSearch = useCallback(() => {
    const downloadedPath = resolveDownloadedFolderPath();
    if (!downloadedPath) return;
    setMainImageViewFilter("all");
    setCurrentPath(downloadedPath);
  }, [resolveDownloadedFolderPath]);

  useEffect(() => {
    if (!currentPath) return;
    const handlePaste = (event: ClipboardEvent) => {
      const clipboardData = event.clipboardData;
      if (!clipboardData) return;
      const itemFiles = Array.from(clipboardData.items || [])
        .filter((item) => item.kind === "file" && item.type.startsWith("image/"))
        .map((item) => item.getAsFile())
        .filter((file): file is File => Boolean(file));
      const fallbackFiles =
        itemFiles.length > 0
          ? []
          : Array.from(clipboardData.files || []).filter((file) =>
              file.type.startsWith("image/")
            );
      const sourceFiles = itemFiles.length > 0 ? itemFiles : fallbackFiles;
      if (sourceFiles.length === 0) return;

      event.preventDefault();
      const segments = currentPath.split("/").filter(Boolean);
      const currentFolderName = sanitizeFileNameSegment(
        segments[segments.length - 1] || "folder"
      );
      const stamp = formatClipboardDateTime();
      const renamedFiles = sourceFiles.map((file, index) => {
        const ext = imageExtensionFromMime(file.type);
        const suffix = sourceFiles.length > 1 ? `-${index + 1}` : "";
        const fileName = `${currentFolderName}-clipboard-${stamp}${suffix}.${ext}`;
        return new File([file], fileName, {
          type: file.type || `image/${ext}`,
        });
      });

      void uploadFilesToCurrentPath(renamedFiles);
    };

    window.addEventListener("paste", handlePaste);
    return () => {
      window.removeEventListener("paste", handlePaste);
    };
  }, [currentPath, uploadFilesToCurrentPath]);

  const buildExpandedInputStyle = useCallback(
    (value: string, options?: { minPx?: number; maxPx?: number }) => {
      const maxPx = options?.maxPx ?? 1400;
      const expandedWidth = estimateExpandedInputWidthPx(value, options);
      return {
        width: `${expandedWidth}px`,
        minWidth: "100%",
        maxWidth: `min(92vw, ${maxPx}px)`,
      };
    },
    []
  );

  const renderVariantEditorInput = useCallback(
    (
      row: DraftVariantEditorRow,
      field: DraftVariantEditorEditableField,
      options?: { numeric?: boolean }
    ) => {
      const value = String(row[field] ?? "");
      return (
        <div className={styles.variantsEditorInputWrap}>
          <Input
            size="small"
            className={styles.variantsEditorInput}
            value={value}
            type="text"
            inputMode={options?.numeric ? "decimal" : undefined}
            onChange={(_, data) =>
              handleVariantEditorCellChange(row.key, field, data.value)
            }
          />
        </div>
      );
    },
    [
      handleVariantEditorCellChange,
      styles.variantsEditorInput,
      styles.variantsEditorInputWrap,
    ]
  );

  const renderVariantEditorShippingClassSelect = useCallback(
    (row: DraftVariantEditorRow) => (
      <div className={styles.variantsEditorInputWrap}>
        <select
          className={styles.variantsEditorSelect}
          value={normalizeShippingClassCode(row.draft_shipping_class)}
          onChange={(event) =>
            handleVariantEditorCellChange(
              row.key,
              "draft_shipping_class",
              normalizeShippingClassCode(event.target.value)
            )
          }
        >
          <option value="">-</option>
          {SHIPPING_CLASS_OPTIONS.map((code) => (
            <option key={code} value={code}>
              {code}
            </option>
          ))}
        </select>
      </div>
    ),
    [handleVariantEditorCellChange, styles.variantsEditorInputWrap, styles.variantsEditorSelect]
  );

  const toggleExpanded = (id: string) => {
    setExpandedSkus((prev) => {
      const next = new Set(prev);
      if (next.has(id)) {
        next.delete(id);
      } else {
        next.add(id);
      }
      return next;
    });
  };

  const startEdit = (table: "spu" | "sku", id: string, field: string, value: string | number | null) => {
    setEditingCell({ table, id, field });
    setEditingValue(value == null ? "" : String(value));
  };

  const cancelEdit = () => {
    setEditingCell(null);
    setEditingValue("");
  };

  const commitEdit = async () => {
    if (!editingCell) return;
    setIsSaving(true);
    setDraftError(null);
    const endpoint =
      editingCell.table === "spu"
        ? "/api/drafts/products/update"
        : "/api/drafts/variants/update";
    const field = editingCell.field;
    const rawValue = editingValue.trim();
    const payloadValue = rawValue === "" ? null : rawValue;
    const isNumericSkuField =
      editingCell.table === "sku" && (field === "draft_price" || field === "draft_weight");
    const numericPayloadValue = isNumericSkuField
      ? parseNumericFieldValue(payloadValue)
      : null;
    const isRawField = field.startsWith("raw_");
    try {
      const response = await fetch(endpoint, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          id: editingCell.id,
          field,
          value: isNumericSkuField ? numericPayloadValue : payloadValue,
        }),
      });
      if (!response.ok) {
        const message = await response.text();
        throw new Error(message || "Update failed.");
      }
      if (editingCell.table === "spu") {
        setSpuRows((prev) =>
          prev.map((row) =>
            row.id === editingCell.id ? { ...row, [field]: payloadValue } : row
          )
        );
      } else {
        setSkuRows((prev) =>
          prev.map((row) =>
            row.id === editingCell.id
              ? isRawField
                ? {
                    ...row,
                    draft_raw_row: {
                      ...(row.draft_raw_row ?? {}),
                      [field.replace(/^raw_/, "")]: payloadValue ?? "",
                    },
                  }
                : { ...row, [field]: payloadValue }
              : row
          )
        );
      }
      cancelEdit();
    } catch (err) {
      setDraftError((err as Error).message);
    } finally {
      setIsSaving(false);
    }
  };

  const renderEditableCell = (
    table: "spu" | "sku",
    rowId: string,
    field: string,
    value: string | number | null,
    options?: { numeric?: boolean; clamp?: boolean }
  ) => {
    const isEditing =
      editingCell?.table === table &&
      editingCell?.id === rowId &&
      editingCell?.field === field;
    const display = value == null ? "" : String(value);

    if (isEditing) {
      const editStyle = buildExpandedInputStyle(editingValue, {
        minPx: options?.numeric ? 140 : 220,
        maxPx: 1800,
      });
      return (
        <div className={styles.tableEditInputWrap}>
          <Input
            value={editingValue}
            onChange={(_, data) => setEditingValue(data.value)}
            onBlur={() => commitEdit()}
            onKeyDown={(event) => {
              if (event.key === "Enter") {
                commitEdit();
              }
              if (event.key === "Escape") {
                cancelEdit();
              }
            }}
            type="text"
            inputMode={options?.numeric ? "decimal" : undefined}
            size="small"
            className={mergeClasses(styles.tableEditInput, styles.expandedInlineInput)}
            style={editStyle}
            autoFocus
          />
        </div>
      );
    }

    return (
      <Text
        size={200}
        className={mergeClasses(options?.clamp ? styles.clampTwo : undefined)}
        title={display}
        onClick={() => startEdit(table, rowId, field, value)}
        style={{ cursor: "text" }}
      >
        {display || "-"}
      </Text>
    );
  };

  const openEntry = (entry: DraftEntry) => {
    if (entry.type === "dir") {
      setCurrentPath(entry.path);
      return;
    }
    if (isImage(entry.name)) {
      setPreviewPath(entry.path);
      return;
    }
    triggerBrowserDownload(`/api/drafts/download?path=${encodeURIComponent(entry.path)}`);
  };

  const downloadEntry = (entry: DraftEntry) => {
    if (entry.type === "dir") {
      triggerBrowserDownload(`/api/drafts/zip?path=${encodeURIComponent(entry.path)}`);
      return;
    }
    triggerBrowserDownload(`/api/drafts/download?path=${encodeURIComponent(entry.path)}`);
  };

  const resolveContextActionTargets = useCallback(
    (entry: DraftEntry, options?: { imageOnly?: boolean }) => {
      const selectedSource = options?.imageOnly
        ? displayImageEntriesRef.current
        : entries;
      const selected = selectedSource.filter(
        (candidate) => candidate.type === "file" && selectedFiles.has(candidate.path)
      );
      const entryIsSelected =
        entry.type === "file" && selectedFiles.has(entry.path);
      if (entryIsSelected && selected.length > 1) {
        const filtered = options?.imageOnly
          ? selected.filter((candidate) => isImage(candidate.name))
          : selected;
        if (filtered.length > 0) {
          return filtered;
        }
      }
      if (options?.imageOnly) {
        return entry.type === "file" && isImage(entry.name) ? [entry] : [];
      }
      return [entry];
    },
    [entries, isImage, selectedFiles]
  );

  const restoreSelectionAfterContextAction = useCallback(
    (_targets: DraftEntry[]) => {
      void _targets;
      clearSelectedFilesForImageActions();
    },
    [clearSelectedFilesForImageActions]
  );

  const applyLocalImageRename = useCallback(
    (oldPathRaw: string, nextPathRaw: string, nextNameRaw: string) => {
      const oldPath = String(oldPathRaw || "").trim();
      const nextPath = String(nextPathRaw || "").trim();
      const nextName = String(nextNameRaw || "").trim();
      if (!oldPath || !nextPath || !nextName) return;

      const applyEntryRename = (item: DraftEntry) =>
        item.path === oldPath ? { ...item, name: nextName, path: nextPath } : item;

      setEntries((prev) => prev.map(applyEntryRename));
      setMainViewVariantImageEntries((prev) => prev.map(applyEntryRename));
      setSelectedFiles((prev) => {
        if (!prev.has(oldPath)) return prev;
        const next = new Set(prev);
        next.delete(oldPath);
        next.add(nextPath);
        return next;
      });
      setPreviewPath((prev) => (prev === oldPath ? nextPath : prev));
      setImageDimensions((prev) => {
        const dims = prev[oldPath];
        if (!dims) return prev;
        const next: Record<string, { width: number; height: number }> = { ...prev };
        delete next[oldPath];
        next[nextPath] = dims;
        return next;
      });
      setContextMenu((prev) => {
        if (!prev) return prev;
        if (prev.entry.path !== oldPath) return prev;
        return {
          ...prev,
          entry: {
            ...prev.entry,
            path: nextPath,
            name: nextName,
          },
        };
      });
    },
    []
  );

  const clearVariantImageAssignmentsByFileKey = useCallback(
    async (input: { spu: string; fileNameKey: string }) => {
      const normalizedSpu = String(input.spu || "").trim().toUpperCase();
      const normalizedFileNameKey = normalizeVariantImageFileNameKey(input.fileNameKey);
      if (!normalizedSpu || !normalizedFileNameKey) return;

      const matchingRows = skuRows.filter((row) => {
        const rowSpu = String(row.draft_spu || "").trim().toUpperCase();
        if (rowSpu !== normalizedSpu) return false;
        const fileName = extractVariantImageFileName(row.draft_variant_image_url);
        const fileNameKey = normalizeVariantImageFileNameKey(fileName);
        return fileNameKey === normalizedFileNameKey;
      });
      if (matchingRows.length === 0) return;

      const rowIds = matchingRows
        .map((row) => String(row.id || "").trim())
        .filter(Boolean);
      if (rowIds.length === 0) return;

      const previousById = new Map<string, string | null>(
        matchingRows.map((row) => [
          String(row.id || "").trim(),
          row.draft_variant_image_url ?? null,
        ])
      );

      setSkuRows((prev) =>
        prev.map((row) =>
          rowIds.includes(String(row.id || "").trim())
            ? { ...row, draft_variant_image_url: null }
            : row
        )
      );

      setVariantPickerRowsBySpu((prev) => {
        const existing = prev[normalizedSpu];
        if (!existing || existing.length === 0) return prev;
        let changed = false;
        const nextRows = existing.map((row) => {
          const rowId = String(row.id || "").trim();
          if (!rowIds.includes(rowId)) return row;
          changed = true;
          return { ...row, draft_variant_image_url: null };
        });
        if (!changed) return prev;
        return {
          ...prev,
          [normalizedSpu]: nextRows,
        };
      });

      const failures: string[] = [];
      for (const rowId of rowIds) {
        try {
          const response = await fetch("/api/drafts/variants/update", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({
              id: rowId,
              field: "draft_variant_image_url",
              value: null,
            }),
          });
          const payload = await response.json().catch(() => ({}));
          if (!response.ok) {
            throw new Error(payload?.error || "Unable to clear variant image mapping.");
          }
        } catch (err) {
          const previousValue = previousById.get(rowId) ?? null;
          setSkuRows((prev) =>
            prev.map((row) =>
              String(row.id || "").trim() === rowId
                ? { ...row, draft_variant_image_url: previousValue }
                : row
            )
          );
          setVariantPickerRowsBySpu((prev) => {
            const existing = prev[normalizedSpu];
            if (!existing || existing.length === 0) return prev;
            let changed = false;
            const nextRows = existing.map((row) => {
              if (String(row.id || "").trim() !== rowId) return row;
              changed = true;
              return { ...row, draft_variant_image_url: previousValue };
            });
            if (!changed) return prev;
            return {
              ...prev,
              [normalizedSpu]: nextRows,
            };
          });
          failures.push((err as Error).message);
        }
      }

      if (failures.length > 0) {
        setError(
          `Variant release completed with ${failures.length} issue(s): ${failures
            .slice(0, 2)
            .join("; ")}${failures.length > 2 ? "..." : ""}`
        );
      }
    },
    [skuRows]
  );

  const handleApplyImageTag = useCallback(
    async (sourceEntries: DraftEntry[], tag: ImageTagOption) => {
      clearSelectedFilesForImageActions();
      const targets = sourceEntries.filter(
        (entry, index, arr) =>
          entry.type === "file" &&
          isImage(entry.name) &&
          arr.findIndex((candidate) => candidate.path === entry.path) === index
      );
      if (targets.length === 0 || bulkImageActionPending) return;

      setBulkImageActionPending(true);
      setError(null);
      const failures: string[] = [];
      const foldersNeedingResort = new Set<string>();

      try {
        for (const sourceEntry of targets) {
          const entry = entryByPath.get(sourceEntry.path) ?? sourceEntry;
          if (pendingAiEditsByOriginal[entry.path] || aiEditJobsByPath[entry.path]) {
            failures.push(`${entry.name}: resolve AI status first.`);
            continue;
          }
          const previousPath = entry.path;
          const previousName = entry.name;
          const requestedName = buildTaggedImageFileName(entry.name, tag);
          if (!requestedName || requestedName === entry.name) continue;
          const parentPath = getParentFolderPathFromEntryPath(previousPath);
          const optimisticPath = parentPath
            ? `${parentPath}/${requestedName}`.replace(/\/{2,}/g, "/")
            : previousPath;
          applyLocalImageRename(previousPath, optimisticPath, requestedName);
          if (parentPath) foldersNeedingResort.add(parentPath);
          const optimisticFolderPath = getParentFolderPathFromEntryPath(optimisticPath);
          if (optimisticFolderPath) foldersNeedingResort.add(optimisticFolderPath);
          try {
            const response = await fetch("/api/drafts/images/rename", {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                path: previousPath,
                name: requestedName,
                allowRenameOnConflict: true,
              }),
            });
            const payload = await response.json().catch(() => ({}));
            if (!response.ok) {
              throw new Error(payload?.error || "Rename failed.");
            }
            const renamedPath = String(payload?.path || optimisticPath || previousPath).trim();
            const renamedName = String(payload?.name || requestedName);
            const safeRenamedPath = renamedPath || optimisticPath || previousPath;
            const safeRenamedName = renamedName || requestedName;
            if (
              safeRenamedPath !== optimisticPath ||
              safeRenamedName !== requestedName
            ) {
              applyLocalImageRename(optimisticPath, safeRenamedPath, safeRenamedName);
            }
            const renamedFolderPath = getParentFolderPathFromEntryPath(safeRenamedPath);
            if (renamedFolderPath) foldersNeedingResort.add(renamedFolderPath);
          } catch (err) {
            applyLocalImageRename(optimisticPath, previousPath, previousName);
            failures.push(`${entry.name}: ${(err as Error).message}`);
          }
        }

        if (foldersNeedingResort.size > 0) {
          const reorderFailures = await resortImageFoldersByHierarchy(foldersNeedingResort);
          reorderFailures.forEach((message) => failures.push(`re-sort: ${message}`));
        }
      } finally {
        setBulkImageActionPending(false);
        clearSelectedFilesForImageActions();
      }

      if (failures.length > 0) {
        setError(
          `Tag update completed with ${failures.length} issue(s): ${failures
            .slice(0, 3)
            .join("; ")}${failures.length > 3 ? "..." : ""}`
        );
      }
    },
    [
      aiEditJobsByPath,
      applyLocalImageRename,
      bulkImageActionPending,
      clearSelectedFilesForImageActions,
      entryByPath,
      isImage,
      pendingAiEditsByOriginal,
      resortImageFoldersByHierarchy,
    ]
  );

  const handleToggleImageTag = useCallback(
    async (sourceEntries: DraftEntry[], tag: ImageTagOption) => {
      clearSelectedFilesForImageActions();
      const targets = sourceEntries.filter(
        (entry, index, arr) =>
          entry.type === "file" &&
          isImage(entry.name) &&
          arr.findIndex((candidate) => candidate.path === entry.path) === index
      );
      if (targets.length === 0 || bulkImageActionPending) return;

      setBulkImageActionPending(true);
      setError(null);
      const failures: string[] = [];
      const foldersNeedingResort = new Set<string>();

      try {
        for (const sourceEntry of targets) {
          const entry = entryByPath.get(sourceEntry.path) ?? sourceEntry;
          if (pendingAiEditsByOriginal[entry.path] || aiEditJobsByPath[entry.path]) {
            failures.push(`${entry.name}: resolve AI status first.`);
            continue;
          }
          const previousPath = entry.path;
          const previousName = entry.name;
          const previousTags = extractImageTagsFromFileName(previousName);
          const requestedName = toggleImageTagInFileName(entry.name, tag);
          if (!requestedName || requestedName === entry.name) continue;
          const nextTags = extractImageTagsFromFileName(requestedName);
          const removingVariantTag =
            tag === "VAR" && previousTags.includes("VAR") && !nextTags.includes("VAR");
          const previousFileName = extractFileNameFromPath(previousPath || previousName);
          const previousVariantFileNameKey = normalizeVariantImageFileNameKey(previousFileName);
          const previousPathParts = String(previousPath || "")
            .split("/")
            .filter(Boolean);
          const previousSpu = String(previousPathParts[1] || "").trim().toUpperCase();
          const parentPath = getParentFolderPathFromEntryPath(previousPath);
          const optimisticPath = parentPath
            ? `${parentPath}/${requestedName}`.replace(/\/{2,}/g, "/")
            : previousPath;
          applyLocalImageRename(previousPath, optimisticPath, requestedName);
          if (parentPath) foldersNeedingResort.add(parentPath);
          const optimisticFolderPath = getParentFolderPathFromEntryPath(optimisticPath);
          if (optimisticFolderPath) foldersNeedingResort.add(optimisticFolderPath);
          try {
            const response = await fetch("/api/drafts/images/rename", {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                path: previousPath,
                name: requestedName,
                allowRenameOnConflict: true,
                clearVariantMappings: removingVariantTag,
              }),
            });
            const payload = await response.json().catch(() => ({}));
            if (!response.ok) {
              throw new Error(payload?.error || "Rename failed.");
            }
            const renamedPath = String(payload?.path || optimisticPath || previousPath).trim();
            const renamedName = String(payload?.name || requestedName);
            const safeRenamedPath = renamedPath || optimisticPath || previousPath;
            const safeRenamedName = renamedName || requestedName;
            if (
              safeRenamedPath !== optimisticPath ||
              safeRenamedName !== requestedName
            ) {
              applyLocalImageRename(optimisticPath, safeRenamedPath, safeRenamedName);
            }
            const renamedFolderPath = getParentFolderPathFromEntryPath(safeRenamedPath);
            if (renamedFolderPath) foldersNeedingResort.add(renamedFolderPath);
            if (removingVariantTag && previousSpu && previousVariantFileNameKey) {
              await clearVariantImageAssignmentsByFileKey({
                spu: previousSpu,
                fileNameKey: previousVariantFileNameKey,
              });
            }
          } catch (err) {
            applyLocalImageRename(optimisticPath, previousPath, previousName);
            failures.push(`${entry.name}: ${(err as Error).message}`);
          }
        }

        if (foldersNeedingResort.size > 0) {
          const reorderFailures = await resortImageFoldersByHierarchy(foldersNeedingResort);
          reorderFailures.forEach((message) => failures.push(`re-sort: ${message}`));
        }
      } finally {
        setBulkImageActionPending(false);
        clearSelectedFilesForImageActions();
      }

      if (failures.length > 0) {
        setError(
          `Tag update completed with ${failures.length} issue(s): ${failures
            .slice(0, 3)
            .join("; ")}${failures.length > 3 ? "..." : ""}`
        );
      }
    },
    [
      aiEditJobsByPath,
      applyLocalImageRename,
      bulkImageActionPending,
      clearSelectedFilesForImageActions,
      entryByPath,
      isImage,
      pendingAiEditsByOriginal,
      clearVariantImageAssignmentsByFileKey,
      resortImageFoldersByHierarchy,
    ]
  );

  const collectVariantHintsForSpu = useCallback(
    async (spu: string) => {
      const normalizedSpu = String(spu || "").trim();
      if (!normalizedSpu) return [] as string[];

      const collectFromRows = (rows: Array<Record<string, unknown>>) => {
        const out: string[] = [];
        const seen = new Set<string>();
        rows.forEach((row) => {
          const combined = buildVariantCombinedZhValue({
            draft_option1: String(row.draft_option1 ?? ""),
            draft_option2: String(row.draft_option2 ?? ""),
            draft_option3: String(row.draft_option3 ?? ""),
            draft_option4: String(row.draft_option4 ?? ""),
            fallback: String(row.draft_option_combined_zh ?? ""),
          }).trim();
          if (!combined) return;
          const key = combined.toLowerCase();
          if (seen.has(key)) return;
          seen.add(key);
          out.push(combined);
        });
        return out;
      };

      const localRows = skuRows
        .filter(
          (row) => String(row.draft_spu || "").trim().toUpperCase() === normalizedSpu.toUpperCase()
        )
        .map((row) => row as unknown as Record<string, unknown>);
      const localHints = collectFromRows(localRows);
      if (localHints.length > 0) {
        return localHints.slice(0, 24);
      }

      try {
        const url = new URL("/api/drafts/variants", window.location.origin);
        url.searchParams.set("spu", normalizedSpu);
        const response = await fetch(url.toString());
        const payload = await response.json().catch(() => ({}));
        if (!response.ok) {
          throw new Error(payload?.error || "Unable to load variant hints.");
        }
        const items = Array.isArray(payload?.items) ? (payload.items as unknown[]) : [];
        const hints = collectFromRows(
          items.filter((value) => value && typeof value === "object") as Array<
            Record<string, unknown>
          >
        );
        return hints.slice(0, 24);
      } catch {
        return [];
      }
    },
    [skuRows]
  );

  const applyTagAssignments = useCallback(
    async (assignments: Array<{ path: string; tag: ImageTagOption }>) => {
      const normalizedAssignments = assignments
        .map((assignment) => ({
          path: String(assignment.path || "").trim(),
          tag: String(assignment.tag || "").trim().toUpperCase(),
        }))
        .filter(
          (assignment): assignment is { path: string; tag: ImageTagOption } =>
            Boolean(assignment.path) &&
            (assignment.tag === "MAIN" ||
              assignment.tag === "ENV" ||
              assignment.tag === "VAR" ||
              assignment.tag === "INF" ||
              assignment.tag === "DIGI")
        );

      if (normalizedAssignments.length === 0) {
        return { applied: 0, failures: [] as string[] };
      }

      setBulkImageActionPending(true);
      const failures: string[] = [];
      const renames: Record<string, { name: string; path: string }> = {};
      const workingEntries = new Map(
        entries
          .filter((entry) => entry.type === "file" && isImage(entry.name))
          .map((entry) => [entry.path, { path: entry.path, name: entry.name }])
      );

      let applied = 0;

      try {
        for (const assignment of normalizedAssignments) {
          const target =
            workingEntries.get(assignment.path) ?? {
              path: assignment.path,
              name: extractFileNameFromPath(assignment.path),
            };
          if (!target.path || !target.name) {
            failures.push(`${assignment.path}: file not found`);
            continue;
          }
          if (
            pendingAiEditsByOriginal[target.path] ||
            aiEditJobsByPath[target.path]
          ) {
            failures.push(`${target.name}: resolve AI status first.`);
            continue;
          }

          const requestedName = buildTaggedImageFileName(target.name, assignment.tag);
          if (!requestedName || requestedName === target.name) {
            continue;
          }

          try {
            const response = await fetch("/api/drafts/images/rename", {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({ path: target.path, name: requestedName }),
            });
            const payload = await response.json().catch(() => ({}));
            if (!response.ok) {
              throw new Error(payload?.error || "Rename failed.");
            }
            const renamedPath = String(payload?.path || target.path);
            const renamedName = String(payload?.name || requestedName);
            renames[target.path] = { path: renamedPath, name: renamedName };
            if (workingEntries.has(target.path)) {
              workingEntries.delete(target.path);
              workingEntries.set(renamedPath, { path: renamedPath, name: renamedName });
            }
            applied += 1;
          } catch (err) {
            failures.push(`${target.name}: ${(err as Error).message}`);
          }
        }

        if (Object.keys(renames).length > 0) {
          setEntries((prev) =>
            prev.map((item) => {
              const renamed = renames[item.path];
              if (!renamed) return item;
              return { ...item, name: renamed.name, path: renamed.path };
            })
          );
          setSelectedFiles((prev) => {
            if (prev.size === 0) return prev;
            const next = new Set(prev);
            Object.entries(renames).forEach(([oldPath, renamed]) => {
              if (!next.has(oldPath)) return;
              next.delete(oldPath);
              next.add(renamed.path);
            });
            return next;
          });
          setPreviewPath((prev) => {
            if (!prev) return prev;
            const renamed = renames[prev];
            return renamed ? renamed.path : prev;
          });
          setImageDimensions((prev) => {
            const next: Record<string, { width: number; height: number }> = { ...prev };
            Object.entries(renames).forEach(([oldPath, renamed]) => {
              const dims = next[oldPath];
              if (!dims) return;
              delete next[oldPath];
              next[renamed.path] = dims;
            });
            return next;
          });
        }
      } finally {
        setBulkImageActionPending(false);
        clearSelectedFilesForImageActions();
      }

      return { applied, failures };
    },
    [
      aiEditJobsByPath,
      clearSelectedFilesForImageActions,
      entries,
      isImage,
      pendingAiEditsByOriginal,
    ]
  );

  const persistImageOrderForFolder = useCallback(
    async (folderPath: string, orderedPaths: string[]) => {
      const normalizedFolderPath = String(folderPath || "").trim();
      if (!normalizedFolderPath) return;
      setImageOrderPersisting(true);
      try {
        await enqueueImageOrderSaveForFolder(normalizedFolderPath, orderedPaths);
      } catch (err) {
        setError((err as Error).message);
        if (currentPathRef.current === normalizedFolderPath) {
          refreshEntries(normalizedFolderPath);
        }
        throw err;
      } finally {
        setImageOrderPersisting(false);
      }
    },
    [enqueueImageOrderSaveForFolder, refreshEntries]
  );

  const persistImageOrder = useCallback(
    async (orderedPaths: string[]) => {
      const normalizedCurrentPath = String(currentPath || "").trim();
      if (!normalizedCurrentPath) return;
      try {
        await persistImageOrderForFolder(normalizedCurrentPath, orderedPaths);
      } catch {
        // Error state is already handled in persistImageOrderForFolder.
      }
    },
    [currentPath, persistImageOrderForFolder]
  );

  const completedSpuPathsInSelectedRun = useMemo(() => {
    const run = String(selectedFolder || "").trim();
    if (!run) return [] as string[];
    return Array.from(completedSpuFolders)
      .map((pathValue) => String(pathValue || "").trim())
      .filter((pathValue) => pathValue.startsWith(`${run}/`))
      .filter((pathValue) => pathValue.split("/").filter(Boolean).length === 2)
      .sort((left, right) => left.localeCompare(right));
  }, [completedSpuFolders, selectedFolder]);

  const runImageClassifierTaggingForFolder = useCallback(
    async ({
      folderPath,
      imagePaths,
      spu,
      variantHints,
    }: {
      folderPath: string;
      imagePaths: string[];
      spu?: string;
      variantHints?: string[];
    }) => {
      const normalizedImages = imagePaths
        .map((pathValue) => String(pathValue || "").trim())
        .filter(Boolean)
        .filter((pathValue) => !/\(\s*DIGI\s*\)/i.test(extractFileNameFromPath(pathValue)));
      if (normalizedImages.length === 0) {
        return { failures: [] as string[], applied: 0 };
      }

      const response = await fetch("/api/drafts/images/tag-images", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          folderPath,
          imagePaths: normalizedImages,
          spu: String(spu || "").trim(),
          variantHints: Array.isArray(variantHints) ? variantHints : [],
        }),
      });
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        throw new Error(payload?.error || "Unable to classify images.");
      }

      const decisions = Array.isArray(payload?.decisions)
        ? (payload.decisions as Array<Record<string, unknown>>)
        : [];

      const decisionRows = decisions
        .map((row) => {
          const confidenceRaw = Number(row.confidence);
          return {
            path: String(row.path || "").trim(),
            tag: String(row.primary_tag || "").trim().toUpperCase(),
            confidence:
              Number.isFinite(confidenceRaw) && confidenceRaw >= 0
                ? confidenceRaw
                : null,
          };
        })
        .filter(
          (
            row
          ): row is {
            path: string;
            tag: ImageTagOption;
            confidence: number | null;
          } =>
            Boolean(row.path) &&
            (row.tag === "MAIN" ||
              row.tag === "ENV" ||
              row.tag === "VAR" ||
              row.tag === "INF" ||
              row.tag === "DIGI")
        );

      const mainCandidates = decisionRows
        .filter((row) => row.tag === "MAIN")
        .sort((left, right) => {
          const leftConfidence = left.confidence ?? -1;
          const rightConfidence = right.confidence ?? -1;
          return rightConfidence - leftConfidence;
        });
      const primaryMainPath = mainCandidates[0]?.path ?? "";

      const assignments = decisionRows
        .filter((row) => row.tag !== "MAIN" || row.path === primaryMainPath)
        .map((row) => ({
          path: row.path,
          tag: row.tag,
        }));

      const { failures, applied } = await applyTagAssignments(assignments);

      try {
        const folderEntries = await listPathEntries(folderPath);
        const folderImageEntries = folderEntries.filter(
          (entry) => entry.type === "file" && isImage(entry.name)
        );
        if (folderImageEntries.length > 1) {
          const currentOrder = folderImageEntries.map((entry) => entry.path);
          const prioritizedOrder = buildTaggedImageOrderPaths(folderImageEntries);
          const orderChanged =
            prioritizedOrder.length === currentOrder.length &&
            prioritizedOrder.some((pathValue, index) => pathValue !== currentOrder[index]);
          if (orderChanged) {
            await persistImageOrderForFolder(folderPath, prioritizedOrder);
          }
        }
      } catch (err) {
        failures.push(
          `Unable to reorder tagged images: ${
            err instanceof Error ? err.message : "unknown error"
          }`
        );
      }
      return { failures, applied };
    },
    [applyTagAssignments, isImage, listPathEntries, persistImageOrderForFolder]
  );

  const handleRunImageClassifierTagging = useCallback(async () => {
    if (!currentPath || tagImagesRunning || bulkImageActionPending || tagAllImagesRunning) {
      return;
    }
    const pathAtStart = String(currentPath || "");

    const targets = entries
      .filter(
        (entry) =>
          entry.type === "file" &&
          isImage(entry.name) &&
          !isDigiTaggedImageName(entry.name)
      )
      .map((entry) => entry.path);
    if (targets.length === 0) return;

    clearSelectedFilesForImageActions();
    setError(null);
    setTagImagesRunningPaths((prev) => {
      const next = new Set(prev);
      next.add(pathAtStart);
      return next;
    });

    try {
      const pathParts = pathAtStart.split("/").filter(Boolean);
      const currentSpu = pathParts.length >= 2 ? pathParts[1] : "";
      const variantHints = currentSpu
        ? await collectVariantHintsForSpu(currentSpu)
        : [];

      const { failures } = await runImageClassifierTaggingForFolder({
        folderPath: pathAtStart,
        imagePaths: targets,
        spu: currentSpu,
        variantHints,
      });

      if (currentPathRef.current === pathAtStart) {
        await refreshEntries(pathAtStart);
      }

      if (failures.length > 0) {
        setError(
          `Classifier tagged images with ${failures.length} rename issue(s): ${failures
            .slice(0, 2)
            .join("; ")}${failures.length > 2 ? "..." : ""}`
        );
      }
    } catch (err) {
      setError((err as Error).message);
    } finally {
      setTagImagesRunningPaths((prev) => {
        if (!prev.has(pathAtStart)) return prev;
        const next = new Set(prev);
        next.delete(pathAtStart);
        return next;
      });
      clearSelectedFilesForImageActions();
    }
  }, [
    bulkImageActionPending,
    clearSelectedFilesForImageActions,
    collectVariantHintsForSpu,
    currentPath,
    entries,
    isImage,
    refreshEntries,
    runImageClassifierTaggingForFolder,
    setTagImagesRunningPaths,
    tagAllImagesRunning,
    tagImagesRunning,
  ]);

  const handleTagAllImagesForCompletedSpuFolders = useCallback(async () => {
    if (
      !selectedFolder ||
      tagAllImagesRunning ||
      tagImagesRunning ||
      bulkImageActionPending
    ) {
      return;
    }

    const targets = completedSpuPathsInSelectedRun;
    if (targets.length === 0) {
      setError("No completed SPU folders were found for this run.");
      return;
    }

    clearSelectedFilesForImageActions();
    setError(null);
    setTagAllImagesRunning(true);

    try {
      let processedFolders = 0;
      let totalFailures = 0;
      const failureMessages: string[] = [];

      for (const spuPath of targets) {
        const pathParts = spuPath.split("/").filter(Boolean);
        const spu = pathParts.length >= 2 ? pathParts[1] : "";
        try {
          const folderEntries = await listPathEntries(spuPath);
          const imagePaths = folderEntries
            .filter(
              (entry) =>
                entry.type === "file" &&
                isImage(entry.name) &&
                !isDigiTaggedImageName(entry.name)
            )
            .map((entry) => entry.path);
          if (imagePaths.length === 0) continue;

          processedFolders += 1;
          const variantHints = spu ? await collectVariantHintsForSpu(spu) : [];
          const { failures } = await runImageClassifierTaggingForFolder({
            folderPath: spuPath,
            imagePaths,
            spu,
            variantHints,
          });
          if (failures.length > 0) {
            totalFailures += failures.length;
            failureMessages.push(
              `${spu || spuPath}: ${failures[0]}${failures.length > 1 ? "..." : ""}`
            );
          }
        } catch (err) {
          totalFailures += 1;
          failureMessages.push(
            `${spu || spuPath}: ${
              err instanceof Error ? err.message : "Unable to tag images."
            }`
          );
        }
      }

      if (
        currentPath &&
        currentPath.startsWith(`${selectedFolder}/`)
      ) {
        await refreshEntries(currentPath);
      }

      if (processedFolders === 0) {
        setError("No direct images were found in completed SPU folders.");
      } else if (totalFailures > 0) {
        setError(
          `Tag All Images finished with ${totalFailures} issue(s): ${failureMessages
            .slice(0, 3)
            .join("; ")}${failureMessages.length > 3 ? "..." : ""}`
        );
      }
    } finally {
      setTagAllImagesRunning(false);
      clearSelectedFilesForImageActions();
    }
  }, [
    bulkImageActionPending,
    clearSelectedFilesForImageActions,
    collectVariantHintsForSpu,
    completedSpuPathsInSelectedRun,
    currentPath,
    isImage,
    listPathEntries,
    refreshEntries,
    runImageClassifierTaggingForFolder,
    selectedFolder,
    tagAllImagesRunning,
    tagImagesRunning,
  ]);

  const getContextMenuImageTagState = useCallback(
    (tag: ImageTagOption): "none" | "some" | "all" => {
      if (!contextMenu?.image) return "none";
      const targets = resolveContextActionTargets(contextMenu.entry, {
        imageOnly: true,
      });
      if (targets.length === 0) return "none";
      const taggedCount = targets.reduce((count, sourceEntry) => {
        const entry = entryByPath.get(sourceEntry.path) ?? sourceEntry;
        const tags = extractImageTagsFromFileName(entry.name);
        return tags.includes(tag) ? count + 1 : count;
      }, 0);
      if (taggedCount === 0) return "none";
      if (taggedCount === targets.length) return "all";
      return "some";
    },
    [contextMenu, entryByPath, resolveContextActionTargets]
  );
  const contextMenuLiveEntry = contextMenu
    ? entryByPath.get(contextMenu.entry.path) ?? contextMenu.entry
    : null;
  const contextMenuUpscaleDisabled =
    contextMenuLiveEntry &&
    contextMenuLiveEntry.type === "file" &&
    isImage(contextMenuLiveEntry.name)
      ? isImageEntryUpscaled(contextMenuLiveEntry)
      : false;
  const contextMenuImageTargets = useMemo(
    () =>
      contextMenu?.image
        ? resolveContextActionTargets(contextMenu.entry, {
            imageOnly: true,
          })
        : [],
    [contextMenu, resolveContextActionTargets]
  );
  const contextMenuCollageAllowed = contextMenuImageTargets.length >= 2;
  const contextMenuStackOverlayAllowed =
    contextMenuImageTargets.length >= STACK_OVERLAY_MIN_IMAGES;
  const contextMenuStackImagesAllowed = contextMenuImageTargets.length >= 2;
  const contextMenuFlipHorizontalAllowed = contextMenuImageTargets.length >= 1;
  const contextMenuMergeDigidealMainAllowed = contextMenuImageTargets.length === 2;
  const contextMenuMergeDigidealMainOverlayAllowed = contextMenuImageTargets.length === 2;
  const contextMenuMergeDigidealMainStackOverlayAllowed =
    contextMenuImageTargets.length >= DIGIDEAL_MAIN_STACK_OVERLAY_MIN_IMAGES;
  const contextMenuCollectionAllowed =
    contextMenuImageTargets.length >= 2 && contextMenuImageTargets.length <= 4;
  const contextMenuVariantSpu = useMemo(() => {
    if (!contextMenu?.image) return "";
    const anchorPath = String(contextMenu.entry.path || "").trim();
    const parts = anchorPath.split("/").filter(Boolean);
    if (parts.length < 2) return "";
    return String(parts[1] || "").trim().toUpperCase();
  }, [contextMenu]);
  const contextMenuVariantImageFileName = useMemo(() => {
    if (!contextMenuLiveEntry || contextMenuLiveEntry.type !== "file") return "";
    return extractFileNameFromPath(contextMenuLiveEntry.path || contextMenuLiveEntry.name);
  }, [contextMenuLiveEntry]);
  const contextMenuVariantImageFileNameKey = normalizeVariantImageFileNameKey(
    contextMenuVariantImageFileName
  );
  const contextMenuVariantImageIsTaggedVar = useMemo(() => {
    if (!contextMenuLiveEntry || contextMenuLiveEntry.type !== "file") return false;
    return extractImageTagsFromFileName(contextMenuLiveEntry.name).includes("VAR");
  }, [contextMenuLiveEntry]);
  const contextMenuVariantRows = useMemo(() => {
    if (!contextMenuVariantSpu) return [] as DraftSkuRow[];
    const mergedById = new Map<string, DraftSkuRow>();
    const skuRowsForSpu = skuRows.filter(
      (row) => String(row.draft_spu || "").trim().toUpperCase() === contextMenuVariantSpu
    );
    const skuIdsForSpu = new Set(
      skuRowsForSpu
        .map((row) => String(row.id || "").trim())
        .filter((rowId) => Boolean(rowId))
    );
    const shouldPruneCachedRows = !String(searchQuery || "").trim() && skuIdsForSpu.size > 0;
    const cached = variantPickerRowsBySpu[contextMenuVariantSpu] ?? [];
    cached.forEach((row) => {
      const rowId = String(row.id || "").trim();
      if (!rowId) return;
      if (shouldPruneCachedRows && !skuIdsForSpu.has(rowId)) return;
      mergedById.set(rowId, row);
    });
    skuRowsForSpu.forEach((row) => {
      const rowId = String(row.id || "").trim();
      if (!rowId) return;
      mergedById.set(rowId, row);
    });
    return Array.from(mergedById.values()).sort((left, right) => {
      const leftSku = String(left.draft_sku || "").trim();
      const rightSku = String(right.draft_sku || "").trim();
      return leftSku.localeCompare(rightSku);
    });
  }, [contextMenuVariantSpu, searchQuery, skuRows, variantPickerRowsBySpu]);
  const contextMenuVariantRowsById = useMemo(() => {
    const next = new Map<string, DraftSkuRow>();
    contextMenuVariantRows.forEach((row) => {
      const rowId = String(row.id || "").trim();
      if (!rowId) return;
      next.set(rowId, row);
    });
    return next;
  }, [contextMenuVariantRows]);
  const contextMenuVariantGroups = useMemo(() => {
    const byId = new Map<
      string,
      { id: string; label: string; rowIds: string[]; skuHint: string }
    >();
    contextMenuVariantRows.forEach((row) => {
      const rowId = String(row.id || "").trim();
      if (!rowId) return;
      const groupId = buildVariantAssignmentGroupIdFromRow(row) || `row:${rowId}`;
      const sku = String(row.draft_sku || "").trim();
      const existing = byId.get(groupId);
      if (!existing) {
        byId.set(groupId, {
          id: groupId,
          label: buildVariantAssignmentGroupLabelFromRow(row) || "-",
          rowIds: [rowId],
          skuHint: sku,
        });
        return;
      }
      if (!existing.rowIds.includes(rowId)) {
        existing.rowIds.push(rowId);
      }
      if (!existing.skuHint && sku) {
        existing.skuHint = sku;
      }
    });

    return Array.from(byId.values()).sort((left, right) => {
      const labelCompare = left.label.localeCompare(right.label, undefined, {
        sensitivity: "base",
        numeric: true,
      });
      if (labelCompare !== 0) return labelCompare;
      return left.skuHint.localeCompare(right.skuHint, undefined, {
        sensitivity: "base",
        numeric: true,
      });
    });
  }, [contextMenuVariantRows]);
  const contextMenuVariantGroupsById = useMemo(() => {
    const next = new Map<string, { id: string; label: string; rowIds: string[]; skuHint: string }>();
    contextMenuVariantGroups.forEach((group) => {
      if (!group.id) return;
      next.set(group.id, group);
    });
    return next;
  }, [contextMenuVariantGroups]);
  const contextMenuVariantInferredRowIds = useMemo(() => {
    if (!contextMenuVariantImageIsTaggedVar) return new Set<string>();
    if (!contextMenuVariantImageFileName) return new Set<string>();
    if (!contextMenuVariantSpu) return new Set<string>();
    const inferred = new Set<string>();
    contextMenuVariantRows.forEach((row) => {
      const rowId = String(row.id || "").trim();
      if (!rowId) return;
      const explicitFileName = extractVariantImageFileName(row.draft_variant_image_url);
      if (normalizeVariantImageFileNameKey(explicitFileName)) return;
      if (
        doesVariantRowLikelyMatchImageFileName(
          row,
          contextMenuVariantImageFileName,
          contextMenuVariantSpu
        )
      ) {
        inferred.add(rowId);
      }
    });
    return inferred;
  }, [
    contextMenuVariantImageFileName,
    contextMenuVariantImageIsTaggedVar,
    contextMenuVariantRows,
    contextMenuVariantSpu,
  ]);
  const fetchVariantPickerRowsForSpu = useCallback(
    async (spuRaw: string) => {
      const spu = String(spuRaw || "").trim().toUpperCase();
      if (!spu) return [] as DraftSkuRow[];
      const url = new URL("/api/drafts/variants", window.location.origin);
      url.searchParams.set("spu", spu);
      const runFilter = String(selectedFolder || "").trim();
      if (runFilter) {
        url.searchParams.set("run", runFilter);
      }
      const response = await fetch(url.toString());
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        throw new Error(payload?.error || "Unable to load variants for this product.");
      }
      const items = Array.isArray(payload?.items)
        ? (payload.items as DraftSkuRow[]).filter((row) => Boolean(String(row?.id || "").trim()))
        : [];
      return items;
    },
    [selectedFolder]
  );
  useEffect(() => {
    setVariantPickerRowsBySpu({});
  }, [selectedFolder]);
  useEffect(() => {
    if (contextMenuSubmenu !== "assign-variant") return;
    if (!contextMenuVariantSpu) return;
    let cancelled = false;
    setContextMenuVariantRowsLoading(true);
    setContextMenuVariantRowsRefreshing(
      (variantPickerRowsBySpu[contextMenuVariantSpu] ?? []).length > 0
    );
    setContextMenuVariantRowsError(null);
    const run = async () => {
      try {
        const items = await fetchVariantPickerRowsForSpu(contextMenuVariantSpu);
        if (cancelled) return;
        setVariantPickerRowsBySpu((prev) => ({
          ...prev,
          [contextMenuVariantSpu]: items,
        }));
      } catch (err) {
        if (cancelled) return;
        setContextMenuVariantRowsError((err as Error).message);
      } finally {
        if (!cancelled) {
          setContextMenuVariantRowsLoading(false);
          setContextMenuVariantRowsRefreshing(false);
        }
      }
    };
    void run();
    return () => {
      cancelled = true;
    };
  }, [
    contextMenuSubmenu,
    contextMenuVariantSpu,
    fetchVariantPickerRowsForSpu,
  ]);
  useEffect(() => {
    const spu = String(currentImageSpuForDraftFilter || "").trim().toUpperCase();
    if (!spu) return;
    let cancelled = false;
    const run = async () => {
      try {
        const items = await fetchVariantPickerRowsForSpu(spu);
        if (cancelled) return;
        setVariantPickerRowsBySpu((prev) => ({
          ...prev,
          [spu]: items,
        }));
      } catch {
        // Keep current cache if refresh fails.
      }
    };
    void run();
    return () => {
      cancelled = true;
    };
  }, [currentImageSpuForDraftFilter, fetchVariantPickerRowsForSpu]);
  const autoVariantRowsForCurrentSpu = useMemo(() => {
    const spu = String(currentImageSpuForDraftFilter || "").trim().toUpperCase();
    if (!spu) return [] as DraftSkuRow[];
    const mergedById = new Map<string, DraftSkuRow>();
    const skuRowsForSpu = skuRows.filter(
      (row) => String(row.draft_spu || "").trim().toUpperCase() === spu
    );
    const skuIdsForSpu = new Set(
      skuRowsForSpu
        .map((row) => String(row.id || "").trim())
        .filter((rowId) => Boolean(rowId))
    );
    const shouldPruneCachedRows = !String(searchQuery || "").trim() && skuIdsForSpu.size > 0;
    const cachedRows = variantPickerRowsBySpu[spu] ?? [];
    cachedRows.forEach((row) => {
      const rowId = String(row.id || "").trim();
      if (!rowId) return;
      if (shouldPruneCachedRows && !skuIdsForSpu.has(rowId)) return;
      mergedById.set(rowId, row);
    });
    skuRowsForSpu.forEach((row) => {
      const rowId = String(row.id || "").trim();
      if (!rowId) return;
      mergedById.set(rowId, row);
    });
    return Array.from(mergedById.values());
  }, [currentImageSpuForDraftFilter, searchQuery, skuRows, variantPickerRowsBySpu]);
  const autoVariantImageFileNamesForCurrentSpu = useMemo(() => {
    const uniqueByKey = new Map<string, string>();
    [...entries, ...mainViewVariantImageEntries].forEach((entry) => {
      if (entry.type !== "file" || !isImage(entry.name)) return;
      const fileName = extractFileNameFromPath(entry.path || entry.name);
      if (!fileName || fileName.startsWith(".")) return;
      const key = normalizeVariantImageFileNameKey(fileName);
      if (!key) return;
      if (!uniqueByKey.has(key)) {
        uniqueByKey.set(key, fileName);
      }
    });
    return Array.from(uniqueByKey.values());
  }, [entries, isImage, mainViewVariantImageEntries]);
  const applyVariantImageAssignmentToLocalState = useCallback(
    (spu: string, variantIdsRaw: string[], nextImageValue: string | null) => {
      const normalizedVariantIds = Array.from(
        new Set(
          variantIdsRaw
            .map((value) => String(value || "").trim())
            .filter((value) => Boolean(value))
        )
      );
      if (normalizedVariantIds.length === 0) return;
      const variantIdSet = new Set(normalizedVariantIds);
      setSkuRows((prev) =>
        prev.map((row) =>
          variantIdSet.has(String(row.id || "").trim())
            ? { ...row, draft_variant_image_url: nextImageValue }
            : row
        )
      );
      setVariantPickerRowsBySpu((prev) => {
        if (!spu) return prev;
        const existing = prev[spu];
        if (!existing || existing.length === 0) return prev;
        let changed = false;
        const nextRows = existing.map((row) => {
          if (!variantIdSet.has(String(row.id || "").trim())) return row;
          changed = true;
          return { ...row, draft_variant_image_url: nextImageValue };
        });
        if (!changed) return prev;
        return {
          ...prev,
          [spu]: nextRows,
        };
      });
    },
    []
  );
  const handleToggleContextMenuVariantAssignment = useCallback(
    async (assignmentIdRaw: string) => {
      const assignmentId = String(assignmentIdRaw || "").trim();
      if (!assignmentId) return;
      if (!contextMenuVariantSpu || !contextMenuVariantImageFileName) return;

      const targetGroup = contextMenuVariantGroupsById.get(assignmentId);
      const targetVariantIds = targetGroup
        ? targetGroup.rowIds
        : contextMenuVariantRowsById.has(assignmentId)
          ? [assignmentId]
          : [];
      if (targetVariantIds.length === 0) {
        setError("Variant row not found.");
        return;
      }

      const targetRows = targetVariantIds
        .map((rowId) => contextMenuVariantRowsById.get(rowId))
        .filter((row): row is DraftSkuRow => Boolean(row));
      if (targetRows.length === 0) {
        setError("Variant row not found.");
        return;
      }

      const isFullyAssigned = targetRows.every((row) => {
        const currentRefFileName = extractVariantImageFileName(row.draft_variant_image_url);
        const currentRefKey = normalizeVariantImageFileNameKey(currentRefFileName);
        return Boolean(currentRefKey) && currentRefKey === contextMenuVariantImageFileNameKey;
      });
      const nextImageValue =
        isFullyAssigned
          ? null
          : contextMenuVariantImageFileName;
      const previousValuesById = new Map<string, string | null>(
        targetRows.map((row) => [String(row.id || "").trim(), row.draft_variant_image_url ?? null])
      );
      setContextMenuVariantSavingIds((prev) => {
        const next = new Set(prev);
        next.add(assignmentId);
        return next;
      });
      try {
        applyVariantImageAssignmentToLocalState(
          contextMenuVariantSpu,
          targetVariantIds,
          nextImageValue
        );

        const failures: string[] = [];
        let successCount = 0;
        for (const variantId of targetVariantIds) {
          try {
            const response = await fetch("/api/drafts/variants/update", {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                id: variantId,
                field: "draft_variant_image_url",
                value: nextImageValue,
              }),
            });
            const payload = await response.json().catch(() => ({}));
            if (!response.ok) {
              throw new Error(payload?.error || "Unable to update variant image mapping.");
            }
            successCount += 1;
          } catch (err) {
            const previousValue = previousValuesById.get(variantId) ?? null;
            applyVariantImageAssignmentToLocalState(
              contextMenuVariantSpu,
              [variantId],
              previousValue
            );
            failures.push((err as Error).message);
          }
        }

        if (successCount > 0) {
          try {
            const refreshed = await fetchVariantPickerRowsForSpu(contextMenuVariantSpu);
            setVariantPickerRowsBySpu((prev) => ({
              ...prev,
              [contextMenuVariantSpu]: refreshed,
            }));
          } catch {
            // Keep optimistic update if refresh fails.
          }
        }

        if (failures.length > 0) {
          setError(
            `Variant mapping updated with ${failures.length} issue(s): ${failures
              .slice(0, 2)
              .join("; ")}${failures.length > 2 ? "..." : ""}`
          );
        }
      } finally {
        setContextMenuVariantSavingIds((prev) => {
          if (!prev.has(assignmentId)) return prev;
          const next = new Set(prev);
          next.delete(assignmentId);
          return next;
        });
      }
    },
    [
      applyVariantImageAssignmentToLocalState,
      contextMenuVariantImageFileName,
      contextMenuVariantImageFileNameKey,
      contextMenuVariantGroupsById,
      contextMenuVariantRowsById,
      contextMenuVariantSpu,
      fetchVariantPickerRowsForSpu,
    ]
  );
  useEffect(() => {
    if (!ENABLE_VARIANT_IMAGE_AUTO_INFER) return;
    const spu = String(currentImageSpuForDraftFilter || "").trim().toUpperCase();
    if (!spu) return;
    if (autoVariantRowsForCurrentSpu.length === 0) return;
    if (autoVariantImageFileNamesForCurrentSpu.length === 0) return;
    if (autoVariantLinkInFlightSpusRef.current.has(spu)) return;

    const rowById = new Map<string, DraftSkuRow>();
    const assignedImageFileNameKeys = new Set<string>();
    const rowMatchCandidates: Array<{ rowId: string; candidates: string[] }> = [];

    autoVariantRowsForCurrentSpu.forEach((row) => {
      const rowId = String(row.id || "").trim();
      if (!rowId) return;
      rowById.set(rowId, row);
      const explicitFileName = extractVariantImageFileName(row.draft_variant_image_url);
      const explicitKey = normalizeVariantImageFileNameKey(explicitFileName);
      if (explicitKey) {
        assignedImageFileNameKeys.add(explicitKey);
        return;
      }
      const candidates = buildVariantComparableCandidatesFromRow(row);
      if (candidates.length === 0) return;
      rowMatchCandidates.push({ rowId, candidates });
    });

    if (rowMatchCandidates.length === 0) return;

    const imageCandidates = autoVariantImageFileNamesForCurrentSpu
      .map((fileName) => {
        const fileNameKey = normalizeVariantImageFileNameKey(fileName);
        if (!fileNameKey || assignedImageFileNameKeys.has(fileNameKey)) return null;
        const label = extractVariantLabelFromFilename(fileName, spu);
        if (!label || !hasHanCharacters(label)) return null;
        const comparable = normalizeVariantComparableValue(label);
        if (comparable.length < 2) return null;
        return { fileName, fileNameKey, comparable };
      })
      .filter(
        (
          candidate
        ): candidate is { fileName: string; fileNameKey: string; comparable: string } =>
          Boolean(candidate)
      );

    if (imageCandidates.length === 0) return;

    const candidateAssignments: Array<{ rowId: string; fileName: string; fileNameKey: string }> =
      [];
    imageCandidates.forEach((imageCandidate) => {
      const exactMatches = new Set<string>();
      const fuzzyMatches = new Set<string>();
      rowMatchCandidates.forEach((rowCandidate) => {
        if (rowCandidate.candidates.some((candidate) => candidate === imageCandidate.comparable)) {
          exactMatches.add(rowCandidate.rowId);
          return;
        }
        if (
          rowCandidate.candidates.some(
            (candidate) =>
              candidate.length >= 2 &&
              (imageCandidate.comparable.includes(candidate) ||
                candidate.includes(imageCandidate.comparable))
          )
        ) {
          fuzzyMatches.add(rowCandidate.rowId);
        }
      });
      const matches = exactMatches.size > 0 ? exactMatches : fuzzyMatches;
      if (matches.size !== 1) return;
      candidateAssignments.push({
        rowId: Array.from(matches)[0],
        fileName: imageCandidate.fileName,
        fileNameKey: imageCandidate.fileNameKey,
      });
    });

    if (candidateAssignments.length === 0) return;

    const rowHitCount = new Map<string, number>();
    candidateAssignments.forEach((item) => {
      rowHitCount.set(item.rowId, (rowHitCount.get(item.rowId) || 0) + 1);
    });
    const updates = candidateAssignments.filter(
      (item) => (rowHitCount.get(item.rowId) || 0) === 1
    );
    if (updates.length === 0) return;

    const signature = `${spu}::${updates
      .map((item) => `${item.rowId}:${item.fileNameKey}`)
      .sort()
      .join("|")}`;
    if (autoVariantLinkAttemptedSignaturesRef.current.has(signature)) return;
    autoVariantLinkAttemptedSignaturesRef.current.add(signature);
    const inFlightSpus = autoVariantLinkInFlightSpusRef.current;
    inFlightSpus.add(spu);

    let cancelled = false;
    const run = async () => {
      const failures: string[] = [];
      let successfulUpdates = 0;
      for (const update of updates) {
        if (cancelled) break;
        const currentRow = rowById.get(update.rowId);
        const previousValue = currentRow?.draft_variant_image_url ?? null;
        applyVariantImageAssignmentToLocalState(spu, [update.rowId], update.fileName);
        try {
          const response = await fetch("/api/drafts/variants/update", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({
              id: update.rowId,
              field: "draft_variant_image_url",
              value: update.fileName,
            }),
          });
          const payload = await response.json().catch(() => ({}));
          if (!response.ok) {
            throw new Error(payload?.error || "Unable to update variant image mapping.");
          }
          successfulUpdates += 1;
        } catch (err) {
          applyVariantImageAssignmentToLocalState(spu, [update.rowId], previousValue);
          failures.push((err as Error).message);
        }
      }

      if (!cancelled && successfulUpdates > 0) {
        try {
          const refreshed = await fetchVariantPickerRowsForSpu(spu);
          if (!cancelled) {
            setVariantPickerRowsBySpu((prev) => ({
              ...prev,
              [spu]: refreshed,
            }));
          }
        } catch {
          // Keep optimistic local state if refresh fails.
        }
      }

      if (!cancelled && failures.length > 0) {
        setError(
          `Auto-assign variant images finished with ${failures.length} issue(s).`
        );
      }
      inFlightSpus.delete(spu);
    };

    void run();
    return () => {
      cancelled = true;
      inFlightSpus.delete(spu);
    };
  }, [
    applyVariantImageAssignmentToLocalState,
    autoVariantImageFileNamesForCurrentSpu,
    autoVariantRowsForCurrentSpu,
    currentImageSpuForDraftFilter,
    fetchVariantPickerRowsForSpu,
  ]);
  useEffect(() => {
    if (!ENABLE_VARIANT_IMAGE_AUTO_INFER) return;
    if (contextMenuSubmenu !== "assign-variant") return;
    if (!contextMenuVariantImageIsTaggedVar) return;
    if (!contextMenuVariantImageFileNameKey) return;
    if (contextMenuVariantRowsLoading || Boolean(contextMenuVariantRowsError)) return;
    if (contextMenuVariantInferredRowIds.size !== 1) return;
    const inferredVariantId = Array.from(contextMenuVariantInferredRowIds)[0];
    if (!inferredVariantId) return;
    if (contextMenuVariantSavingIds.has(inferredVariantId)) return;
    const inferredRow = contextMenuVariantRowsById.get(inferredVariantId);
    if (!inferredRow) return;
    const currentFileName = extractVariantImageFileName(inferredRow.draft_variant_image_url);
    const currentKey = normalizeVariantImageFileNameKey(currentFileName);
    if (currentKey === contextMenuVariantImageFileNameKey) return;
    if (currentKey) return;
    void handleToggleContextMenuVariantAssignment(inferredVariantId);
  }, [
    contextMenuSubmenu,
    contextMenuVariantImageFileNameKey,
    contextMenuVariantImageIsTaggedVar,
    contextMenuVariantInferredRowIds,
    contextMenuVariantRowsById,
    contextMenuVariantRowsError,
    contextMenuVariantRowsLoading,
    contextMenuVariantSavingIds,
    handleToggleContextMenuVariantAssignment,
  ]);
  const contextMenuMoveToMainPath = useMemo(() => {
    if (!contextMenu?.image) return "";
    const targets = resolveContextActionTargets(contextMenu.entry, {
      imageOnly: true,
    });
    const anchorPath = String(targets[0]?.path || contextMenu.entry.path || "").trim();
    const parts = anchorPath.split("/").filter(Boolean);
    if (parts.length < 2) return "";
    return `${parts[0]}/${parts[1]}`;
  }, [contextMenu, resolveContextActionTargets]);
  const contextMenuMoveToMainSourcePaths = useMemo(() => {
    if (!contextMenu?.image || !contextMenuMoveToMainPath) {
      return [] as string[];
    }
    const targets = resolveContextActionTargets(contextMenu.entry, {
      imageOnly: true,
    });
    if (targets.length === 0) return [] as string[];
    return Array.from(
      new Set(
        targets
          .map((sourceEntry) => String(sourceEntry.path || "").trim())
          .filter((pathValue) => Boolean(pathValue))
          .filter((pathValue) => {
            const slashIndex = pathValue.lastIndexOf("/");
            if (slashIndex <= 0) return false;
            const parentPath = pathValue.slice(0, slashIndex);
            return parentPath !== contextMenuMoveToMainPath;
          })
      )
    );
  }, [contextMenu, contextMenuMoveToMainPath, resolveContextActionTargets]);
  const contextMenuMoveToMainDisabled =
    movingEntry || contextMenuMoveToMainSourcePaths.length === 0;

  const handleContextMenuAction = (action: string) => {
    if (!contextMenu) return;
    const { entry } = contextMenu;
    if (action.startsWith("tag-image-toggle:")) {
      const tag = action.slice("tag-image-toggle:".length).trim().toUpperCase();
      if (
        tag === "MAIN" ||
        tag === "ENV" ||
        tag === "VAR" ||
        tag === "INF" ||
        tag === "DIGI"
      ) {
        const targets = resolveContextActionTargets(entry, { imageOnly: true });
        restoreSelectionAfterContextAction(targets);
        void handleToggleImageTag(targets, tag);
      }
      return;
    }
    if (action.startsWith("assign-variant-toggle:")) {
      const encodedVariantId = action.slice("assign-variant-toggle:".length).trim();
      let variantId = encodedVariantId;
      try {
        variantId = decodeURIComponent(encodedVariantId);
      } catch {
        variantId = encodedVariantId;
      }
      void handleToggleContextMenuVariantAssignment(variantId);
      return;
    }
    if (action.startsWith("assign-variant-group-toggle:")) {
      const encodedGroupId = action
        .slice("assign-variant-group-toggle:".length)
        .trim();
      let groupId = encodedGroupId;
      try {
        groupId = decodeURIComponent(encodedGroupId);
      } catch {
        groupId = encodedGroupId;
      }
      void handleToggleContextMenuVariantAssignment(groupId);
      return;
    }
    setContextMenu(null);
    setContextMenuSubmenu(null);
    setContextMenuNestedSubmenu(null);
    if (action.startsWith("tag-image:")) {
      const tag = action.slice("tag-image:".length).trim().toUpperCase();
      if (
        tag === "MAIN" ||
        tag === "ENV" ||
        tag === "VAR" ||
        tag === "INF" ||
        tag === "DIGI"
      ) {
        const targets = resolveContextActionTargets(entry, { imageOnly: true });
        restoreSelectionAfterContextAction(targets);
        void handleApplyImageTag(targets, tag);
        return;
      }
    }
    if (action === "open") {
      clearSelectedFilesForImageActions();
      openEntry(entry);
      return;
    }
    if (action === "google-lens-search" || action.startsWith("google-lens-search:")) {
      clearSelectedFilesForImageActions();
      if (entry.type === "file" && isImage(entry.name)) {
        const localeRaw =
          action === "google-lens-search"
            ? "sweden"
            : action.slice("google-lens-search:".length).trim().toLowerCase();
        const locale: ReverseSearchLocale =
          localeRaw === "us_global" || localeRaw === "us" || localeRaw === "usa"
            ? "us_global"
            : "sweden";
        void handleRunGoogleReverseSearch(entry, locale);
      }
      return;
    }
    if (action === "move-to-main") {
      if (!contextMenuMoveToMainPath) {
        setError("Main folder could not be resolved for this product.");
        return;
      }
      if (contextMenuMoveToMainSourcePaths.length === 0) {
        setError("Selected image(s) are already in Main.");
        return;
      }
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      restoreSelectionAfterContextAction(targets);
      void handleMoveEntriesToFolder(
        contextMenuMoveToMainSourcePaths,
        contextMenuMoveToMainPath
      );
      return;
    }
    if (action === "download") {
      clearSelectedFilesForImageActions();
      downloadEntry(entry);
      return;
    }
    if (action === "view") {
      clearSelectedFilesForImageActions();
      handleOpenFileViewer(entry);
      return;
    }
    if (action === "create-copy") {
      const targets = resolveContextActionTargets(entry);
      restoreSelectionAfterContextAction(targets);
      void handleCreateCopiesForEntries(targets);
      return;
    }
    if (action === "copy-to-clipboard") {
      if (entry.type === "file" && isImage(entry.name)) {
        clearSelectedFilesForImageActions();
        void handleCopyImageToClipboard(entry);
      }
      return;
    }
    if (action === "undo-last-change") {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length > 0) {
        restoreSelectionAfterContextAction(targets);
        void handleUndoLastChangeForEntries(targets);
      }
      return;
    }
    if (action === "photopea") {
      clearSelectedFilesForImageActions();
      void openPhotopeaEditor(entry);
      return;
    }
    if (action === "auto-levels") {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length > 0) {
        restoreSelectionAfterContextAction(targets);
        openAutoLevelsForEntries(targets);
      }
      return;
    }
    if (action === "flip-horizontal") {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length > 0) {
        restoreSelectionAfterContextAction(targets);
        void handleFlipHorizontalForEntries(targets);
      } else {
        setError("Select at least one image.");
      }
      return;
    }
    if (action === "create-collage") {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length > 1) {
        restoreSelectionAfterContextAction(targets);
        void handleCreateCollageForEntries(targets);
      } else {
        setError("Select at least two images.");
      }
      return;
    }
    if (action === "stack-overlay") {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length >= STACK_OVERLAY_MIN_IMAGES) {
        restoreSelectionAfterContextAction(targets);
        void handleStackOverlayForEntries(targets);
      } else {
        setError("Select at least two images.");
      }
      return;
    }
    if (action === "stack-images") {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length > 1) {
        restoreSelectionAfterContextAction(targets);
        void handleStackImagesForEntries(targets);
      } else {
        setError("Select at least two images.");
      }
      return;
    }
    if (action === "merge-digideal-main") {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length === 2) {
        restoreSelectionAfterContextAction(targets);
        void handleMergeDigidealMainForEntries(targets);
      } else {
        setError("Select exactly two images.");
      }
      return;
    }
    if (action === "merge-digideal-main-overlay") {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length === 2) {
        restoreSelectionAfterContextAction(targets);
        void handleMergeDigidealMainOverlayForEntries(targets);
      } else {
        setError("Select exactly two images.");
      }
      return;
    }
    if (action === "merge-digideal-main-stack-overlay") {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length >= DIGIDEAL_MAIN_STACK_OVERLAY_MIN_IMAGES) {
        restoreSelectionAfterContextAction(targets);
        void handleMergeDigidealMainStackOverlayForEntries(targets);
      } else {
        setError("Select at least three images.");
      }
      return;
    }
    if (action === "ai-auto-center-white") {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length > 0) {
        restoreSelectionAfterContextAction(targets);
        startAiEditForEntries(targets, "zimage", "auto_center_white");
      }
      return;
    }
    if (action.startsWith("ai-chatgpt-template")) {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length > 0) {
        restoreSelectionAfterContextAction(targets);
        const countRaw = action.includes(":") ? action.split(":").pop() : undefined;
        const outputCount = countRaw ? Number(countRaw) : 1;
        startAiEditForEntries(targets, "chatgpt", "template", {
          outputCount: Number.isFinite(outputCount)
            ? Math.max(1, Math.min(3, Math.floor(outputCount)))
            : 1,
        });
      }
      return;
    }
    if (action.startsWith("ai-chatgpt-digideal-main-dual")) {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length > 0) {
        restoreSelectionAfterContextAction(targets);
        const countRaw = action.includes(":") ? action.split(":").pop() : undefined;
        const outputCount = countRaw ? Number(countRaw) : 1;
        startAiDualTemplatePresetForEntries(
          targets,
          "chatgpt",
          Number.isFinite(outputCount)
            ? Math.max(1, Math.min(3, Math.floor(outputCount)))
            : 1
        );
      }
      return;
    }
    if (
      action.startsWith("ai-chatgpt-digideal-main-single") ||
      action.startsWith("ai-chatgpt-digideal-main")
    ) {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length > 0) {
        restoreSelectionAfterContextAction(targets);
        const countRaw = action.includes(":") ? action.split(":").pop() : undefined;
        const outputCount = countRaw ? Number(countRaw) : 1;
        startAiTemplatePresetForEntries(
          targets,
          "chatgpt",
          "digideal_main",
          Number.isFinite(outputCount)
            ? Math.max(1, Math.min(3, Math.floor(outputCount)))
            : 1
        );
      }
      return;
    }
	    if (action.startsWith("ai-chatgpt-product-scene")) {
	      const targets = resolveContextActionTargets(entry, { imageOnly: true });
	      if (targets.length > 0) {
          restoreSelectionAfterContextAction(targets);
          const countRaw = action.includes(":") ? action.split(":").pop() : undefined;
          const outputCount = countRaw ? Number(countRaw) : 1;
	        startAiTemplatePresetForEntries(
	          targets,
	          "chatgpt",
	          "product_scene",
	          Number.isFinite(outputCount) ? Math.max(1, Math.min(3, Math.floor(outputCount))) : 1
	        );
	      }
	      return;
	    }
    if (action === "ai-chatgpt-product-collection") {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length > 0) {
        restoreSelectionAfterContextAction(targets);
        startAiProductCollectionForEntries(targets, "chatgpt");
      }
      return;
    }
    if (action.startsWith("ai-chatgpt-direct")) {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length > 0) {
        restoreSelectionAfterContextAction(targets);
        const countRaw = action.includes(":") ? action.split(":").pop() : undefined;
        const outputCount = countRaw ? Number(countRaw) : 1;
        startAiEditForEntries(targets, "chatgpt", "direct", {
          outputCount: Number.isFinite(outputCount)
            ? Math.max(1, Math.min(3, Math.floor(outputCount)))
            : 1,
        });
      }
      return;
    }
    if (action.startsWith("ai-gemini-template")) {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length > 0) {
        restoreSelectionAfterContextAction(targets);
        const countRaw = action.includes(":") ? action.split(":").pop() : undefined;
        const outputCount = countRaw ? Number(countRaw) : 1;
        startAiEditForEntries(targets, "gemini", "template", {
          outputCount: Number.isFinite(outputCount)
            ? Math.max(1, Math.min(3, Math.floor(outputCount)))
            : 1,
        });
      }
      return;
    }
    if (action.startsWith("ai-gemini-digideal-main-dual")) {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length > 0) {
        restoreSelectionAfterContextAction(targets);
        const countRaw = action.includes(":") ? action.split(":").pop() : undefined;
        const outputCount = countRaw ? Number(countRaw) : 1;
        startAiDualTemplatePresetForEntries(
          targets,
          "gemini",
          Number.isFinite(outputCount)
            ? Math.max(1, Math.min(3, Math.floor(outputCount)))
            : 1
        );
      }
      return;
    }
    if (
      action.startsWith("ai-gemini-digideal-main-single") ||
      action.startsWith("ai-gemini-digideal-main")
    ) {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length > 0) {
        restoreSelectionAfterContextAction(targets);
        const countRaw = action.includes(":") ? action.split(":").pop() : undefined;
        const outputCount = countRaw ? Number(countRaw) : 1;
        startAiTemplatePresetForEntries(
          targets,
          "gemini",
          "digideal_main",
          Number.isFinite(outputCount)
            ? Math.max(1, Math.min(3, Math.floor(outputCount)))
            : 1
        );
      }
      return;
    }
	    if (action.startsWith("ai-gemini-product-scene")) {
	      const targets = resolveContextActionTargets(entry, { imageOnly: true });
	      if (targets.length > 0) {
          restoreSelectionAfterContextAction(targets);
          const countRaw = action.includes(":") ? action.split(":").pop() : undefined;
          const outputCount = countRaw ? Number(countRaw) : 1;
	        startAiTemplatePresetForEntries(
	          targets,
	          "gemini",
	          "product_scene",
	          Number.isFinite(outputCount) ? Math.max(1, Math.min(3, Math.floor(outputCount))) : 1
	        );
	      }
	      return;
	    }
    if (action === "ai-gemini-product-collection") {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length > 0) {
        restoreSelectionAfterContextAction(targets);
        startAiProductCollectionForEntries(targets, "gemini");
      }
      return;
    }
    if (action.startsWith("ai-gemini-direct")) {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length > 0) {
        restoreSelectionAfterContextAction(targets);
        const countRaw = action.includes(":") ? action.split(":").pop() : undefined;
        const outputCount = countRaw ? Number(countRaw) : 1;
        startAiEditForEntries(targets, "gemini", "direct", {
          outputCount: Number.isFinite(outputCount)
            ? Math.max(1, Math.min(3, Math.floor(outputCount)))
            : 1,
        });
      }
      return;
    }
    if (action === "ai-zimage-white") {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length > 0) {
        restoreSelectionAfterContextAction(targets);
        startAiEditForEntries(targets, "zimage", "white_background");
      }
      return;
    }
    if (action === "ai-zimage-eraser") {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length > 0) {
        restoreSelectionAfterContextAction(targets);
        startAiEditForEntries(targets, "zimage", "eraser");
      }
      return;
    }
    if (action === "ai-zimage-direct") {
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length > 0) {
        restoreSelectionAfterContextAction(targets);
        startAiEditForEntries(targets, "zimage", "direct");
      }
      return;
    }
    if (action === "ai-zimage-upscale") {
      if (contextMenuUpscaleDisabled) {
        setError("Z-image upscale has already been applied to this image.");
        return;
      }
      const targets = resolveContextActionTargets(entry, { imageOnly: true });
      if (targets.length > 0) {
        restoreSelectionAfterContextAction(targets);
        startAiEditForEntries(targets, "zimage", "upscale");
      }
      return;
    }
    if (action === "ai-review" && pendingAiEditsByOriginal[entry.path]) {
      clearSelectedFilesForImageActions();
      setAiReviewOriginalPath(entry.path);
      return;
    }
    setError(`Unknown action "${action}".`);
  };

  const renderFolderTreeNode = (
    node: DraftFolderTreeNode,
    ancestorHasNext: boolean[],
    isLast: boolean
  ) => {
    const isCurrent = currentPath === node.path;
    const isCompleted = completedSpuFolders.has(node.path);
    const isCollapsed = collapsedTreeFolders.has(node.path);
    const isSelected = selectedTreeFolders.has(node.path);
    const isDropTarget = folderDropTargetPath === node.path;
    const hasChildren = node.children.length > 0;

    return (
      <div key={node.path}>
        <div
          className={mergeClasses(
            styles.folderTreeRow,
            isCurrent ? styles.folderTreeRowActive : undefined,
            isCompleted ? styles.folderTreeRowCompleted : undefined,
            isDropTarget ? styles.folderTreeRowDrop : undefined
          )}
          onClick={() => setCurrentPath(node.path)}
          onDragOver={(event) => {
            if (draggingEntryPaths.length === 0) return;
            event.stopPropagation();
            event.preventDefault();
            setFolderDropTargetPath(node.path);
          }}
          onDragLeave={(event) => {
            event.stopPropagation();
            if (folderDropTargetPath === node.path) {
              setFolderDropTargetPath(null);
            }
          }}
          onDrop={(event) => {
            event.stopPropagation();
            event.preventDefault();
            const draggedPaths = readDraggedPaths(event.dataTransfer);
            if (draggedPaths.length === 0) return;
            if (
              draggedPaths.every(
                (draggedPath) =>
                  draggedPath === node.path || node.path.startsWith(`${draggedPath}/`)
              )
            ) {
              setFolderDropTargetPath(null);
              setDraggingEntryPaths([]);
              return;
            }
            handleMoveEntriesToFolder(draggedPaths, node.path);
          }}
        >
          <span className={styles.folderTreeConnector} aria-hidden="true">
            {ancestorHasNext.map((hasNext, index) => (
              <span
                key={`${node.path}-segment-${index}`}
                className={styles.folderTreeConnectorSegment}
              >
                {hasNext ? <span className={styles.folderTreeConnectorLine} /> : null}
              </span>
            ))}
            <span className={styles.folderTreeConnectorJoin}>
              <span
                className={styles.folderTreeConnectorJoinVertical}
                style={isLast ? { top: 0, bottom: "8px" } : { top: 0, bottom: 0 }}
              />
              <span className={styles.folderTreeConnectorJoinHorizontal} />
            </span>
          </span>
          {hasChildren ? (
            <button
              type="button"
              className={styles.folderTreeCaretButton}
              onClick={(event) => {
                event.stopPropagation();
                toggleTreeFolder(node.path);
              }}
              aria-label={isCollapsed ? "Expand folder" : "Collapse folder"}
            >
              {isCollapsed ? "▸" : "▾"}
            </button>
          ) : (
            <span className={styles.folderTreeCaretSpacer} aria-hidden="true" />
          )}
          <input
            type="checkbox"
            checked={isSelected}
            onChange={(event) => {
              event.stopPropagation();
              handleToggleTreeFolder(node.path);
            }}
            onClick={(event) => event.stopPropagation()}
          />
          <button
            type="button"
            className={styles.folderTreeName}
            onClick={(event) => {
              event.stopPropagation();
              setCurrentPath(node.path);
            }}
          >
            <svg
              viewBox="0 0 24 24"
              aria-hidden="true"
              className={styles.folderTreeFolderIcon}
            >
              <path
                fill="currentColor"
                d="M9.5 4h-5A2.5 2.5 0 0 0 2 6.5v11A2.5 2.5 0 0 0 4.5 20h15A2.5 2.5 0 0 0 22 17.5v-9A2.5 2.5 0 0 0 19.5 6H12l-2-2.5A2.5 2.5 0 0 0 9.5 4Z"
              />
            </svg>
            <span className={styles.folderTreeNameText}>{node.name}</span>
            {node.fileCount > 0 ? (
              <span className={styles.folderTreeCount}>({node.fileCount})</span>
            ) : null}
          </button>
        </div>
        {!isCollapsed && hasChildren ? (
          <div className={styles.folderTreeChildren}>
            {node.children.map((child, index) =>
              renderFolderTreeNode(
                child,
                [...ancestorHasNext, !isLast],
                index === node.children.length - 1
              )
            )}
          </div>
        ) : null}
      </div>
    );
  };

  const imageEntries = entries.filter(
    (entry) =>
      entry.type === "file" &&
      isImage(entry.name) &&
      !optimisticHiddenImagePaths.has(entry.path)
  );

  const normalizeFolderToken = useCallback(
    (value: string) => String(value || "").toLowerCase().replace(/[\s_-]+/g, ""),
    []
  );

  const isVariantImagesFolderName = useCallback(
    (name: string) => {
      const normalized = normalizeFolderToken(name);
      if (!normalized) return false;
      if (normalized === "variants" || normalized === "variant") return true;
      if (normalized === "variantimages" || normalized === "variantimage") return true;
      return normalized.includes("variant") && normalized.includes("image");
    },
    [normalizeFolderToken]
  );

  const isOcrImagesFolderName = useCallback(
    (name: string) => {
      const normalized = normalizeFolderToken(name);
      if (!normalized) return false;
      if (normalized === "ocrchinese" || normalized === "ocr") return true;
      return normalized.includes("ocr") && normalized.includes("chinese");
    },
    [normalizeFolderToken]
  );

  const isOtherImagesFolderName = useCallback(
    (name: string) => {
      const normalized = normalizeFolderToken(name);
      if (!normalized) return false;
      if (
        normalized === "other" ||
        normalized === "others" ||
        normalized === "otherimage" ||
        normalized === "otherimages" ||
        normalized === "nonsquare"
      ) {
        return true;
      }
      return normalized.includes("otherimages");
    },
    [normalizeFolderToken]
  );

  const isDownloadedImagesFolderName = useCallback(
    (name: string) => {
      const normalized = normalizeFolderToken(name);
      if (!normalized) return false;
      if (
        normalized === "downloaded" ||
        normalized === "downloadedimages" ||
        normalized === "downloadedimage" ||
        normalized === "downloads"
      ) {
        return true;
      }
      return normalized.includes("downloaded") && normalized.includes("image");
    },
    [normalizeFolderToken]
  );

  const isRejectVariantMismatchFolderName = useCallback(
    (name: string) => {
      const normalized = normalizeFolderToken(name);
      if (!normalized) return false;
      if (normalized.includes("variantmismatch")) return true;
      return (
        normalized.includes("rejectvariant") && normalized.includes("mismatch")
      );
    },
    [normalizeFolderToken]
  );

  const isLegacyOthersFolderName = useCallback(
    (name: string) => {
      const normalized = normalizeFolderToken(name);
      if (!normalized) return false;

      const rejectBadComposite =
        normalized === "rejectbadcomposite" ||
        (normalized.includes("reject") &&
          normalized.includes("bad") &&
          normalized.includes("composite"));

      const rejectNotSuitable =
        normalized === "rejectnotsuitable" ||
        (normalized.includes("reject") && normalized.includes("notsuitable")) ||
        (normalized.includes("reject") &&
          normalized.includes("not") &&
          normalized.includes("suitable"));

      // Rejected variant buckets are intentionally excluded from "Others" tab badges/view.
      return rejectBadComposite || rejectNotSuitable;
    },
    [normalizeFolderToken]
  );

  const imageTabTargets = useMemo(() => {
    if (!currentPath) {
      return {
        mainPath: null as string | null,
        variantsPath: null as string | null,
        ocrPath: null as string | null,
        downloadedPath: null as string | null,
        othersPath: null as string | null,
        othersPaths: [] as string[],
        active: null as ImageFolderTabValue | null,
      };
    }

    const parts = currentPath.split("/").filter(Boolean);
    if (parts.length < 2) {
      return {
        mainPath: null,
        variantsPath: null,
        ocrPath: null,
        downloadedPath: null,
        othersPath: null,
        othersPaths: [] as string[],
        active: null,
      };
    }

    const run = parts[0];
    const top = parts[1];
    if (!run || !top || isChunksDirectory(top)) {
      return {
        mainPath: null,
        variantsPath: null,
        ocrPath: null,
        downloadedPath: null,
        othersPath: null,
        othersPaths: [] as string[],
        active: null,
      };
    }

    const mainPath = `${run}/${top}`;
    let variantsPath: string | null = null;
    let ocrPath: string | null = null;
    let downloadedPath: string | null = null;
    let othersPath: string | null = null;
    let othersPaths: string[] = [];

    const findNodeByPath = (
      node: DraftFolderTreeNode,
      targetPath: string
    ): DraftFolderTreeNode | null => {
      if (node.path === targetPath) return node;
      for (const child of node.children) {
        const hit = findNodeByPath(child, targetPath);
        if (hit) return hit;
      }
      return null;
    };

    const findVariantNode = (
      node: DraftFolderTreeNode
    ): DraftFolderTreeNode | null => {
      const findFirstMatchingNode = (
        predicate: (child: DraftFolderTreeNode) => boolean
      ) => {
        const directMatch = node.children.find((child) => predicate(child));
        if (directMatch) return directMatch;
        const queue = [...node.children];
        while (queue.length > 0) {
          const next = queue.shift();
          if (!next) break;
          if (predicate(next)) return next;
          queue.push(...next.children);
        }
        return null;
      };

      const direct = findFirstMatchingNode((child) =>
        isVariantImagesFolderName(child.name)
      );
      if (direct) return direct;
      const fallback = findFirstMatchingNode((child) =>
        normalizeFolderToken(child.name).includes("variant")
      );
      if (fallback) return fallback;
      return null;
    };

    const findOcrNode = (node: DraftFolderTreeNode): DraftFolderTreeNode | null => {
      const queue = [...node.children];
      while (queue.length > 0) {
        const next = queue.shift();
        if (!next) break;
        if (isOcrImagesFolderName(next.name)) return next;
        queue.push(...next.children);
      }
      return null;
    };

    const findDownloadedNode = (
      node: DraftFolderTreeNode
    ): DraftFolderTreeNode | null => {
      const queue = [...node.children];
      while (queue.length > 0) {
        const next = queue.shift();
        if (!next) break;
        if (isDownloadedImagesFolderName(next.name)) return next;
        queue.push(...next.children);
      }
      return null;
    };

    const findOtherNodes = (
      node: DraftFolderTreeNode
    ): DraftFolderTreeNode[] => {
      const hits: DraftFolderTreeNode[] = [];
      const queue = [...node.children];
      while (queue.length > 0) {
        const next = queue.shift();
        if (!next) break;
        if (
          !isRejectVariantMismatchFolderName(next.name) &&
          (isOtherImagesFolderName(next.name) ||
            isLegacyOthersFolderName(next.name))
        ) {
          hits.push(next);
        }
        queue.push(...next.children);
      }
      return hits;
    };

    if (folderTree) {
      const mainNode = findNodeByPath(folderTree, mainPath);
      if (mainNode) {
        const variantNode = findVariantNode(mainNode);
        variantsPath = variantNode?.path ?? null;
        const ocrNode = findOcrNode(mainNode);
        ocrPath = ocrNode?.path ?? null;
        const downloadedNode = findDownloadedNode(mainNode);
        downloadedPath = downloadedNode?.path ?? null;
        const otherNodes = findOtherNodes(mainNode);
        othersPaths = Array.from(
          new Set(
            otherNodes
              .map((node) => String(node.path || "").trim())
              .filter((value) => Boolean(value))
          )
        );
        othersPath = othersPaths[0] ?? null;
      }
    }

    let active: ImageFolderTabValue | null = null;
    if (
      variantsPath &&
      (currentPath === variantsPath || currentPath.startsWith(`${variantsPath}/`))
    ) {
      active = "variants";
    } else if (
      downloadedPath &&
      (currentPath === downloadedPath || currentPath.startsWith(`${downloadedPath}/`))
    ) {
      active = "downloaded";
    } else if (
      ocrPath &&
      (currentPath === ocrPath || currentPath.startsWith(`${ocrPath}/`))
    ) {
      active = "ocr";
    } else if (
      othersPaths.some(
        (pathValue) =>
          currentPath === pathValue || currentPath.startsWith(`${pathValue}/`)
      )
    ) {
      active = "others";
    } else if (currentPath === mainPath || currentPath.startsWith(`${mainPath}/`)) {
      active = "main";
    }

    return {
      mainPath,
      variantsPath,
      ocrPath,
      downloadedPath,
      othersPath,
      othersPaths,
      active,
    };
  }, [
    currentPath,
    folderTree,
    isDownloadedImagesFolderName,
    isLegacyOthersFolderName,
    isOcrImagesFolderName,
    isOtherImagesFolderName,
    isRejectVariantMismatchFolderName,
    isChunksDirectory,
    isVariantImagesFolderName,
    normalizeFolderToken,
  ]);

  useEffect(() => {
    currentMainPathRef.current = String(imageTabTargets.mainPath || "").trim();
    currentVariantsPathRef.current = String(imageTabTargets.variantsPath || "").trim();
  }, [imageTabTargets.mainPath, imageTabTargets.variantsPath]);

  useEffect(() => {
    const normalizedCurrentPath = String(currentPath || "").trim();
    const normalizedMainPath = String(imageTabTargets.mainPath || "").trim();
    const normalizedVariantsPath = String(imageTabTargets.variantsPath || "").trim();
    if (!normalizedCurrentPath || !normalizedMainPath) return;
    if (normalizedCurrentPath !== normalizedMainPath) return;
    void fetchEntries(normalizedCurrentPath, {
      variantsPath: normalizedVariantsPath || null,
    });
  }, [currentPath, fetchEntries, imageTabTargets.mainPath, imageTabTargets.variantsPath]);

  const ensureSizeChartsForMainPath = useCallback(
    async (mainPath: string) => {
      const normalizedMainPath = String(mainPath || "").trim();
      if (!normalizedMainPath) return;
      if (sizeChartAutoGenerateInFlightRef.current) return;
      if (sizeChartAutoGenerateSignatureRef.current === normalizedMainPath) return;

      sizeChartAutoGenerateSignatureRef.current = normalizedMainPath;
      sizeChartAutoGenerateInFlightRef.current = true;
      try {
        const response = await fetch("/api/drafts/images/size-charts/generate", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ mainPath: normalizedMainPath }),
        });
        if (!response.ok) {
          return;
        }
        const payload = await response.json().catch(() => ({}));
        const generatedCount = Array.isArray(payload?.generated)
          ? payload.generated.length
          : 0;
        if (generatedCount > 0) {
          const currentMainPath = String(currentPathRef.current || "").trim();
          if (currentMainPath === normalizedMainPath) {
            await refreshEntries(normalizedMainPath);
          }
        }
      } catch {
        // Ignore auto-generation failures to keep explorer interaction smooth.
      } finally {
        sizeChartAutoGenerateInFlightRef.current = false;
      }
    },
    [refreshEntries]
  );

  useEffect(() => {
    const normalizedMainPath = String(imageTabTargets.mainPath || "").trim();
    if (!normalizedMainPath) return;
    if (String(currentPath || "").trim() !== normalizedMainPath) return;
    void ensureSizeChartsForMainPath(normalizedMainPath);
  }, [currentPath, ensureSizeChartsForMainPath, imageTabTargets.mainPath]);

  const countImagesInEntryList = useCallback(
    (items: DraftEntry[], predicate?: (item: DraftEntry) => boolean) =>
      items.reduce((count, item) => {
        if (item.type !== "file" || !isImage(item.name)) return count;
        if (optimisticHiddenImagePaths.has(item.path)) return count;
        if (predicate && !predicate(item)) return count;
        return count + 1;
      }, 0),
    [isImage, optimisticHiddenImagePaths]
  );

  const countImagesForPath = useCallback(
    async (
      pathValue: string | null,
      predicate?: (item: DraftEntry) => boolean
    ) => {
      const normalizedPath = String(pathValue || "").trim();
      if (!normalizedPath) return 0;
      if (normalizedPath === currentPath) {
        return countImagesInEntryList(entries, predicate);
      }
      try {
        const items = await listPathEntries(normalizedPath);
        return countImagesInEntryList(items, predicate);
      } catch {
        return 0;
      }
    },
    [countImagesInEntryList, currentPath, entries, listPathEntries]
  );

  const countSizeChartImagesForPaths = useCallback(
    async (mainPathValue: string | null, variantsPathValue: string | null) => {
      const normalizedMainPath = String(mainPathValue || "").trim();
      const normalizedVariantsPath = String(variantsPathValue || "").trim();
      const uniquePaths = Array.from(
        new Set([normalizedMainPath, normalizedVariantsPath].filter(Boolean))
      );
      if (uniquePaths.length === 0) return 0;

      const seenImagePaths = new Set<string>();
      let count = 0;

      for (const pathValue of uniquePaths) {
        let items: DraftEntry[] = [];
        if (pathValue === currentPath) {
          items = entries;
        } else {
          try {
            items = await listPathEntries(pathValue);
          } catch {
            continue;
          }
        }

        items.forEach((entry) => {
          if (entry.type !== "file" || !isImage(entry.name)) return;
          const entryPath = String(entry.path || "").trim();
          if (!entryPath || seenImagePaths.has(entryPath)) return;
          if (optimisticHiddenImagePaths.has(entryPath)) return;
          if (!isSizeChartImageName(entry.name)) return;
          seenImagePaths.add(entryPath);
          count += 1;
        });
      }

      return count;
    },
    [currentPath, entries, isImage, listPathEntries, optimisticHiddenImagePaths]
  );

  const countVariantImagesForPaths = useCallback(
    async (
      mainPathValue: string | null,
      variantsPathValue: string | null,
      assignedFileNameKeys: Set<string>
    ) => {
      const normalizedMainPath = String(mainPathValue || "").trim();
      const uniquePaths = Array.from(
        new Set([normalizedMainPath, String(variantsPathValue || "").trim()].filter(Boolean))
      );
      if (uniquePaths.length === 0) return 0;

      const seenImagePaths = new Set<string>();
      let count = 0;

      for (const pathValue of uniquePaths) {
        let items: DraftEntry[] = [];
        if (pathValue === currentPath) {
          items = entries;
        } else {
          try {
            items = await listPathEntries(pathValue);
          } catch {
            continue;
          }
        }

        items.forEach((entry) => {
          if (entry.type !== "file" || !isImage(entry.name)) return;
          const entryPath = String(entry.path || "").trim();
          if (!entryPath || seenImagePaths.has(entryPath)) return;
          if (optimisticHiddenImagePaths.has(entryPath)) return;

          const fileTags = extractImageTagsFromFileName(entry.name);
          const fileName = extractFileNameFromPath(entry.path || entry.name);
          const fileNameKey = normalizeVariantImageFileNameKey(fileName);
          const hasVariantAssignment = fileNameKey
            ? assignedFileNameKeys.has(fileNameKey)
            : false;
          if (!(fileTags.includes("VAR") || hasVariantAssignment)) {
            return;
          }

          seenImagePaths.add(entryPath);
          count += 1;
        });
      }

      return count;
    },
    [currentPath, entries, isImage, listPathEntries, optimisticHiddenImagePaths]
  );

  useEffect(() => {
    let cancelled = false;

    const loadImageTabCounts = async () => {
      const normalizedSpu = String(currentImageSpuForDraftFilter || "")
        .trim()
        .toUpperCase();
      const variantAssignmentKeys = new Set<string>();
      if (normalizedSpu) {
        const mergedById = new Map<string, DraftSkuRow>();
        const skuRowsForSpu = skuRows.filter(
          (row) => String(row.draft_spu || "").trim().toUpperCase() === normalizedSpu
        );
        const skuIdsForSpu = new Set(
          skuRowsForSpu
            .map((row) => String(row.id || "").trim())
            .filter((rowId) => Boolean(rowId))
        );
        const shouldPruneCachedRows =
          !String(searchQuery || "").trim() && skuIdsForSpu.size > 0;
        const cachedRows = variantPickerRowsBySpu[normalizedSpu] ?? [];
        cachedRows.forEach((row) => {
          const rowId = String(row.id || "").trim();
          if (!rowId) return;
          if (shouldPruneCachedRows && !skuIdsForSpu.has(rowId)) return;
          mergedById.set(rowId, row);
        });
        skuRowsForSpu.forEach((row) => {
          const rowId = String(row.id || "").trim();
          if (!rowId) return;
          mergedById.set(rowId, row);
        });
        mergedById.forEach((row) => {
          const fileName = extractVariantImageFileName(row.draft_variant_image_url);
          const key = normalizeVariantImageFileNameKey(fileName);
          if (!key) return;
          variantAssignmentKeys.add(key);
        });
      }

      const uniqueOthersPaths = Array.from(
        new Set(
          imageTabTargets.othersPaths
            .map((pathValue) => String(pathValue || "").trim())
            .filter(Boolean)
        )
      );

      const [
        mainDirectCount,
        variantsCount,
        ocrCount,
        downloadedCount,
        othersCounts,
        variantCount,
        sizeChartsCount,
      ] =
        await Promise.all([
          countImagesForPath(imageTabTargets.mainPath),
          countImagesForPath(imageTabTargets.variantsPath),
          countImagesForPath(imageTabTargets.ocrPath),
          countImagesForPath(imageTabTargets.downloadedPath),
          Promise.all(uniqueOthersPaths.map((pathValue) => countImagesForPath(pathValue))),
          countVariantImagesForPaths(
            imageTabTargets.mainPath,
            imageTabTargets.variantsPath,
            variantAssignmentKeys
          ),
          countSizeChartImagesForPaths(
            imageTabTargets.mainPath,
            imageTabTargets.variantsPath
          ),
        ]);

      if (cancelled) return;

      const othersCount = othersCounts.reduce((total, value) => total + value, 0);
      setImageTabImageCounts({
        // Main view intentionally includes merged variant images, excluding SIZE charts.
        main: Math.max(0, mainDirectCount + variantsCount - sizeChartsCount),
        variants: variantCount,
        sizecharts: sizeChartsCount,
        ocr: ocrCount,
        downloaded: downloadedCount,
        others: othersCount,
      });
    };

    void loadImageTabCounts();
    return () => {
      cancelled = true;
    };
  }, [
    countImagesForPath,
    countSizeChartImagesForPaths,
    countVariantImagesForPaths,
    currentImageSpuForDraftFilter,
    imageTabTargets.mainPath,
    imageTabTargets.downloadedPath,
    imageTabTargets.ocrPath,
    imageTabTargets.othersPaths,
    imageTabTargets.variantsPath,
    searchQuery,
    skuRows,
    variantPickerRowsBySpu,
  ]);

  const mainViewShowsMergedImages =
    Boolean(imageTabTargets.mainPath) && currentPath === imageTabTargets.mainPath;

  useEffect(() => {
    if (mainImageViewFilter === "all") return;
    if (!mainViewShowsMergedImages) {
      setMainImageViewFilter("all");
    }
  }, [mainImageViewFilter, mainViewShowsMergedImages]);

  const selectedImageTabValue: ImageFolderTabValue = useMemo(() => {
    if (mainViewShowsMergedImages && mainImageViewFilter === "var_only") {
      return "variants";
    }
    if (mainViewShowsMergedImages && mainImageViewFilter === "size_only") {
      return "sizecharts";
    }
    return imageTabTargets.active ?? "main";
  }, [imageTabTargets.active, mainImageViewFilter, mainViewShowsMergedImages]);

  useEffect(() => {
    const variantsPath = String(imageTabTargets.variantsPath || "").trim();
    const mainPath = String(imageTabTargets.mainPath || "").trim();
    const normalizedCurrentPath = String(currentPath || "").trim();
    if (!variantsPath || !mainPath || !normalizedCurrentPath) return;
    const isInsideVariantsPath =
      normalizedCurrentPath === variantsPath ||
      normalizedCurrentPath.startsWith(`${variantsPath}/`);
    if (!isInsideVariantsPath) return;

    setMainImageViewFilter("var_only");
    if (normalizedCurrentPath !== mainPath) {
      setCurrentPath(mainPath);
    }
  }, [currentPath, imageTabTargets.mainPath, imageTabTargets.variantsPath]);

  useEffect(() => {
    const requestedImageTab = initialUrlImageTabPendingRef.current;
    if (!requestedImageTab) return;

    if (requestedImageTab === "variants") {
      const mainPath = String(imageTabTargets.mainPath || "").trim();
      if (!mainPath) return;
      setMainImageViewFilter("var_only");
      if (String(currentPath || "").trim() !== mainPath) {
        setCurrentPath(mainPath);
      }
      initialUrlImageTabPendingRef.current = "";
      return;
    }

    if (requestedImageTab === "sizecharts") {
      const mainPath = String(imageTabTargets.mainPath || "").trim();
      if (!mainPath) return;
      setMainImageViewFilter("size_only");
      if (String(currentPath || "").trim() !== mainPath) {
        setCurrentPath(mainPath);
      }
      initialUrlImageTabPendingRef.current = "";
      return;
    }

    if (requestedImageTab === "main") {
      const mainPath = String(imageTabTargets.mainPath || "").trim();
      if (!mainPath) return;
      setMainImageViewFilter("all");
      if (String(currentPath || "").trim() !== mainPath) {
        setCurrentPath(mainPath);
      }
      initialUrlImageTabPendingRef.current = "";
      return;
    }

    if (requestedImageTab === "others") {
      const othersPath = String(imageTabTargets.othersPaths[0] || "").trim();
      if (!othersPath) return;
      setMainImageViewFilter("all");
      if (String(currentPath || "").trim() !== othersPath) {
        setCurrentPath(othersPath);
      }
      initialUrlImageTabPendingRef.current = "";
      return;
    }

    if (requestedImageTab === "ocr") {
      const ocrPath = String(imageTabTargets.ocrPath || "").trim();
      if (!ocrPath) return;
      setMainImageViewFilter("all");
      if (String(currentPath || "").trim() !== ocrPath) {
        setCurrentPath(ocrPath);
      }
      initialUrlImageTabPendingRef.current = "";
      return;
    }

    if (requestedImageTab === "downloaded") {
      const downloadedPath = String(imageTabTargets.downloadedPath || "").trim();
      if (!downloadedPath) return;
      setMainImageViewFilter("all");
      if (String(currentPath || "").trim() !== downloadedPath) {
        setCurrentPath(downloadedPath);
      }
      initialUrlImageTabPendingRef.current = "";
      return;
    }
  }, [
    currentPath,
    imageTabTargets.downloadedPath,
    imageTabTargets.mainPath,
    imageTabTargets.ocrPath,
    imageTabTargets.othersPaths,
  ]);

  useEffect(() => {
    const pathValue = String(currentPath || "").trim();
    if (!pathValue) return;
    if (entriesLoading || movingEntry) return;

    const folderImageEntries = entries.filter(
      (entry) => entry.type === "file" && isImage(entry.name)
    );
    if (folderImageEntries.length <= 1) return;

    const currentOrder = folderImageEntries.map((entry) => entry.path);
    const nextOrder = buildTaggedImageOrderPaths(folderImageEntries);
    if (nextOrder.length !== currentOrder.length) return;
    const changed = nextOrder.some((pathAtIndex, index) => pathAtIndex !== currentOrder[index]);
    if (!changed) return;

    const byPath = new Map(entries.map((entry) => [entry.path, entry]));
    const dirs = entries.filter((entry) => entry.type === "dir");
    const nonImageFiles = entries.filter(
      (entry) => entry.type === "file" && !isImage(entry.name)
    );
    const sortedImages = nextOrder
      .map((pathEntry) => byPath.get(pathEntry))
      .filter(
        (entry): entry is DraftEntry =>
          Boolean(entry && entry.type === "file" && isImage(entry.name))
      );

    setEntries([...dirs, ...sortedImages, ...nonImageFiles]);

    const signature = `${pathValue}::${nextOrder.join("|")}`;
    if (autoMainSortPersistSignatureRef.current === signature) return;
    autoMainSortPersistSignatureRef.current = signature;
    void persistImageOrderForFolder(pathValue, nextOrder).catch(() => {
      autoMainSortPersistSignatureRef.current = "";
    });
  }, [
    currentPath,
    entries,
    entriesLoading,
    isImage,
    movingEntry,
    persistImageOrderForFolder,
  ]);

  const variantAssignedImageFileNameKeysForCurrentSpu = useMemo(() => {
    const spu = String(currentImageSpuForDraftFilter || "").trim().toUpperCase();
    if (!spu) return new Set<string>();
    const mergedById = new Map<string, DraftSkuRow>();
    const skuRowsForSpu = skuRows.filter(
      (row) => String(row.draft_spu || "").trim().toUpperCase() === spu
    );
    const skuIdsForSpu = new Set(
      skuRowsForSpu
        .map((row) => String(row.id || "").trim())
        .filter((rowId) => Boolean(rowId))
    );
    const shouldPruneCachedRows = !String(searchQuery || "").trim() && skuIdsForSpu.size > 0;
    const cachedRows = variantPickerRowsBySpu[spu] ?? [];
    cachedRows.forEach((row) => {
      const rowId = String(row.id || "").trim();
      if (!rowId) return;
      if (shouldPruneCachedRows && !skuIdsForSpu.has(rowId)) return;
      mergedById.set(rowId, row);
    });
    skuRowsForSpu.forEach((row) => {
      const rowId = String(row.id || "").trim();
      if (!rowId) return;
      mergedById.set(rowId, row);
    });
    const keys = new Set<string>();
    mergedById.forEach((row) => {
      const fileName = extractVariantImageFileName(row.draft_variant_image_url);
      const key = normalizeVariantImageFileNameKey(fileName);
      if (key) {
        keys.add(key);
      }
    });
    return keys;
  }, [currentImageSpuForDraftFilter, searchQuery, skuRows, variantPickerRowsBySpu]);

  const entryHasVariantAssignment = useCallback(
    (entry: DraftEntry) => {
      const fileName = extractFileNameFromPath(entry.path || entry.name);
      const key = normalizeVariantImageFileNameKey(fileName);
      if (!key) return false;
      return variantAssignedImageFileNameKeysForCurrentSpu.has(key);
    },
    [variantAssignedImageFileNameKeysForCurrentSpu]
  );

  const getDisplayImageTagsForEntry = useCallback(
    (entry: DraftEntry) => {
      const fileTags = extractImageTagsFromFileName(entry.name);
      if (fileTags.includes("VAR")) return fileTags;
      if (entryHasVariantAssignment(entry)) {
        return normalizeImageTags([...fileTags, "VAR"]);
      }
      return fileTags;
    },
    [entryHasVariantAssignment]
  );

  const isSizeChartEntry = useCallback(
    (entry: DraftEntry) => {
      const tags = getDisplayImageTagsForEntry(entry);
      if (tags.includes("SIZE")) return true;
      return isSizeChartImageName(entry.name);
    },
    [getDisplayImageTagsForEntry]
  );

  const displayImageEntries = useMemo(() => {
    const combined: DraftEntry[] = [];
    const seenPaths = new Set<string>();

    const pushUnique = (entry: DraftEntry) => {
      if (optimisticHiddenImagePaths.has(entry.path)) return;
      if (seenPaths.has(entry.path)) return;
      seenPaths.add(entry.path);
      combined.push(entry);
    };

    imageEntries.forEach(pushUnique);
    if (mainViewShowsMergedImages) {
      mainViewVariantImageEntries.forEach(pushUnique);
    }

    if (combined.length <= 1) return combined;

    const nextOrder = buildTaggedImageOrderPaths(combined, {
      resolveTags: getDisplayImageTagsForEntry,
    });
    if (nextOrder.length !== combined.length) return combined;
    const byPath = new Map(combined.map((entry) => [entry.path, entry]));
    const sorted = nextOrder
      .map((pathValue) => byPath.get(pathValue))
      .filter((entry): entry is DraftEntry => Boolean(entry));
    if (sorted.length !== combined.length) return combined;

    if (mainViewShowsMergedImages && mainImageViewFilter === "var_only") {
      return sorted.filter((entry) => {
        return getDisplayImageTagsForEntry(entry).includes("VAR");
      });
    }
    if (mainViewShowsMergedImages && mainImageViewFilter === "size_only") {
      return sorted.filter((entry) => isSizeChartEntry(entry));
    }
    if (mainViewShowsMergedImages && mainImageViewFilter === "all") {
      return sorted.filter((entry) => !isSizeChartEntry(entry));
    }
    return sorted;
  }, [
    getDisplayImageTagsForEntry,
    imageEntries,
    isSizeChartEntry,
    mainImageViewFilter,
    mainViewShowsMergedImages,
    mainViewVariantImageEntries,
    optimisticHiddenImagePaths,
  ]);

  useEffect(() => {
    const requestedImagePath = normalizeExplorerLocationPath(
      initialUrlImagePathPendingRef.current
    );
    if (!requestedImagePath) return;
    const existsInCurrentView = displayImageEntries.some(
      (entry) => String(entry.path || "").trim() === requestedImagePath
    );
    if (!existsInCurrentView) {
      if (!entriesLoading) {
        initialUrlImagePathPendingRef.current = "";
      }
      return;
    }
    if (previewPath !== requestedImagePath) {
      setPreviewPath(requestedImagePath);
    }
    initialUrlImagePathPendingRef.current = "";
  }, [displayImageEntries, entriesLoading, previewPath]);

  useEffect(() => {
    displayImageEntriesRef.current = displayImageEntries;
  }, [displayImageEntries]);

  useEffect(() => {
    const imagePaths = displayImageEntries
      .filter((entry) => entry.type === "file" && isImage(entry.name))
      .map((entry) => String(entry.path || "").trim())
      .filter(Boolean)
      .slice(0, 180);
    if (imagePaths.length === 0) return;
    let cancelled = false;
    const timer = window.setTimeout(() => {
      void (async () => {
        try {
          const storedJobs = await fetchStoredReverseSearchJobs(imagePaths, false);
          if (cancelled) return;
          const targetSet = new Set(imagePaths);
          setReverseSearchJobsByPath((prev) => {
            const next: Record<string, ReverseSearchJobState> = { ...prev };
            imagePaths.forEach((pathValue) => {
              const existing = prev[pathValue];
              if (existing?.status === "running") {
                next[pathValue] = existing;
                return;
              }
              const stored = storedJobs[pathValue];
              if (!stored) {
                // Keep in-memory status when persistence is delayed/unavailable so the
                // user can still see/open the latest Lens run result or error state.
                if (existing) {
                  next[pathValue] = existing;
                }
                return;
              }
              if (existing && existing.items.length > 0) {
                next[pathValue] = {
                  ...stored,
                  items: existing.items,
                  amazonLinks:
                    existing.amazonLinks.length > 0
                      ? existing.amazonLinks
                      : stored.amazonLinks,
                  inputPayload: existing.inputPayload || stored.inputPayload,
                  debugPayload: existing.debugPayload || stored.debugPayload,
                };
              } else {
                next[pathValue] = stored;
              }
            });

            Object.keys(next).forEach((pathValue) => {
              if (!targetSet.has(pathValue)) return;
              const existing = next[pathValue];
              if (!existing) return;
              if (existing.status === "running") return;
            });
            return next;
          });
        } catch {
          // Ignore failed hydration and keep in-memory state.
        }
      })();
    }, 120);
    return () => {
      cancelled = true;
      window.clearTimeout(timer);
    };
  }, [displayImageEntries, fetchStoredReverseSearchJobs, isImage]);

  const currentSpuMainPath = imageTabTargets.mainPath;
  const currentMainProductContext = useMemo(() => {
    const parts = String(currentSpuMainPath || "").split("/").filter(Boolean);
    if (parts.length < 2) {
      return { run: "", spu: "" };
    }
    return { run: parts[0], spu: parts[1] };
  }, [currentSpuMainPath]);
  const currentPathProductContext = useMemo(() => {
    const spuRoot = getSpuRootFromDraftPath(String(currentPath || ""));
    const parts = String(spuRoot || "").split("/").filter(Boolean);
    if (parts.length < 2) {
      return { run: "", spu: "" };
    }
    return { run: parts[0], spu: parts[1] };
  }, [currentPath]);
  const activeProductContext = useMemo(() => {
    if (currentMainProductContext.run && currentMainProductContext.spu) {
      return currentMainProductContext;
    }
    return currentPathProductContext;
  }, [currentMainProductContext, currentPathProductContext]);

  useEffect(() => {
    const scope = getSpuRootFromDraftPath(String(currentPath || ""));
    if (!scope) return;
    setLastDeletedImageUndoItems((prev) => {
      const next = prev.filter((item) => {
        const sourceSpuPath = getSpuRootFromDraftPath(String(item.sourcePath || ""));
        const destinationSpuPath = getSpuRootFromDraftPath(
          String(item.destinationPath || "")
        );
        return sourceSpuPath === scope && destinationSpuPath === scope;
      });
      return next.length === prev.length ? prev : next;
    });
  }, [currentPath]);

  const currentSpuMarkedCompleted = currentSpuMainPath
    ? completedSpuFolders.has(currentSpuMainPath)
    : false;
  const confirmablePendingAiPaths = useMemo(() => {
    if (!mainViewShowsMergedImages) return [] as string[];
    const unique = new Set<string>();
    displayImageEntries.forEach((entry) => {
      if (pendingAiEditsByOriginal[entry.path]) {
        unique.add(entry.path);
      }
    });
    return Array.from(unique);
  }, [displayImageEntries, mainViewShowsMergedImages, pendingAiEditsByOriginal]);

  const handleToggleCurrentSpuCompleted = useCallback(() => {
    if (!currentSpuMainPath) return;
    setCompletedSpuFolders((prev) => {
      const next = new Set(prev);
      if (next.has(currentSpuMainPath)) {
        next.delete(currentSpuMainPath);
      } else {
        next.add(currentSpuMainPath);
      }
      return next;
    });
  }, [currentSpuMainPath]);

  const handleDeleteCurrentProduct = useCallback(async () => {
    if (deleteProductPending) return;

    const run = String(activeProductContext.run || "").trim();
    const spu = String(activeProductContext.spu || "").trim();
    const spuMainPath = run && spu ? `${run}/${spu}` : "";
    if (!run || !spu || !spuMainPath) return;
    const currentSpuIndex = runSpuOptions.findIndex(
      (option) => option.path === spuMainPath
    );
    const nextSpuPathAfterDelete =
      runSpuOptions[currentSpuIndex + 1]?.path ||
      runSpuOptions[currentSpuIndex - 1]?.path ||
      runSpuOptions.find((option) => option.path !== spuMainPath)?.path ||
      run;

    const confirmed = window.confirm(
      [
        `Delete product ${spu}?`,
        "",
        "This will permanently delete:",
        `- ${spuMainPath} and all subfolders`,
        "- related draft SPU and SKU rows",
        "",
        "This cannot be undone.",
      ].join("\n")
    );
    if (!confirmed) return;

    setDeleteProductPending(true);
    setError(null);
    setDraftError(null);
    try {
      const response = await fetch(
        `/api/drafts/folders/${encodeURIComponent(run)}/cleanup`,
        {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ spus: [spu] }),
        }
      );
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        throw new Error(payload?.error || "Unable to delete this product.");
      }

      setCompletedSpuFolders((prev) => {
        if (prev.size === 0) return prev;
        const next = new Set(prev);
        Array.from(next).forEach((pathValue) => {
          if (pathValue === spuMainPath || pathValue.startsWith(`${spuMainPath}/`)) {
            next.delete(pathValue);
          }
        });
        return next;
      });
      setUnseenAiUpdateSpuPaths((prev) => {
        if (prev.size === 0) return prev;
        if (!prev.has(spuMainPath)) return prev;
        const next = new Set(prev);
        next.delete(spuMainPath);
        return next;
      });
      setSelectedTreeFolders((prev) => {
        if (prev.size === 0) return prev;
        const next = new Set(prev);
        Array.from(next).forEach((pathValue) => {
          if (pathValue === spuMainPath || pathValue.startsWith(`${spuMainPath}/`)) {
            next.delete(pathValue);
          }
        });
        return next;
      });
      setCollapsedTreeFolders((prev) => {
        if (prev.size === 0) return prev;
        const next = new Set(prev);
        Array.from(next).forEach((pathValue) => {
          if (pathValue === spuMainPath || pathValue.startsWith(`${spuMainPath}/`)) {
            next.delete(pathValue);
          }
        });
        return next;
      });
      setMainViewVariantImageEntries([]);
      setSelectedFiles(new Set());
      setSelectedSpus(new Set());
      setSelectedSkus(new Set());
      setPreviewPath(null);

      setCurrentPath(nextSpuPathAfterDelete);
      await Promise.all([fetchSpuRows(), fetchSkuRows(), fetchFolderTree(run)]);
    } catch (err) {
      const message =
        err instanceof Error ? err.message : "Unable to delete this product.";
      setError(message);
      setDraftError(message);
    } finally {
      setDeleteProductPending(false);
    }
  }, [
    activeProductContext.run,
    activeProductContext.spu,
    deleteProductPending,
    fetchFolderTree,
    fetchSkuRows,
    fetchSpuRows,
    runSpuOptions,
  ]);

  const handleConfirmCurrentProductAiEdits = useCallback(async () => {
    if (confirmAiEditsPending) return;
    if (!mainViewShowsMergedImages) return;
    if (confirmablePendingAiPaths.length === 0) return;

    const confirmed = window.confirm(
      [
        `Confirm ${confirmablePendingAiPaths.length} pending AI edit${
          confirmablePendingAiPaths.length === 1 ? "" : "s"
        }?`,
        "",
        "This will replace the original images with the pending AI versions and discard the old pending files.",
      ].join("\n")
    );
    if (!confirmed) return;

    const pendingPaths = [...confirmablePendingAiPaths];
    const foldersNeedingResort = new Set(
      pendingPaths
        .map((pathValue) => getParentFolderPathFromEntryPath(pathValue))
        .filter((pathValue) => Boolean(pathValue))
    );
    const refreshedScoreByPath = new Map<string, number | null>();
    const discardedUndoItems: DraftDeletedImageUndoItem[] = [];
    const resolveFailures: string[] = [];
    const reorderFailures: string[] = [];
    const pathAtStart = String(currentPathRef.current || "");

    setConfirmAiEditsPending(true);
    setConfirmingAiEditPaths(new Set(pendingPaths));
    setAiReviewOriginalPath(null);
    setError(null);

    try {
      for (const originalPath of pendingPaths) {
        try {
          const response = await fetch("/api/drafts/ai-edits/resolve", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({
              originalPath,
              decision: "replace_with_ai",
            }),
          });
          const payload = await response.json().catch(() => ({}));
          if (!response.ok) {
            throw new Error(payload?.error || "Unable to confirm AI edit.");
          }
          setPendingAiEditsByOriginal((prev) => {
            const next = { ...prev };
            delete next[originalPath];
            return next;
          });
          const refreshedScoresRaw = Array.isArray(payload?.refreshedScores)
            ? (payload.refreshedScores as Array<{ path?: unknown; pixelQualityScore?: unknown }>)
            : [];
          const discardedMovesRaw = Array.isArray(payload?.discardedMoves)
            ? (payload.discardedMoves as Array<{ sourcePath?: unknown; destinationPath?: unknown }>)
            : [];
          refreshedScoresRaw.forEach((row) => {
            const pathValue = String(row?.path || "").trim();
            if (!pathValue) return;
            const scoreValue =
              typeof row?.pixelQualityScore === "number" &&
              Number.isFinite(row.pixelQualityScore)
                ? Math.round(row.pixelQualityScore)
                : null;
            refreshedScoreByPath.set(pathValue, scoreValue);
          });
          discardedMovesRaw
            .map((row) => ({
              sourcePath: String(row?.sourcePath || "").trim(),
              destinationPath: String(row?.destinationPath || "").trim(),
            }))
            .filter(
              (row) =>
                Boolean(row.sourcePath) &&
                Boolean(row.destinationPath) &&
                row.sourcePath !== row.destinationPath
            )
            .forEach((row) => {
              discardedUndoItems.push(row);
            });
        } catch (err) {
          resolveFailures.push(
            `${originalPath.split("/").filter(Boolean).pop() || originalPath}: ${
              (err as Error).message
            }`
          );
        }
      }

      if (refreshedScoreByPath.size > 0) {
        const now = new Date().toISOString();
        setEntries((prev) =>
          prev.map((item) => {
            if (!refreshedScoreByPath.has(item.path)) return item;
            return {
              ...item,
              modifiedAt: now,
              pixelQualityScore: refreshedScoreByPath.get(item.path) ?? null,
            };
          })
        );
      }
      setLastDeletedImageUndoItems(discardedUndoItems);

      if (foldersNeedingResort.size > 0) {
        const reorderFailuresResult = await resortImageFoldersByHierarchy(
          foldersNeedingResort
        );
        reorderFailures.push(...reorderFailuresResult);
      }

      const latestPath = String(currentPathRef.current || "");
      if (pathAtStart && latestPath === pathAtStart) {
        await refreshEntries(pathAtStart);
        await fetchPendingAiEdits(pathAtStart);
      }
      if (pendingPaths.length > 0) {
        markSpuNeedsAiReview(pendingPaths);
      }

      if (resolveFailures.length > 0 || reorderFailures.length > 0) {
        const issueDetails: string[] = [];
        if (resolveFailures.length > 0) {
          issueDetails.push(
            `resolve: ${resolveFailures.slice(0, 2).join("; ")}${
              resolveFailures.length > 2 ? "..." : ""
            }`
          );
        }
        if (reorderFailures.length > 0) {
          issueDetails.push(
            `re-sort: ${reorderFailures.slice(0, 2).join("; ")}${
              reorderFailures.length > 2 ? "..." : ""
            }`
          );
        }
        setError(
          `Confirmed ${
            pendingPaths.length - resolveFailures.length
          }/${pendingPaths.length} pending AI edits. Issues: ${issueDetails.join(" | ")}`
        );
      }
    } finally {
      setConfirmingAiEditPaths(new Set());
      setConfirmAiEditsPending(false);
    }
  }, [
    confirmAiEditsPending,
    confirmablePendingAiPaths,
    fetchPendingAiEdits,
    mainViewShowsMergedImages,
    markSpuNeedsAiReview,
    refreshEntries,
    resortImageFoldersByHierarchy,
  ]);

  const reorderImagesInGrid = useCallback(
    (draggedPaths: string[], dropTargetPath: string | null) => {
      const currentImagePaths = imageEntries.map((entry) => entry.path);
      if (currentImagePaths.length <= 1) return;
      const movingPaths = draggedPaths.filter((pathValue) =>
        currentImagePaths.includes(pathValue)
      );
      if (movingPaths.length === 0) return;
      const uniqueMoving = Array.from(new Set(movingPaths));
      if (dropTargetPath && uniqueMoving.includes(dropTargetPath)) {
        return;
      }
      const remaining = currentImagePaths.filter(
        (pathValue) => !uniqueMoving.includes(pathValue)
      );
      let insertAt = remaining.length;
      if (dropTargetPath) {
        const targetIndex = remaining.indexOf(dropTargetPath);
        if (targetIndex >= 0) {
          insertAt = targetIndex;
        }
      }
      const nextImagePaths = [
        ...remaining.slice(0, insertAt),
        ...uniqueMoving,
        ...remaining.slice(insertAt),
      ];
      if (
        nextImagePaths.length !== currentImagePaths.length ||
        nextImagePaths.every((pathValue, index) => pathValue === currentImagePaths[index])
      ) {
        return;
      }

      const nextImageOrder = new Map<string, number>();
      nextImagePaths.forEach((pathValue, index) => {
        nextImageOrder.set(pathValue, index);
      });
      setEntries((prev) => {
        const dirs = prev.filter((entry) => entry.type === "dir");
        const files = prev.filter((entry) => entry.type === "file");
        const sortedFiles = [...files].sort((left, right) => {
          const leftOrder = nextImageOrder.get(left.path);
          const rightOrder = nextImageOrder.get(right.path);
          if (leftOrder !== undefined && rightOrder !== undefined) {
            return leftOrder - rightOrder;
          }
          if (leftOrder !== undefined) return -1;
          if (rightOrder !== undefined) return 1;
          return left.name.localeCompare(right.name);
        });
        return [...dirs, ...sortedFiles];
      });
      void persistImageOrder(nextImagePaths);
    },
    [imageEntries, persistImageOrder]
  );

  const selectedImageEntries = displayImageEntries.filter((entry) =>
    selectedFiles.has(entry.path)
  );
  const selectedImageCollectionAllowed =
    selectedImageEntries.length >= 2 && selectedImageEntries.length <= 4;
  const currentUndoScopeSpuPath = getSpuRootFromDraftPath(String(currentPath || ""));
  const scopedLastDeletedImageUndoItems = useMemo(() => {
    const scope = String(currentUndoScopeSpuPath || "").trim();
    if (!scope) return [] as DraftDeletedImageUndoItem[];
    return lastDeletedImageUndoItems.filter((item) => {
      const sourceSpuPath = getSpuRootFromDraftPath(String(item.sourcePath || ""));
      const destinationSpuPath = getSpuRootFromDraftPath(
        String(item.destinationPath || "")
      );
      return sourceSpuPath === scope && destinationSpuPath === scope;
    });
  }, [currentUndoScopeSpuPath, lastDeletedImageUndoItems]);
  const undoLastChangeAvailable =
    selectedImageEntries.length > 0 || scopedLastDeletedImageUndoItems.length > 0;
  const handleUndoLastChangeFromImageActions = useCallback(() => {
    if (selectedImageEntries.length > 0) {
      void handleUndoLastChangeForEntries(selectedImageEntries);
      return;
    }
    if (scopedLastDeletedImageUndoItems.length > 0) {
      void handleUndoLastDeletedImageMove(scopedLastDeletedImageUndoItems);
    }
  }, [
    handleUndoLastChangeForEntries,
    handleUndoLastDeletedImageMove,
    scopedLastDeletedImageUndoItems,
    selectedImageEntries,
  ]);
  const selectedImageUpscaleEligibleCount = selectedImageEntries.reduce(
    (count, entry) => count + (isImageEntryUpscaled(entry) ? 0 : 1),
    0
  );
  const regularBatchFolders = useMemo(
    () => folders.filter((folder) => !isArchiveFolder(folder.path)),
    [folders, isArchiveFolder]
  );
  const archiveBatchFolders = useMemo(
    () => folders.filter((folder) => isArchiveFolder(folder.path)),
    [folders, isArchiveFolder]
  );
  const selectedMergeableRunsCount = useMemo(
    () => {
      const runs = Array.from(selectedRunsForMerge).filter(
        (run) => !isArchiveFolder(run)
      );
      if (runs.length === 0) return 0;
      const runTypes = new Set(runs.map((run) => getRunTypeForPath(run)));
      if (runTypes.size > 1) return 0;
      return runs.length;
    },
    [getRunTypeForPath, selectedRunsForMerge, isArchiveFolder]
  );
  const isSelectedFolderArchive = isArchiveFolder(selectedFolder);
  const isSelectedFolderReEdit = isReEditFolder(selectedFolder);
  const isBatchPickerActionTarget = useCallback((target: EventTarget | null) => {
    if (!(target instanceof Element)) return false;
    return Boolean(target.closest("[data-batch-picker-action='true']"));
  }, []);
  const handleBatchPickerRunSelect = useCallback(
    (folderPath: string) => {
      setBatchPickerOpen(false);
      void handleSelectFolder(folderPath);
    },
    [handleSelectFolder]
  );
  const nonImageFileEntries = entries.filter(
    (entry) => entry.type === "file" && !isImage(entry.name)
  );
  const nonImageFilesSelectedCount = nonImageFileEntries.reduce(
    (count, entry) => count + (selectedFiles.has(entry.path) ? 1 : 0),
    0
  );
  const nonImageFilesAllSelected =
    nonImageFileEntries.length > 0 &&
    nonImageFilesSelectedCount === nonImageFileEntries.length;
  const nonImageFilesSomeSelected =
    nonImageFilesSelectedCount > 0 && !nonImageFilesAllSelected;
  const mainTabBadgeCount = imageTabImageCounts.main;
  const variantsTabBadgeCount = Math.max(
    imageTabImageCounts.variants,
    variantAssignedImageFileNameKeysForCurrentSpu.size
  );
  const sizeChartsTabBadgeCount = imageTabImageCounts.sizecharts;
  const othersTabBadgeCount = imageTabImageCounts.others;
  const ocrTabBadgeCount = imageTabImageCounts.ocr;
  const downloadedTabBadgeCount = imageTabImageCounts.downloaded;
  // Main + Variants should always be reachable while a product/main path exists,
  // even when the current image count is zero.
  const mainTabDisabled = !imageTabTargets.mainPath || movingEntry;
  const variantsTabDisabled = !imageTabTargets.mainPath || movingEntry;
  const sizeChartsTabDisabled =
    !imageTabTargets.mainPath || movingEntry || sizeChartsTabBadgeCount === 0;
  const ocrTabDisabled =
    !imageTabTargets.ocrPath || movingEntry || ocrTabBadgeCount === 0;
  const downloadedTabDisabled =
    !imageTabTargets.downloadedPath ||
    movingEntry ||
    downloadedTabBadgeCount === 0;
  const othersTabDisabled =
    imageTabTargets.othersPaths.length === 0 || movingEntry || othersTabBadgeCount === 0;

  useEffect(() => {
    if (mainImageViewFilter !== "size_only") return;
    if (sizeChartsTabBadgeCount > 0) return;
    setMainImageViewFilter("all");
  }, [mainImageViewFilter, sizeChartsTabBadgeCount]);

  useEffect(() => {
    const el = nonImageFileSelectAllRef.current;
    if (!el) return;
    el.indeterminate = nonImageFilesSomeSelected;
  }, [nonImageFilesSomeSelected]);
  const aiReviewRecord = aiReviewOriginalPath
    ? pendingAiEditsByOriginal[aiReviewOriginalPath] ?? null
    : null;
  const aiReviewOriginalScoreRaw = aiReviewRecord
    ? entryByPath.get(aiReviewRecord.originalPath)?.pixelQualityScore
    : null;
  const aiReviewOriginalScore =
    typeof aiReviewOriginalScoreRaw === "number" && Number.isFinite(aiReviewOriginalScoreRaw)
      ? Math.round(aiReviewOriginalScoreRaw)
      : null;
  const aiReviewPendingScore =
    aiReviewRecord &&
    typeof aiReviewRecord.pendingPixelQualityScore === "number" &&
    Number.isFinite(aiReviewRecord.pendingPixelQualityScore)
      ? Math.round(aiReviewRecord.pendingPixelQualityScore)
      : null;
  const aiReviewScoreDelta =
    aiReviewPendingScore !== null && aiReviewOriginalScore !== null
      ? aiReviewPendingScore - aiReviewOriginalScore
      : null;
  const displayEntryByPath = useMemo(() => {
    const next = new Map(entryByPath);
    displayImageEntries.forEach((entry) => {
      if (!next.has(entry.path)) {
        next.set(entry.path, entry);
      }
    });
    return next;
  }, [displayImageEntries, entryByPath]);
  const previewEntry = previewPath ? displayEntryByPath.get(previewPath) ?? null : null;
  const previewDimensions = previewPath ? imageDimensions[previewPath] ?? null : null;
  const previewFileName =
    previewEntry?.name ??
    (previewPath ? previewPath.split("/").filter(Boolean).pop() ?? previewPath : "");
  const previewFileSizeText =
    previewEntry && previewEntry.type === "file" ? formatSizeKb(previewEntry.size) : "-";
  const previewPendingAi = previewPath
    ? pendingAiEditsByOriginal[previewPath] ?? null
    : null;
  const previewDisplayPath = previewPendingAi?.pendingPath ?? previewPath;
  const previewDisplayModifiedAt =
    previewPendingAi?.updatedAt ??
    (previewPath ? displayEntryByPath.get(previewPath)?.modifiedAt ?? null : null);
  const previewRuntimeJob = previewPath ? aiEditJobsByPath[previewPath] ?? null : null;
  const previewNav = useMemo(() => {
    if (!previewPath) {
      return {
        index: -1,
        prevPath: null as string | null,
        nextPath: null as string | null,
        next2Path: null as string | null,
      };
    }
    const paths = displayImageEntries.map((entry) => entry.path);
    const index = paths.indexOf(previewPath);
    const prevPath = index > 0 ? paths[index - 1] ?? null : null;
    const nextPath =
      index >= 0 && index < paths.length - 1 ? paths[index + 1] ?? null : null;
    const next2Path =
      index >= 0 && index < paths.length - 2 ? paths[index + 2] ?? null : null;
    return { index, prevPath, nextPath, next2Path };
  }, [displayImageEntries, previewPath]);
  const previewBusy =
    Boolean(previewRuntimeJob) ||
    Boolean(
      previewPath &&
        (reloadingImagePaths.has(previewPath) || confirmingAiEditPaths.has(previewPath))
    );

  useEffect(() => {
    if (!previewPath) return;
    const candidates = [previewNav.nextPath, previewNav.next2Path].filter(Boolean) as string[];
    candidates.forEach((pathValue) => {
      const entry = displayEntryByPath.get(pathValue);
      const pendingAi = pendingAiEditsByOriginal[pathValue] ?? null;
      const displayPath = pendingAi?.pendingPath ?? pathValue;
      const displayModifiedAt = pendingAi?.updatedAt ?? entry?.modifiedAt;
      const img = new Image();
      img.decoding = "async";
      img.src = buildDraftDownloadUrl(displayPath, displayModifiedAt);
      // Best-effort decode to make next/next+1 swaps feel instantaneous.
      if (typeof img.decode === "function") {
        void img.decode().catch(() => undefined);
      }
    });
  }, [
    buildDraftDownloadUrl,
    displayEntryByPath,
    pendingAiEditsByOriginal,
    previewNav.next2Path,
    previewNav.nextPath,
    previewPath,
  ]);

  const handlePreviewNavigate = useCallback(
    (direction: "prev" | "next") => {
      if (!previewPath) return;
      const target =
        direction === "prev" ? previewNav.prevPath : previewNav.nextPath;
      if (!target) return;
      setPreviewPath(target);
    },
    [previewNav.nextPath, previewNav.prevPath, previewPath]
  );

  const handlePreviewDelete = useCallback(async () => {
    if (!previewPath || previewDeletePending) return;
    if (aiEditJobsByPath[previewPath]) {
      setError("Resolve running AI edits before deleting this image.");
      return;
    }
    const nextPreviewPath = previewNav.nextPath ?? previewNav.prevPath ?? null;
    setPreviewDeletePending(true);
    setError(null);
    try {
      await handleMoveEntriesToDeletedImages([previewPath]);
      setPreviewPath(nextPreviewPath);
    } catch (err) {
      setError((err as Error).message);
    } finally {
      setPreviewDeletePending(false);
    }
  }, [
    aiEditJobsByPath,
    handleMoveEntriesToDeletedImages,
    previewDeletePending,
    previewNav.nextPath,
    previewNav.prevPath,
    previewPath,
  ]);
  const aiEditHasPromptInput =
    aiEditMode === "template" || aiEditMode === "direct" || aiEditMode === "eraser";
  const aiEditShowPromptTemplates =
    aiEditMode === "direct" &&
    (aiEditProvider === "chatgpt" || aiEditProvider === "gemini");
  const aiDirectPromptHistoryPreviewRows = useMemo(
    () =>
      aiDirectPromptHistory.map((prompt) => ({
        key: prompt,
        preview: buildPromptHistoryPreview(prompt),
      })),
    [aiDirectPromptHistory]
  );
  const aiEditProviderLabel =
    aiEditProvider === "chatgpt"
      ? "ChatGPT"
      : aiEditProvider === "gemini"
        ? "Gemini"
        : "ZImage";
  const aiEditModeLabel =
    aiEditMode === "template"
      ? aiEditTemplatePreset === "digideal_main"
        ? "DigiDeal Main Single"
        : aiEditTemplatePreset === "digideal_main_dual"
          ? "DigiDeal Main Dual"
        : aiEditTemplatePreset === "product_scene"
          ? "Product Scene"
        : aiEditTemplatePreset === "product_collection"
          ? "Product Collection"
          : "Standard Template"
      : aiEditMode === "direct"
        ? "Direct"
        : aiEditMode === "white_background"
          ? "White Background"
          : aiEditMode === "auto_center_white"
            ? "Auto Center Wide"
            : aiEditMode === "eraser"
              ? "Eraser"
              : "Upscale";
  const aiEditOutputCountLabel =
    (aiEditMode === "template" || aiEditMode === "direct") && aiEditOutputCount > 1
      ? `, ${aiEditOutputCount} outputs`
      : "";
  const aiEditSingleTarget = aiEditTargets.length === 1 ? aiEditTargets[0] : null;
  const aiEditSingleTargetPreviewUrl = aiEditSingleTarget
    ? buildDraftDownloadUrl(
        aiEditSingleTarget.path,
        entryByPath.get(aiEditSingleTarget.path)?.modifiedAt
      )
    : "";
  const aiEditShowMaskPainter =
    aiEditMode === "eraser" && aiEraserRunMode === "mask" && Boolean(aiEditSingleTarget);
  const aiDualProviderLabel = aiDualDialog
    ? aiDualDialog.provider === "chatgpt"
      ? "ChatGPT"
      : "Gemini"
    : "";
  const aiDualProductPreviewUrl = aiDualDialog
    ? buildDraftDownloadUrl(
        aiDualDialog.productEntry.path,
        entryByPath.get(aiDualDialog.productEntry.path)?.modifiedAt ??
          aiDualDialog.productEntry.modifiedAt
      )
    : "";
  const aiDualGuidancePreviewUrl = aiDualDialog
    ? buildDraftDownloadUrl(
        aiDualDialog.guidanceEntry.path,
        entryByPath.get(aiDualDialog.guidanceEntry.path)?.modifiedAt ??
          aiDualDialog.guidanceEntry.modifiedAt
      )
    : "";
  const aiCollectionProviderLabel = aiCollectionDialog
    ? aiCollectionDialog.provider === "chatgpt"
      ? "ChatGPT"
      : "Gemini"
    : "";
  const aiCollectionPreviewItems = aiCollectionDialog
    ? aiCollectionDialog.entries.map((entry, index) => ({
        key: entry.path || `${entry.name}-${index}`,
        name: entry.name,
        url: buildDraftDownloadUrl(
          entry.path,
          entryByPath.get(entry.path)?.modifiedAt ?? entry.modifiedAt
        ),
      }))
    : [];
  const variantsEditorAllSelected =
    variantsEditorRows.length > 0 &&
    variantsEditorRows.every((row) => variantsEditorSelectedRows.has(row.key));
  const variantsEditorSomeSelected =
    variantsEditorRows.some((row) => variantsEditorSelectedRows.has(row.key));
  const autoLevelsBounds = useMemo(
    () => mapAutoLevelsBounds(autoLevelsLevelPercent),
    [autoLevelsLevelPercent]
  );
  const getVariantSortIndicator = (key: VariantEditorSortKey) => {
    if (variantsEditorSort.key !== key) return "";
    return variantsEditorSort.direction === "asc" ? "▲" : "▼";
  };
  const renderContextMenuIcon = (
    type:
      | "open"
      | "move-main"
      | "download"
      | "duplicate"
      | "clipboard"
      | "undo"
      | "tag"
      | "ai"
      | "photopea"
      | "focuscenter"
      | "background"
      | "eraser"
      | "upscale"
      | "google-search"
  ) => {
    if (type === "open") {
      return (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
          className={styles.contextMenuIcon}
          aria-hidden="true"
        >
          <path stroke="none" d="M0 0h24v24H0z" fill="none" />
          <path d="M4 8v-2a2 2 0 0 1 2 -2h2" />
          <path d="M4 16v2a2 2 0 0 0 2 2h2" />
          <path d="M16 4h2a2 2 0 0 1 2 2v2" />
          <path d="M16 20h2a2 2 0 0 0 2 -2v-2" />
          <path d="M8 11a3 3 0 1 0 6 0a3 3 0 0 0 -6 0" />
          <path d="M16 16l-2.5 -2.5" />
        </svg>
      );
    }
    if (type === "move-main") {
      return (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
          className={styles.contextMenuIcon}
          aria-hidden="true"
        >
          <path stroke="none" d="M0 0h24v24H0z" fill="none" />
          <path d="M4 12v-6a2 2 0 0 1 2 -2h12a2 2 0 0 1 2 2v8" />
          <path d="M20 18h-17" />
          <path d="M6 15l-3 3l3 3" />
        </svg>
      );
    }
    if (type === "google-search") {
      return (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
          className={styles.contextMenuIcon}
          aria-hidden="true"
        >
          <path stroke="none" d="M0 0h24v24H0z" fill="none" />
          <path d="M4 4m0 2a2 2 0 0 1 2 -2h12a2 2 0 0 1 2 2v12a2 2 0 0 1 -2 2h-12a2 2 0 0 1 -2 -2z" />
          <path d="M10 10a2 2 0 1 0 4 0a2 2 0 0 0 -4 0" />
          <path d="M16 16l-1.5 -1.5" />
        </svg>
      );
    }
    if (type === "download") {
      return (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
          className={styles.contextMenuIcon}
          aria-hidden="true"
        >
          <path stroke="none" d="M0 0h24v24H0z" fill="none" />
          <path d="M14 3v4a1 1 0 0 0 1 1h4" />
          <path d="M17 21h-10a2 2 0 0 1 -2 -2v-14a2 2 0 0 1 2 -2h7l5 5v11a2 2 0 0 1 -2 2" />
          <path d="M12 17v-6" />
          <path d="M9.5 14.5l2.5 2.5l2.5 -2.5" />
        </svg>
      );
    }
    if (type === "duplicate") {
      return (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
          className={styles.contextMenuIcon}
          aria-hidden="true"
        >
          <path stroke="none" d="M0 0h24v24H0z" fill="none" />
          <path d="M7 9.667a2.667 2.667 0 0 1 2.667 -2.667h8.666a2.667 2.667 0 0 1 2.667 2.667v8.666a2.667 2.667 0 0 1 -2.667 2.667h-8.666a2.667 2.667 0 0 1 -2.667 -2.667l0 -8.666" />
          <path d="M4.012 16.737a2.005 2.005 0 0 1 -1.012 -1.737v-10c0 -1.1 .9 -2 2 -2h10c.75 0 1.158 .385 1.5 1" />
        </svg>
      );
    }
    if (type === "clipboard") {
      return (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
          className={styles.contextMenuIcon}
          aria-hidden="true"
        >
          <path stroke="none" d="M0 0h24v24H0z" fill="none" />
          <path d="M9 5h-2a2 2 0 0 0 -2 2v12a2 2 0 0 0 2 2h3m9 -9v-5a2 2 0 0 0 -2 -2h-2" />
          <path d="M13 17v-1a1 1 0 0 1 1 -1h1m3 0h1a1 1 0 0 1 1 1v1m0 3v1a1 1 0 0 1 -1 1h-1m-3 0h-1a1 1 0 0 1 -1 -1v-1" />
          <path d="M9 5a2 2 0 0 1 2 -2h2a2 2 0 0 1 2 2a2 2 0 0 1 -2 2h-2a2 2 0 0 1 -2 -2" />
        </svg>
      );
    }
    if (type === "undo") {
      return (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
          className={styles.contextMenuIcon}
          aria-hidden="true"
        >
          <path stroke="none" d="M0 0h24v24H0z" fill="none" />
          <path d="M9 14l-4 -4l4 -4" />
          <path d="M5 10h10a4 4 0 1 1 0 8h-1" />
        </svg>
      );
    }
    if (type === "photopea") {
      return (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
          className={styles.contextMenuIcon}
          aria-hidden="true"
        >
          <path stroke="none" d="M0 0h24v24H0z" fill="none" />
          <path d="M12 20h9" />
          <path d="M16.5 3.5a2.121 2.121 0 0 1 3 3l-11 11l-4 1l1 -4l11 -11z" />
        </svg>
      );
    }
    if (type === "tag") {
      return (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
          className={styles.contextMenuIcon}
          aria-hidden="true"
        >
          <path stroke="none" d="M0 0h24v24H0z" fill="none" />
          <path d="M7 7h.01" />
          <path d="M3 11l8.586 8.586a2 2 0 0 0 2.828 0l5.172 -5.172a2 2 0 0 0 0 -2.828l-8.586 -8.586h-6v6" />
        </svg>
      );
    }
    if (type === "focuscenter") {
      return (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
          className={styles.contextMenuIcon}
          aria-hidden="true"
        >
          <path stroke="none" d="M0 0h24v24H0z" fill="none" />
          <path d="M11 12a1 1 0 1 0 2 0a1 1 0 1 0 -2 0" />
          <path d="M4 8v-2a2 2 0 0 1 2 -2h2" />
          <path d="M4 16v2a2 2 0 0 0 2 2h2" />
          <path d="M16 4h2a2 2 0 0 1 2 2v2" />
          <path d="M16 20h2a2 2 0 0 0 2 -2v-2" />
        </svg>
      );
    }
    if (type === "background") {
      return (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
          className={styles.contextMenuIcon}
          aria-hidden="true"
        >
          <path stroke="none" d="M0 0h24v24H0z" fill="none" />
          <path d="M4 8l4 -4" />
          <path d="M14 4l-10 10" />
          <path d="M4 20l16 -16" />
          <path d="M20 10l-10 10" />
          <path d="M20 16l-4 4" />
        </svg>
      );
    }
    if (type === "upscale") {
      return (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
          className={styles.contextMenuIcon}
          aria-hidden="true"
        >
          <path stroke="none" d="M0 0h24v24H0z" fill="none" />
          <path d="M16 4l4 0l0 4" />
          <path d="M14 10l6 -6" />
          <path d="M8 20l-4 0l0 -4" />
          <path d="M4 20l6 -6" />
          <path d="M16 20l4 0l0 -4" />
          <path d="M14 14l6 6" />
          <path d="M8 4l-4 0l0 4" />
          <path d="M4 4l6 6" />
        </svg>
      );
    }
    if (type === "eraser") {
      return (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
          className={styles.contextMenuIcon}
          aria-hidden="true"
        >
          <path stroke="none" d="M0 0h24v24H0z" fill="none" />
          <path d="M19 20h-10.5l-4.21 -4.3a1 1 0 0 1 0 -1.41l10 -10a1 1 0 0 1 1.41 0l5 5a1 1 0 0 1 0 1.41l-9.2 9.3" />
          <path d="M18 13.3l-6.3 -6.3" />
        </svg>
      );
    }
    return (
      <svg
        xmlns="http://www.w3.org/2000/svg"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
        className={styles.contextMenuIcon}
        aria-hidden="true"
      >
        <path stroke="none" d="M0 0h24v24H0z" fill="none" />
        <path d="M15 8h.01" />
        <path d="M10 21h-4a3 3 0 0 1 -3 -3v-12a3 3 0 0 1 3 -3h12a3 3 0 0 1 3 3v5" />
        <path d="M3 16l5 -5c.928 -.893 2.072 -.893 3 0l1 1" />
        <path d="M14 21v-4a2 2 0 1 1 4 0v4" />
        <path d="M14 19h4" />
        <path d="M21 15v6" />
      </svg>
    );
  };

  return (
    <div className={styles.page}>
      <Card className={styles.tableCard}>
        <div className={styles.draftToolbarRow}>
          <TabList
            selectedValue={draftTab}
            onTabSelect={(_, data) => setDraftTab(data.value as "spu" | "sku")}
            className={styles.draftToolbarTabs}
          >
            <Tab value="spu">
              <span className={styles.draftTabLabel}>Products</span>
            </Tab>
            <Tab value="sku">
              <span className={styles.draftTabLabel}>
                <span>Variations</span>
                <span className={styles.imageTabMainBadgeSpacer} aria-hidden="true">
                  {" "}
                </span>
                <span className={styles.imageTabBadge}>{skuTabBadgeCount}</span>
              </span>
            </Tab>
          </TabList>
          <Input
            aria-label={t("draftExplorer.searchLabel")}
            value={searchInput}
            onChange={(_, data) => setSearchInput(data.value)}
            placeholder={t("draftExplorer.searchPlaceholder")}
            className={mergeClasses(styles.draftSearch, styles.draftSearchOffset)}
          />
          <Button
            appearance="outline"
            className={mergeClasses(
              styles.draftToolbarActionButton,
              draftTableShowAll ? styles.draftToolbarActionButtonActive : undefined
            )}
            onClick={() => setDraftTableShowAll(true)}
            disabled={!currentImageSpuForDraftFilter || draftTableShowAll}
          >
            Show All
          </Button>
          <div className={styles.draftToolbarActions}>
            <Button
              appearance="outline"
              className={mergeClasses(
                styles.draftToolbarActionButton,
                canGenerateSkusForSelection
                  ? styles.draftToolbarActionButtonNeedsSku
                  : undefined
              )}
              onClick={handleGenerateSkus}
              disabled={!canGenerateSkusForSelection || skuStatus === "running"}
            >
              {skuStatus === "running" ? (
                <span style={{ display: "inline-flex", gap: "6px", alignItems: "center" }}>
                  <Spinner size="tiny" />
                  {t("draftExplorer.generateSkuRunning")}
                </span>
              ) : (
                t("draftExplorer.generateSkuButton")
              )}
            </Button>
            <Button
              appearance="outline"
              className={mergeClasses(
                styles.draftToolbarActionButton,
                styles.draftToolbarFlagButton
              )}
              onClick={openProductNoteDialog}
              disabled={!activeNoteRun || !activeNoteSpu}
              title={
                hasActiveProductNote
                  ? "Flag note added"
                  : "No flag note"
              }
              aria-label={
                hasActiveProductNote
                  ? "Edit product flag note"
                  : "Add product flag note"
              }
            >
              {hasActiveProductNote ? (
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  viewBox="0 0 24 24"
                  fill="currentColor"
                  className={mergeClasses(
                    styles.draftToolbarFlagIcon,
                    styles.draftToolbarFlagIconHasNote
                  )}
                  aria-hidden="true"
                >
                  <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                  <path d="M4 5a1 1 0 0 1 .3 -.714a6 6 0 0 1 8.213 -.176l.351 .328a4 4 0 0 0 5.272 0l.249 -.227c.61 -.483 1.527 -.097 1.61 .676l.005 .113v9a1 1 0 0 1 -.3 .714a6 6 0 0 1 -8.213 .176l-.351 -.328a4 4 0 0 0 -5.136 -.114v6.552a1 1 0 0 1 -1.993 .117l-.007 -.117v-16z" />
                </svg>
              ) : (
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  strokeWidth="2"
                  strokeLinecap="round"
                  strokeLinejoin="round"
                  className={styles.draftToolbarFlagIcon}
                  aria-hidden="true"
                >
                  <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                  <path d="M5 5a5 5 0 0 1 7 0a5 5 0 0 0 7 0v9a5 5 0 0 1 -7 0a5 5 0 0 0 -7 0v-9" />
                  <path d="M5 21v-7" />
                </svg>
              )}
            </Button>
            <Button
              appearance="outline"
              className={mergeClasses(
                styles.draftToolbarActionButton,
                (draftTab === "spu" ? someSpuSelected : someSkuSelected)
                  ? styles.dangerOutlineButton
                  : undefined
              )}
              onClick={handleDeleteRows}
              disabled={
                !(draftTab === "spu" ? someSpuSelected : someSkuSelected) ||
                deleteRowsPending
              }
            >
              {deleteRowsPending ? (
                <span style={{ display: "inline-flex", gap: "6px", alignItems: "center" }}>
                  <Spinner size="tiny" />
                  {t("draftExplorer.deleteRowsRunning")}
                </span>
              ) : (
                t("draftExplorer.deleteRowsButton")
              )}
            </Button>
            <Button
              appearance="outline"
              className={mergeClasses(
                styles.draftToolbarActionButton,
                draftTab === "spu" && someSpuSelected
                  ? styles.draftToolbarActionButtonPrimaryActive
                  : undefined
              )}
              onClick={handleDuplicateProducts}
              disabled={draftTab !== "spu" || !someSpuSelected || duplicateProductPending}
            >
              {duplicateProductPending ? (
                <span style={{ display: "inline-flex", gap: "6px", alignItems: "center" }}>
                  <Spinner size="tiny" />
                  Duplicating...
                </span>
              ) : (
                "Duplicate Product"
              )}
            </Button>
            <Button
              appearance="outline"
              className={mergeClasses(
                styles.draftToolbarActionButton,
                draftTab === "sku" && someSkuSelected
                  ? styles.draftToolbarActionButtonPrimaryActive
                  : undefined
              )}
              onClick={handleDuplicateSkuRoles}
              disabled={draftTab !== "sku" || !someSkuSelected || duplicateRolesPending}
            >
              {duplicateRolesPending ? (
                <span style={{ display: "inline-flex", gap: "6px", alignItems: "center" }}>
                  <Spinner size="tiny" />
                  Duplicating...
                </span>
              ) : (
                "Duplicate row"
              )}
            </Button>
            <Button
              appearance="outline"
              className={mergeClasses(
                styles.draftToolbarActionButton,
                draftTab === "spu" && someSpuSelected
                  ? styles.draftToolbarActionButtonPrimaryActive
                  : undefined
              )}
              onClick={handlePublishDrafts}
              disabled={
                draftTab !== "spu" ||
                !someSpuSelected ||
                selectedPublishHasSkuGaps ||
                publishStatus === "running" ||
                skuStatus === "running" ||
                hasPendingDetailAiRewrite
              }
            >
              {t("draftExplorer.publishButton")}
            </Button>
          </div>
        </div>

        {draftError ? (
          <Text size={200} style={{ color: tokens.colorStatusDangerForeground1 }}>
            {draftError}
          </Text>
        ) : null}
        {publishMessage ? (
          <Text
            size={200}
            style={{
              color:
                publishStatus === "error"
                  ? tokens.colorStatusDangerForeground1
                  : tokens.colorStatusSuccessForeground1,
            }}
          >
            {publishMessage}
          </Text>
        ) : null}
        {skuStatus === "error" && skuMessage ? (
          <Text
            size={200}
            style={{
              color: tokens.colorStatusDangerForeground1,
            }}
          >
            {skuMessage}
          </Text>
        ) : null}
        <div className={styles.tableWrapper}>
          {draftLoading ? (
            <div style={{ padding: "12px" }}>
              <Spinner size="tiny" />
            </div>
          ) : draftTab === "spu" ? (
            <Table size="small" className={styles.contentSizedTable}>
              <TableHeader>
                <TableRow>
                  <TableHeaderCell
                    className={mergeClasses(
                      styles.stickyHeader,
                      styles.resizableHeader,
                      styles.selectionCol
                    )}
                    style={spuColumnStyles.selection}
                  >
                    <Checkbox
                      className={styles.selectionCheckbox}
                      checked={allSpuSelected ? true : someSpuSelected ? "mixed" : false}
                      onChange={toggleSelectAllSpus}
                      aria-label={t("common.selectAll")}
                    />
                  </TableHeaderCell>
                  <TableHeaderCell
                    className={mergeClasses(
                      styles.stickyHeader,
                      styles.resizableHeader,
                      styles.spuCol
                    )}
                    style={spuColumnStyles.spu}
                  >
                    {t("draftExplorer.columns.spu")}
                  </TableHeaderCell>
                  <TableHeaderCell
                    className={mergeClasses(styles.stickyHeader, styles.resizableHeader)}
                    style={spuColumnStyles.title}
                  >
                    {t("draftExplorer.columns.title")}
                  </TableHeaderCell>
                  <TableHeaderCell
                    className={mergeClasses(
                      styles.stickyHeader,
                      styles.resizableHeader,
                      styles.sourceCol
                    )}
                    style={spuColumnStyles.source}
                  >
                    {t("draftExplorer.columns.source")}
                  </TableHeaderCell>
                  <TableHeaderCell
                    className={mergeClasses(
                      styles.stickyHeader,
                      styles.resizableHeader,
                      styles.supplierCol
                    )}
                    style={spuColumnStyles.supplier}
                  >
                    Sources
                  </TableHeaderCell>
                  <TableHeaderCell
                    className={mergeClasses(
                      styles.stickyHeader,
                      styles.resizableHeader,
                      styles.imagesCol
                    )}
                    style={spuColumnStyles.images}
                  >
                    {t("draftExplorer.columns.images")}
                  </TableHeaderCell>
                  <TableHeaderCell
                    className={mergeClasses(
                      styles.stickyHeader,
                      styles.resizableHeader,
                      styles.videosCol
                    )}
                    style={spuColumnStyles.videos}
                  >
                    {t("draftExplorer.columns.videos")}
                  </TableHeaderCell>
                  <TableHeaderCell
                    className={mergeClasses(
                      styles.stickyHeader,
                      styles.resizableHeader,
                      styles.variantsCol
                    )}
                    style={spuColumnStyles.variants}
                  >
                    {t("draftExplorer.columns.variants")}
                  </TableHeaderCell>
                  <TableHeaderCell
                    className={mergeClasses(
                      styles.stickyHeader,
                      styles.resizableHeader,
                      styles.updatedCol
                    )}
                    style={spuColumnStyles.updated}
                  >
                    {t("draftExplorer.columns.updated")}
                  </TableHeaderCell>
                  <TableHeaderCell
                    className={mergeClasses(
                      styles.stickyHeader,
                      styles.resizableHeader,
                      styles.createdCol
                    )}
                    style={spuColumnStyles.created}
                  >
                    {t("draftExplorer.columns.created")}
                  </TableHeaderCell>
                  <TableHeaderCell
                    className={mergeClasses(
                      styles.stickyHeader,
                      styles.resizableHeader,
                      styles.detailsCol
                    )}
                    style={spuColumnStyles.details}
                  >
                    Descriptions
                  </TableHeaderCell>
                  <TableHeaderCell
                    className={mergeClasses(
                      styles.stickyHeader,
                      styles.resizableHeader,
                      styles.jsonCol
                    )}
                    style={spuColumnStyles.json}
                  >
                    JSON
                  </TableHeaderCell>
                </TableRow>
              </TableHeader>
              <TableBody>
                {visibleSpuRows.length === 0 ? (
                  <TableRow>
                    <TableCell colSpan={12}>
                      {t("draftExplorer.empty")}
                    </TableCell>
                  </TableRow>
                ) : (
                  visibleSpuRows.map((row, index) => {
                    const altClass = index % 2 === 1 ? styles.tableRowAlt : undefined;
                    const spuMainFolderPath = resolveSpuMainFolderPath(row);
                    const imagesMarkedCompleted = Boolean(
                      spuMainFolderPath && completedSpuFolders.has(spuMainFolderPath)
                    );
                    const detailsAiRunning = detailBackgroundRegeneratingRowIds.has(row.id);
                    const rowSpuKey = String(row.draft_spu || "").trim().toUpperCase();
                    const variantIssueState =
                      rowSpuKey && variantCriticalIssuesBySpu.has(rowSpuKey)
                        ? variantCriticalIssuesBySpu.get(rowSpuKey) ?? null
                        : null;
                    const missingVariantPrice = Boolean(variantIssueState?.missingPrice);
                    const missingVariantWeight = Boolean(variantIssueState?.missingWeight);
                    const hasVariantCriticalIssue = missingVariantPrice || missingVariantWeight;
                    const variantIssueTitle = hasVariantCriticalIssue
                      ? [
                          missingVariantPrice ? "Missing purchase price" : "",
                          missingVariantWeight ? "Missing weight" : "",
                        ]
                          .filter(Boolean)
                          .join(" • ")
                      : undefined;
                    const rowExternalSourceUrls = getExternalSourceUrlsForRow(row);
                    const rowExternalSourceCount = rowExternalSourceUrls.length;
                    const rowExternalSourcesLoading = Boolean(
                      sourceJsonExternalSourcesLoadingBySpu[rowSpuKey]
                    );
                    const rowRaw = parseDraftRawRow(row.draft_raw_row);
                    const rowReviewRaw =
                      toObjectRecordOrNull(rowRaw[DRAFT_EXPLORER_REVIEW_KEY]) ?? {};
                    const variantsReviewed = Boolean(
                      String(rowReviewRaw[VARIANTS_REVIEWED_AT_KEY] ?? "").trim()
                    );
                    const descriptionsReviewed = Boolean(
                      String(rowReviewRaw[DESCRIPTIONS_REVIEWED_AT_KEY] ?? "").trim()
                    );
                    const rowClass = imagesMarkedCompleted
                      ? styles.tableRowCompleted
                      : altClass;
                    return (
                      <Fragment key={row.id}>
                        <TableRow
                          key={row.id}
                          className={mergeClasses(styles.tableRow, rowClass)}
                        >
                          <TableCell
                            className={mergeClasses(
                              styles.tableCell,
                              styles.selectionCol
                            )}
                            style={spuColumnStyles.selection}
                          >
                            <Checkbox
                              className={styles.selectionCheckbox}
                              checked={selectedSpus.has(row.id)}
                              onChange={() => toggleSelectSpu(row.id)}
                              aria-label={t("common.selectItem", { item: row.draft_spu })}
                            />
                          </TableCell>
                          <TableCell
                            className={mergeClasses(styles.tableCell, styles.spuCol)}
                            style={spuColumnStyles.spu}
                          >
                            <button
                              type="button"
                              className={styles.tableCellLinkButton}
                              onClick={() => openSpuImagesInExplorer(row)}
                              title={row.draft_spu ?? ""}
                            >
                              {row.draft_spu}
                            </button>
                          </TableCell>
                          <TableCell className={styles.tableCell} style={spuColumnStyles.title}>
                            {renderEditableCell(
                              "spu",
                              row.id,
                              "draft_title",
                              row.draft_title,
                              { clamp: true }
                            )}
                          </TableCell>
                          <TableCell
                            className={mergeClasses(styles.tableCell, styles.sourceCol)}
                            style={spuColumnStyles.source}
                          >
                            {renderEditableCell(
                              "spu",
                              row.id,
                              "draft_source",
                              row.draft_source
                            )}
                          </TableCell>
                          <TableCell
                            className={mergeClasses(styles.tableCell, styles.supplierCol)}
                            style={spuColumnStyles.supplier}
                          >
                            <div className={styles.supplierSplitGroup}>
                              <Button
                                size="small"
                                appearance="outline"
                                className={mergeClasses(
                                  styles.tableActionButton,
                                  styles.supplierLinkButton
                                )}
                                onClick={() =>
                                  handleOpenSupplierLink(row.draft_supplier_1688_url)
                                }
                                disabled={!String(row.draft_supplier_1688_url || "").trim()}
                              >
                                Supplier Link
                              </Button>
                              <Menu
                                onOpenChange={(_, data) => {
                                  if (data.open) {
                                    void handleSupplierSourcesMenuOpen(row);
                                  }
                                }}
                              >
                                <MenuTrigger disableButtonEnhancement>
                                  <Button
                                    size="small"
                                    appearance="outline"
                                    className={mergeClasses(
                                      styles.tableActionButton,
                                      styles.supplierMenuButton
                                    )}
                                    aria-label="Source actions"
                                    title="Source actions"
                                  >
                                    <svg
                                      xmlns="http://www.w3.org/2000/svg"
                                      viewBox="0 0 24 24"
                                      fill="none"
                                      stroke="currentColor"
                                      strokeWidth="2"
                                      strokeLinecap="round"
                                      strokeLinejoin="round"
                                      className={styles.supplierMenuIcon}
                                      aria-hidden="true"
                                    >
                                      <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                                      <path d="M6 9l6 6l6 -6" />
                                    </svg>
                                  </Button>
                                </MenuTrigger>
                                <MenuPopover className={styles.supplierMenuPopover}>
                                  <MenuList className={styles.supplierMenuList}>
                                    <MenuItem onClick={() => openSupplierEditor(row)}>
                                      Change Supplier
                                    </MenuItem>
                                    {rowExternalSourcesLoading ? (
                                      <MenuItem disabled>Loading External Sources...</MenuItem>
                                    ) : rowExternalSourceCount > 0 ? (
                                      rowExternalSourceUrls.map((sourceUrl, sourceIndex) => (
                                        <MenuItem
                                          key={`${row.id}-external-source-${sourceIndex}`}
                                          onClick={() => handleOpenSupplierLink(sourceUrl)}
                                        >
                                          {rowExternalSourceCount === 1
                                            ? "External Source"
                                            : `External Source ${sourceIndex + 1}`}
                                        </MenuItem>
                                      ))
                                    ) : (
                                      <MenuItem disabled>External Source</MenuItem>
                                    )}
                                  </MenuList>
                                </MenuPopover>
                              </Menu>
                            </div>
                          </TableCell>
                          <TableCell
                            className={mergeClasses(
                              styles.tableCell,
                              styles.imagesCol
                            )}
                            style={spuColumnStyles.images}
                          >
                            <Button
                              size="small"
                              appearance="outline"
                              className={styles.tableActionButton}
                              onClick={() => openSpuImagesInExplorer(row)}
                            >
                              View
                            </Button>
                          </TableCell>
                          <TableCell
                            className={mergeClasses(
                              styles.tableCell,
                              styles.numericCell,
                              styles.videosCol
                            )}
                            style={spuColumnStyles.videos}
                          >
                            {Number(row.video_count ?? 0) > 0 ? row.video_count : "-"}
                          </TableCell>
                          <TableCell
                            className={mergeClasses(
                              styles.tableCell,
                              styles.variantsCol
                            )}
                            style={spuColumnStyles.variants}
                          >
                            <Button
                              size="small"
                              appearance="outline"
                              className={mergeClasses(
                                styles.tableActionButton,
                                styles.variantEditButton,
                                hasVariantCriticalIssue
                                  ? styles.variantEditButtonCritical
                                  : variantsReviewed
                                    ? styles.tableActionButtonCompleted
                                    : styles.tableActionButtonNeedsReview
                              )}
                              onClick={() => openVariantsEditor(row.draft_spu)}
                              disabled={!row.draft_spu}
                              title={variantIssueTitle}
                            >
                              <span className={styles.variantEditButtonContent}>
                                <span>{`Edit (${row.variant_count ?? 0})`}</span>
                                {hasVariantCriticalIssue ? (
                                  <span
                                    className={styles.variantEditIssueIcons}
                                    aria-label={variantIssueTitle}
                                  >
                                    {missingVariantPrice ? (
                                      <svg
                                        xmlns="http://www.w3.org/2000/svg"
                                        viewBox="0 0 24 24"
                                        fill="none"
                                        stroke="currentColor"
                                        strokeWidth="2"
                                        strokeLinecap="round"
                                        strokeLinejoin="round"
                                        className={styles.variantEditIssueIcon}
                                        aria-hidden="true"
                                      >
                                        <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                                        <path d="M12 19v-7l-5 -7" />
                                        <path d="M17 5l-5 7" />
                                        <path d="M8 13h8" />
                                      </svg>
                                    ) : null}
                                    {missingVariantWeight ? (
                                      <svg
                                        xmlns="http://www.w3.org/2000/svg"
                                        viewBox="0 0 24 24"
                                        fill="none"
                                        stroke="currentColor"
                                        strokeWidth="2"
                                        strokeLinecap="round"
                                        strokeLinejoin="round"
                                        className={styles.variantEditIssueIcon}
                                        aria-hidden="true"
                                      >
                                        <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                                        <path d="M9 6a3 3 0 1 0 6 0a3 3 0 1 0 -6 0" />
                                        <path d="M6.835 9h10.33a1 1 0 0 1 .984 .821l1.637 9a1 1 0 0 1 -.984 1.179h-13.604a1 1 0 0 1 -.984 -1.179l1.637 -9a1 1 0 0 1 .984 -.821" />
                                      </svg>
                                    ) : null}
                                  </span>
                                ) : null}
                              </span>
                            </Button>
                          </TableCell>
                          <TableCell
                            className={mergeClasses(styles.tableCell, styles.updatedCol)}
                            style={spuColumnStyles.updated}
                          >
                            <Text size={100}>{formatDate(row.draft_updated_at)}</Text>
                          </TableCell>
                          <TableCell
                            className={mergeClasses(styles.tableCell, styles.createdCol)}
                            style={spuColumnStyles.created}
                          >
                            <Text size={100}>{formatDate(row.draft_created_at)}</Text>
                          </TableCell>
                          <TableCell
                            className={mergeClasses(styles.tableCell, styles.detailsCol)}
                            style={spuColumnStyles.details}
                          >
                            <Button
                              size="small"
                              appearance="outline"
                              className={mergeClasses(
                                styles.tableActionButton,
                                descriptionsReviewed
                                  ? styles.tableActionButtonCompleted
                                  : styles.tableActionButtonNeedsReview
                              )}
                              onClick={() => openDetails(row)}
                              aria-busy={detailsAiRunning}
                            >
                              {detailsAiRunning ? (
                                <span
                                  style={{
                                    display: "inline-flex",
                                    gap: "6px",
                                    alignItems: "center",
                                  }}
                                >
                                  <Spinner
                                    size="tiny"
                                    className={styles.detailsInlineSpinner}
                                  />
                                  Edit
                                </span>
                              ) : (
                                "Edit"
                              )}
                            </Button>
                          </TableCell>
                          <TableCell
                            className={mergeClasses(styles.tableCell, styles.jsonCol)}
                            style={spuColumnStyles.json}
                          >
                            <Button
                              size="small"
                              appearance="outline"
                              className={styles.tableActionButton}
                              onClick={() => {
                                void handleOpenSpuJsonViewer(row);
                              }}
                            >
                              JSON File
                            </Button>
                          </TableCell>
                        </TableRow>
                      </Fragment>
                    );
                  })
                )}
              </TableBody>
            </Table>
          ) : (
            <Table size="small" className={styles.contentSizedTable}>
              <TableHeader>
                <TableRow>
                  <TableHeaderCell
                    className={mergeClasses(
                      styles.stickyHeader,
                      styles.resizableHeader,
                      styles.selectionCol
                    )}
                    style={skuColumnStyles.selection}
                  >
                    <Checkbox
                      className={styles.selectionCheckbox}
                      checked={allSkuSelected ? true : someSkuSelected ? "mixed" : false}
                      onChange={toggleSelectAllSkus}
                      aria-label={t("common.selectAll")}
                    />
                  </TableHeaderCell>
                  {skuHeaderColumns.map((column) => (
                    <TableHeaderCell
                      key={column.key}
                      className={mergeClasses(
                        styles.stickyHeader,
                        styles.resizableHeader
                      )}
                      style={column.style}
                    >
                      {column.label}
                    </TableHeaderCell>
                  ))}
                </TableRow>
              </TableHeader>
              <TableBody>
                {visibleSkuRows.length === 0 ? (
                  <TableRow>
                    <TableCell colSpan={13}>
                      {t("draftExplorer.empty")}
                    </TableCell>
                  </TableRow>
                ) : (
                  visibleSkuRows.map((row, index) => {
                    const isExpanded = expandedSkus.has(row.id);
                    const altClass = index % 2 === 1 ? styles.tableRowAlt : undefined;
                    const imagesMarkedCompleted = isSpuImagesMarkedCompleted(row.draft_spu);
                    const rowClass = imagesMarkedCompleted
                      ? styles.tableRowCompleted
                      : altClass;
                    const rawRow = row.draft_raw_row ?? {};
                    return (
                      <Fragment key={row.id}>
                        <TableRow
                          key={row.id}
                          className={mergeClasses(styles.tableRow, rowClass)}
                        >
                          <TableCell
                            className={mergeClasses(
                              styles.tableCell,
                              styles.selectionCol
                            )}
                            style={skuColumnStyles.selection}
                          >
                            <Checkbox
                              className={styles.selectionCheckbox}
                              checked={selectedSkus.has(row.id)}
                              onChange={() => toggleSelectSku(row.id)}
                              aria-label={t("common.selectItem", { item: row.draft_sku ?? "" })}
                            />
                          </TableCell>
                          <TableCell className={styles.tableCell} style={skuColumnStyles.sku}>
                            {renderEditableCell(
                              "sku",
                              row.id,
                              "draft_sku",
                              row.draft_sku,
                              { clamp: true }
                            )}
                          </TableCell>
                          <TableCell className={styles.tableCell} style={skuColumnStyles.colorSe}>
                            {renderEditableCell(
                              "sku",
                              row.id,
                              "raw_variation_color_se",
                              getRawValue(rawRow, "variation_color_se"),
                              { clamp: true }
                            )}
                          </TableCell>
                          <TableCell className={styles.tableCell} style={skuColumnStyles.sizeSe}>
                            {renderEditableCell(
                              "sku",
                              row.id,
                              "raw_variation_size_se",
                              getRawValue(rawRow, "variation_size_se"),
                              { clamp: true }
                            )}
                          </TableCell>
                          <TableCell className={styles.tableCell} style={skuColumnStyles.otherSe}>
                            {renderEditableCell(
                              "sku",
                              row.id,
                              "raw_variation_other_se",
                              getRawValue(rawRow, "variation_other_se"),
                              { clamp: true }
                            )}
                          </TableCell>
                          <TableCell className={styles.tableCell} style={skuColumnStyles.amountSe}>
                            {renderEditableCell(
                              "sku",
                              row.id,
                              "raw_variation_amount_se",
                              getRawValue(rawRow, "variation_amount_se"),
                              { clamp: true }
                            )}
                          </TableCell>
                          <TableCell
                            className={styles.tableCell}
                            style={skuColumnStyles.optionCombined}
                          >
                            {renderEditableCell(
                              "sku",
                              row.id,
                              "draft_option_combined_zh",
                              row.draft_option_combined_zh,
                              { clamp: true }
                            )}
                          </TableCell>
                          <TableCell
                            className={mergeClasses(
                              styles.tableCell,
                              styles.numericCell
                            )}
                            style={skuColumnStyles.price}
                          >
                            {renderEditableCell(
                              "sku",
                              row.id,
                              "draft_price",
                              row.draft_price,
                              { numeric: true }
                            )}
                          </TableCell>
                          <TableCell
                            className={mergeClasses(
                              styles.tableCell,
                              styles.numericCell
                            )}
                            style={skuColumnStyles.weight}
                          >
                            {renderEditableCell(
                              "sku",
                              row.id,
                              "draft_weight",
                              row.draft_weight,
                              { numeric: true }
                            )}
                            {row.draft_weight_unit ? ` ${row.draft_weight_unit}` : ""}
                          </TableCell>
                          <TableCell
                            className={styles.tableCell}
                            style={skuColumnStyles.variantImage}
                          >
                            {renderEditableCell(
                              "sku",
                              row.id,
                              "draft_variant_image_url",
                              row.draft_variant_image_url,
                              { clamp: true }
                            )}
                          </TableCell>
                          <TableCell className={styles.tableCell} style={skuColumnStyles.status}>
                            <Text size={200}>{row.draft_status ?? ""}</Text>
                          </TableCell>
                          <TableCell className={styles.tableCell} style={skuColumnStyles.updated}>
                            <Text size={100}>{formatDate(row.draft_updated_at)}</Text>
                          </TableCell>
                          <TableCell className={styles.tableCell} style={skuColumnStyles.details}>
                            <Button
                              size="small"
                              appearance="outline"
                              className={styles.tableActionButton}
                              onClick={() => toggleExpanded(row.id)}
                            >
                              {isExpanded
                                ? t("draftExplorer.collapse")
                                : t("draftExplorer.expand")}
                            </Button>
                          </TableCell>
                        </TableRow>
                        {isExpanded ? (
                          <TableRow className={styles.detailsRow}>
                            <TableCell colSpan={13}>
                              <div className={styles.detailsGrid}>
                                <div className={styles.detailsBlock}>
                                  <Text size={200} weight="semibold">
                                    {t("draftExplorer.details.raw")}
                                  </Text>
                                  <Text size={100}>
                                    {JSON.stringify(row.draft_raw_row ?? {}, null, 2)}
                                  </Text>
                                </div>
                              </div>
                            </TableCell>
                          </TableRow>
                        ) : null}
                      </Fragment>
                    );
                  })
                )}
              </TableBody>
            </Table>
          )}
        </div>
      </Card>

      <Dialog
        open={productNoteDialogOpen}
        onOpenChange={(_, data) => {
          if (!data.open) {
            closeProductNoteDialog();
          }
        }}
      >
        <DialogSurface className={styles.productNoteDialogSurface}>
          <DialogBody className={styles.productNoteDialogBody}>
            <DialogTitle>
              Flag Note
              {activeNoteSpu ? ` - ${activeNoteSpu}` : ""}
            </DialogTitle>
            {productNoteError ? (
              <Text size={200} style={{ color: tokens.colorStatusDangerForeground1 }}>
                {productNoteError}
              </Text>
            ) : null}
            <div className={styles.productNoteCurrentWrap}>
              <Text size={200} weight="semibold">
                Current note
              </Text>
              {activeProductNote && String(activeProductNote.note || "").trim() ? (
                <div className={styles.productNoteCurrentText}>
                  <Text size={200}>{activeProductNote.note}</Text>
                  <Text
                    size={100}
                    style={{
                      display: "block",
                      marginTop: "6px",
                      color: tokens.colorNeutralForeground3,
                    }}
                  >
                    Updated: {formatDateTime(activeProductNote.updatedAt)}
                  </Text>
                </div>
              ) : (
                <Text size={100} style={{ color: tokens.colorNeutralForeground3 }}>
                  No note added.
                </Text>
              )}
            </div>
            <Field label="Note">
              <Textarea
                value={productNoteValue}
                onChange={(_, data) => setProductNoteValue(data.value)}
                placeholder="Add a short problem note for this product."
                className={styles.productNoteTextarea}
                disabled={productNoteLoading || productNoteSaving}
              />
            </Field>
            <DialogActions>
              <Button
                appearance="secondary"
                onClick={closeProductNoteDialog}
                disabled={productNoteSaving}
              >
                Close
              </Button>
              <Button
                appearance="primary"
                onClick={saveProductNote}
                disabled={productNoteLoading || productNoteSaving}
              >
                {productNoteSaving ? (
                  <span
                    style={{
                      display: "inline-flex",
                      gap: "6px",
                      alignItems: "center",
                    }}
                  >
                    <Spinner size="tiny" />
                    Saving...
                  </span>
                ) : (
                  "Save and Close"
                )}
              </Button>
            </DialogActions>
          </DialogBody>
        </DialogSurface>
      </Dialog>

      <Dialog
        open={detailOpen}
        onOpenChange={(_, data) => {
          if (!data.open) {
            closeDetails();
          }
        }}
      >
        <DialogSurface className={styles.detailsDialogSurface}>
          <DialogBody className={styles.detailsDialogBody}>
            <DialogTitle>
              {`Product Descriptions${
                detailTarget?.draft_spu ? ` - ${detailTarget.draft_spu}` : ""
              }`}
            </DialogTitle>
            {detailError ? (
              <Text size={200} style={{ color: tokens.colorStatusDangerForeground1 }}>
                {detailError}
              </Text>
            ) : null}
            <div className={styles.detailsDialogContent}>
              <div className={styles.detailsGallery}>
                <Text size={200} weight="semibold">
                  {t("draftExplorer.detailsDialog.images")}
                </Text>
                {detailImagesLoading ? (
                  <Spinner size="tiny" />
                ) : detailImages.length === 0 ? (
                  <Text size={100}>{t("draftExplorer.detailsDialog.imagesEmpty")}</Text>
                ) : (
                  <div className={styles.detailsGalleryGrid}>
                    {detailImages.map((entry) => (
                      <img
                        key={entry.path}
                        src={buildDraftDownloadUrl(entry.path, entry.modifiedAt)}
                        alt={entry.name}
                        className={styles.detailsGalleryImage}
                      />
                    ))}
                  </div>
                )}
              </div>
              <div className={styles.detailsDialogColumns}>
                <div className={styles.detailsDialogColumn}>
                  <Field label={t("draftExplorer.detailsDialog.shortTitle")}>
                    <Input
                      value={detailDraft.draft_mf_product_short_title ?? ""}
                      onChange={(_, data) =>
                        updateDetailField("draft_mf_product_short_title", data.value)
                      }
                    />
                  </Field>
                  <Field label={t("draftExplorer.detailsDialog.subtitle")}>
                    <Input
                      value={detailDraft.draft_mf_product_subtitle ?? ""}
                      onChange={(_, data) =>
                        updateDetailField("draft_mf_product_subtitle", data.value)
                      }
                    />
                  </Field>
                  <Field label={t("draftExplorer.detailsDialog.longTitle")}>
                    <Input
                      value={detailDraft.draft_mf_product_long_title ?? ""}
                      onChange={(_, data) =>
                        updateDetailField("draft_mf_product_long_title", data.value)
                      }
                    />
                  </Field>
                  <Field label={t("draftExplorer.detailsDialog.bulletsShort")}>
                    <Textarea
                      value={detailDraft.draft_mf_product_bullets_short ?? ""}
                      onChange={(_, data) =>
                        updateDetailField("draft_mf_product_bullets_short", data.value)
                      }
                      resize="vertical"
                      rows={4}
                    />
                  </Field>
                  <Field label={t("draftExplorer.detailsDialog.bullets")}>
                    <Textarea
                      value={detailDraft.draft_mf_product_bullets ?? ""}
                      onChange={(_, data) =>
                        updateDetailField("draft_mf_product_bullets", data.value)
                      }
                      resize="vertical"
                      rows={5}
                    />
                  </Field>
                  <Field label={t("draftExplorer.detailsDialog.bulletsLong")}>
                    <Textarea
                      value={detailDraft.draft_mf_product_bullets_long ?? ""}
                      onChange={(_, data) =>
                        updateDetailField("draft_mf_product_bullets_long", data.value)
                      }
                      resize="vertical"
                      rows={6}
                    />
                  </Field>
                </div>
                <div className={styles.detailsDialogColumn}>
                  <Field label={t("draftExplorer.detailsDialog.descriptionMain")}>
                    <Textarea
                      value={detailDraft.draft_product_description_main_html ?? ""}
                      onChange={(_, data) =>
                        updateDetailField(
                          "draft_product_description_main_html",
                          data.value
                        )
                      }
                      resize="vertical"
                      rows={7}
                    />
                  </Field>
                  <Field label={t("draftExplorer.detailsDialog.descriptionExtended")}>
                    <Textarea
                      value={detailDraft.draft_mf_product_description_extended_html ?? ""}
                      onChange={(_, data) =>
                        updateDetailField(
                          "draft_mf_product_description_extended_html",
                          data.value
                        )
                      }
                      resize="vertical"
                      rows={6}
                    />
                  </Field>
                  <Field label={t("draftExplorer.detailsDialog.descriptionShort")}>
                    <Textarea
                      value={detailDraft.draft_mf_product_description_short_html ?? ""}
                      onChange={(_, data) =>
                        updateDetailField(
                          "draft_mf_product_description_short_html",
                          data.value
                        )
                      }
                      resize="vertical"
                      rows={5}
                    />
                  </Field>
                  <Field label={t("draftExplorer.detailsDialog.specs")}>
                    <Textarea
                      value={detailDraft.draft_mf_product_specs ?? ""}
                      onChange={(_, data) =>
                        updateDetailField("draft_mf_product_specs", data.value)
                      }
                      resize="vertical"
                      rows={7}
                    />
                  </Field>
                </div>
              </div>
            </div>
            <div className={styles.detailsInstruction}>
              <Field label={t("draftExplorer.detailsDialog.instructionLabel")}>
                <Textarea
                  value={detailInstruction}
                  onChange={(_, data) => setDetailInstruction(data.value)}
                  resize="vertical"
                  rows={3}
                />
              </Field>
            </div>
            <DialogActions className={styles.detailsActionsRow}>
              <Button
                appearance="secondary"
                className={styles.detailsCloseButton}
                onClick={closeDetails}
              >
                {t("common.close")}
              </Button>
              <Menu>
                <MenuTrigger disableButtonEnhancement>
                  <Button
                    appearance={
                      detailInstruction.trim().length > 0 &&
                      !detailRegenerating &&
                      !detailSaving
                        ? "primary"
                        : "outline"
                    }
                    disabled={
                      detailRegenerating ||
                      detailSaving ||
                      detailInstruction.trim().length === 0
                    }
                  >
                    {detailRegenerating ? (
                      <span
                        style={{
                          display: "inline-flex",
                          gap: "6px",
                          alignItems: "center",
                        }}
                      >
                        <Spinner size="tiny" className={styles.detailsInlineSpinner} />
                        Editing with AI...
                      </span>
                    ) : (
                      "Edit with AI"
                    )}
                  </Button>
                </MenuTrigger>
                <MenuPopover>
                  <MenuList>
                    <MenuItem
                      disabled={
                        detailRegenerating ||
                        detailSaving ||
                        detailInstruction.trim().length === 0
                      }
                      onClick={() => handleDetailRegenerate("stay")}
                    >
                      Edit and stay open
                    </MenuItem>
                    <MenuItem
                      disabled={
                        detailRegenerating ||
                        detailSaving ||
                        detailInstruction.trim().length === 0
                      }
                      onClick={() => handleDetailRegenerate("close")}
                    >
                      Edit, save, and close
                    </MenuItem>
                  </MenuList>
                </MenuPopover>
              </Menu>
              <Button
                appearance="primary"
                onClick={handleDetailSave}
                disabled={detailSaving}
              >
                {detailSaving ? (
                  <span style={{ display: "inline-flex", gap: "6px", alignItems: "center" }}>
                    <Spinner size="tiny" />
                    {t("draftExplorer.detailsDialog.saving")}
                  </span>
                ) : (
                  t("common.save")
                )}
              </Button>
            </DialogActions>
          </DialogBody>
        </DialogSurface>
      </Dialog>

      <Dialog
        open={variantsEditorOpen}
        onOpenChange={(_, data) => {
          if (!data.open) {
            closeVariantsEditor();
          }
        }}
      >
        <DialogSurface className={styles.variantsEditorSurface}>
          <DialogBody className={styles.variantsEditorBody}>
            <DialogTitle>
              {variantsEditorSpu
                ? `Edit variants for ${variantsEditorSpu}`
                : "Edit variants"}
            </DialogTitle>
            {variantsEditorError ? (
              <Text size={200} style={{ color: tokens.colorStatusDangerForeground1 }}>
                {variantsEditorError}
              </Text>
            ) : null}
            <div
              className={mergeClasses(
                styles.variantsEditorContent,
                variantsEditorAiRunning || variantsEditorSkuRegenerating
                  ? styles.variantsEditorBusy
                  : undefined
              )}
            >
              <div className={styles.variantsEditorToolbar}>
                <div className={styles.variantsEditorToolbarLeft}>
                  <Button
                    appearance="outline"
                    onClick={handleVariantEditorAddRow}
                    disabled={
                      variantsEditorLoading ||
                      variantsEditorSaving ||
                      variantsEditorAiRunning ||
                      variantsEditorSkuRegenerating
                    }
                  >
                    Add Variant
                  </Button>
                  <Button
                    appearance="outline"
                    onClick={handleVariantEditorDeleteSelected}
                    disabled={
                      variantsEditorLoading ||
                      variantsEditorSaving ||
                      variantsEditorAiRunning ||
                      variantsEditorSkuRegenerating ||
                      variantsEditorSelectedRows.size === 0
                    }
                  >
                    Remove Selected
                  </Button>
                  <Input
                    className={styles.variantsEditorPacksInput}
                    value={variantsEditorPacksText}
                    onChange={(_, data) => setVariantsEditorPacksText(data.value)}
                    placeholder="1, 2, 4, 10"
                  />
                  <Button
                    appearance="outline"
                    onClick={handleVariantEditorAddPacks}
                    disabled={
                      variantsEditorLoading ||
                      variantsEditorSaving ||
                      variantsEditorAiRunning ||
                      variantsEditorSkuRegenerating
                    }
                  >
                    Only Add Packs
                  </Button>
                  <Button
                    appearance="outline"
                    onClick={handleVariantEditorRegenerateSkus}
                    disabled={
                      variantsEditorLoading ||
                      variantsEditorSaving ||
                      variantsEditorAiRunning ||
                      variantsEditorSkuRegenerating ||
                      variantsEditorRows.length === 0
                    }
                  >
                    {variantsEditorSkuRegenerating ? "Regenerating..." : "Regenerate SKUs"}
                  </Button>
                </div>
              </div>

              {variantsEditorLoading ? (
                <Spinner size="small" />
              ) : (
                <div className={styles.variantsEditorTableWrap}>
                  <table
                    className={mergeClasses(
                      styles.variantsEditorTable,
                      styles.contentSizedTable
                    )}
                  >
                    <thead>
                      <tr>
                        <th
                          className={mergeClasses(
                            styles.variantsEditorHeadCell,
                            styles.resizableHeader,
                            styles.variantsEditorCheckCol
                          )}
                          style={variantsEditorColumnStyles.selection}
                        >
                          <Checkbox
                            checked={
                              variantsEditorAllSelected
                                ? true
                                : variantsEditorSomeSelected
                                  ? "mixed"
                                  : false
                            }
                            onChange={handleVariantEditorToggleAll}
                            aria-label="Select all variants"
                          />
                        </th>
                        <th
                          className={mergeClasses(
                            styles.variantsEditorHeadCell,
                            styles.resizableHeader
                          )}
                          style={variantsEditorColumnStyles.sku}
                        >
                          <button
                            type="button"
                            className={styles.variantsEditorSortButton}
                            onClick={() => handleVariantEditorSort("sku")}
                          >
                            <span>SKU</span>
                            <span className={styles.variantsEditorSortIndicator}>
                              {getVariantSortIndicator("sku")}
                            </span>
                          </button>
                        </th>
                        <th
                          className={mergeClasses(
                            styles.variantsEditorHeadCell,
                            styles.resizableHeader
                          )}
                          style={variantsEditorColumnStyles.colorSe}
                        >
                          <div className={styles.variantsEditorHeadCellActions}>
                            <button
                              type="button"
                              className={mergeClasses(
                                styles.variantsEditorSortButton,
                                styles.variantsEditorSortButtonInline
                              )}
                              onClick={() => handleVariantEditorSort("color")}
                            >
                              <span>Color (SE)</span>
                              <span className={styles.variantsEditorSortIndicator}>
                                {getVariantSortIndicator("color")}
                              </span>
                            </button>
                            <button
                              type="button"
                              className={styles.variantsEditorClearColumnLink}
                              onClick={() =>
                                handleVariantEditorClearColumn("variation_color_se")
                              }
                              disabled={
                                variantsEditorLoading ||
                                variantsEditorSaving ||
                                variantsEditorAiRunning ||
                                variantsEditorSkuRegenerating
                              }
                            >
                              Clear all
                            </button>
                          </div>
                        </th>
                        <th
                          className={mergeClasses(
                            styles.variantsEditorHeadCell,
                            styles.resizableHeader
                          )}
                          style={variantsEditorColumnStyles.sizeSe}
                        >
                          <div className={styles.variantsEditorHeadCellActions}>
                            <button
                              type="button"
                              className={mergeClasses(
                                styles.variantsEditorSortButton,
                                styles.variantsEditorSortButtonInline
                              )}
                              onClick={() => handleVariantEditorSort("size")}
                            >
                              <span>Size (SE)</span>
                              <span className={styles.variantsEditorSortIndicator}>
                                {getVariantSortIndicator("size")}
                              </span>
                            </button>
                            <button
                              type="button"
                              className={styles.variantsEditorClearColumnLink}
                              onClick={() =>
                                handleVariantEditorClearColumn("variation_size_se")
                              }
                              disabled={
                                variantsEditorLoading ||
                                variantsEditorSaving ||
                                variantsEditorAiRunning ||
                                variantsEditorSkuRegenerating
                              }
                            >
                              Clear all
                            </button>
                          </div>
                        </th>
                        <th
                          className={mergeClasses(
                            styles.variantsEditorHeadCell,
                            styles.resizableHeader
                          )}
                          style={variantsEditorColumnStyles.otherSe}
                        >
                          <div className={styles.variantsEditorHeadCellActions}>
                            <button
                              type="button"
                              className={mergeClasses(
                                styles.variantsEditorSortButton,
                                styles.variantsEditorSortButtonInline
                              )}
                              onClick={() => handleVariantEditorSort("order")}
                            >
                              <span>Other (SE)</span>
                              <span className={styles.variantsEditorSortIndicator}>
                                {getVariantSortIndicator("order")}
                              </span>
                            </button>
                            <button
                              type="button"
                              className={styles.variantsEditorClearColumnLink}
                              onClick={() =>
                                handleVariantEditorClearColumn("variation_other_se")
                              }
                              disabled={
                                variantsEditorLoading ||
                                variantsEditorSaving ||
                                variantsEditorAiRunning ||
                                variantsEditorSkuRegenerating
                              }
                            >
                              Clear all
                            </button>
                          </div>
                        </th>
                        <th
                          className={mergeClasses(
                            styles.variantsEditorHeadCell,
                            styles.resizableHeader
                          )}
                          style={variantsEditorColumnStyles.amountSe}
                        >
                          <div className={styles.variantsEditorHeadCellActions}>
                            <button
                              type="button"
                              className={mergeClasses(
                                styles.variantsEditorSortButton,
                                styles.variantsEditorSortButtonInline
                              )}
                              onClick={() => handleVariantEditorSort("amount")}
                            >
                              <span>Amount (SE)</span>
                              <span className={styles.variantsEditorSortIndicator}>
                                {getVariantSortIndicator("amount")}
                              </span>
                            </button>
                            <button
                              type="button"
                              className={styles.variantsEditorClearColumnLink}
                              onClick={() =>
                                handleVariantEditorClearColumn("variation_amount_se")
                              }
                              disabled={
                                variantsEditorLoading ||
                                variantsEditorSaving ||
                                variantsEditorAiRunning ||
                                variantsEditorSkuRegenerating
                              }
                            >
                              Clear all
                            </button>
                          </div>
                        </th>
                        <th
                          className={mergeClasses(
                            styles.variantsEditorHeadCell,
                            styles.resizableHeader
                          )}
                          style={variantsEditorColumnStyles.optionCombinedZh}
                        >
                          Option Combined (ZH)
                        </th>
                        <th
                          className={mergeClasses(
                            styles.variantsEditorHeadCell,
                            styles.resizableHeader
                          )}
                          style={variantsEditorColumnStyles.colorZh}
                        >
                          Color (ZH)
                        </th>
                        <th
                          className={mergeClasses(
                            styles.variantsEditorHeadCell,
                            styles.resizableHeader
                          )}
                          style={variantsEditorColumnStyles.sizeZh}
                        >
                          Size (ZH)
                        </th>
                        <th
                          className={mergeClasses(
                            styles.variantsEditorHeadCell,
                            styles.resizableHeader
                          )}
                          style={variantsEditorColumnStyles.otherZh}
                        >
                          Other (ZH)
                        </th>
                        <th
                          className={mergeClasses(
                            styles.variantsEditorHeadCell,
                            styles.resizableHeader
                          )}
                          style={variantsEditorColumnStyles.amountZh}
                        >
                          Amount (ZH)
                        </th>
                        <th
                          className={mergeClasses(
                            styles.variantsEditorHeadCell,
                            styles.resizableHeader
                          )}
                          style={variantsEditorColumnStyles.shippingClass}
                        >
                          Shipping class
                        </th>
                        <th
                          className={mergeClasses(
                            styles.variantsEditorHeadCell,
                            styles.resizableHeader
                          )}
                          style={variantsEditorColumnStyles.price}
                        >
                          Price
                        </th>
                        <th
                          className={mergeClasses(
                            styles.variantsEditorHeadCell,
                            styles.resizableHeader
                          )}
                          style={variantsEditorColumnStyles.weight}
                        >
                          Weight
                        </th>
                      </tr>
                    </thead>
                    <tbody>
                      {variantsEditorRows.length === 0 ? (
                        <tr>
                          <td className={styles.variantsEditorCell} colSpan={14}>
                            No variants yet. Add one or run AI update.
                          </td>
                        </tr>
                      ) : (
                        variantsEditorSortedRows.map((row) => (
                          <tr key={row.key}>
                            <td
                              className={mergeClasses(
                                styles.variantsEditorCell,
                                styles.variantsEditorCheckCol
                              )}
                              style={variantsEditorColumnStyles.selection}
                            >
                              <Checkbox
                                checked={variantsEditorSelectedRows.has(row.key)}
                                onChange={() => handleVariantEditorToggleRow(row.key)}
                                aria-label={`Select ${row.draft_sku || row.key}`}
                              />
                            </td>
                            <td
                              className={styles.variantsEditorCell}
                              style={variantsEditorColumnStyles.sku}
                            >
                              {renderVariantEditorInput(row, "draft_sku")}
                            </td>
                            <td
                              className={styles.variantsEditorCell}
                              style={variantsEditorColumnStyles.colorSe}
                            >
                              {renderVariantEditorInput(row, "variation_color_se")}
                            </td>
                            <td
                              className={styles.variantsEditorCell}
                              style={variantsEditorColumnStyles.sizeSe}
                            >
                              {renderVariantEditorInput(row, "variation_size_se")}
                            </td>
                            <td
                              className={styles.variantsEditorCell}
                              style={variantsEditorColumnStyles.otherSe}
                            >
                              {renderVariantEditorInput(row, "variation_other_se")}
                            </td>
                            <td
                              className={styles.variantsEditorCell}
                              style={variantsEditorColumnStyles.amountSe}
                            >
                              {renderVariantEditorInput(row, "variation_amount_se")}
                            </td>
                            <td
                              className={styles.variantsEditorCell}
                              style={variantsEditorColumnStyles.optionCombinedZh}
                            >
                              {renderVariantEditorInput(row, "draft_option_combined_zh")}
                            </td>
                            <td
                              className={styles.variantsEditorCell}
                              style={variantsEditorColumnStyles.colorZh}
                            >
                              {renderVariantEditorInput(row, "draft_option1")}
                            </td>
                            <td
                              className={styles.variantsEditorCell}
                              style={variantsEditorColumnStyles.sizeZh}
                            >
                              {renderVariantEditorInput(row, "draft_option2")}
                            </td>
                            <td
                              className={styles.variantsEditorCell}
                              style={variantsEditorColumnStyles.otherZh}
                            >
                              {renderVariantEditorInput(row, "draft_option3")}
                            </td>
                            <td
                              className={styles.variantsEditorCell}
                              style={variantsEditorColumnStyles.amountZh}
                            >
                              {renderVariantEditorInput(row, "draft_option4")}
                            </td>
                            <td
                              className={styles.variantsEditorCell}
                              style={variantsEditorColumnStyles.shippingClass}
                            >
                              {renderVariantEditorShippingClassSelect(row)}
                            </td>
                            <td
                              className={styles.variantsEditorCell}
                              style={variantsEditorColumnStyles.price}
                            >
                              {renderVariantEditorInput(row, "draft_price", {
                                numeric: true,
                              })}
                            </td>
                            <td
                              className={styles.variantsEditorCell}
                              style={variantsEditorColumnStyles.weight}
                            >
                              {renderVariantEditorInput(row, "draft_weight", {
                                numeric: true,
                              })}
                            </td>
                          </tr>
                        ))
                      )}
                    </tbody>
                  </table>
                </div>
              )}

              <div className={styles.variantsEditorBottomSplit}>
                <div className={styles.variantsEditorInstruction}>
                  <Textarea
                    value={variantsEditorAiPrompt}
                    onChange={(_, data) => setVariantsEditorAiPrompt(data.value)}
                    resize="vertical"
                    rows={5}
                    placeholder="Describe how to restructure, add, or remove variants."
                  />
                  <div className={styles.variantsEditorInstructionActions}>
                    <Button
                      appearance="primary"
                      onClick={handleVariantEditorRunAi}
                      disabled={
                        variantsEditorLoading ||
                        variantsEditorSaving ||
                        variantsEditorAiRunning ||
                        variantsEditorSkuRegenerating ||
                        variantsEditorRows.length === 0
                      }
                    >
                      {variantsEditorAiRunning ? "Updating..." : "Update Variants with AI"}
                    </Button>
                  </div>
                </div>
                <div className={styles.variantsEditorThumbPanel}>
                  <Text size={200} weight="semibold">
                    Variant images
                  </Text>
                  {variantsEditorThumbsLoading ? (
                    <Spinner size="tiny" />
                  ) : variantsEditorThumbs.length === 0 ? (
                    <Text size={100}>No images found in the variant images folder.</Text>
                  ) : (
                    <div className={styles.variantsEditorThumbGrid}>
                      {variantsEditorThumbs.map((entry) => {
                        const label =
                          extractVariantLabelFromFilename(entry.name, variantsEditorSpu) ||
                          entry.name;
                        return (
                          <div key={entry.path} className={styles.variantsEditorThumbCard}>
                            <button
                              type="button"
                              className={styles.variantsEditorThumbButton}
                              onClick={() =>
                                setVariantsImagePreview({
                                  src: buildDraftDownloadUrl(entry.path, entry.modifiedAt),
                                  label,
                                })
                              }
                            >
                              <img
                                src={buildDraftDownloadUrl(entry.path, entry.modifiedAt)}
                                alt={label}
                                className={mergeClasses(
                                  styles.variantsEditorThumbImage,
                                  styles.variantsEditorThumbImageClickable
                                )}
                                loading="lazy"
                              />
                            </button>
                            <Text className={styles.variantsEditorThumbLabel} size={100}>
                              {label}
                            </Text>
                          </div>
                        );
                      })}
                    </div>
                  )}
                </div>
              </div>
            </div>
            {variantsEditorAiRunning || variantsEditorSkuRegenerating ? (
              <div className={styles.variantsEditorOverlay}>
                <span style={{ display: "inline-flex", gap: "8px", alignItems: "center" }}>
                  <Spinner size="small" />
                  {variantsEditorSkuRegenerating
                    ? "Regenerating SKUs..."
                    : "Updating variants with AI..."}
                </span>
              </div>
            ) : null}
            <DialogActions className={styles.variantsEditorActions}>
              <Button
                appearance="outline"
                onClick={() => closeVariantsEditor()}
                disabled={
                  variantsEditorSaving ||
                  variantsEditorAiRunning ||
                  variantsEditorSkuRegenerating
                }
              >
                {t("common.close")}
              </Button>
              <Button
                appearance="primary"
                onClick={handleVariantEditorSave}
                disabled={
                  variantsEditorLoading ||
                  variantsEditorSaving ||
                  variantsEditorAiRunning ||
                  variantsEditorSkuRegenerating
                }
              >
                {variantsEditorSaving ? "Saving..." : t("common.save")}
              </Button>
            </DialogActions>
          </DialogBody>
        </DialogSurface>
      </Dialog>

      <Dialog
        open={Boolean(variantsImagePreview)}
        onOpenChange={(_, data) => {
          if (!data.open) setVariantsImagePreview(null);
        }}
      >
        <DialogSurface className={styles.variantsImagePreviewSurface}>
          <DialogBody className={styles.variantsImagePreviewBody}>
            <div className={styles.variantsImagePreviewTop}>
              <Button
                appearance="subtle"
                size="small"
                onClick={() => setVariantsImagePreview(null)}
              >
                Close
              </Button>
            </div>
            <div className={styles.variantsImagePreviewImgWrap}>
              {variantsImagePreview ? (
                <img
                  src={variantsImagePreview.src}
                  alt={variantsImagePreview.label}
                  className={styles.variantsImagePreviewImg}
                />
              ) : null}
            </div>
          </DialogBody>
        </DialogSurface>
      </Dialog>

	      <Dialog
	        open={runPreviewOpen}
	        onOpenChange={(_, data) => {
	          if (!data.open) {
	            setRunPreviewOpen(false);
	            setRunPreviewRun("");
	            setRunPreviewItems([]);
	            setRunPreviewSelectedSpus(new Set());
	            setRunPreviewDeletedSpus(new Set());
	          }
	        }}
	      >
	        <DialogSurface className={styles.runPreviewSurface}>
	          <DialogBody className={styles.runPreviewBody}>
            <DialogTitle>
              Batch Preview{runPreviewRun ? ` - ${runPreviewRun}` : ""}
            </DialogTitle>
            {runPreviewLoading ? <Spinner size="small" /> : null}
	            <div className={styles.runPreviewTableWrap}>
	              <Table
	                size="small"
	                aria-label="Batch preview"
	                className={styles.runPreviewTable}
	              >
		                <TableHeader>
		                  <TableRow className={styles.runPreviewHeaderRow}>
		                    <TableHeaderCell className={styles.runPreviewImageHeaderCell}>
		                      Image
		                    </TableHeaderCell>
		                    <TableHeaderCell className={styles.runPreviewSpuHeaderCell}>
		                      SPU
		                    </TableHeaderCell>
		                    <TableHeaderCell>Title</TableHeaderCell>
		                    <TableHeaderCell className={styles.runPreviewSelectHeaderCell}>
		                      <div className={styles.runPreviewCellCenter}>
		                        <Checkbox
		                          checked={
		                            allRunPreviewSelected
		                              ? true
		                              : someRunPreviewSelected
		                                ? "mixed"
		                                : false
		                          }
		                          onChange={toggleSelectAllRunPreview}
		                          aria-label={t("common.selectAll")}
		                        />
		                      </div>
		                    </TableHeaderCell>
		                    <TableHeaderCell className={styles.runPreviewDeleteHeaderCell} />
		                  </TableRow>
		                </TableHeader>
		                <TableBody>
		                  {visibleRunPreviewItems.map((item) => (
		                      <TableRow key={item.draft_spu}>
		                        <TableCell className={styles.runPreviewImageCell}>
		                          <div className={styles.runPreviewCellCenter}>
		                            {item.preview_image_path ? (
		                              <img
		                                src={buildDraftDownloadUrl(
		                                  item.preview_image_path,
		                                  item.preview_image_modified_at ?? undefined
		                                )}
		                                alt={item.draft_spu}
		                                className={styles.runPreviewThumb}
		                                loading="lazy"
		                              />
		                            ) : item.draft_main_image_url ? (
		                              <img
		                                src={item.draft_main_image_url}
		                                alt={item.draft_spu}
		                                className={styles.runPreviewThumb}
		                                loading="lazy"
		                              />
		                            ) : (
		                              <div
		                                className={styles.runPreviewThumb}
		                                style={{ display: "inline-block" }}
		                              />
		                            )}
		                          </div>
		                        </TableCell>
		                        <TableCell>{item.draft_spu}</TableCell>
		                        <TableCell className={styles.runPreviewTitleCell}>
		                          <span className={styles.runPreviewTitleText}>{item.title}</span>
		                        </TableCell>
		                        <TableCell className={styles.runPreviewSelectCell}>
		                          <div className={styles.runPreviewCellCenter}>
		                            <Checkbox
		                              checked={runPreviewSelectedSpus.has(item.draft_spu)}
		                              onChange={() => toggleSelectRunPreviewSpu(item.draft_spu)}
		                              aria-label={`Select ${item.draft_spu}`}
		                            />
		                          </div>
		                        </TableCell>
		                        <TableCell className={styles.runPreviewDeleteCell}>
		                          <div className={styles.runPreviewCellCenter}>
		                            <Button
		                              appearance="outline"
		                              size="small"
		                              className={mergeClasses(
		                                styles.runPreviewDeleteButton,
		                                styles.whiteActionButton
		                              )}
		                              onClick={() => handleRunPreviewDelete(item.draft_spu)}
		                            >
		                              Delete
		                            </Button>
		                          </div>
		                        </TableCell>
		                      </TableRow>
		                    ))}
		                </TableBody>
		              </Table>
	            </div>
	            <div className={styles.runPreviewActions}>
	              <Button
	                appearance="outline"
	                className={styles.whiteActionButton}
	                onClick={handleRunPreviewDeleteSelected}
	                disabled={runPreviewSelectedSpus.size === 0 || runPreviewSaving}
	              >
	                Delete selected
	              </Button>
	              <div className={styles.runPreviewActionsRight}>
	                <Button
	                  appearance="outline"
	                  onClick={() => setRunPreviewOpen(false)}
	                  disabled={runPreviewSaving}
	                >
	                  Cancel
	                </Button>
	                <Button
	                  appearance="primary"
	                  onClick={() => void handleRunPreviewSave()}
	                  disabled={runPreviewSaving}
	                >
	                  {runPreviewSaving ? "Saving..." : "Save"}
	                </Button>
	              </div>
	            </div>
	          </DialogBody>
	        </DialogSurface>
      </Dialog>

      <Card className={styles.logCard}>
        {error ? (
          <Text size={200} style={{ color: tokens.colorStatusDangerForeground1 }}>
            {error}
          </Text>
        ) : null}

        <div className={styles.explorerControlsRow}>
          <div className={styles.explorerControlsLeft}>
            <Popover
              open={batchPickerOpen}
              onOpenChange={(_, data) => setBatchPickerOpen(data.open)}
              positioning="below-start"
            >
              <PopoverTrigger disableButtonEnhancement>
                <Button
                  appearance="outline"
                  className={mergeClasses(
                    styles.batchPickerTrigger,
                    styles.explorerWhiteButton
                  )}
                  aria-label={t("bulkProcessing.explorer.selectFolder")}
                  title={t("bulkProcessing.explorer.selectFolder")}
                >
                  <span className={styles.batchPickerTriggerLabel}>
                    {selectedFolder ||
                      t("bulkProcessing.explorer.selectFolder")}
                  </span>
                  <svg
                    xmlns="http://www.w3.org/2000/svg"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    strokeWidth="2"
                    strokeLinecap="round"
                    strokeLinejoin="round"
                    className={styles.batchPickerChevron}
                    aria-hidden="true"
                  >
                    <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                    <path d="M6 9l6 6l6 -6" />
                  </svg>
                </Button>
              </PopoverTrigger>
              <PopoverSurface className={styles.batchPickerSurface}>
                {regularBatchFolders.map((folder) => {
                  const active = folder.path === selectedFolder;
                  const checked = selectedRunsForMerge.has(folder.path);
                  const reEdit = isReEditFolder(folder.path);
                  return (
                    <div
                      key={folder.path}
                      className={mergeClasses(
                        styles.batchPickerRow,
                        active && !reEdit ? styles.batchPickerRowActive : undefined,
                        reEdit ? styles.batchPickerRowReEdit : undefined,
                        reEdit && active ? styles.batchPickerRowReEditActive : undefined
                      )}
                      onClick={(event) => {
                        if (isBatchPickerActionTarget(event.target)) return;
                        handleBatchPickerRunSelect(folder.path);
                      }}
                      role="button"
                      tabIndex={0}
                      onKeyDown={(event) => {
                        if (event.key === "Enter" || event.key === " ") {
                          event.preventDefault();
                          handleBatchPickerRunSelect(folder.path);
                        }
                      }}
                    >
                      <span className={styles.batchPickerRowName}>{folder.name}</span>
                      <Button
                        size="small"
                        appearance="outline"
                        className={mergeClasses(
                          styles.batchPickerViewButton,
                          styles.whiteActionButton
                        )}
                        data-batch-picker-action="true"
                        onClick={(event) => {
                          event.preventDefault();
                          event.stopPropagation();
                          void openRunPreview(folder.path);
                        }}
                      >
                        Preview
                      </Button>
                      <span
                        data-batch-picker-action="true"
                        onPointerDown={(event) => {
                          event.stopPropagation();
                        }}
                        onClick={(event) => {
                          event.stopPropagation();
                        }}
                        onKeyDown={(event) => {
                          event.stopPropagation();
                        }}
                      >
                        <Checkbox
                          checked={checked}
                          onChange={(event, data) => {
                            event.stopPropagation();
                            setRunMergeSelection(
                              folder.path,
                              data.checked === true
                            );
                          }}
                          className={styles.batchPickerMergeCheckbox}
                          data-batch-picker-action="true"
                        />
                      </span>
                    </div>
                  );
                })}
                {archiveBatchFolders.length > 0 ? (
                  <div className={styles.batchPickerArchiveDivider}>
                    {archiveBatchFolders.map((folder) => {
                      const active = folder.path === selectedFolder;
                      return (
                        <div
                          key={folder.path}
                          className={mergeClasses(
                            styles.batchPickerRow,
                            styles.batchPickerRowArchive,
                            active ? styles.batchPickerRowActive : undefined
                          )}
                          onClick={(event) => {
                            if (isBatchPickerActionTarget(event.target)) return;
                            handleBatchPickerRunSelect(folder.path);
                          }}
                          role="button"
                          tabIndex={0}
                          onKeyDown={(event) => {
                            if (event.key === "Enter" || event.key === " ") {
                              event.preventDefault();
                              handleBatchPickerRunSelect(folder.path);
                            }
                          }}
                        >
                          <span className={styles.batchPickerRowName}>{folder.name}</span>
                        </div>
                      );
                    })}
                  </div>
                ) : null}
                <div className={styles.batchPickerActions}>
                  <Button
                    appearance="outline"
                    size="small"
                    onClick={handleSelectAllRunsForMerge}
                    disabled={mergeRunsPending || deleteRunsPending}
                    className={mergeClasses(
                      styles.batchPickerActionButton,
                      styles.whiteActionButton
                    )}
                  >
                    Select All
                  </Button>
                  <Button
                    appearance="outline"
                    size="small"
                    onClick={handleUnselectRunsForMerge}
                    disabled={
                      selectedRunsForMerge.size === 0 ||
                      mergeRunsPending ||
                      deleteRunsPending
                    }
                    className={mergeClasses(
                      styles.batchPickerActionButton,
                      styles.whiteActionButton
                    )}
                  >
                    Unselect
                  </Button>
                  <Button
                    appearance="primary"
                    size="small"
                    disabled={
                      selectedMergeableRunsCount < 2 ||
                      mergeRunsPending ||
                      deleteRunsPending
                    }
                    onClick={() => void handleMergeRuns()}
                    className={styles.batchPickerActionButton}
                  >
                    {mergeRunsPending ? "Merging..." : "Merge"}
                  </Button>
                  <Button
                    appearance="outline"
                    size="small"
                    disabled={
                      selectedMergeableRunsCount === 0 ||
                      mergeRunsPending ||
                      deleteRunsPending
                    }
                    onClick={() => void handleDeleteRuns()}
                    className={mergeClasses(
                      styles.batchPickerActionButton,
                      styles.whiteActionButton
                    )}
                  >
                    {deleteRunsPending ? "Deleting..." : "Delete"}
                  </Button>
                </div>
              </PopoverSurface>
            </Popover>
            <Popover
              open={spuPickerOpen}
              onOpenChange={(_, data) => setSpuPickerOpen(data.open)}
              positioning="below-start"
            >
              <PopoverTrigger disableButtonEnhancement>
                <Button
                  appearance="outline"
                  disabled={!selectedFolder || runSpuOptions.length === 0}
                  className={mergeClasses(
                    styles.spuPickerTrigger,
                    styles.explorerWhiteButton
                  )}
                  style={{
                    width: `${spuPickerTriggerWidthPx}px`,
                    minWidth: `${spuPickerTriggerWidthPx}px`,
                    maxWidth: "92vw",
                  }}
                  aria-label="Select SPU folder"
                  title="Select SPU folder"
                >
                  <span className={styles.batchPickerTriggerLabel}>
                    {selectedRunSpuLabel}
                  </span>
                  <svg
                    xmlns="http://www.w3.org/2000/svg"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    strokeWidth="2"
                    strokeLinecap="round"
                    strokeLinejoin="round"
                    className={styles.batchPickerChevron}
                    aria-hidden="true"
                  >
                    <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                    <path d="M6 9l6 6l6 -6" />
                  </svg>
                </Button>
              </PopoverTrigger>
              <PopoverSurface
                className={mergeClasses(
                  styles.spuPickerSurface,
                  spuPickerMenuShouldScroll
                    ? styles.spuPickerSurfaceScrollable
                    : undefined
                )}
                style={{
                  width: `${spuPickerMenuWidthPx}px`,
                  minWidth: `${spuPickerMenuWidthPx}px`,
                  maxWidth: "92vw",
                }}
              >
                {runSpuOptions.length === 0 ? (
                  <Text size={100}>No SPU folders in this batch.</Text>
                ) : (
                  runSpuOptions.map((option) => {
                    const active = option.path === selectedSpuPathInRun;
                    const isCompleted = completedSpuFolders.has(option.path);
                    const status = runSpuStatusByPath[option.path] ?? {
                      hasPendingAi: false,
                      hasDigi: false,
                      hasTaggedImage: false,
                      hasImageIssue: false,
                      hasUnseenAiUpdate: false,
                    };
                    const hasPendingAi = Boolean(status?.hasPendingAi);
                    const hasDigi = Boolean(status?.hasDigi);
                    const hasTaggedImage = Boolean(status?.hasTaggedImage);
                    const hasImageIssue = Boolean(status?.hasImageIssue);
                    const hasUnseenAiUpdate = Boolean(status?.hasUnseenAiUpdate);
                    const shortTitleLabel =
                      runSpuShortTitleByCode[String(option.name || "").trim().toUpperCase()] ||
                      "";
                    return (
                      <div
                        key={option.path}
                        className={mergeClasses(
                          styles.spuPickerRow,
                          active ? styles.spuPickerRowActive : undefined,
                          isCompleted ? styles.spuPickerRowCompleted : undefined
                        )}
                        onClick={() => handleSelectRunSpu(option.path)}
                        role="button"
                        tabIndex={0}
                        onKeyDown={(event) => {
                          if (event.key === "Enter" || event.key === " ") {
                            event.preventDefault();
                            handleSelectRunSpu(option.path);
                          }
                        }}
                      >
                        <span className={styles.spuPickerRowText}>
                          <span className={styles.spuPickerRowName}>{option.name}</span>
                          {shortTitleLabel ? (
                            <>
                              <span className={styles.spuPickerRowSeparator} aria-hidden="true">
                                {" - "}
                              </span>
                              <span className={styles.spuPickerRowTitleWord}>
                                {shortTitleLabel}
                              </span>
                            </>
                          ) : null}
                        </span>
                        <span className={styles.spuPickerRowIndicators}>
                          <span
                            className={mergeClasses(
                              styles.spuPickerRowIndicatorIcon,
                              hasTaggedImage
                                ? styles.spuPickerRowTagIconPresent
                                : styles.spuPickerRowTagIconMissing
                            )}
                            aria-hidden="true"
                            title={
                              hasTaggedImage
                                ? "One or more images have tags"
                                : "No tagged images in this SPU folder"
                            }
                          >
                            <svg
                              xmlns="http://www.w3.org/2000/svg"
                              viewBox="0 0 24 24"
                              fill="none"
                              stroke="currentColor"
                              strokeWidth="2"
                              strokeLinecap="round"
                              strokeLinejoin="round"
                              className={styles.spuPickerTagSvg}
                              aria-hidden="true"
                            >
                              <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                              <path d="M6.5 7.5a1 1 0 1 0 2 0a1 1 0 1 0 -2 0" />
                              <path d="M3 6v5.172a2 2 0 0 0 .586 1.414l7.71 7.71a2.41 2.41 0 0 0 3.408 0l5.592 -5.592a2.41 2.41 0 0 0 0 -3.408l-7.71 -7.71a2 2 0 0 0 -1.414 -.586h-5.172a3 3 0 0 0 -3 3" />
                            </svg>
                          </span>
                          <span
                            className={mergeClasses(
                              styles.spuPickerRowIndicatorIcon,
                              hasDigi
                                ? styles.spuPickerRowDigiIconPresent
                                : styles.spuPickerRowDigiIconMissing
                            )}
                            aria-hidden="true"
                            title={
                              hasDigi
                                ? "DIGI tagged image found"
                                : "Missing DIGI tagged image"
                            }
                          >
                            <svg
                              xmlns="http://www.w3.org/2000/svg"
                              viewBox="0 0 24 24"
                              fill="none"
                              stroke="currentColor"
                              strokeWidth="2"
                              strokeLinecap="round"
                              strokeLinejoin="round"
                              className={styles.spuPickerIndicatorSvg}
                              aria-hidden="true"
                            >
                              <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                              <path d="M4 10a2 2 0 0 1 2 -2h12a2 2 0 0 1 2 2v4a2 2 0 0 1 -2 2h-12a2 2 0 0 1 -2 -2l0 -4" />
                            </svg>
                          </span>
                          {hasImageIssue ? (
                            <span
                              className={mergeClasses(
                                styles.spuPickerRowIndicatorIcon,
                                styles.spuPickerRowImageIssueIcon
                              )}
                              aria-hidden="true"
                              title="Image issue: low size, low quality score, or non-square format"
                            >
                              <svg
                                xmlns="http://www.w3.org/2000/svg"
                                viewBox="0 0 24 24"
                                fill="none"
                                stroke="currentColor"
                                strokeWidth="2"
                                strokeLinecap="round"
                                strokeLinejoin="round"
                                className={styles.spuPickerIndicatorSvg}
                                aria-hidden="true"
                              >
                                <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                                <path d="M15 8h.01" />
                                <path d="M15 21h-9a3 3 0 0 1 -3 -3v-12a3 3 0 0 1 3 -3h12a3 3 0 0 1 3 3v6" />
                                <path d="M3 16l5 -5c.928 -.893 2.072 -.893 3 0l4 4" />
                                <path d="M14 14l1 -1c.665 -.64 1.44 -.821 2.167 -.545" />
                                <path d="M19 16v3" />
                                <path d="M19 22v.01" />
                              </svg>
                            </span>
                          ) : null}
                          {hasPendingAi ? (
                            <span
                              className={mergeClasses(
                                styles.spuPickerRowIndicatorIcon,
                                styles.spuPickerRowAiIcon
                              )}
                              aria-hidden="true"
                              title="Pending AI image decisions"
                            >
                              <svg
                                xmlns="http://www.w3.org/2000/svg"
                                viewBox="0 0 24 24"
                                fill="none"
                                stroke="currentColor"
                                strokeWidth="2"
                                strokeLinecap="round"
                                strokeLinejoin="round"
                                className={styles.spuPickerIndicatorSvg}
                                aria-hidden="true"
                              >
                                <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                                <path d="M15 8h.01" />
                                <path d="M10 21h-4a3 3 0 0 1 -3 -3v-12a3 3 0 0 1 3 -3h12a3 3 0 0 1 3 3v5" />
                                <path d="M3 16l5 -5c.928 -.893 2.072 -.893 3 0l1 1" />
                                <path d="M14 21v-4a2 2 0 1 1 4 0v4" />
                                <path d="M14 19h4" />
                                <path d="M21 15v6" />
                              </svg>
                            </span>
                          ) : null}
                          {hasUnseenAiUpdate ? (
                            <span
                              className={mergeClasses(
                                styles.spuPickerRowIndicatorIcon,
                                styles.spuPickerRowAiNeedsReviewIcon
                              )}
                              aria-hidden="true"
                              title="AI updates available to review"
                            >
                              <svg
                                xmlns="http://www.w3.org/2000/svg"
                                viewBox="0 0 24 24"
                                fill="none"
                                stroke="currentColor"
                                strokeWidth="2"
                                strokeLinecap="round"
                                strokeLinejoin="round"
                                className={styles.spuPickerIndicatorSvg}
                                aria-hidden="true"
                              >
                                <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                                <path d="M10 12a2 2 0 1 0 4 0a2 2 0 0 0 -4 0" />
                                <path d="M11.102 17.957c-3.204 -.307 -5.904 -2.294 -8.102 -5.957c2.4 -4 5.4 -6 9 -6c3.6 0 6.6 2 9 6a19.5 19.5 0 0 1 -.663 1.032" />
                                <path d="M15 19l2 2l4 -4" />
                              </svg>
                            </span>
                          ) : null}
                        </span>
                      </div>
                    );
                  })
                )}
              </PopoverSurface>
            </Popover>
            <Menu
              open={explorerActionsMenuOpen}
              onOpenChange={(_, data) => setExplorerActionsMenuOpen(data.open)}
            >
              <MenuTrigger disableButtonEnhancement>
                <Button
                  appearance="outline"
                  disabled={
                    !selectedFolder ||
                    tagAllImagesRunning ||
                    tagImagesRunning ||
                    bulkImageActionPending ||
                    publishStatus === "running" ||
                    deleteFolderPending
                  }
                  className={mergeClasses(
                    styles.explorerWhiteButton,
                    styles.actionMenuTriggerButton
                  )}
                >
                  <span className={styles.actionMenuTriggerContent}>
                    <span>{tagAllImagesRunning ? "Working..." : "Actions"}</span>
                    <svg
                      xmlns="http://www.w3.org/2000/svg"
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      strokeWidth="2"
                      strokeLinecap="round"
                      strokeLinejoin="round"
                      className={styles.actionMenuCaretIcon}
                      aria-hidden="true"
                    >
                      <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                      <path d="M6 9l6 6l6 -6" />
                    </svg>
                  </span>
                </Button>
              </MenuTrigger>
              <MenuPopover>
                <MenuList className={styles.compactMenuList}>
                  <MenuItem
                    disabled={
                      !selectedFolder ||
                      completedSpuPathsInSelectedRun.length === 0 ||
                      tagAllImagesRunning ||
                      tagImagesRunning ||
                      bulkImageActionPending
                    }
                    onClick={() => void handleTagAllImagesForCompletedSpuFolders()}
                  >
                    Tag All Images
                  </MenuItem>
                  <MenuItem
                    disabled={
                      !selectedFolder ||
                      currentBatchSpuCount === 0 ||
                      currentBatchHasSkuGaps ||
                      publishStatus === "running" ||
                      skuStatus === "running" ||
                      hasPendingDetailAiRewrite
                    }
                    onClick={() => void handlePublishCurrentBatchDrafts()}
                  >
                    {`${t("draftExplorer.publishButton")} (${currentBatchSpuCount})`}
                  </MenuItem>
                  <MenuItem
                    disabled={
                      !selectedFolder || deleteFolderPending || isSelectedFolderArchive
                    }
                    onClick={() => void handleDeleteFolder()}
                  >
                    {deleteFolderPending
                      ? "Deleting..."
                      : `Delete draft folder (${currentBatchSpuCount})`}
                  </MenuItem>
                </MenuList>
              </MenuPopover>
            </Menu>
            <Button
              appearance="outline"
              onClick={() => setFolderTreeHidden((prev) => !prev)}
              className={mergeClasses(styles.iconButton, styles.explorerWhiteButton)}
              disabled={!USE_NEW_FILE_EXPLORER}
              aria-label={folderTreeHidden ? "Show folders" : "Hide folders"}
              title={folderTreeHidden ? "Show folders" : "Hide folders"}
            >
              {folderTreeHidden ? (
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  strokeWidth="2"
                  strokeLinecap="round"
                  strokeLinejoin="round"
                  className={styles.iconSvg}
                  aria-hidden="true"
                >
                  <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                  <path d="M8 4h1l3 3h7a2 2 0 0 1 2 2v8m-2 2h-14a2 2 0 0 1 -2 -2v-11a2 2 0 0 1 1.189 -1.829" />
                  <path d="M3 3l18 18" />
                </svg>
              ) : (
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  strokeWidth="2"
                  strokeLinecap="round"
                  strokeLinejoin="round"
                  className={styles.iconSvg}
                  aria-hidden="true"
                >
                  <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                  <path d="M5 4h4l3 3h7a2 2 0 0 1 2 2v8a2 2 0 0 1 -2 2h-14a2 2 0 0 1 -2 -2v-11a2 2 0 0 1 2 -2" />
                </svg>
              )}
            </Button>
            <Button
              appearance="outline"
              onClick={() => setFilesPaneHidden((prev) => !prev)}
              className={mergeClasses(styles.iconButton, styles.explorerWhiteButton)}
              aria-label={filesPaneHidden ? "Show files" : "Hide files"}
              title={filesPaneHidden ? "Show files" : "Hide files"}
            >
              {filesPaneHidden ? (
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  strokeWidth="2"
                  strokeLinecap="round"
                  strokeLinejoin="round"
                  className={styles.iconSvg}
                  aria-hidden="true"
                >
                  <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                  <path d="M15 3v4a1 1 0 0 0 1 1h4" />
                  <path d="M17 17h-6a2 2 0 0 1 -2 -2v-6m0 -4a2 2 0 0 1 2 -2h4l5 5v7c0 .294 -.063 .572 -.177 .823" />
                  <path d="M16 17v2a2 2 0 0 1 -2 2h-7a2 2 0 0 1 -2 -2v-10a2 2 0 0 1 2 -2" />
                  <path d="M3 3l18 18" />
                </svg>
              ) : (
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  strokeWidth="2"
                  strokeLinecap="round"
                  strokeLinejoin="round"
                  className={styles.iconSvg}
                  aria-hidden="true"
                >
                  <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                  <path d="M15 3v4a1 1 0 0 0 1 1h4" />
                  <path d="M18 17h-7a2 2 0 0 1 -2 -2v-10a2 2 0 0 1 2 -2h4l5 5v7a2 2 0 0 1 -2 2" />
                  <path d="M16 17v2a2 2 0 0 1 -2 2h-7a2 2 0 0 1 -2 -2v-10a2 2 0 0 1 2 -2h2" />
                </svg>
              )}
            </Button>
          </div>
          <div className={styles.explorerControlsRight}>
            {USE_NEW_FILE_EXPLORER ? (
              <>
                <Button
                  appearance="outline"
                  onClick={() => handleNavigateRunSpu(-1)}
                  disabled={!canNavigatePrevSpu}
                  className={mergeClasses(styles.iconButton, styles.explorerWhiteButton)}
                  aria-label="Previous SPU folder"
                  title="Previous SPU folder"
                >
                  <svg
                    xmlns="http://www.w3.org/2000/svg"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    strokeWidth="2"
                    strokeLinecap="round"
                    strokeLinejoin="round"
                    className={styles.iconSvg}
                    aria-hidden="true"
                  >
                    <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                    <path d="M5 12h14" />
                    <path d="M5 12l6 6" />
                    <path d="M5 12l6 -6" />
                  </svg>
                </Button>
                <Button
                  appearance="outline"
                  onClick={() => handleNavigateRunSpu(1)}
                  disabled={!canNavigateNextSpu}
                  className={mergeClasses(styles.iconButton, styles.explorerWhiteButton)}
                  aria-label="Next SPU folder"
                  title="Next SPU folder"
                >
                  <svg
                    xmlns="http://www.w3.org/2000/svg"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    strokeWidth="2"
                    strokeLinecap="round"
                    strokeLinejoin="round"
                    className={styles.iconSvg}
                    aria-hidden="true"
                  >
                    <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                    <path d="M5 12h14" />
                    <path d="M13 6l6 6" />
                    <path d="M13 18l6 -6" />
                  </svg>
                </Button>
              </>
            ) : null}
            {!USE_NEW_FILE_EXPLORER ? (
              <div className={styles.viewToggle}>
                <Text size={100}>{t("bulkProcessing.explorer.viewSmall")}</Text>
                <Switch
                  checked={explorerView === "grid"}
                  onChange={(_, data) =>
                    setExplorerView(data.checked ? "grid" : "list")
                  }
                />
                <Text size={100}>{t("bulkProcessing.explorer.viewLarge")}</Text>
              </div>
            ) : null}
          </div>
        </div>

        {USE_NEW_FILE_EXPLORER ? (
          <div
            className={mergeClasses(
              styles.explorerLayout,
              folderTreeHidden ? styles.explorerLayoutSingleColumn : undefined
            )}
          >
            {!folderTreeHidden ? (
            <div
              className={styles.folderPane}
              onDragOver={(event) => {
                if (draggingEntryPaths.length === 0 || !selectedFolder) return;
                event.preventDefault();
                setFolderDropTargetPath(selectedFolder);
              }}
              onDragLeave={() => {
                if (folderDropTargetPath === selectedFolder) {
                  setFolderDropTargetPath(null);
                }
              }}
              onDrop={(event) => {
                if (!selectedFolder) return;
                event.preventDefault();
                const draggedPaths = readDraggedPaths(event.dataTransfer);
                if (
                  draggedPaths.length === 0 ||
                  draggedPaths.every((draggedPath) => draggedPath === selectedFolder)
                ) {
                  setFolderDropTargetPath(null);
                  setDraggingEntryPaths([]);
                  return;
                }
                handleMoveEntriesToFolder(draggedPaths, selectedFolder);
              }}
            >
              <div className={styles.folderPaneHeader}>
                <Text size={200} weight="semibold">
                  Folders
                </Text>
                {folderTreeLoading || movingEntry ? <Spinner size="tiny" /> : null}
              </div>
              {!selectedFolder ? (
                <Text size={100}>Select a folder to browse.</Text>
              ) : folderTreeLoading && !folderTree ? (
                <Spinner size="tiny" />
              ) : folderTree ? (
                <div className={styles.folderTreeRoot}>
                  {folderTree.children.length > 0 ? (
                    folderTree.children.map((child, index) =>
                      renderFolderTreeNode(
                        child,
                        [],
                        index === folderTree.children.length - 1
                      )
                    )
                  ) : (
                    <Text size={100}>No subfolders in this folder.</Text>
                  )}
                </div>
              ) : (
                <Text size={100}>Unable to load folder tree.</Text>
              )}
              <div className={styles.folderPaneActions}>
                <Button
                  appearance="outline"
                  onClick={handleDownloadAllFolders}
                  disabled={!selectedFolder}
                  className={styles.explorerWhiteButton}
                >
                  Download all
                </Button>
                <Button
                  appearance="outline"
                  onClick={handleDownloadSelectedZip}
                  disabled={selectedTreeFolders.size === 0}
                  className={styles.explorerWhiteButton}
                >
                  <span className={styles.iconWithZipLabel}>
                    <svg
                      xmlns="http://www.w3.org/2000/svg"
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      strokeWidth="2"
                      strokeLinecap="round"
                      strokeLinejoin="round"
                      className={styles.iconSvg}
                      aria-hidden="true"
                    >
                      <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                      <path d="M4 17v2a2 2 0 0 0 2 2h12a2 2 0 0 0 2 -2v-2" />
                      <path d="M7 11l5 5l5 -5" />
                      <path d="M12 4l0 12" />
                    </svg>
                    <span>(ZIP)</span>
                  </span>
                </Button>
                <Button
                  appearance="outline"
                  onClick={handleDeleteSelectedFolders}
                  disabled={selectedTreeFolders.size === 0}
                  className={mergeClasses(styles.iconButton, styles.explorerWhiteButton)}
                  aria-label={t("bulkProcessing.explorer.deleteSelected")}
                  title={t("bulkProcessing.explorer.deleteSelected")}
                >
                  <svg
                    xmlns="http://www.w3.org/2000/svg"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    strokeWidth="2"
                    strokeLinecap="round"
                    strokeLinejoin="round"
                    className={styles.iconSvg}
                    aria-hidden="true"
                  >
                    <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                    <path d="M4 7l16 0" />
                    <path d="M10 11l0 6" />
                    <path d="M14 11l0 6" />
                    <path d="M5 7l1 12a2 2 0 0 0 2 2h8a2 2 0 0 0 2 -2l1 -12" />
                    <path d="M9 7v-3a1 1 0 0 1 1 -1h4a1 1 0 0 1 1 1v3" />
                  </svg>
                </Button>
              </div>
            </div>
            ) : null}

            <div className={styles.contentColumn}>
            <div
              className={mergeClasses(
                styles.filePane,
                isSelectedFolderReEdit ? styles.filePaneReEdit : undefined
              )}
            >
              <div className={styles.imageToolbar}>
                <div className={styles.imageToolbarTabs}>
                  <TabList
                    size="small"
                    selectedValue={selectedImageTabValue}
                    onTabSelect={(_, data) => {
                      const value = data.value as ImageFolderTabValue;
                      if (value === "variants") {
                        setMainImageViewFilter("var_only");
                      } else if (value === "sizecharts") {
                        setMainImageViewFilter("size_only");
                      } else {
                        setMainImageViewFilter("all");
                      }
                      const nextPath =
                        value === "main" ||
                        value === "variants" ||
                        value === "sizecharts"
                          ? imageTabTargets.mainPath
                          : value === "downloaded"
                            ? imageTabTargets.downloadedPath
                          : value === "ocr"
                            ? imageTabTargets.ocrPath
                            : value === "others"
                              ? imageTabTargets.othersPaths[0] ?? null
                              : null;
                      if (nextPath) setCurrentPath(nextPath);
                    }}
                  >
                    <Tab value="main" disabled={mainTabDisabled}>
                      <span className={styles.imageTabLabel}>
                        <span>Main</span>
                        <span
                          className={styles.imageTabMainBadgeSpacer}
                          aria-hidden="true"
                        >
                          {" "}
                        </span>
                        <span
                          className={mergeClasses(
                            styles.imageTabBadge,
                            mainTabBadgeCount === 0
                              ? styles.imageTabBadgeDisabled
                              : undefined
                          )}
                        >
                          {mainTabBadgeCount}
                        </span>
                      </span>
                    </Tab>
                    <Tab value="variants" disabled={variantsTabDisabled}>
                      <span className={styles.imageTabLabel}>
                        <span>Variants</span>
                        <span
                          className={mergeClasses(
                            styles.imageTabBadge,
                            variantsTabBadgeCount === 0
                              ? styles.imageTabBadgeDisabled
                              : undefined
                          )}
		                        >
		                          {variantsTabBadgeCount}
		                        </span>
		                      </span>
		                    </Tab>
                    <Tab value="sizecharts" disabled={sizeChartsTabDisabled}>
                      <span className={styles.imageTabLabel}>
                        <span>Size Charts</span>
                        <span
                          className={mergeClasses(
                            styles.imageTabBadge,
                            sizeChartsTabBadgeCount === 0
                              ? styles.imageTabBadgeDisabled
                              : undefined
                          )}
                        >
                          {sizeChartsTabBadgeCount}
                        </span>
                      </span>
                    </Tab>
                    <Tab value="others" disabled={othersTabDisabled}>
                      <span className={styles.imageTabLabel}>
                        <span>Others</span>
                        <span
                          className={mergeClasses(
                            styles.imageTabBadge,
                            othersTabBadgeCount === 0
                              ? styles.imageTabBadgeDisabled
                              : undefined
                          )}
                        >
                          {othersTabBadgeCount}
                        </span>
                      </span>
                    </Tab>
                    <Tab value="ocr" disabled={ocrTabDisabled}>
                      <span className={styles.imageTabLabel}>
                        <span>OCR</span>
                        <span
                          className={mergeClasses(
                            styles.imageTabBadge,
                            ocrTabBadgeCount === 0
                              ? styles.imageTabBadgeDisabled
                              : undefined
                          )}
                        >
                          {ocrTabBadgeCount}
                        </span>
                      </span>
                    </Tab>
                    <Tab value="downloaded" disabled={downloadedTabDisabled}>
                      <span className={styles.imageTabLabel}>
                        <span>Downloaded</span>
                        <span
                          className={mergeClasses(
                            styles.imageTabBadge,
                            downloadedTabBadgeCount === 0
                              ? styles.imageTabBadgeDisabled
                              : undefined
                          )}
                        >
                          {downloadedTabBadgeCount}
                        </span>
                      </span>
                    </Tab>
                  </TabList>
                </div>

                <div className={styles.imageToolbarRight}>
                  {mainViewShowsMergedImages && currentMainProductContext.spu ? (
                    <Button
                      appearance="outline"
                      disabled={
                        confirmAiEditsPending ||
                        deleteProductPending ||
                        aiReviewSubmitting ||
                        movingEntry ||
                        confirmablePendingAiPaths.length === 0
                      }
                      className={mergeClasses(
                        styles.imageToolbarActions,
                        styles.explorerWhiteButton
                      )}
                      onClick={() => void handleConfirmCurrentProductAiEdits()}
                    >
                      {confirmAiEditsPending ? "Confirming..." : "Confirm AI Edits"}
                    </Button>
                  ) : null}
                  <Menu
                    open={imageActionsMenuOpen}
                    onOpenChange={(_, data) => setImageActionsMenuOpen(data.open)}
                  >
                    <MenuTrigger disableButtonEnhancement>
                      <Button
                        appearance="outline"
                        disabled={
                          bulkImageActionPending || addImagesSubmitting || deleteProductPending
                        }
                        className={mergeClasses(
                          styles.imageToolbarActions,
                          styles.explorerWhiteButton,
                          styles.actionMenuTriggerButton
                        )}
                      >
                        <span className={styles.actionMenuTriggerContent}>
                          <span>
                            {bulkImageActionPending || addImagesSubmitting || deleteProductPending
                              ? "Working..."
                              : "Actions"}
                          </span>
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            strokeWidth="2"
                            strokeLinecap="round"
                            strokeLinejoin="round"
                            className={styles.actionMenuCaretIcon}
                            aria-hidden="true"
                          >
                            <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                            <path d="M6 9l6 6l6 -6" />
                          </svg>
                        </span>
                      </Button>
                    </MenuTrigger>
                    <MenuPopover>
                      <MenuList className={styles.compactMenuList}>
                        <MenuItem
                          disabled={
                            !currentPath ||
                            bulkImageActionPending ||
                            addImagesSubmitting ||
                            deleteProductPending
                          }
                          onClick={() => setAddImagesDialogOpen(true)}
                        >
                          Add Images
                        </MenuItem>
                        <MenuItem
                          disabled={
                            importEditsSubmitting ||
                            bulkImageActionPending ||
                            addImagesSubmitting ||
                            deleteProductPending
                          }
                          onClick={() => {
                            setExcelFile(null);
                            setZipFile(null);
                            setExcelStatus("idle");
                            setZipStatus("idle");
                            setImportEditsDialogOpen(true);
                            if (importEditsExcelInputRef.current) {
                              importEditsExcelInputRef.current.value = "";
                            }
                            if (importEditsZipInputRef.current) {
                              importEditsZipInputRef.current.value = "";
                            }
                          }}
                        >
                          Import Edits
                        </MenuItem>
                        <MenuItem
                          disabled={
                            !undoLastChangeAvailable ||
                            bulkImageActionPending ||
                            addImagesSubmitting ||
                            deleteProductPending
                          }
                          onClick={handleUndoLastChangeFromImageActions}
                        >
                          Undo Last Change
                        </MenuItem>
                        <MenuItem
                          disabled={
                            !activeProductContext.spu ||
                            confirmAiEditsPending ||
                            deleteProductPending ||
                            aiReviewSubmitting ||
                            movingEntry ||
                            bulkImageActionPending ||
                            addImagesSubmitting
                          }
                          onClick={() => void handleDeleteCurrentProduct()}
                        >
                          {deleteProductPending ? "Deleting..." : "Delete product"}
                        </MenuItem>
                        <MenuItem
                          disabled={
                            selectedImageEntries.length === 0 || bulkImageActionPending
                          }
                          onClick={() => handleDownloadEntries(selectedImageEntries)}
                        >
                          Download Selected Images
                        </MenuItem>
                        <MenuItem
                          disabled={
                            selectedImageEntries.length === 0 || bulkImageActionPending
                          }
                          onClick={() => handleCreateCopiesForEntries(selectedImageEntries)}
                        >
                          Create Copy
                        </MenuItem>
                        <MenuItem
                          disabled={
                            selectedImageEntries.length === 0 ||
                            bulkImageActionPending ||
                            addImagesSubmitting ||
                            deleteProductPending
                          }
                          onClick={() =>
                            void handleDeleteSelected(
                              selectedImageEntries.map((entry) => entry.path)
                            )
                          }
                        >
                          Delete Selected Images
                        </MenuItem>
                      </MenuList>
                    </MenuPopover>
                  </Menu>
                  <Button
                    appearance={tagImagesRunningInCurrentPath ? "primary" : "outline"}
                    disabled={
                      imageEntries.length === 0 ||
                      bulkImageActionPending ||
                      tagImagesRunning ||
                      tagAllImagesRunning ||
                      !currentPath
                    }
                    className={mergeClasses(
                      styles.imageToolbarActions,
                      tagImagesRunningInCurrentPath
                        ? undefined
                        : styles.explorerWhiteButton
                    )}
                    onClick={() => void handleRunImageClassifierTagging()}
                  >
                    {tagImagesRunningInCurrentPath ? "Tagging..." : "Tag Images"}
                  </Button>
                  <Button
                    appearance={currentSpuMarkedCompleted ? "primary" : "outline"}
                    disabled={!currentSpuMainPath}
                    className={mergeClasses(
                      styles.imageToolbarActions,
                      currentSpuMarkedCompleted ? undefined : styles.explorerWhiteButton
                    )}
                    onClick={handleToggleCurrentSpuCompleted}
                  >
                    {currentSpuMarkedCompleted ? "Uncomplete" : "Completed"}
                  </Button>
                  <div className={styles.imageToolbarIconGroup}>
                    {entriesRefreshing ? <Spinner size="tiny" /> : null}
                    <Button
                      appearance="outline"
                      onClick={handleDownloadSelectedIndividually}
                      disabled={selectedFiles.size === 0}
                      className={mergeClasses(styles.iconButton, styles.explorerWhiteButton)}
                      aria-label={t("bulkProcessing.explorer.downloadSelected")}
                      title={t("bulkProcessing.explorer.downloadSelected")}
                    >
                      <svg
                        xmlns="http://www.w3.org/2000/svg"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        strokeWidth="2"
                        strokeLinecap="round"
                        strokeLinejoin="round"
                        className={styles.iconSvg}
                        aria-hidden="true"
                      >
                        <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                        <path d="M4 17v2a2 2 0 0 0 2 2h12a2 2 0 0 0 2 -2v-2" />
                        <path d="M7 11l5 5l5 -5" />
                        <path d="M12 4l0 12" />
                      </svg>
                    </Button>
                  </div>
                  <div className={styles.imageToolbarIconGroup}>
                    <Button
                      appearance="outline"
                      className={mergeClasses(styles.iconButton, styles.explorerWhiteButton)}
                      disabled={displayImageEntries.length === 0 || imageViewMode === "big"}
                      aria-label="Make images bigger"
                      title="Make images bigger"
                      onClick={() => {
                        setImageViewMode("big");
                      }}
                    >
                      <svg
                        xmlns="http://www.w3.org/2000/svg"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        strokeWidth="2"
                        strokeLinecap="round"
                        strokeLinejoin="round"
                        className={styles.imageResizeIconSvg}
                        aria-hidden="true"
                      >
                        <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                        <path d="M3 17a1 1 0 0 1 1 -1h3a1 1 0 0 1 1 1v3a1 1 0 0 1 -1 1h-3a1 1 0 0 1 -1 -1l0 -3" />
                        <path d="M4 12v-6a2 2 0 0 1 2 -2h12a2 2 0 0 1 2 2v12a2 2 0 0 1 -2 2h-6" />
                        <path d="M12 8h4v4" />
                        <path d="M16 8l-5 5" />
                      </svg>
                    </Button>
                    <Button
                      appearance="outline"
                      className={mergeClasses(styles.iconButton, styles.explorerWhiteButton)}
                      disabled={displayImageEntries.length === 0 || imageViewMode === "small"}
                      aria-label="Make images smaller"
                      title="Make images smaller"
                      onClick={() => {
                        setImageViewMode("small");
                      }}
                    >
                      <svg
                        xmlns="http://www.w3.org/2000/svg"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        strokeWidth="2"
                        strokeLinecap="round"
                        strokeLinejoin="round"
                        className={styles.imageResizeIconSvg}
                        aria-hidden="true"
                      >
                        <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                        <path d="M3 17a1 1 0 0 1 1 -1h3a1 1 0 0 1 1 1v3a1 1 0 0 1 -1 1h-3a1 1 0 0 1 -1 -1l0 -3" />
                        <path d="M4 12v-6a2 2 0 0 1 2 -2h12a2 2 0 0 1 2 2v12a2 2 0 0 1 -2 2h-6" />
                        <path d="M15 13h-4v-4" />
                        <path d="M11 13l5 -5" />
                      </svg>
                    </Button>
                  </div>
                </div>
              </div>
              <div
                className={mergeClasses(
                  styles.entriesContentArea,
                  currentSpuMarkedCompleted ? styles.entriesContentAreaCompleted : undefined
                )}
                aria-busy={entriesLoading}
              >
                  {displayImageEntries.length === 0 ? (
                    <Text size={200}>No images in this folder.</Text>
                  ) : (
                    <div
                      className={mergeClasses(
                        styles.dualGrid,
                        tagImagesRunningInCurrentPath
                          ? styles.dualGridTagging
                          : undefined
                      )}
                      aria-busy={imageOrderPersisting}
                      style={{
                        gridTemplateColumns:
                          imageViewMode === "big"
                            ? "repeat(auto-fill, minmax(320px, 1fr))"
                            : "repeat(auto-fill, minmax(180px, 1fr))",
                      }}
                      onDragOver={(event) => {
                        const draggedPaths = readDraggedPaths(event.dataTransfer);
                        const hasImageDrag = draggedPaths.some((pathValue) => {
                          const draggedEntry = entryByPath.get(pathValue);
                          return Boolean(
                            draggedEntry?.type === "file" && isImage(draggedEntry.name)
                          );
                        });
                        if (!hasImageDrag) return;
                        event.preventDefault();
                      }}
                      onDrop={(event) => {
                        const draggedPaths = readDraggedPaths(event.dataTransfer);
                        const hasImageDrag = draggedPaths.some((pathValue) => {
                          const draggedEntry = entryByPath.get(pathValue);
                          return Boolean(
                            draggedEntry?.type === "file" && isImage(draggedEntry.name)
                          );
                        });
                        if (!hasImageDrag) return;
                        event.preventDefault();
                        reorderImagesInGrid(draggedPaths, null);
                        setImageReorderDropPath(null);
                        setFolderDropTargetPath(null);
                        setDraggingEntryPaths([]);
                      }}
                      onDragLeave={() => {
                        setImageReorderDropPath(null);
                      }}
                    >
                      {displayImageEntries.map((entry) => {
                        const pendingAi = pendingAiEditsByOriginal[entry.path] ?? null;
                        const displayPath = pendingAi?.pendingPath ?? entry.path;
                        const displayModifiedAt = pendingAi?.updatedAt ?? entry.modifiedAt;
                        const runtimeJob = aiEditJobsByPath[entry.path] ?? null;
                        const reloadBusy = reloadingImagePaths.has(entry.path);
                        const confirmBusy = confirmingAiEditPaths.has(entry.path);
                        const busy = Boolean(runtimeJob) || reloadBusy || confirmBusy;
                        const imageTags = getDisplayImageTagsForEntry(entry);
                        const isDisplayOnlyMainVariant =
                          mainViewShowsMergedImages && !entryByPath.has(entry.path);
                        const originalPixelQualityScore =
                          typeof entry.pixelQualityScore === "number" &&
                          Number.isFinite(entry.pixelQualityScore)
                            ? Math.round(entry.pixelQualityScore)
                            : null;
                        const pendingPixelQualityScore =
                          typeof pendingAi?.pendingPixelQualityScore === "number" &&
                          Number.isFinite(pendingAi.pendingPixelQualityScore)
                            ? Math.round(pendingAi.pendingPixelQualityScore)
                            : null;
                        const isPixelQualityPending =
                          pendingImageScorePaths.has(entry.path) &&
                          pendingPixelQualityScore === null &&
                          originalPixelQualityScore === null;
                        const pixelQualityScore =
                          pendingPixelQualityScore ?? originalPixelQualityScore;
                        const pixelQualityDelta =
                          pendingPixelQualityScore !== null &&
                          originalPixelQualityScore !== null
                            ? pendingPixelQualityScore - originalPixelQualityScore
                            : null;
                        const pixelQualityClass =
                          pixelQualityScore === null
                            ? styles.mediaMetaQualityUnknown
                            : pixelQualityScore > 60
                              ? styles.mediaMetaQualityGood
                              : pixelQualityScore >= 40
                                ? styles.mediaMetaQualityMedium
                                : styles.mediaMetaQualityBad;
                        const pixelQualityLabel =
                          pixelQualityDelta === null
                            ? `Q ${pixelQualityScore ?? "-"}`
                            : `Q ${pixelQualityScore ?? "-"} (${
                                pixelQualityDelta > 0
                                  ? `+${pixelQualityDelta}`
                                  : pixelQualityDelta
                              })`;
                        const hasZimageUpscaled = isImageEntryUpscaled(entry);
                        const reverseSearchJob = reverseSearchJobsByPath[entry.path] ?? null;
                        const reverseSearchRunning = reverseSearchJob?.status === "running";
                        const reverseSearchReady =
                          reverseSearchJob?.status === "ready" ||
                          reverseSearchJob?.status === "error";
                        const reverseSearchBadgeLabel =
                          reverseSearchJob?.status === "error"
                            ? "Google Lens search completed with errors. Open details."
                            : "Google Lens search is ready. Open results.";
                        return (
                          <div
                            key={buildStableImageCardKey(entry)}
                            className={mergeClasses(
                              styles.mediaCard,
                              imageReorderDropPath === entry.path
                                ? styles.mediaCardDropTarget
                                : undefined,
                              selectedFiles.has(entry.path)
                                ? styles.mediaCardSelected
                                : undefined
                            )}
                            draggable={!isDisplayOnlyMainVariant}
                            onDragStart={(event) => {
                              if (isDisplayOnlyMainVariant) return;
                              const draggedPaths = buildDraggedPathsForEntry(entry);
                              event.dataTransfer.effectAllowed = "move";
                              event.dataTransfer.setData(
                                "text/plain",
                                draggedPaths[0] ?? entry.path
                              );
                              event.dataTransfer.setData(
                                "application/x-nordexo-paths",
                                JSON.stringify(draggedPaths)
                              );
                              setDraggingEntryPaths(draggedPaths);
                              setImageReorderDropPath(null);
                            }}
                            onDragEnd={() => {
                              setDraggingEntryPaths([]);
                              setFolderDropTargetPath(null);
                              setImageReorderDropPath(null);
                            }}
                            onDragOver={(event) => {
                              if (isDisplayOnlyMainVariant) return;
                              const draggedPaths = readDraggedPaths(event.dataTransfer);
                              const hasImageDrag = draggedPaths.some((pathValue) => {
                                const draggedEntry = entryByPath.get(pathValue);
                                return Boolean(
                                  draggedEntry?.type === "file" &&
                                    isImage(draggedEntry.name)
                                );
                              });
                              if (!hasImageDrag) return;
                              event.preventDefault();
                              event.stopPropagation();
                              setImageReorderDropPath(entry.path);
                            }}
                            onDragLeave={(event) => {
                              event.stopPropagation();
                              if (imageReorderDropPath === entry.path) {
                                setImageReorderDropPath(null);
                              }
                            }}
                            onDrop={(event) => {
                              if (isDisplayOnlyMainVariant) return;
                              const draggedPaths = readDraggedPaths(event.dataTransfer);
                              const hasImageDrag = draggedPaths.some((pathValue) => {
                                const draggedEntry = entryByPath.get(pathValue);
                                return Boolean(
                                  draggedEntry?.type === "file" &&
                                    isImage(draggedEntry.name)
                                );
                              });
                              if (!hasImageDrag) return;
                              event.preventDefault();
                              event.stopPropagation();
                              reorderImagesInGrid(draggedPaths, entry.path);
                              setImageReorderDropPath(null);
                              setFolderDropTargetPath(null);
                              setDraggingEntryPaths([]);
                            }}
                            onContextMenu={(event) => {
                              event.preventDefault();
                              syncSelectionForContextMenuEntry(entry);
                              setContextMenuSubmenu(null);
                              setContextMenu({
                                entry,
                                image: true,
                                x: event.clientX,
                                y: event.clientY,
                              });
                            }}
                          >
                            <div
                              className={styles.mediaSquare}
                              onClick={(event) => {
                                handleSelectImageCard(entry.path, {
                                  shiftKey: event.shiftKey,
                                  ctrlKey: event.ctrlKey,
                                  metaKey: event.metaKey,
                                });
                              }}
                              role="button"
                              tabIndex={0}
                              onKeyDown={(event) => {
                                if (event.key !== "Enter" && event.key !== " ") return;
                                event.preventDefault();
                                handleSelectImageCard(entry.path, {
                                  shiftKey: event.shiftKey,
                                  ctrlKey: event.ctrlKey,
                                  metaKey: event.metaKey,
                                });
                              }}
                            >
                              {imageTags.length > 0 ? (
                                <div
                                  className={mergeClasses(
                                    styles.mediaTagStack,
                                    pendingAi ? styles.mediaTagStackShifted : undefined
                                  )}
                                >
                                  {imageTags.map((tag) => (
                                    <span
                                      key={`${entry.path}-${tag}`}
                                      className={mergeClasses(
                                        styles.mediaTagBadge,
                                        tag === "MAIN"
                                          ? styles.mediaTagMain
                                          : tag === "ENV"
                                            ? styles.mediaTagEnv
                                            : tag === "VAR"
                                              ? styles.mediaTagVar
                                              : tag === "SIZE"
                                                ? styles.mediaTagSize
                                              : tag === "DIGI"
                                                ? styles.mediaTagDigi
                                              : styles.mediaTagInf
                                      )}
                                    >
                                      {tag}
                                    </span>
                                  ))}
                                </div>
                              ) : null}
                              {pendingAi ? (
                                <button
                                  type="button"
                                  className={styles.aiPendingBadge}
                                  title={`Review AI edit for ${entry.name}`}
                                  aria-label={`Review AI edit for ${entry.name}`}
                                  onClick={(event) => {
                                    event.stopPropagation();
                                    setAiReviewOriginalPath(entry.path);
                                  }}
                                >
                                  <svg
                                    xmlns="http://www.w3.org/2000/svg"
                                    viewBox="0 0 24 24"
                                    fill="none"
                                    stroke="currentColor"
                                    strokeWidth="2"
                                    strokeLinecap="round"
                                    strokeLinejoin="round"
                                    width="16"
                                    height="16"
                                    aria-hidden="true"
                                  >
                                    <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                                    <path d="M15 8h.01" />
                                    <path d="M10 21h-4a3 3 0 0 1 -3 -3v-12a3 3 0 0 1 3 -3h12a3 3 0 0 1 3 3v5" />
                                    <path d="M3 16l5 -5c.928 -.893 2.072 -.893 3 0l1 1" />
                                    <path d="M14 21v-4a2 2 0 1 1 4 0v4" />
                                    <path d="M14 19h4" />
                                    <path d="M21 15v6" />
                                  </svg>
                                </button>
                              ) : null}
                              <button
                                type="button"
                                className={mergeClasses(
                                  styles.thumbDownloadButton,
                                  "thumbDownloadButton"
                                )}
                                aria-label={`Preview ${entry.name}`}
                                onClick={(event) => {
                                  event.stopPropagation();
                                  setPreviewPath(entry.path);
                                }}
                              >
                                <svg
                                  xmlns="http://www.w3.org/2000/svg"
                                  viewBox="0 0 24 24"
                                  fill="none"
                                  stroke="currentColor"
                                  strokeWidth="2"
                                  strokeLinecap="round"
                                  strokeLinejoin="round"
                                  width="16"
                                  height="16"
                                  aria-hidden="true"
                                >
                                  <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                                  <path d="M3 10a7 7 0 1 0 14 0a7 7 0 1 0 -14 0" />
                                  <path d="M7 10l6 0" />
                                  <path d="M10 7l0 6" />
                                  <path d="M21 21l-6 -6" />
                                </svg>
                              </button>
                              <img
                                src={buildDraftDownloadUrl(
                                  displayPath,
                                  displayModifiedAt
                                )}
                                alt={entry.name}
                                className={mergeClasses(
                                  styles.thumbImage,
                                  busy ? styles.mediaImageBusy : undefined,
                                  reverseSearchRunning
                                    ? styles.mediaImageReverseSearchRunning
                                    : undefined
                                )}
                                onLoad={(event) => {
                                  const img = event.currentTarget;
                                  const pathValue = entry.path;
                                  setImageDimensions((prev) => ({
                                    ...prev,
                                    [pathValue]: {
                                      width: img.naturalWidth,
                                      height: img.naturalHeight,
                                    },
                                  }));
                                  setReloadingImagePaths((prev) => {
                                    if (!prev.has(pathValue)) return prev;
                                    const next = new Set(prev);
                                    next.delete(pathValue);
                                    return next;
                                  });
                                }}
                              />
                              {busy ? (
                                <div className={styles.mediaBusyOverlay}>
                                  <div className={styles.mediaBusyContent}>
                                    <Spinner size="small" />
                                    <span>Updating</span>
                                  </div>
                                </div>
                              ) : null}
                              {reverseSearchRunning ? (
                                <div className={styles.reverseSearchRunningOverlay}>
                                  <div className={styles.reverseSearchRunningContent}>
                                    <Spinner size="tiny" />
                                  </div>
                                </div>
                              ) : null}
                              {reverseSearchReady ? (
                                <button
                                  type="button"
                                  className={styles.googleSearchReadyBadge}
                                  title={reverseSearchBadgeLabel}
                                  aria-label={reverseSearchBadgeLabel}
                                  onClick={(event) => {
                                    event.stopPropagation();
                                    void openReverseSearchDialogForImage(entry.path);
                                  }}
                                >
                                  <img
                                    src={GOOGLE_SEARCH_BADGE_ICON_SRC}
                                    alt=""
                                    className={styles.googleSearchBadgeImage}
                                    loading="lazy"
                                    decoding="async"
                                  />
                                </button>
                              ) : null}
                            </div>
                          <div className={styles.mediaFooter}>
                            <div className={styles.mediaFooterColumn}>
                              {renamingPath === entry.path ? (
                                <Input
                                  size="small"
                                  value={renameValue}
                                  onChange={(_, data) => setRenameValue(data.value)}
                                  onBlur={() => commitRename(entry)}
                                  onKeyDown={(event) => {
                                    if (event.key === "Enter") {
                                      event.preventDefault();
                                      commitRename(entry);
                                    }
                                    if (event.key === "Escape") {
                                      event.preventDefault();
                                      cancelRename();
                                    }
                                  }}
                                  autoFocus
                                  className={styles.renameInput}
                                />
                              ) : (
                                <button
                                  type="button"
                                  className={styles.mediaLabel}
                                  onClick={() => startRename(entry)}
                                >
                                  {entry.name}
                                </button>
                              )}
                              <Text size={100} className={styles.mediaMeta}>
                                {imageDimensions[entry.path] ? (
                                  <>
                                    <span
                                      className={mergeClasses(
                                        imageDimensions[entry.path].width < 800 ||
                                          imageDimensions[entry.path].height < 800
                                          ? styles.mediaMetaDimLow
                                          : undefined
                                      )}
                                    >
                                      {`${imageDimensions[entry.path].width} x ${
                                        imageDimensions[entry.path].height
                                      }`}
                                    </span>
                                    <span className={styles.mediaMetaDivider}>|</span>
                                    <span>{formatSizeKb(entry.size)}</span>
                                  </>
                                ) : (
                                  <>
                                    <span>-</span>
                                    <span className={styles.mediaMetaDivider}>|</span>
                                    <span>{formatSizeKb(entry.size)}</span>
                                  </>
                                )}
                                <span className={styles.mediaMetaDivider}>|</span>
                                <span
                                  className={mergeClasses(
                                    styles.mediaMetaQuality,
                                    pixelQualityClass
                                  )}
                                >
                                  {isPixelQualityPending ? (
                                    <span className={styles.mediaMetaQualityPendingInline}>
                                      <span>Q</span>
                                      <Spinner
                                        size="tiny"
                                        className={styles.mediaMetaQualityPendingSpinner}
                                      />
                                    </span>
                                  ) : (
                                    pixelQualityLabel
                                  )}
                                </span>
                                {hasZimageUpscaled ? (
                                  <>
                                    <span className={styles.mediaMetaDivider}>|</span>
                                    <span
                                      className={styles.mediaMetaUpscaleIcon}
                                      title="Upscaled with Z-image"
                                      aria-label="Upscaled with Z-image"
                                    >
                                      <svg
                                        xmlns="http://www.w3.org/2000/svg"
                                        viewBox="0 0 24 24"
                                        fill="none"
                                        stroke="currentColor"
                                        strokeWidth="2"
                                        strokeLinecap="round"
                                        strokeLinejoin="round"
                                        className={styles.mediaMetaUpscaleIconSvg}
                                        aria-hidden="true"
                                      >
                                        <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                                        <path d="M3 7a2 2 0 0 1 2 -2h14a2 2 0 0 1 2 2v10a2 2 0 0 1 -2 2h-14a2 2 0 0 1 -2 -2v-10" />
                                        <path d="M7 9v2a1 1 0 0 0 1 1h1" />
                                        <path d="M10 9v6" />
                                        <path d="M14 9v6" />
                                        <path d="M17 9l-2 3l2 3" />
                                        <path d="M15 12h-1" />
                                      </svg>
                                    </span>
                                  </>
                                ) : null}
                              </Text>
                            </div>
                          </div>
                          </div>
                        );
                      })}
                    </div>
                  )}

                {entriesLoading || tagImagesRunningInCurrentPath ? (
                  <div className={styles.entriesContentOverlay}>
                    <div className={styles.mediaBusyContent}>
                      <Spinner size="tiny" />
                      <span>
                        {tagImagesRunningInCurrentPath
                          ? "Classifying images..."
                          : "Loading"}
                      </span>
                    </div>
                  </div>
                ) : null}
              </div>
            </div>
              {!filesPaneHidden ? (
                <div className={styles.filesUploadRow}>
                  <div className={styles.filesSection}>
                    <div className={styles.entriesContentArea} aria-busy={entriesLoading}>
                      {nonImageFileEntries.length === 0 ? (
                        <Text size={100}>No files in this folder.</Text>
                      ) : (
                        <Table size="small" className={styles.filesTable}>
                          <TableHeader>
                            <TableRow>
                              <TableHeaderCell className={styles.filesColSelect}>
                                <input
                                  ref={nonImageFileSelectAllRef}
                                  type="checkbox"
                                  checked={nonImageFilesAllSelected}
                                  onChange={handleToggleAllNonImageFiles}
                                  aria-label="Select all files"
                                />
                              </TableHeaderCell>
                              <TableHeaderCell className={styles.filesColName}>
                                File Name
                              </TableHeaderCell>
                              <TableHeaderCell className={styles.filesColSize}>
                                File Size
                              </TableHeaderCell>
                              <TableHeaderCell className={styles.filesColDate}>
                                Date
                              </TableHeaderCell>
                              <TableHeaderCell className={styles.filesColAction}>
                                Actions
                              </TableHeaderCell>
                            </TableRow>
                          </TableHeader>
                          <TableBody>
                            {nonImageFileEntries.map((entry) => (
                              <TableRow
                                key={`file-${entry.path}`}
                                draggable
                                onDragStart={(event) => {
                                  const draggedPaths = buildDraggedPathsForEntry(entry);
                                  event.dataTransfer.effectAllowed = "move";
                                  event.dataTransfer.setData(
                                    "text/plain",
                                    draggedPaths[0] ?? entry.path
                                  );
                                  event.dataTransfer.setData(
                                    "application/x-nordexo-paths",
                                    JSON.stringify(draggedPaths)
                                  );
                                  setDraggingEntryPaths(draggedPaths);
                                }}
                                onDragEnd={() => {
                                  setDraggingEntryPaths([]);
                                  setFolderDropTargetPath(null);
                                }}
                                onContextMenu={(event) => {
                                  event.preventDefault();
                                  syncSelectionForContextMenuEntry(entry);
                                  setContextMenuSubmenu(null);
                                  setContextMenu({
                                    entry,
                                    image: false,
                                    x: event.clientX,
                                    y: event.clientY,
                                  });
                                }}
                              >
                                <TableCell className={styles.filesColSelect}>
                                  <input
                                    type="checkbox"
                                    checked={selectedFiles.has(entry.path)}
                                    onChange={() => handleToggleFile(entry.path)}
                                  />
                                </TableCell>
                                <TableCell className={styles.filesColName}>
                                  {renamingPath === entry.path ? (
                                    <Input
                                      size="small"
                                      value={renameValue}
                                      onChange={(_, data) => setRenameValue(data.value)}
                                      onBlur={() => commitRename(entry)}
                                      onKeyDown={(event) => {
                                        if (event.key === "Enter") {
                                          event.preventDefault();
                                          commitRename(entry);
                                        }
                                        if (event.key === "Escape") {
                                          event.preventDefault();
                                          cancelRename();
                                        }
                                      }}
                                      autoFocus
                                      className={styles.renameInput}
                                    />
                                  ) : (
                                    <button
                                      type="button"
                                      className={styles.filesNameButton}
                                      onClick={() => startRename(entry)}
                                    >
                                      {entry.name}
                                    </button>
                                  )}
                                </TableCell>
                                <TableCell className={styles.filesColSize}>
                                  <Text size={100} className={styles.filesInfo}>
                                    {formatFileSize(entry.size)}
                                  </Text>
                                </TableCell>
                                <TableCell className={styles.filesColDate}>
                                  <Text size={100} className={styles.filesInfo}>
                                    {formatDateTime(entry.modifiedAt)}
                                  </Text>
                                </TableCell>
                                <TableCell className={styles.filesColAction}>
                                  <div className={styles.fileActions}>
                                    <Button
                                      appearance="outline"
                                      size="small"
                                      onClick={() => downloadEntry(entry)}
                                      className={styles.fileActionButton}
                                    >
                                      {t("bulkProcessing.explorer.download")}
                                    </Button>
                                    <Button
                                      appearance="outline"
                                      size="small"
                                      onClick={() => handleOpenFileViewer(entry)}
                                      disabled={!isTextFileEditable(entry)}
                                      className={mergeClasses(
                                        styles.fileActionButton,
                                        styles.fileActionButtonNarrow
                                      )}
                                    >
                                      View
                                    </Button>
                                  </div>
                                </TableCell>
                              </TableRow>
                            ))}
                          </TableBody>
                        </Table>
                      )}
                      {entriesLoading ? (
                        <div className={styles.entriesContentOverlay}>
                          <Spinner size="tiny" />
                        </div>
                      ) : null}
                    </div>
                  </div>
                </div>
              ) : null}
            </div>
          </div>
        ) : explorerView === "list" ? (
          <Table size="small" className={styles.explorerTable}>
            <TableHeader>
              <TableRow>
                <TableHeaderCell className={styles.explorerColName}>
                  {t("bulkProcessing.explorer.name")}
                </TableHeaderCell>
                <TableHeaderCell className={styles.explorerColSize}>
                  {t("bulkProcessing.explorer.size")}
                </TableHeaderCell>
                <TableHeaderCell className={styles.explorerColModified}>
                  {t("bulkProcessing.explorer.modified")}
                </TableHeaderCell>
                <TableHeaderCell className={styles.explorerColActions}>
                  {t("bulkProcessing.explorer.actions")}
                </TableHeaderCell>
              </TableRow>
            </TableHeader>
            <TableBody>
              {entriesLoading ? (
                <TableRow>
                  <TableCell colSpan={4}>
                    <Spinner size="tiny" />
                  </TableCell>
                </TableRow>
              ) : entries.length === 0 ? (
                <TableRow>
                  <TableCell colSpan={4}>
                    {t("bulkProcessing.explorer.empty")}
                  </TableCell>
                </TableRow>
              ) : (
                entries.map((entry) => (
                  <TableRow
                    key={entry.path}
                    className={mergeClasses(
                      entry.type === "dir" ? styles.folderRow : undefined
                    )}
                    onClick={(event) => {
                      if (entry.type !== "dir") return;
                      const target = event.target as HTMLElement;
                      if (target.closest("button, input, a")) return;
                      setCurrentPath(entry.path);
                    }}
                  >
                    <TableCell className={styles.explorerColName}>
                      <div className={styles.explorerRow}>
                        <input
                          type="checkbox"
                          checked={selectedFiles.has(entry.path)}
                          onChange={() => handleToggleFile(entry.path)}
                        />
                        {entry.type === "dir" ? (
                          <svg
                            viewBox="0 0 24 24"
                            aria-hidden="true"
                            className={styles.explorerIcon}
                          >
                            <path
                              fill="currentColor"
                              d="M9.5 4h-5A2.5 2.5 0 0 0 2 6.5v11A2.5 2.5 0 0 0 4.5 20h15A2.5 2.5 0 0 0 22 17.5v-9A2.5 2.5 0 0 0 19.5 6H12l-2-2.5A2.5 2.5 0 0 0 9.5 4Z"
                            />
                          </svg>
                        ) : null}
                        {renamingPath === entry.path ? (
                          <Input
                            size="small"
                            value={renameValue}
                            onChange={(_, data) => setRenameValue(data.value)}
                            onBlur={() => commitRename(entry)}
                            onKeyDown={(event) => {
                              if (event.key === "Enter") {
                                event.preventDefault();
                                commitRename(entry);
                              }
                              if (event.key === "Escape") {
                                event.preventDefault();
                                cancelRename();
                              }
                            }}
                            autoFocus
                            onClick={(event) => event.stopPropagation()}
                            className={styles.renameInput}
                          />
                        ) : entry.type === "dir" ? (
                          <button
                            type="button"
                            className={styles.explorerName}
                            onClick={(event) => {
                              event.stopPropagation();
                              setCurrentPath(entry.path);
                            }}
                            style={{ cursor: "pointer" }}
                          >
                            {entry.name}
                          </button>
                        ) : (
                          <button
                            type="button"
                            className={styles.explorerName}
                            onClick={(event) => {
                              event.stopPropagation();
                              startRename(entry);
                            }}
                          >
                            {entry.name}
                          </button>
                        )}
                      </div>
                    </TableCell>
                    <TableCell className={styles.explorerColSize}>
                      <Text size={100} className={styles.explorerMeta}>
                        {entry.type === "file" ? formatFileSize(entry.size) : "-"}
                      </Text>
                    </TableCell>
                    <TableCell className={styles.explorerColModified}>
                      <Text size={100} className={styles.explorerMeta}>
                        {formatDateTime(entry.modifiedAt)}
                      </Text>
                    </TableCell>
                    <TableCell className={styles.explorerColActions}>
                      {entry.type === "dir" ? (
                        <Button
                          appearance="outline"
                          size="small"
                          onClick={() => setCurrentPath(entry.path)}
                        >
                          {t("bulkProcessing.explorer.open")}
                        </Button>
                      ) : (
                        <div className={styles.explorerRow}>
                          <Button
                            appearance="outline"
                            size="small"
                            onClick={() =>
                              window.open(
                                `/api/drafts/download?path=${encodeURIComponent(
                                  entry.path
                                )}`,
                                "_blank"
                              )
                            }
                          >
                            {t("bulkProcessing.explorer.download")}
                          </Button>
                          {isImage(entry.name) ? (
                            <Button
                              appearance="subtle"
                              size="small"
                              onClick={() => setPreviewPath(entry.path)}
                            >
                              {t("bulkProcessing.explorer.preview")}
                            </Button>
                          ) : null}
                        </div>
                      )}
                    </TableCell>
                  </TableRow>
                ))
              )}
            </TableBody>
          </Table>
        ) : entriesLoading ? (
          <div style={{ padding: "12px" }}>
            <Spinner size="tiny" />
          </div>
        ) : entries.length === 0 ? (
          <Text size={200}>{t("bulkProcessing.explorer.empty")}</Text>
        ) : (
          <div className={styles.thumbGrid}>
            {entries.map((entry) => (
              <div key={entry.path} className={styles.thumbCard}>
                <div className={styles.explorerRow}>
                  <input
                    type="checkbox"
                    checked={selectedFiles.has(entry.path)}
                    onChange={() => handleToggleFile(entry.path)}
                  />
                  <Text size={100} className={styles.explorerMeta}>
                    {entry.type === "dir"
                      ? t("bulkProcessing.explorer.folder")
                      : t("bulkProcessing.explorer.file")}
                  </Text>
                </div>
                <div
                  className={styles.thumbImageWrap}
                  onClick={() => {
                    if (entry.type === "dir") {
                      setCurrentPath(entry.path);
                      return;
                    }
                    if (isImage(entry.name)) {
                      setPreviewPath(entry.path);
                      return;
                    }
                    triggerBrowserDownload(
                      `/api/drafts/download?path=${encodeURIComponent(entry.path)}`
                    );
                  }}
                  role="button"
                  tabIndex={0}
                  onKeyDown={(event) => {
                    if (event.key !== "Enter" && event.key !== " ") return;
                    event.preventDefault();
                    if (entry.type === "dir") {
                      setCurrentPath(entry.path);
                      return;
                    }
                    if (isImage(entry.name)) {
                      setPreviewPath(entry.path);
                      return;
                    }
                    triggerBrowserDownload(
                      `/api/drafts/download?path=${encodeURIComponent(entry.path)}`
                    );
                  }}
                >
                  <button
                    type="button"
                    className={mergeClasses(
                      styles.thumbDownloadButton,
                      "thumbDownloadButton"
                    )}
                    aria-label={
                      entry.type === "dir"
                        ? `Download ${entry.name} as ZIP`
                        : `Download ${entry.name}`
                    }
                    onClick={(event) => {
                      event.stopPropagation();
                      if (entry.type === "dir") {
                        triggerBrowserDownload(
                          `/api/drafts/zip?path=${encodeURIComponent(entry.path)}`
                        );
                        return;
                      }
                      triggerBrowserDownload(
                        `/api/drafts/download?path=${encodeURIComponent(entry.path)}`
                      );
                    }}
                  >
                    <svg
                      viewBox="0 0 24 24"
                      width="16"
                      height="16"
                      aria-hidden="true"
                    >
                      <path
                        d="M12 3v10m0 0l4-4m-4 4l-4-4M4 17v3h16v-3"
                        fill="none"
                        stroke="currentColor"
                        strokeWidth="2"
                        strokeLinecap="round"
                        strokeLinejoin="round"
                      />
                    </svg>
                  </button>
                  {entry.type === "dir" ? (
                    <svg
                      viewBox="0 0 24 24"
                      aria-hidden="true"
                      className={styles.thumbIcon}
                    >
                      <path
                        fill="currentColor"
                        d="M9.5 4h-5A2.5 2.5 0 0 0 2 6.5v11A2.5 2.5 0 0 0 4.5 20h15A2.5 2.5 0 0 0 22 17.5v-9A2.5 2.5 0 0 0 19.5 6H12l-2-2.5A2.5 2.5 0 0 0 9.5 4Z"
                      />
                    </svg>
                  ) : isImage(entry.name) ? (
                    <img
                      src={buildDraftDownloadUrl(entry.path, entry.modifiedAt)}
                      alt={entry.name}
                      className={styles.thumbImage}
                    />
                  ) : (
                    <svg
                      viewBox="0 0 24 24"
                      aria-hidden="true"
                      className={styles.thumbIcon}
                    >
                      <path
                        fill="currentColor"
                        d="M7 3h7l5 5v11a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2Z"
                      />
                    </svg>
                  )}
                </div>
                <div>
                  {renamingPath === entry.path ? (
                    <Input
                      size="small"
                      value={renameValue}
                      onChange={(_, data) => setRenameValue(data.value)}
                      onBlur={() => commitRename(entry)}
                      onKeyDown={(event) => {
                        if (event.key === "Enter") {
                          event.preventDefault();
                          commitRename(entry);
                        }
                        if (event.key === "Escape") {
                          event.preventDefault();
                          cancelRename();
                        }
                      }}
                      autoFocus
                      className={styles.renameInput}
                    />
                  ) : entry.type === "dir" ? (
                    <button
                      type="button"
                      className={styles.thumbName}
                      onClick={() => setCurrentPath(entry.path)}
                      style={{ cursor: "pointer" }}
                    >
                      {entry.name}
                    </button>
                  ) : (
                    <button
                      type="button"
                      className={styles.thumbName}
                      onClick={() => startRename(entry)}
                    >
                      {entry.name}
                    </button>
                  )}
                </div>
              </div>
            ))}
          </div>
        )}

		        {contextMenu ? (
		          <div
		            className={styles.contextMenu}
		            style={{ left: `${contextMenu.x}px`, top: `${contextMenu.y}px` }}
		            ref={contextMenuRef}
		            onContextMenu={(event) => event.preventDefault()}
	              onClick={(event) => event.stopPropagation()}
	              onMouseEnter={() => keepContextMenuOpen()}
	              onMouseLeave={() => scheduleContextMenuClose()}
		          >
		            {contextMenu.image ? (
			              <div
			                className={styles.contextMenuSubmenuWrap}
			                onMouseEnter={() => openContextMenuSubmenu("tag-image")}
	                        onMouseLeave={() => {
	                          scheduleContextMenuSubmenuClose("tag-image");
	                        }}
			              >
	                <button
	                  type="button"
	                  className={styles.contextMenuButton}
	                  onMouseEnter={() => openContextMenuSubmenu("tag-image")}
	                  onClick={(event) => {
	                    event.preventDefault();
	                    event.stopPropagation();
	                    openContextMenuSubmenu("tag-image");
	                  }}
	                >
	                  {renderContextMenuIcon("tag")}
	                  <span>Tag Image</span>
	                  <span className={styles.contextMenuButtonCaret}>
	                    {contextMenuSubmenuSide === "left" ? "‹" : "›"}
	                  </span>
	                </button>
	                {contextMenuSubmenu === "tag-image" ? (
		                  <div
		                    className={mergeClasses(
		                      styles.contextMenuSubmenu,
		                      contextMenuSubmenuSide === "left"
		                        ? styles.contextMenuSubmenuLeft
		                        : undefined
		                    )}
                        onMouseEnter={() => openContextMenuSubmenu("tag-image")}
                        onMouseLeave={() => scheduleContextMenuSubmenuClose("tag-image")}
		                  >
	                    {TAG_IMAGE_OPTIONS.map((tag) => {
	                      const state = getContextMenuImageTagState(tag);
	                      return (
	                        <button
	                          type="button"
	                          className={styles.contextMenuButton}
	                          onClick={(event) => {
                            event.preventDefault();
                            event.stopPropagation();
                            handleContextMenuAction(`tag-image-toggle:${tag}`);
                          }}
	                          key={`tag-toggle-${tag}`}
	                        >
                          <span className={styles.contextMenuTagLabel}>{tag}</span>
                          <span
                            className={mergeClasses(
                              styles.contextMenuTagCheckbox,
                              state === "all"
                                ? styles.contextMenuTagCheckboxChecked
                                : undefined,
                              state === "some"
                                ? styles.contextMenuTagCheckboxMixed
                                : undefined
                            )}
                            aria-hidden="true"
                          >
                            {state === "all" ? (
                              <svg
                                xmlns="http://www.w3.org/2000/svg"
                                viewBox="0 0 12 12"
                                fill="none"
                                stroke="currentColor"
                                strokeWidth="1.8"
                                strokeLinecap="round"
                                strokeLinejoin="round"
                                className={styles.contextMenuTagCheckboxIcon}
                              >
                                <path d="M2.4 6.4l2.2 2.2l5 -5" />
                              </svg>
                            ) : state === "some" ? (
                              <span className={styles.contextMenuTagCheckboxBar} />
                            ) : null}
                          </span>
	                        </button>
	                      );
	                    })}
	                  </div>
                ) : null}
              </div>
	            ) : null}
		            {contextMenu.image ? (
		              <div
		                className={styles.contextMenuSubmenuWrap}
		                onMouseEnter={() => openContextMenuSubmenu("assign-variant")}
		                onMouseLeave={() => {
		                  scheduleContextMenuSubmenuClose("assign-variant");
		                }}
		              >
		                <button
		                  type="button"
		                  className={styles.contextMenuButton}
		                  onMouseEnter={() => openContextMenuSubmenu("assign-variant")}
		                  onClick={(event) => {
		                    event.preventDefault();
		                    event.stopPropagation();
		                    openContextMenuSubmenu("assign-variant");
		                  }}
		                >
	                  {renderContextMenuIcon("tag")}
	                  <span>Assign Variant</span>
	                  <span className={styles.contextMenuButtonCaret}>
	                    {contextMenuSubmenuSide === "left" ? "‹" : "›"}
	                  </span>
	                </button>
	                {contextMenuSubmenu === "assign-variant" ? (
		                  <div
		                    className={mergeClasses(
		                      styles.contextMenuSubmenu,
		                      styles.contextMenuSubmenuScrollable,
		                      contextMenuSubmenuSide === "left"
		                        ? styles.contextMenuSubmenuLeft
		                        : undefined
		                    )}
                        onMouseEnter={() => openContextMenuSubmenu("assign-variant")}
                        onMouseLeave={() => scheduleContextMenuSubmenuClose("assign-variant")}
		                  >
                    {contextMenuVariantRowsError ? (
                      <div className={styles.contextMenuButton}>
                        <span>{contextMenuVariantRowsError}</span>
                      </div>
                    ) : contextMenuVariantGroups.length === 0 && contextMenuVariantRowsLoading ? (
                      <div className={styles.contextMenuButton}>
                        <Spinner size="tiny" />
                        <span>Loading variants...</span>
                      </div>
                    ) : contextMenuVariantGroups.length === 0 ? (
                      <div className={styles.contextMenuButton}>
                        <span>No variants found for this product.</span>
                      </div>
                    ) : (
                      <>
                        {contextMenuVariantRowsRefreshing ? (
                          <div className={styles.contextMenuButton}>
                            <Spinner size="tiny" />
                            <span>Refreshing variants...</span>
                          </div>
                        ) : null}
                        {contextMenuVariantGroups.map((group) => {
                          const rowsInGroup = group.rowIds
                            .map((rowId) => contextMenuVariantRowsById.get(rowId))
                            .filter((row): row is DraftSkuRow => Boolean(row));
                          if (rowsInGroup.length === 0) return null;
                          let assignedCount = 0;
                          rowsInGroup.forEach((row) => {
                            const currentFileName = extractVariantImageFileName(
                              row.draft_variant_image_url
                            );
                            const currentFileNameKey =
                              normalizeVariantImageFileNameKey(currentFileName);
                            if (
                              currentFileNameKey &&
                              currentFileNameKey === contextMenuVariantImageFileNameKey
                            ) {
                              assignedCount += 1;
                            }
                          });
                          const state: "none" | "some" | "all" =
                            assignedCount === 0
                              ? "none"
                              : assignedCount === rowsInGroup.length
                                ? "all"
                                : "some";
                          const isSaving = contextMenuVariantSavingIds.has(group.id);
                          const skuLabel =
                            rowsInGroup.length > 1
                              ? `${rowsInGroup.length} SKUs`
                              : group.skuHint || String(rowsInGroup[0].draft_sku || "").trim();
                          return (
                            <button
                              type="button"
                              className={styles.contextMenuButton}
                              onClick={(event) => {
                                event.preventDefault();
                                event.stopPropagation();
                                handleContextMenuAction(
                                  `assign-variant-group-toggle:${encodeURIComponent(group.id)}`
                                );
                              }}
                              disabled={isSaving}
                              key={`assign-variant-group-${group.id}`}
                            >
                              <span className={styles.contextMenuVariantLabelWrap}>
                                <span className={styles.contextMenuVariantPrimaryLabel}>
                                  {group.label || "-"}
                                </span>
                                {skuLabel ? (
                                  <span className={styles.contextMenuVariantSecondaryLabel}>
                                    {skuLabel}
                                  </span>
                                ) : null}
                              </span>
                              <span className={styles.contextMenuVariantSelectionWrap}>
                                <span
                                  className={mergeClasses(
                                    styles.contextMenuTagCheckbox,
                                    state === "all"
                                      ? styles.contextMenuTagCheckboxChecked
                                      : undefined,
                                    state === "some"
                                      ? styles.contextMenuTagCheckboxMixed
                                      : undefined
                                  )}
                                  aria-hidden="true"
                                >
                                  {state === "all" ? (
                                    <svg
                                      xmlns="http://www.w3.org/2000/svg"
                                      viewBox="0 0 12 12"
                                      fill="none"
                                      stroke="currentColor"
                                      strokeWidth="1.8"
                                      strokeLinecap="round"
                                      strokeLinejoin="round"
                                      className={styles.contextMenuTagCheckboxIcon}
                                    >
                                      <path d="M2.4 6.4l2.2 2.2l5 -5" />
                                    </svg>
                                  ) : state === "some" ? (
                                    <span className={styles.contextMenuTagCheckboxBar} />
                                  ) : null}
                                </span>
                              </span>
                            </button>
                          );
                        })}
                      </>
                    )}
                  </div>
                ) : null}
	              </div>
	            ) : null}
		            <button
			              type="button"
			              className={styles.contextMenuButton}
		              onMouseEnter={closeAnyOpenContextMenuSubmenuWithDelay}
		              onClick={() => handleContextMenuAction("open")}
		            >
              {renderContextMenuIcon("open")}
              <span>Open</span>
            </button>
		            {contextMenu.image ? (
		              <div
		                className={styles.contextMenuSubmenuWrap}
		                onMouseEnter={() => openContextMenuSubmenu("google-lens-search")}
	                    onMouseLeave={() => {
	                      scheduleContextMenuSubmenuClose("google-lens-search");
	                    }}
		              >
	                <button
	                  type="button"
	                  className={styles.contextMenuButton}
	                  onMouseEnter={() => openContextMenuSubmenu("google-lens-search")}
	                  onClick={(event) => {
	                    event.preventDefault();
	                    event.stopPropagation();
	                    openContextMenuSubmenu("google-lens-search");
	                  }}
	                >
                  {renderContextMenuIcon("google-search")}
                  <span>Google Lens Search</span>
                  <span className={styles.contextMenuButtonCaret}>
                    {contextMenuSubmenuSide === "left" ? "‹" : "›"}
                  </span>
                </button>
                {contextMenuSubmenu === "google-lens-search" ? (
	                  <div
	                    className={mergeClasses(
	                      styles.contextMenuSubmenu,
	                      contextMenuSubmenuSide === "left"
	                        ? styles.contextMenuSubmenuLeft
	                        : undefined
	                    )}
                      onMouseEnter={() => openContextMenuSubmenu("google-lens-search")}
                      onMouseLeave={() => scheduleContextMenuSubmenuClose("google-lens-search")}
	                  >
                    <button
                      type="button"
                      className={styles.contextMenuButton}
                      onClick={() => handleContextMenuAction("google-lens-search:sweden")}
                    >
                      <span>Sweden</span>
                    </button>
                    <button
                      type="button"
                      className={styles.contextMenuButton}
                      onClick={() => handleContextMenuAction("google-lens-search:us_global")}
                    >
                      <span>USA / Global</span>
                    </button>
                  </div>
                ) : null}
              </div>
            ) : null}
            {contextMenu.image ? (
              <button
                type="button"
                className={styles.contextMenuButton}
                disabled={contextMenuMoveToMainDisabled}
                onMouseEnter={closeAnyOpenContextMenuSubmenuWithDelay}
                onClick={() => handleContextMenuAction("move-to-main")}
              >
                {renderContextMenuIcon("move-main")}
                <span>Move to Main</span>
              </button>
            ) : null}
	            <button
	              type="button"
	              className={styles.contextMenuButton}
	              onMouseEnter={closeAnyOpenContextMenuSubmenuWithDelay}
	              onClick={() => handleContextMenuAction("download")}
	            >
              {renderContextMenuIcon("download")}
              <span>Download</span>
            </button>
	            {isTextFileEditable(contextMenu.entry) ? (
	              <button
	                type="button"
	                className={styles.contextMenuButton}
	                onMouseEnter={closeAnyOpenContextMenuSubmenuWithDelay}
	                onClick={() => handleContextMenuAction("view")}
	              >
                {renderContextMenuIcon("open")}
                <span>View</span>
              </button>
            ) : null}
	            <button
		              type="button"
		              className={styles.contextMenuButton}
		              onMouseEnter={closeAnyOpenContextMenuSubmenuWithDelay}
		              onClick={() => handleContextMenuAction("create-copy")}
		            >
	              {renderContextMenuIcon("duplicate")}
	              <span>Duplicate</span>
	            </button>
	            {contextMenu.image ? (
	              <button
	                type="button"
	                className={styles.contextMenuButton}
	                onMouseEnter={closeAnyOpenContextMenuSubmenuWithDelay}
	                onClick={() => handleContextMenuAction("copy-to-clipboard")}
	              >
	                {renderContextMenuIcon("clipboard")}
	                <span>Copy to Clipboard</span>
	              </button>
	            ) : null}
	            {contextMenu.image ? (
	              <button
	                type="button"
	                className={styles.contextMenuButton}
	                onMouseEnter={closeAnyOpenContextMenuSubmenuWithDelay}
	                onClick={() => handleContextMenuAction("undo-last-change")}
	              >
	                {renderContextMenuIcon("undo")}
	                <span>Undo last change</span>
	              </button>
	            ) : null}
	            {contextMenu.image ? (
                <div
                  className={styles.contextMenuSubmenuWrap}
                  onMouseEnter={() => openContextMenuSubmenu("edit-image")}
                  onMouseLeave={() => {
                    scheduleContextMenuSubmenuClose("edit-image");
                  }}
                >
                  <button
                    type="button"
                    className={styles.contextMenuButton}
                    onMouseEnter={() => openContextMenuSubmenu("edit-image")}
                    onClick={(event) => {
                      event.preventDefault();
                      event.stopPropagation();
                      openContextMenuSubmenu("edit-image");
                    }}
                  >
                    {renderContextMenuIcon("photopea")}
                    <span>Edit Image</span>
                    <span className={styles.contextMenuButtonCaret}>
                      {contextMenuSubmenuSide === "left" ? "‹" : "›"}
                    </span>
                  </button>
                  {contextMenuSubmenu === "edit-image" ? (
                    <div
                      className={mergeClasses(
                        styles.contextMenuSubmenu,
                        contextMenuSubmenuSide === "left"
                          ? styles.contextMenuSubmenuLeft
                          : undefined
                      )}
                      onMouseEnter={() => openContextMenuSubmenu("edit-image")}
                      onMouseLeave={() => scheduleContextMenuSubmenuClose("edit-image")}
                    >
                      <button
                        type="button"
                        className={styles.contextMenuButton}
                        onClick={() => handleContextMenuAction("photopea")}
                      >
                        <span>Edit with Photopea</span>
                      </button>
                      <button
                        type="button"
                        className={styles.contextMenuButton}
                        onClick={() => handleContextMenuAction("auto-levels")}
                      >
                        <span>Auto Levels</span>
                      </button>
                      <button
                        type="button"
                        className={styles.contextMenuButton}
                        disabled={!contextMenuFlipHorizontalAllowed}
                        onClick={() => handleContextMenuAction("flip-horizontal")}
                      >
                        <span>Flip Horizontal</span>
                      </button>
                      <button
                        type="button"
                        className={styles.contextMenuButton}
                        disabled={!contextMenuCollageAllowed}
                        onClick={() => handleContextMenuAction("create-collage")}
                      >
                        <span>Create Collage</span>
                      </button>
                      <button
                        type="button"
                        className={styles.contextMenuButton}
                        disabled={!contextMenuStackOverlayAllowed}
                        onClick={() => handleContextMenuAction("stack-overlay")}
                      >
                        <span>Stack Overlay</span>
                      </button>
                      <button
                        type="button"
                        className={styles.contextMenuButton}
                        disabled={!contextMenuStackImagesAllowed}
                        onClick={() => handleContextMenuAction("stack-images")}
                      >
                        <span>Stack Images</span>
                      </button>
                      <button
                        type="button"
                        className={styles.contextMenuButton}
                        disabled={!contextMenuMergeDigidealMainAllowed}
                        onClick={() => handleContextMenuAction("merge-digideal-main")}
                      >
                        <span>Merge DigiDeal Main</span>
                      </button>
                      <button
                        type="button"
                        className={styles.contextMenuButton}
                        disabled={!contextMenuMergeDigidealMainOverlayAllowed}
                        onClick={() =>
                          handleContextMenuAction("merge-digideal-main-overlay")
                        }
                      >
                        <span>DigiDeal Main Overlay</span>
                      </button>
                      <button
                        type="button"
                        className={styles.contextMenuButton}
                        disabled={!contextMenuMergeDigidealMainStackOverlayAllowed}
                        onClick={() =>
                          handleContextMenuAction("merge-digideal-main-stack-overlay")
                        }
                      >
                        <span>DigiDeal Main Stack Overlay</span>
                      </button>
                    </div>
                  ) : null}
                </div>
	            ) : null}
	            {contextMenu.image ? (
	              <>
			                <button
			                  type="button"
			                  className={styles.contextMenuButton}
		                  onMouseEnter={closeAnyOpenContextMenuSubmenuWithDelay}
		                  onClick={() => handleContextMenuAction("ai-auto-center-white")}
		                >
	                  {renderContextMenuIcon("focuscenter")}
	                  <span>Edit Auto Center Wide</span>
	                </button>
			                <div
			                  className={styles.contextMenuSubmenuWrap}
			                  onMouseEnter={() => {
	                            openContextMenuSubmenu("edit-chatgpt", {
                              resetNested: true,
                            });
	                          }}
	                          onMouseLeave={() => {
	                            scheduleContextMenuSubmenuClose("edit-chatgpt");
	                          }}
			                >
		                  <button
		                    type="button"
		                    className={styles.contextMenuButton}
		                    onMouseEnter={() => {
	                          openContextMenuSubmenu("edit-chatgpt", {
                              resetNested: true,
                            });
	                        }}
		                    onClick={(event) => {
		                      event.preventDefault();
		                      event.stopPropagation();
		                      openContextMenuSubmenu("edit-chatgpt", {
                              resetNested: true,
                            });
		                    }}
		                  >
	                    {renderContextMenuIcon("ai")}
	                    <span>Edit ChatGPT</span>
	                    <span className={styles.contextMenuButtonCaret}>
	                      {contextMenuSubmenuSide === "left" ? "‹" : "›"}
	                    </span>
	                  </button>
		                  {contextMenuSubmenu === "edit-chatgpt" ? (
			                    <div
			                      className={mergeClasses(
			                        styles.contextMenuSubmenu,
			                        contextMenuSubmenuSide === "left"
			                          ? styles.contextMenuSubmenuLeft
			                          : undefined
			                      )}
                              onMouseEnter={() =>
                                openContextMenuSubmenu("edit-chatgpt", {
                                  resetNested: false,
                                })
                              }
                              onMouseLeave={() =>
                                scheduleContextMenuSubmenuClose("edit-chatgpt")
                              }
			                    >
			                        <div
			                          className={styles.contextMenuSubmenuWrap}
			                          onMouseEnter={() =>
			                            openContextMenuNestedSubmenu("chatgpt-template-count")
			                          }
		                              onMouseLeave={() =>
		                                scheduleContextMenuNestedClose("chatgpt-template-count")
		                              }
			                        >
			                          <button
			                            type="button"
			                            className={styles.contextMenuButton}
		                                onMouseEnter={() =>
		                                  openContextMenuNestedSubmenu("chatgpt-template-count")
		                                }
		                                onClick={(event) => {
		                                  event.preventDefault();
		                                  event.stopPropagation();
		                                  openContextMenuNestedSubmenu("chatgpt-template-count");
		                                }}
			                          >
			                            <span>Standard Template</span>
			                            <span className={styles.contextMenuButtonCaret}>
			                              {contextMenuSubmenuSide === "left" ? "‹" : "›"}
			                            </span>
			                          </button>
			                          {contextMenuNestedSubmenu === "chatgpt-template-count" ? (
		                                <div
		                                  className={mergeClasses(
		                                    styles.contextMenuSubmenu,
		                                    contextMenuSubmenuSide === "left"
		                                      ? styles.contextMenuSubmenuLeft
		                                      : undefined
		                                  )}
		                                  onMouseEnter={() =>
		                                    openContextMenuNestedSubmenu("chatgpt-template-count")
		                                  }
		                                  onMouseLeave={() =>
		                                    scheduleContextMenuNestedClose("chatgpt-template-count")
		                                  }
		                                >
		                                  <button
		                                    type="button"
		                                    className={styles.contextMenuButton}
		                                    onClick={() =>
		                                      handleContextMenuAction("ai-chatgpt-template:1")
		                                    }
		                                  >
		                                    <span>1 image</span>
		                                  </button>
		                                  <button
		                                    type="button"
		                                    className={styles.contextMenuButton}
		                                    onClick={() =>
		                                      handleContextMenuAction("ai-chatgpt-template:2")
		                                    }
		                                  >
		                                    <span>2 images</span>
		                                  </button>
		                                  <button
		                                    type="button"
		                                    className={styles.contextMenuButton}
		                                    onClick={() =>
		                                      handleContextMenuAction("ai-chatgpt-template:3")
		                                    }
		                                  >
		                                    <span>3 images</span>
		                                  </button>
		                                </div>
		                              ) : null}
			                        </div>
			                        <div
			                          className={styles.contextMenuSubmenuWrap}
			                          onMouseEnter={() =>
			                            openContextMenuNestedSubmenu("chatgpt-direct-count")
			                          }
		                              onMouseLeave={() =>
		                                scheduleContextMenuNestedClose("chatgpt-direct-count")
		                              }
			                        >
			                          <button
			                            type="button"
			                            className={styles.contextMenuButton}
		                                onMouseEnter={() =>
		                                  openContextMenuNestedSubmenu("chatgpt-direct-count")
		                                }
		                                onClick={(event) => {
		                                  event.preventDefault();
		                                  event.stopPropagation();
		                                  openContextMenuNestedSubmenu("chatgpt-direct-count");
		                                }}
			                          >
			                            <span>Direct</span>
			                            <span className={styles.contextMenuButtonCaret}>
			                              {contextMenuSubmenuSide === "left" ? "‹" : "›"}
			                            </span>
			                          </button>
			                          {contextMenuNestedSubmenu === "chatgpt-direct-count" ? (
		                                <div
		                                  className={mergeClasses(
		                                    styles.contextMenuSubmenu,
		                                    contextMenuSubmenuSide === "left"
		                                      ? styles.contextMenuSubmenuLeft
		                                      : undefined
		                                  )}
		                                  onMouseEnter={() =>
		                                    openContextMenuNestedSubmenu("chatgpt-direct-count")
		                                  }
		                                  onMouseLeave={() =>
		                                    scheduleContextMenuNestedClose("chatgpt-direct-count")
		                                  }
		                                >
		                                  <button
		                                    type="button"
		                                    className={styles.contextMenuButton}
		                                    onClick={() =>
		                                      handleContextMenuAction("ai-chatgpt-direct:1")
		                                    }
		                                  >
		                                    <span>1 image</span>
		                                  </button>
		                                  <button
		                                    type="button"
		                                    className={styles.contextMenuButton}
		                                    onClick={() =>
		                                      handleContextMenuAction("ai-chatgpt-direct:2")
		                                    }
		                                  >
		                                    <span>2 images</span>
		                                  </button>
		                                  <button
		                                    type="button"
		                                    className={styles.contextMenuButton}
		                                    onClick={() =>
		                                      handleContextMenuAction("ai-chatgpt-direct:3")
		                                    }
		                                  >
		                                    <span>3 images</span>
		                                  </button>
		                                </div>
		                              ) : null}
			                        </div>
		                        <div
		                          className={styles.contextMenuSubmenuWrap}
		                          onMouseEnter={() =>
		                            openContextMenuNestedSubmenu("chatgpt-digideal-single")
		                          }
	                              onMouseLeave={() =>
	                                scheduleContextMenuNestedClose(
                                    "chatgpt-digideal-single"
                                  )
	                              }
		                        >
                          <button
                            type="button"
                            className={styles.contextMenuButton}
	                            onMouseEnter={() =>
	                              openContextMenuNestedSubmenu("chatgpt-digideal-single")
	                            }
	                            onClick={(event) => {
	                              event.preventDefault();
	                              event.stopPropagation();
	                              openContextMenuNestedSubmenu(
                                "chatgpt-digideal-single"
                              );
	                            }}
                          >
                            <span>DigiDeal Main Single</span>
                            <span className={styles.contextMenuButtonCaret}>
                              {contextMenuSubmenuSide === "left" ? "‹" : "›"}
                            </span>
                          </button>
                          {contextMenuNestedSubmenu === "chatgpt-digideal-single" ? (
	                            <div
	                              className={mergeClasses(
	                                styles.contextMenuSubmenu,
	                                contextMenuSubmenuSide === "left"
	                                  ? styles.contextMenuSubmenuLeft
	                                  : undefined
	                              )}
                                onMouseEnter={() =>
                                  openContextMenuNestedSubmenu(
                                    "chatgpt-digideal-single"
                                  )
                                }
                                onMouseLeave={() =>
                                  scheduleContextMenuNestedClose(
                                    "chatgpt-digideal-single"
                                  )
                                }
	                            >
                              <button
                                type="button"
                                className={styles.contextMenuButton}
                                onClick={() =>
                                  handleContextMenuAction("ai-chatgpt-digideal-main-single:1")
                                }
                              >
                                <span>1 image</span>
                              </button>
                              <button
                                type="button"
                                className={styles.contextMenuButton}
                                onClick={() =>
                                  handleContextMenuAction("ai-chatgpt-digideal-main-single:2")
                                }
                              >
                                <span>2 images</span>
                              </button>
                              <button
                                type="button"
                                className={styles.contextMenuButton}
                                onClick={() =>
                                  handleContextMenuAction("ai-chatgpt-digideal-main-single:3")
                                }
                              >
                                <span>3 images</span>
                              </button>
                            </div>
                          ) : null}
                        </div>

		                        <div
		                          className={styles.contextMenuSubmenuWrap}
		                          onMouseEnter={() =>
		                            openContextMenuNestedSubmenu("chatgpt-digideal-dual")
		                          }
	                              onMouseLeave={() =>
	                                scheduleContextMenuNestedClose("chatgpt-digideal-dual")
	                              }
		                        >
                          <button
                            type="button"
                            className={styles.contextMenuButton}
	                            onMouseEnter={() =>
	                              openContextMenuNestedSubmenu("chatgpt-digideal-dual")
	                            }
	                            onClick={(event) => {
	                              event.preventDefault();
	                              event.stopPropagation();
	                              openContextMenuNestedSubmenu("chatgpt-digideal-dual");
	                            }}
                          >
                            <span>DigiDeal Main Dual</span>
                            <span className={styles.contextMenuButtonCaret}>
                              {contextMenuSubmenuSide === "left" ? "‹" : "›"}
                            </span>
                          </button>
                          {contextMenuNestedSubmenu === "chatgpt-digideal-dual" ? (
	                            <div
	                              className={mergeClasses(
	                                styles.contextMenuSubmenu,
	                                contextMenuSubmenuSide === "left"
	                                  ? styles.contextMenuSubmenuLeft
	                                  : undefined
	                              )}
                                onMouseEnter={() =>
                                  openContextMenuNestedSubmenu("chatgpt-digideal-dual")
                                }
                                onMouseLeave={() =>
                                  scheduleContextMenuNestedClose("chatgpt-digideal-dual")
                                }
	                            >
                              <button
                                type="button"
                                className={styles.contextMenuButton}
                                onClick={() =>
                                  handleContextMenuAction("ai-chatgpt-digideal-main-dual:1")
                                }
                              >
                                <span>1 image</span>
                              </button>
                              <button
                                type="button"
                                className={styles.contextMenuButton}
                                onClick={() =>
                                  handleContextMenuAction("ai-chatgpt-digideal-main-dual:2")
                                }
                              >
                                <span>2 images</span>
                              </button>
                              <button
                                type="button"
                                className={styles.contextMenuButton}
                                onClick={() =>
                                  handleContextMenuAction("ai-chatgpt-digideal-main-dual:3")
                                }
                              >
                                <span>3 images</span>
                              </button>
                            </div>
                          ) : null}
                        </div>

		                        <div
		                          className={styles.contextMenuSubmenuWrap}
		                          onMouseEnter={() =>
                                openContextMenuNestedSubmenu("chatgpt-scene")
                              }
	                              onMouseLeave={() =>
                                scheduleContextMenuNestedClose("chatgpt-scene")
                              }
		                        >
                          <button
                            type="button"
                            className={styles.contextMenuButton}
	                            onMouseEnter={() =>
                                openContextMenuNestedSubmenu("chatgpt-scene")
                              }
	                            onClick={(event) => {
	                              event.preventDefault();
	                              event.stopPropagation();
	                              openContextMenuNestedSubmenu("chatgpt-scene");
	                            }}
                          >
                            <span>Product Scene</span>
                            <span className={styles.contextMenuButtonCaret}>
                              {contextMenuSubmenuSide === "left" ? "‹" : "›"}
                            </span>
                          </button>
                          {contextMenuNestedSubmenu === "chatgpt-scene" ? (
	                            <div
	                              className={mergeClasses(
	                                styles.contextMenuSubmenu,
	                                contextMenuSubmenuSide === "left"
	                                  ? styles.contextMenuSubmenuLeft
	                                  : undefined
	                              )}
                                onMouseEnter={() =>
                                  openContextMenuNestedSubmenu("chatgpt-scene")
                                }
                                onMouseLeave={() =>
                                  scheduleContextMenuNestedClose("chatgpt-scene")
                                }
	                            >
                              <button
                                type="button"
                                className={styles.contextMenuButton}
                                onClick={() =>
                                  handleContextMenuAction("ai-chatgpt-product-scene:1")
                                }
                              >
                                <span>1 image</span>
                              </button>
                              <button
                                type="button"
                                className={styles.contextMenuButton}
                                onClick={() =>
                                  handleContextMenuAction("ai-chatgpt-product-scene:2")
                                }
                              >
                                <span>2 images</span>
                              </button>
                              <button
                                type="button"
                                className={styles.contextMenuButton}
                                onClick={() =>
                                  handleContextMenuAction("ai-chatgpt-product-scene:3")
                                }
                              >
                                <span>3 images</span>
                              </button>
                            </div>
                          ) : null}
                        </div>
	                        <button
	                          type="button"
	                          className={styles.contextMenuButton}
	                          disabled={!contextMenuCollectionAllowed}
                              onMouseEnter={closeAnyOpenContextMenuNestedSubmenuWithDelay}
	                          onClick={() =>
	                            handleContextMenuAction("ai-chatgpt-product-collection")
	                          }
	                        >
                          <span>Product Collection</span>
                        </button>
	                    </div>
	                  ) : null}
	                </div>

				                <div
				                  className={styles.contextMenuSubmenuWrap}
				                  onMouseEnter={() => {
	                            openContextMenuSubmenu("edit-gemini", {
                              resetNested: true,
                            });
	                          }}
	                          onMouseLeave={() => {
	                            scheduleContextMenuSubmenuClose("edit-gemini");
	                          }}
			                >
		                  <button
		                    type="button"
		                    className={styles.contextMenuButton}
		                    onMouseEnter={() => {
	                          openContextMenuSubmenu("edit-gemini", {
                              resetNested: true,
                            });
	                        }}
		                    onClick={(event) => {
		                      event.preventDefault();
		                      event.stopPropagation();
		                      openContextMenuSubmenu("edit-gemini", {
                              resetNested: true,
                            });
		                    }}
		                  >
	                    {renderContextMenuIcon("ai")}
	                    <span>Edit Gemini</span>
	                    <span className={styles.contextMenuButtonCaret}>
	                      {contextMenuSubmenuSide === "left" ? "‹" : "›"}
	                    </span>
	                  </button>
		                  {contextMenuSubmenu === "edit-gemini" ? (
			                    <div
			                      className={mergeClasses(
			                        styles.contextMenuSubmenu,
			                        contextMenuSubmenuSide === "left"
			                          ? styles.contextMenuSubmenuLeft
			                          : undefined
			                      )}
                              onMouseEnter={() =>
                                openContextMenuSubmenu("edit-gemini", {
                                  resetNested: false,
                                })
                              }
                              onMouseLeave={() =>
                                scheduleContextMenuSubmenuClose("edit-gemini")
                              }
			                    >
			                        <div
			                          className={styles.contextMenuSubmenuWrap}
			                          onMouseEnter={() =>
			                            openContextMenuNestedSubmenu("gemini-template-count")
			                          }
		                              onMouseLeave={() =>
		                                scheduleContextMenuNestedClose("gemini-template-count")
		                              }
			                        >
			                          <button
			                            type="button"
			                            className={styles.contextMenuButton}
		                                onMouseEnter={() =>
		                                  openContextMenuNestedSubmenu("gemini-template-count")
		                                }
		                                onClick={(event) => {
		                                  event.preventDefault();
		                                  event.stopPropagation();
		                                  openContextMenuNestedSubmenu("gemini-template-count");
		                                }}
			                          >
			                            <span>Standard Template</span>
			                            <span className={styles.contextMenuButtonCaret}>
			                              {contextMenuSubmenuSide === "left" ? "‹" : "›"}
			                            </span>
			                          </button>
			                          {contextMenuNestedSubmenu === "gemini-template-count" ? (
		                                <div
		                                  className={mergeClasses(
		                                    styles.contextMenuSubmenu,
		                                    contextMenuSubmenuSide === "left"
		                                      ? styles.contextMenuSubmenuLeft
		                                      : undefined
		                                  )}
		                                  onMouseEnter={() =>
		                                    openContextMenuNestedSubmenu("gemini-template-count")
		                                  }
		                                  onMouseLeave={() =>
		                                    scheduleContextMenuNestedClose("gemini-template-count")
		                                  }
		                                >
		                                  <button
		                                    type="button"
		                                    className={styles.contextMenuButton}
		                                    onClick={() =>
		                                      handleContextMenuAction("ai-gemini-template:1")
		                                    }
		                                  >
		                                    <span>1 image</span>
		                                  </button>
		                                  <button
		                                    type="button"
		                                    className={styles.contextMenuButton}
		                                    onClick={() =>
		                                      handleContextMenuAction("ai-gemini-template:2")
		                                    }
		                                  >
		                                    <span>2 images</span>
		                                  </button>
		                                  <button
		                                    type="button"
		                                    className={styles.contextMenuButton}
		                                    onClick={() =>
		                                      handleContextMenuAction("ai-gemini-template:3")
		                                    }
		                                  >
		                                    <span>3 images</span>
		                                  </button>
		                                </div>
		                              ) : null}
			                        </div>
			                        <div
			                          className={styles.contextMenuSubmenuWrap}
			                          onMouseEnter={() =>
			                            openContextMenuNestedSubmenu("gemini-direct-count")
			                          }
		                              onMouseLeave={() =>
		                                scheduleContextMenuNestedClose("gemini-direct-count")
		                              }
			                        >
			                          <button
			                            type="button"
			                            className={styles.contextMenuButton}
		                                onMouseEnter={() =>
		                                  openContextMenuNestedSubmenu("gemini-direct-count")
		                                }
		                                onClick={(event) => {
		                                  event.preventDefault();
		                                  event.stopPropagation();
		                                  openContextMenuNestedSubmenu("gemini-direct-count");
		                                }}
			                          >
			                            <span>Direct</span>
			                            <span className={styles.contextMenuButtonCaret}>
			                              {contextMenuSubmenuSide === "left" ? "‹" : "›"}
			                            </span>
			                          </button>
			                          {contextMenuNestedSubmenu === "gemini-direct-count" ? (
		                                <div
		                                  className={mergeClasses(
		                                    styles.contextMenuSubmenu,
		                                    contextMenuSubmenuSide === "left"
		                                      ? styles.contextMenuSubmenuLeft
		                                      : undefined
		                                  )}
		                                  onMouseEnter={() =>
		                                    openContextMenuNestedSubmenu("gemini-direct-count")
		                                  }
		                                  onMouseLeave={() =>
		                                    scheduleContextMenuNestedClose("gemini-direct-count")
		                                  }
		                                >
		                                  <button
		                                    type="button"
		                                    className={styles.contextMenuButton}
		                                    onClick={() =>
		                                      handleContextMenuAction("ai-gemini-direct:1")
		                                    }
		                                  >
		                                    <span>1 image</span>
		                                  </button>
		                                  <button
		                                    type="button"
		                                    className={styles.contextMenuButton}
		                                    onClick={() =>
		                                      handleContextMenuAction("ai-gemini-direct:2")
		                                    }
		                                  >
		                                    <span>2 images</span>
		                                  </button>
		                                  <button
		                                    type="button"
		                                    className={styles.contextMenuButton}
		                                    onClick={() =>
		                                      handleContextMenuAction("ai-gemini-direct:3")
		                                    }
		                                  >
		                                    <span>3 images</span>
		                                  </button>
		                                </div>
		                              ) : null}
			                        </div>
		                        <div
		                          className={styles.contextMenuSubmenuWrap}
		                          onMouseEnter={() =>
		                            openContextMenuNestedSubmenu("gemini-digideal-single")
		                          }
	                              onMouseLeave={() =>
	                                scheduleContextMenuNestedClose("gemini-digideal-single")
	                              }
		                        >
                          <button
                            type="button"
                            className={styles.contextMenuButton}
	                            onMouseEnter={() =>
	                              openContextMenuNestedSubmenu("gemini-digideal-single")
	                            }
	                            onClick={(event) => {
	                              event.preventDefault();
	                              event.stopPropagation();
	                              openContextMenuNestedSubmenu("gemini-digideal-single");
	                            }}
                          >
                            <span>DigiDeal Main Single</span>
                            <span className={styles.contextMenuButtonCaret}>
                              {contextMenuSubmenuSide === "left" ? "‹" : "›"}
                            </span>
                          </button>
                          {contextMenuNestedSubmenu === "gemini-digideal-single" ? (
	                            <div
	                              className={mergeClasses(
	                                styles.contextMenuSubmenu,
	                                contextMenuSubmenuSide === "left"
	                                  ? styles.contextMenuSubmenuLeft
	                                  : undefined
	                              )}
                                onMouseEnter={() =>
                                  openContextMenuNestedSubmenu("gemini-digideal-single")
                                }
                                onMouseLeave={() =>
                                  scheduleContextMenuNestedClose("gemini-digideal-single")
                                }
	                            >
                              <button
                                type="button"
                                className={styles.contextMenuButton}
                                onClick={() =>
                                  handleContextMenuAction("ai-gemini-digideal-main-single:1")
                                }
                              >
                                <span>1 image</span>
                              </button>
                              <button
                                type="button"
                                className={styles.contextMenuButton}
                                onClick={() =>
                                  handleContextMenuAction("ai-gemini-digideal-main-single:2")
                                }
                              >
                                <span>2 images</span>
                              </button>
                              <button
                                type="button"
                                className={styles.contextMenuButton}
                                onClick={() =>
                                  handleContextMenuAction("ai-gemini-digideal-main-single:3")
                                }
                              >
                                <span>3 images</span>
                              </button>
                            </div>
                          ) : null}
                        </div>

		                        <div
		                          className={styles.contextMenuSubmenuWrap}
		                          onMouseEnter={() =>
		                            openContextMenuNestedSubmenu("gemini-digideal-dual")
		                          }
	                              onMouseLeave={() =>
	                                scheduleContextMenuNestedClose("gemini-digideal-dual")
	                              }
		                        >
                          <button
                            type="button"
                            className={styles.contextMenuButton}
	                            onMouseEnter={() =>
	                              openContextMenuNestedSubmenu("gemini-digideal-dual")
	                            }
	                            onClick={(event) => {
	                              event.preventDefault();
	                              event.stopPropagation();
	                              openContextMenuNestedSubmenu("gemini-digideal-dual");
	                            }}
                          >
                            <span>DigiDeal Main Dual</span>
                            <span className={styles.contextMenuButtonCaret}>
                              {contextMenuSubmenuSide === "left" ? "‹" : "›"}
                            </span>
                          </button>
                          {contextMenuNestedSubmenu === "gemini-digideal-dual" ? (
	                            <div
	                              className={mergeClasses(
	                                styles.contextMenuSubmenu,
	                                contextMenuSubmenuSide === "left"
	                                  ? styles.contextMenuSubmenuLeft
	                                  : undefined
	                              )}
                                onMouseEnter={() =>
                                  openContextMenuNestedSubmenu("gemini-digideal-dual")
                                }
                                onMouseLeave={() =>
                                  scheduleContextMenuNestedClose("gemini-digideal-dual")
                                }
	                            >
                              <button
                                type="button"
                                className={styles.contextMenuButton}
                                onClick={() =>
                                  handleContextMenuAction("ai-gemini-digideal-main-dual:1")
                                }
                              >
                                <span>1 image</span>
                              </button>
                              <button
                                type="button"
                                className={styles.contextMenuButton}
                                onClick={() =>
                                  handleContextMenuAction("ai-gemini-digideal-main-dual:2")
                                }
                              >
                                <span>2 images</span>
                              </button>
                              <button
                                type="button"
                                className={styles.contextMenuButton}
                                onClick={() =>
                                  handleContextMenuAction("ai-gemini-digideal-main-dual:3")
                                }
                              >
                                <span>3 images</span>
                              </button>
                            </div>
                          ) : null}
                        </div>

		                        <div
		                          className={styles.contextMenuSubmenuWrap}
		                          onMouseEnter={() =>
                                openContextMenuNestedSubmenu("gemini-scene")
                              }
	                              onMouseLeave={() =>
                                scheduleContextMenuNestedClose("gemini-scene")
                              }
		                        >
                          <button
                            type="button"
                            className={styles.contextMenuButton}
	                            onMouseEnter={() =>
                                openContextMenuNestedSubmenu("gemini-scene")
                              }
	                            onClick={(event) => {
	                              event.preventDefault();
	                              event.stopPropagation();
	                              openContextMenuNestedSubmenu("gemini-scene");
	                            }}
                          >
                            <span>Product Scene</span>
                            <span className={styles.contextMenuButtonCaret}>
                              {contextMenuSubmenuSide === "left" ? "‹" : "›"}
                            </span>
                          </button>
                          {contextMenuNestedSubmenu === "gemini-scene" ? (
	                            <div
	                              className={mergeClasses(
	                                styles.contextMenuSubmenu,
	                                contextMenuSubmenuSide === "left"
	                                  ? styles.contextMenuSubmenuLeft
	                                  : undefined
	                              )}
                                onMouseEnter={() =>
                                  openContextMenuNestedSubmenu("gemini-scene")
                                }
                                onMouseLeave={() =>
                                  scheduleContextMenuNestedClose("gemini-scene")
                                }
	                            >
                              <button
                                type="button"
                                className={styles.contextMenuButton}
                                onClick={() =>
                                  handleContextMenuAction("ai-gemini-product-scene:1")
                                }
                              >
                                <span>1 image</span>
                              </button>
                              <button
                                type="button"
                                className={styles.contextMenuButton}
                                onClick={() =>
                                  handleContextMenuAction("ai-gemini-product-scene:2")
                                }
                              >
                                <span>2 images</span>
                              </button>
                              <button
                                type="button"
                                className={styles.contextMenuButton}
                                onClick={() =>
                                  handleContextMenuAction("ai-gemini-product-scene:3")
                                }
                              >
                                <span>3 images</span>
                              </button>
                            </div>
                          ) : null}
                        </div>
	                        <button
	                          type="button"
	                          className={styles.contextMenuButton}
	                          disabled={!contextMenuCollectionAllowed}
                              onMouseEnter={closeAnyOpenContextMenuNestedSubmenuWithDelay}
	                          onClick={() =>
	                            handleContextMenuAction("ai-gemini-product-collection")
	                          }
	                        >
                          <span>Product Collection</span>
                        </button>
	                    </div>
	                  ) : null}
	                </div>
	                <button
	                  type="button"
	                  className={styles.contextMenuButton}
	                  onMouseEnter={closeAnyOpenContextMenuSubmenuWithDelay}
	                  onClick={() => handleContextMenuAction("ai-zimage-direct")}
	                >
                  {renderContextMenuIcon("ai")}
                  <span>Edit Z-Image: Direct</span>
                </button>
	                <button
	                  type="button"
	                  className={styles.contextMenuButton}
	                  onMouseEnter={closeAnyOpenContextMenuSubmenuWithDelay}
	                  onClick={() => handleContextMenuAction("ai-zimage-white")}
	                >
                  {renderContextMenuIcon("background")}
                  <span>Edit Z-Image: White BG</span>
                </button>
	                <button
	                  type="button"
	                  className={styles.contextMenuButton}
	                  onMouseEnter={closeAnyOpenContextMenuSubmenuWithDelay}
	                  onClick={() => handleContextMenuAction("ai-zimage-eraser")}
	                >
                  {renderContextMenuIcon("eraser")}
                  <span>Edit Z-image: Eraser</span>
                </button>
	                <button
	                  type="button"
	                  className={styles.contextMenuButton}
                    disabled={contextMenuUpscaleDisabled}
	                  onMouseEnter={closeAnyOpenContextMenuSubmenuWithDelay}
	                  onClick={() => handleContextMenuAction("ai-zimage-upscale")}
	                >
                  {renderContextMenuIcon("upscale")}
                  <span>Edit Z-Image: Upscale</span>
                </button>
              </>
            ) : null}
	          </div>
	        ) : null}

        <Dialog
          open={autoLevelsOpen}
          onOpenChange={(_, data) => {
            if (!data.open) {
              closeAutoLevelsDialog();
            }
          }}
        >
          <DialogSurface className={styles.autoLevelsSurface}>
            <DialogBody className={styles.autoLevelsBody}>
              <DialogTitle>{`Auto Levels (${autoLevelsTargets.length})`}</DialogTitle>
              {autoLevelsError ? (
                <Text size={200} style={{ color: tokens.colorStatusDangerForeground1 }}>
                  {autoLevelsError}
                </Text>
              ) : (
                <Text size={100} className={styles.filesInfo}>
                  Adjust level intensity and quality, then apply to selected images.
                </Text>
              )}
              <div className={styles.autoLevelsControls}>
                <div className={styles.autoLevelsControlCard}>
                  <Field label="Level Percent">
                    <div className={styles.autoLevelsInlineRow}>
                      <input
                        type="range"
                        min={0}
                        max={100}
                        step={1}
                        value={autoLevelsLevelPercent}
                        className={styles.autoLevelsSlider}
                        disabled={autoLevelsSubmitting}
                        onChange={(event) => {
                          const nextValue = Number(event.currentTarget.value);
                          setAutoLevelsLevelPercent(clampAutoLevelsPercent(nextValue));
                        }}
                      />
                      <Input
                        type="number"
                        min={0}
                        max={100}
                        step={1}
                        value={String(autoLevelsLevelPercent)}
                        className={styles.autoLevelsNumberInput}
                        disabled={autoLevelsSubmitting}
                        onChange={(_, data) => {
                          const raw = String(data.value || "").trim();
                          if (!raw) {
                            setAutoLevelsLevelPercent(50);
                            return;
                          }
                          const nextValue = Number(raw);
                          if (!Number.isFinite(nextValue)) return;
                          setAutoLevelsLevelPercent(clampAutoLevelsPercent(nextValue));
                        }}
                      />
                    </div>
                  </Field>
                  <Field label="Quality">
                    <div className={styles.autoLevelsInlineRow}>
                      <input
                        type="range"
                        min={1}
                        max={100}
                        step={1}
                        value={autoLevelsQuality}
                        className={styles.autoLevelsSlider}
                        disabled={autoLevelsSubmitting}
                        onChange={(event) => {
                          const nextValue = Number(event.currentTarget.value);
                          setAutoLevelsQuality(clampAutoLevelsQuality(nextValue));
                        }}
                      />
                      <Input
                        type="number"
                        min={1}
                        max={100}
                        step={1}
                        value={String(autoLevelsQuality)}
                        className={styles.autoLevelsNumberInput}
                        disabled={autoLevelsSubmitting}
                        onChange={(_, data) => {
                          const raw = String(data.value || "").trim();
                          if (!raw) {
                            setAutoLevelsQuality(92);
                            return;
                          }
                          const nextValue = Number(raw);
                          if (!Number.isFinite(nextValue)) return;
                          setAutoLevelsQuality(clampAutoLevelsQuality(nextValue));
                        }}
                      />
                    </div>
                  </Field>
                  <Text size={100} className={styles.filesInfo}>
                    {`Mapped bounds: lower ${autoLevelsBounds.lower} / upper ${autoLevelsBounds.upper}`}
                  </Text>
                </div>
                <div className={styles.autoLevelsThumbWrap}>
                  <div
                    className={mergeClasses(
                      styles.autoLevelsThumbGrid,
                      autoLevelsSubmitting ? styles.autoLevelsThumbGridBusy : undefined
                    )}
                  >
                    {autoLevelsTargets.map((entry) => (
                      <div key={entry.path} className={styles.autoLevelsThumbCard}>
                        <div className={styles.autoLevelsThumbImageFrame}>
                          <img
                            src={buildDraftDownloadUrl(
                              entry.path,
                              entryByPath.get(entry.path)?.modifiedAt ?? entry.modifiedAt
                            )}
                            alt={entry.name}
                            className={styles.autoLevelsThumbImage}
                          />
                        </div>
                        <Text
                          size={100}
                          className={styles.autoLevelsThumbName}
                          title={entry.name}
                        >
                          {entry.name}
                        </Text>
                      </div>
                    ))}
                  </div>
                  {autoLevelsSubmitting ? (
                    <div className={styles.autoLevelsBusyOverlay}>
                      <Spinner size="medium" />
                    </div>
                  ) : null}
                </div>
              </div>
              <DialogActions>
                <Button
                  appearance="outline"
                  onClick={() => closeAutoLevelsDialog()}
                  disabled={autoLevelsSubmitting}
                >
                  {t("common.close")}
                </Button>
                <Button
                  appearance="primary"
                  onClick={() => void applyAutoLevelsForDialogTargets()}
                  disabled={autoLevelsSubmitting || autoLevelsTargets.length === 0}
                >
                  {autoLevelsSubmitting ? "Applying..." : "Save and Edit"}
                </Button>
              </DialogActions>
            </DialogBody>
          </DialogSurface>
        </Dialog>

        <Dialog
          open={stackOverlayPickerOpen}
          onOpenChange={(_, data) => {
            if (!data.open) {
              closeStackOverlayPickerDialog();
            }
          }}
        >
          <DialogSurface className={styles.stackOverlaySurface}>
            <DialogBody className={styles.stackOverlayBody}>
              <DialogTitle>{`${
                stackOverlayMode === "stack_overlay"
                  ? "Stack Overlay"
                  : "DigiDeal Main Stack Overlay"
              } (${stackOverlayTargets.length})`}</DialogTitle>
              {stackOverlayError ? (
                <Text size={200} style={{ color: tokens.colorStatusDangerForeground1 }}>
                  {stackOverlayError}
                </Text>
              ) : stackOverlayInfo ? (
                <Text size={100} className={styles.filesInfo}>
                  {stackOverlayInfo}
                </Text>
              ) : (
                <Text size={100} className={styles.filesInfo}>
                  Select which white-background image should be on top, then save.
                </Text>
              )}
              {stackOverlayMode === "digideal_main_stack_overlay" ? (
                <Text size={100} className={styles.stackOverlayTagLine}>
                  Only white-background candidates are shown here.
                </Text>
              ) : null}
              <div className={styles.stackOverlayThumbGrid}>
                {stackOverlayCandidateTargets.map((entry) => (
                  <button
                    key={entry.path}
                    type="button"
                    className={mergeClasses(
                      styles.stackOverlayThumbCardButton,
                      styles.autoLevelsThumbCard,
                      entry.path === stackOverlayTopImagePath
                        ? styles.stackOverlayThumbCardSelected
                        : undefined
                    )}
                    disabled={stackOverlaySubmitting}
                    onClick={() => setStackOverlayTopImagePath(entry.path)}
                  >
                    <div className={styles.autoLevelsThumbImageFrame}>
                      <img
                        src={buildDraftDownloadUrl(
                          entry.path,
                          entryByPath.get(entry.path)?.modifiedAt ?? entry.modifiedAt
                        )}
                        alt={entry.name}
                        className={styles.autoLevelsThumbImage}
                      />
                    </div>
                    <Text size={100} className={styles.autoLevelsThumbName} title={entry.name}>
                      {entry.name}
                    </Text>
                    {entry.path === stackOverlayTopImagePath ? (
                      <Text size={100} className={styles.stackOverlayTopMarker}>
                        Top image
                      </Text>
                    ) : null}
                  </button>
                ))}
              </div>
              <DialogActions>
                <Button
                  appearance="outline"
                  onClick={() => closeStackOverlayPickerDialog()}
                  disabled={stackOverlaySubmitting}
                >
                  {t("common.close")}
                </Button>
                <Button
                  appearance="primary"
                  onClick={() => void applyStackOverlayForDialogTargets()}
                  disabled={
                    stackOverlaySubmitting ||
                    stackOverlayCandidateTargets.length === 0 ||
                    !stackOverlayTopImagePath
                  }
                >
                  {stackOverlaySubmitting ? "Running..." : "Save"}
                </Button>
              </DialogActions>
            </DialogBody>
          </DialogSurface>
        </Dialog>

	        <Dialog
	          open={photopeaOpen}
	          onOpenChange={(_, data) => {
	            if (!data.open) {
	              if (photopeaExporting || photopeaPersisting) return;
	              closePhotopea();
	            }
	          }}
	        >
	          <DialogSurface className={styles.photopeaSurface}>
	            <DialogBody className={styles.photopeaBody}>
	              <DialogTitle>
	                {`Photopea - ${photopeaEntry?.name ?? ""}`}
	              </DialogTitle>
	              {photopeaError ? (
	                <Text size={200} style={{ color: tokens.colorStatusDangerForeground1 }}>
	                  {photopeaError}
	                </Text>
	              ) : (
	                <Text size={100} className={styles.filesInfo}>
	                  Click Save below to export a JPG from Photopea and replace the draft file.
	                </Text>
	              )}
	              <div className={styles.photopeaFrameWrap}>
	                <iframe
	                  key={`photopea-${photopeaSessionKey}`}
	                  ref={photopeaIframeRef}
	                  className={styles.photopeaFrame}
	                  src={buildPhotopeaUrl()}
	                  title="Photopea"
	                  allow="clipboard-read; clipboard-write"
	                  onLoad={() => {
	                    const win = photopeaIframeRef.current?.contentWindow;
	                    if (!win) return;
	                    // Ping Photopea so it echoes a ready marker back to us (and then "done").
	                    win.postMessage(
	                      'app.echoToOE("__hub_photopea_ready__");',
	                      "https://www.photopea.com"
	                    );
	                  }}
	                />
	                {photopeaLoading || photopeaExporting || photopeaPersisting ? (
	                  <div className={styles.entriesContentOverlay}>
	                    <Spinner size="tiny" />
	                  </div>
	                ) : null}
	              </div>
	              <DialogActions>
	                <Button
	                  appearance="outline"
	                  onClick={requestPhotopeaExport}
	                  disabled={!photopeaReady || photopeaLoading || photopeaExporting || photopeaPersisting}
	                >
	                  {photopeaExporting
	                    ? "Exporting..."
	                    : photopeaPersisting
	                      ? "Saving..."
	                      : "Save"}
	                </Button>
	                <Button
	                  appearance="primary"
	                  onClick={closePhotopea}
	                  disabled={photopeaExporting || photopeaPersisting}
	                >
	                  {t("common.close")}
	                </Button>
	              </DialogActions>
	            </DialogBody>
	          </DialogSurface>
	        </Dialog>

        <Dialog
          open={Boolean(aiDualDialog)}
          onOpenChange={(_, data) => {
            if (!data.open) {
              closeAiDualDialog();
            }
          }}
        >
          <DialogSurface className={styles.aiDualSurface}>
            <DialogBody className={styles.aiDualBody}>
              <DialogTitle>
                {`Generate Dual Image with ${aiDualProviderLabel}${
                  aiDualDialog
                    ? ` (${aiDualDialog.outputCount} output${
                        aiDualDialog.outputCount === 1 ? "" : "s"
                      })`
                    : ""
                }`}
              </DialogTitle>
              {aiDualError ? (
                <Text size={200} style={{ color: tokens.colorStatusDangerForeground1 }}>
                  {aiDualError}
                </Text>
              ) : null}
              <div className={styles.aiDualSwapRow}>
                <Text size={200} className={styles.aiDualCardHint}>
                  Left side is the pure product image on white. Right side is scene guidance.
                  Drag one card onto the other or use swap.
                </Text>
                <Button appearance="outline" onClick={swapAiDualRoles} disabled={aiDualSubmitting}>
                  Swap Left/Right
                </Button>
              </div>
              <div className={styles.aiDualGrid}>
                <div
                  className={mergeClasses(
                    styles.aiDualCard,
                    aiDualDragRole === "guidance" ? styles.aiDualCardDropTarget : undefined,
                    aiDualDragRole === "product" ? styles.aiDualCardDragging : undefined
                  )}
                  draggable={!aiDualSubmitting}
                  onDragStart={() => setAiDualDragRole("product")}
                  onDragEnd={() => setAiDualDragRole(null)}
                  onDragOver={(event) => event.preventDefault()}
                  onDrop={(event) => {
                    event.preventDefault();
                    handleAiDualRoleDrop("product");
                  }}
                >
                  <Text size={300} className={styles.aiDualCardTitle}>
                    Left: Pure Product (White Background)
                  </Text>
                  <Text size={200} className={styles.aiDualCardHint}>
                    {aiDualDialog?.productEntry.name || "-"}
                  </Text>
                  <div className={styles.aiDualPreviewFrame}>
                    {aiDualDialog ? (
                      <img
                        src={aiDualProductPreviewUrl}
                        alt={aiDualDialog.productEntry.name}
                        className={styles.aiDualPreviewImage}
                      />
                    ) : null}
                  </div>
                </div>
                <div
                  className={mergeClasses(
                    styles.aiDualCard,
                    aiDualDragRole === "product" ? styles.aiDualCardDropTarget : undefined,
                    aiDualDragRole === "guidance" ? styles.aiDualCardDragging : undefined
                  )}
                  draggable={!aiDualSubmitting}
                  onDragStart={() => setAiDualDragRole("guidance")}
                  onDragEnd={() => setAiDualDragRole(null)}
                  onDragOver={(event) => event.preventDefault()}
                  onDrop={(event) => {
                    event.preventDefault();
                    handleAiDualRoleDrop("guidance");
                  }}
                >
                  <Text size={300} className={styles.aiDualCardTitle}>
                    Right: Scene / Environment Guidance
                  </Text>
                  <Text size={200} className={styles.aiDualCardHint}>
                    {aiDualDialog?.guidanceEntry.name || "-"}
                  </Text>
                  <div className={styles.aiDualPreviewFrame}>
                    {aiDualDialog ? (
                      <img
                        src={aiDualGuidancePreviewUrl}
                        alt={aiDualDialog.guidanceEntry.name}
                        className={styles.aiDualPreviewImage}
                      />
                    ) : null}
                  </div>
                </div>
              </div>
              <Field label="Additional guidance (optional)">
                <Textarea
                  value={aiDualPrompt}
                  onChange={(_, data) => setAiDualPrompt(data.value)}
                  resize="vertical"
                  rows={4}
                  disabled={aiDualSubmitting}
                  onKeyDown={(event) => {
                    if ((event.ctrlKey || event.metaKey) && event.key === "Enter") {
                      event.preventDefault();
                      submitAiDualTemplatePreset();
                    }
                  }}
                />
              </Field>
              <Text size={200} className={styles.aiDualCardHint}>
                The two images are welded into one reference image with white separation before
                being sent to the model.
              </Text>
              <DialogActions>
                <Button
                  appearance="outline"
                  onClick={closeAiDualDialog}
                  disabled={aiDualSubmitting}
                >
                  {t("common.close")}
                </Button>
                <Button
                  appearance="primary"
                  onClick={submitAiDualTemplatePreset}
                  disabled={!aiDualDialog || aiDualSubmitting}
                >
                  {aiDualSubmitting ? "Running..." : "Generate Dual Image"}
                </Button>
              </DialogActions>
            </DialogBody>
          </DialogSurface>
        </Dialog>

        <Dialog
          open={Boolean(aiCollectionDialog)}
          onOpenChange={(_, data) => {
            if (!data.open) {
              closeAiCollectionDialog();
            }
          }}
        >
          <DialogSurface className={styles.aiCollectionSurface}>
            <DialogBody className={styles.aiCollectionBody}>
              <DialogTitle>
                {`Product Collection with ${aiCollectionProviderLabel} (${
                  aiCollectionDialog?.entries.length ?? 0
                } images)`}
              </DialogTitle>
              {aiCollectionError ? (
                <Text size={200} style={{ color: tokens.colorStatusDangerForeground1 }}>
                  {aiCollectionError}
                </Text>
              ) : null}
              <Text size={200} className={styles.aiCollectionHint}>
                Selected images are welded into one contact-sheet input before running AI.
              </Text>
              <div className={styles.aiCollectionPreviewBox}>
                <div className={styles.aiCollectionGrid}>
                  {aiCollectionPreviewItems.map((item, index) => (
                    <div className={styles.aiCollectionCard} key={item.key}>
                      <Text size={200} className={styles.aiCollectionCardTitle}>
                        {`Image ${index + 1}`}
                      </Text>
                      <Text size={100} className={styles.aiCollectionHint}>
                        {item.name}
                      </Text>
                      <div className={styles.aiCollectionThumbFrame}>
                        <img
                          src={item.url}
                          alt={item.name}
                          className={styles.aiCollectionThumbImage}
                        />
                      </div>
                    </div>
                  ))}
                </div>
              </div>
              <Field label="Additional guidance (optional)">
                <Textarea
                  value={aiCollectionPrompt}
                  onChange={(_, data) => setAiCollectionPrompt(data.value)}
                  resize="vertical"
                  rows={3}
                  disabled={aiCollectionSubmitting}
                  onKeyDown={(event) => {
                    if ((event.ctrlKey || event.metaKey) && event.key === "Enter") {
                      event.preventDefault();
                      submitAiCollection();
                    }
                  }}
                />
              </Field>
              <DialogActions>
                <Button
                  appearance="outline"
                  onClick={closeAiCollectionDialog}
                  disabled={aiCollectionSubmitting}
                >
                  {t("common.close")}
                </Button>
                <Button
                  appearance="primary"
                  onClick={submitAiCollection}
                  disabled={!aiCollectionDialog || aiCollectionSubmitting}
                >
                  {aiCollectionSubmitting ? "Running..." : "Make AI Edit"}
                </Button>
              </DialogActions>
            </DialogBody>
          </DialogSurface>
        </Dialog>

	        <Dialog
	          open={aiEditTargets.length > 0}
	          onOpenChange={(_, data) => {
	            if (!data.open) {
              cancelAiEdit();
            }
          }}
        >
          <DialogSurface className={styles.aiPromptSurface}>
            <DialogBody className={styles.aiPromptBody}>
              <DialogTitle>
                {aiEditTargets.length > 0
                  ? aiEditTargets.length === 1
                    ? `Edit one image with ${aiEditProviderLabel} (${aiEditModeLabel}${aiEditOutputCountLabel})`
                    : `Edit ${aiEditTargets.length} images with ${aiEditProviderLabel} (${aiEditModeLabel}${aiEditOutputCountLabel})`
                  : "AI Edit"}
              </DialogTitle>
              {aiEditError ? (
                <Text size={200} style={{ color: tokens.colorStatusDangerForeground1 }}>
                  {aiEditError}
                </Text>
              ) : null}
              {aiEditMode === "eraser" ? (
                <Field label="Eraser Mode">
                  <div style={{ display: "flex", gap: "8px", flexWrap: "wrap" }}>
                    <Button
                      appearance={aiEraserRunMode === "prompt" ? "primary" : "outline"}
                      onClick={() => setAiEraserRunMode("prompt")}
                      disabled={aiEditSubmitting}
                    >
                      Prompt only
                    </Button>
                    <Button
                      appearance={aiEraserRunMode === "mask" ? "primary" : "outline"}
                      onClick={() => setAiEraserRunMode("mask")}
                      disabled={aiEditTargets.length !== 1 || aiEditSubmitting}
                    >
                      Prompt + painted mask
                    </Button>
                  </div>
                  {aiEditTargets.length !== 1 ? (
                    <Text size={200} className={styles.aiMaskHint}>
                      Mask mode is available only for one image at a time.
                    </Text>
                  ) : null}
                </Field>
              ) : null}
              {aiEditSingleTarget ? (
                aiEditShowMaskPainter ? (
                  <div className={styles.aiMaskEditor}>
                    <div className={styles.aiMaskStage}>
                      <img
                        ref={aiMaskImageRef}
                        src={aiEditSingleTargetPreviewUrl}
                        alt={aiEditSingleTarget.name}
                        className={styles.aiMaskBaseImage}
                        onLoad={() => {
                          syncAiMaskCanvasToImage(true);
                        }}
                      />
                      <div
                        className={styles.aiMaskCursor}
                        style={{
                          left: `${aiMaskCursor.x}px`,
                          top: `${aiMaskCursor.y}px`,
                          width: `${aiMaskCursor.size}px`,
                          height: `${aiMaskCursor.size}px`,
                          display: aiMaskCursor.visible ? "block" : "none",
                        }}
                      />
                      <canvas
                        ref={aiMaskCanvasRef}
                        className={styles.aiMaskCanvas}
                        onPointerEnter={(event) => {
                          setAiMaskCursorFromPointer(event, true);
                        }}
                        onPointerDown={handleAiMaskPointerDown}
                        onPointerMove={handleAiMaskPointerMove}
                        onPointerUp={(event) => stopAiMaskDrawing(event.pointerId)}
                        onPointerCancel={(event) => {
                          stopAiMaskDrawing(event.pointerId);
                          setAiMaskCursor((prev) =>
                            prev.visible ? { ...prev, visible: false } : prev
                          );
                        }}
                        onPointerLeave={(event) => {
                          if (
                            aiMaskDrawingRef.current &&
                            aiMaskPointerIdRef.current === event.pointerId
                          ) {
                            stopAiMaskDrawing(event.pointerId);
                          }
                          setAiMaskCursor((prev) =>
                            prev.visible ? { ...prev, visible: false } : prev
                          );
                        }}
                      />
                    </div>
                    <div className={styles.aiMaskToolbar}>
                      <Field label={`Brush size (${aiMaskBrushSize}px)`}>
                        <input
                          type="range"
                          min={6}
                          max={180}
                          step={1}
                          value={aiMaskBrushSize}
                          onChange={(event) => {
                            const nextValue = Number(event.currentTarget.value);
                            setAiMaskBrushSize(
                              Number.isFinite(nextValue)
                                ? Math.max(6, Math.min(180, Math.round(nextValue)))
                                : 36
                            );
                          }}
                          className={styles.aiMaskBrushSlider}
                        />
                      </Field>
                      <div style={{ display: "flex", justifyContent: "flex-end" }}>
                        <Button
                          appearance="subtle"
                          onClick={clearAiMaskCanvas}
                          disabled={!aiMaskHasPaint || aiEditSubmitting}
                        >
                          Clear mask
                        </Button>
                      </div>
                    </div>
                    <Text size={200} className={styles.aiMaskHint}>
                      {aiMaskHasPaint
                        ? "Mask will be uploaded and aligned pixel-for-pixel to this image."
                        : "No mask painted yet. Running now will use prompt-only auto edit."}
                    </Text>
                  </div>
                ) : (
                  <div className={styles.aiPromptImageWrap}>
                    <img
                      src={aiEditSingleTargetPreviewUrl}
                      alt={aiEditSingleTarget.name}
                      className={styles.aiPromptImagePreview}
                    />
                  </div>
                )
              ) : null}
              {aiEditMode === "eraser" && aiEditTargets.length > 1 ? (
                <Text size={200} className={styles.aiMaskHint}>
                  Mask painter is available only for single-image eraser edits. Multi-image runs
                  use prompt-only auto mode.
                </Text>
              ) : null}
              {aiEditHasPromptInput ? (
	                <Field
	                  label={
	                    aiEditMode === "template"
                      ? aiEditTemplatePreset === "digideal_main"
                          ? "Micro prompt (optional, added to DigiDeal Main Single template)"
                          : aiEditTemplatePreset === "digideal_main_dual"
                            ? "Guidance prompt (optional, added to DigiDeal Main Dual template)"
                          : aiEditTemplatePreset === "product_scene"
                            ? "Guidance prompt (optional, added to Product Scene template)"
                          : aiEditTemplatePreset === "product_collection"
                            ? "Guidance prompt (optional, added to Product Collection template)"
                          : "Prompt (inserted into standard template)"
	                      : "Prompt (sent directly)"
	                  }
	                >
                  <Textarea
                    value={aiEditPrompt}
                    onChange={(_, data) => setAiEditPrompt(data.value)}
                    resize="vertical"
                    rows={3}
                    onKeyDown={(event) => {
                      if (event.key === "Enter" && !event.shiftKey) {
                        event.preventDefault();
                        submitAiEdit();
                      }
                    }}
                  />
                </Field>
              ) : (
                <Text size={200} className={styles.filesInfo}>
                  This action runs without a prompt.
                </Text>
              )}
              <DialogActions>
                <Button
                  appearance="outline"
                  onClick={cancelAiEdit}
                  disabled={aiEditSubmitting}
                >
                  {t("common.close")}
                </Button>
                {aiEditShowPromptTemplates ? (
                  <Menu positioning="above-end">
                    <MenuTrigger disableButtonEnhancement>
                      <Button appearance="outline" disabled={aiEditSubmitting}>
                        Templates
                      </Button>
                    </MenuTrigger>
                    <MenuPopover>
                      <MenuList className={styles.compactMenuList}>
                        <MenuItem
                          onClick={() => setAiEditPrompt(AI_DIRECT_TEMPLATE_SIMPLE_SWEDISH_PROMPT)}
                        >
                          Translate text to Swedish (Simple)
                        </MenuItem>
                        <MenuItem
                          onClick={() => setAiEditPrompt(AI_DIRECT_TEMPLATE_ADVANCED_SWEDEN_PROMPT)}
                        >
                          Localize to Sweden (Advanced)
                        </MenuItem>
                      </MenuList>
                    </MenuPopover>
                  </Menu>
                ) : null}
                {aiEditShowPromptTemplates ? (
                  <Menu positioning="above-end">
                    <MenuTrigger disableButtonEnhancement>
                      <Button appearance="outline" disabled={aiEditSubmitting}>
                        History
                      </Button>
                    </MenuTrigger>
                    <MenuPopover className={styles.aiPromptHistoryPopover}>
                      <MenuList className={styles.aiPromptHistoryMenuList}>
                        {aiDirectPromptHistoryPreviewRows.length === 0 ? (
                          <MenuItem disabled>No recent prompts yet.</MenuItem>
                        ) : (
                          aiDirectPromptHistoryPreviewRows.map((item) => (
                            <MenuItem key={item.key} onClick={() => setAiEditPrompt(item.key)}>
                              <span className={styles.aiPromptHistoryMenuItemText}>
                                {item.preview}
                              </span>
                            </MenuItem>
                          ))
                        )}
                      </MenuList>
                    </MenuPopover>
                  </Menu>
                ) : null}
                <Button
                  appearance="primary"
                  onClick={submitAiEdit}
                  disabled={aiEditTargets.length === 0 || aiEditSubmitting}
                >
                  {aiEditSubmitting ? "Running..." : "Run AI Edit"}
                </Button>
              </DialogActions>
            </DialogBody>
          </DialogSurface>
        </Dialog>

        <Dialog
          open={Boolean(aiReviewOriginalPath && aiReviewRecord)}
          onOpenChange={(_, data) => {
            if (!data.open && !aiReviewSubmitting) {
              setAiReviewOriginalPath(null);
            }
          }}
        >
          <DialogSurface className={styles.aiCompareSurface}>
            <DialogBody className={styles.aiCompareBody}>
              <DialogTitle>AI Edit Review</DialogTitle>
              {aiReviewRecord ? (
                <div className={styles.aiCompareGrid}>
                  <div className={styles.aiComparePanel}>
                    <Text size={300} className={styles.aiCompareLabel}>
                      AI Generated (Primary)
                    </Text>
                    <Text size={200} className={styles.filesInfo}>
                      {`Q ${aiReviewPendingScore ?? "-"}${
                        aiReviewScoreDelta === null
                          ? ""
                          : aiReviewScoreDelta > 0
                            ? ` (+${aiReviewScoreDelta})`
                            : ` (${aiReviewScoreDelta})`
                      }`}
                    </Text>
                    <div className={styles.aiCompareImageFrame}>
                      <img
                        src={buildDraftDownloadUrl(
                          aiReviewRecord.pendingPath,
                          aiReviewRecord.updatedAt
                        )}
                        alt="AI generated"
                        className={styles.aiCompareImage}
                      />
                    </div>
                  </div>
                  <div className={styles.aiComparePanel}>
                    <Text size={300} className={styles.aiCompareLabel}>
                      Original (Secondary)
                    </Text>
                    <Text size={200} className={styles.filesInfo}>
                      {`Q ${aiReviewOriginalScore ?? "-"}`}
                    </Text>
                    <div className={styles.aiCompareImageFrame}>
                      <img
                        src={buildDraftDownloadUrl(
                          aiReviewRecord.originalPath,
                          entryByPath.get(aiReviewRecord.originalPath)?.modifiedAt ??
                            aiReviewRecord.updatedAt
                        )}
                        alt="Original"
                        className={styles.aiCompareImage}
                      />
                    </div>
                  </div>
                </div>
              ) : (
                <Text size={200}>No pending AI edit found.</Text>
              )}
              <DialogActions>
                <Button
                  appearance="outline"
                  onClick={() => setAiReviewOriginalPath(null)}
                  disabled={aiReviewSubmitting}
                >
                  {t("common.close")}
                </Button>
                <Button
                  appearance="outline"
                  onClick={() =>
                    aiReviewOriginalPath
                      ? resolveAiEdit(aiReviewOriginalPath, "keep_original")
                      : undefined
                  }
                  disabled={!aiReviewOriginalPath || aiReviewSubmitting}
                >
                  {aiReviewSubmitting ? "Saving..." : "Keep Original"}
                </Button>
                <Button
                  appearance="outline"
                  onClick={() =>
                    aiReviewOriginalPath
                      ? resolveAiEdit(aiReviewOriginalPath, "keep_both")
                      : undefined
                  }
                  disabled={!aiReviewOriginalPath || aiReviewSubmitting}
                >
                  {aiReviewSubmitting ? "Saving..." : "Keep Both"}
                </Button>
                <Button
                  appearance="primary"
                  onClick={() =>
                    aiReviewOriginalPath
                      ? resolveAiEdit(aiReviewOriginalPath, "replace_with_ai")
                      : undefined
                  }
                  disabled={!aiReviewOriginalPath || aiReviewSubmitting}
                >
                  {aiReviewSubmitting ? "Saving..." : "Replace with AI Image"}
                </Button>
              </DialogActions>
            </DialogBody>
          </DialogSurface>
        </Dialog>

        <Dialog
          open={addImagesDialogOpen}
          onOpenChange={(_, data) => {
            if (!data.open) {
              closeAddImagesDialog();
            }
          }}
        >
          <DialogSurface className={styles.addImagesDialogSurface}>
            <DialogBody className={styles.addImagesDialogBody}>
              <DialogTitle>Add Images</DialogTitle>
              <div className={styles.addImagesDialogSection}>
                <Text size={200} weight="semibold">
                  Image URL Input
                </Text>
                <Textarea
                  rows={3}
                  resize="vertical"
                  value={imageUrlInput}
                  onChange={(_, data) => setImageUrlInput(data.value)}
                  placeholder="Paste image URLs separated by commas"
                  className={styles.urlUploadInput}
                  disabled={addImagesSubmitting}
                />
              </div>
              <div
                className={mergeClasses(
                  styles.addImagesDialogDropZone,
                  addImagesDialogDragActive ? styles.addImagesDialogDropZoneActive : undefined
                )}
                onDragOver={(event) => {
                  event.preventDefault();
                  setAddImagesDialogDragActive(true);
                }}
                onDragLeave={() => setAddImagesDialogDragActive(false)}
                onDrop={(event) => {
                  event.preventDefault();
                  setAddImagesDialogDragActive(false);
                  queueAddImagesDialogFiles(Array.from(event.dataTransfer.files ?? []));
                }}
              >
                <Text size={200} className={styles.uploadDropHint}>
                  Drag and drop images or ZIP files, or choose from your computer.
                </Text>
                <div className={styles.uploadDropCenter}>
                  <svg
                    xmlns="http://www.w3.org/2000/svg"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    strokeWidth="2"
                    strokeLinecap="round"
                    strokeLinejoin="round"
                    className={styles.uploadDropIcon}
                    aria-hidden="true"
                  >
                    <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                    <path d="M14 3v4a1 1 0 0 0 1 1h4" />
                    <path d="M17 21h-10a2 2 0 0 1 -2 -2v-14a2 2 0 0 1 2 -2h7l5 5v11a2 2 0 0 1 -2 2" />
                    <path d="M12 11v6" />
                    <path d="M9.5 13.5l2.5 -2.5l2.5 2.5" />
                  </svg>
                </div>
                <div className={styles.uploadInputWrap}>
                  <input
                    ref={addImagesFileInputRef}
                    type="file"
                    multiple
                    disabled={addImagesSubmitting}
                    onChange={(event) => {
                      queueAddImagesDialogFiles(Array.from(event.target.files ?? []));
                      event.currentTarget.value = "";
                    }}
                  />
                </div>
                {addImagesDialogFiles.length > 0 ? (
                  <ul className={styles.addImagesDialogFileList}>
                    {addImagesDialogFiles.slice(0, 10).map((file, index) => (
                      <li
                        key={`${file.name}-${file.size}-${file.lastModified}-${index}`}
                        className={styles.addImagesDialogFileItem}
                        title={file.name}
                      >
                        {file.name}
                      </li>
                    ))}
                    {addImagesDialogFiles.length > 10 ? (
                      <li className={styles.addImagesDialogFileItem}>
                        +{addImagesDialogFiles.length - 10} more file(s)
                      </li>
                    ) : null}
                  </ul>
                ) : null}
              </div>
              <DialogActions className={styles.addImagesDialogActions}>
                <Button
                  appearance="outline"
                  onClick={closeAddImagesDialog}
                  disabled={addImagesSubmitting}
                >
                  {t("common.close")}
                </Button>
                <Button
                  appearance="primary"
                  onClick={() => void handleSubmitAddImagesDialog()}
                  disabled={!currentPath || addImagesSubmitting}
                >
                  {addImagesSubmitting ? "Uploading..." : "Upload"}
                </Button>
              </DialogActions>
            </DialogBody>
          </DialogSurface>
        </Dialog>

        <Dialog
          open={importEditsDialogOpen}
          onOpenChange={(_, data) => {
            if (!data.open) {
              closeImportEditsDialog();
            }
          }}
        >
          <DialogSurface className={styles.addImagesDialogSurface}>
            <DialogBody className={styles.addImagesDialogBody}>
              <DialogTitle>Import Edits</DialogTitle>
              <div className={styles.addImagesDialogSection}>
                <Field label="Upload Excel file">
                  <input
                    ref={importEditsExcelInputRef}
                    type="file"
                    accept=".xlsx"
                    className={styles.fileInput}
                    disabled={importEditsSubmitting}
                    onChange={(event) => {
                      setExcelFile(event.target.files?.[0] ?? null);
                      setExcelStatus("idle");
                    }}
                  />
                </Field>
              </div>
              <div className={styles.addImagesDialogSection}>
                <Field label="Upload image ZIP file (optional)">
                  <input
                    ref={importEditsZipInputRef}
                    type="file"
                    accept=".zip"
                    className={styles.fileInput}
                    disabled={importEditsSubmitting}
                    onChange={(event) => {
                      setZipFile(event.target.files?.[0] ?? null);
                      setZipStatus("idle");
                    }}
                  />
                </Field>
              </div>
              <DialogActions className={styles.addImagesDialogActions}>
                <Button
                  appearance="outline"
                  onClick={closeImportEditsDialog}
                  disabled={importEditsSubmitting}
                >
                  {t("common.close")}
                </Button>
                <Button
                  appearance="primary"
                  onClick={() => void handleSubmitImportEditsDialog()}
                  disabled={importEditsSubmitting || (!excelFile && !zipFile)}
                >
                  {importEditsSubmitting ? "Uploading..." : "Upload"}
                </Button>
              </DialogActions>
            </DialogBody>
          </DialogSurface>
        </Dialog>

        <Dialog
          open={reverseSearchOpen}
          onOpenChange={(_, data) => {
            if (!data.open) {
              closeReverseSearchDialog();
            }
          }}
        >
          <DialogSurface className={styles.reverseSearchSurface}>
            <DialogBody className={styles.reverseSearchBody}>
              <DialogTitle>
                {reverseSearchImageName
                  ? `Google Lens Search - ${reverseSearchImageName}`
                  : "Google Lens Search"}
              </DialogTitle>
              <div className={styles.reverseSearchHeaderRow}>
                <div className={styles.reverseSearchSourceBlock}>
                  <span className={styles.reverseSearchSourceFrame}>
                    {reverseSearchDisplayImageUrl ? (
                      <img
                        src={reverseSearchDisplayImageUrl}
                        alt={reverseSearchImageName || "Selected image"}
                        className={styles.reverseSearchSourceImage}
                      />
                    ) : null}
                  </span>
                  <div className={styles.reverseSearchSourceMeta}>
                    <Text size={300} weight="semibold" className={styles.reverseSearchSourceTitle}>
                      {reverseSearchDisplayProductTitle || "Selected product"}
                    </Text>
                    <Text
                      size={100}
                      className={styles.reverseSearchSmallText}
                      title={reverseSearchDisplayImageUrl || reverseSearchDisplayImagePath || reverseSearchImagePath || "No image selected"}
                    >
                      {reverseSearchDisplayImageUrl || reverseSearchDisplayImagePath || reverseSearchImagePath || "No image selected"}
                    </Text>
                    <Text size={100} className={styles.reverseSearchSmallText}>
                      {`${reverseSearchItems.length} result(s) loaded`}
                    </Text>
                  </div>
                </div>
                <div style={{ display: "inline-flex", gap: "8px", alignItems: "center" }}>
                  {reverseSearchSearchId ? (
                    <Text size={100} className={styles.reverseSearchSmallText}>
                      {`Search ID: ${reverseSearchSearchId}`}
                    </Text>
                  ) : null}
                </div>
              </div>
              {reverseSearchError ? (
                <Text size={200} style={{ color: tokens.colorStatusDangerForeground1 }}>
                  {reverseSearchError}
                </Text>
              ) : null}
              <div className={styles.reverseSearchContent}>
                <div
                  className={mergeClasses(
                    styles.reverseSearchTableWrap,
                    reverseSearchLoading ? styles.reverseSearchBlurred : undefined
                  )}
                >
                  {reverseSearchItems.length === 0 && !reverseSearchLoading ? (
                    <div style={{ padding: "14px" }}>
                      <Text size={200}>No search results found.</Text>
                    </div>
                  ) : (
                    <Table
                      size="small"
                      aria-label="Google Lens search results table"
                      className={styles.reverseSearchTable}
                    >
                      <TableHeader>
                        <TableRow>
                          <TableHeaderCell className={styles.reverseSearchImageColumn}>
                            Image
                          </TableHeaderCell>
                          <TableHeaderCell className={styles.reverseSearchTitleColumn}>
                            Title
                          </TableHeaderCell>
                          <TableHeaderCell className={styles.reverseSearchLinkColumn}>
                            Link
                          </TableHeaderCell>
                          <TableHeaderCell className={styles.reverseSearchActionColumn}>
                            Action
                          </TableHeaderCell>
                        </TableRow>
                      </TableHeader>
                      <TableBody>
                        {reverseSearchItems.map((item, index) => {
                          const rawPreviewUrl =
                            item.thumbnail || item.image || item.originalImage || "";
                          const useFallbackPreview = Boolean(
                            rawPreviewUrl && reverseSearchBrokenPreviewUrls[rawPreviewUrl]
                          );
                          const previewUrl =
                            !rawPreviewUrl || useFallbackPreview
                              ? reverseSearchDisplayImageUrl
                              : rawPreviewUrl;
                          const linkHost =
                            item.sourceDomain ||
                            item.domain ||
                            (() => {
                              try {
                                return item.link ? new URL(item.link).hostname : "";
                              } catch {
                                return "";
                              }
                            })();
                          const amazonAsin = item.isAmazon ? extractAmazonAsin(item) : "";
                          const rowKey = `${item.link || "nolink"}-${item.rank}-${index}`;
                          return (
                            <TableRow key={rowKey}>
                              <TableCell
                                className={mergeClasses(
                                  styles.reverseSearchImageColumn,
                                  styles.reverseSearchImageCell
                                )}
                              >
                                <span className={styles.reverseSearchImageCellContent}>
                                  <span className={styles.reverseSearchThumbWrap}>
                                    {previewUrl ? (
                                      <img
                                        src={previewUrl}
                                        alt={item.title || `Result ${index + 1}`}
                                        className={styles.reverseSearchThumb}
                                        loading="lazy"
                                        referrerPolicy="no-referrer"
                                        onError={() => {
                                          if (!rawPreviewUrl) return;
                                          if (reverseSearchBrokenPreviewUrls[rawPreviewUrl]) return;
                                          setReverseSearchBrokenPreviewUrls((prev) => ({
                                            ...prev,
                                            [rawPreviewUrl]: true,
                                          }));
                                        }}
                                      />
                                    ) : null}
                                  </span>
                                </span>
                              </TableCell>
                              <TableCell
                                className={mergeClasses(
                                  styles.reverseSearchTitleColumn,
                                  styles.reverseSearchRowCellMiddle
                                )}
                              >
                                <div
                                  style={{
                                    display: "flex",
                                    flexDirection: "column",
                                    gap: "4px",
                                    minWidth: 0,
                                  }}
                                >
                                  <Text
                                    size={200}
                                    className={styles.reverseSearchTitleClamp}
                                    title={item.title || "-"}
                                  >
                                    {item.title || "-"}
                                  </Text>
                                </div>
                              </TableCell>
                              <TableCell
                                className={mergeClasses(
                                  styles.reverseSearchLinkColumn,
                                  styles.reverseSearchRowCellMiddle
                                )}
                              >
                                <div
                                  style={{
                                    display: "flex",
                                    flexDirection: "column",
                                    alignItems: "flex-start",
                                  }}
                                >
                                  {item.link ? (
                                    <Button
                                      size="small"
                                      appearance="outline"
                                      className={styles.reverseSearchOpenLinkButton}
                                      onClick={() => {
                                        window.open(item.link || "", "_blank", "noopener,noreferrer");
                                      }}
                                    >
                                      Open Link
                                    </Button>
                                  ) : (
                                    <Text size={200}>-</Text>
                                  )}
                                  {item.link ? (
                                    <Text className={styles.reverseSearchLinkMeta}>
                                      {linkHost || "-"}
                                    </Text>
                                  ) : null}
                                </div>
                              </TableCell>
                              <TableCell
                                className={mergeClasses(
                                  styles.reverseSearchActionColumn,
                                  styles.reverseSearchRowCellMiddle
                                )}
                              >
                                {item.isAmazon && item.link ? (
                                  <div
                                    style={{
                                      display: "flex",
                                      flexDirection: "column",
                                      alignItems: "flex-end",
                                    }}
                                  >
                                    <Button
                                      size="small"
                                      appearance="outline"
                                      className={styles.reverseSearchRowActionButton}
                                      disabled={
                                        reverseSearchLoading ||
                                        reverseSearchFetchingAmazon ||
                                        reverseSearchAddingDownloaded
                                      }
                                      onClick={() =>
                                        void handleFetchAmazonDataFromReverseSearch([
                                          String(item.link || ""),
                                        ])
                                      }
                                    >
                                      {reverseSearchFetchingAmazon
                                        ? "Fetching..."
                                        : "Fetch Amazon Data"}
                                    </Button>
                                    <Text className={styles.reverseSearchLinkMeta}>
                                      {amazonAsin ? `ASIN ${amazonAsin}` : "ASIN -"}
                                    </Text>
                                  </div>
                                ) : (
                                  <Text size={100} className={styles.reverseSearchSmallText}>
                                    -
                                  </Text>
                                )}
                              </TableCell>
                            </TableRow>
                          );
                        })}
                      </TableBody>
                    </Table>
                  )}
                </div>
                {reverseSearchLoading ? (
                  <div className={styles.reverseSearchLoadingOverlay}>
                    <div className={styles.reverseSearchBusyContent}>
                      <Spinner size="medium" />
                      <Text size={200}>Searching Google Lens...</Text>
                    </div>
                  </div>
                ) : null}
              </div>
              <DialogActions className={styles.reverseSearchActions}>
                <Button
                  appearance="outline"
                  onClick={handleOpenDownloadedFolderFromReverseSearch}
                  disabled={
                    reverseSearchLoading ||
                    reverseSearchAddingDownloaded ||
                    reverseSearchDownloadedCount === 0
                  }
                >
                  Open Downloaded
                </Button>
                <Button
                  appearance="outline"
                  onClick={handleDownloadReverseSearchJson}
                  disabled={
                    reverseSearchLoading ||
                    (!reverseSearchDebugPayload &&
                      !reverseSearchInputPayload &&
                      reverseSearchItems.length === 0)
                  }
                >
                  Download JSON
                </Button>
                <Button
                  appearance="primary"
                  onClick={closeReverseSearchDialog}
                  disabled={
                    reverseSearchLoading ||
                    reverseSearchFetchingAmazon ||
                    reverseSearchAddingDownloaded
                  }
                >
                  {t("common.close")}
                </Button>
              </DialogActions>
            </DialogBody>
          </DialogSurface>
        </Dialog>

        <Dialog
          open={Boolean(previewPath)}
          onOpenChange={(_, data) => {
            if (!data.open) {
              setPreviewPath(null);
            }
          }}
        >
          <DialogSurface className={styles.previewDialog}>
            <DialogBody className={styles.previewDialogBody}>
              <DialogTitle>{`Image - ${previewFileName || ""}`}</DialogTitle>
              <div
                className={styles.previewImageFrame}
                onContextMenu={(event) => {
                  event.preventDefault();
                  if (!previewEntry) return;
                  syncSelectionForContextMenuEntry(previewEntry);
                  setContextMenuSubmenu(null);
                  setContextMenuNestedSubmenu(null);
                  setContextMenu({
                    entry: previewEntry,
                    image: true,
                    x: event.clientX,
                    y: event.clientY,
                  });
                }}
              >
                {previewPendingAi && previewPath ? (
                  <button
                    type="button"
                    className={styles.aiPendingBadge}
                    title={`Review AI edit for ${previewFileName || "image"}`}
                    aria-label={`Review AI edit for ${previewFileName || "image"}`}
                    onClick={(event) => {
                      event.preventDefault();
                      event.stopPropagation();
                      const pathValue = previewPath;
                      setPreviewPath(null);
                      setAiReviewOriginalPath(pathValue);
                    }}
                    disabled={previewBusy}
                  >
                    <svg
                      xmlns="http://www.w3.org/2000/svg"
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      strokeWidth="2"
                      strokeLinecap="round"
                      strokeLinejoin="round"
                      width="16"
                      height="16"
                      aria-hidden="true"
                    >
                      <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                      <path d="M15 8h.01" />
                      <path d="M10 21h-4a3 3 0 0 1 -3 -3v-12a3 3 0 0 1 3 -3h12a3 3 0 0 1 3 3v5" />
                      <path d="M3 16l5 -5c.928 -.893 2.072 -.893 3 0l1 1" />
                      <path d="M14 21v-4a2 2 0 1 1 4 0v4" />
                      <path d="M14 19h4" />
                      <path d="M21 15v6" />
                    </svg>
                  </button>
                ) : null}

                {previewNav.prevPath ? (
                  <button
                    type="button"
                    className={mergeClasses(
                      styles.previewNavButton,
                      styles.previewNavButtonLeft
                    )}
                    onClick={() => handlePreviewNavigate("prev")}
                    aria-label="Previous image"
                    title="Previous image"
                    disabled={previewBusy || previewDeletePending}
                  >
                    <svg
                      xmlns="http://www.w3.org/2000/svg"
                      viewBox="0 0 24 24"
                      fill="currentColor"
                      className={styles.previewNavIcon}
                      aria-hidden="true"
                    >
                      <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                      <path d="M13.883 5.007l.058 -.005h.118l.058 .005l.06 .009l.052 .01l.108 .032l.067 .027l.132 .07l.09 .065l.081 .073l.083 .094l.054 .077l.054 .096l.017 .036l.027 .067l.032 .108l.01 .053l.01 .06l.004 .057l.002 .059v12c0 .852 -.986 1.297 -1.623 .783l-.084 -.076l-6 -6a1 1 0 0 1 -.083 -1.32l.083 -.094l6 -6l.094 -.083l.077 -.054l.096 -.054l.036 -.017l.067 -.027l.108 -.032l.053 -.01l.06 -.01z" />
                    </svg>
                  </button>
                ) : null}

                {previewNav.nextPath ? (
                  <button
                    type="button"
                    className={mergeClasses(
                      styles.previewNavButton,
                      styles.previewNavButtonRight
                    )}
                    onClick={() => handlePreviewNavigate("next")}
                    aria-label="Next image"
                    title="Next image"
                    disabled={previewBusy || previewDeletePending}
                  >
                    <svg
                      xmlns="http://www.w3.org/2000/svg"
                      viewBox="0 0 24 24"
                      fill="currentColor"
                      className={styles.previewNavIcon}
                      aria-hidden="true"
                    >
                      <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                      <path d="M9 6c0 -.852 .986 -1.297 1.623 -.783l.084 .076l6 6a1 1 0 0 1 .083 1.32l-.083 .094l-6 6l-.094 .083l-.077 .054l-.096 .054l-.036 .017l-.067 .027l-.108 .032l-.053 .01l-.06 .01l-.057 .004l-.059 .002l-.059 -.002l-.058 -.005l-.06 -.009l-.052 -.01l-.108 -.032l-.067 -.027l-.132 -.07l-.09 -.065l-.081 -.073l-.083 -.094l-.054 -.077l-.054 -.096l-.017 -.036l-.027 -.067l-.032 -.108l-.01 -.053l-.01 -.06l-.004 -.057l-.002 -12.059z" />
                    </svg>
                  </button>
                ) : null}

                {previewDisplayPath ? (
                  <img
                    src={buildDraftDownloadUrl(
                      previewDisplayPath,
                      previewDisplayModifiedAt
                    )}
                    alt={previewDisplayPath}
                    data-preview-path={previewPath}
                    className={mergeClasses(
                      styles.previewImageLarge,
                      previewBusy ? styles.mediaImageBusy : undefined
                    )}
                    onLoad={(event) => {
                      const img = event.currentTarget;
                      const pathValue =
                        event.currentTarget.dataset.previewPath || previewPath;
                      if (!pathValue) return;
                      setImageDimensions((prev) => ({
                        ...prev,
                        [pathValue]: {
                          width: img.naturalWidth,
                          height: img.naturalHeight,
                        },
                      }));
                      setReloadingImagePaths((prev) => {
                        if (!prev.has(pathValue)) return prev;
                        const next = new Set(prev);
                        next.delete(pathValue);
                        return next;
                      });
                    }}
                  />
                ) : null}

                {previewBusy ? (
                  <div className={styles.mediaBusyOverlay}>
                    <div className={styles.mediaBusyContent}>
                      <Spinner size="small" />
                      <span>Updating</span>
                    </div>
                  </div>
                ) : null}
              </div>
              <div className={styles.previewMetaRow}>
                <Text size={100} className={styles.previewMetaText}>
                  {previewDimensions ? (
                    <>
                      <span
                        className={mergeClasses(
                          previewDimensions.width < 800 || previewDimensions.height < 800
                            ? styles.mediaMetaDimLow
                            : undefined
                        )}
                      >
                        {`${previewDimensions.width} x ${previewDimensions.height}`}
                      </span>
                      {` | ${previewFileSizeText}`}
                    </>
                  ) : (
                    `- | ${previewFileSizeText}`
                  )}
                </Text>
                <DialogActions className={styles.previewActions}>
                  <Button
                    appearance="outline"
                    onClick={() => void handlePreviewDelete()}
                    disabled={!previewPath || previewBusy || previewDeletePending}
                  >
                    {previewDeletePending ? "Deleting..." : "Delete"}
                  </Button>
                  <Button appearance="primary" onClick={() => setPreviewPath(null)}>
                    {t("common.close")}
                  </Button>
                </DialogActions>
              </div>
            </DialogBody>
          </DialogSurface>
        </Dialog>

        <Dialog
          open={Boolean(fileViewerPath)}
          onOpenChange={(_, data) => {
            if (!data.open) {
              setFileViewerPath(null);
              setFileViewerContent("");
              setFileViewerError(null);
            }
          }}
        >
          <DialogSurface className={styles.textViewerSurface}>
            <DialogBody className={styles.textViewerBody}>
              <DialogTitle>{fileViewerPath ?? "File viewer"}</DialogTitle>
              {fileViewerError ? (
                <Text size={200} style={{ color: tokens.colorStatusDangerForeground1 }}>
                  {fileViewerError}
                </Text>
              ) : null}
              {fileViewerLoading ? (
                <Spinner size="small" />
              ) : (
                <Textarea
                  value={fileViewerContent}
                  onChange={(_, data) => setFileViewerContent(data.value)}
                  resize="vertical"
                  rows={24}
                  className={styles.textViewerArea}
                />
              )}
              <DialogActions className={styles.textViewerActions}>
                <Button
                  appearance="outline"
                  onClick={() => {
                    setFileViewerPath(null);
                    setFileViewerContent("");
                    setFileViewerError(null);
                  }}
                  disabled={fileViewerSaving}
                >
                  {t("common.close")}
                </Button>
                <Button
                  appearance="primary"
                  onClick={handleSaveFileViewer}
                  disabled={!fileViewerPath || fileViewerLoading || fileViewerSaving}
                >
                  {fileViewerSaving ? "Saving..." : t("common.save")}
                </Button>
              </DialogActions>
            </DialogBody>
          </DialogSurface>
        </Dialog>

        <Dialog
          open={supplierEditorOpen}
          onOpenChange={(_, data) => {
            if (!data.open) {
              closeSupplierEditor();
            }
          }}
        >
          <DialogSurface className={styles.supplierEditorSurface}>
            <DialogBody className={styles.supplierEditorBody}>
              <DialogTitle>
                {`Change Supplier${supplierEditorSpu ? ` - ${supplierEditorSpu}` : ""}`}
              </DialogTitle>
              {supplierEditorError ? (
                <Text size={200} style={{ color: tokens.colorStatusDangerForeground1 }}>
                  {supplierEditorError}
                </Text>
              ) : null}
              <Field label="Supplier URL">
                <Input
                  value={supplierEditorValue}
                  onChange={(_, data) => setSupplierEditorValue(data.value)}
                  placeholder="https://..."
                />
              </Field>
              <DialogActions>
                <Button
                  appearance="outline"
                  onClick={closeSupplierEditor}
                  disabled={supplierEditorSaving}
                >
                  {t("common.close")}
                </Button>
                <Button
                  appearance="primary"
                  onClick={() => void handleSaveSupplierEditor()}
                  disabled={supplierEditorSaving}
                >
                  {supplierEditorSaving ? "Saving..." : t("common.save")}
                </Button>
              </DialogActions>
            </DialogBody>
          </DialogSurface>
        </Dialog>

        <Dialog
          open={Boolean(spuJsonViewer)}
          onOpenChange={(_, data) => {
            if (!data.open) {
              setSpuJsonViewer(null);
            }
          }}
        >
          <DialogSurface className={styles.jsonViewerSurface}>
            <DialogBody className={styles.jsonViewerBody}>
              <DialogTitle>
                {spuJsonViewer?.spu
                  ? `${spuJsonViewer.spu} - Original Input JSON`
                  : "Original Input JSON"}
              </DialogTitle>
              <Text size={200} className={styles.jsonViewerSource}>
                {spuJsonViewer?.sourceLabel ?? ""}
              </Text>
              {spuJsonViewer?.loading ? (
                <Spinner
                  size="tiny"
                  style={{ color: tokens.colorNeutralForeground3 }}
                  labelPosition="after"
                  label="Loading..."
                />
              ) : null}
              <Textarea
                value={spuJsonViewer?.content ?? ""}
                readOnly
                resize="vertical"
                rows={24}
                className={styles.jsonViewerArea}
              />
              <DialogActions className={styles.textViewerActions}>
                <Button appearance="primary" onClick={() => setSpuJsonViewer(null)}>
                  {t("common.close")}
                </Button>
              </DialogActions>
            </DialogBody>
          </DialogSurface>
        </Dialog>
      </Card>

    </div>
  );
}
